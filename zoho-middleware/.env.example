# Zoho OAuth 2.0 credentials
# Register your app at your region's API console:
#   US: https://api-console.zoho.com
#   CA: https://api-console.zohocloud.ca
#   EU: https://api-console.zoho.eu
ZOHO_CLIENT_ID=your_client_id_here
ZOHO_CLIENT_SECRET=your_client_secret_here
ZOHO_REDIRECT_URI=http://localhost:3001/auth/zoho/callback
ZOHO_ORG_ID=your_organization_id_here

# Zoho data center — must match where your account is hosted
# Options: .com (US), .ca (Canada), .eu (EU), .in (India), .com.au (AU), .jp (Japan)
ZOHO_DOMAIN=.com

# Zoho Bookings — service & staff IDs (from Bookings admin UI)
ZOHO_BOOKINGS_SERVICE_ID=your_service_id_here
ZOHO_BOOKINGS_STAFF_ID=your_staff_id_here

# Zoho Books — custom field API names for Sales Order appointment fields
ZOHO_CF_APPOINTMENT_ID=cf_appointment_id
ZOHO_CF_TIMESLOT=cf_appointment_timeslot
ZOHO_CF_STATUS=cf_reservation_status

# Redis (optional — server works without it, caching is just disabled)
REDIS_URL=redis://localhost:6379

# Server
PORT=3001

# Frontend origin (for CORS)
FRONTEND_URL=http://localhost:3000
