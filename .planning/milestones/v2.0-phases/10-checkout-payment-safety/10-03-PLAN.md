---
phase: 10-checkout-payment-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - js/main.js
  - js/main.min.js
  - css/styles.min.css
autonomous: true
requirements:
  - PAY-SAFE-01
  - PAY-SAFE-02
  - PAY-SAFE-03

must_haves:
  truths:
    - "Full test suite passes for both frontend and middleware"
    - "Lint passes with no errors"
    - "Build produces updated main.js and main.min.js with payment safety code"
    - "All three PAY-SAFE requirements are verifiable in the built artifacts"
  artifacts:
    - path: "js/main.js"
      provides: "Concatenated frontend JS with payment safety"
      contains: "_paymentChargeInFlight"
    - path: "js/main.min.js"
      provides: "Minified frontend JS for deployment"
      min_lines: 1
  key_links:
    - from: "js/main.js"
      to: "js/modules/12-checkout.js"
      via: "build concatenation"
      pattern: "_paymentChargeInFlight"
---

<objective>
Run full test suites, lint, and build to verify all payment safety changes integrate correctly.

Purpose: Ensure Plans 01 and 02 integrate without regressions. The build step produces the deployable artifacts (main.js, main.min.js) that include the payment state machine.

Output: Passing tests, clean lint, updated build artifacts ready for staging deployment.
</objective>

<execution_context>
@.planning/phases/10-checkout-payment-safety/10-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@js/modules/12-checkout.js
@zoho-middleware/routes/checkout.js
@zoho-middleware/lib/mailer.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suites and lint, fix any issues</name>
  <files>js/modules/12-checkout.js, zoho-middleware/routes/checkout.js, zoho-middleware/lib/mailer.js</files>
  <read_first>
    - js/modules/12-checkout.js (verify _paymentChargeInFlight exists from Plan 01)
    - zoho-middleware/lib/mailer.js (verify sendCustomerConfirmation exists from Plan 02)
    - zoho-middleware/routes/checkout.js (verify fallback email wiring from Plan 02)
  </read_first>
  <action>
Run the following verification commands in sequence:

1. `cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm test` — all frontend tests must pass
2. `cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website/zoho-middleware" && npm test` — all middleware tests must pass
3. `cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm run lint` — zero lint errors

If any test fails:
- Read the failing test output to identify the root cause
- If the failure is in 12-checkout.js (e.g. module.exports shape changed), update the export block to include new test helpers while preserving all existing exports
- If the failure is in checkout.js middleware (e.g. mock shape doesn't include sendCustomerConfirmation), update the relevant mock in the existing test file
- Re-run the specific failing test to confirm fix

If lint fails:
- Fix lint issues in the source files (12-checkout.js or checkout.js)
- Common issues: missing semicolons, unused variables, undeclared variables
- Do NOT disable lint rules — fix the code
  </action>
  <verify>
    <automated>cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm test 2>&1 | grep -E "Tests:|Test Suites:" | tail -2 && cd zoho-middleware && npm test 2>&1 | grep -E "Tests:|Test Suites:" | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` output contains "Tests: ... passed" with 0 failed
    - `cd zoho-middleware && npm test` output contains "Tests: ... passed" with 0 failed
    - `npm run lint` exits with code 0 (no errors)
  </acceptance_criteria>
  <done>Both test suites pass with zero failures. Lint is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Run build to produce deployable artifacts</name>
  <files>js/main.js, js/main.min.js</files>
  <read_first>
    - js/modules/12-checkout.js (confirm payment safety code present before build)
  </read_first>
  <action>
Run `npm run build` in the project root to:
1. Concatenate all js/modules/*.js into js/main.js
2. Minify js/main.js into js/main.min.js via terser
3. Minify css/styles.css into css/styles.min.css via cleancss
4. Stamp cache-busting versions on admin.html references

After build completes, verify the payment safety code is present in the built artifact:
- `grep "_paymentChargeInFlight" js/main.js` should return matches
- `grep "idempotency_key" js/main.js` should return matches
- `grep "clearPaymentCooldown" js/main.js` should return matches
- `grep "generateIdempotencyKey" js/main.js` should return matches

If build fails, read the error output and fix. Common issues:
- Syntax error in 12-checkout.js (missing comma, bracket)
- terser failure on invalid JS
  </action>
  <verify>
    <automated>cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && grep -c "_paymentChargeInFlight" js/main.js && grep -c "idempotency_key" js/main.js && grep -c "clearPaymentCooldown" js/main.js && test -f js/main.min.js && echo "BUILD OK"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - js/main.js contains "_paymentChargeInFlight" (at least 5 occurrences)
    - js/main.js contains "idempotency_key" (at least 3 occurrences)
    - js/main.js contains "clearPaymentCooldown" (at least 4 occurrences)
    - js/main.min.js exists and is non-empty
    - css/styles.min.css exists
  </acceptance_criteria>
  <done>Build artifacts produced successfully. Payment safety code is present in deployable main.js/main.min.js.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline -> Deployed artifact | Ensures no code is lost during concatenation/minification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | build output | mitigate | Verify key payment safety strings survive minification via grep checks |
</threat_model>

<verification>
1. `npm test` — all frontend tests pass
2. `cd zoho-middleware && npm test` — all middleware tests pass
3. `npm run lint` — zero errors
4. `npm run build` — exits cleanly
5. `grep "_paymentChargeInFlight" js/main.js` — present in built artifact
6. `grep "idempotency_key" js/main.js` — present in built artifact
</verification>

<success_criteria>
- Zero test failures across both suites
- Zero lint errors
- Build produces main.js with all payment safety code
- Build produces main.min.js (minified, non-empty)
- Ready for `git push origin main` to deploy to staging
</success_criteria>

<output>
After completion, create `.planning/phases/10-checkout-payment-safety/10-03-SUMMARY.md`
</output>
