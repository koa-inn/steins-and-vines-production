---
phase: 19-hop-inventory-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hops.html
  - css/hops.css
  - sitemap.xml
  - index.html
  - products.html
  - products/ferment-in-store.html
  - products/ingredients-supplies.html
  - custom-labels.html
  - reservation.html
  - about.html
  - contact.html
autonomous: true
requirements: [D-03, D-04, D-07, D-08, D-09, D-11, D-14, D-16, D-17]

must_haves:
  truths:
    - "hops.html loads in browser with hero, catalog container, filter/sort controls, and standard header/footer"
    - "Products dropdown nav on all 9 public pages (including hops.html) contains a Hops link"
    - "hops.html appears in sitemap.xml"
    - "hops.html has correct SEO meta tags, og tags, canonical, and LocalBusiness JSON-LD"
    - "css/hops.css contains radar chart, flavor tag, size toggle, and hop card styles"
  artifacts:
    - path: "hops.html"
      provides: "Hop catalog public page with all HTML structure"
      contains: "data-page=\"hops\""
    - path: "css/hops.css"
      provides: "Hop-specific styles: radar chart, flavor tags, size toggle, detail panel"
      contains: ".hop-radar"
    - path: "sitemap.xml"
      provides: "Updated sitemap including hops page"
      contains: "steinsandvines.ca/hops"
  key_links:
    - from: "hops.html"
      to: "css/hops.css"
      via: "stylesheet link tag"
      pattern: "hops\\.min\\.css"
    - from: "hops.html"
      to: "js/modules/15-hops.js"
      via: "script tag"
      pattern: "15-hops\\.min\\.js"
    - from: "index.html"
      to: "hops.html"
      via: "nav dropdown link"
      pattern: "hops\\.html"
---

<objective>
Create the hops.html page structure, all CSS, nav updates across all public pages, sitemap entry, and SEO meta. This plan delivers the complete static foundation that Plan 02's JS module will render into.

Purpose: Establishes the HTML shell, element IDs, CSS classes, and navigation links so that Plan 02's 15-hops.js has correct DOM targets and styling from the start. Following the same pattern used for custom-labels.html in Phase 17/18.

Output: hops.html page, css/hops.css stylesheet, updated nav on 9 pages, updated sitemap.xml
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-hop-inventory-catalog/19-CONTEXT.md
@.planning/phases/19-hop-inventory-catalog/19-RESEARCH.md
@.planning/phases/19-hop-inventory-catalog/19-PATTERNS.md
@.planning/phases/19-hop-inventory-catalog/19-UI-SPEC.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From custom-labels.html (analog for standalone page pattern):
- GTM snippet in head: (function(w,d,s,l,i){...})(window,document,'script','dataLayer','GTM-NHRCGLC5');
- CSP meta tag with script-src, style-src, img-src, connect-src, font-src, frame-src
- Script loading: js/vendor/sentry.min.js, js/sentry-init.js, js/sheets-config.js (defer), js/main.min.js?v=XXXXX (defer), js/modules/14-labels.min.js?v=XXXXX (defer)
- body data-page attribute: data-page="labels" (hops page uses data-page="hops")

From css/styles.css (reusable classes - DO NOT redeclare):
- .product-grid: CSS Grid, grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)), gap: 2rem
- .product-card: white bg, border-top: 3px solid var(--color-burgundy), border-radius
- .notes-wrap, .notes-toggle, .notes-body: accordion animation (max-height: 0 -> 600px on .open)
- .catalog-filter-row, .catalog-filter-btn: filter button row
- .catalog-no-results: empty state message
- CSS variables: --color-green: #4a6f4b, --color-burgundy: #370e13, --color-cream: #e5dec1, --color-text: #2c2c2c, --color-muted: #5f5f5f

From index.html nav (lines 136-146):
```html
<li class="nav-dropdown">
  <a href="products/ferment-in-store.html">Products</a>
  <ul class="nav-dropdown-menu">
    <li><a href="products/ferment-in-store.html">Ferment in Store</a></li>
    <li><a href="products/ingredients-supplies.html">Ingredients &amp; Supplies</a></li>
    <li><a href="custom-labels.html">Custom Labels</a></li>
  </ul>
</li>
```

From products/ferment-in-store.html nav (lines 92-98) — subdirectory pages use relative paths:
```html
<ul class="nav-dropdown-menu">
  <li><a href="ferment-in-store.html">Ferment in Store</a></li>
  <li><a href="ingredients-supplies.html">Ingredients &amp; Supplies</a></li>
  <li><a href="../custom-labels.html">Custom Labels</a></li>
</ul>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hops.html page and css/hops.css stylesheet</name>
  <files>hops.html, css/hops.css</files>
  <read_first>
    - custom-labels.html (analog: standalone page structure, head/meta/nav/script pattern, CSP, GTM, JSON-LD)
    - css/labels.css (analog: page-specific stylesheet structure)
    - css/styles.css (lines 117-138 for CSS variable definitions, lines 1505-1545 for .product-grid/.product-card, lines 4246-4293 for .notes-* accordion classes)
    - .planning/phases/19-hop-inventory-catalog/19-UI-SPEC.md (visual contract)
    - .planning/phases/19-hop-inventory-catalog/19-PATTERNS.md (exact HTML and CSS patterns)
  </read_first>
  <action>
**Create `hops.html`** following the custom-labels.html pattern exactly:

1. **Head section** — copy custom-labels.html head structure verbatim, replacing:
   - `<title>`: "Hop Pellets -- Brewing Hops in Squamish | Steins &amp; Vines"
   - `<meta name="description">`: "Browse our selection of brewing hop pellets in Squamish, BC. View sensory profiles, alpha acid percentages, and order online for pickup. Citrus, tropical, floral, and noble hop varieties available at Steins &amp; Vines."
   - `<meta property="og:title">`: "Hop Pellets | Steins &amp; Vines"
   - `<meta property="og:description">`: "Brewing hop pellets with sensory radar charts. Browse varieties, compare flavour profiles, and add to your ingredients cart."
   - `<meta property="og:url">`: "https://steinsandvines.ca/hops"
   - `<link rel="canonical">`: "https://steinsandvines.ca/hops"
   - Stylesheet link: `<link rel="stylesheet" href="css/hops.min.css?v=XXXXX">` (after styles.min.css)
   - CSP: keep same policy as custom-labels.html (no new sources needed)
   - JSON-LD LocalBusiness: copy from custom-labels.html verbatim (same business entity)
   - GTM container snippet: same GTM-NHRCGLC5 head + noscript body pattern

2. **Body attribute**: `<body data-page="hops">`

3. **Header**: exact same header block as custom-labels.html, with Hops nav link added (see Task 2 pattern) and `class="active"` on the Hops link in the dropdown

4. **Hero section** (per D-16, UI-SPEC):
```html
<section class="hops-hero">
  <div class="container">
    <h1>Hops</h1>
    <p>Browse our selection of brewing hops -- from citrusy American varieties to classic European nobles.</p>
  </div>
</section>
```

5. **Intro section**:
```html
<section class="hops-intro">
  <div class="container">
    <p>Explore our hop pellet catalog below. Each variety includes a sensory profile chart showing flavour characteristics, alpha acid percentage, and origin information. Use the filters to find hops by flavour profile, or search by name.</p>
  </div>
</section>
```

6. **Filter and catalog controls** — HTML elements that 15-hops.js will target:
```html
<section class="hops-catalog-section">
  <div class="container">
    <div class="catalog-controls">
      <div class="catalog-search-wrap">
        <input type="text" id="hops-search" class="catalog-search" placeholder="Search hops..." aria-label="Search hops">
      </div>
      <div class="catalog-sort-wrap">
        <select id="hops-sort" class="catalog-sort" aria-label="Sort hops">
          <option value="name-asc">Name (A-Z)</option>
          <option value="name-desc">Name (Z-A)</option>
          <option value="alpha-desc">Alpha Acid (High-Low)</option>
          <option value="price-asc">Price (Low-High)</option>
          <option value="price-desc">Price (High-Low)</option>
        </select>
      </div>
    </div>
    <div id="hops-filter-row" class="catalog-filter-row">
      <!-- JS populates flavor profile filter buttons -->
    </div>
    <div id="hops-catalog" class="product-grid">
      <!-- JS populates hop cards -->
    </div>
  </div>
</section>
```

7. **Footer**: copy footer block from custom-labels.html exactly (same hours, contact, links)

8. **Script tags** (before closing body, per custom-labels.html pattern):
```html
<script src="js/vendor/sentry.min.js"></script>
<script src="js/sentry-init.js"></script>
<script src="js/sheets-config.js" defer></script>
<script src="js/main.min.js?v=XXXXX" defer></script>
<script src="js/modules/15-hops.min.js?v=XXXXX" defer></script>
```

**Create `css/hops.css`** with all hop-specific styles per UI-SPEC and PATTERNS.md:

```css
/* Hops page -- hops.html */

/* Hero */
.hops-hero {
  background-color: var(--color-green);
  color: var(--color-cream);
  text-align: center;
  padding: 5rem 0;
}
.hops-hero h1 {
  font-family: var(--font-display);
  font-weight: 700;
  color: var(--color-cream);
  margin-bottom: 0.75rem;
}
.hops-hero p {
  font-family: var(--font-body);
  font-size: 1.125rem;
  max-width: 40ch;
  margin: 0 auto;
}

/* Intro */
.hops-intro {
  padding: 2rem 0 0;
}
.hops-intro p {
  max-width: 65ch;
  margin: 0 auto;
  text-align: center;
  color: var(--color-muted);
}

/* Catalog section */
.hops-catalog-section {
  padding: 2rem 0 4rem;
}

/* Hop card -- extends .product-card (do not redeclare .product-card rules) */
.hop-card {
  border-top: 3px solid var(--color-green);
  text-align: center;
}
.hop-card h4 {
  font-family: var(--font-display);
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--color-burgundy);
  margin: 0.5rem 0 0.25rem;
}

/* Alpha acid spec */
.hop-alpha {
  font-size: 0.875rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-muted);
  margin: 0.25rem 0;
}

/* Flavor tags */
.hop-flavor-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 0.3rem;
  justify-content: center;
  margin: 0.4rem 0;
}
.hop-flavor-tag {
  font-size: 0.875rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding: 4px 8px;
  background: rgba(74, 111, 75, 0.1);
  border: 1px solid var(--color-green);
  color: var(--color-green);
  border-radius: 999px;
  font-family: var(--font-body);
}

/* Accordion override -- hop panel needs taller max-height than default 600px */
.hop-notes-body {
  max-height: 900px;
}

/* Expanded detail panel */
.hop-detail {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 16px;
  width: 100%;
  padding: 1rem 0;
}

/* Radar chart SVG */
.hop-radar-wrap {
  width: 200px;
  height: 200px;
}
.hop-radar {
  width: 100%;
  height: 100%;
}
.radar-bg {
  fill: none;
  stroke: var(--color-green);
  stroke-width: 1;
  opacity: 0.2;
}
.radar-axis {
  stroke: rgba(0, 0, 0, 0.15);
  stroke-width: 1;
}
.radar-fill {
  fill: rgba(74, 111, 75, 0.25);
  stroke: var(--color-green);
  stroke-width: 2;
}
.radar-label {
  font-size: 14px;
  fill: #5f5f5f;
  font-family: var(--font-body);
}
.radar-web {
  fill: none;
  stroke: rgba(0, 0, 0, 0.1);
  stroke-width: 1;
}

/* Specs */
.hop-specs {
  text-align: center;
  font-size: 0.875rem;
  color: var(--color-muted);
}
.hop-specs strong {
  font-weight: 700;
}

/* Origin */
.hop-origin {
  font-size: 0.875rem;
  color: var(--color-muted);
}
.hop-origin strong {
  font-weight: 700;
}

/* Notes/history text */
.hop-notes {
  font-size: 0.875rem;
  color: var(--color-text);
  line-height: 1.5;
  max-width: 52ch;
  text-align: left;
}

/* Size toggle */
.hop-size-toggle-group {
  display: flex;
  gap: 8px;
  justify-content: center;
  margin: 0.75rem 0;
}
.hop-size-btn {
  padding: 8px 16px;
  min-height: 44px;
  border: 2px solid var(--color-brown, #77462d);
  border-radius: 4px;
  background: transparent;
  color: var(--color-brown, #77462d);
  cursor: pointer;
  font-family: var(--font-body);
  font-size: 0.875rem;
  font-weight: 700;
}
.hop-size-btn:hover {
  border-color: var(--color-green);
  color: var(--color-green);
}
.hop-size-btn.active {
  border-color: var(--color-green);
  color: var(--color-green);
  background: rgba(74, 111, 75, 0.08);
}
.hop-size-btn:focus-visible {
  outline: 2px solid var(--color-burgundy);
  outline-offset: 2px;
}

/* Price display in expanded panel */
.hop-price {
  font-size: 1.125rem;
  font-weight: 700;
  color: var(--color-burgundy);
  margin: 0.25rem 0;
}

/* No-data radar placeholder */
.hop-radar-placeholder {
  font-size: 0.875rem;
  color: var(--color-muted);
  font-style: italic;
  text-align: center;
  padding: 1rem 0;
}

/* Responsive */
@media (max-width: 768px) {
  .hops-hero {
    padding: 3rem 0;
  }
  .hop-radar-wrap {
    width: 160px;
    height: 160px;
  }
}

@media (max-width: 480px) {
  .hop-radar-wrap {
    width: 140px;
    height: 140px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .hop-notes-body {
    transition: none;
  }
}
```
  </action>
  <verify>
    <automated>grep -c 'data-page="hops"' hops.html | grep -q '^1$' && grep -c 'hops-catalog' hops.html | grep -q '^[1-9]' && grep -c '.hop-radar' css/hops.css | grep -q '^[1-9]' && grep -c 'hops-search' hops.html | grep -q '^[1-9]' && grep -c '15-hops.min.js' hops.html | grep -q '^[1-9]' && grep -c 'hops.min.css' hops.html | grep -q '^[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - hops.html contains `data-page="hops"` on the body tag
    - hops.html contains `id="hops-catalog"` container div
    - hops.html contains `id="hops-search"` input element
    - hops.html contains `id="hops-sort"` select element
    - hops.html contains `id="hops-filter-row"` div
    - hops.html contains script tag referencing `15-hops.min.js`
    - hops.html contains stylesheet link referencing `hops.min.css`
    - hops.html contains stylesheet link referencing `styles.min.css`
    - hops.html contains GTM-NHRCGLC5 snippet
    - hops.html contains LocalBusiness JSON-LD script
    - hops.html contains `<link rel="canonical" href="https://steinsandvines.ca/hops">`
    - hops.html contains `<meta property="og:url" content="https://steinsandvines.ca/hops">`
    - hops.html nav dropdown has 4 items: Ferment in Store, Ingredients &amp; Supplies, Hops (active), Custom Labels
    - css/hops.css contains `.hop-radar` class
    - css/hops.css contains `.hop-flavor-tag` class
    - css/hops.css contains `.hop-size-btn` class
    - css/hops.css contains `.hop-notes-body` with `max-height: 900px`
    - css/hops.css contains `.radar-fill` class
    - css/hops.css contains `.hop-detail` class
    - css/hops.css contains `@media (prefers-reduced-motion: reduce)` block
  </acceptance_criteria>
  <done>hops.html is a complete standalone page with correct head/meta/SEO/nav/hero/catalog structure targeting all element IDs that 15-hops.js will use; css/hops.css contains all hop-specific styles including radar chart, flavor tags, size toggle, expanded panel, and responsive breakpoints</done>
</task>

<task type="auto">
  <name>Task 2: Update nav dropdown on all public pages and add hops.html to sitemap</name>
  <files>index.html, products.html, products/ferment-in-store.html, products/ingredients-supplies.html, custom-labels.html, reservation.html, about.html, contact.html, sitemap.xml</files>
  <read_first>
    - index.html (nav dropdown at lines 136-146, verify current nav structure)
    - products/ferment-in-store.html (nav at lines 89-103, uses relative paths with ../)
    - products/ingredients-supplies.html (nav section)
    - custom-labels.html (nav at lines 97-112)
    - products.html (nav section)
    - reservation.html (nav section)
    - about.html (nav section)
    - contact.html (nav section)
    - sitemap.xml
  </read_first>
  <action>
**Update nav dropdown on all 8 existing public pages** to add a "Hops" link between "Ingredients &amp; Supplies" and "Custom Labels".

For **root-level pages** (index.html, products.html, custom-labels.html, reservation.html, about.html, contact.html), insert this line immediately after the "Ingredients &amp; Supplies" `<li>`:
```html
<li><a href="hops.html">Hops</a></li>
```

For **subdirectory pages** (products/ferment-in-store.html, products/ingredients-supplies.html), insert this line using the `../` relative path pattern that matches their existing custom-labels link:
```html
<li><a href="../hops.html">Hops</a></li>
```

The updated nav-dropdown-menu will have 4 items in this order:
1. Ferment in Store
2. Ingredients &amp; Supplies
3. Hops (NEW)
4. Custom Labels

Do NOT add `class="active"` to the Hops link on any of these 8 pages -- only hops.html itself gets `class="active"` (already handled in Task 1).

**Update `sitemap.xml`** to add a new `<url>` entry for the hops page, inserted before the closing `</urlset>` tag:
```xml
<url>
  <loc>https://steinsandvines.ca/hops</loc>
  <lastmod>2026-05-18</lastmod>
  <changefreq>weekly</changefreq>
  <priority>0.8</priority>
</url>
```
  </action>
  <verify>
    <automated>for f in index.html products.html custom-labels.html reservation.html about.html contact.html; do grep -q 'hops.html' "$f" || { echo "FAIL: $f missing hops link"; exit 1; }; done && for f in products/ferment-in-store.html products/ingredients-supplies.html; do grep -q '../hops.html' "$f" || { echo "FAIL: $f missing hops link"; exit 1; }; done && grep -q 'steinsandvines.ca/hops' sitemap.xml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - index.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - products.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - custom-labels.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - reservation.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - about.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - contact.html nav-dropdown-menu contains `<a href="hops.html">Hops</a>`
    - products/ferment-in-store.html nav-dropdown-menu contains `<a href="../hops.html">Hops</a>`
    - products/ingredients-supplies.html nav-dropdown-menu contains `<a href="../hops.html">Hops</a>`
    - On all 8 pages, "Hops" appears between "Ingredients &amp; Supplies" and "Custom Labels" in the dropdown order
    - sitemap.xml contains `<loc>https://steinsandvines.ca/hops</loc>`
    - No `class="active"` on the Hops link in any of these 8 pages
  </acceptance_criteria>
  <done>All 8 existing public pages have the Hops nav link in the correct position; sitemap.xml includes hops page entry</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zoho data -> DOM | Hop names, descriptions, origin text from Zoho rendered to page (handled in Plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Spoofing | hops.html (public page) | accept | Public page, no auth required, no session state. No spoofing vector. |
| T-19-02 | Tampering | hops.html CSP | mitigate | CSP meta tag copied from custom-labels.html restricts script-src, style-src, img-src, connect-src. No inline scripts except GTM. |
| T-19-03 | Information Disclosure | hops.html meta/SEO | accept | All content is intentionally public. No PII or secrets in page source. |
</threat_model>

<verification>
- hops.html loads without JS errors when styles.min.css and main.min.js are available
- All 9 public pages (8 existing + hops.html) have 4-item Products dropdown
- sitemap.xml is valid XML with hops entry
- css/hops.css references only existing CSS variables (no new variable declarations)
</verification>

<success_criteria>
hops.html exists as a complete standalone public page with hero, filter controls, catalog container, correct SEO/meta, GTM, JSON-LD, and nav. css/hops.css contains all visual styles needed for radar charts, flavor tags, size toggles, hop cards, and responsive breakpoints. All 8 existing public pages link to hops.html in their Products dropdown. sitemap.xml includes the hops page.
</success_criteria>

<output>
After completion, create `.planning/phases/19-hop-inventory-catalog/19-01-SUMMARY.md`
</output>
