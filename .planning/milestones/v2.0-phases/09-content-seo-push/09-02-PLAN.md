---
phase: 09-content-seo-push
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - products/ferment-in-store.html
  - products/ingredients-supplies.html
  - css/styles.css
autonomous: false
requirements: [SEO-01]
must_haves:
  truths:
    - "Ferment-in-store landing page has SEO-targeted copy visible in page source (not JS-injected)"
    - "Ingredients/supplies landing page has SEO-targeted copy visible in page source (not JS-injected)"
    - "Copy is compliance-reviewed content used verbatim — no substantive edits"
    - "Each landing page has a CTA button linking to #product-catalog"
    - ".landing-copy CSS is present in styles.css and built into styles.min.css"
  artifacts:
    - path: "products/ferment-in-store.html"
      provides: "SEO landing copy for ferment-in-store"
      contains: "landing-copy"
    - path: "products/ingredients-supplies.html"
      provides: "SEO landing copy for ingredients/supplies"
      contains: "landing-copy"
    - path: "css/styles.css"
      provides: "Landing copy section styling"
      contains: ".landing-copy"
  key_links:
    - from: "products/ferment-in-store.html"
      to: "#product-catalog"
      via: "CTA anchor link"
      pattern: "href=\"#product-catalog\""
    - from: "products/ingredients-supplies.html"
      to: "#product-catalog"
      via: "CTA anchor link"
      pattern: "href=\"#product-catalog\""
    - from: "css/styles.css"
      to: "products/ferment-in-store.html"
      via: ".landing-copy class usage"
      pattern: "\\.landing-copy"
---

<objective>
Add SEO landing copy to both product landing pages (ferment-in-store.html and ingredients-supplies.html) as inline HTML sections between the page header and the product catalog. Also add the .landing-copy CSS block.

Purpose: Provide unique, keyword-targeted content for search engine ranking (SEO-01). Copy is inline HTML for guaranteed Googlebot crawlability — not loaded via JavaScript content loader. Copy is compliance-reviewed against BC FOP licence, CRTC code, and BC Liquor Regulation (per D-01).
Output: Both landing pages contain rich SEO copy visible in page source, with proper styling.
</objective>

<execution_context>
@.planning/phases/09-content-seo-push/09-PATTERNS.md
@.planning/phases/09-content-seo-push/09-CONTEXT.md
@.planning/phases/09-content-seo-push/09-RESEARCH.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-content-seo-push/09-CONTEXT.md

<interfaces>
From products/ferment-in-store.html (insertion point — lines 101-103):
```html
    </section>  <!-- end .page-header -->

    <section class="catalog-section">  <!-- catalog starts here -->
```

Landing copy section inserts between these two sections.

From index.html (existing .intro section pattern):
```html
<section class="intro">
  <div class="container">
    <h2>Why Make Your Own Wine?</h2>
    <p>Making your own wine in Squamish is a chance...</p>
  </div>
</section>
```

CTA button pattern (from about.html):
```html
<a href="#product-catalog" class="btn">Browse Wine Kits</a>
```
</interfaces>

<copy_source>
IMPORTANT: The landing page copy was provided by the user during the discussion phase and is compliance-reviewed. Task 0 (checkpoint) gates on the user saving this copy to `.planning/phases/09-content-seo-push/09-LANDING-COPY.md` before Tasks 1-2 proceed.

The copy covers two pages:
1. "Ferment on Premise / U-Vin Winemaking" — for ferment-in-store.html
2. "Homebrew Ingredients & Supplies" — for ingredients-supplies.html

Each page has ~400-500 words with multiple sections and headings. Target keywords per D-02:
- Ferment page: u-vin squamish, make your own wine squamish, ferment on premise squamish
- Ingredients page: homebrew supplies squamish, beer brewing supplies, wine making supplies squamish
</copy_source>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 0: User saves landing page copy to file</name>
  <what-built>The landing page copy was provided verbatim during the discussion phase and is compliance-reviewed against BC FOP licence, CRTC code, and BC Liquor Regulation. It needs to be saved as a file before the executor can use it.</what-built>
  <how-to-verify>
Save the compliance-reviewed landing page copy to the following file:

```
.planning/phases/09-content-seo-push/09-LANDING-COPY.md
```

The file should contain two clearly separated sections:
1. **"Ferment on Premise / U-Vin Winemaking"** — copy for ferment-in-store.html (~400-500 words with headings)
2. **"Homebrew Ingredients & Supplies"** — copy for ingredients-supplies.html (~400-500 words with headings)

This is the same copy you provided during the `/gsd-discuss-phase` conversation. Paste it into the file above so the executor can read it programmatically.
  </how-to-verify>
  <resume-signal>Type "copy saved" after creating the file, or paste the copy directly and Claude will save it</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add SEO landing copy to ferment-in-store.html and add .landing-copy CSS</name>
  <files>products/ferment-in-store.html, css/styles.css</files>
  <read_first>
    - products/ferment-in-store.html (full file — understand current structure, find insertion point between line 101 .page-header closing and line 103 .catalog-section opening)
    - .planning/phases/09-content-seo-push/09-LANDING-COPY.md (the verbatim copy)
    - .planning/phases/09-content-seo-push/09-CONTEXT.md (D-01, D-02, D-03 decisions)
    - css/styles.css (find end of file or after last section comment block to append .landing-copy CSS)
  </read_first>
  <action>
**Part A: Add .landing-copy CSS** to `css/styles.css`. Append the following block (from 09-PATTERNS.md) at the end of the file or after the last section comment block:

```css
/* ===== Landing Copy (SEO sections on product landing pages) ===== */
.landing-copy {
  padding: 3rem 0;
  text-align: center;
}

.landing-copy h2,
.landing-copy h3 {
  color: var(--color-green);
  margin-bottom: 1rem;
}

.landing-copy p {
  margin-left: auto;
  margin-right: auto;
  max-width: 800px;
}

.landing-copy .btn {
  margin-top: 1.5rem;
}
```

**Part B: Insert landing copy** into `products/ferment-in-store.html`.

Insert a `<section class="landing-copy">` between the closing `</section>` of `.page-header` (line 101) and the opening `<section class="catalog-section">` (line 103).

**CRITICAL:** Read the copy from `.planning/phases/09-content-seo-push/09-LANDING-COPY.md`. Use the "Ferment on Premise / U-Vin Winemaking" page section VERBATIM. Do not rephrase, summarize, or editorialize the copy — it is compliance-reviewed (D-01).

**Structure template** (fill headings and paragraphs from the copy document):
```html
    <section class="landing-copy">
      <div class="container">
        <h2>{Main H2 heading from copy doc — should include primary keyword}</h2>
        <p>{First paragraph from copy doc}</p>

        <h3>{First sub-section heading from copy doc}</h3>
        <p>{Sub-section paragraph}</p>

        <h3>{Second sub-section heading from copy doc}</h3>
        <p>{Sub-section paragraph}</p>

        <!-- Continue for all sections in the copy document -->

        <a href="#product-catalog" class="btn">Browse Wine Kits</a>
      </div>
    </section>
```

**Rules:**
- Per D-03: OMIT any "[Insert address, hours, phone number]" placeholder — that info is in header/footer/about already
- Use `&amp;` for ampersands, `&mdash;` for em-dashes, `&ndash;` for en-dashes in the HTML
- Keep all headings as `<h2>` (main) and `<h3>` (sub-sections) — no `<h1>` (already used by page header)
- End with CTA button: `<a href="#product-catalog" class="btn">Browse Wine Kits</a>`
- No photos in this task — photos are handled in Plan 03
- Wrap in `.container` div inside the section

**Part C: Run `npm run build`** to regenerate styles.min.css with the new .landing-copy CSS.
  </action>
  <verify>
    <automated>grep -c "landing-copy" products/ferment-in-store.html && grep -c "Browse Wine Kits" products/ferment-in-store.html && grep -c '#product-catalog' products/ferment-in-store.html && grep -c "\.landing-copy" css/styles.css && grep -c "landing-copy" css/styles.min.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - products/ferment-in-store.html contains `<section class="landing-copy">`
    - products/ferment-in-store.html contains `<a href="#product-catalog" class="btn">Browse Wine Kits</a>`
    - The landing-copy section appears AFTER `</section>` closing .page-header and BEFORE `<section class="catalog-section">`
    - The copy text is present in the raw HTML source (not loaded via JavaScript)
    - No "[Insert address, hours, phone number]" placeholders remain (per D-03)
    - Content includes at least one `<h2>` and at least one `<h3>` heading
    - Copy word count is approximately 400-500 words (per D-02)
    - css/styles.css contains `.landing-copy {` with padding and text-align rules
    - styles.min.css contains `landing-copy` (build ran successfully)
  </acceptance_criteria>
  <done>ferment-in-store.html has inline SEO landing copy with target keywords, proper heading hierarchy, and CTA button. .landing-copy CSS added and built into styles.min.css.</done>
</task>

<task type="auto">
  <name>Task 2: Add SEO landing copy to ingredients-supplies.html</name>
  <files>products/ingredients-supplies.html</files>
  <read_first>
    - products/ingredients-supplies.html (full file — find insertion point between .page-header closing and .catalog-section opening)
    - .planning/phases/09-content-seo-push/09-LANDING-COPY.md (the verbatim copy — "Homebrew Ingredients & Supplies" section)
    - .planning/phases/09-content-seo-push/09-CONTEXT.md (D-01, D-02, D-03 decisions)
  </read_first>
  <action>
Insert a `<section class="landing-copy">` between the closing `</section>` of `.page-header` (line 101) and the opening `<section class="catalog-section">` (line 103) in `products/ingredients-supplies.html`.

**CRITICAL:** Read the copy from `.planning/phases/09-content-seo-push/09-LANDING-COPY.md`. Use the "Homebrew Ingredients & Supplies" page section VERBATIM. Do not rephrase, summarize, or editorialize — it is compliance-reviewed (D-01).

**Structure template** (fill headings and paragraphs from the copy document):
```html
    <section class="landing-copy">
      <div class="container">
        <h2>{Main H2 heading from copy doc — should include primary keyword}</h2>
        <p>{First paragraph from copy doc}</p>

        <h3>{First sub-section heading from copy doc}</h3>
        <p>{Sub-section paragraph}</p>

        <h3>{Second sub-section heading from copy doc}</h3>
        <p>{Sub-section paragraph}</p>

        <!-- Continue for all sections in the copy document -->

        <a href="#product-catalog" class="btn">Browse Homebrew Supplies</a>
      </div>
    </section>
```

**Rules:**
- Per D-03: OMIT any "[Insert address, hours, phone number]" placeholder
- Use `&amp;` for ampersands, `&mdash;` for em-dashes, `&ndash;` for en-dashes
- Keep headings as `<h2>` (main) and `<h3>` (sub-sections)
- End with CTA button: `<a href="#product-catalog" class="btn">Browse Homebrew Supplies</a>`
- No photos in this task — photos are handled in Plan 03
- Wrap in `.container` div inside the section
  </action>
  <verify>
    <automated>grep -c "landing-copy" products/ingredients-supplies.html && grep -c "Browse Homebrew Supplies" products/ingredients-supplies.html && grep -c '#product-catalog' products/ingredients-supplies.html && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - products/ingredients-supplies.html contains `<section class="landing-copy">`
    - products/ingredients-supplies.html contains `<a href="#product-catalog" class="btn">Browse Homebrew Supplies</a>`
    - The landing-copy section appears AFTER `</section>` closing .page-header and BEFORE `<section class="catalog-section">`
    - The copy text is present in the raw HTML source (not loaded via JavaScript)
    - No "[Insert address, hours, phone number]" placeholders remain (per D-03)
    - Content includes at least one `<h2>` and at least one `<h3>` heading
    - Copy word count is approximately 400-500 words (per D-02)
  </acceptance_criteria>
  <done>ingredients-supplies.html has inline SEO landing copy with target keywords, proper heading hierarchy, and CTA button linking to product catalog</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML served by GitHub Pages | No dynamic input; content is authored by site owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | Landing page copy | accept | Copy is public marketing content; no sensitive data. Compliance-reviewed by site owner per D-01. |
| T-09-05 | Tampering | Static HTML integrity | accept | GitHub Pages serves from git — only authorized pushes can modify content. No additional mitigation needed for static content. |
</threat_model>

<verification>
1. View page source of both landing pages — SEO copy visible in raw HTML without JS
2. `curl -s products/ferment-in-store.html | grep -c "landing-copy"` returns 1
3. `curl -s products/ingredients-supplies.html | grep -c "landing-copy"` returns 1
4. Both pages have a CTA button linking to #product-catalog
5. No placeholder text like "[Insert address...]" remains
6. css/styles.css contains .landing-copy CSS block
7. styles.min.css contains landing-copy (build successful)
</verification>

<success_criteria>
Both product landing pages have 400-500 words of inline SEO copy with keyword-targeted headings, proper HTML structure, and CTA buttons. Copy is visible in page source for Googlebot. No JavaScript dependency for SEO-critical text. .landing-copy CSS is present and built.
</success_criteria>

<output>
After completion, create `.planning/phases/09-content-seo-push/09-02-SUMMARY.md`
</output>
