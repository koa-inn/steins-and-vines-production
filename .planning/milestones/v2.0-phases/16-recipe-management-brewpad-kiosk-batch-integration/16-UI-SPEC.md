---
phase: 16
slug: recipe-management-brewpad-kiosk-batch-integration
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-17
---

# Phase 16 — UI Design Contract
# Recipe Management: BrewPad, Kiosk & Batch Integration

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — vanilla CSS with custom CSS variables |
| Preset | not applicable |
| Component library | none — hand-rolled components |
| Icon library | Unicode characters and emoji (existing pattern) |
| Font | Lato (body), Georgia (display) — loaded via Google Fonts |

**Source:** Detected from `css/brewpad.css` and `css/kiosk.css` `:root` blocks. Both files use identical token sets — brewpad.css is self-contained and does not import admin.css or styles.css.

**shadcn Gate:** Not applicable. Stack is vanilla JS/ES5 — not React/Next.js/Vite. No shadcn initialization recommended or possible.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, label-to-input margins (`margin-bottom: 4px` in `.bp-form-group label`) |
| sm | 8px | Section title bottom margin (`margin-bottom: 8px` in `.bp-detail-section-title`), inline element gaps |
| md | 16px | Default form group margin, button padding, form action padding-top |
| lg | 24px | Recipe section body padding (internal padding on recipe detail panel) |
| xl | 32px | Not used in BrewPad/kiosk panels — reserved for page-level layout |
| 2xl | 48px | Not used in scope of this phase |
| 3xl | 64px | Not used in scope of this phase |

**Exceptions:**
- Touch targets in kiosk (sale type buttons): minimum 64px height — matches `.kiosk-sale-type-btn { min-height: 64px }` existing pattern
- Form group internal padding: 12px (`.bp-form-subgroup { padding: 12px }`) — pre-existing, not a new exception introduced by Phase 16

**Note on inherited values:** `.bp-detail-section { margin-bottom: 18px }` is an existing value not touched by Phase 16. It is not declared here because Phase 16 does not introduce or modify it.

**Source:** `css/brewpad.css` lines 734-1256; `css/kiosk.css` lines 498-2653.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.6 |
| Label / section title | 12px | 700 | 1.0 (uppercase, letter-spacing: 0.06em) |
| Form label | 13.1px (0.82rem) | 700 | default |
| Recipe name / display | 20px (1.25rem) | 700 | 1.25 |

**Declared weights:** 400 (regular) and 700 (bold) only. No other weights are used in Phase 16 components.

**Size mapping for component specs:** All component-level font sizes must resolve to one of the four declared roles above:
- Sub-label sizes (0.72rem, 0.78rem) → 12px (label role)
- Small body sizes (0.8rem, 0.85rem, 0.9rem) → treat as body (16px) or form-label (13.1px) context — do not introduce additional discrete size values in CSS beyond what already exists in the component files

**Notes:**
- 16px is the base `font-size: 16px` on `html` in both brewpad.css and kiosk.css. Inputs must remain at 16px minimum to prevent iOS Safari auto-zoom (explicit comment in brewpad.css line 152).
- Section title style (`text-transform: uppercase; letter-spacing: 0.06em; font-size: 0.75rem; font-weight: 700; color: var(--ink-tertiary)`) is the established pattern for ALL `.bp-detail-section-title` elements including the new Recipe section title. Do not deviate.
- Recipe display name in kiosk prompt: `.kiosk-recipe-selected-name { font-family: var(--font-display); font-size: 1.25rem; font-weight: 700 }` — use this for recipe name in kiosk quick-edit prompt header.
- Product card names in kiosk recipe grid: inherit the form-label role (13.1px, 700) for the name line and the label role (12px, 400) for the style/ABV sub-line. Do not introduce weight 600.

**Source:** `css/brewpad.css` lines 70-82, 738-747, 1222-1228; `css/kiosk.css` lines 67-77, 545-553, 2621-2627.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--cellar-canvas: #e8e2ca` | Page background, app shell background |
| Secondary (30%) | `--cellar-raised: #faf8f2` | Cards, detail panels, form subgroups, dropdowns |
| Accent (10%) | `--cellar-green: #4a6f4b` | Active tab indicator, primary CTA hover state, active batch status |
| Brand interactive | `--stain: #370e13` | Primary button fill, focus ring, active sale type button |
| Muted brand | `--barrel: #77462d` | Hover states on cards and tabs, ingredient row interactive hints |
| Destructive | `--batch-danger: #a83232` | Destructive action buttons and danger toast border only |

**Accent (`--cellar-green`) reserved for:**
1. Active tab underline indicator in the `[Kits] [Recipes]` tabbed picker (`.bp-tab.active { color: var(--cellar-green) }` existing pattern)
2. "Save" button hover state (not fill — fill uses `--stain`)
3. Recipe status badge background when `status = active`

**Secondary semantic colors (informational only — not for interactive elements):**
- `--batch-success: #3d7a40` — success toast left border
- `--batch-warning: #b87a1a` — warning toast left border
- `--batch-info: #4a6f8a` — info toast left border

**Source:** `css/brewpad.css` lines 17-50 (`:root`); `css/kiosk.css` lines 13-62.

---

## Component Inventory

### New Components (Phase 16 additions)

All new components extend existing CSS classes — no new base styles needed beyond targeted additions to `css/brewpad.css` and `css/kiosk.css`.

#### 1. BrewPad Tabbed Product Picker (`.bp-product-tabs`)

Replaces single product text input in `buildCreateForm()`. Two tabs: "Kits" (existing behavior) and "Recipes" (new).

**Structure:**
```
.bp-product-tabs
  .bp-product-tab-bar
    button.bp-product-tab[data-picker-tab="kits"]  — active by default
    button.bp-product-tab[data-picker-tab="recipes"]
  .bp-vessel-wrap  (existing — contains input + dropdown)
```

**Tab bar visual contract:**
- Tab buttons: `font-size: 0.82rem; font-weight: 400; padding: 6px 12px; border: none; background: none; border-bottom: 2px solid transparent; color: var(--ink-secondary); cursor: pointer`
- Active tab: `border-bottom-color: var(--barrel); font-weight: 700; color: var(--ink-primary)`
- Hover: `color: var(--barrel)`
- Container: `display: flex; border-bottom: 1px solid var(--ledger-soft); margin-bottom: 8px`
- Matches the `.kiosk-mode-toggle__btn` pattern from `kiosk.css` lines 2593-2618 — use same visual language in BrewPad context.

**Recipe dropdown item format:**
- One line: `[Recipe Name]` — `font-weight: 700` (bold role)
- Second line: `[Style] · [ABV]%` — label role (12px, 400, `color: var(--ink-tertiary)`)
- Reuses `.bp-vessel-dropdown` existing scroll list item style

#### 2. BrewPad Collapsible Recipe Section (`.bp-detail-section--recipe`)

Extends `.bp-detail-section`. Added after the Notes section in `renderBatchDetail()`.

**Primary focal point — batch detail:** The collapsible Recipe section toggle header anchors the new interaction area. The chevron and section title are the visual entry point for all recipe functionality on this screen.

**Toggle header:**
- Inherits `.bp-detail-section-title` styles exactly (12px, 700, uppercase, `--ink-tertiary`)
- Add: `cursor: pointer; user-select: none; display: flex; align-items: center; justify-content: space-between`
- Toggle chevron: Unicode `▶` (`&#9656;`) rotates 90deg on expand — `transition: transform 0.2s ease`
- Hover: `color: var(--barrel)`
- Collapsed default: `aria-expanded="false"`, body hidden via `display: none`
- Keyboard accessible: `role="button"; tabindex="0"` — responds to Enter and Space

**Section body — State A (has recipe_snapshot):**
| Element | Spec |
|---------|------|
| Recipe metadata row | `display: flex; gap: 24px; flex-wrap: wrap; margin-bottom: 12px` — form-label role (13.1px) |
| Metadata label | label role: 12px, 700, uppercase, `color: var(--ink-tertiary)` |
| Metadata value | form-label role: 13.1px, 700, `color: var(--ink-primary)` |
| Ingredient table | Full-width table, form-label role (13.1px), alternating row bg `--cellar-canvas` / `--cellar-surface` |
| "Edit Recipe" button | `.btn-secondary` (existing) — small size: body role (16px) at existing `.btn-secondary` padding |

Metadata fields shown: Style, ABV (%), IBU, Batch Size (L). One row, wrapping on narrow screens.

**Section body — State B (no recipe_snapshot — kit batch):**
| Element | Spec |
|---------|------|
| Prompt text | form-label role: 13.1px, 400, `color: var(--ink-secondary); margin-bottom: 12px` |
| "Attach Recipe" button | `.btn-secondary` (existing) |
| "Create Recipe" button | `.btn` (existing primary style) |
| Button row | `display: flex; gap: 8px` |

**Inline edit mode (triggered by "Edit Recipe" button):**
- Ingredient table rows switch from read-only text to `<input type="number">` for quantity
- Notes cells become `<textarea>` 2 rows
- "Save Changes" button (`.btn`) + "Discard Changes" button (`.btn-secondary`) appear below table
- No modal — inline replacement within the section body, consistent with existing bp inline edit pattern

#### 3. Create Recipe from Batch — Slide-out Panel (`.bp-recipe-create-sheet`)

**Decision from Claude's Discretion:** A slide-out panel using the same `.bp-create-sheet` pattern as the New Batch form. Not a modal. Not a redirect. Self-contained within BrewPad.

**Panel structure:** Same as existing `bp-create-sheet` slide-in. Header: "Create Recipe from Batch". Pre-fills recipe name from `batch.product_name`. Fields: Name (required), Style, ABV, IBU, Batch Size (L). Ingredient rows pulled from existing recipe ingredient pattern if any snapshot exists, otherwise empty.

**Save behavior:** Calls `POST /api/recipes` (existing endpoint) → on success, calls `update_batch` with `recipe_id` + `recipe_snapshot` → closes panel and refreshes Recipe section.

**Double-submit guard:** "Save Recipe" button disables immediately on click, re-enables on error.

#### 4. Kiosk Recipe Quick-Edit Panel

**Decision from Claude's Discretion:** Added as a second state within the existing recipe prompt/detail view — not a separate navigation level. After staff taps a recipe card, the recipe detail pane shows a "Edit Recipe" button in the action row. Tapping it transforms the detail pane into quick-edit mode inline.

**Primary focal point — kiosk recipe panel:** The "Edit Recipe" button is the primary CTA. It is the sole entry point to edit mode and should be visually prominent within the action row using the `.btn` (primary) style.

**Quick-edit fields per D-03/D-09:** Name, Notes, Price (locked_price), Status (draft/active select).

**Field layout:**
```
.kiosk-recipe-quick-edit (panel, inherits existing kiosk panel padding: 1rem)
  h3 "Edit Recipe"  — font-display, body role (16px), --ink-primary
  form
    label + input.bp-inline-input  for Name
    label + textarea (3 rows)      for Notes
    label + input[type=number]     for Price (step=0.01)
    label + select                 for Status (draft / active)
  .kiosk-quick-edit-actions
    button.btn "Save Changes"
    button.btn-secondary "Discard Changes"
```

- Status select values: `draft` and `active` only
- Price input: `step="0.01"`, `inputmode="decimal"`, minimum `0`
- "Save Changes" disables on submit, re-enables on error
- On success: show success toast, return to recipe detail view (read-only)
- Status guard: send current status value if staff does not change it (never omit from payload)

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Tabbed picker — Kits tab label | "Kits" |
| Tabbed picker — Recipes tab label | "Recipes" |
| Recipe section title (batch detail) | "Recipe" (uppercase via CSS) |
| Recipe section toggle — collapsed | "Recipe ▶" (chevron rotates on expand) |
| Recipe section — State B prompt | "No recipe attached to this batch." |
| Attach Recipe button | "Attach Recipe" |
| Create Recipe button | "Create Recipe" |
| Recipe create slide-out title | "Create Recipe from Batch" |
| Inline edit mode — save button | "Save Changes" |
| Inline edit mode — cancel button | "Discard Changes" |
| Kiosk recipe quick-edit — title | "Edit Recipe" |
| Kiosk quick-edit — save button | "Save Changes" |
| Kiosk quick-edit — cancel button | "Discard Changes" |
| Primary CTA (BrewPad new batch with recipe) | "Create Batch" (unchanged — existing label) |
| Empty state — recipe picker (no results) | "No recipes found. Try a different name." |
| Empty state — kiosk recipe grid | "No active recipes. Add recipes in the Admin panel." |
| Error — recipe load failed | "Could not load recipes. Check your connection and try again." |
| Error — save failed (batch recipe snapshot) | "Could not save recipe. Please try again." |
| Error — kiosk quick-edit save failed | "Could not update recipe. Please try again." |
| Error — create recipe from batch failed | "Could not create recipe. Please try again." |
| Destructive: none in this phase | — (no destructive actions in Phase 16) |

**Notes on destructive actions:** Phase 16 contains no delete or irreversible actions. Batch-local recipe edits can be undone by re-attaching the original recipe. No confirmation dialogs are required.

**Source:** CONTEXT.md decisions D-01 through D-09; RESEARCH.md patterns; extended using established project copywriting style (imperative verb + noun, no ellipsis).

---

## Interaction Contracts

### Tabbed Product Picker

| Trigger | Behavior |
|---------|----------|
| Page load / form open | "Kits" tab active; product text input focused; kit dropdown behavior unchanged |
| Click "Recipes" tab | Tab switches; product text input placeholder changes to "Search recipes…"; dropdown clears and binds to recipe API; `_productPickerTab = 'recipes'` |
| Click "Kits" tab | Reverts to kit search behavior; clears recipe hidden fields; `_productPickerTab = 'kits'` |
| Select recipe from dropdown | Product name pre-filled from recipe.name; `bp-new-recipe-id` and `bp-new-recipe-snapshot` hidden fields populated; dropdown closes; input shows recipe name (read-only appearance via disabled state) |
| Clear / deselect recipe | Clear button resets name input, clears hidden fields, re-enables search |
| Form submit | If recipe selected: payload includes `recipe_id` and `recipe_snapshot`; product_sku left blank; if kit selected: existing behavior unchanged |

### Collapsible Recipe Section

| Trigger | Behavior |
|---------|----------|
| Batch detail renders | Recipe section appended after Notes; collapsed by default (`aria-expanded="false"`, body `display:none`) |
| Click / Enter / Space on section header | Toggle: `aria-expanded` flips; body `display` toggles; chevron rotates 90deg |
| "Edit Recipe" clicked | Ingredient table rows replace text with inputs; quantity `<input type="number">`; notes `<textarea>`; Save/Discard Changes row appears |
| "Save Changes" clicked | Button disables; `adminApiPost('update_batch', {updates: {recipe_snapshot, recipe_id}})` fires; on success: inline edit mode exits, section re-renders with updated data, success toast shows; on error: button re-enables, error toast shows |
| "Discard Changes" clicked | Inputs revert to read-only display; no API call |
| "Attach Recipe" clicked | Inline recipe search input appears below button; staff searches and selects; `update_batch` fires with recipe_id + snapshot; on success section re-renders with State A |
| "Create Recipe" clicked | Button disables; `.bp-recipe-create-sheet` slide-out panel opens |
| Recipe create save | `POST /api/recipes` → on success: `update_batch` with recipe_id + snapshot → panel closes → Recipe section re-renders State A |

### Kiosk Recipe Quick-Edit

| Trigger | Behavior |
|---------|----------|
| Staff taps recipe card | Existing recipe detail pane opens; "Edit Recipe" button visible below metadata |
| "Edit Recipe" clicked | Pane transitions to quick-edit form (fields pre-filled from current recipe data); status field pre-filled with current status |
| "Save Changes" clicked | Button disables; `PUT /api/recipes/:id` fires with all 4 fields (name, notes, locked_price, status); on success: success toast, return to read-only detail view; on error: button re-enables, error toast |
| "Discard Changes" clicked | Return to read-only detail view; no API call |

---

## Accessibility Contracts

- All interactive section headers use `role="button"; tabindex="0"` — keyboard operable via Enter and Space
- Recipe ingredient table has `<thead>` with `<th scope="col">` for screen reader column context
- Quick-edit form inputs have associated `<label>` elements (not aria-label — explicit labels)
- Status select includes visible label "Status" — not icon-only
- Disabled buttons have `disabled` attribute (not just `opacity: 0.6` pointer-events) so screen readers announce unavailability
- Focus management: when slide-out panel opens, focus moves to first field; when it closes, focus returns to triggering button
- Touch targets: all interactive elements minimum 44px height in kiosk context (kiosk runs on tablet touchscreen)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — not a shadcn project |
| Third-party | none | not applicable |

No third-party component registries are used. All components are hand-rolled vanilla CSS/JS consistent with the existing codebase pattern.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
