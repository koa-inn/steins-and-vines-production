---
phase: 11-producer-brand-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/catalog.js
  - zoho-middleware/__tests__/catalog.test.js
autonomous: true
requirements:
  - PROD-VIS-01
user_setup: []

must_haves:
  truths:
    - "The /api/products response includes a 'manufacturer' field for each product"
    - "The /api/kiosk/products response includes a 'manufacturer' field for each product"
    - "Products with manufacturer set in Zoho show the value; products without show empty string"
  artifacts:
    - path: "zoho-middleware/routes/catalog.js"
      provides: "Manufacturer enrichment in all 3 paths + shapeProduct()"
      contains: "manufacturer"
    - path: "zoho-middleware/__tests__/catalog.test.js"
      provides: "Tests verifying manufacturer flows through enrichment"
      contains: "manufacturer"
  key_links:
    - from: "zoho-middleware/routes/catalog.js"
      to: "Zoho /itemdetails API"
      via: "detail.manufacturer_name in enrichment loops"
      pattern: "detail\\.manufacturer_name"
    - from: "zoho-middleware/routes/catalog.js shapeProduct()"
      to: "API response JSON"
      via: "z.manufacturer mapping"
      pattern: "manufacturer.*z\\.manufacturer"
---

<objective>
Add `manufacturer` field to the middleware catalog enrichment pipeline so all product API responses include producer data from Zoho Inventory.

Purpose: PROD-VIS-01 requires the middleware to pipe the Zoho `manufacturer_name` field through catalog enrichment. This is the data foundation that all frontend producer displays depend on.

Output: Updated `catalog.js` with manufacturer in all 3 enrichment paths + `shapeProduct()`, plus regression tests confirming the field flows through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-producer-brand-visibility/11-CONTEXT.md
@.planning/phases/11-producer-brand-visibility/11-RESEARCH.md
@.planning/phases/11-producer-brand-visibility/11-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase analysis. -->

From zoho-middleware/routes/catalog.js:
- shapeProduct(z) at ~line 810: maps Zoho item fields to frontend shape. Currently includes `brand: z.brand || ''`. Add `manufacturer: z.manufacturer || ''` after brand.
- Products enrichment at ~line 176: `item.brand = detail.brand || '';` -- add `item.manufacturer = detail.manufacturer_name || '';` after this line. NOTE: The Zoho detail object uses field name `manufacturer_name` (per D-05/D-06), but we store it as `item.manufacturer` for frontend consistency with the `brand` pattern.
- Ingredients enrichment at ~line 543: same pattern as products enrichment.
- Kiosk enrichment inline object at ~lines 738-755: returns an object with fields like `brand: detail.brand || item.brand || ''`. Add `manufacturer: detail.manufacturer_name || item.manufacturer || ''`.

From zoho-middleware/lib/zoho-api.js:
- fetchItemDetailsBulk(itemIds): Returns detail objects from Zoho /itemdetails. These objects include `brand` (proven by working code). The manufacturer field in Zoho is `manufacturer_name` (per D-05, D-06).

CRITICAL FIELD NAME MAPPING (per D-05, D-06):
- Zoho source field: `detail.manufacturer_name` (the raw Zoho API field)
- Enriched item field: `item.manufacturer` (how it's stored on the enriched item, matching the `brand` pattern)
- shapeProduct output: `manufacturer: z.manufacturer || ''` (the API response field name)
- Frontend consumption: `product.manufacturer` / `item.manufacturer`

This follows the same pattern as brand: `detail.brand` -> `item.brand` -> `z.brand` -> `product.brand`. The only difference is the Zoho source field has a `_name` suffix.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add manufacturer to catalog enrichment pipeline and shapeProduct</name>
  <files>zoho-middleware/routes/catalog.js</files>
  <read_first>
    - zoho-middleware/routes/catalog.js (full file -- executor must see all 3 enrichment paths and shapeProduct)
    - zoho-middleware/lib/zoho-api.js (fetchItemDetailsBulk to understand what detail objects contain)
    - .planning/phases/11-producer-brand-visibility/11-PATTERNS.md (exact line numbers and patterns)
  </read_first>
  <action>
Add `manufacturer` field to all 4 locations in catalog.js, following the exact same pattern as `brand` but reading from the Zoho field `manufacturer_name` (per D-05/D-06):

**Location 1 -- Products enrichment** (find `item.brand = detail.brand || '';` around line 176):
Add immediately after the brand line:
```javascript
item.manufacturer = detail.manufacturer_name || '';
```
NOTE: The Zoho API field is `manufacturer_name` (not `manufacturer`). This matches D-05 and D-06. The enriched item stores it as `item.manufacturer` for frontend consistency.

**Location 2 -- Ingredients enrichment** (find `item.brand = detail.brand || '';` around line 543):
Add immediately after the brand line:
```javascript
item.manufacturer = detail.manufacturer_name || '';
```
Note: Even though ingredients don't display producer (kit-only per D-04), adding enrichment here keeps the pipeline consistent and prevents future bugs if ingredients ever need manufacturer.

**Location 3 -- Kiosk enrichment inline return object** (find the inline object around lines 738-755):
Add `manufacturer` to the returned object alongside other detail fields:
```javascript
manufacturer: detail.manufacturer_name || item.manufacturer || '',
```
IMPORTANT: Check if `brand` already exists in this inline object. Per PATTERNS.md observation, the kiosk path may need BOTH `brand` and `manufacturer` added. If `brand` is missing from the kiosk inline object, also add `brand: detail.brand || item.brand || ''`.

**Location 4 -- shapeProduct()** (find `brand: z.brand || '',` around line 815):
Add immediately after the brand line:
```javascript
manufacturer: z.manufacturer || '',
```
NOTE: shapeProduct reads from the enriched item (`z`), which already has `item.manufacturer` set by the enrichment loops above. So this uses `z.manufacturer` (no `_name` suffix).

Do NOT modify the product key logic (`name + '|' + brand`) -- manufacturer is display-only, not part of the cart key.
  </action>
  <verify>
    <automated>cd zoho-middleware && grep -n 'manufacturer' routes/catalog.js | head -20 && npm test -- --verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - catalog.js contains `item.manufacturer = detail.manufacturer_name || ''` in at least 2 enrichment paths (products, ingredients)
    - catalog.js shapeProduct() contains `manufacturer: z.manufacturer || ''`
    - catalog.js kiosk enrichment inline object contains `manufacturer: detail.manufacturer_name`
    - The string `name + '|' + brand` is unchanged (product key not modified)
  </acceptance_criteria>
  <done>All 4 locations in catalog.js have manufacturer enrichment. Enrichment reads from Zoho's `detail.manufacturer_name` field (per D-05/D-06) and stores as `item.manufacturer`. shapeProduct() maps `z.manufacturer` to the API response shape.</done>
</task>

<task type="auto">
  <name>Task 2: Add middleware tests for manufacturer enrichment</name>
  <files>zoho-middleware/__tests__/catalog.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/catalog.test.js (understand existing test structure, mock patterns, and where to add new assertions)
    - zoho-middleware/routes/catalog.js (see the enrichment code just added in Task 1)
  </read_first>
  <action>
Extend the existing catalog test suite to verify manufacturer flows through enrichment. Follow the established test patterns in catalog.test.js.

**Test 1 -- shapeProduct includes manufacturer:**
Find existing tests that verify `shapeProduct()` output (look for tests that check `brand` in the shaped result). Add a parallel assertion for `manufacturer`:
```javascript
expect(result.manufacturer).toBe('Test Manufacturer');
```
If no direct shapeProduct test exists, add the manufacturer field to the mock input data of the product enrichment test and verify it appears in the API response.

**Test 2 -- Products enrichment passes manufacturer_name from detail:**
Find the test that exercises the `/api/products` route handler. In the mock data for `fetchItemDetailsBulk`, add `manufacturer_name: 'RJS Craft Winemaking'` to the detail object. Assert the enriched product in the response includes `manufacturer: 'RJS Craft Winemaking'`.
NOTE: The mock detail uses `manufacturer_name` (the Zoho field name) but the assertion checks `manufacturer` (the enriched item field name).

**Test 3 -- Manufacturer defaults to empty string when missing:**
Add a test (or assertion within an existing test) where the detail object has NO `manufacturer_name` field. Assert the enriched product has `manufacturer: ''` (empty string, not undefined or null).

**Test 4 -- Kiosk enrichment includes manufacturer:**
Find the test that exercises the `/api/kiosk/products` route handler. Add `manufacturer_name: 'Winexpert'` to the mock detail object. Assert the kiosk product response includes `manufacturer: 'Winexpert'`.

Use the same mock patterns already established in the file (jest.mock for zoho-api, cache, logger). Do NOT modify existing test assertions -- only ADD new ones or new test cases.

Run the full middleware test suite to verify nothing breaks:
```bash
cd zoho-middleware && npm test
```
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- --verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - catalog.test.js contains at least 2 new test assertions referencing `manufacturer`
    - `cd zoho-middleware && npm test` exits with code 0 (all tests pass)
    - At least one test verifies manufacturer defaults to empty string when absent from Zoho detail
    - At least one test uses `manufacturer_name` in mock detail data (verifying the field name mapping)
    - No existing tests are modified (only new assertions or test cases added)
  </acceptance_criteria>
  <done>Middleware test suite passes with new manufacturer enrichment tests covering the happy path (manufacturer_name present in detail), fallback (manufacturer_name absent), and kiosk enrichment path. Tests verify the Zoho field name mapping (detail.manufacturer_name -> item.manufacturer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zoho API -> Middleware | Zoho detail response fields are treated as trusted upstream data, but values are sanitized with `|| ''` fallback |
| Middleware -> Frontend | API response includes manufacturer as a string field; frontend uses textContent for XSS safety |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | catalog.js enrichment | accept | Zoho is a trusted data source; manufacturer_name field is a standard Zoho inventory field. The `|| ''` fallback prevents undefined/null from propagating. Risk is LOW. |
| T-11-02 | Information Disclosure | API response | accept | manufacturer is intentionally public product metadata (producer name). No PII or sensitive data. Exposure is by design. |
</threat_model>

<verification>
1. `cd zoho-middleware && npm test` passes with 0 failures
2. `grep -c 'manufacturer' zoho-middleware/routes/catalog.js` returns at least 4 (one per enrichment location)
3. `grep -c 'manufacturer' zoho-middleware/__tests__/catalog.test.js` returns at least 2
</verification>

<success_criteria>
- The middleware catalog enrichment pipeline includes manufacturer in all 3 enrichment paths (products, ingredients, kiosk) and in shapeProduct()
- Enrichment reads from Zoho's `detail.manufacturer_name` field (per D-05/D-06), stores as `item.manufacturer`
- All existing middleware tests still pass
- New tests verify manufacturer enrichment works correctly with both present and absent Zoho data
- New tests verify the field name mapping (detail.manufacturer_name -> item.manufacturer)
</success_criteria>

<output>
After completion, create `.planning/phases/11-producer-brand-visibility/11-01-SUMMARY.md`
</output>
