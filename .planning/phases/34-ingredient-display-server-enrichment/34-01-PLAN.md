---
phase: 34-ingredient-display-server-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/lib/constants.js
  - js/modules/17-search-overlay.js
  - js/lib/recipe-grouping.js
  - tests/frontend/recipe-grouping.test.js
autonomous: true
requirements: [RDISP-02]
must_haves:
  truths:
    - "groupRecipeIngredients groups top-level by cf_type, nesting by cf_subcategory only when 2+ distinct subcats (D-01, D-02)"
    - "Sections emit in fixed brewing-process order Grain->Hops->Yeast->Additive then Packaging->Equipment->Cleaning/Sanitization, Other last (D-03, D-06)"
    - "Within-section order preserves recipe-entry order (D-05)"
    - "Each emitted group carries a per-item count (D-11)"
    - "Ingredients with no cf_type/cf_subcategory fall into Other; cold-cache (no cf fields at all) returns one flat group (D-06, D-07)"
    - "CATEGORY_DISPLAY_NAMES is shared from js/lib/constants.js and reused for section labels (D-04)"
  artifacts:
    - path: "js/lib/recipe-grouping.js"
      provides: "groupRecipeIngredients shared helper (D-09)"
      exports: ["groupRecipeIngredients"]
    - path: "js/lib/constants.js"
      provides: "promoted CATEGORY_DISPLAY_NAMES map (D-04)"
      contains: "CATEGORY_DISPLAY_NAMES"
    - path: "tests/frontend/recipe-grouping.test.js"
      provides: "Jest coverage of grouping helper (D-01..D-07, D-11)"
  key_links:
    - from: "js/lib/recipe-grouping.js"
      to: "js/lib/constants.js"
      via: "global CATEGORY_DISPLAY_NAMES (in-browser) / require (Jest)"
      pattern: "CATEGORY_DISPLAY_NAMES"
---

<objective>
Establish the single source of truth for ingredient grouping (D-09): promote the existing `CATEGORY_DISPLAY_NAMES` label map into shared constants (D-04), then build the shared `groupRecipeIngredients` helper that admin, kiosk, and BrewPad will all consume. This is foundation — the three surface renders in Plan 03 depend on this helper existing.

Purpose: One grouping implementation guarantees the three surfaces group identically (the real intent of RDISP-02), without changing the recipe API response contract.
Output: `js/lib/recipe-grouping.js`, promoted `CATEGORY_DISPLAY_NAMES` in `js/lib/constants.js`, and a Jest suite proving the grouping rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ingredient-display-server-enrichment/34-CONTEXT.md
@.planning/phases/34-ingredient-display-server-enrichment/34-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Existing analog: groupResultsByCategory in js/modules/17-search-overlay.js (L85-110).
     Copy its build-then-emit skeleton; replace count-descending sort with fixed SECTION_ORDER. -->

CATEGORY_DISPLAY_NAMES (currently js/modules/17-search-overlay.js L54-73) collapses raw
subcategory/type values to friendly labels: Grain->"Grains", Bottle+Bag->"Packaging",
Fermenter/Hose/Tubing->"Equipment", etc. Missing keys to ADD for cf_type values not yet
mapped: 'Ingredient', 'Cleaning/Sanitization'.

js/lib/constants.js current export block (L43-50): module.exports = { CART_KEYS, ITEM_TYPES,
PRODUCT_TABS, KIT_CATEGORIES }. Add CATEGORY_DISPLAY_NAMES to both the top-level var and this block.

Enriched ingredient fields (produced by Plan 02): each ingredient object gains additive fields
ing.cf_type, ing.cf_subcategory, ing.display_group. The helper reads these; it must tolerate
their absence (cold cache, D-07).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote CATEGORY_DISPLAY_NAMES into js/lib/constants.js</name>
  <files>js/lib/constants.js, js/modules/17-search-overlay.js</files>
  <read_first>
    - js/lib/constants.js (full — see existing var + module.exports pattern at L43-50)
    - js/modules/17-search-overlay.js L50-115 (source map L54-73 + groupResultsByCategory consumer)
    - tests/frontend/17-search-overlay.test.js L1-70 (confirm what the existing test relies on)
  </read_first>
  <behavior>
    - js/lib/constants.js declares top-level `var CATEGORY_DISPLAY_NAMES = {...}` containing every key currently in search-overlay PLUS 'Ingredient' and 'Cleaning/Sanitization' keys.
    - js/lib/constants.js module.exports block includes CATEGORY_DISPLAY_NAMES.
    - js/modules/17-search-overlay.js no longer declares its own local CATEGORY_DISPLAY_NAMES var; it uses the global one from constants.js (loaded first on every page). Its groupResultsByCategory still works.
    - Existing search-overlay Jest test still passes (add a require of constants.js in that test ONLY if the test currently relied on the module-local copy and now fails — do not otherwise modify existing tests).
  </behavior>
  <action>
    Move the `CATEGORY_DISPLAY_NAMES` object verbatim from js/modules/17-search-overlay.js (L54-73) into js/lib/constants.js as a top-level `var` alongside CART_KEYS/ITEM_TYPES/PRODUCT_TABS/KIT_CATEGORIES. Extend it with two missing cf_type-value keys: `'Ingredient': 'Ingredient'` and `'Cleaning/Sanitization': 'Cleaning/Sanitization'`. Add `CATEGORY_DISPLAY_NAMES: CATEGORY_DISPLAY_NAMES` to the constants.js module.exports block (L43-50 pattern). In search-overlay, delete the now-duplicate local declaration so it references the global (constants.js loads before search-overlay's concatenated module). constants.js is loaded first on admin.html, kiosk.html, brewpad.html, and is listed first in the concat:js build pipeline, so the global is available everywhere. If the existing tests/frontend/17-search-overlay.test.js fails because it depended on the module-local copy, add `require('../../js/lib/constants.js')` at the top of that test to populate the global — this is the only permitted test edit.
  </action>
  <verify>
    <automated>npm test -- tests/frontend/17-search-overlay.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "var c=require('./js/lib/constants.js'); if(!c.CATEGORY_DISPLAY_NAMES) process.exit(1); if(c.CATEGORY_DISPLAY_NAMES['Cleaning/Sanitization']!=='Cleaning/Sanitization') process.exit(2); if(c.CATEGORY_DISPLAY_NAMES['Grain']!=='Grains') process.exit(3);"` exits 0
    - `grep -c "var CATEGORY_DISPLAY_NAMES" js/modules/17-search-overlay.js` returns 0 (local copy removed)
    - npm test on 17-search-overlay.test.js exits 0
  </acceptance_criteria>
  <done>CATEGORY_DISPLAY_NAMES lives in js/lib/constants.js (with the two added keys), search-overlay uses the global, and the search-overlay test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create js/lib/recipe-grouping.js shared helper + Jest suite</name>
  <files>js/lib/recipe-grouping.js, tests/frontend/recipe-grouping.test.js</files>
  <read_first>
    - js/modules/17-search-overlay.js L85-110 (groupResultsByCategory analog — build-then-emit skeleton)
    - js/lib/utils.js L1-19 (file header + module.exports idiom)
    - js/lib/constants.js (CATEGORY_DISPLAY_NAMES after Task 1)
    - tests/frontend/17-search-overlay.test.js L1-70 (test harness structure to mirror)
    - .planning/phases/34-ingredient-display-server-enrichment/34-PATTERNS.md (grouping helper section + test patterns section)
  </read_first>
  <behavior>
    - Test: empty/falsy input -> returns []
    - Test: ingredients with cf_type 'Packaging' and cf_subcategory absent -> single flat 'Packaging' section (D-02, one subcat => no nesting)
    - Test: cf_type 'Ingredient' with cf_subcategory values Grain+Hops+Yeast -> nested sections in order Grain->Hops->Yeast (D-01, D-02, D-03)
    - Test: sections always emit in SECTION_ORDER regardless of input order (D-03)
    - Test: two ingredients in the same section preserve their input array order (D-05)
    - Test: ingredient with no cf_type AND no cf_subcategory -> goes to 'Other' section, emitted last (D-06)
    - Test: ingredients that ALL lack cf_type/cf_subcategory fields entirely (cold cache) -> one flat group containing every ingredient in original order (D-07)
    - Test: every emitted group object has a numeric count equal to its items length (D-11)
    - Test: section labels run through CATEGORY_DISPLAY_NAMES (e.g. raw 'Grain' -> header label 'Grains') (D-04)
  </behavior>
  <action>
    Create js/lib/recipe-grouping.js (ES5/var). Copy the file-header + `if (typeof module !== 'undefined' && module.exports)` idiom from js/lib/utils.js. Define a single fixed `var SECTION_ORDER = ['Grain','Hops','Yeast','Additive','Packaging','Equipment','Cleaning/Sanitization']` (D-03) — this is the ordering authority for the whole feature; 'Other' (D-06) always renders after every named section. Implement `function groupRecipeIngredients(ingredients)` returning an array of group objects shaped `{ label: <CATEGORY_DISPLAY_NAMES[raw]||raw>, count: items.length, items: [<ingredient>] }`. Reference the global CATEGORY_DISPLAY_NAMES (in-browser) with a Jest-safe fallback: at top of file do `var CATEGORY_DISPLAY_NAMES = (typeof module!=='undefined'&&module.exports) ? require('./constants.js').CATEGORY_DISPLAY_NAMES : (typeof window!=='undefined' ? window.CATEGORY_DISPLAY_NAMES : CATEGORY_DISPLAY_NAMES);` — match whatever resolution the repo already uses for cross-lib references; the key requirement is it works in both node (Jest) and browser. Grouping rules: (1) D-07 cold-cache check first — if NO ingredient has either cf_type or cf_subcategory, return one flat group `[{label:'', count:n, items:ingredients}]` (label empty so surfaces render their existing flat list, see Plan 03). (2) Otherwise bucket top-level by cf_type; within a cf_type collect distinct cf_subcategory values; nest (emit a section per cf_subcategory) ONLY when 2+ distinct subcats exist (D-02), else emit one section for that cf_type. (3) Resolve each section's raw key (subcategory when nested, else cf_type) and order sections by SECTION_ORDER index; unknown/missing keys -> 'Other' bucket appended last (D-06). (4) Preserve recipe-entry order within each section by iterating `ingredients` once in order and pushing into buckets, never re-sorting items (D-05). (5) Each group carries count=items.length (D-11). Use `readGroupKey` logic that treats empty-string cf_type/cf_subcategory as missing. Then create tests/frontend/recipe-grouping.test.js mirroring tests/frontend/17-search-overlay.test.js structure, covering every behavior bullet above; require('../../js/lib/constants.js') before requiring the helper so the label map is populated.
  </action>
  <verify>
    <automated>npm test -- tests/frontend/recipe-grouping.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - js/lib/recipe-grouping.js exports groupRecipeIngredients via module.exports
    - `node -e "var g=require('./js/lib/recipe-grouping.js').groupRecipeIngredients; if(typeof g!=='function') process.exit(1); if(g([]).length!==0) process.exit(2);"` exits 0
    - npm test on tests/frontend/recipe-grouping.test.js exits 0 with all behavior-bullet tests present and passing
    - Test asserting cold-cache flat-group (D-07) is present and passing
    - Test asserting 'Other'-last (D-06) is present and passing
    - Test asserting fixed SECTION_ORDER (D-03) is present and passing
  </acceptance_criteria>
  <done>groupRecipeIngredients implements D-01/D-02/D-03/D-05/D-06/D-07/D-11, reuses CATEGORY_DISPLAY_NAMES (D-04), and the Jest suite passes green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog cache -> grouping helper | enriched fields (cf_type/cf_subcategory/display_group) flow in; helper only reads/reorders, never re-serializes the money path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | groupRecipeIngredients reordering ingredient array | mitigate | Helper returns NEW group wrapper objects referencing the same ingredient objects; it does not mutate or drop ingredients — pos-recipe.js/lib/pricing.js continue to consume the untouched flat array (D-08). Test asserts no ingredient is dropped. |
| T-34-02 | Information Disclosure | section labels | accept | Labels are non-sensitive customer-facing category names already shown site-wide; no new data exposed. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; if any install is attempted, stop and route through legitimacy checkpoint. |
</threat_model>

<verification>
- `npm test -- tests/frontend/recipe-grouping.test.js` exits 0
- `npm test -- tests/frontend/17-search-overlay.test.js` exits 0
- `npm run lint` exits 0
</verification>

<success_criteria>
- CATEGORY_DISPLAY_NAMES is shared from js/lib/constants.js (D-04) and search-overlay reuses it with no behavior change
- js/lib/recipe-grouping.js exists, exports groupRecipeIngredients, implements the D-01..D-07/D-11 rules with SECTION_ORDER defined once (D-03, D-09)
- Frontend Jest suite for the helper passes green; lint clean
</success_criteria>

<output>
Create `.planning/phases/34-ingredient-display-server-enrichment/34-01-SUMMARY.md` when done
</output>
