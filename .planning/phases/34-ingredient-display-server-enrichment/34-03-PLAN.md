---
phase: 34-ingredient-display-server-enrichment
plan: 03
type: execute
wave: 2
depends_on: ["34-01", "34-02"]
files_modified:
  - js/admin.js
  - js/brewpad.js
  - js/kiosk.js
  - js/main.js
  - js/main.min.js
autonomous: false
requirements: [RDISP-01, RDISP-03]
must_haves:
  truths:
    - "Admin recipe ingredient table renders labelled group-header rows by cf_type/cf_subcategory with per-group counts, brewing-process order (RDISP-01, D-03, D-11)"
    - "BrewPad buildRecipeIngredientTable renders the same grouped sections (RDISP-03, D-10)"
    - "Kiosk recipe ingredient list renders grouped sub-headings + lists, matching admin grouping (RDISP-03, D-10)"
    - "All three surfaces call the single shared groupRecipeIngredients helper (D-09) — no per-surface grouping logic"
    - "Editable admin/BrewPad rows keep a flat running index across group headers so edit read-back (data-ing-idx / data-idx) still maps to the correct array position"
    - "Cold-cache / un-enriched ingredients (helper returns one flat group with empty label) render exactly the existing flat list (D-07)"
  artifacts:
    - path: "js/admin.js"
      provides: "grouped recipe-ingredient table via groupRecipeIngredients"
      contains: "groupRecipeIngredients"
    - path: "js/brewpad.js"
      provides: "grouped buildRecipeIngredientTable"
      contains: "groupRecipeIngredients"
    - path: "js/kiosk.js"
      provides: "grouped kiosk recipe ingredient render (single shared local fn for both paths)"
      contains: "groupRecipeIngredients"
  key_links:
    - from: "js/admin.js / js/brewpad.js / js/kiosk.js"
      to: "groupRecipeIngredients (js/lib/recipe-grouping.js)"
      via: "global function call (recipe-grouping.js loaded via <script> on each page)"
      pattern: "groupRecipeIngredients\\("
---

<objective>
Wire the shared grouping helper (Plan 01) into all three recipe-ingredient render surfaces so admin, BrewPad, and kiosk display ingredients in identical labelled sections (RDISP-01, RDISP-03). Each surface keeps its native styling (D-10), shows per-group counts (D-11), preserves edit-readback indexing, and falls back to today's flat list when ingredients are un-enriched (D-07). A human checkpoint verifies the three surfaces visually.

Purpose: Closes the display requirements by consuming the one source of truth — no per-surface grouping logic.
Output: grouped renders in admin.js, brewpad.js, kiosk.js + rebuilt main.js/main.min.js + an iPad-Safari-aware human verify of all three surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-ingredient-display-server-enrichment/34-CONTEXT.md
@.planning/phases/34-ingredient-display-server-enrichment/34-PATTERNS.md
@CLAUDE.md
@.planning/phases/34-ingredient-display-server-enrichment/34-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: js/lib/recipe-grouping.js exports groupRecipeIngredients(ingredients).
     Returns [{ label, count, items: [<ingredient>] }]. A cold-cache / un-enriched input
     returns a SINGLE group with an empty-string label (surfaces render their existing flat
     list in that case, D-07). -->

recipe-grouping.js must be loaded via a <script> tag on each standalone page BEFORE the page
script, AFTER constants.js (needs CATEGORY_DISPLAY_NAMES). Pages: admin.html, kiosk.html,
brewpad.html (constants.js already loaded first per CONTEXT). escapeHTML is already in scope
on all three (js/lib/utils.js).

ADMIN analog: js/admin.js renderIngredientRows (L8745) — EDITABLE recipe builder table.
Rows are <tr class="recipes-ing-row" data-ing-idx="IDX"> where IDX is the position in the
original ingredients array, consumed by attachIngredientRowListeners + edit read-back. Keep a
flat running counter across group-header rows so data-ing-idx stays = original array index.
Group-header rows are extra <tr> with a colspan cell; they must not advance the ingredient index
and must not break the totals <tfoot> (L8795-8805) or listener attachment (L8808).

BREWPAD analog: js/brewpad.js buildRecipeIngredientTable (L3024) — returns HTML string for
bp-recipe-ing-table. editable branch uses data-idx tied to readIngredientTableEdits
(L3045-3057) which maps data-idx back to snapIngredients[idx]. Keep a flat running index across
group headers. Empty guard at L3025 stays. When helper returns the single empty-label flat group,
render exactly today's flat table.

KIOSK analog: js/kiosk.js L1205-1261 — TWO duplicated render blocks (cached _fetchedDetail path
L1211-1218 and fetch path L1232-1240), both build <strong>Ingredients:</strong><ul>...<li>name —
qty unit</li></ul>. Extract ONE local kioskRenderRecipeIngredients(ingredients, el) and call from
both paths. Non-editable, so no index concern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Group admin + BrewPad editable ingredient tables</name>
  <files>js/admin.js, js/brewpad.js</files>
  <read_first>
    - js/admin.js L8744-8815 (renderIngredientRows + attachIngredientRowListeners start)
    - js/brewpad.js L3024-3057 (buildRecipeIngredientTable + readIngredientTableEdits)
    - js/lib/recipe-grouping.js (groupRecipeIngredients return shape, from Plan 01)
    - .planning/phases/34-ingredient-display-server-enrichment/34-PATTERNS.md (admin.js + brewpad.js sections — flat-index warning)
  </read_first>
  <action>
    In js/admin.js renderIngredientRows: keep the empty-state and availMap logic. Replace the single `ingredients.forEach((ing, idx) => row)` build with: call `groupRecipeIngredients(ingredients)`, then iterate groups; for a group whose label is non-empty, emit a header `<tr class="recipes-ing-group">` with one `<td colspan="8">` containing `escapeHTML(label) + ' (' + group.count + ')'` (D-11); for each ingredient in the group emit the EXISTING per-ingredient `<tr class="recipes-ing-row" data-ing-idx="FLATIDX">` row unchanged, where FLATIDX is a running counter incremented once per ingredient (NOT per group) so it equals the ingredient's position in the original `ingredients` array — preserving attachIngredientRowListeners/edit read-back. Keep totalCost/totalRetail accumulation and the <tfoot> totals (L8795-8805) and the attachIngredientRowListeners() call (L8808) exactly as-is. When the helper returns the single empty-label flat group, the header row is skipped (label empty) so the table renders as today (D-07). Apply the equivalent change in js/brewpad.js buildRecipeIngredientTable: keep the empty guard and table head; iterate groupRecipeIngredients(ingredients); emit a group-header `<tr>` with a `<td colspan="3">label (count)</td>` for non-empty labels, then the existing per-ingredient `<tr>` (editable branch uses data-idx) with a flat running index across groups so readIngredientTableEdits still maps data-idx -> snapIngredients[idx]. Use escapeHTML for all labels. Do not edit readIngredientTableEdits or attachIngredientRowListeners.
  </action>
  <verify>
    <automated>grep -c "groupRecipeIngredients(" js/admin.js js/brewpad.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupRecipeIngredients(" js/admin.js` returns >=1 and same for js/brewpad.js
    - `grep -n "data-ing-idx" js/admin.js` still present; flat running index is incremented per-ingredient, not per-group (verify by reading the new loop)
    - readIngredientTableEdits and attachIngredientRowListeners are unmodified (no diff in those functions)
    - Group-header rows use colspan and are skipped when group label is empty (flat fallback, D-07)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Admin and BrewPad recipe ingredient tables render grouped sections with counts via the shared helper, edit-readback indices intact, flat fallback preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Group kiosk recipe ingredient list (single shared local render) + build</name>
  <files>js/kiosk.js, js/main.js, js/main.min.js</files>
  <read_first>
    - js/kiosk.js L1205-1261 (both duplicated render blocks)
    - js/lib/recipe-grouping.js (return shape)
    - kiosk.html (confirm constants.js load order; ensure recipe-grouping.js script tag present)
    - admin.html, brewpad.html (confirm recipe-grouping.js script tag present, after constants.js, before page script)
    - .planning/phases/34-ingredient-display-server-enrichment/34-PATTERNS.md (kiosk section)
    - CLAUDE.md (build pipeline — npm run build regenerates main.js/main.min.js; never hand-edit them)
  </read_first>
  <action>
    In js/kiosk.js, extract a single local `function kioskRenderRecipeIngredients(ingredients, el)` that calls `groupRecipeIngredients(ingredients)` and builds, per group, a `<strong>label (count)</strong>` sub-heading followed by the existing `<ul style="margin:0.25rem 0;padding-left:1.25rem;"><li>name — qty unit</li></ul>` block (D-10 keeps the kiosk ul/li idiom + inline styles); when the group label is empty (flat fallback, D-07) render `<strong>Ingredients:</strong>` once with a single ul, matching today's output exactly. Replace BOTH duplicated render blocks (the cached _fetchedDetail path L1211-1218 and the fetch path L1232-1240) with calls to this one helper, passing the resolved ingredients array and the target element. Use escapeHTML on names/units as today. Add a `<script src="js/lib/recipe-grouping.js"></script>` tag to kiosk.html, admin.html, and brewpad.html if not already present — placed AFTER js/lib/constants.js and BEFORE the page script (kiosk.js/admin.js/brewpad.js). recipe-grouping.js is loaded ONLY via this standalone `<script>` tag on the three standalone pages; do NOT add it to the `concat:js` pipeline (it would then be double-loaded inside main.js on public pages where it is not needed). After all JS module/lib changes across this phase, run `npm run build` to regenerate js/main.js and js/main.min.js (per CLAUDE.md rule 9). Do not hand-edit main.js/main.min.js.
  </action>
  <verify>
    <automated>grep -c "kioskRenderRecipeIngredients\|groupRecipeIngredients(" js/kiosk.js && grep -rc "recipe-grouping.js" admin.html kiosk.html brewpad.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "groupRecipeIngredients(" js/kiosk.js` returns >=1
    - Both former duplicated kiosk render blocks now call the single kioskRenderRecipeIngredients helper
    - `grep -c "recipe-grouping.js" admin.html` , kiosk.html, brewpad.html each return >=1, positioned after constants.js
    - `npm run build` exits 0 and regenerates js/main.js + js/main.min.js
    - `npm test` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>Kiosk renders grouped recipe ingredients via one shared local helper consuming groupRecipeIngredients; recipe-grouping.js is loaded on all three pages; build regenerated; frontend tests + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Grouped recipe-ingredient display on all three surfaces (admin recipe builder table, BrewPad recipe ingredient table, kiosk recipe ingredient list), all driven by the single shared groupRecipeIngredients helper, with per-group counts and brewing-process section order.</what-built>
  <how-to-verify>
    1. Run the site locally (or push to staging per the staging-first rule) and open the admin recipe view for a recipe that has ingredients spanning multiple types (e.g. grain + hops + yeast + packaging). Confirm: labelled section headers appear in order Grains -> Hops -> Yeast -> Additives -> Packaging -> Equipment -> Cleaning/Sanitization, each header shows "Label (count)", and editing a quantity in any row then saving still updates the correct ingredient (edit-readback index intact).
    2. Open the same recipe in BrewPad (recipe attach panel / recipe view). Confirm the SAME section labels, SAME order, SAME counts as admin.
    3. Open the kiosk recipe selection for the same recipe. Confirm grouped sub-headings with counts, matching admin/BrewPad order and labels.
    4. Open a recipe whose ingredients are all one type (e.g. all packaging) — confirm it renders flat with no redundant single-child nesting (D-02).
    5. Verify no console errors on each surface (check iPad Safari for kiosk/BrewPad per project norms).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any grouping/order/label/index discrepancies.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| enriched ingredient array -> DOM render | client renders grouped HTML; all dynamic text must be escaped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06 | Tampering (XSS) | group-header labels + ingredient names in rendered HTML | mitigate | All labels, names, units rendered through escapeHTML (already in scope from js/lib/utils.js) — same idiom as the existing rows. |
| T-34-07 | Tampering | editable-row index integrity | mitigate | Flat running ingredient index preserved across group headers so edit read-back maps to the correct array position; no money/quantity field is re-derived from the grouped view. |
| T-34-08 | Information Disclosure | grouped display | accept | No new fields surfaced beyond the cf_type/cf_subcategory/display_group already enriched in Plan 02; no cost/internal fields newly exposed. |
| T-34-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages; if an install is attempted, stop and route through legitimacy checkpoint. |
</threat_model>

<verification>
- `npm test` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0, regenerates main.js/main.min.js
- Human checkpoint: all three surfaces show identical grouping/order/labels/counts; admin/BrewPad edit-readback intact; flat fallback for single-type recipes
</verification>

<success_criteria>
- Admin (RDISP-01), BrewPad and kiosk (RDISP-03) render grouped recipe ingredients via the single shared helper (D-09)
- Per-group counts (D-11), brewing-process order (D-03), native per-surface styling (D-10)
- Editable admin/BrewPad index read-back preserved; cold-cache flat fallback preserved (D-07)
- Build regenerated; frontend tests + lint green; human-verified across surfaces
</success_criteria>

<output>
Create `.planning/phases/34-ingredient-display-server-enrichment/34-03-SUMMARY.md` when done
</output>
