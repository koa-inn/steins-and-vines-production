---
phase: 49-online-captured-amount-verification
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified: []
autonomous: false
requirements: [MONEY-01]
user_setup: []

must_haves:
  truths:
    - "A real small-value online HelcimPay order on staging books the invoice as paid normally — proving getCardTransactionById returns the ACTUAL captured amount for an ONLINE (not just terminal) transaction, so the new fail-closed check does not void legit orders."
    - "A tampered attempt (initialize with a low amount, then a larger order) is voided and rejected with a 4xx on the live path — H2 is closed end-to-end, not just under mocks."
  artifacts:
    - path: ".planning/phases/49-online-captured-amount-verification/49-02-UAT.md"
      provides: "Recorded live-card UAT results (legit-books-paid + tamper-voided), with real Helcim txn ids / Zoho invoice numbers."
  key_links:
    - from: "live /api/checkout (staging)"
      to: "helcimLib.getCardTransactionById"
      via: "authoritative captured-amount readback on a real online HelcimPay transaction"
      pattern: "getCardTransactionById"
---

<objective>
Confirm on the LIVE money path what mocks cannot: that `helcimLib.getCardTransactionById(transactionId)` returns the real captured amount for an **online HelcimPay** transaction (plan 49-01's regression proves the logic under mocks; the terminal webhook's live use of this call (STATE 36-20/F2) covers the card-PRESENT path, but the online HelcimPay path readback is unverified in prod). This matters because the check fails CLOSED: if the online readback returned a wrong/zero/unavailable amount, EVERY legit online paid order would be voided and rejected. This checkpoint de-risks that blast radius before production.

Purpose: prove H2 is closed end-to-end and the fix does not break legit online checkout.
Output: `49-02-UAT.md` recording a real legit order (books paid) and a real tamper attempt (voided + 4xx).

Scope: staging only (per CLAUDE.md — all changes go to staging first; middleware runs on prod Railway but the site under test is staging). No code changes here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-online-captured-amount-verification/49-01-PLAN.md
@zoho-middleware/routes/checkout.js
@zoho-middleware/lib/helcim.js
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-card UAT — legit order books paid, tampered order voided</name>
  <files>.planning/phases/49-online-captured-amount-verification/49-02-UAT.md</files>
  <action>Deploy 49-01 to staging, then run the two live-card scenarios described in how-to-verify (LEGIT small order → books paid, no false-void; TAMPER initialize(low)+larger order → 4xx + void) and record both real outcomes — Helcim txn ids, Zoho invoice numbers, void confirmations, owner cleanup owed — in 49-02-UAT.md. This is human-only: it requires a physical card in the HelcimPay iframe, so there is no CLI/API substitute. Do NOT promote to production if the LEGIT path false-voids.</action>
  <read_first>
    - .planning/phases/49-online-captured-amount-verification/49-01-SUMMARY.md (what shipped: the getCardTransactionById readback + ±$0.01 tolerance + 402-on-mismatch)
    - zoho-middleware/routes/checkout.js (the verification block + catch void path — so the operator knows exactly what to observe in logs)
    - zoho-middleware/lib/helcim.js (getCardTransactionById — confirm the `amount` field is populated for the online txn during the UAT)
    - docs/RUNBOOK.md § Phase 45/46 (staging deploy + live-card UAT precedent, e.g. 45-09) if present
  </read_first>
  <what-built>
    49-01 added a captured-amount verification to online `/api/checkout`: before recording the Zoho customerpayment it reads the real captured amount via `getCardTransactionById(transactionId)` and, on a short/unverifiable capture, voids the charge via `moneyPath.voidWithTimeout` and returns 402. This checkpoint verifies it on the live staging path with a real card.
  </what-built>
  <how-to-verify>
    Deploy 49-01 to staging middleware per the project deploy process, then, using a REAL card on the staging checkout (small amounts):

    1. LEGIT PATH — place a normal small online order (e.g. a single low-price item) and pay in full via the HelcimPay iframe.
       Expected: order returns 201; the Zoho invoice is created AND marked paid-in-full (customerpayment recorded); server logs show the getCardTransactionById readback with a captured amount == the invoice total (within $0.01); NO void fired. Record the Helcim txn id + Zoho invoice number.

    2. TAMPER PATH — reproduce H2 against staging: call `POST /api/payment/initialize` with a low amount (e.g. {amount: 0.01}), complete the HelcimPay charge for that low amount, then submit `/api/checkout` for the real (larger) cart using that transaction_id/payment_token.
       Expected: response is a 4xx (402); NO customerpayment recorded and the invoice is NOT marked paid; server logs show the captured-vs-recorded mismatch and a `moneyPath.voidWithTimeout` void firing on the txn; confirm in the Helcim dashboard that the low charge was reversed/voided. Record the txn id + the void outcome.

    3. Record both outcomes (txn ids, invoice numbers, void confirmations, any owner cleanup owed — e.g. refund/reverse the tiny test charges) in `.planning/phases/49-online-captured-amount-verification/49-02-UAT.md`.

    If the LEGIT path unexpectedly voids/4xx's (getCardTransactionById did NOT return a usable amount for the online txn): STOP — do not promote to production; this is a gap to feed back to planning (the readback contract differs for online HelcimPay and the fix needs adjustment, e.g. an alternate amount source or a scoped fail-open).
  </how-to-verify>
  <verify>
    <human-check>49-02-UAT.md records a real legit order that booked paid-in-full with no false-void, AND a real tamper attempt that returned 4xx + voided the charge + recorded no customerpayment.</human-check>
  </verify>
  <acceptance_criteria>
    - `49-02-UAT.md` exists and records: (1) a real legit order that booked paid-in-full with the captured amount matching the invoice total and no void; (2) a real tamper attempt that returned 4xx, recorded no customerpayment, and voided the charge.
    - The legit path did NOT false-void — confirming online getCardTransactionById returns the real captured amount.
    - Owner cleanup for the tiny live test charges is noted (refund/reverse + any Zoho invoice reversal), consistent with prior live-UAT hygiene (STATE "Cleanup owed").
  </acceptance_criteria>
  <done>Both live outcomes recorded in 49-02-UAT.md; legit path did not false-void; tamper path voided + 4xx; go/no-go for production explicit.</done>
  <resume-signal>Type "approved" once both live outcomes are recorded, or describe the discrepancy (especially a false-void on the legit path) to route back to planning.</resume-signal>
</task>

</tasks>

<verification>
- 49-02-UAT.md present with both real outcomes recorded.
- Legit online order books paid (no false-void) — the fail-closed check is safe for production.
- Tamper attempt is voided + 4xx on the live path — H2 closed end-to-end.
</verification>

<success_criteria>
- Online getCardTransactionById captured-amount readback confirmed live (legit order unaffected).
- H2 tamper confirmed voided + rejected on staging with a real card.
- Owner cleanup for test charges recorded; go/no-go for production is explicit.
</success_criteria>

<output>
Create `.planning/phases/49-online-captured-amount-verification/49-02-SUMMARY.md` when done (link the 49-02-UAT.md results).
</output>
