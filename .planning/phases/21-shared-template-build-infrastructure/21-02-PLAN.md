---
phase: 21-shared-template-build-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - test-subpage.html
  - package.json
  - tests/frontend/16-catalog-subpage.test.js
autonomous: true
requirements:
  - TPL-01
  - TPL-02
  - TPL-03
  - TPL-04
  - BUILD-01

must_haves:
  truths:
    - "npm run build completes without errors including the new CSS and JS files"
    - "test-subpage.html loads and renders a product grid filtered to Grain subcategory"
    - "Unit tests for filterItemsByConfig and buildSortComparator pass"
    - "Cache-bust stamps are updated in test-subpage.html on build"
  artifacts:
    - path: "test-subpage.html"
      provides: "Integration test page for catalog subpage module"
      contains: "SUBPAGE_CONFIG"
    - path: "tests/frontend/16-catalog-subpage.test.js"
      provides: "Unit tests for pure functions in 16-catalog-subpage.js"
      contains: "filterItemsByConfig"
    - path: "package.json"
      provides: "Updated build scripts for new CSS/JS files"
      contains: "catalog-subpage"
    - path: "js/modules/16-catalog-subpage.min.js"
      provides: "Minified JS module (build artifact)"
    - path: "css/catalog-subpage.min.css"
      provides: "Minified CSS (build artifact)"
  key_links:
    - from: "test-subpage.html"
      to: "js/modules/16-catalog-subpage.min.js"
      via: "script tag"
      pattern: "16-catalog-subpage\\.min\\.js"
    - from: "test-subpage.html"
      to: "css/catalog-subpage.min.css"
      via: "link tag"
      pattern: "catalog-subpage\\.min\\.css"
    - from: "package.json"
      to: "js/modules/16-catalog-subpage.js"
      via: "terser minify:js script"
      pattern: "16-catalog-subpage"
---

<objective>
Create the test HTML page, unit tests for pure functions, and integrate all new files into the build pipeline. This wires together Plan 01's module and CSS with the build system and provides verification that everything works end-to-end.

Purpose: Proves the template works before Phase 22 creates real category pages. Ensures BUILD-01 is satisfied.
Output: `test-subpage.html`, `tests/frontend/16-catalog-subpage.test.js`, updated `package.json`, minified build artifacts
</objective>

<execution_context>
@/Users/koa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/koa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-shared-template-build-infrastructure/21-01-SUMMARY.md
@.planning/phases/21-shared-template-build-infrastructure/21-CONTEXT.md
@.planning/phases/21-shared-template-build-infrastructure/21-RESEARCH.md
@.planning/phases/21-shared-template-build-infrastructure/21-PATTERNS.md
@.planning/phases/21-shared-template-build-infrastructure/21-UI-SPEC.md

<interfaces>
<!-- From Plan 01 outputs — executor should read these files -->

From js/modules/16-catalog-subpage.js (module.exports):
  filterItemsByConfig(items, config) — filters items by config.subcategories[] and config.types[], excludes price <= 0
  buildSortComparator(sortMode) — returns comparator function for 'name-asc', 'name-desc', 'price-asc', 'price-desc', 'stock-first'

From package.json (existing build scripts to extend):
  minify:css — cleancss chain, append new entry
  minify:js — terser chain, append new entry
  stamp:pages — file array + regex replacements, extend both
  build — orchestrates all stamp + minify scripts (no change needed to build script itself)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test-subpage.html and unit tests</name>
  <files>test-subpage.html, tests/frontend/16-catalog-subpage.test.js</files>
  <read_first>
    hops.html
    js/modules/16-catalog-subpage.js
    tests/frontend/15-hops.test.js
    .planning/phases/21-shared-template-build-infrastructure/21-PATTERNS.md
    .planning/phases/21-shared-template-build-infrastructure/21-UI-SPEC.md
  </read_first>
  <action>
    Create two files:

    1. test-subpage.html at project root — integration test page modeled on hops.html structure.

    HTML HEAD section:
    - Standard meta charset, viewport, apple-mobile-web-app tags (copy from hops.html)
    - NO Google Tag Manager snippet (test page, not production)
    - NO meta description, og tags, canonical, or JSON-LD (throwaway test page)
    - Content-Security-Policy meta tag: copy from hops.html
    - Title: "Test Subpage | Steins &amp; Vines"
    - Link to css/styles.min.css?v=dev
    - Link to css/catalog-subpage.min.css?v=dev
    - Google Fonts link for Playfair Display, Lato, Oswald (same as other pages)

    BODY section — data-page="test-subpage":
    - Include site header (copy from hops.html — logo, nav, etc.)
    - Include cart sidebar include (same as hops.html)

    INLINE SUBPAGE_CONFIG script block BEFORE main.min.js:
    ```
    window.SUBPAGE_CONFIG = {
      categorySlug: 'grains',
      categoryName: 'Grains',
      heroDescription: 'Base malts, specialty grains, and malt extracts for brewing.',
      heroDescriptionFull: 'Browse our selection of brewing grains in Squamish, BC. We carry base malts, specialty malts, roasted malts, and malt extracts for all-grain and extract brewing. Available for purchase online with in-store pickup.',
      accentColor: '#8b6f3a',
      subcategories: ['Grain', 'Malt Extract'],
      types: [],
      filterGroups: [
        { label: 'Base Malts', values: ['Base Malt'] },
        { label: 'Specialty', values: ['Specialty Malt', 'Roasted Malt'] },
        { label: 'Extracts', values: ['Malt Extract'] }
      ],
      catalogContainerId: 'subpage-catalog'
    };
    ```

    HERO HTML structure (required by JS module):
    - div.subpage-hero containing:
      - h1 (empty — JS populates from config)
      - p.subpage-hero-desc (empty — JS populates)
      - button.subpage-hero-toggle with text "Read more"
      - div.subpage-hero-full (empty — JS populates)

    TOOLBAR HTML structure:
    - div.subpage-toolbar containing:
      - input#subpage-search type="text" placeholder="Search grains..."
      - select#subpage-sort with options: "In Stock First" (value stock-first), "Name (A-Z)" (value name-asc), "Name (Z-A)" (value name-desc), "Price: Low to High" (value price-asc), "Price: High to Low" (value price-desc)
      - div.subpage-view-toggle with two buttons: data-view="grid" (active by default, text "Grid", aria-label "Grid view") and data-view="list" (text "List", aria-label "List view")
      - div#subpage-filter-row (empty — JS populates if filterGroups exist)

    CATALOG CONTAINER:
    - div#subpage-catalog.subpage-catalog-section (empty — JS renders into this)

    FOOTER: copy from hops.html

    SCRIPT LOAD ORDER (per 21-PATTERNS.md test fixture pattern):
    1. js/sheets-config.js?v=dev
    2. js/main.min.js?v=dev
    3. js/modules/16-catalog-subpage.min.js?v=dev

    2. tests/frontend/16-catalog-subpage.test.js — unit tests modeled on 15-hops.test.js.

    Test structure: require the module, test exported pure functions only (no DOM, no fetch mocks).

    describe('filterItemsByConfig'):
    - test: includes items matching subcategory — input items with cf_subcategory 'Grain', config {subcategories: ['Grain'], types: []}, expect length 1
    - test: includes items matching types — input items with cf_type 'Equipment', config {subcategories: [], types: ['Equipment']}, expect length 1
    - test: excludes items with price <= 0 — input item with price_per_unit '0', expect length 0
    - test: excludes items with empty string price — input item with price_per_unit '', expect length 0
    - test: matches either subcategory OR type — item with cf_subcategory 'Fermenter' and cf_type 'Equipment', config {subcategories: ['Fermenter'], types: ['Equipment']}, expect length 1
    - test: returns empty for no matches — items with cf_subcategory 'Grain', config {subcategories: ['Yeast'], types: []}, expect length 0
    - test: returns empty for empty items array — expect [].length 0
    - test: handles items with subcategory field (not cf_subcategory) — item with subcategory 'Grain' (no cf_ prefix), config {subcategories: ['Grain'], types: []}, expect length 1
    - test: handles items with type field (not cf_type) — item with type 'Equipment', config {subcategories: [], types: ['Equipment']}, expect length 1

    describe('buildSortComparator'):
    - test: name-asc sorts alphabetically — items ['Cascade', 'Amarillo'], sorted result first is 'Amarillo'
    - test: name-desc sorts reverse alphabetically — items ['Amarillo', 'Cascade'], sorted result first is 'Cascade'
    - test: price-asc sorts by price ascending — items with price_per_unit ['15.00', '5.00'], sorted first has price '5.00'
    - test: price-desc sorts by price descending — items with price_per_unit ['5.00', '15.00'], sorted first has price '15.00'
    - test: stock-first puts in-stock items before out-of-stock — items with stock ['0', '5'], sorted first has stock '5'
    - test: stock-first then name-asc within groups — two in-stock items ['Cascade', 'Amarillo'], sorted first is 'Amarillo'
    - test: returns identity comparator for unknown sort mode — items retain original order
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="16-catalog-subpage" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test-subpage.html exists at project root
    - test-subpage.html contains window.SUBPAGE_CONFIG with categorySlug 'grains'
    - test-subpage.html contains div.subpage-hero with h1, p.subpage-hero-desc, button.subpage-hero-toggle
    - test-subpage.html contains div.subpage-toolbar with input#subpage-search, select#subpage-sort, div.subpage-view-toggle, div#subpage-filter-row
    - test-subpage.html contains div#subpage-catalog.subpage-catalog-section
    - test-subpage.html script load order: sheets-config.js, main.min.js, 16-catalog-subpage.min.js
    - test-subpage.html contains SUBPAGE_CONFIG script block BEFORE main.min.js script tag
    - test-subpage.html does NOT contain Google Tag Manager snippet
    - tests/frontend/16-catalog-subpage.test.js exists
    - Test file requires ../../js/modules/16-catalog-subpage.js
    - Test file has describe blocks for filterItemsByConfig and buildSortComparator
    - `npm test -- --testPathPattern="16-catalog-subpage"` exits 0 with all tests passing
    - At least 16 test cases across both describe blocks
  </acceptance_criteria>
  <done>Test HTML page with all required DOM structure exists. Unit tests for pure functions all pass.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate build pipeline for new CSS and JS files</name>
  <files>package.json</files>
  <read_first>
    package.json
    test-subpage.html
    .planning/phases/21-shared-template-build-infrastructure/21-PATTERNS.md
    .planning/phases/21-shared-template-build-infrastructure/21-RESEARCH.md
  </read_first>
  <action>
    Update package.json build scripts to handle the new CSS and JS files. Four specific edits:

    1. minify:css script — append at end of the existing chain:
       && cleancss -o css/catalog-subpage.min.css css/catalog-subpage.css

    2. minify:js script — append at end of the existing terser chain (after 15-hops.js entry):
       && terser js/modules/16-catalog-subpage.js -o js/modules/16-catalog-subpage.min.js -c -m

    3. stamp:pages script — TWO changes:
       a. Add 'test-subpage.html' to the files array (after 'hops.html' in the list)
       b. Add two new regex replacement lines inside the .forEach loop body:
          f=f.replace(/catalog-subpage\.min\.css\?v=[^"]+/g,'catalog-subpage.min.css?v='+v);
          f=f.replace(/16-catalog-subpage\.min\.js\?v=[^"]+/g,'16-catalog-subpage.min.js?v='+v);

    4. NO changes to the build script itself — it already calls stamp:pages, minify:css, and minify:js

    After editing package.json, run npm run build to verify:
    - css/catalog-subpage.min.css is created
    - js/modules/16-catalog-subpage.min.js is created
    - test-subpage.html has updated ?v= tokens for catalog-subpage.min.css and 16-catalog-subpage.min.js

    Also run the full test suite to verify nothing is broken:
    - npm test (frontend tests)
    - npm run lint (ESLint)
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && test -f css/catalog-subpage.min.css && echo "CSS minified: OK" && test -f js/modules/16-catalog-subpage.min.js && echo "JS minified: OK" && grep -c "catalog-subpage.min.css" test-subpage.html && grep -c "16-catalog-subpage.min.js" test-subpage.html && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json minify:css contains "catalog-subpage.min.css css/catalog-subpage.css"
    - package.json minify:js contains "16-catalog-subpage.js -o js/modules/16-catalog-subpage.min.js"
    - package.json stamp:pages files array includes 'test-subpage.html'
    - package.json stamp:pages contains regex for catalog-subpage.min.css and 16-catalog-subpage.min.js
    - npm run build exits 0 without errors
    - css/catalog-subpage.min.css exists and is non-empty after build
    - js/modules/16-catalog-subpage.min.js exists and is non-empty after build
    - test-subpage.html ?v= tokens are updated (not 'dev') after build
    - npm test exits 0 (all existing + new tests pass)
    - npm run lint exits 0 (no lint errors)
  </acceptance_criteria>
  <done>Build pipeline produces minified artifacts for both new files, stamps the test page with cache-bust tokens, and all tests and lint pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build script -> artifacts | terser/cleancss transform source files into minified output |
| test-subpage.html -> browser | Static HTML served via GitHub Pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Tampering | Build artifacts | accept | Build runs locally; minified files are committed. No supply chain risk — no new packages |
| T-21-06 | Information Disclosure | test-subpage.html | accept | Test page contains no secrets — only product category config. Not indexed (no SEO meta). Can be removed after Phase 22 |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed — all tooling already in devDependencies |
</threat_model>

<verification>
- `npm run build` exits 0
- `test -f css/catalog-subpage.min.css` succeeds
- `test -f js/modules/16-catalog-subpage.min.js` succeeds
- `npm test` exits 0
- `npm run lint` exits 0
- `grep "catalog-subpage" package.json | wc -l` returns at least 4 (minify:css, minify:js, stamp:pages x2)
</verification>

<success_criteria>
- Build pipeline handles all new files without errors
- Minified artifacts exist and are non-empty
- Test page is stamped with cache-bust tokens
- All existing and new tests pass
- ESLint passes
- The test page can be opened in a browser and renders a product grid (manual verification by opening test-subpage.html locally or on staging)
</success_criteria>

<output>
Create `.planning/phases/21-shared-template-build-infrastructure/21-02-SUMMARY.md` when done
</output>
