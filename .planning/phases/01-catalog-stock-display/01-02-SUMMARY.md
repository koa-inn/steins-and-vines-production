---
phase: 01-catalog-stock-display
plan: 02
subsystem: ui
tags: [kiosk, stock, cart, zoho-inventory, confirm-dialog]

# Dependency graph
requires:
  - phase: 01-01
    provides: "kioskItemCategory simplified, __other__ sentinel for category filter"
provides:
  - kioskCheckStockOverflow(product, newQty) — reusable stock overflow helper
  - Stock overflow confirm dialog wired into kioskAddToCart (grid/list click)
  - Stock overflow confirm dialog wired into kioskSetQty (cart +/- buttons and typed input)
  - Existing STOCK-01 (stock display), STOCK-04 (out-of-stock warning), D-09 (cache bust) verified working
affects: [03-catalog-stock-display]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Stock overflow guard: compute newQty BEFORE cart mutation, call helper, return early on cancel"
    - "Increase-only gate: qty > _kioskCart[itemId].qty guard prevents dialog on decrease"
    - "Advisory-only pattern: confirm() allows override — server-side Zoho is the authoritative gate"

key-files:
  created: []
  modified:
    - js/kiosk.js
    - js/kiosk.min.js

key-decisions:
  - "kioskCheckStockOverflow is advisory (not a hard block): staff may know about incoming shipments, server-side Zoho is the authoritative stock gate (T-01-03 accepted)"
  - "weight items and services skip overflow check: weight items are bulk ingredients where Zoho stock may not be precise; services have no physical stock"
  - "stock <= 0 in helper returns true (skip): out-of-stock case is already handled by the two existing confirm() calls at click handlers (D-04)"
  - "qty increase guard in kioskSetQty: only fire dialog when qty > current qty — decreasing should never prompt"

patterns-established:
  - "Pre-mutation overflow pattern: always compute newQty first, call kioskCheckStockOverflow(product, newQty), return early if false"

requirements-completed: [STOCK-01, STOCK-02, STOCK-04]

# Metrics
duration: 4min
completed: 2026-04-28
---

# Phase 01 Plan 02: Stock Overflow Warning Summary

**Stock overflow confirm dialog added to kiosk cart via kioskCheckStockOverflow helper wired into both kioskAddToCart and kioskSetQty, with advisory override allowed**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-28T05:00:17Z
- **Completed:** 2026-04-28T05:04:36Z
- **Tasks:** 2 of 2
- **Files modified:** 2

## Accomplishments
- Added `kioskCheckStockOverflow(product, newQty)` helper after `kioskIsWeightItem` — exempt services, weight items, and already-out-of-stock items (D-04 pattern preserved)
- Wired into `kioskAddToCart`: computes `currentQty + 1` before mutation, shows confirm if overflow, returns early on cancel
- Wired into `kioskSetQty`: fires only on qty increase (`qty > _kioskCart[itemId].qty` guard), returns early on cancel
- Confirm message: `'"name" — only N in stock, cart would have M. Add anyway?'`
- Verified STOCK-01: product cards already display out/low/in-stock labels from `stock_on_hand`
- Verified STOCK-04: both existing out-of-stock `confirm()` calls at grid and list click handlers are untouched
- Verified D-09: `cache.del(KIOSK_PRODUCTS_CACHE_KEY)` present at two sale completion paths in `pos.js`; `KIOSK_PRODUCTS = 'zoho:kiosk-products'` confirmed in `constants.js`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add kioskCheckStockOverflow helper and wire into kioskAddToCart** - `cca87ed` (feat)
2. **Task 2: Wire overflow check into kioskSetQty, verify STOCK-01/STOCK-04/D-09** - `63da886` (feat)

## Files Created/Modified
- `js/kiosk.js` - Added kioskCheckStockOverflow helper (lines ~770), modified kioskAddToCart non-weight branch, modified kioskSetQty else branch
- `js/kiosk.min.js` - Regenerated by `npm run build`

## Decisions Made
- Used advisory confirm (not hard block): staff need override ability for incoming shipments and floor samples
- Helper placement after `kioskIsWeightItem` so it can call that function without forward-reference issues
- `stock <= 0` branch returns `true` (skip) in helper — the two existing out-of-stock confirms are the correct gate for that case, not the overflow helper
- Qty increase guard `qty > _kioskCart[itemId].qty` in `kioskSetQty` — decrease/zero paths must remain silent

## Deviations from Plan

None - plan executed exactly as written.

## Existing Behaviors Verified

| Requirement | Location | Status |
|-------------|----------|--------|
| STOCK-01: stock display on product cards | `kioskRenderProductGrid` lines ~1090-1114 — out/low/in-stock labels | Verified working |
| STOCK-04: out-of-stock confirm dialog | Grid click handler line ~1135, list click handler line ~1213 | Verified untouched |
| D-09: post-sale cache bust | `zoho-middleware/routes/pos.js` lines 343, 661 — `cache.del(KIOSK_PRODUCTS_CACHE_KEY)` | Verified working |

## Issues Encountered
None

## Threat Surface Scan
No new network endpoints, auth paths, file access patterns, or schema changes introduced. `confirm()` is a native browser dialog that does not interpret HTML — XSS via `product.name` is not possible (T-01-04 mitigated by design). Stock levels shown in dialog are already visible on product cards (T-01-05 accepted).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Stock overflow warning complete and wired into both cart entry points
- STOCK-01, STOCK-04, D-09 all verified working
- Ready for Plan 03 (if any) or phase completion

---
*Phase: 01-catalog-stock-display*
*Completed: 2026-04-28*
