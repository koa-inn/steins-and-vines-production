---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - js/modules/12-checkout.js
  - js/modules/16-catalog-subpage.js
  - js/modules/17-search-overlay.js
  - js/sheets-config.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-03, D-46-10]
user_setup: []

must_haves:
  truths:
    - "The leaked MW_API_KEY value is removed from js/sheets-config.js"
    - "No public bundle sends an x-api-key header (checkout POSTs, catalog GET, search GET)"
    - "Public checkout still functions because /bookings, /contacts, /payment/initialize are keyless server-side (46-03)"
  artifacts:
    - path: "js/sheets-config.js"
      provides: "Config with the leaked middleware key removed"
      contains: "CLIENT_ID"
  key_links:
    - from: "js/modules/12-checkout.js"
      to: "keyless public POSTs"
      via: "x-api-key removed from 6 call sites"
      pattern: "x-api-key"
    - from: "js/sheets-config.js"
      to: "public pages"
      via: "MW_API_KEY deleted"
      pattern: "MW_API_KEY"
---

<objective>
Remove the leaked `MW_API_KEY` from `js/sheets-config.js:65` (the CRITICAL exposure, D-46-03) and strip the `x-api-key` header from every public bundle that sends it: `12-checkout.js` (6 POST call sites → `/api/bookings`, `/api/contacts`, `/api/payment/initialize`), and the GET-only `16-catalog-subpage.js` / `17-search-overlay.js` (pure leak removal — those routes are already GET-exempt server-side). Keep the public Google `CLIENT_ID` (not a secret).

Purpose: This is the removal half of "stop shipping the admin key in public JS." The server-side keyless exemptions for the three checkout POSTs are delivered in 46-03; this plan and that one must both ship before cutover or public checkout POSTs would 403 (Pitfall 4). Do NOT run `npm run build` — the rebuild + full-suite gate is 46-09.
Output: modified `12/16/17` modules and `sheets-config.js`; any test asserting the removed key updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md

<interfaces>
sheets-config.js: line 65 `MW_API_KEY: 'a9QKtDV3DtYSFIdWtfAMg9Ry70bHG55QGhyJa9GD3fM='` (+ its comment lines 62-64) → DELETE. line 17 `CLIENT_ID` → KEEP (public OAuth client id; still needed client-side for GIS and server-side as the aud value).
12-checkout.js: 6 call sites (lines 1512, 1928, 1974, 2054, 2086, 2088) each `headers: { 'Content-Type':'application/json', 'x-api-key': MW_API_KEY }` → remove the x-api-key entry only (keep Content-Type). Endpoints: /api/bookings (1512,2088), /api/contacts (1928,2086), /api/payment/initialize (1974,2054).
16-catalog-subpage.js: builds `headers:{ 'x-api-key': apiKey }` from SHEETS_CONFIG.MW_API_KEY (lines ~141-145) → remove header. GET route, already exempt.
17-search-overlay.js: two call sites (~216-218, ~248-252) same shape → remove header. GET routes, exempt.
Server companion (46-03, separate plan): adds /bookings,/contacts,/payment/initialize to both the key guard and requireAllowedReferer exemptions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip x-api-key from public bundles (checkout POSTs + catalog/search GETs)</name>
  <files>js/modules/12-checkout.js, js/modules/16-catalog-subpage.js, js/modules/17-search-overlay.js</files>
  <read_first>
    - js/modules/12-checkout.js (re-grep `grep -n "x-api-key\|MW_API_KEY" js/modules/12-checkout.js` — expect 6 sites)
    - js/modules/16-catalog-subpage.js (~141-145)
    - js/modules/17-search-overlay.js (~216-218, ~248-252)
  </read_first>
  <action>
    In 12-checkout.js remove the `'x-api-key': MW_API_KEY` entry from all 6 fetch header objects (keep `'Content-Type':'application/json'`). Also remove the now-dead module-scope `MW_API_KEY` reference in 12-checkout.js if it is defined/aliased locally (it derives from SHEETS_CONFIG). In 16-catalog-subpage.js remove the `x-api-key` header (and the local `apiKey` derivation if it becomes unused). In 17-search-overlay.js remove the `x-api-key` header from both call sites (and unused local `apiKey`). These GET routes are already server-exempt so no other change is needed. Leave all request URLs, bodies, and flow logic unchanged.
  </action>
  <verify>
    <automated>test $(grep -c "x-api-key" js/modules/12-checkout.js js/modules/16-catalog-subpage.js js/modules/17-search-overlay.js | awk -F: '{s+=$2} END{print s}') -eq 0 && npx eslint js/modules/12-checkout.js js/modules/16-catalog-subpage.js js/modules/17-search-overlay.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "x-api-key" js/modules/12-checkout.js` is 0 (and same for 16, 17)
    - request URLs/bodies for /bookings, /contacts, /payment/initialize are unchanged (only the header removed)
    - `npx eslint` on the three modules reports no errors
  </acceptance_criteria>
  <done>No public bundle sends x-api-key; checkout POST bodies/URLs are intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete MW_API_KEY from sheets-config.js + fix any test asserting it</name>
  <files>js/sheets-config.js</files>
  <read_first>
    - js/sheets-config.js (lines 12-85 object; lines 62-65 MW_API_KEY + comment; line 17 CLIENT_ID)
    - tests/frontend/ (grep for any test referencing MW_API_KEY)
  </read_first>
  <action>
    Delete the `MW_API_KEY` property (line 65) and its explanatory comment (lines 62-64) from sheets-config.js. Keep CLIENT_ID (line 17) and every other config value. Then `grep -rn "MW_API_KEY" tests/` — if any existing test asserts the presence/value of MW_API_KEY, update that assertion to reflect its intended removal (this removal is the explicit security objective of the phase, so amending such an assertion is authorized here despite the general "don't modify tests" rule). Do NOT touch js/sheets-config.example.js unless it also carries a real key value.
  </action>
  <verify>
    <automated>test $(grep -c "MW_API_KEY" js/sheets-config.js) -eq 0 && grep -q "CLIENT_ID" js/sheets-config.js && npx eslint js/sheets-config.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MW_API_KEY" js/sheets-config.js` is 0
    - `CLIENT_ID` remains present in sheets-config.js
    - no test references a now-removed `MW_API_KEY` (grep tests/ returns nothing, or such assertions were updated)
    - `npx eslint js/sheets-config.js` reports no errors
  </acceptance_criteria>
  <done>The leaked key value is gone from the public config; the public OAuth client id remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public git-tracked JS → any browser | The leaked secret was readable by anyone here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-11b | Information Disclosure | leaked MW_API_KEY in public JS (the CRITICAL) | mitigate | value deleted from sheets-config; rotation of API_SECRET_KEY completes the fix (46-10). Note: value persists in git history — rotation is what neutralizes it |
| T-46-25 | Availability | public checkout POSTs 403 after key removal | mitigate | server keyless exemptions for /bookings,/contacts,/payment/initialize land in 46-03 before cutover (Pitfall 4); 46-09 full suite + 46-10 staging verify catch a mismatch |
| T-46-06b | Information Disclosure | keyless /bookings,/contacts referer-only | accept | matches /promo/validate precedent (recorded in 46-03 audit note) |
</threat_model>

<verification>
- `grep -rc "MW_API_KEY\|x-api-key" js/sheets-config.js js/modules/12-checkout.js js/modules/16-catalog-subpage.js js/modules/17-search-overlay.js` → all 0
- `npx eslint js/sheets-config.js js/modules/12-checkout.js js/modules/16-catalog-subpage.js js/modules/17-search-overlay.js` → clean
- (build + full public checkout suite in 46-09; live staging checkout in 46-10)
</verification>

<success_criteria>
- Leaked key removed from public config; no public bundle sends x-api-key; checkout flow untouched except headers; lint clean
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-08-SUMMARY.md` when done.
</output>
