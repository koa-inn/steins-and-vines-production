---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/package.json
  - zoho-middleware/package-lock.json
  - zoho-middleware/lib/deviceToken.js
  - zoho-middleware/lib/session.js
  - zoho-middleware/lib/validateEnv.js
  - zoho-middleware/__tests__/device-token-guard.test.js
  - zoho-middleware/__tests__/session.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-01, D-46-04, D-46-06, D-46-07]
user_setup: []

must_haves:
  truths:
    - "A constant-time device-token comparison rejects wrong/empty tokens and accepts the configured KIOSK_DEVICE_TOKEN"
    - "A session can be created, looked up, and destroyed, backed by Redis, surviving a Redis blip via in-process fallback"
    - "Boot validation knows about STAFF_EMAILS, KIOSK_DEVICE_TOKEN, and SHEETS_CLIENT_ID"
  artifacts:
    - path: "zoho-middleware/lib/deviceToken.js"
      provides: "Constant-time kiosk device-token guard (getKey/matches)"
      exports: ["getKey", "matches"]
    - path: "zoho-middleware/lib/session.js"
      provides: "Redis-backed opaque session store with in-process fallback"
      exports: ["createSession", "getSession", "destroySession"]
    - path: "zoho-middleware/lib/validateEnv.js"
      provides: "Boot env validation including new auth vars"
      contains: "KIOSK_DEVICE_TOKEN"
  key_links:
    - from: "zoho-middleware/lib/session.js"
      to: "zoho-middleware/lib/cache.js"
      via: "cache.get/set/del + cache.isConnected fallback"
      pattern: "require\\(['\"]\\./cache['\"]\\)"
    - from: "zoho-middleware/lib/deviceToken.js"
      to: "process.env.KIOSK_DEVICE_TOKEN"
      via: "constant-time comparison"
      pattern: "timingSafeEqual"
---

<objective>
Create the two net-new backend credential primitives this phase's auth model rests on — a constant-time kiosk **device-token** guard and a Redis-backed **session** store with in-process fallback — and register the new auth env vars in boot validation. Install the two verified npm dependencies (`google-auth-library`, `cookie-parser`) that later plans consume.

Purpose: These primitives are the foundation of the three-tier guard (46-03) and the Google sign-in exchange (46-02). Building and unit-testing them in isolation first keeps the keystone guard plan focused on wiring, not primitive correctness.
Output: `lib/deviceToken.js`, `lib/session.js`, updated `lib/validateEnv.js`, two unit test files, and the two installed dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md

<interfaces>
Analog to REPLICATE for lib/deviceToken.js — zoho-middleware/lib/apiKey.js (verbatim getKey/matches shape, constant-time):
  function getKey() { return process.env.API_SECRET_KEY || process.env.MW_API_KEY || ''; }
  function matches(sent) { key=getKey(); if(!key||typeof sent!=='string') return false; a=Buffer.from(sent); b=Buffer.from(key); if(a.length!==b.length) return false; return crypto.timingSafeEqual(a,b); }
  module.exports = { getKey, matches }

Analog to REPLICATE for lib/session.js fallback — zoho-middleware/lib/cache.js:
  exports get(key)->Promise<obj|null>, set(key,value,ttlSeconds)->Promise, del(key)->Promise, isConnected()->bool
  in-process fallback pattern: inProcessLocks Map consulted only when !connected (acquireLock/acquireInProcessLock)

Package Legitimacy Audit (46-RESEARCH.md): google-auth-library@10.9.0 [OK Approved], cookie-parser@1.4.7 [OK Approved]. Both slopcheck [OK]. Install inside zoho-middleware/ ONLY, never the frontend root.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install auth dependencies and register new env vars</name>
  <files>zoho-middleware/package.json, zoho-middleware/package-lock.json, zoho-middleware/lib/validateEnv.js</files>
  <read_first>
    - zoho-middleware/package.json (confirm current deps; both new packages must NOT already be present)
    - zoho-middleware/lib/validateEnv.js (REQUIRED / REQUIRED_IN_PROD / OPTIONAL array pattern + isProd branch)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md (Package Legitimacy Audit table; Environment Availability table)
  </read_first>
  <action>
    Run `cd zoho-middleware && npm install google-auth-library cookie-parser` (pinning to the audited versions 10.9.0 and 1.4.7 is acceptable; both are pre-approved [OK] in 46-RESEARCH.md's Package Legitimacy Audit, so no legitimacy checkpoint is required). Confirm they land in zoho-middleware/package.json, NOT the repo-root package.json.
    Then edit lib/validateEnv.js to add three new entries mirroring the existing array-of-objects shape: add to REQUIRED_IN_PROD both `STAFF_EMAILS` (desc: "Comma-separated staff Google email allowlist (D-46-07)") and `KIOSK_DEVICE_TOKEN` (desc: "Kiosk device token, constant-time compared (D-46-01)"), and add `SHEETS_CLIENT_ID` (desc: "Google OAuth client ID for server-side aud check (D-46-05)") — value in prod equals the public client id 8605205683-tck2da2tpp03vcbr5etauu9q7kompg3q.apps.googleusercontent.com. Retain the existing `MW_API_KEY` OPTIONAL entry for the dual-accept window (removed at rotation in 46-10). Do NOT remove `API_SECRET_KEY`.
  </action>
  <verify>
    <automated>cd zoho-middleware && node -e "require('google-auth-library');require('cookie-parser');console.log('deps ok')" && grep -q "KIOSK_DEVICE_TOKEN" lib/validateEnv.js && grep -q "STAFF_EMAILS" lib/validateEnv.js && grep -q "SHEETS_CLIENT_ID" lib/validateEnv.js</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/package.json` dependencies include `google-auth-library` and `cookie-parser`
    - repo-root `package.json` is UNCHANGED (no new deps leaked to frontend)
    - `lib/validateEnv.js` contains the tokens `STAFF_EMAILS`, `KIOSK_DEVICE_TOKEN`, `SHEETS_CLIENT_ID`
    - `lib/validateEnv.js` still contains `API_SECRET_KEY` and the `MW_API_KEY` OPTIONAL entry
  </acceptance_criteria>
  <done>Both deps installed under zoho-middleware/; validateEnv registers the three new vars; frontend root package.json untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lib/deviceToken.js constant-time kiosk token guard</name>
  <files>zoho-middleware/lib/deviceToken.js, zoho-middleware/__tests__/device-token-guard.test.js</files>
  <read_first>
    - zoho-middleware/lib/apiKey.js (the verbatim pattern to replicate; full file)
    - zoho-middleware/__tests__/api-key-guard.test.js (env save/restore + fail-closed unit test pattern, lines 112-127)
  </read_first>
  <behavior>
    - matches(token) returns true only when token exactly equals process.env.KIOSK_DEVICE_TOKEN (constant-time)
    - matches() returns false when KIOSK_DEVICE_TOKEN is unset/empty (fail closed)
    - matches() returns false for a non-string argument, wrong-length token, and wrong-value same-length token
    - getKey() reads process.env.KIOSK_DEVICE_TOKEN (no legacy alias)
  </behavior>
  <action>
    Create lib/deviceToken.js as a near-verbatim copy of lib/apiKey.js's structure: `getKey()` returns `process.env.KIOSK_DEVICE_TOKEN || ''` (single env var, no legacy alias since this is net-new), and `matches(sent)` does the identical length-check-then-`crypto.timingSafeEqual(Buffer.from(sent), Buffer.from(key))` comparison, returning false for empty key or non-string input. Export `{ getKey, matches }`. Write device-token-guard.test.js as a unit test (no supertest needed here) that saves/restores `process.env.KIOSK_DEVICE_TOKEN` per test and asserts each behavior above, including the mandatory fail-closed case (no env var → rejects everything).
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/device-token-guard.test.js -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/deviceToken.js` contains `crypto.timingSafeEqual` and reads `KIOSK_DEVICE_TOKEN`
    - `lib/deviceToken.js` does NOT reference `API_SECRET_KEY` or `MW_API_KEY`
    - `npx jest __tests__/device-token-guard.test.js` exits 0
    - test file includes an explicit "no env var configured → matches() returns false" (fail-closed) case
  </acceptance_criteria>
  <done>deviceToken.matches enforces constant-time equality against KIOSK_DEVICE_TOKEN and fails closed; unit tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lib/session.js Redis-backed session store with in-process fallback</name>
  <files>zoho-middleware/lib/session.js, zoho-middleware/__tests__/session.test.js</files>
  <read_first>
    - zoho-middleware/lib/cache.js (get/set/del/isConnected + acquireLock/acquireInProcessLock fallback pattern, lines 64-147, 173-175)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md (lib/session.js illustrative shape, lines 134-158)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md (Finding #5)
  </read_first>
  <behavior>
    - createSession(email) returns a 64-hex-char opaque id (crypto.randomBytes(32).toString('hex')), writes `session:<id>` to cache with { email, createdAt } at TTL 604800s (7 days), and write-through to an in-process Map
    - getSession(id) returns the payload from Redis when connected; when cache.isConnected() is false, returns the in-process Map entry (survives a Redis blip)
    - getSession(unknownId) returns null
    - destroySession(id) removes from both Redis (cache.del) and the in-process Map
    - touchSession(id) (sliding expiry) re-sets the TTL; refresh is coarse — only re-write if >1h since last write (per 46-RESEARCH.md open-question #3 recommendation)
  </behavior>
  <action>
    Create lib/session.js mirroring lib/cache.js's Redis-primary / in-process-Map-fallback style (the exact "check cache.isConnected(), else consult an in-process object" shape used by acquireLock). Session id = `crypto.randomBytes(32).toString('hex')`; Redis key prefix `session:`; TTL constant `7 * 24 * 3600`. The in-process Map (`inProcessSessions`, `Object.create(null)`) is written-through on create and consulted only when `cache.isConnected() === false`, mirroring `inProcessLocks`. Do NOT reimplement Redis access — call `cache.get/set/del`. Implement sliding expiry via a `touchSession(id)` that re-sets the same TTL only when the stored `lastRefresh` is older than 1 hour (coarse refresh to limit Redis writes). Export `{ createSession, getSession, destroySession, touchSession }`. Do NOT HMAC-sign the id — a high-entropy opaque id looked up server-side has no forgeable structure (per 46-RESEARCH.md anti-patterns). Write session.test.js mocking `../lib/cache` (get/set/del/isConnected jest.fn) to assert each behavior, including the Redis-disconnected fallback returning the in-process entry.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/session.test.js -x 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/session.js` contains `crypto.randomBytes(32)` and `require('./cache')`
    - `lib/session.js` consults an in-process Map when `cache.isConnected()` is false (grep: `isConnected`)
    - `npx jest __tests__/session.test.js` exits 0
    - test asserts: unknown id → null; disconnected-Redis getSession returns the written-through in-process payload; destroySession clears both stores
  </acceptance_criteria>
  <done>createSession/getSession/destroySession/touchSession work against Redis with an in-process fallback that does not sign a session out during a brief Redis outage; unit tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API (device token header) | Untrusted device-token value crosses here; compared constant-time |
| API → Redis (session store) | Session payloads persisted; Redis may briefly disconnect |
| build machine → npm registry | New dependencies fetched at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Information Disclosure | deviceToken.matches timing side-channel | mitigate | `crypto.timingSafeEqual` on equal-length buffers; length checked first (not secret) — replicates apiKey.js |
| T-46-10 | Denial of Service (mass sign-out) | session lookup during Redis blip | mitigate | in-process Map fallback consulted when `cache.isConnected()===false` (Finding #5) — a Redis blip must not invalidate live sessions |
| T-46-12 | Spoofing | opaque session id forgeability | mitigate | `crypto.randomBytes(32)` high-entropy id; no client-controlled structure to forge |
| T-46-SC | Tampering | google-auth-library / cookie-parser installs | mitigate | 46-RESEARCH.md Package Legitimacy Audit both `[OK]/Approved` (slopcheck [OK]); official googleapis + expressjs orgs; no [ASSUMED]/[SUS] → no blocking checkpoint required |
| T-46-13 | Elevation of Privilege | deviceToken fails open when env unset | mitigate | `matches()` returns false when `KIOSK_DEVICE_TOKEN` empty (fail closed); validateEnv marks it REQUIRED_IN_PROD |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/device-token-guard.test.js __tests__/session.test.js` → all pass
- `grep -n "KIOSK_DEVICE_TOKEN\|STAFF_EMAILS\|SHEETS_CLIENT_ID" zoho-middleware/lib/validateEnv.js` → all three present
- repo-root `git diff --stat package.json` → no change
</verification>

<success_criteria>
- deviceToken and session primitives exist, are constant-time / fallback-correct, and are unit-tested green
- New auth env vars registered in boot validation
- Both npm deps installed under zoho-middleware only
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-01-SUMMARY.md` when done.
</output>
