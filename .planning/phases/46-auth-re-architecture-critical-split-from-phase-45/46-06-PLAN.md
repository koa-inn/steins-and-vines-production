---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - js/admin.js
  - tests/frontend/admin-session-auth.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-05, D-46-06, D-46-08]
user_setup: []

must_haves:
  truths:
    - "admin.html verifies identity by POSTing the Google access token to /auth/google and relying on the returned session cookie"
    - "Every admin API call (including the admin-embedded kiosk) sends the session cookie via credentials:'include' instead of MW_API_KEY"
    - "A non-allowlisted or unverified staff member is denied the dashboard"
  artifacts:
    - path: "js/admin.js"
      provides: "Session-cookie authenticated admin surface"
      contains: "credentials"
  key_links:
    - from: "js/admin.js"
      to: "/auth/google"
      via: "checkAuthorization POSTs access_token"
      pattern: "/auth/google"
    - from: "js/admin.js"
      to: "session cookie transport"
      via: "credentials:'include' on staff fetches"
      pattern: "credentials"
---

<objective>
Migrate `js/admin.js` from the shared `MW_API_KEY` header to the new server session. `checkAuthorization()` stops calling the Apps-Script `check_auth` round-trip and instead POSTs the (unchanged) GIS access token to `POST /auth/google` (with `credentials:'include'`), reading `{authorized,email}` to show the dashboard or the denied state. Every admin staff fetch — including the admin-embedded kiosk (`#tab-kiosk`), which rides the admin session (Finding #2) — drops `x-api-key`/`mwApiKey()` and adds `credentials:'include'` so the `sv_session` cookie is sent.

Purpose: Voids and reports become attributable to a named Google identity (D-46-05), enforced by the server allowlist (D-46-07/08). The GIS flow itself (`js/lib/auth.js`, `onTokenResponse`) is unchanged. Do NOT run `npm run build` — the rebuild is 46-09.
Output: modified `js/admin.js`, a frontend test proving session-cookie transport and denial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md

<interfaces>
Server (46-02/46-03): POST /auth/google body {access_token} → {authorized:bool, email} + Set-Cookie sv_session (httpOnly). All /api staff calls now authenticate via the sv_session cookie → fetches MUST use `credentials:'include'` (CORS credentials:true already set server-side; cookie is SameSite=None;Secure in prod).

js/admin.js sites (46-PATTERNS.md admin section): onTokenResponse (452-474, UNCHANGED shape); checkAuthorization (476-523) — replace the Apps-Script `adminApiGet('check_auth')` branch with a POST to `mwUrl()+'/auth/google'` {access_token: accessToken}, credentials:'include'; the client-side Config-sheet fallback branch (498-522) becomes dead — remove or make it call showDenied. adminApiGet/adminApiPost (653-697) retained for non-auth Apps-Script actions. 36 x-api-key/mwApiKey refs (grep-confirmed incl. lines 3246-3247, 5469, 6094, 7091, 7240, 8491-8492, 8530-8531, 10173, 10246, 10332, 10371, 10447, 10848, 10878, 11079, 11163, 11378, 11759-11760, 11856-11857, 11906-11907, 12188-12189, 12418-12419, 12452-12453, 12585-12586).
mwUrl() helper already exists in admin.js (used for middleware base URL). ES5 only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: checkAuthorization() → POST /auth/google session exchange</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (onTokenResponse 452-474, checkAuthorization 476-523, mwUrl helper, showDashboard/showDenied)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md (js/admin.js section)
  </read_first>
  <action>
    Rewrite checkAuthorization() so that instead of `adminApiGet('check_auth')` it does `fetch(mwUrl() + '/auth/google', { method:'POST', credentials:'include', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ access_token: accessToken }) })`, parses JSON, and calls `showDashboard()` when `result.authorized` is true else `showDenied()`; on network/parse error call `showDenied()`. Keep the `accessToken` variable sourced from the unchanged onTokenResponse callback. Remove (or route to showDenied) the client-side Config-sheet fallback branch — the server allowlist (D-46-07) is now the sole source of truth. Do not change onTokenResponse's signature or the GIS init. Leave adminApiGet/adminApiPost intact for remaining Apps-Script (Sheets) actions unrelated to auth.
  </action>
  <verify>
    <automated>grep -q "/auth/google" js/admin.js && grep -q "credentials" js/admin.js && npx eslint js/admin.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `js/admin.js` checkAuthorization POSTs to `/auth/google` with `credentials:'include'` and body `access_token`
    - dashboard/denied gating reads `authorized` from the response
    - client-side Config-sheet allowlist no longer grants access
    - `npx eslint js/admin.js` reports no errors
  </acceptance_criteria>
  <done>Admin identity is established by the server session exchange, not Apps-Script check_auth or a client allowlist.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap all admin API calls to credentials:'include' (incl. embedded kiosk)</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (re-grep `grep -n "x-api-key\|mwApiKey\|MW_API_KEY" js/admin.js` for the authoritative sites; inspect the #tab-kiosk fetch calls ~9825-12900)
  </read_first>
  <action>
    Re-grep for the authoritative list. For every admin fetch that sends `'x-api-key': SHEETS_CONFIG.MW_API_KEY` or `mwApiKey()`: remove that header entry and add `credentials: 'include'` to the fetch options object (if the options object lacks a headers object after removal, keep any remaining Content-Type header). This includes the admin-embedded kiosk calls under `#tab-kiosk` — they authenticate with the admin session (NOT the device token), per Finding #2's resolution that the embedded kiosk rides the admin session. If a `mwApiKey()` helper becomes unused, remove it or leave it returning '' harmlessly. Ensure no `SHEETS_CONFIG.MW_API_KEY` reference remains in admin.js. Do not alter money-path/cart logic.
  </action>
  <verify>
    <automated>test $(grep -c "MW_API_KEY" js/admin.js) -eq 0 && test $(grep -c "x-api-key" js/admin.js) -eq 0 && grep -q "credentials: 'include'\|credentials:'include'" js/admin.js && npx eslint js/admin.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MW_API_KEY" js/admin.js` is 0 and `grep -c "x-api-key" js/admin.js` is 0
    - admin staff fetches (including #tab-kiosk) use `credentials:'include'`
    - `npx eslint js/admin.js` reports no errors
  </acceptance_criteria>
  <done>All admin + embedded-kiosk calls transport the session cookie; no MW_API_KEY reference remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Frontend test for session transport + denial</name>
  <files>tests/frontend/admin-session-auth.test.js</files>
  <read_first>
    - tests/frontend/admin-gift-card-mgmt.test.js (admin jsdom harness pattern)
    - js/admin.js (checkAuthorization + a representative staff fetch)
  </read_first>
  <action>
    Write a jsdom test that: (1) mocks fetch so `/auth/google` returns {authorized:true, email:'staff@x'} → checkAuthorization shows the dashboard; (2) `/auth/google` returns {authorized:false} → showDenied path; (3) a representative admin staff fetch is invoked with `credentials:'include'` and no `x-api-key` header. Follow the existing admin test harness for loading admin.js and stubbing SHEETS_CONFIG/mwUrl.
  </action>
  <verify>
    <automated>npx jest tests/frontend/admin-session-auth.test.js -x 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - test proves authorized:true → dashboard, authorized:false → denied
    - test proves a staff fetch uses `credentials:'include'` and omits `x-api-key`
    - `npx jest tests/frontend/admin-session-auth.test.js` exits 0
  </acceptance_criteria>
  <done>Session-based admin auth and denial are proven in jsdom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-site browser → admin API | Session cookie replaces shared secret |
| admin-embedded kiosk → kiosk routes | Rides admin session, not device token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-22 | Elevation of Privilege | client-side Config-sheet allowlist granting access | mitigate | removed; server STAFF_EMAILS allowlist is sole source of truth (D-46-07) |
| T-46-02b | Spoofing | attribution of voids/reports | mitigate | session tied to server-verified Google email (D-46-05); one equal tier (D-46-08) |
| T-46-04b | Tampering (CSRF) | credentialed cross-site admin calls | mitigate | server-side requireAllowedReferer still runs (46-03); httpOnly cookie not script-readable |
</threat_model>

<verification>
- `npx jest tests/frontend/admin-session-auth.test.js` → passes
- `grep -c "MW_API_KEY\|x-api-key" js/admin.js` → 0
- `npx eslint js/admin.js` → clean
</verification>

<success_criteria>
- Admin (and embedded kiosk) authenticate via the server session cookie; identity verified + allowlisted server-side; tests + lint green
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-06-SUMMARY.md` when done.
</output>
