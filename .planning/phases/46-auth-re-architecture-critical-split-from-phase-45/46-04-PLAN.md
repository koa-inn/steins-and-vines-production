---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/routes/consignment.js
  - zoho-middleware/routes/catalog.js
  - zoho-middleware/__tests__/pos-auth-tier.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-02, D-46-11]
user_setup: []

must_haves:
  truths:
    - "All 13 in-route credential checks (GET and POST/PUT) authenticate via an inline requireTiers wrap that re-resolves the tier, so they work whether or not the global guard ran and the existing 2-arg handler unit tests keep passing unedited"
    - "requireTiers returns 401 (synchronously) when no credential is present and 403 when the credential's tier is not allowed — the kiosk sales-order history/detail views keep working post-rotation"
    - "Admin-grade in-route checks (gift-card void, consignment) still reject a device token"
    - "After key rotation the kiosk (device token) still passes every pos.js route it needs"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "In-route checks consult req.authTier, not x-api-key directly"
      contains: "authTier"
  key_links:
    - from: "zoho-middleware/routes/pos.js"
      to: "authTiers.requireTiers inline wrap"
      via: "each handler wraps its body in requireTiers([...])(req,res,next)"
      pattern: "requireTiers\\("
    - from: "zoho-middleware/routes/consignment.js"
      to: "admin-grade rejection of device tier"
      via: "requireTiers(['legacy','session']) inline wrap"
      pattern: "requireTiers\\("
---

<objective>
Migrate the **13 in-route `apiKeyGuard.matches(req.headers['x-api-key'])` checks in `routes/pos.js`** (plus `consignment.js` and `catalog.js`) so they consult the `req.authTier` the 46-03 guard now attaches, rather than independently re-requiring the legacy key. Without this, the kiosk breaks the moment `API_SECRET_KEY` is rotated (46-10): the global guard would admit a device-token request but these in-route checks would 403 it because they only recognise the old key.

Purpose: Close the second, easily-missed enforcement layer (Pitfall: "remove key = one-line fix"). Kiosk-scoped pos routes must accept any authenticated tier; the admin-grade ones (`/api/kiosk/gift-card/void`, consignment) must still reject a device token.
Output: modified pos.js/consignment.js/catalog.js in-route checks, plus a test proving device-token acceptance on kiosk routes and rejection on the void route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md

<interfaces>
From 46-03: server.js guard sets `req.authTier` ∈ {'legacy','device','session'} for every non-exempt /api request that passed, and already rejects device tier on non-kiosk routes. lib/authTiers.js exports allowKiosk(tier), allowAdmin(tier), isKioskRoute(path), resolveTier(req).

pos.js in-route checks to migrate — 13 sites, all shape `if (!apiKeyGuard.matches(req.headers['x-api-key'])) { return res.status(403)... }` at lines: 1455, 1526, 1569, 2009, 2079, 2111, 2142, 2260, 2440 (grep-confirmed) plus the remaining of 13 total — re-grep `grep -n "apiKeyGuard.matches" routes/pos.js` for the authoritative current list.
consignment.js: 1 check at line 26 (admin-grade — consignment reports).
catalog.js: 1 check at line 634 (helper `isAdmin`-style used by a catalog route — inspect its use; treat as admin-grade unless it guards a kiosk catalog read).
contacts/search: GET route at pos.js:2553 — kiosk-scoped; it is a GET so the global guard's GET-exempt early-return means req.authTier is NOT set for it. This route needs its OWN tier resolution (call authTiers.resolveTier(req) and allow legacy|device|session; reject only when null) so the kiosk device token can reach it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate pos.js in-route checks to inline requireTiers wrap</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (all `apiKeyGuard.matches` sites — re-grep for the authoritative 13; current clauses respond `res.status(401)`)
    - zoho-middleware/lib/authTiers.js (requireTiers/resolveTier)
    - zoho-middleware/__tests__/kiosk-salesorders.test.js, batch-customer.test.js, batch-scan-invoices.test.js, batch-reassign-customer.test.js, batch-bottling-invite.test.js (the existing 2-arg `router.<verb>(path, handler)` harness these routes MUST stay compatible with — each asserts a SYNCHRONOUS 401 for a present-but-WRONG `x-api-key`; DO NOT edit these, CLAUDE.md rule #10)
  </read_first>
  <action>
    Re-grep `grep -n "apiKeyGuard.matches" routes/pos.js` for the authoritative list (13 sites). Add `var authTiers = require('../lib/authTiers');` to the imports. Migrate EVERY one of the 13 in-route checks using the SAME inline-wrap mechanism (do NOT use `req.authTier`, and do NOT use the 3-argument `router.get(path, mw, handler)` registration): the existing unit tests call each route handler DIRECTLY via a 2-arg `router.get(path, handler)` mock (bypassing the global guard, so `req.authTier` is undefined), and 3-arg registration would make that mock capture the middleware and drop the real handler. Instead, keep each route as a single 2-arg handler and call the tier check INLINE at the top of that handler, wrapping the existing body in the continuation:

      `router.get('/api/...', function (req, res) { authTiers.requireTiers([...])(req, res, function () { /* existing handler body, minus the old apiKeyGuard 401 clause */ }); });`

    Delete the old `if (!apiKeyGuard.matches(req.headers['x-api-key'])) return res.status(401)...` opening clause from each handler (its 401-no-credential behavior is now provided by requireTiers — see 46-03 Task 1: requireTiers responds 401 SYNCHRONOUSLY when no credential is present, preserving the existing tests' synchronous no-key 401 assertions, and 403 when a credential is present but its tier is not allowed). Because `process.env.MW_API_KEY='test-api-key'` is set in those tests, the legacy tier resolves for their credentialed cases, so wrapped handlers still run. Per-route tier sets (apply the wrap to each of the 13):

      KIOSK (['legacy','device','session']):
      - GET /api/kiosk/salesorders (~1569), GET /api/kiosk/salesorder/:id (~2923), PUT /api/kiosk/salesorder-update (~2009), GET /api/contacts/search (~2553)
      BREWPAD/SESSION (['legacy','session'] — device rejected):
      - POST /api/batch/sync-zoho (~2079), POST /api/batch/bulk-create (~2440), POST /api/batch/reassign-customer (~2600), POST /api/batch/bottling-invite (~2885), GET /api/batch/search-invoices (~2111), GET /api/batch/customer-by-number (~2142), GET /api/batch/scan-invoices (~2260)
      ADMIN (['legacy','session'] — device rejected):
      - GET /api/orders/recent (~1455), GET /api/admin/inventory-ledger (~1526)

    (Gift-card void is NOT in pos.js's in-route list — it is enforced by the 46-03 global guard, which rejects device on the non-kiosk void path; covered by 46-03 Task 3.) Remove the `apiKeyGuard` import if no longer referenced. Do NOT change money-path or Zoho logic — only the credential gate lines. After editing, run the five existing test files named in read_first to confirm they still pass UNCHANGED (each includes a present-but-wrong-key SYNC 401 case that requireTiers' legacy branch must satisfy).
  </action>
  <verify>
    <automated>cd zoho-middleware && test $(grep -c "apiKeyGuard.matches(req.headers\['x-api-key'\])" routes/pos.js) -eq 0 && test $(grep -c "requireTiers(\[" routes/pos.js) -ge 13 && node -e "require('./routes/pos'); console.log('pos loads')" && npx jest __tests__/kiosk-salesorders.test.js __tests__/batch-customer.test.js __tests__/batch-scan-invoices.test.js __tests__/batch-reassign-customer.test.js __tests__/batch-bottling-invite.test.js 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apiKeyGuard.matches" routes/pos.js` is 0; `grep -c "requireTiers([" routes/pos.js` is >= 13 (all 13 checks wrapped inline)
    - every migrated handler keeps a 2-arg `router.<verb>(path, function(req,res){...})` signature (no 3-arg middleware registration) so the existing findHandler harness still captures the real handler
    - kiosk routes use `['legacy','device','session']`; BrewPad/admin routes use `['legacy','session']` (device rejected)
    - all five existing test files (kiosk-salesorders, batch-customer, batch-scan-invoices, batch-reassign-customer, batch-bottling-invite) PASS unedited — including their present-but-wrong-key SYNC 401 cases
    - `node -e "require('./routes/pos')"` loads without throwing
  </acceptance_criteria>
  <done>All 13 pos.js gates use the inline requireTiers wrap (2-arg handlers preserved); kiosk device tokens pass kiosk routes, device is rejected on BrewPad/admin routes, and the existing GET/POST unit tests pass unedited.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Admin-grade in-route checks in consignment.js and catalog.js</name>
  <files>zoho-middleware/routes/consignment.js, zoho-middleware/routes/catalog.js</files>
  <read_first>
    - zoho-middleware/routes/consignment.js (line 8 import, line 26 check)
    - zoho-middleware/routes/catalog.js (line 10 import, line 634 helper + its call sites)
  </read_first>
  <action>
    consignment.js: `GET /api/admin/consignment-report` (~line 25) is captured by consignment.test.js via the same 2-arg `router.get(path, handler)` mock and asserts 401 for no-key — so use the identical INLINE wrap, NOT 3-arg registration and NOT `req.authTier`: keep it a single 2-arg handler and wrap its body with `authTiers.requireTiers(['legacy','session'])(req, res, function () { /* existing body minus the old apiKeyGuard 401 clause */ })`. This admits legacy or session, returns 401 synchronously for no-credential (matching consignment.test.js), and rejects device (admin-grade per D-46-02). Add `var authTiers = require('../lib/authTiers');`. catalog.js: the line-634 helper's existing comment says "Admin gate... staff-only" — this is an ADMIN gate, so make it accept `legacy | session` ONLY and reject `device` (do not treat it as a public/kiosk read). Implement by resolving the tier inline and returning true only for legacy/session (mirror requireTiers' allow-set semantics; a bare boolean helper can call a synchronous legacy-key check plus, where a request object is available, the session — keep it admin-grade). Add `var authTiers = require('../lib/authTiers');` where needed. Remove now-unused `apiKeyGuard` imports if fully replaced. After editing, run consignment.test.js to confirm it passes unedited.
  </action>
  <verify>
    <automated>cd zoho-middleware && node -e "require('./routes/consignment');require('./routes/catalog');console.log('routes load')" && grep -q "requireTiers(\[" routes/consignment.js && npx jest __tests__/consignment.test.js 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `routes/consignment.js` wraps the report handler INLINE with `authTiers.requireTiers(['legacy','session'])` (2-arg handler preserved; device rejected; 401 sync for no-cred)
    - consignment.test.js PASSES unedited
    - `routes/catalog.js` line-634 admin gate accepts `legacy | session` only and rejects `device` (explicit, per the existing "staff-only" comment)
    - both files `require` load without throwing
  </acceptance_criteria>
  <done>Admin-grade consignment/catalog gates use the inline wrap (2-arg handlers preserved), accept legacy|session only, reject device, and keep consignment.test.js green unedited.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: pos.js tier-acceptance integration test</name>
  <files>zoho-middleware/__tests__/pos-auth-tier.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/api-key-guard.test.js (supertest mock roster)
    - zoho-middleware/routes/pos.js (migrated checks)
  </read_first>
  <action>
    Write pos-auth-tier.test.js (supertest, full mock roster + mock `../lib/session`, `../lib/deviceToken`, `../lib/cache`). Set `process.env.KIOSK_DEVICE_TOKEN` to a known value and configure the deviceToken mock (or real matches) so the device tier resolves. Assert: (1) a kiosk-scoped POST (e.g. /api/kiosk/verify-pin) with a valid x-device-token → NOT 403 (reaches the handler / 200 or a domain error, not an auth 403); (2) the same route with NO credential → 403; (3) /api/kiosk/gift-card/void with a valid device token → 403 (admin-grade); (4) /api/kiosk/gift-card/void with a valid session cookie → NOT auth-403; (5) simulate post-rotation by NOT setting API_SECRET_KEY and confirming the device-token kiosk path still passes; (6) GET regression (the blocker this plan closes): `GET /api/kiosk/salesorders` with a valid x-device-token → NOT 401/403, and with NO credential → 401 (requireTiers 401s synchronously for missing credential, matching the existing kiosk-salesorders no-key test); (7) `GET /api/batch/search-invoices` with a valid session cookie → NOT 403, and with an x-device-token present-but-not-allowed → 403 (BrewPad GETs are session-only; present-but-insufficient is 403, not 401); (8) `GET /api/orders/recent` with x-device-token → 403 (admin GET rejects device), with legacy key or session → NOT 403. This pos-auth-tier.test.js may use supertest against the app OR the same 2-arg `findHandler(method,path)(req,res)` unit harness the existing files use; if using the unit harness, pass a real continuation and assert on the terminal `res` (requireTiers calls next() internally on success). Focus assertions on the auth gate (401/403), not deep Zoho behavior. Do NOT edit any existing test file.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/pos-auth-tier.test.js -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test proves a kiosk route accepts the device token and rejects no-credential
    - test proves gift-card void rejects device token but accepts session
    - test includes a "no API_SECRET_KEY (post-rotation) → device token still works on kiosk route" case
    - test covers at least one kiosk-scoped GET (device→not-401/403, none→401), one BrewPad GET (session→not-403, device→403), and one admin GET (device→403)
    - `npx jest __tests__/pos-auth-tier.test.js` exits 0
  </acceptance_criteria>
  <done>Post-rotation kiosk survival and admin-grade device rejection are proven at the pos route layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk device token → pos.js in-route checks | Second enforcement layer that must not silently re-require the retired key |
| device token → gift-card void / consignment | Admin-grade capability behind a kiosk-reachable prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-18 | Denial of Service (self-lockout) | pos.js in-route checks re-requiring legacy key post-rotation | mitigate | migrate all 13 checks to the inline requireTiers wrap (re-resolves tier); explicit post-rotation test (Task 3 case 5) |
| T-46-18b | Denial of Service (kiosk lockout) | GET routes get no `req.authTier` (guard skips GETs); relying on `req.authTier` in-route would 401/403 every credential | mitigate | all 13 in-route checks resolve their own tier via inline requireTiers (not `req.authTier`); regression tests for kiosk/BrewPad/admin GETs (Task 3 cases 6-8) |
| T-46-18c | Availability (CI red / rule #10) | 3-arg route registration breaks 5 existing 2-arg handler-capture test files | mitigate | inline requireTiers wrap preserves the 2-arg `router.<verb>(path, handler)` signature; 401-sync-no-cred preserves existing status assertions; no existing test edited |
| T-46-03b | Elevation of Privilege | device token reaching gift-card void via in-route bypass | mitigate | void/consignment checks reject `authTier==='device'` (defense in depth over the 46-03 guard) |
| T-46-19 | Spoofing | contacts/search GET unauthenticated (GET-exempt at global guard) | mitigate | contacts/search resolves its own tier and 403s on null credential |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` → full suite green (incl. the 5 existing files kiosk-salesorders / batch-customer / batch-scan-invoices / batch-reassign-customer / consignment — UNEDITED)
- `grep -c "apiKeyGuard.matches" zoho-middleware/routes/pos.js` → 0
- `grep -c "requireTiers(\[" zoho-middleware/routes/pos.js` → >= 13; `grep -c "requireTiers(\[" zoho-middleware/routes/consignment.js` → >= 1
- no migrated route uses a 3-arg `router.<verb>(path, mw, handler)` registration (2-arg handlers preserved)
</verification>

<success_criteria>
- All in-route credential checks are tier-aware; kiosk survives rotation; admin-grade routes reject device tokens; proven by tests
- `cd zoho-middleware && npm test` passes
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-04-SUMMARY.md` when done.
</output>
