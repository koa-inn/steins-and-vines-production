---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/routes/consignment.js
  - zoho-middleware/routes/catalog.js
  - zoho-middleware/__tests__/pos-auth-tier.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-02, D-46-11]
user_setup: []

must_haves:
  truths:
    - "Every in-route credential check in pos.js accepts the request when the guard already classified it (req.authTier for POST/PUT), instead of re-requiring the legacy x-api-key"
    - "GET routes (kiosk salesorders/detail, BrewPad search/scan/lookup, admin ledger/recent/consignment) authenticate via requireTiers middleware since the global guard skips GETs — the kiosk sales-order history/detail views keep working post-rotation"
    - "Admin-grade in-route checks (gift-card void, consignment) still reject a device token"
    - "After key rotation the kiosk (device token) still passes every pos.js route it needs"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "In-route checks consult req.authTier, not x-api-key directly"
      contains: "authTier"
  key_links:
    - from: "zoho-middleware/routes/pos.js"
      to: "req.authTier (set by server.js guard)"
      via: "tier check replaces apiKeyGuard.matches"
      pattern: "req.authTier"
    - from: "zoho-middleware/routes/consignment.js"
      to: "admin-grade rejection of device tier"
      via: "reject when authTier==='device'"
      pattern: "authTier"
---

<objective>
Migrate the **13 in-route `apiKeyGuard.matches(req.headers['x-api-key'])` checks in `routes/pos.js`** (plus `consignment.js` and `catalog.js`) so they consult the `req.authTier` the 46-03 guard now attaches, rather than independently re-requiring the legacy key. Without this, the kiosk breaks the moment `API_SECRET_KEY` is rotated (46-10): the global guard would admit a device-token request but these in-route checks would 403 it because they only recognise the old key.

Purpose: Close the second, easily-missed enforcement layer (Pitfall: "remove key = one-line fix"). Kiosk-scoped pos routes must accept any authenticated tier; the admin-grade ones (`/api/kiosk/gift-card/void`, consignment) must still reject a device token.
Output: modified pos.js/consignment.js/catalog.js in-route checks, plus a test proving device-token acceptance on kiosk routes and rejection on the void route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md

<interfaces>
From 46-03: server.js guard sets `req.authTier` ∈ {'legacy','device','session'} for every non-exempt /api request that passed, and already rejects device tier on non-kiosk routes. lib/authTiers.js exports allowKiosk(tier), allowAdmin(tier), isKioskRoute(path), resolveTier(req).

pos.js in-route checks to migrate — 13 sites, all shape `if (!apiKeyGuard.matches(req.headers['x-api-key'])) { return res.status(403)... }` at lines: 1455, 1526, 1569, 2009, 2079, 2111, 2142, 2260, 2440 (grep-confirmed) plus the remaining of 13 total — re-grep `grep -n "apiKeyGuard.matches" routes/pos.js` for the authoritative current list.
consignment.js: 1 check at line 26 (admin-grade — consignment reports).
catalog.js: 1 check at line 634 (helper `isAdmin`-style used by a catalog route — inspect its use; treat as admin-grade unless it guards a kiosk catalog read).
contacts/search: GET route at pos.js:2553 — kiosk-scoped; it is a GET so the global guard's GET-exempt early-return means req.authTier is NOT set for it. This route needs its OWN tier resolution (call authTiers.resolveTier(req) and allow legacy|device|session; reject only when null) so the kiosk device token can reach it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate pos.js in-route checks to req.authTier (kiosk-scoped)</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (all `apiKeyGuard.matches` sites — re-grep for the authoritative 13; the contacts/search GET at 2553; the gift-card void handler if present here)
    - zoho-middleware/lib/authTiers.js (allowKiosk/allowAdmin/resolveTier)
  </read_first>
  <action>
    Re-grep `grep -n "apiKeyGuard.matches" routes/pos.js` for the authoritative list (13 sites). Add `var authTiers = require('../lib/authTiers');` to the imports. The migration DIFFERS BY HTTP METHOD, because the 46-03 global `/api` guard early-returns on `req.method==='GET'` and therefore NEVER sets `req.authTier` for GET routes — GET routes must resolve their own tier via the `authTiers.requireTiers([...])` route-middleware factory (attach it as middleware in the `router.get(path, requireTiers([...]), handler)` position and DELETE the in-route `apiKeyGuard.matches` clause). POST/PUT routes keep the guard's classification via `if (!req.authTier)`.

    POST/PUT routes (guard runs — replace the clause with `if (!req.authTier) return res.status(403)...`):
      - PUT /api/kiosk/salesorder-update (~2009) — kiosk
      - POST /api/batch/sync-zoho (~2079), POST /api/batch/bulk-create (~2440), POST /api/batch/reassign-customer (~2600), POST /api/batch/bottling-invite (~2885) — session/BrewPad (device is already blocked upstream by the guard for these non-kiosk POSTs, so `!req.authTier` is sufficient).

    GET routes (guard SKIPS them — attach `authTiers.requireTiers([...])` middleware, delete the apiKeyGuard clause):
      - GET /api/kiosk/salesorders (~1569) → `requireTiers(['legacy','device','session'])` (kiosk-scoped)
      - GET /api/kiosk/salesorder/:id (~2923) → `requireTiers(['legacy','device','session'])` (kiosk-scoped)
      - GET /api/contacts/search (~2553) → `requireTiers(['legacy','device','session'])` (kiosk-scoped; replaces the inline-resolveTier note — use the factory for consistency)
      - GET /api/batch/search-invoices (~2111), GET /api/batch/customer-by-number (~2142), GET /api/batch/scan-invoices (~2260) → `requireTiers(['legacy','session'])` (BrewPad/session per D-46-09; device NOT needed and rejected)
      - GET /api/orders/recent (~1455), GET /api/admin/inventory-ledger (~1526) → `requireTiers(['legacy','session'])` (admin-grade; device rejected per D-46-02)

    For any ADMIN-GRADE POST inside pos.js (e.g. `/api/kiosk/gift-card/void` if present here): use `if (!req.authTier || req.authTier === 'device')` (device rejected, defense in depth). Keep the `apiKeyGuard` import only if still referenced; otherwise remove it. Do NOT change money-path or Zoho logic — only the credential gate lines.
  </action>
  <verify>
    <automated>cd zoho-middleware && test $(grep -c "apiKeyGuard.matches(req.headers\['x-api-key'\])" routes/pos.js) -eq 0 && grep -q "req.authTier" routes/pos.js && grep -q "requireTiers" routes/pos.js && node -e "require('./routes/pos'); console.log('pos loads')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "apiKeyGuard.matches" routes/pos.js` is 0
    - every GET route above uses `authTiers.requireTiers([...])` middleware (grep `requireTiers` present); POST/PUT routes use `req.authTier`
    - kiosk GETs (salesorders, salesorder/:id, contacts/search) accept device; BrewPad/admin GETs use `['legacy','session']` (device rejected)
    - no GET route reads a bare `req.authTier` (which would always be undefined for GETs)
    - `node -e "require('./routes/pos')"` loads without throwing
  </acceptance_criteria>
  <done>All pos.js credential gates consult the unified tier; kiosk device tokens pass kiosk routes; void rejects device; contacts/search authenticates its own GET.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Admin-grade in-route checks in consignment.js and catalog.js</name>
  <files>zoho-middleware/routes/consignment.js, zoho-middleware/routes/catalog.js</files>
  <read_first>
    - zoho-middleware/routes/consignment.js (line 8 import, line 26 check)
    - zoho-middleware/routes/catalog.js (line 10 import, line 634 helper + its call sites)
  </read_first>
  <action>
    consignment.js: the report route `GET /api/admin/consignment-report` (~line 25) is a GET, so the global guard skips it and `req.authTier` is never set — do NOT use `req.authTier` here. Instead attach `authTiers.requireTiers(['legacy','session'])` as route middleware (`router.get('/api/admin/consignment-report', authTiers.requireTiers(['legacy','session']), handler)`) and delete the in-route `apiKeyGuard.matches` clause; this admits legacy or session and rejects device (admin-grade per D-46-02). Add `var authTiers = require('../lib/authTiers');`. catalog.js: inspect the line-634 helper (returns apiKeyGuard.matches(...)) and its callers; if it gates an admin-only catalog mutation, change it to return true only for legacy|session tier (reject device); if it merely gates a public/kiosk catalog read reachable via GET, resolve via authTiers.resolveTier and accept any non-null tier. Add `var authTiers = require('../lib/authTiers');` where needed. Remove now-unused `apiKeyGuard` imports if fully replaced.
  </action>
  <verify>
    <automated>cd zoho-middleware && node -e "require('./routes/consignment');require('./routes/catalog');console.log('routes load')" && grep -q "authTier" routes/consignment.js</automated>
  </verify>
  <acceptance_criteria>
    - `routes/consignment.js` uses `authTiers.requireTiers(['legacy','session'])` on the GET report route (device rejected, NOT via `req.authTier`)
    - `routes/catalog.js` line-634 helper no longer requires the legacy key exclusively (tier-aware)
    - both files `require` load without throwing
  </acceptance_criteria>
  <done>Admin-grade consignment/catalog gates are tier-aware and reject device tokens.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: pos.js tier-acceptance integration test</name>
  <files>zoho-middleware/__tests__/pos-auth-tier.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/api-key-guard.test.js (supertest mock roster)
    - zoho-middleware/routes/pos.js (migrated checks)
  </read_first>
  <action>
    Write pos-auth-tier.test.js (supertest, full mock roster + mock `../lib/session`, `../lib/deviceToken`, `../lib/cache`). Set `process.env.KIOSK_DEVICE_TOKEN` to a known value and configure the deviceToken mock (or real matches) so the device tier resolves. Assert: (1) a kiosk-scoped POST (e.g. /api/kiosk/verify-pin) with a valid x-device-token → NOT 403 (reaches the handler / 200 or a domain error, not an auth 403); (2) the same route with NO credential → 403; (3) /api/kiosk/gift-card/void with a valid device token → 403 (admin-grade); (4) /api/kiosk/gift-card/void with a valid session cookie → NOT auth-403; (5) simulate post-rotation by NOT setting API_SECRET_KEY and confirming the device-token kiosk path still passes; (6) GET regression (the blocker this plan closes): `GET /api/kiosk/salesorders` with a valid x-device-token → NOT 403, and with NO credential → 403; (7) `GET /api/batch/search-invoices` with a valid session cookie → NOT 403, and with an x-device-token only → 403 (BrewPad GETs are session-only); (8) `GET /api/orders/recent` with x-device-token → 403 (admin GET rejects device), with legacy key or session → NOT 403. Focus assertions on the auth gate (401/403), not deep Zoho behavior.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/pos-auth-tier.test.js -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - test proves a kiosk route accepts the device token and rejects no-credential
    - test proves gift-card void rejects device token but accepts session
    - test includes a "no API_SECRET_KEY (post-rotation) → device token still works on kiosk route" case
    - test covers at least one kiosk-scoped GET (device→not-403, none→403), one BrewPad GET (session→not-403, device→403), and one admin GET (device→403)
    - `npx jest __tests__/pos-auth-tier.test.js` exits 0
  </acceptance_criteria>
  <done>Post-rotation kiosk survival and admin-grade device rejection are proven at the pos route layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk device token → pos.js in-route checks | Second enforcement layer that must not silently re-require the retired key |
| device token → gift-card void / consignment | Admin-grade capability behind a kiosk-reachable prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-18 | Denial of Service (self-lockout) | pos.js in-route checks re-requiring legacy key post-rotation | mitigate | migrate all 13 checks: POST/PUT to `req.authTier`, GETs to `requireTiers` middleware; explicit post-rotation test (Task 3 case 5) |
| T-46-18b | Denial of Service (kiosk lockout) | GET routes get no `req.authTier` (guard skips GETs) → blanket `if(!req.authTier)` would 403 every credential | mitigate | GET routes resolve their own tier via `requireTiers`; regression tests for kiosk/BrewPad/admin GETs (Task 3 cases 6-8) |
| T-46-03b | Elevation of Privilege | device token reaching gift-card void via in-route bypass | mitigate | void/consignment checks reject `authTier==='device'` (defense in depth over the 46-03 guard) |
| T-46-19 | Spoofing | contacts/search GET unauthenticated (GET-exempt at global guard) | mitigate | contacts/search resolves its own tier and 403s on null credential |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` → full suite green
- `grep -c "apiKeyGuard.matches" zoho-middleware/routes/pos.js` → 0 (or only within the authTiers-based contacts/search path)
- `grep -n "authTier" zoho-middleware/routes/pos.js zoho-middleware/routes/consignment.js` → tier checks present
</verification>

<success_criteria>
- All in-route credential checks are tier-aware; kiosk survives rotation; admin-grade routes reject device tokens; proven by tests
- `cd zoho-middleware && npm test` passes
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-04-SUMMARY.md` when done.
</output>
