---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - zoho-middleware/lib/googleVerify.js
  - zoho-middleware/routes/auth.js
  - zoho-middleware/__tests__/google-verify.test.js
  - zoho-middleware/__tests__/auth-google-route.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-05, D-46-06, D-46-07, D-46-08]
user_setup: []

must_haves:
  truths:
    - "The server independently derives a staff email from a Google access token, never trusting a client-supplied email"
    - "A token issued to a different Google app (wrong aud) is rejected even if the email is on the allowlist"
    - "POST /auth/google issues an httpOnly session cookie only for allowlisted, verified staff; POST /auth/logout destroys the session"
  artifacts:
    - path: "zoho-middleware/lib/googleVerify.js"
      provides: "getTokenInfo + explicit aud check + email_verified check"
      exports: ["verifyStaffAccessToken"]
    - path: "zoho-middleware/routes/auth.js"
      provides: "POST /auth/google (verify+issue session), POST /auth/logout"
      contains: "/auth/google"
  key_links:
    - from: "zoho-middleware/routes/auth.js"
      to: "zoho-middleware/lib/googleVerify.js"
      via: "verifyStaffAccessToken(accessToken)"
      pattern: "verifyStaffAccessToken"
    - from: "zoho-middleware/routes/auth.js"
      to: "zoho-middleware/lib/session.js"
      via: "session.createSession(email) + res.cookie"
      pattern: "createSession"
---

<objective>
Add the net-new server-side Google identity verification and the sign-in/sign-out endpoints. `lib/googleVerify.js` calls `google-auth-library`'s `getTokenInfo(accessToken)` and performs the **mandatory explicit `aud` check** the library does not do for you, plus `email_verified`, then returns the lowercased email. `routes/auth.js` gains `POST /auth/google` (verify → allowlist → issue `sv_session` cookie) and `POST /auth/logout` (destroy session).

Purpose: This is the identity truth-source for admin/BrewPad (D-46-05/06/07). The frontend GIS flow (`js/lib/auth.js`) is unchanged — it already yields an access token; only this server exchange is new.
Output: `lib/googleVerify.js`, modified `routes/auth.js`, two test files including the mandatory wrong-audience rejection test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md

<interfaces>
From 46-01 (already built): lib/session.js exports createSession(email)->Promise<sid>, getSession(id), destroySession(id), touchSession(id).

google-auth-library (Context7-verified, 46-RESEARCH.md Pattern 4):
  var { OAuth2Client } = require('google-auth-library'); var client = new OAuth2Client();
  client.getTokenInfo(accessToken) -> Promise<{ aud, email, email_verified, scopes, expiry_date, sub, azp, ... }>
  getTokenInfo does NOT validate audience — caller MUST check tokenInfo.aud === process.env.SHEETS_CLIENT_ID itself.

routes/auth.js existing exchange pattern to mirror (GET /auth/zoho/callback, lines 28-56): validate input → do async work → respond; catch logs via log.error and returns JSON error. Router already mounted at server.js:128 BEFORE the api guard, so /auth/google and /auth/logout are reachable pre-guard with no server.js change.

Allowlist: (process.env.STAFF_EMAILS||'').split(',').map(e=>e.trim().toLowerCase()); reject with 403 {authorized:false} if email not in list.
Cookie: res.cookie('sv_session', sid, { httpOnly:true, secure: NODE_ENV==='production', sameSite: NODE_ENV==='production'?'none':'lax', maxAge: 7*24*60*60*1000, path:'/' }).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lib/googleVerify.js with mandatory aud check</name>
  <files>zoho-middleware/lib/googleVerify.js, zoho-middleware/__tests__/google-verify.test.js</files>
  <read_first>
    - zoho-middleware/lib/zohoAuth.js (existing OAuth wrapper style, error handling)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md (Finding #4/#7 + Pitfall 1 + Pattern 4)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md (googleVerify.js section, lines 162-193)
  </read_first>
  <behavior>
    - verifyStaffAccessToken(token) resolves to lowercased tokenInfo.email when aud === SHEETS_CLIENT_ID AND email_verified === true
    - rejects (throws) when tokenInfo.aud !== process.env.SHEETS_CLIENT_ID — even if email is otherwise valid (the mandatory wrong-audience case)
    - rejects when email_verified is false
    - rejects when getTokenInfo itself rejects (expired/invalid token)
  </behavior>
  <action>
    Create lib/googleVerify.js: construct a module-scope `new OAuth2Client()`, export `verifyStaffAccessToken(accessToken)` that calls `client.getTokenInfo(accessToken)`, then throws `Error('Token audience mismatch')` unless `tokenInfo.aud === process.env.SHEETS_CLIENT_ID`, throws `Error('Email not verified')` unless `tokenInfo.email_verified`, and otherwise returns `tokenInfo.email.toLowerCase()`. Write google-verify.test.js mocking `google-auth-library` so `getTokenInfo` returns controllable fixtures derived from the REAL token-info shape (aud/email/email_verified fields — mock-mirrors-real-contract, not invented keys). MUST include the companion pair: "valid token + matching aud → resolves email" AND "valid token + WRONG aud → rejects" (Pitfall 1). Also test email_verified=false → rejects, and getTokenInfo-throws → rejects.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/google-verify.test.js -x 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `lib/googleVerify.js` contains `getTokenInfo` and an explicit `tokenInfo.aud !== process.env.SHEETS_CLIENT_ID` comparison
    - `lib/googleVerify.js` contains `email_verified` check and `.toLowerCase()`
    - google-verify.test.js contains BOTH a matching-aud accept case and a wrong-aud reject case
    - `npx jest __tests__/google-verify.test.js` exits 0
  </acceptance_criteria>
  <done>Server-side Google verification derives the email itself, enforces the aud check, and is proven by an explicit wrong-audience rejection test.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: POST /auth/google and POST /auth/logout in routes/auth.js</name>
  <files>zoho-middleware/routes/auth.js, zoho-middleware/__tests__/auth-google-route.test.js</files>
  <read_first>
    - zoho-middleware/routes/auth.js (existing imports lines 1-8, GET /auth/zoho/callback exchange+respond shape lines 28-56)
    - zoho-middleware/__tests__/api-key-guard.test.js (supertest mock-registration roster + require('../server') pattern, lines 18-57)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md (routes/auth.js section + full illustrative /auth/google shape, lines 197-277)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md (Code Examples: sign-in exchange endpoint)
  </read_first>
  <action>
    In routes/auth.js add `var googleVerify = require('../lib/googleVerify');` and `var session = require('../lib/session');`. Add `router.post('/auth/google', ...)`: read `req.body.access_token` (400 {error:'Missing access_token'} if absent); call `googleVerify.verifyStaffAccessToken(accessToken)`; parse STAFF_EMAILS allowlist (split/trim/lowercase); if email not in allowlist respond 403 {authorized:false}; else `session.createSession(email)` and set the `sv_session` cookie with the exact attributes in the interfaces block (httpOnly, prod-branched secure+sameSite, 7-day maxAge, path '/'), respond 200 {authorized:true, email}; on verify catch, `log.warn('[auth/google] verification failed: '+err.message)` and 401 {authorized:false}. Add `router.post('/auth/logout', ...)`: read `req.cookies && req.cookies.sv_session`; if present `session.destroySession(sid)`; clear the cookie (`res.clearCookie('sv_session', { path:'/' })`); respond 200 {ok:true}. NOTE the logout route reads req.cookies, which requires cookie-parser — mounted in 46-03; the route is defined here but its cookie-read path is exercised end-to-end only after 46-03 mounts cookie-parser (guard against `req.cookies` being undefined). Write auth-google-route.test.js (supertest, full mock roster incl. mocking `../lib/googleVerify` and `../lib/session`) covering: missing access_token → 400; non-allowlisted verified email → 403 {authorized:false}; allowlisted verified email → 200 {authorized:true} with a Set-Cookie sv_session header; verify-throws → 401.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/auth-google-route.test.js -x 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `routes/auth.js` contains `router.post('/auth/google'` and `router.post('/auth/logout'`
    - `routes/auth.js` sets cookie name `sv_session` with `httpOnly:true` and a `sameSite` prod branch
    - route derives allowlist from `process.env.STAFF_EMAILS` and never reads an email from the request body
    - `npx jest __tests__/auth-google-route.test.js` exits 0 including the Set-Cookie assertion and the 403 non-allowlisted case
  </acceptance_criteria>
  <done>POST /auth/google verifies+allowlists+issues sv_session; POST /auth/logout destroys it; route tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /auth/google | Untrusted access token + (ignored) client claims cross here |
| API → Google tokeninfo | Token provenance verified against Google |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02 | Spoofing | getTokenInfo audience confusion | mitigate | explicit `tokenInfo.aud === SHEETS_CLIENT_ID` check + companion wrong-aud rejection test (Pitfall 1) |
| T-46-09 | Spoofing | trusting client-reported email | mitigate | server derives email ONLY from getTokenInfo result; request body email is never read |
| T-46-14 | Elevation of Privilege | non-staff Google account | mitigate | STAFF_EMAILS allowlist enforced server-side after verification (D-46-07); one tier, equal (D-46-08) |
| T-46-15 | Spoofing | unverified Google email | mitigate | `email_verified === true` required before allowlist check |
| T-46-16 | Information Disclosure | verbose auth error leaking token internals | accept | errors return generic {authorized:false}; details only to server log.warn |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/google-verify.test.js __tests__/auth-google-route.test.js` → all pass
- `grep -n "aud !== process.env.SHEETS_CLIENT_ID\|aud ===" zoho-middleware/lib/googleVerify.js` → aud check present
- `grep -n "req.body" zoho-middleware/routes/auth.js` → no email read from body (only access_token)
</verification>

<success_criteria>
- Google verification with mandatory aud check exists and is tested for the wrong-audience path
- /auth/google issues sessions only for allowlisted verified staff; /auth/logout revokes
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-02-SUMMARY.md` when done.
</output>
