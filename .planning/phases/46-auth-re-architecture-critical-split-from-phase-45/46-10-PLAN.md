---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 10
type: execute
wave: 6
depends_on: ["46-09"]
files_modified:
  - docs/RUNBOOK.md
autonomous: false
requirements: [AUDIT-CRITICAL-AUTH, D-46-01, D-46-04, D-46-05, D-46-07, D-46-11, D-46-12, D-46-13]
user_setup:
  - service: railway
    why: "New auth env vars + the API_SECRET_KEY rotation that ends the dual-accept window"
    env_vars:
      - name: STAFF_EMAILS
        source: "Owner-defined comma-separated Google emails (D-46-07) — set in Railway → Variables"
      - name: KIOSK_DEVICE_TOKEN
        source: "Owner-generated: openssl rand -base64 48 → stored in password manager + Railway → Variables"
      - name: SHEETS_CLIENT_ID
        source: "Public Google OAuth client id 8605205683-tck2da2tpp03vcbr5etauu9q7kompg3q.apps.googleusercontent.com → Railway → Variables"
      - name: API_SECRET_KEY_PREVIOUS
        source: "Optional canary: set to the retired API_SECRET_KEY value immediately after rotation"
    dashboard_config:
      - task: "Rotate API_SECRET_KEY to a new value (openssl rand -base64 32) to end the dual-accept window"
        location: "Railway → project → Variables → API_SECRET_KEY"

must_haves:
  truths:
    - "The new auth deploys to prod with STAFF_EMAILS/KIOSK_DEVICE_TOKEN/SHEETS_CLIENT_ID set, accepting old key AND new credentials (dual-accept)"
    - "The iPad kiosk (device token), admin, and BrewPad (Google session) all work under the new auth before rotation"
    - "After the owner rotates API_SECRET_KEY, the old key is dead and no surface is locked out"
    - "The rotation runbook is recorded in docs/RUNBOOK.md"
  artifacts:
    - path: "docs/RUNBOOK.md"
      provides: "Auth cutover + rotation checklist and outcome record"
      contains: "API_SECRET_KEY rotation"
  key_links:
    - from: "Railway env (new vars + rotated key)"
      to: "prod middleware auth tiers"
      via: "owner cutover checklist"
      pattern: "KIOSK_DEVICE_TOKEN"
---

<objective>
Execute the owner-coordinated **cutover** that closes the CRITICAL: deploy the new auth to prod alongside the old key (dual-accept), migrate each surface one at a time (provision the iPad device token; sign in to admin + BrewPad; verify sale/void/report), then **rotate `API_SECRET_KEY`** to end the window (D-46-11/12), and confirm the old key is dead with no lockout. Record the runbook and outcome in `docs/RUNBOOK.md`. D-46-13 (interim IP allowlist) is intentionally SKIPPED and recorded as such.

Purpose: The rotation is what actually neutralizes the leaked key (its value persists in git history). This plan is a sequence of blocking human checkpoints because it involves Railway secrets, a production deploy, physical iPad provisioning, and an irreversible key rotation — Claude automates everything around them (generating tokens, drafting the runbook, verification commands) but the owner performs the gated actions.
Output: `docs/RUNBOOK.md` cutover section + recorded outcome; leaked key neutralized.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md

<interfaces>
Deploy reality (STATE.md): staging serves frontend from the repo and CALLS PROD MIDDLEWARE; there is no separate staging middleware. Railway auto-deploys from koa-inn/steins-and-vines-production on zoho-middleware/** — a prod force-push IS a middleware deploy. Frontend prod = `git push production main --force`; staging = `git push origin main`. ALL changes go to staging first.
Dual-accept: with the new env vars set and the guard live (46-03/04), the middleware accepts legacy x-api-key AND x-device-token (kiosk) AND sv_session (admin/BrewPad) simultaneously. Rotating API_SECRET_KEY's VALUE ends the legacy branch — no feature flag.
Canary (46-RESEARCH.md Finding #6): optional API_SECRET_KEY_PREVIOUS + a log.warn when a request still presents the retired value.
Rotation-day checklist (46-CONTEXT.md specifics): provision iPad → verify sale → sign in admin + BrewPad → verify void/report → rotate key → verify old key dead.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Set new env vars + deploy backend (dual-accept live)</name>
  <what-built>Backend 3-tier guard (46-03/04) and auth endpoints (46-02) are committed and tested; frontend bundles rebuilt (46-09). Claude has generated a candidate device token and drafted the Railway variable list.</what-built>
  <read_first>
    - docs/RUNBOOK.md (existing prod deploy/rollback runbook format to append to)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md (Environment Availability + Finding #6)
  </read_first>
  <action>
    Claude: generate a strong device token with `openssl rand -base64 48` and present it; draft the four Railway variables (STAFF_EMAILS with the owner's staff emails, KIOSK_DEVICE_TOKEN=the generated token, SHEETS_CLIENT_ID=the public client id, and leave API_SECRET_KEY UNCHANGED for now). Append a "Phase 46 Auth Cutover" section to docs/RUNBOOK.md capturing the full checklist and the values' locations (not the secret values themselves). Then PAUSE for the owner to: (1) set STAFF_EMAILS, KIOSK_DEVICE_TOKEN, SHEETS_CLIENT_ID in Railway Variables; (2) deploy the middleware (merge/push zoho-middleware changes to the production repo so Railway redeploys) — old API_SECRET_KEY stays set, so old key + new credentials are BOTH accepted; (3) push the frontend to staging (`git push origin main`) so staging (which calls prod middleware) can be verified.
  </action>
  <how-to-verify>
    1. In Railway, confirm STAFF_EMAILS, KIOSK_DEVICE_TOKEN, SHEETS_CLIENT_ID are present and the deploy is live (check /health returns 200 authenticated:true redis:true).
    2. curl the middleware with the OLD x-api-key on a mutating route → still accepted (dual-accept proven).
    3. Store KIOSK_DEVICE_TOKEN in the password manager.
  </how-to-verify>
  <acceptance_criteria>
    - Railway shows STAFF_EMAILS, KIOSK_DEVICE_TOKEN, SHEETS_CLIENT_ID set; API_SECRET_KEY unchanged
    - `GET /health` returns 200 with authenticated:true and redis:true
    - a request bearing the old x-api-key still succeeds (dual-accept confirmed)
    - docs/RUNBOOK.md has the Phase 46 cutover section
  </acceptance_criteria>
  <resume-signal>Type "deployed" once env vars are set, middleware is live, dual-accept is confirmed, and staging has the new frontend.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Provision iPad + verify all three surfaces under new auth</name>
  <what-built>Dual-accept backend is live; new frontend is on staging. Claude provides the exact per-surface verification steps.</what-built>
  <how-to-verify>
    1. KIOSK: on the store iPad, open kiosk.html (staging), open the hidden device-token settings prompt, paste KIOSK_DEVICE_TOKEN, save. Confirm the PIN pad appears (no Google sign-in). Enter the PIN and ring up a real test sale end-to-end (terminal charge → Zoho invoice) — confirm it books. Confirm customer search returns results (now via /api/contacts/search).
    2. ADMIN: open admin.html on a laptop, sign in with an allowlisted Google account → dashboard loads. Perform an attributable action that hits an admin-grade route (e.g. open a report / a gift-card void view) → succeeds. Sign in with a NON-allowlisted Google account → denied.
    3. BREWPAD: open brewpad.html, Google sign-in → authenticated; load a batch list (session-authenticated) → works.
    4. NEGATIVE: from the kiosk device token, confirm an admin-grade route (gift-card void) is refused (403) — device scope holds.
  </how-to-verify>
  <acceptance_criteria>
    - kiosk reaches the PIN pad from a stored device token with no Google login, and completes a real sale
    - allowlisted admin loads dashboard + an admin-grade action; non-allowlisted admin is denied
    - BrewPad loads session-authenticated data
    - kiosk device token is refused on gift-card void (403)
  </acceptance_criteria>
  <resume-signal>Type "verified" once all three surfaces work under the new auth and the negative device-scope check passes, or describe any failure.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rotate API_SECRET_KEY + confirm old key dead, no lockout</name>
  <what-built>All surfaces verified under new credentials. Claude provides the new key value and the canary/verification commands.</what-built>
  <action>
    Claude: generate the new key with `openssl rand -base64 32` and present it; optionally set API_SECRET_KEY_PREVIOUS to the OUTGOING value for canary logging (if 46-03 wired the canary). PAUSE for the owner to: (1) within ~2-3 business days of go-live (D-46-12), promote the frontend to production (`git push production main --force`); (2) rotate API_SECRET_KEY in Railway to the new value (this ends dual-accept — nothing should still need the old key because all surfaces migrated); (3) optionally set API_SECRET_KEY_PREVIOUS to the retired value for canary detection. Then update docs/RUNBOOK.md with the rotation date, the retired-key disposition, and the D-46-13 note (interim IP allowlist SKIPPED — not needed; cutover completed in days).
  </action>
  <how-to-verify>
    1. curl the middleware with the OLD (retired) x-api-key on a mutating route → now 403 (old key dead).
    2. Re-verify a kiosk sale, an admin action, and a BrewPad load still work (no lockout post-rotation).
    3. Public checkout on prod (a ferment reservation hitting /api/bookings + /api/contacts + /api/payment/initialize) completes without 403 (keyless exemptions hold).
    4. If canary enabled, confirm logs show no legitimate traffic still presenting the old key (any hit = a missed migration to chase).
  </how-to-verify>
  <acceptance_criteria>
    - old API_SECRET_KEY value returns 403 after rotation (leaked key neutralized)
    - kiosk + admin + BrewPad all still function post-rotation (no lockout)
    - public prod checkout completes with no 403 on /bookings, /contacts, /payment/initialize
    - docs/RUNBOOK.md records the rotation date, retired-key disposition, and the D-46-13 SKIPPED note
  </acceptance_criteria>
  <resume-signal>Type "rotated" once the old key is confirmed dead, all surfaces still work, public checkout passes, and the runbook is updated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leaked key (git history + prior public JS) → prod middleware | Valid until rotation; the whole point of cutover |
| owner secrets → Railway | Device token + rotated key handled out-of-band |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-11c | Information Disclosure | leaked API_SECRET_KEY still valid | mitigate | rotation ends validity; verified dead via curl 403 post-rotation (the CRITICAL close-out) |
| T-46-05 | Repudiation | legacy-key stragglers post-rotation | mitigate | optional API_SECRET_KEY_PREVIOUS canary logging; any hit chased as a missed migration |
| T-46-28 | Availability | rotation locks out a surface still on old key | mitigate | migrate + verify ALL surfaces (Task 2) BEFORE rotating (Task 3); dual-accept window gives soak time |
| T-46-29 | Elevation of Privilege | window lingers too long (D-46-12) | mitigate | rotate within ~2-3 business days; scheduled owner action with checklist |
| D-46-13 | (decision record) | interim IP allowlist | accept/skip | SKIPPED per D-46-13 — Phase 45 containment shipped, cutover is days away, store IP may be dynamic; recorded in RUNBOOK |
</threat_model>

<verification>
- Post-rotation curl with old key → 403
- kiosk sale + admin action + BrewPad load + public prod checkout all succeed post-rotation
- docs/RUNBOOK.md updated with rotation outcome + D-46-13 disposition
</verification>

<success_criteria>
- Leaked key neutralized (old key dead), no surface locked out, public checkout intact, runbook recorded — the audit CRITICAL is closed
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-10-SUMMARY.md` when done.
</output>
