---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 09
type: execute
wave: 5
depends_on: ["46-03", "46-04", "46-05", "46-06", "46-07", "46-08"]
files_modified:
  - js/main.js
  - js/main.min.js
  - js/admin.min.js
  - js/kiosk.min.js
  - js/brewpad.min.js
  - js/modules/16-catalog-subpage.min.js
  - js/modules/17-search-overlay.min.js
  - admin.html
  - kiosk.html
  - brewpad.html
  - index.html
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-03, D-46-10]
user_setup: []

must_haves:
  truths:
    - "All frontend bundles are rebuilt from the migrated sources and cache-busted"
    - "The full frontend + middleware test suites and lint pass on the complete auth re-architecture"
    - "No x-api-key or MW_API_KEY reference survives anywhere in shipped source"
  artifacts:
    - path: "js/kiosk.min.js"
      provides: "Rebuilt kiosk bundle with device-token auth"
    - path: "js/admin.min.js"
      provides: "Rebuilt admin bundle with session auth"
  key_links:
    - from: "js/*.js sources"
      to: "js/*.min.js bundles"
      via: "npm run build regenerates artifacts + stamps HTML"
      pattern: "min.js"
---

<objective>
Regenerate every frontend bundle from the migrated sources (`npm run build`) so the shipped `*.min.js` and cache-busted HTML reflect the new auth model, then run the **complete pre-cutover gate**: full frontend Jest suite, full middleware Jest suite, and lint — all green — plus a repo-wide grep proving no `x-api-key`/`MW_API_KEY` reference survives in source. This is the last plan before the owner-driven cutover; it must certify the whole system builds and tests clean.

Purpose: The build stamps all HTML and regenerates all bundles in one shot (the repo's monolithic build), so it runs once after every source plan (03-08) is complete — not per plan. Output: rebuilt artifacts + a clean full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md

<interfaces>
Build: `npm run build` = stamp + stamp:admin/kiosk/brewpad/index/pages + minify:css + minify:js. It regenerates js/main.js (concat), js/main.min.js, js/admin.min.js, js/kiosk.min.js, js/brewpad.min.js, js/modules/16/17 min, and cache-busts every HTML page. NEVER hand-edit *.min.js.
Suites: root `npm test` (jest jsdom, tests/frontend/**), root `npm run lint` (eslint js/), and `cd zoho-middleware && npm test`.
CLAUDE.md gate: never commit with failing tests; run both suites + lint before commit.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild all bundles</name>
  <files>js/main.js, js/main.min.js, js/admin.min.js, js/kiosk.min.js, js/brewpad.min.js, js/modules/16-catalog-subpage.min.js, js/modules/17-search-overlay.min.js, admin.html, kiosk.html, brewpad.html, index.html</files>
  <read_first>
    - package.json (build/minify/stamp scripts)
  </read_first>
  <action>
    Run `npm run build` from the repo root. Confirm it completes without terser/cleancss errors and that admin/kiosk/brewpad/index/pages HTML cache versions were bumped. Do not hand-edit any generated file. The full set of build-churn files (other product/subpage HTML) will also be re-stamped — that is expected repo build behavior.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 && test -f js/kiosk.min.js && test -f js/admin.min.js && test -f js/brewpad.min.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 with no minify errors
    - `js/kiosk.min.js`, `js/admin.min.js`, `js/brewpad.min.js`, `js/main.min.js` are regenerated (newer mtime)
    - `kiosk.html`/`admin.html`/`brewpad.html`/`index.html` cache-bust `?v=` tokens updated
  </acceptance_criteria>
  <done>All bundles rebuilt from migrated sources and HTML cache-busted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full-suite gate + no-key grep proof</name>
  <files>zoho-middleware/package.json</files>
  <read_first>
    - CLAUDE.md (pre-commit gate)
  </read_first>
  <action>
    Run, in order: (1) `npm test` (frontend jest) — all green; (2) `npm run lint` — zero errors; (3) `cd zoho-middleware && npm test` — all green. Then prove the leak is gone from source (excluding generated *.min.js which mirror source and comment lines): `grep -rn --include=*.js -e "x-api-key" -e "MW_API_KEY" js/ | grep -v "\.min\.js"` must return nothing (kiosk/admin/brewpad/sheets-config/12/16/17 all migrated). If any grep hit remains, it indicates an unmigrated call site — fix it in the owning source file (do not suppress). Re-run the affected suite after any fix.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -4 && npm run lint 2>&1 | tail -3 && (cd zoho-middleware && npm test 2>&1 | tail -4) && test -z "$(grep -rn --include=*.js -e 'x-api-key' -e 'MW_API_KEY' js/ | grep -v '\.min\.js')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend `npm test` exits 0
    - `npm run lint` exits 0
    - `cd zoho-middleware && npm test` exits 0
    - `grep -rn --include=*.js -e "x-api-key" -e "MW_API_KEY" js/ | grep -v '.min.js'` returns empty
  </acceptance_criteria>
  <done>The complete auth re-architecture builds and passes both suites + lint with no surviving key reference in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source → shipped bundles | Stale bundles could ship the old auth model |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-26 | Information Disclosure | stale *.min.js still embedding MW_API_KEY | mitigate | full rebuild + grep gate over source; bundles regenerated from migrated sources |
| T-46-27 | Availability | a missed unmigrated call site breaking a surface | mitigate | repo-wide no-key grep + both full suites gate before cutover |
</threat_model>

<verification>
- `npm test` + `npm run lint` + `cd zoho-middleware && npm test` → all green
- no-key grep over `js/**.js` (excluding *.min.js) → empty
</verification>

<success_criteria>
- Bundles rebuilt; both suites + lint green; leak absent from source — system certified ready for owner cutover
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-09-SUMMARY.md` when done.
</output>
