---
phase: 46-auth-re-architecture-critical-split-from-phase-45
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - js/brewpad.js
  - tests/frontend/brewpad-session-auth.test.js
autonomous: true
requirements: [AUDIT-CRITICAL-AUTH, D-46-09, D-46-06]
user_setup: []

must_haves:
  truths:
    - "BrewPad verifies identity via /auth/google and relies on the sv_session cookie, dropping MW_API_KEY"
    - "Every BrewPad API call sends the session cookie via credentials:'include'"
    - "BrewPad's silent-refresh fallback still works (checkAuthorization(onError) signature preserved)"
  artifacts:
    - path: "js/brewpad.js"
      provides: "Session-cookie authenticated BrewPad"
      contains: "credentials"
  key_links:
    - from: "js/brewpad.js"
      to: "/auth/google"
      via: "checkAuthorization POSTs access_token"
      pattern: "/auth/google"
    - from: "js/brewpad.js"
      to: "session cookie transport"
      via: "credentials:'include' on staff fetches"
      pattern: "credentials"
---

<objective>
Migrate `js/brewpad.js` to the same server-session model as admin (D-46-09) — BrewPad already Google-signs-in, so point its existing sign-in at `POST /auth/google` and drop the `MW_API_KEY` header from all ~22 staff calls in favour of `credentials:'include'`. Preserve BrewPad's distinct `checkAuthorization(onError)` signature (it is used as the silent-refresh fallback callback), swapping only its internal implementation.

Purpose: BrewPad ships the leaked key today; it must migrate before rotation or it breaks. One auth system across all staff surfaces. Do NOT run `npm run build` — the rebuild is 46-09.
Output: modified `js/brewpad.js`, a frontend test proving session transport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-CONTEXT.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-RESEARCH.md
@.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md

<interfaces>
Server: POST /auth/google {access_token} → {authorized, email} + sv_session cookie. Staff fetches use credentials:'include'.
js/brewpad.js sites (46-PATTERNS.md brewpad section): initGoogleAuth (~869), onTokenResponse (~1008), checkAuthorization(onError) (~1042 — NOTE the extra onError param vs admin's parameterless version; PRESERVE it), adminApiGet (~1221), mwApiKey() helper (~1265: returns SHEETS_CONFIG.MW_API_KEY||''). ~22 x-api-key/mwApiKey refs (grep-confirmed incl. lines 1491, 1512, 1546, 1563, 1605, 1692, 3843, 3889, 3984, 4013, 4453, 4484, 4627, 5274, 5484, 6846, 6882, 6960, 7021). Silent-refresh fallback context: line ~935-936 "checkAuthorization failed... fall back to silent refresh".
ES5 only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: checkAuthorization(onError) → /auth/google, preserving signature</name>
  <files>js/brewpad.js</files>
  <read_first>
    - js/brewpad.js (initGoogleAuth ~869, onTokenResponse ~1008, checkAuthorization ~1042, silent-refresh fallback ~935)
    - js/admin.js (the analog checkAuthorization change from 46-06 for consistency)
    - .planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-PATTERNS.md (js/brewpad.js section)
  </read_first>
  <action>
    Rewrite checkAuthorization(onError)'s body to `fetch(mwUrl()+'/auth/google', { method:'POST', credentials:'include', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ access_token: accessToken }) })`, parse JSON, on `authorized` proceed to the authenticated BrewPad view, else invoke the existing denied/`onError` path. KEEP the `onError` parameter and continue calling it on failure so the silent-refresh fallback (line ~935) keeps working — on a fetch/verify failure, call `onError` exactly as today (which triggers `tokenClient.requestAccessToken({prompt:''})` re-auth then re-check). Do not change initGoogleAuth or onTokenResponse init. Any client-side allowlist logic is superseded by the server allowlist.
  </action>
  <verify>
    <automated>grep -q "/auth/google" js/brewpad.js && grep -q "credentials" js/brewpad.js && grep -q "checkAuthorization" js/brewpad.js && npx eslint js/brewpad.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - checkAuthorization still accepts an `onError` argument and calls it on failure
    - it POSTs to `/auth/google` with `credentials:'include'` and body `access_token`
    - `npx eslint js/brewpad.js` reports no errors
  </acceptance_criteria>
  <done>BrewPad identity uses the server session exchange while retaining its silent-refresh fallback contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap all BrewPad API calls to credentials:'include'</name>
  <files>js/brewpad.js</files>
  <read_first>
    - js/brewpad.js (re-grep `grep -n "x-api-key\|mwApiKey\|MW_API_KEY" js/brewpad.js` for the authoritative sites)
  </read_first>
  <action>
    Re-grep for the authoritative list. For every BrewPad fetch sending `'x-api-key': mwApiKey()`/`SHEETS_CONFIG.MW_API_KEY`: remove the header and add `credentials:'include'` to the fetch options. Then delete or neutralize the now-unused `mwApiKey()` helper (leaving it returning '' harmlessly is acceptable if simpler). Ensure no `SHEETS_CONFIG.MW_API_KEY` reference remains. Leave batch/recipe logic untouched.
  </action>
  <verify>
    <automated>test $(grep -c "MW_API_KEY" js/brewpad.js) -eq 0 && test $(grep -c "x-api-key" js/brewpad.js) -eq 0 && grep -q "credentials: 'include'\|credentials:'include'" js/brewpad.js && npx eslint js/brewpad.js 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MW_API_KEY" js/brewpad.js` is 0 and `grep -c "x-api-key" js/brewpad.js` is 0
    - BrewPad staff fetches use `credentials:'include'`
    - `npx eslint js/brewpad.js` reports no errors
  </acceptance_criteria>
  <done>All BrewPad calls transport the session cookie; no MW_API_KEY reference remains.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Frontend test for BrewPad session transport</name>
  <files>tests/frontend/brewpad-session-auth.test.js</files>
  <read_first>
    - tests/frontend/brewpad-activation.test.js (brewpad jsdom harness pattern)
    - js/brewpad.js (checkAuthorization + a representative staff fetch)
  </read_first>
  <action>
    Write a jsdom test that: (1) `/auth/google` → {authorized:true} proceeds to the authenticated view; (2) {authorized:false} invokes the denied/onError path; (3) a representative BrewPad staff fetch uses `credentials:'include'` and omits `x-api-key`. Follow the existing brewpad test harness.
  </action>
  <verify>
    <automated>npx jest tests/frontend/brewpad-session-auth.test.js -x 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - test proves authorized true/false branches
    - test proves a staff fetch uses `credentials:'include'` and omits `x-api-key`
    - `npx jest tests/frontend/brewpad-session-auth.test.js` exits 0
  </acceptance_criteria>
  <done>BrewPad session auth proven in jsdom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| off-site browser → BrewPad API | Session cookie replaces shared secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-23 | Denial of Service (lockout) | BrewPad still sending old key at rotation | mitigate | migrate BrewPad to session before rotation (D-46-09); grep gate asserts no MW_API_KEY |
| T-46-04c | Tampering (CSRF) | credentialed cross-site BrewPad calls | mitigate | requireAllowedReferer still runs server-side; httpOnly cookie |
| T-46-24 | Availability | silent-refresh fallback broken by signature change | mitigate | preserve checkAuthorization(onError) contract; test both branches |
</threat_model>

<verification>
- `npx jest tests/frontend/brewpad-session-auth.test.js` → passes
- `grep -c "MW_API_KEY\|x-api-key" js/brewpad.js` → 0
- `npx eslint js/brewpad.js` → clean
</verification>

<success_criteria>
- BrewPad authenticates via the server session cookie with its refresh fallback intact; tests + lint green
</success_criteria>

<output>
Create `.planning/phases/46-auth-re-architecture-critical-split-from-phase-45/46-07-SUMMARY.md` when done.
</output>
