---
phase: 54-gift-card-management-on-the-kiosk-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/authTiers.js
  - zoho-middleware/__tests__/auth-tiers-guard.test.js
  - zoho-middleware/__tests__/pos-auth-tier.test.js
autonomous: true
requirements: [KIOSK-GC-54]

must_haves:
  truths:
    - "A request to POST /api/kiosk/gift-card/void with a valid x-device-token is NOT rejected with 403 (D-54-GC)"
    - "A device token still cannot reach other admin-grade routes (PII GET /api/contacts, BrewPad GET /api/batch/search-invoices, admin GET /api/orders/recent all remain device→403)"
    - "The authTiers.js comment block records the D-54-GC reversal while preserving the explicit-allowlist (not-a-prefix) rationale for future routes"
  artifacts:
    - path: "zoho-middleware/lib/authTiers.js"
      provides: "KIOSK_ROUTES entry for /api/kiosk/gift-card/void"
      contains: "/api/kiosk/gift-card/void"
    - path: "zoho-middleware/__tests__/auth-tiers-guard.test.js"
      provides: "flipped device-not-403 void assertion + intact PII negative"
    - path: "zoho-middleware/__tests__/pos-auth-tier.test.js"
      provides: "flipped device-not-403 void assertion + intact BrewPad/admin negatives"
  key_links:
    - from: "authTiers.js KIOSK_ROUTES"
      to: "isKioskRoute() / global guard on /api/kiosk/gift-card/void"
      via: "array membership (indexOf)"
      pattern: "gift-card/void"
---

<objective>
Widen the kiosk device-token scope so a bare device token may void a gift certificate, per owner decision D-54-GC (consciously superseding D-46-02 / T-46-07 which kept void session/admin-only). This is a surgical, explicit-path allowlist change to `/api/kiosk/gift-card/void` ONLY — negative-scope coverage for every other admin-grade route stays intact and asserted.

Purpose: The owner runs everything from the standalone kiosk; the void handler relies on the global guard + `isKioskRoute`, so a single KIOSK_ROUTES membership change flips its scope with no route-handler edit. This backend change is independently deployable and MUST land before / alongside the frontend panel (Plan 02) that calls it.

Output: Updated `authTiers.js` allowlist + corrected T-46-07 comment; the two existing `device→403 on gift-card/void` tests flipped to expect not-403 with positive coverage; negatives preserved; full middleware AND frontend suites green (authTiers.js is a shared lib).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-gift-card-management-on-the-kiosk-surface/54-CONTEXT.md
@.planning/phases/54-gift-card-management-on-the-kiosk-surface/54-PATTERNS.md

<interfaces>
<!-- Extracted from zoho-middleware/lib/authTiers.js — executor uses these directly. -->
KIOSK_ROUTES is an explicit-path array (line 36+). Current relevant entries:
  '/api/kiosk/gift-card/lookup'   (line 39, already device-scoped — the analog entry)
  '/api/kiosk/gift-card/next-number' (line 40)
isKioskRoute(path) (lines 75-83): returns KIOSK_ROUTES.indexOf(path) !== -1
The T-46-07 comment block sits at lines ~20-27 and CURRENTLY names /api/kiosk/gift-card/void
as the example of what must NOT be pulled in by a prefix. That void-specific claim is the
one being reversed by D-54-GC; the "explicit list, not a prefix" rationale stays.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /api/kiosk/gift-card/void to KIOSK_ROUTES and correct the T-46-07 comment</name>
  <files>zoho-middleware/lib/authTiers.js</files>
  <read_first>
    - zoho-middleware/lib/authTiers.js (read the T-46-07 comment block ~lines 20-27, the KIOSK_ROUTES array ~lines 36-57, and isKioskRoute ~75-83 before editing)
    - .planning/phases/54-gift-card-management-on-the-kiosk-surface/54-PATTERNS.md (§authTiers.js — the exact insertion point and comment-rewrite guidance)
  </read_first>
  <action>
    Insert the string literal '/api/kiosk/gift-card/void' into the KIOSK_ROUTES array, adjacent to the existing '/api/kiosk/gift-card/lookup' / '/api/kiosk/gift-card/next-number' entries, with an inline comment referencing D-54-GC (note it supersedes T-46-07 / D-46-02; void is status-only, reason-required, logged). Then rewrite the T-46-07 comment block (~lines 20-27): remove the claim that gift-card/void must be kept OUT of the allowlist, replace it with a note that void is now an intentionally-reviewed member per D-54-GC, and KEEP the "explicit list, not a `/api/kiosk/*` prefix" rationale for any FUTURE kiosk route. Do NOT change isKioskRoute() or requireTiers() — membership alone flips the scope. No other route strings added.
  </action>
  <verify>
    <automated>grep -q "'/api/kiosk/gift-card/void'" zoho-middleware/lib/authTiers.js && grep -q "D-54-GC" zoho-middleware/lib/authTiers.js && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `authTiers.js` KIOSK_ROUTES contains the exact literal `/api/kiosk/gift-card/void`.
    - The inline comment on that entry names D-54-GC and supersedes T-46-07/D-46-02.
    - The comment block still contains the "explicit list, not a prefix" rationale (grep for "prefix" still matches) but no longer claims void must be excluded.
    - `isKioskRoute` and `requireTiers` function bodies are byte-unchanged (only the array + comments changed).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Flip the two device→403 void tests to not-403 + add positive coverage; keep all other negatives; gate on BOTH suites</name>
  <files>zoho-middleware/__tests__/auth-tiers-guard.test.js, zoho-middleware/__tests__/pos-auth-tier.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/auth-tiers-guard.test.js (test (3) ~line 118 to flip; test (4) session-not-403 ~128 as the target shape; PII negative (7a) ~157 to KEEP)
    - zoho-middleware/__tests__/pos-auth-tier.test.js (test (3) ~line 123 to flip; test (4) ~133; negatives (7b) ~210 and (8a) ~223 to KEEP)
    - .planning/phases/54-gift-card-management-on-the-kiosk-surface/54-PATTERNS.md (§both test files — exact lines + the mirror-test-(4) assertion shape)
  </read_first>
  <behavior>
    - auth-tiers-guard test (3): POST /api/kiosk/gift-card/void with x-device-token → status NOT 403 (mirror test (4)'s `not.toBe(403)`). Rename/reword title + describe to record D-54-GC (device now allowed).
    - pos-auth-tier test (3): POST /api/kiosk/gift-card/void with x-device-token → NOT 403.
    - Unchanged and still passing: auth-tiers-guard (7a) GET /api/contacts device→403; pos-auth-tier (7b) GET /api/batch/search-invoices device→403; pos-auth-tier (8a) GET /api/orders/recent device→403.
  </behavior>
  <action>
    In each test file, change the void-route assertion from `expect(res.status).toBe(403)` to `expect(res.status).not.toBe(403)` and update the test title/describe comment to state that D-54-GC now allows the device token to void (intended behavior change, not a broken test). Do NOT modify test (4) (session was already not-403). Do NOT weaken the PII-GET / BrewPad-GET / admin-GET device→403 negatives — leave them exactly as-is; they are the "other admin-grade routes stay locked" guarantee. Because authTiers.js is a shared lib (CLAUDE.md rule 7), run the FULL middleware suite AND the full frontend suite; both must be green before this plan is considered done.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test && cd .. && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test` exits 0 (full middleware suite green).
    - `npm test` (frontend) exits 0 — required because authTiers.js is a shared lib.
    - `npm run lint` exits 0.
    - The two flipped tests assert `not.toBe(403)` on POST /api/kiosk/gift-card/void with x-device-token.
    - `grep -c "expect(res.status).toBe(403)"` still finds the PII-GET, BrewPad-GET and admin-GET device negatives in these files (they were NOT removed).
    - No test other than the two void tests was modified (CLAUDE.md rule 10 — do not touch other tests).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk iPad → middleware | bare device token crosses here; now authorized for gift-card/void |
| other clients → middleware | device token must remain rejected on PII/BrewPad/admin routes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Elevation of Privilege | `KIOSK_ROUTES` + POST /api/kiosk/gift-card/void | accept | Owner-accepted per D-54-GC. A leaked/stolen device token could void a certificate, but void is status-only (no cash / no Zoho money movement), a non-empty `reason` is enforced server-side (400 if empty, routes/gift-cards.js), and every void is logged via `eventLog.logEvent('kiosk.gift_card_voided', …)`. Manager-PIN gate consciously declined (deferred). |
| T-54-02 | Elevation of Privilege | scope-widening blast radius | mitigate | Add ONLY the explicit path `/api/kiosk/gift-card/void` (NOT an `/api/kiosk/*` prefix, per T-46-07). Preserve and assert the device→403 negatives for PII GET `/api/contacts`, BrewPad GET `/api/batch/search-invoices`, admin GET `/api/orders/recent`. isKioskRoute stays an exact-membership check. |
| T-54-SC | Tampering | npm/pip installs | accept | No new packages installed this phase — supply-chain surface unchanged. |
</threat_model>

<verification>
- `grep "'/api/kiosk/gift-card/void'" zoho-middleware/lib/authTiers.js` → 1 match inside KIOSK_ROUTES.
- `cd zoho-middleware && npm test` green; `npm test` (frontend) green; `npm run lint` clean.
- Device→403 negatives for PII/BrewPad/admin GETs still present and passing.
</verification>

<success_criteria>
- POST /api/kiosk/gift-card/void with a valid x-device-token is no longer rejected 403 (D-54-GC in effect).
- Every other admin-grade route still rejects a device token with 403 (asserted).
- Both test suites + lint green (authTiers.js shared-lib dual-suite gate satisfied).
- One logical change per commit: Task 1 (scope + comment) and Task 2 (test flips) committed separately.
</success_criteria>

<output>
Create `.planning/phases/54-gift-card-management-on-the-kiosk-surface/54-01-SUMMARY.md` when done.
</output>
