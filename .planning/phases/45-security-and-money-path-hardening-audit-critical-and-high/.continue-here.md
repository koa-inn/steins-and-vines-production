---
context: phase
phase: 45-security-and-money-path-hardening-audit-critical-and-high
task: "45-09 live UAT — F2 fixed, paused after step 4a"
total_tasks: 8
status: in_progress
last_updated: 2026-07-01T19:33:26.701Z
---

# Continue Here — Phase 45 / 45-09 Gift-Card Split-Tender Live UAT

<current_state>
45-09 was executed and is in **live-card UAT** (standalone kiosk → prod Railway → live Helcim + Zoho, store closed, owner's own card). UAT is **paused after step 4a**. Steps 1–3 PASS; 4a done-with-findings; 4b/5/6/7/8 NOT run.

This session (2026-07-01, Opus 4.8 — Fable 5 still unavailable for the account) **investigated and fixed F2**, the last open UAT defect. F1/F3/F4 were already handled in the prior session. F2 fix is **committed on `main` but NOT deployed and NOT live-verified**.
</current_state>

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mocks mirroring invented shapes | F1 (prior session): unit mocks used a fictional Apps Script key (`balance`) that never occurs in prod (`current_balance`) — code + tests agreed on a shape that never exists → false green, prod-blocking 503. | blocking | Derive mock shapes from a **real captured response/contract**, not from the code under test. For any money-path mock, cite the source of the shape. |
| Assuming logs are retrievable | F2: the incident txn's own logs (INV-000129) had **rotated out** — Railway CLI tails only the *current* deployment, and 3 in-session deploys replaced it. | advisory | When reasoning from logs, confirm the target deployment is still live; otherwise reason from adjacent same-deployment evidence and say so explicitly. |

<completed_work>

- F1: gift-card balance read `current_balance` — FIXED `51f3c64`, deployed + verified live.
- F3: exempt custom line tagged with `ZOHO_TAX_ZERO_ID` — FIXED `97e8124`, **deployed**, needs live end-to-end verify.
- F4: filed GitHub issue #108 (cart re-sync UX).
- **F2: FIXED this session (A + B), committed, NOT deployed:**
  - Root cause (Railway logs): premature manual-confirm race. The "Confirm Manually" button appeared at **15s**, but a real card approval takes **~21s** — staff preempted the poll/webhook, booking `transaction_id='manual-confirm'` (no real Helcim id). Proven by a same-session HEALTHY auto-confirm (INV-000130 @ ~21s via API-poll fallback); no webhook/token-scope errors.
  - Fix A (`js/kiosk.js`): manual-confirm reveal `15s → POLL_TIMEOUT_MS` (45s). Rebuilt `kiosk.min.js`.
  - Fix B (`zoho-middleware/routes/pos.js`): `/api/kiosk/sale/confirm` verifies a manual confirm via `helcimLib.pollTerminalResult(reference_number)` before booking — approved → real id; declined → 400; unverifiable → 409 fail-closed (45-08 sweep settles a genuine orphan). Real txn id (auto-confirm) trusted, skips the lookup. Closes phantom-revenue + txn-id-fidelity gaps.
  - Regression: `pos-money-defects.test.js` F2-A/B/C/D (RED→GREEN). Suites green (mw 1122, fe 928), lint 0 errors.
  - Commits: **`d8bf965`** (backend + tests + findings doc), **`e029108`** (frontend + build).
</completed_work>

<remaining_work>

1. **Deploy the F2 commits (`d8bf965` + `e029108`)** to staging → prod, then **live-verify F2** at the kiosk: a real manual-confirm records the true Helcim id; a manual-confirm with NO charge refuses to book (400/409).
2. **Verify F3 live** — one throwaway exempt custom-line probe sale must book `tax_total:0` in Zoho.
3. **Re-run UAT** steps 4b (full redeem), 5 (void cert), 6 (over-balance clamp), 7 (redeem-failure → needs_manual_review), 8 (double-tap idempotency).
4. **Cleanup** — void cert `GC-000001` ($10 active); reverse `INV-000127`/`000128`/`000129`; refund ~$20 of real card charges.
5. **Reconcile build churn + stashes** (see Infrastructure State).
</remaining_work>

<decisions_made>

- F2 root cause = premature manual-confirm race, NOT a broken webhook/poll (both healthy in logs).
- F2 fix = A (frontend delay) + B (server-side verification), owner-approved.
- B **fails closed** (409) on an unverifiable terminal result — booking without proof-of-charge is the phantom-revenue risk; reconciliation (45-08) settles a genuine orphan.
- F2 severity downgraded: not lost money (recovered same-second); real defects were missing txn id + latent no-verification phantom risk.
- Committed as 2 clean file-scoped commits on `main`; pre-existing build churn left untouched.
</decisions_made>

<blockers>
- F2 fix committed but **undeployed / unverified live** → deploy + one live manual-confirm.
- F3 fix deployed but **not confirmed** against live Zoho → probe sale.
- Build churn + 2 stashes unreconciled → owner decision.
</blockers>

## Required Reading (in order)
1. `.planning/phases/45-.../45-09-UAT-FINDINGS.md` — F2 section (root cause + fix) and F1/F3/F4 context.
2. `.planning/STATE.md` — Session Continuity + Decisions (D-11..D-13, 45-05..45-08 money-path primitives).
3. `.planning/HANDOFF.json` — structured state (this pause).

## Infrastructure State
- **Prod middleware:** image `sha256:5ec14723` (after F3). All Phase 45 money-path waves + 45-09 are live. **The F2 fix is the ONLY undeployed change.**
- **Working tree:** pre-existing `npm run build` churn — `about.html`, `admin.html`, `js/admin.*`, `products/*.html`, `reservation.html`, etc. + `zoho-middleware/ingredients-cache.json`. None of it is the F2 fix.
- **Stashes:** `stash@{0}` = "45-09 deploy prep: park build churn + resume edits"; `stash@{1}` = "stale-build-stamps-pre-29.4 (recoverable)". Both pending. This session's `git reset` moved pre-existing STAGED churn → unstaged (nothing lost).
- **`.planning/STATE.md`** F2 continuity update is uncommitted (intentional).
- **`~/.zshrc:16`** has a stale `GH_TOKEN` shadowing valid keychain creds — `gh` works only via `env -u GH_TOKEN gh`.
- **Model:** Fable 5 still unavailable for this account (checked 2026-07-01); running on Opus 4.8.

<context>
The money path is server-authoritative and fail-closed by design (D-11..D-13; shared `lib/money-path.js`). F2's Fix B extends that philosophy to the manual-confirm escape hatch: never book a card payment we can't prove happened. When resuming, the single highest-value next action is deploying + live-verifying F2 so the whole 45-09 UAT can be driven to completion, then the cleanup + churn reconcile close out the phase.
</context>

<next_action>
Start with: deploy the F2 commits (`d8bf965` + `e029108`) to staging (`git push origin main`), smoke-check, then to production per CLAUDE.md deploy steps — and live-verify F2 at the kiosk (real manual-confirm → true Helcim id; no-charge manual-confirm → refuses to book). Then F3 probe, UAT 4b–8, cleanup, and build-churn/stash reconcile.
</next_action>
