---
phase: 45-security-and-money-path-hardening-audit-critical-and-high
plan: 08
type: execute
wave: 4
depends_on: ["45-05", "45-07"]
files_modified:
  - zoho-middleware/lib/reconcile.js
  - zoho-middleware/routes/webhooks.js
  - zoho-middleware/server.js
  - zoho-middleware/__tests__/reconcile.test.js
autonomous: true
requirements: [AUDIT-H-ORPHAN]

must_haves:
  truths:
    - "Traceability — D-13: bounded Helcim↔Zoho reconciliation backstop keyed on reference_number"
    - "A late Helcim approval for a pending kiosk charge with no matching Zoho order is auto-voided or flagged for manual review"
    - "A pending kiosk charge that DID create a Zoho order is cleared (no false orphan)"
    - "A bounded periodic sweep reconciles pending charges and no-ops cleanly when Redis is down"
  artifacts:
    - path: "zoho-middleware/lib/reconcile.js"
      provides: "reconcilePendingCharge + sweepPendingCharges keyed on reference_number"
      exports: ["reconcilePendingCharge", "sweepPendingCharges"]
      min_lines: 50
    - path: "zoho-middleware/routes/webhooks.js"
      provides: "kiosk pending-charge reconciliation on late approval"
      contains: "reconcilePendingCharge"
    - path: "zoho-middleware/server.js"
      provides: "registered periodic sweep"
      contains: "sweepPendingCharges"
  key_links:
    - from: "lib/reconcile.js"
      to: "lib/helcim.js getCardTransactionById"
      via: "Helcim<->Zoho match on reference_number"
      pattern: "getCardTransactionById"
---

<objective>
Add the bounded Helcim↔Zoho reconciliation backstop (D-13): synchronous void-on-failure (45-06/07) cannot catch a LATE terminal approval after the 90s timeout, so reconcile any charge with no matching Zoho order — via the late webhook AND a periodic sweep — keying the match on reference_number = Helcim transaction id. Auto-void or flag orphans for manual review.

Purpose: A real orphan charge already occurred (money in, no order, no void, nothing detects it). This backstop closes that late-approval window.
Output: lib/reconcile.js (new) + webhook reconcile wiring + a registered periodic sweep + tests.

This was planned as a SEPARABLE track (D-13) so it can be trimmed if too large; it is scoped tightly to consuming the pending-charge records 45-07 persists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AUDIT-2026-06-29.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-PATTERNS.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-07-SUMMARY.md

<interfaces>
Consume from 45-07: C.CACHE_KEYS.KIOSK_PENDING_CHARGE_PREFIX + the pending-charge record shape (reference_number, amount, salesorder/invoice ids, idempotency_key, created_at).
lib/helcim.js: getCardTransactionById(id) at :304-325 → { status, transactionId, invoiceNumber, cardType, amount }; voidTransaction(transactionId) at :144.
routes/webhooks.js: handleCardTransaction(event) at :84-209; resolves txn via getCardTransactionById at :90; collect-pending block at :166-208; respond-200-before-async at :47/:56.
routes/collect.js:99-127 — the pending-context settlement pattern.
server.js: existing scheduled-sweep area ~:522-531 (brewpad retry sweeps) — the place to register a periodic interval.
Void-failure persistence convention: cache.set('sv:void-failure:'+Date.now(), { ..., needs_manual_review:true }, 30d) (pos.js:1007/1664).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/reconcile.js (match on reference_number)</name>
  <files>zoho-middleware/lib/reconcile.js, zoho-middleware/__tests__/reconcile.test.js</files>
  <read_first>
    - zoho-middleware/lib/helcim.js (lines 304-325 getCardTransactionById; 144 voidTransaction)
    - zoho-middleware/routes/collect.js (lines 99-127 pending settlement pattern)
    - zoho-middleware/lib/constants.js (KIOSK_PENDING_CHARGE_PREFIX from 45-07)
    - zoho-middleware/routes/pos.js (lines 1007, 1664 — the sv:void-failure persistence convention)
  </read_first>
  <behavior>
    - Test 1: a pending charge whose Helcim txn is APPROVED but has NO matching Zoho order → voidTransaction is called (or, if void fails, an sv:void-failure record with needs_manual_review is persisted + alert).
    - Test 2: a pending charge that DID create a matching Zoho order → the pending record is cleared, no void.
    - Test 3: a Helcim lookup failure → the pending record is left intact for a later attempt (no destructive action).
  </behavior>
  <action>
    Create lib/reconcile.js exporting reconcilePendingCharge(reference) and sweepPendingCharges(). For each KIOSK_PENDING_CHARGE_PREFIX pending record: resolve the Helcim txn via helcimLib.getCardTransactionById keyed on reference_number; if status is APPROVED and no matching Zoho order exists (check by the pending record's salesorder/invoice id / reference) → call helcimLib.voidTransaction, and on void failure persist sv:void-failure:<ts> { ..., needs_manual_review:true } (30d TTL) + mailer alert (reuse the pos.js:1007/1664 convention); if a matching Zoho order exists → delete the pending record (settled, mirror collect.js settlement); if the Helcim lookup fails → leave the record for a later attempt. sweepPendingCharges iterates the pending keys (bounded) and calls reconcilePendingCharge per record; it must no-op cleanly when cache is disconnected.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- reconcile</automated>
  </verify>
  <acceptance_criteria>
    - reconcilePendingCharge: APPROVED + no Zoho order → voidTransaction (or orphan flagged on void failure); matching order → pending cleared; lookup failure → record intact.
    - lib/reconcile.js exports reconcilePendingCharge and sweepPendingCharges.
    - Unit tests cover all three branches.
  </acceptance_criteria>
  <done>The reconciliation logic detects and remediates orphan charges keyed on reference_number, with tests for each branch.</done>
</task>

<task type="auto">
  <name>Task 2: Wire webhook reconcile + register the periodic sweep</name>
  <files>zoho-middleware/routes/webhooks.js, zoho-middleware/server.js</files>
  <read_first>
    - zoho-middleware/routes/webhooks.js (lines 84-209 handleCardTransaction + collect-pending block 166-208; respond-200 at 47/56)
    - zoho-middleware/server.js (lines ~522-531 — the existing scheduled-sweep registration pattern)
    - zoho-middleware/lib/reconcile.js (from Task 1)
  </read_first>
  <action>
    In webhooks.js handleCardTransaction, after the existing collect-pending block (:166-208), also reconcile KIOSK_PENDING_CHARGE records: on a late Helcim approval, call reconcile.reconcilePendingCharge for the matching reference_number (preserve the respond-200-before-async pattern at webhooks.js:47/:56 — process after responding). In server.js, register a bounded periodic sweep near the existing scheduled-sweep area (~:522-531) that calls reconcile.sweepPendingCharges on a fixed cadence (e.g. every 5 minutes); guard it so it no-ops cleanly when Redis is disconnected and does not throw on the interval.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- reconcile webhooks</automated>
  </verify>
  <acceptance_criteria>
    - A late terminal webhook for a pending kiosk charge invokes reconcile.reconcilePendingCharge (asserted).
    - server.js registers a periodic sweep calling sweepPendingCharges; the sweep no-ops when cache is disconnected.
    - webhooks.js still responds 200 before async processing; full suite green.
  </acceptance_criteria>
  <done>Late approvals and a periodic sweep both reconcile pending kiosk charges; orphan charges are auto-voided or flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helcim terminal/webhook → middleware → Zoho | Late approvals after the timeout deadline can orphan a charge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08-ORPHAN | Repudiation / Integrity | post-timeout late approval | mitigate | webhook reconcile + periodic sweep auto-void/flag charges with no Zoho order (match on reference_number) — same class as the original incident |
| T-45-08-CADENCE | — | sweep window | accept | bounded cadence (~5 min) leaves a small detection window; acceptable as a backstop behind synchronous void (45-06/07) |
| T-45-08-MULTI | — | multi-instance sweep duplication | accept | single Railway instance; documented (matches the brewpad-sweep assumption) |
| T-45-08-SC | Tampering | npm installs | mitigate | no new packages (uses existing helcim/cache/mailer) |
</threat_model>

<verification>
- cd zoho-middleware && npm test (full suite green)
- Orphan (APPROVED + no Zoho order) → voided/flagged; settled pending → cleared
- Late webhook + periodic sweep both invoke reconciliation; sweep no-ops when Redis down
</verification>

<success_criteria>
The late-approval orphan-charge window is closed by a reconciliation backstop keyed on reference_number, layered behind the synchronous void-on-failure from 45-06/07.
</success_criteria>

<output>
Create `.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-08-SUMMARY.md` when done.
</output>
