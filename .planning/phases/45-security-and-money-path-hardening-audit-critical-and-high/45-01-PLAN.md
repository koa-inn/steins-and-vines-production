---
phase: 45-security-and-money-path-hardening-audit-critical-and-high
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/pos.js
  - .gitignore
autonomous: true
requirements: [AUDIT-H-PII, AUDIT-L-PIN, AUDIT-L-RDB]

must_haves:
  truths:
    - "GET /api/kiosk/salesorders and /api/kiosk/salesorder/:id reject requests with no API key (401)"
    - "A misconfigured KIOSK_PIN no longer 500s every login (no staff lockout)"
    - "dump.rdb cannot be accidentally committed"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "apiKeyGuard on the two kiosk PII GETs + KIOSK_PIN length guard"
      contains: "apiKeyGuard.matches"
    - path: ".gitignore"
      provides: "rdb ignore rule"
      contains: "rdb"
  key_links:
    - from: "routes/pos.js GET /api/kiosk/salesorders"
      to: "lib/apiKey.js apiKeyGuard.matches"
      via: "x-api-key header guard"
      pattern: "apiKeyGuard\\.matches"
---

<objective>
Close the Wave-1 quick-win containments from the audit that are pure backend code changes: guard the two unguarded kiosk PII GET routes (D-09), add a KIOSK_PIN length-check before timingSafeEqual so a misconfig can't lock out staff (D-15), and gitignore dump.rdb (D-15).

Purpose: Reduce blast radius NOW — the order book is currently readable unauthenticated, and a non-4-char KIOSK_PIN 500s every login. These are owner-approved to ship to prod first (D-15).
Output: Hardened pos.js (two guarded routes + PIN length guard) and a dump.rdb ignore rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AUDIT-2026-06-29.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-PATTERNS.md

<interfaces>
From zoho-middleware/lib/apiKey.js (already imported at pos.js:8 as apiKeyGuard):
- apiKeyGuard.matches(sent): boolean — header-only, length-checked-first, constant-time compare.
Sibling guarded routes already use this exact pattern at pos.js:1192, 1263, 1728, 1798.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the two unguarded kiosk PII GET routes (D-09)</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (lines ~1190-1375 for the salesorders list + sibling guards at 1192/1263; lines ~2620-2660 for salesorder/:id)
    - zoho-middleware/lib/apiKey.js (the matches() guard)
  </read_first>
  <action>
    Prepend the sibling guard pattern (already used at pos.js:1192, 1263, 1728, 1798) to the two currently-unguarded handlers: router.get('/api/kiosk/salesorders', ...) at pos.js:1303 and router.get('/api/kiosk/salesorder/:id', ...) at ~pos.js:2638. The guard checks apiKeyGuard.matches(req.headers['x-api-key']) and returns 401 {error:'Unauthorized'} when it does not match. apiKeyGuard is already imported at pos.js:8 — do not add a new import. Do NOT change either route's success response shape. Use the inline-guard approach (not server.js PII_GET_ROUTES) because the :id route needs an Express path pattern; the inline mirror is the clean match here.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- pos</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/kiosk/salesorders with no x-api-key header returns 401; with a valid key returns 200 with the unchanged { salesorders: [...] } shape.
    - GET /api/kiosk/salesorder/:id with no x-api-key returns 401; with a valid key returns the order.
    - A new supertest asserts the 401-without-key and 200-with-key for both routes.
  </acceptance_criteria>
  <done>Both kiosk PII GETs reject keyless requests with 401 and serve authorized requests unchanged; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: KIOSK_PIN length-check before timingSafeEqual (D-15)</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (lines ~568-585, the verify-pin handler — note the existing `if (!process.env.KIOSK_PIN)` at 574 and the unguarded timingSafeEqual at 578)
    - zoho-middleware/lib/apiKey.js (lines ~29-36, the length-first-then-timingSafeEqual pattern to mirror)
  </read_first>
  <action>
    Before crypto.timingSafeEqual(Buffer.from(pin), Buffer.from(process.env.KIOSK_PIN)) at pos.js:578, add a length guard mirroring lib/apiKey.js: if process.env.KIOSK_PIN is unset OR process.env.KIOSK_PIN.length !== pin.length, return 503 { ok:false, error:'PIN not configured' } instead of letting timingSafeEqual throw a RangeError (which currently surfaces as a 500 on every login → staff lockout). Length is not secret, so comparing lengths first is safe. Write the regression test FIRST (per CLAUDE.md): a mismatched-length KIOSK_PIN must yield 503, not a 500/RangeError.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- pos</automated>
  </verify>
  <acceptance_criteria>
    - With KIOSK_PIN set to a value whose length differs from the submitted pin, verify-pin returns 503 (not 500 / no RangeError).
    - With a correctly-configured KIOSK_PIN, a correct pin still authenticates and a wrong same-length pin returns the existing 401.
    - A regression test reproduces the misconfig-500 first, then passes after the guard.
  </acceptance_criteria>
  <done>verify-pin returns 503 on a misconfigured PIN length and never throws RangeError; happy-path login unchanged; regression test green.</done>
</task>

<task type="auto">
  <name>Task 3: Gitignore dump.rdb (D-15)</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (confirm no existing rdb rule)
    - zoho-middleware/dump.rdb (present in the working tree; currently untracked)
  </read_first>
  <action>
    Add `*.rdb` and an explicit `zoho-middleware/dump.rdb` entry to .gitignore so the Redis dump (which can contain cached PII/tokens) can never be picked up by an accidental `git add .`. dump.rdb is currently untracked (git ls-files returns nothing), so no `git rm --cached` is required — but if `git ls-files zoho-middleware/dump.rdb` ever returns the path, run `git rm --cached zoho-middleware/dump.rdb` to untrack while preserving the working-tree file.
  </action>
  <verify>
    <automated>git check-ignore zoho-middleware/dump.rdb && test -z "$(git ls-files zoho-middleware/dump.rdb)" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore zoho-middleware/dump.rdb` prints the path (it is ignored).
    - `git ls-files zoho-middleware/dump.rdb` returns empty (not tracked).
  </acceptance_criteria>
  <done>dump.rdb is gitignored and untracked; the working-tree file is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → kiosk API | Unauthenticated HTTP requests reach kiosk GET/POST routes |
| working tree → git history | Cached PII/tokens could be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-PII | Information Disclosure | GET /api/kiosk/salesorders, /api/kiosk/salesorder/:id | mitigate | apiKeyGuard.matches() → 401 (audit: unauthenticated read of whole order book) |
| T-45-01-PIN | Denial of Service | verify-pin (pos.js:578) | mitigate | length guard before timingSafeEqual → 503, no RangeError 500 lockout |
| T-45-01-RDB | Information Disclosure / Tampering | zoho-middleware/dump.rdb | mitigate | gitignore so cached PII/tokens cannot be committed |
| T-45-01-AUTH | Spoofing | static API key still in use | accept | residual: the leaked key remains valid until the Phase 46 auth cutover (D-04). Guarding these routes still reduces blast radius; exploitation requires reaching the middleware with the key. |
| T-45-01-SC | Tampering | npm installs | mitigate | no new packages added by this plan; no install step |
</threat_model>

<verification>
- cd zoho-middleware && npm test (full middleware suite green — no regression to existing kiosk tests)
- The two kiosk PII GETs return 401 without a key
- verify-pin returns 503 (not 500) on a misconfigured PIN length
- dump.rdb is gitignored and untracked
</verification>

<success_criteria>
Wave-1 code containments landed: both kiosk PII GETs guarded, KIOSK_PIN misconfig no longer locks out staff, dump.rdb ignored. Full middleware test suite passes.
</success_criteria>

<output>
Create `.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-01-SUMMARY.md` when done.
</output>
