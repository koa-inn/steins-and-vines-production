---
phase: 45-security-and-money-path-hardening-audit-critical-and-high
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified: []
autonomous: false
requirements: [AUDIT-H-PII, AUDIT-C-AUTH]

must_haves:
  truths:
    - "Traceability — D-15: ship Wave-1 containments to prod Railway via railway up; D-17: middleware deploys straight to prod after tests pass"
    - "The already-committed key-guard (#2 e8b81ce) and PII-log redaction (#10 7c68f05) are live on prod Railway"
    - "Prod rejects keyless reads of the two kiosk PII GETs (401)"
    - "Kiosk PIN login still works on prod after the KIOSK_PIN length guard"
  artifacts: []
  key_links:
    - from: "prod Railway middleware"
      to: "lib/apiKey.js header-only guard (#2) + lib/redact.js (#10) + 45-01 changes"
      via: "railway up deploy"
      pattern: "railway"
---

<objective>
Ship the Wave-1 containments to production now (owner-approved, D-15): deploy the already-committed key-guard (#2, e8b81ce) and PII-log redaction (#10, 7c68f05) plus the 45-01 code changes to the prod Railway middleware, then verify the containment holds on prod.

Purpose: D-15 — the first wave ships to prod immediately to reduce blast radius before the larger money-path and auth waves land. Middleware deploys straight to prod Railway (no staging middleware, D-17).
Output: Deployed prod middleware + verified containment.

DEPLOY ORDERING NOTE: This deploy must happen BEFORE the later money-path waves (45-05/06/07/08) land in prod, so the production ship is a clean containment of HEAD-at-this-point. Coordinate the `railway up` against the repo state that contains 45-01 (and the pre-committed #2/#10) but not yet the money-path refactors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AUDIT-2026-06-29.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy Wave-1 containments to prod Railway</name>
  <action>Deploy the Wave-1 containments (#2 e8b81ce key guard, #10 7c68f05 PII redaction, and the 45-01 code) to the prod Railway middleware via `railway up`; record the deploy id. Owner-gated prod money-path deploy — must precede the later money-path waves landing in prod.</action>
  <read_first>
    - zoho-middleware/lib/apiKey.js (the #2 header-only guard already committed in e8b81ce)
    - zoho-middleware/lib/redact.js (the #10 PII redaction already committed in 7c68f05)
  </read_first>
  <what-built>
    The repo HEAD now contains: the header-only constant-time key guard (#2, e8b81ce), the PII-log email redaction (#10, 7c68f05), and the 45-01 quick-win code (two guarded kiosk PII GETs, KIOSK_PIN length guard, dump.rdb gitignore). None of #2/#10 is live on prod yet (deployed-pending per D-15).
  </what-built>
  <how-to-verify>
    1. Confirm `cd zoho-middleware && npm test` is green at the commit being deployed.
    2. Deploy the middleware to prod Railway: `railway up` (or the owner's standard prod-Railway deploy flow). There is no staging middleware (D-17) — this is the prod instance.
    3. Record the Railway deploy id / SHA for the runbook.
  </how-to-verify>
  <resume-signal>Type the Railway deploy id (or "deployed") once `railway up` reports success, or describe any deploy failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify prod containment</name>
  <action>Verify the deployed containment on prod: keyless kiosk PII GETs return 401 (200 with key), kiosk PIN login still works, and PII logs are redacted. Confirm all five how-to-verify checks before resuming.</action>
  <what-built>Wave-1 containments live on prod.</what-built>
  <how-to-verify>
    1. `curl -s -o /dev/null -w "%{http_code}" https://<prod-middleware-host>/api/kiosk/salesorders` → expect 401 (no key).
    2. Same request WITH the valid `x-api-key` header → expect 200 with the order list.
    3. `curl` `/api/kiosk/salesorder/<a-known-id>` with no key → 401; with key → 200.
    4. On the in-store iPad, log in with the correct kiosk PIN → succeeds (KIOSK_PIN length guard did not break the happy path).
    5. Trigger a payload that logs an email and confirm the prod logs show the redacted form (#10).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass, or describe the failing check.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → prod middleware | Unauthenticated requests hit the live money-path service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-PII | Information Disclosure | live kiosk PII GETs | mitigate | deploy + verify 401-without-key on prod |
| T-45-02-AUTH | Spoofing | leaked static key | accept (documented residual) | D-04 rotate-at-cutover: the key in git history stays valid until Phase 46. This deploy guards the PII reads and redacts PII logs but does NOT rotate the key. Residual risk window is explicit and owner-accepted; an interim network containment (D-05) may front-run it if cutover is delayed. |
</threat_model>

<verification>
- Prod returns 401 on keyless kiosk PII GETs, 200 with key
- Kiosk PIN login works on prod
- PII logs redacted on prod
</verification>

<success_criteria>
Wave-1 containments are live on prod and verified. Residual key-rotation risk is documented and carried to Phase 46.
</success_criteria>

<output>
Create `.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-02-SUMMARY.md` when done. Record the prod Railway deploy id / SHA.
</output>
