---
phase: 45-security-and-money-path-hardening-audit-critical-and-high
plan: 09
type: execute
wave: 5
depends_on: ["45-03", "45-05", "45-06", "45-07", "45-08"]
files_modified: []
autonomous: false
requirements: [AUDIT-H-GC, AUDIT-H-IDEM, AUDIT-H-CONFIRM, AUDIT-H-ORPHAN]

must_haves:
  truths:
    - "The hardened money-path waves are deployed to prod Railway"
    - "A single controlled real-card session verifies BOTH Phase 44's deferred live gift-card UAT and the new split-tender balance-validation fix"
    - "A deliberate double-tap during the session does not double-charge"
  artifacts: []
  key_links:
    - from: "prod Railway middleware (money-path waves)"
      to: "live Helcim terminal + Zoho"
      via: "one controlled real-card UAT session"
      pattern: "UAT"
---

<objective>
Deploy the hardened money-path waves to prod and run ONE controlled real-card session that verifies both Phase 44's deferred live gift-card UAT and the new split-tender balance-validation fix (D-16) — avoiding running the live gift-card test twice. Money-path waves get extra care given the live dependency (D-17).

Purpose: D-16 — bundle the split-tender fix verification with Phase 44's still-deferred live gift-card UAT (one real card session covers both). The money-path changes (45-03/05/06/07/08) can only be fully trusted after a real charge.
Output: Deployed prod money-path + a completed live UAT (or gaps → a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AUDIT-2026-06-29.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-CONTEXT.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-08-UAT.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy money-path waves to prod Railway</name>
  <action>Deploy the money-path waves (45-03/05/06/07/08) to the prod Railway middleware via `railway up` only after the full middleware suite is green; record the deploy id. Owner-gated.</action>
  <read_first>
    - .planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-06-SUMMARY.md
    - .planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-07-SUMMARY.md
    - .planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-08-SUMMARY.md
  </read_first>
  <what-built>
    Redis fail-closed policy (45-03), shared money-path lib + checkout refactor (45-05), kiosk sale/confirm atomic idempotency + error-propagating confirm (45-06), split-tender balance validation + timeout pending-persist (45-07), and the reconciliation backstop (45-08).
  </what-built>
  <how-to-verify>
    1. Confirm `cd zoho-middleware && npm test` is green at the commit being deployed (the full money-path suite, including the new pos-money / pos-giftcard / reconcile / redis-failclosed tests).
    2. Deploy the middleware to prod Railway (`railway up` / owner's standard flow). No staging middleware (D-17).
    3. Record the Railway deploy id / SHA for the runbook.
  </how-to-verify>
  <resume-signal>Type the Railway deploy id (or "deployed"), or describe any failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bundled live gift-card + money-path UAT (D-16)</name>
  <action>Run ONE controlled real-card session that verifies both Phase 44s deferred live gift-card UAT and the new split-tender balance-validation + atomic-idempotency fixes, following the how-to-verify steps; confirm Zoho accounting and no orphan charge.</action>
  <read_first>
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-08-UAT.md (the deferred P44 live gift-card checklist)
  </read_first>
  <what-built>Hardened kiosk money path: atomic idempotency, error-propagating confirm, split-tender balance validation, reconciliation backstop.</what-built>
  <how-to-verify>
    Run ONE controlled real-card session on the in-store iPad (one physical gift certificate + one real card), covering both checklists in a single pass:

    Phase 44 deferred live gift-card UAT (from 44-08-UAT.md):
    1. Issue a new certificate via cart + terminal → real charge → certificate activates on payment success → confirm the Zoho liability accounting (Gift Card Sales / liability, no tax at sale).
    2. Look up the live balance.
    3. Reload the certificate via cart + terminal → balance increments on payment success.
    4. Partial redeem (split tender) → terminal charges the remainder; full redeem → terminal skipped.
    5. Void a certificate (admin-only).

    New Phase 45 money-path fixes:
    6. Split-tender balance validation: attempt to apply MORE than the certificate's balance → applied amount is clamped to the real balance and the terminal charges the correct remainder.
    7. Redeem-failure signal: confirm a forced/simulated redeem failure surfaces needs_manual_review to staff (if safely reproducible; otherwise confirm the unit-test coverage and note it).
    8. Double-tap safety: deliberately double-tap the pay/confirm action with the same idempotency_key → exactly one charge / one Zoho invoice (atomic idempotency), no duplicate.

    Verify Zoho reflects each step correctly and there is no orphaned charge.
  </how-to-verify>
  <resume-signal>Type "approved" if every step passes, or describe the failing step(s) — gaps route to a `/gsd:plan-phase 45 --gaps` closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-store iPad → live Helcim terminal → Zoho | Real money moves; the only place the hardened path is proven under a real charge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09-VERIFY | Integrity | money-path under real charge | mitigate | one controlled real-card session proves split-tender clamp, atomic idempotency, and gift-card accounting end-to-end |
| T-45-09-LATE | Repudiation | late-approval reconciliation | accept | a real post-90s late approval cannot be reliably forced in UAT; covered by 45-08 unit tests + the periodic sweep — documented |
| T-45-09-SC | Tampering | deploy | mitigate | deploy only after the full money-path suite is green (Task 1 gate) |
</threat_model>

<verification>
- Money-path waves deployed to prod with a green test suite
- One real-card session passes both the P44 gift-card checklist and the new split-tender/idempotency checks
- No orphaned charge; Zoho accounting correct
</verification>

<success_criteria>
The hardened money path is proven on a real charge: gift-card lifecycle + split-tender balance validation + atomic idempotency all verified in a single controlled session, closing both the P44 deferred UAT and the Phase 45 money fixes.
</success_criteria>

<output>
Create `.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-09-SUMMARY.md` when done. Update `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-08-UAT.md` to mark the deferred live gift-card UAT closed (or record gaps).
</output>
