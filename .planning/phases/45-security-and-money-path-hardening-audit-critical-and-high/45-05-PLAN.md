---
phase: 45-security-and-money-path-hardening-audit-critical-and-high
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/money-path.js
  - zoho-middleware/routes/checkout.js
  - zoho-middleware/__tests__/money-path.test.js
autonomous: true
requirements: [AUDIT-H-IDEM, AUDIT-H-CONFIRM, AUDIT-H-ORPHAN]

must_haves:
  truths:
    - "checkout.js's safety primitives live in a shared lib/money-path.js that pos.js can also consume"
    - "checkout.js still passes its existing money-path tests after the refactor (no behaviour change)"
  artifacts:
    - path: "zoho-middleware/lib/money-path.js"
      provides: "acquireIdempotencyLock, assertTxnNotReplayed, markTxnUsed, rejectWithVoid, voidWithTimeout"
      exports: ["acquireIdempotencyLock", "rejectWithVoid", "voidWithTimeout"]
      min_lines: 60
    - path: "zoho-middleware/routes/checkout.js"
      provides: "checkout consuming the shared lib"
      contains: "money-path"
  key_links:
    - from: "routes/checkout.js"
      to: "lib/money-path.js"
      via: "require + call the extracted primitives"
      pattern: "require\\(.*money-path"
---

<objective>
Extract the four checkout.js money-path safety primitives into a shared lib/money-path.js (D-11), then refactor checkout.js to consume it WITHOUT any behaviour change. This is the structural heart of the phase: nearly every money High is "kiosk pos.js lacks what checkout.js already has" — fix the divergence by creating one shared source of truth that both routes call (pos.js wired in 45-06/07/08).

Purpose: D-11 — stop maintaining two divergent money flows. The existing checkout money-path Jest tests are the guardrail (must stay green).
Output: lib/money-path.js (the four primitives) + checkout.js calling into it + lib unit tests.

This plan is INTERFACE-FIRST: it defines the contract that 45-06, 45-07, and 45-08 build against. Get the exported signatures right.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AUDIT-2026-06-29.md
@.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-PATTERNS.md

<interfaces>
Source primitives to extract (from zoho-middleware/routes/checkout.js):
- rejectWithVoid(res, body, status, errorMsg) — checkout.js:45-61 (voids an already-charged token before a 4xx; eventLog + mailer.sendVoidFailureAlert fallback)
- atomic idempotency lock gate — checkout.js:158-185 (cache.get replay → cache.acquireLock → 409 on contention; fail-CLOSED-in-prod on Redis throw, fail-open only in dev)
- transaction-id replay guard — checkout.js:248-270 (check, fail-closed) + :718-724 (mark used, 86400s TTL); key form 'helcim:txn:' + transactionId + suffix
- void-on-failure with 8s timeout — checkout.js:825-873 via helpers.withTimeout
Shaping template: zoho-middleware/lib/checkout-helpers.js (pure fns + lazy require, module.exports at :249; withTimeout at :34-39).
CHECKOUT_IDEMPOTENCY_TTL = 600 (checkout.js:35).
Dependencies the lib needs (lazy-require): lib/cache.js, lib/helcim.js, lib/mailer.js, lib/checkout-helpers.js (withTimeout).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/money-path.js with the four primitives</name>
  <files>zoho-middleware/lib/money-path.js, zoho-middleware/__tests__/money-path.test.js</files>
  <read_first>
    - zoho-middleware/routes/checkout.js (lines 35-61 rejectWithVoid; 158-185 lock gate; 248-270 + 718-724 replay guard; 825-873 void-on-failure)
    - zoho-middleware/lib/checkout-helpers.js (lines 34-39 withTimeout; 249 module.exports shape)
    - zoho-middleware/lib/cache.js, zoho-middleware/lib/helcim.js, zoho-middleware/lib/mailer.js (the collaborators the primitives call)
  </read_first>
  <behavior>
    - Test 1: acquireIdempotencyLock returns a "replay" result when the key is already cached (the cached body is surfaced for a 201 replay).
    - Test 2: acquireIdempotencyLock returns "contention" (→ 409) when the lock cannot be acquired.
    - Test 3: with NODE_ENV=production and cache throwing, acquireIdempotencyLock fails CLOSED (signals 409, not proceed); with NODE_ENV unset it fails open.
    - Test 4: assertTxnNotReplayed signals replay (409) when the txn key exists; markTxnUsed writes the key with an 86400s TTL.
    - Test 5: rejectWithVoid calls helcim.voidTransaction for a valid token and, on void rejection, calls mailer.sendVoidFailureAlert.
    - Test 6: voidWithTimeout resolves within the timeout and routes a timeout to the void-failure alert path.
  </behavior>
  <action>
    Create lib/money-path.js exporting (mirroring checkout.js verbatim, parametrised so both routes share them):
    - acquireIdempotencyLock(cache, key, ttl, opts) — replicates checkout.js:158-185: cache.get replay-return, cache.acquireLock → 409-on-contention, fail-CLOSED-in-prod on a Redis throw (opts.isProd, default from NODE_ENV==='production'). Return a discriminated result ({ status:'replay', cached } | { status:'contention' } | { status:'acquired' } | { status:'failclosed' }) so callers stay framework-thin.
    - assertTxnNotReplayed(cache, transactionId, suffix) and markTxnUsed(cache, transactionId, suffix, ttl=86400) — replicate checkout.js:248-270 / :718-724, key 'helcim:txn:'+transactionId+suffix, fail-closed on Redis error.
    - rejectWithVoid(res, body, status, errorMsg, deps) — verbatim from checkout.js:45-61 (token validation, helcim.voidTransaction, mailer.sendVoidFailureAlert fallback); deps lazily default to lib/helcim + lib/mailer.
    - voidWithTimeout(helcimLib, token, amount, opts) — replicate checkout.js:825-873 wrapped in checkout-helpers.withTimeout (default 8000ms), persisting sv:void-failure on failure + mailer alert.
    Use lazy require + module.exports object like checkout-helpers.js. Keep CHECKOUT_IDEMPOTENCY_TTL=600 importable.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- money-path</automated>
  </verify>
  <acceptance_criteria>
    - lib/money-path.js exports acquireIdempotencyLock, assertTxnNotReplayed, markTxnUsed, rejectWithVoid, voidWithTimeout.
    - Unit tests cover: replay return, 409 contention, fail-closed-in-prod on Redis throw, replay-guard check+mark, rejectWithVoid void+alert, voidWithTimeout timeout path.
    - cd zoho-middleware && npm test -- money-path exits 0.
  </acceptance_criteria>
  <done>The shared money-path primitives exist with tests, ready for both checkout.js and pos.js to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor checkout.js to consume lib/money-path.js (no behaviour change)</name>
  <files>zoho-middleware/routes/checkout.js</files>
  <read_first>
    - zoho-middleware/routes/checkout.js (the full money flow + test exports at :998-1002)
    - zoho-middleware/__tests__/ (the existing checkout money-path tests — the regression guardrail)
  </read_first>
  <action>
    Replace checkout.js's inline rejectWithVoid (:45-61), the lock gate (:158-185), the replay guard (:248-270 / :718-724), and the void-on-failure block (:825-873) with calls into lib/money-path.js. NO behaviour change — map each money-path result back to the exact same HTTP responses checkout.js returns today (201 replay, 409 contention/replay, 400 with void, etc.). Keep the existing test exports at :998-1002. The existing checkout money-path Jest suite is the guardrail and MUST stay green unchanged (do NOT modify those tests per CLAUDE.md rule 10).
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- checkout</automated>
  </verify>
  <acceptance_criteria>
    - checkout.js requires lib/money-path.js and no longer defines an inline rejectWithVoid / lock gate / replay guard / void-on-failure block.
    - The existing checkout money-path test suite passes unchanged.
    - Full middleware suite green (cd zoho-middleware && npm test).
  </acceptance_criteria>
  <done>checkout.js behaves identically but now sources its safety primitives from the shared lib.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → checkout/charge → Zoho | The money path where double-charge / orphan-charge defenses live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-05-REG | Tampering | checkout.js refactor | mitigate | existing checkout money-path tests are the regression guardrail; no behaviour change |
| T-45-05-DUP | — | divergent money flows | mitigate | single shared lib eliminates the source of the kiosk-vs-checkout drift (root cause of the money Highs) |
| T-45-05-SC | Tampering | npm installs | mitigate | no new packages; pure extraction from existing code |
</threat_model>

<verification>
- cd zoho-middleware && npm test (full suite green; existing checkout tests unchanged and passing)
- lib/money-path.js exports the four primitives with passing unit tests
- checkout.js imports from lib/money-path.js
</verification>

<success_criteria>
The checkout safety primitives are extracted into a shared, tested lib and checkout.js consumes them with zero behaviour change — the contract is ready for pos.js to adopt in 45-06/07/08.
</success_criteria>

<output>
Create `.planning/phases/45-security-and-money-path-hardening-audit-critical-and-high/45-05-SUMMARY.md` when done. Record the exact exported signatures so 45-06/07/08 executors consume them without re-exploring.
</output>
