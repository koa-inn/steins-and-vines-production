---
phase: 67-kiosk-tax-quote-charge-correctness
plan: 03
type: execute
wave: 3
depends_on: ["67-01", "67-02"]
files_modified: []
autonomous: false
requirements: [KIOSK-TAX-QUOTE-01]
must_haves:
  truths:
    - "On a real kiosk sale of a compound-tax (GST+PST) item, the kiosk-displayed grand total equals the card charge and the Zoho invoice total"
    - "A deliberately stale/divergent kiosk total is rejected by the server pre-charge assertion instead of charging the wrong amount"
    - "The deploy order held: middleware (67-01) went live before the frontend (67-02) started sending client_grand_total"
  artifacts: []
  key_links:
    - from: "kiosk displayed total"
      to: "Helcim charge + Zoho invoice"
      via: "live sale of a compound-tax item"
      pattern: "quote == charge == invoice"
---

<objective>
Live-verify the fix on the real kiosk against a real (or refundable test) sale, after 67-01 (middleware) and 67-02 (frontend) are deployed in that order. This is the goal-proof for the whole phase: the kiosk-displayed quote must equal the card charge and the Zoho invoice for a compound-tax item — the exact seam INV-000160 exposed. It is a separate wave-3 checkpoint so it never blocks the autonomous code waves.

Purpose: prove the quote≠charge seam is closed in production behavior, not just in tests.
Output: an owner-confirmed live-verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-kiosk-tax-quote-charge-correctness-pre-charge-total-assertio/67-CONTEXT.md
@.planning/debug/kiosk-tax-under-quote.md
@.planning/phases/67-kiosk-tax-quote-charge-correctness-pre-charge-total-assertio/67-01-PLAN.md
@.planning/phases/67-kiosk-tax-quote-charge-correctness-pre-charge-total-assertio/67-02-PLAN.md

<deploy_ordering>
CRITICAL: 67-01 (middleware) MUST be deployed to prod Railway BEFORE 67-02 (frontend) reaches the kiosk. The middleware asserts only when client_grand_total is present, so old cached kiosk JS stays safe; but the frontend must not send the field to a server that has not yet learned to accept/assert it. There is no separate staging middleware (v4.2 decision) — the middleware rides a prod deploy. Confirm the middleware is live (assertion + fail-closed computeTax) before pushing the frontend, then hard-refresh the kiosk to pick up the new kiosk-core.min.js.
</deploy_ordering>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>67-01 removed the server 5% guess (fail-closed on unresolvable tax) and added a pre-charge assertion comparing client_grand_total to the server grandTotal (±$0.01). 67-02 removed the client 5% fallback (missing tax now flags the item + blocks checkout), sends client_grand_total/client_tax_total on the sale body, and force-refreshes the catalog at cart-start. Deploy middleware first, then frontend.</what-built>
  <how-to-verify>
    1. Confirm deploy order: middleware (67-01) is live on prod Railway BEFORE the frontend (67-02) reaches the kiosk. Then hard-refresh the kiosk iPad so it loads the new kiosk-core.min.js (check the `kiosk-core.min.js?v=` query changed).
    2. Happy path — compound-tax quote==charge==invoice: on the real kiosk, ring a sale that includes a compound-tax (GST+PST, 12%) item — e.g. the champagne bottle SKU `750-champ-fl` from INV-000160, or `375-bor-br`. Note the kiosk-displayed grand total BEFORE tapping to pay. Complete the sale on the terminal (use a refundable test amount / a card you will refund). Confirm: (a) the Helcim charge amount equals the kiosk-displayed grand total, and (b) the resulting Zoho invoice total equals the same amount. Quote == charge == invoice, all at 12% on the compound-tax line.
    3. Assertion fires on divergence (optional but recommended): force a stale/divergent client total (e.g. keep the kiosk parked so its in-memory catalog is older than a catalog tax change, OR temporarily tamper the displayed total in devtools) and attempt checkout — confirm the server rejects with the "Totals changed — refresh the product list and re-ring the sale." error and NO card charge occurs.
    4. Missing-tax fail-closed (optional): if a catalog item without a resolvable tax rate is available, confirm the kiosk names it and blocks checkout rather than quoting 5%.
    5. Refund/reverse any real test charges and reverse the test Zoho invoice(s) afterward (follow the existing kiosk test-order cleanup routine used in prior phases).
  </how-to-verify>
  <resume-signal>Type "approved" with the invoice number + confirmation that displayed total == charge == invoice on the compound-tax item (and, if tested, that the assertion rejected a divergent total). Describe any mismatch instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy pipeline → prod | Middleware-before-frontend ordering must hold or old kiosk JS could hit a server that changed contract; verified in step 1. |
| live money path | Real terminal charge + Zoho invoice — verification uses refundable test charges cleaned up afterward. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-10 | Tampering / ordering | deploy sequence | mitigate | Step 1 confirms middleware live before frontend; assertion is field-present-only so old JS is unaffected. |
| T-67-11 | Denial of Service | false-positive assertion on a legit sale | mitigate | Happy-path step proves a correct compound-tax sale proceeds (no false reject) before relying on the assertion. |
| T-67-SC | Tampering (supply chain) | npm installs | accept | Verification-only plan — no code, no dependency installs. N/A. |
</threat_model>

<verification>
- Deploy order confirmed (middleware before frontend).
- Compound-tax item: kiosk-displayed total == Helcim charge == Zoho invoice.
- (If tested) divergent total → assertion 400, no charge.
- Test charges refunded and invoices reversed.
</verification>

<success_criteria>
- Owner-confirmed: a real compound-tax kiosk sale quotes, charges, and invoices the identical 12% total — the INV-000160 seam is closed in production.
</success_criteria>

<output>
Create `.planning/phases/67-kiosk-tax-quote-charge-correctness-pre-charge-total-assertio/67-03-SUMMARY.md` when done. Record the verification invoice number, the displayed/charged/invoiced totals (proving equality), whether the assertion-on-divergence path was exercised, and confirmation the deploy order held and test charges were refunded.
</output>
