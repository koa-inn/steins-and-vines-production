---
phase: 28-zoho-customer-read-back-path
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps-script/adminApi.gs
autonomous: false   # contains a checkpoint:human-verify and a checkpoint:human-action (Apps Script deploy)
# No requirement closes in this phase — foundation for ZSYNC-01 and ZSYNC-02 (Phase 29).
requirements: []
foundation_for: [ZSYNC-01, ZSYNC-02]

must_haves:
  truths:
    - "updateBatch() accepts customer_email and customer_phone as writable fields"
    - "Writing customer_email/customer_phone updates only those columns, leaving other batch fields untouched"
    - "sanitizeInput passes email addresses and phone numbers through unmangled"
    - "Calling the read endpoint then update_batch for a known linked batch makes the batch show current Zoho customer details on staging"
  artifacts:
    - path: "apps-script/adminApi.gs"
      provides: "customer_email and customer_phone in updateBatch allowedFields"
      contains: "customer_email"
  key_links:
    - from: "apps-script/adminApi.gs updateBatch allowedFields"
      to: "Batches sheet customer_email / customer_phone columns"
      via: "headers.indexOf(field) column lookup + sanitizeInput write"
      pattern: "customer_email"
---

<objective>
Open the Apps Script write path for the refreshed fields: add `customer_email` and `customer_phone` to `updateBatch()`'s `allowedFields` array so the Phase 29 browser caller can persist refreshed customer details onto a batch record by ID, leaving every other batch field untouched. Then manually verify the full read→write loop on staging for one known linked batch (success criterion #4).

Purpose: Closes the Apps Script side of the read-back path. The middleware read endpoint (Plan 01) plus this write extension complete the foundation Phase 29's UI orchestrates.
Output: Two-string edit to `adminApi.gs` allowedFields; a human-deployed Apps Script + a verified read→write loop on staging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-zoho-customer-read-back-path/28-CONTEXT.md
@.planning/phases/28-zoho-customer-read-back-path/28-RESEARCH.md
@.planning/phases/28-zoho-customer-read-back-path/28-PATTERNS.md

<interfaces>
<!-- Verified against the live adminApi.gs source. -->

From apps-script/adminApi.gs updateBatch() (allowedFields ~line 2157):
  var allowedFields = [
    'status', 'vessel_id', 'shelf_id', 'bin_id', 'notes',
    'zoho_so_number', 'customer_id', 'customer_name', 'product_name',
    'customer_firstname', 'customer_lastname',
    'fermentation_started_at', 'completed_at',
    'recipe_id',
    'start_date'
    // Phase 28 adds: 'customer_email', 'customer_phone'
  ];
  // The write loop (immediately below) does, per field:
  //   var colIndex = headers.indexOf(field);
  //   if (colIndex !== -1) sheet.getRange(row, colIndex + 1).setValue(sanitizeInput(String(updates[field])));
  // So a field whose column header is MISSING silently no-ops (colIndex === -1).

  // createBatch() already writes customer_phone (sanitizeInput(payload.customer_phone || '')) at ~line 834,
  // confirming the Batches sheet column header is literally 'customer_phone'.
  // sanitizeInput (~line 3126) strips HTML/script/handlers but NOT @ + - . ( ) or digits —
  // emails and phone numbers pass through unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add customer_email and customer_phone to updateBatch allowedFields</name>
  <files>apps-script/adminApi.gs</files>
  <read_first>
    - apps-script/adminApi.gs (lines ~2157–2173: the allowedFields array AND the write loop that indexes columns by header and sanitizes — confirm the loop no-ops on missing columns)
    - apps-script/adminApi.gs (lines ~799–834: createBatch positional write proving the sheet has a 'customer_email' column at create time and a 'customer_phone' column written via sanitizeInput — these are the exact header strings to match)
    - .planning/phases/28-zoho-customer-read-back-path/28-PATTERNS.md (the start_date Phase 27 precedent for how a field was added previously)
  </read_first>
  <action>
    In `updateBatch()`'s `allowedFields` array (~line 2157), append exactly two new string entries after `'start_date'`: `'customer_email'` and `'customer_phone'`, with a trailing comment `// Phase 28: refresh-from-Zoho write-back (D-09)`. These exact header strings must match the Batches sheet columns written by createBatch (verified: createBatch writes customer_email and sanitizeInput(payload.customer_phone || '') — both columns exist with these literal names). Make NO other change to updateBatch — the existing write loop, optimistic-locking (expectedVersion) check, and sanitizeInput call already handle the new fields. Do not add a new action, do not touch the recipe_snapshot special-case, do not reorder existing fields (D-09 locks "extend allowedFields, no dedicated action"). This mirrors the Phase 27 start_date addition exactly.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' apps-script/adminApi.gs | grep -c "'customer_email'" | grep -qx 1 && grep -v '^[[:space:]]*//' apps-script/adminApi.gs | grep -q "'customer_phone'" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The allowedFields array in updateBatch() contains both 'customer_email' and 'customer_phone' as string entries
    - Both strings appear after 'start_date' in the same array literal
    - No new Apps Script action/function was added (the diff is confined to the allowedFields array + one comment)
    - The recipe_snapshot special-case block below allowedFields is unchanged
  </acceptance_criteria>
  <done>updateBatch() now permits customer_email and customer_phone writes via the existing allowedFields mechanism; no other behavior changed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy updated adminApi.gs to the Apps Script project</name>
  <what-built>
    The `customer_email`/`customer_phone` write extension is in the local `apps-script/adminApi.gs` source. Apps Script runs from the Google-hosted project, which has no CLI deploy in this repo — the user pastes/pushes the updated source (this is how Phase 27's adminApi.gs changes went live, per STATE.md blocker note "deployed manually by user").
  </what-built>
  <how-to-verify>
    1. Open the bound Apps Script project for the Batches admin backend.
    2. Update `adminApi.gs` with the change from Task 1 (the two new allowedFields entries).
    3. Save and deploy the Apps Script so the staging-facing web app serves the new code.
    4. Confirm there are no Apps Script editor errors on save.
  </how-to-verify>
  <resume-signal>Type "deployed" once the updated adminApi.gs is live in the Apps Script project, or describe any editor/deploy errors.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the read→write loop on staging for one known linked batch (success criterion #4)</name>
  <what-built>
    The middleware read endpoint (Plan 01, deployed to staging) plus the Apps Script write extension (Task 2) together let staff pull current Zoho customer details onto a batch. D-12 locks manual verification — no throwaway script.
  </what-built>
  <how-to-verify>
    Prerequisite: Plan 01's endpoint must be deployed to staging first (`git push origin main` for the middleware; Railway redeploys). Pick one known batch whose `zoho_so_number` resolves in Zoho (an INV- number from a kiosk sale is ideal — see the INV-000078 contact_persons case as a real example).

    1. Read endpoint — curl the staging middleware:
       `curl -s -H "x-api-key: <MW_API_KEY>" "https://<staging-middleware>/api/batch/customer-by-number?number=INV-000078"`
       Confirm the JSON contains customer_name, customer_email (note D-07: email may come from the contact person), customer_phone, customer_id, document_number, document_status.
    2. Write-back — from the browser console or curl, call the staging Apps Script `update_batch` action for that batch's ID, sending ONLY the non-empty fields returned in step 1 plus the batch's `expectedVersion` (its current `last_updated`). (This hand-simulates the Phase 29 two-call flow, D-10/D-02.)
    3. Reload the batch in the admin Batches view (or re-fetch via get_batch) and confirm the batch record now shows the current Zoho customer name/email/phone, and that no OTHER batch field changed.

    Expected: the batch record reflects the live Zoho customer details after the loop. If email/phone came back null (contact_unavailable), confirm the existing batch values were preserved, not erased.
  </how-to-verify>
  <resume-signal>Type "verified" if the batch shows current Zoho details after the read→write loop with no collateral field changes, or describe the discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Apps Script update_batch | Staff-OAuth-gated write into the Batches sheet; `updates` payload carries customer email/phone strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Tampering / XSS | customer_email / customer_phone written into Batches sheet | mitigate | The existing allowedFields write loop runs `sanitizeInput(String(value))` on every field, including the two new ones — strips script/HTML/handlers; verified safe for emails/phones (Task 1) |
| T-28-06 | Tampering | Concurrent batch edit overwriting refreshed fields | mitigate | Existing optimistic-locking (`expectedVersion` vs `last_updated`) in updateBatch is unchanged and still guards these writes; the Phase 29 caller must pass expectedVersion (noted for Phase 29) |
| T-28-07 | Information Disclosure | Refresh erasing staff-entered data by writing blanks | accept (out of this phase's enforcement) | D-02 preserve-existing policy is enforced by the Phase 29 caller (send only non-empty fields); Apps Script writes whatever it receives. Task 3 verifies the preserve behavior manually; documented for Phase 29 |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `grep -c "'customer_email'" apps-script/adminApi.gs` (comment lines filtered) confirms the field is in allowedFields
- Human checkpoint Task 2: Apps Script deployed without editor errors
- Human checkpoint Task 3: staging read endpoint returns customer details and the subsequent update_batch makes the batch record show current Zoho details with no collateral changes (success criterion #4)
</verification>

<success_criteria>
- Phase success criterion #3: Apps Script exposes an update path writing refreshed customer name/email/contact onto an existing batch by ID, other fields untouched — satisfied via the allowedFields extension (Task 1) + deploy (Task 2)
- Phase success criterion #4: read endpoint + Apps Script update for a known linked batch results in the batch showing current Zoho details, verified on staging — satisfied via Task 3
</success_criteria>

<output>
Create `.planning/phases/28-zoho-customer-read-back-path/28-02-SUMMARY.md` when done
</output>
