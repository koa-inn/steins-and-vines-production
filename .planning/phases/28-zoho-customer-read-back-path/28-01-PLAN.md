---
phase: 28-zoho-customer-read-back-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/__tests__/batch-customer.test.js
autonomous: true
# No requirement closes in this phase — it is foundation for ZSYNC-01 and ZSYNC-02
# (built in Phase 29). Listed here for traceability per planning instructions.
requirements: []
foundation_for: [ZSYNC-01, ZSYNC-02]

must_haves:
  truths:
    - "GET /api/batch/customer-by-number resolves an INV- number to the linked customer's name, email, and phone"
    - "The endpoint resolves SO- numbers via /salesorders and INV- numbers via /invoices"
    - "A number that does not resolve returns 404 not_found, never a wrong document"
    - "A Zoho outage/quota/auth failure on the document call returns 502 zoho_error"
    - "A document that resolves but whose contact detail fetch fails returns 200 with contact_unavailable:true and null email/phone"
    - "Inputs not matching INV-/SO- shapes return 400 invalid_number before any Zoho call"
    - "Email falls back to the primary contact_person's email when the top-level contact email is blank"
    - "Middleware Jest suite covers success, not-found, Zoho-error, partial-200, validation, and auth paths; lint clean"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "GET /api/batch/customer-by-number handler"
      contains: "/api/batch/customer-by-number"
    - path: "zoho-middleware/__tests__/batch-customer.test.js"
      provides: "Unit tests for the new endpoint"
      contains: "customer-by-number"
  key_links:
    - from: "zoho-middleware/routes/pos.js"
      to: "lib/zoho-api.zohoGet"
      via: "two sequential params-object calls (document then contact)"
      pattern: "zohoGet\\('/(invoices|salesorders|contacts)"
    - from: "zoho-middleware/__tests__/batch-customer.test.js"
      to: "routes/pos.js handler"
      via: "express Router mock + findHandler('get', '/api/batch/customer-by-number')"
      pattern: "customer-by-number"
---

<objective>
Add the read direction to the existing write-only Zoho sync: a new authenticated middleware endpoint `GET /api/batch/customer-by-number` that, given an invoice or sales-order number, resolves the linked Zoho customer's name, email, and phone via two sequential Zoho Books calls, and returns a precise success/not-found/partial/error contract.

Purpose: This is the net-new read path Phase 29's "Refresh from Zoho" button depends on. No UI here.
Output: New route handler in `pos.js` and a new Jest test file covering all documented paths, lint clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-zoho-customer-read-back-path/28-CONTEXT.md
@.planning/phases/28-zoho-customer-read-back-path/28-RESEARCH.md
@.planning/phases/28-zoho-customer-read-back-path/28-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from the codebase. Use these directly — no exploration needed. -->

From zoho-middleware/lib/zoho-api.js (zohoGet — pass params as the SECOND argument):
  zohoGet(path, params) -> Promise<responseData>
  // organization_id is merged into params via Object.assign internally.
  // CORRECT:  zohoGet('/invoices', { invoice_number: number })
  // WRONG:    zohoGet('/invoices?invoice_number=' + number)   // breaks org_id merge

From zoho-middleware/routes/pos.js (existing analog handlers — already present):
  // Auth guard used by sync-zoho (line ~1306) and search-invoices (line ~1339):
  var apiKey = req.headers['x-api-key'] || req.query.api_key;
  if (apiKey !== process.env.MW_API_KEY) { return res.status(401).json({ error: 'Unauthorized' }); }

  // search-invoices uses string-concat zohoGet('/invoices?search_text=' + ...) — that is the
  // LEGACY anti-pattern. Do NOT copy it. Use the params-object form above.

  // 502 error block (kiosk/salesorder/:id, line ~1403):
  // log.error('[<prefix>] Zoho error: ' + (err.message || err));
  // res.status(502).json({ error: 'zoho_error', ... });

  // log is required at top of file as: var log = require('../lib/logger');
  // zohoGet is destructured at top: var zohoGet = zohoApi.zohoGet;

Zoho Books response shapes (verified in RESEARCH.md):
  // /invoices?invoice_number=INV-000123  -> { invoices: [{ invoice_id, customer_id, customer_name, status }] }
  // /salesorders?salesorder_number=SO-1  -> { salesorders: [{ salesorder_id, customer_id, customer_name, status }] }
  // /contacts/{customer_id}              -> { contact: { email, contact_persons: [{ email, phone, mobile, is_primary_contact }] } }
  // Zero-result list queries return 200 with an EMPTY ARRAY, never HTTP 404.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write batch-customer.test.js with the full path matrix (RED)</name>
  <files>zoho-middleware/__tests__/batch-customer.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/kiosk-salesorders.test.js (canonical mock block, express Router capture, makeReq/makeRes/flushPromises helpers, MW_API_KEY beforeEach/afterEach — copy these verbatim)
    - .planning/phases/28-zoho-customer-read-back-path/28-RESEARCH.md (the "Test file skeleton" code example and the path-by-path expectations)
    - .planning/phases/28-zoho-customer-read-back-path/28-PATTERNS.md (sequential mockResolvedValueOnce pattern for the two-call sequence)
  </read_first>
  <behavior>
    - 401 when x-api-key header is absent or wrong (no Zoho call made)
    - 400 invalid_number when number is missing, empty, or fails the INV-/SO- shape (no Zoho call made)
    - 404 not_found when zohoGet resolves { invoices: [] } (empty list, NOT an HTTP 404 from Zoho)
    - Success: zohoGet resolves invoice with customer_id/customer_name/status, then contact with blank top-level email but contact_persons[0].email set → response has customer_name, customer_email from the contact person (D-07 fallback), customer_phone from phone, document_number, document_status; no res.status() call (implicit 200)
    - Phone fallback: contact_persons[0].phone blank but mobile set → customer_phone equals the mobile value (D-04)
    - SO routing: number 'SO-1' calls zohoGet('/salesorders', { salesorder_number: 'SO-1' }) and reads data.salesorders (D-05)
    - Partial 200 (D-15): document resolves, second zohoGet rejects → 200 with customer_name set, customer_email null, customer_phone null, contact_unavailable true
    - 502 zoho_error when the first zohoGet rejects
  </behavior>
  <action>
    Create the test file modeled on kiosk-salesorders.test.js. Copy its top-level jest.mock block verbatim (helcim, zoho-api, cache, eventLog, logger, mailer, inventory-ledger) — all are required because require('../routes/pos') loads the whole file at parse time; omitting any mock breaks the require. Use the express Router capture pattern with a findHandler('get', '/api/batch/customer-by-number') lookup. Use makeReq/makeRes/flushPromises helpers and the MW_API_KEY save/restore in beforeEach/afterEach (test key 'test-api-key'). Drive the two-call sequence with chained mockResolvedValueOnce (document then contact); use mockRejectedValueOnce on the second call for the D-15 partial case and mockRejectedValue on the first for the 502 case. Assert on res._status and res._json per the behavior list. These tests MUST fail initially because the handler does not exist yet (findHandler throws) — that is the RED state.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/batch-customer.test.js 2>&1 | grep -q "No GET handler registered\|Cannot read\|fail\|FAIL" && echo "RED confirmed: handler not yet implemented"</automated>
  </verify>
  <acceptance_criteria>
    - File zoho-middleware/__tests__/batch-customer.test.js exists and contains the string 'customer-by-number'
    - It declares all seven jest.mock targets present in kiosk-salesorders.test.js (helcim, zoho-api, cache, eventLog, logger, mailer, inventory-ledger)
    - It contains at least these distinct test cases: 401-no-key, 400-invalid-number, 404-not-found, success-with-email-fallback, phone-mobile-fallback, SO-routing, partial-200-contact-fail, 502-zoho-error
    - Running the suite fails (RED) because the handler is not yet registered
  </acceptance_criteria>
  <done>Test file exists covering all documented paths; suite is RED (handler missing).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /api/batch/customer-by-number in pos.js (GREEN)</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (lines ~1304–1411: sync-zoho auth guard, search-invoices, kiosk/salesorder/:id 502 block — replicate auth + log-prefix + 502 conventions; place the new handler immediately after search-invoices, before module.exports)
    - zoho-middleware/lib/zoho-api.js (lines ~70–90: confirm zohoGet merges organization_id into the params object so you pass { invoice_number: number } as the second arg)
    - .planning/phases/28-zoho-customer-read-back-path/28-RESEARCH.md (the "GET handler skeleton" code example — the reference shape for response fields and branch handling)
    - zoho-middleware/__tests__/batch-customer.test.js (the contract your handler must satisfy — read the assertions before implementing)
  </read_first>
  <action>
    Add a `router.get('/api/batch/customer-by-number', ...)` handler. Auth: `var apiKey = req.headers['x-api-key'] || req.query.api_key; if (apiKey !== process.env.MW_API_KEY) return res.status(401).json({ error: 'Unauthorized' });` (copy from sync-zoho). Read `var number = (req.query.number || '').trim();`. Validate with `/^INV-\d+$/` and `/^SO-\d+$/`; if neither matches return `res.status(400).json({ error: 'invalid_number', message: 'number must match INV-NNNN or SO-NNNN format' })` BEFORE any Zoho call (D-16). Route by prefix (D-05): INV → path '/invoices', filter key 'invoice_number', list key 'invoices'; SO → path '/salesorders', filter key 'salesorder_number', list key 'salesorders'. Call 1: `zohoGet(path, filterParamsObject)` using the params-object form (NOT string concat — see interfaces). On resolve, read `docs = data[listKey] || []`; if `docs.length === 0` return `res.status(404).json({ error: 'not_found', message: ... })` (Zoho returns an empty array, never HTTP 404). Take `docs[0]`; pull `customer_id`, `customer_name`, `status` with `|| ''` defaults. If `customer_id` is blank, return a 200 partial with `contact_unavailable: true` and null email/phone. Otherwise Call 2: `zohoGet('/contacts/' + customerId)`. On resolve: `email = contact.email || primaryPerson.email || null` where primaryPerson is the `is_primary_contact` entry else `contact_persons[0]` else `{}` (D-07); `phone = primaryPerson.phone || primaryPerson.mobile || null` (D-04). Return 200 JSON with exactly: `customer_name`, `customer_id`, `customer_email`, `customer_phone`, `document_number` (the input number), `document_status` (the doc status, D-14 — voided/deleted statuses still return data). On Call 2 reject: log.warn with prefix `[batch/customer-by-number]` and return 200 partial (`customer_email: null, customer_phone: null, contact_unavailable: true`, name/status preserved) per D-15. Outer `.catch()` on Call 1: `log.error('[batch/customer-by-number] Zoho error: ' + (err.message || err)); res.status(502).json({ error: 'zoho_error', message: 'Failed to retrieve document from Zoho' });` per D-13. Do NOT cache any result (D-11). Do not add a new rate limiter — the global apiLimiter already covers /api/* at 60 req/min.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/batch-customer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - zoho-middleware/routes/pos.js registers `router.get('/api/batch/customer-by-number'` (grep -c confirms exactly one)
    - The handler uses the params-object form: `grep -n "zohoGet('/invoices', {" or zohoGet(path, ...)` present; NO `zohoGet('/invoices?invoice_number=` string-concat in the new handler
    - All tests in batch-customer.test.js pass (GREEN)
    - Response on success contains keys: customer_name, customer_id, customer_email, customer_phone, document_number, document_status
    - Validation rejects 'BADNUM' with 400 invalid_number before any zohoGet call (assert via mock not called)
  </acceptance_criteria>
  <done>Handler implemented; batch-customer.test.js fully GREEN; success/404/502/partial/400/401 contract satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: Run full middleware suite + lint to satisfy success criterion #2</name>
  <files>zoho-middleware/routes/pos.js, zoho-middleware/__tests__/batch-customer.test.js</files>
  <read_first>
    - CLAUDE.md (Non-Negotiable Rules: run middleware tests AND lint before commit; middleware has its own node_modules — cd first)
    - zoho-middleware/package.json (confirm the test and lint script names)
  </read_first>
  <action>
    Run the FULL middleware Jest suite (not just the new file) to confirm the new handler did not break any of the existing ~323 tests — route registration order and shared mocks can interact. Then run middleware lint. Fix any lint errors introduced by the new handler or test file (ES5 style: `var`, no arrow functions, no template literals — match surrounding pos.js code). If the middleware has no dedicated lint script, run the repo-level `npm run lint` scoped appropriately. Do not modify any existing test (CLAUDE.md rule 10).
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test` exits 0 with the full suite passing (existing tests + new batch-customer tests)
    - Lint runs clean over the modified files (no new errors); success criterion #2 (covered + lint clean) is met
    - No existing test file was modified
  </acceptance_criteria>
  <done>Full middleware suite green and lint clean; phase success criterion #2 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → middleware GET endpoint | Staff browser (semi-public MW_API_KEY) crosses into the new read endpoint; untrusted `number` query param enters here |
| middleware → Zoho Books API | Authenticated server-to-server; Zoho is the data source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Information Disclosure | GET /api/batch/customer-by-number (returns customer PII) | mitigate | `x-api-key` (MW_API_KEY) guard copied verbatim from sync-zoho; server-level `requireAllowedReferer` + CORS guard already wrap /api/* (Task 2 enforces the key check before any Zoho call) |
| T-28-02 | Information Disclosure | Sequential INV-/SO- enumeration to harvest customer data | accept | Global `apiLimiter` (60 req/min) + required `x-api-key` already throttle; MW_API_KEY is semi-public by design per SECURITY.md (documented accepted risk) — no new control added |
| T-28-03 | Tampering / DoS | Malformed `number` param triggering unexpected Zoho behavior | mitigate | D-16 prefix regex (`/^INV-\d+$/`, `/^SO-\d+$/`) validates and rejects with 400 BEFORE any Zoho call; all Zoho calls in this handler are read-only |
| T-28-04 | Information Disclosure | Customer PII (email/phone) leaking into logs | mitigate | Handler logs only the doc number and customer_id in error/warn paths — never email or phone; matches the no-PII-in-logs convention established in checkout.js/pos.js |
| T-28-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH "Package Legitimacy Audit: not applicable") — no install task exists |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/batch-customer.test.js` — all new-endpoint tests pass (success, not_found, zoho_error, partial 200, 400, 401)
- `cd zoho-middleware && npm test` — full suite green (no regressions in the existing ~323 tests)
- Middleware lint clean over the new/modified files
- `grep -c "/api/batch/customer-by-number" zoho-middleware/routes/pos.js` returns 1 (single registration)
</verification>

<success_criteria>
- Phase success criterion #1: endpoint returns linked customer name/email/contact for a valid INV-/SO- number, with clear not-found (404) and no-link (partial 200) responses — implemented and unit-verified
- Phase success criterion #2: endpoint covered by middleware unit tests for success, not-found, and Zoho-error paths and passes with lint clean — satisfied
</success_criteria>

<output>
Create `.planning/phases/28-zoho-customer-read-back-path/28-01-SUMMARY.md` when done
</output>
