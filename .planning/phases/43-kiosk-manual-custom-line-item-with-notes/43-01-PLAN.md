---
phase: 43-kiosk-manual-custom-line-item-with-notes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/__tests__/pos-custom-line.test.js
autonomous: true
requirements: [KIOSK-02]

must_haves:
  truths:
    - "KIOSK-02: POST /api/kiosk/sale accepts a line with no item_id when custom:true (bypasses catalog-rejection)"
    - "D-01/D-02: a taxable custom line charges 5% GST in computeTax AND carries a resolved GST tax_id on the Zoho invoice line, so terminal charge == invoice tax"
    - "D-02: a taxable custom line with no resolvable GST tax_id fails closed (sale rejected with actionable error), never silently undertaxed"
    - "D-01: a tax-exempt custom line (taxable:false) charges 0% tax and carries no tax_id"
    - "D-03: server caps custom rate magnitude (abs <= 10000) and keeps grandTotal>0 and grandTotal<=10000 guards unchanged (net-negative/zero sale still rejected)"
    - "D-04: the Zoho invoice line description is '<Description> — <Note>' (or just '<Description>' when note blank)"
    - "D-05: Description required (1-100 chars after sanitize), quantity integer 1-100, rate numeric within bounds"
    - "D-08: resolveDiscount never applies cart-scope or type-scope discounts to a custom line"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "custom-line acceptance + bounded pricing + GST tax_id resolution + note->description across /api/kiosk/sale and /api/kiosk/sale/confirm"
      contains: "resolveGstTaxId"
    - path: "zoho-middleware/__tests__/pos-custom-line.test.js"
      provides: "regression suite covering taxable/exempt/negative/large custom lines, fail-closed GST, discount-skip"
  key_links:
    - from: "computeTax"
      to: "li.tax_percentage on custom lines"
      via: "if (li.custom) branch before catalogMap[li.item_id]"
      pattern: "li\\.custom"
    - from: "lineItems builder (both handlers)"
      to: "Zoho invoice line { description, rate, quantity, tax_id? }"
      via: "resolveGstTaxId(catalogMap) when taxable"
      pattern: "resolveGstTaxId"
---

<objective>
Add a server-side custom-line path to the two kiosk money-path handlers in `zoho-middleware/routes/pos.js` so a staff-entered ad-hoc line (no catalog `item_id`) flows through the terminal-charge -> Zoho-invoice -> payment-record pipeline without weakening the v4.2-hardened money path.

Purpose: Catalog items ignore client price (server-authoritative). The custom line is the ONE place a staff price is trusted — it must be bounded, taxed correctly, and tax-consistent between the terminal charge and the Zoho invoice (D-02 invariant). Tests are written first because this is a money-path change (CLAUDE.md rule 3).

Output: An extended `routes/pos.js` and a new `__tests__/pos-custom-line.test.js` regression suite, both test suites and lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md
@.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md
@CLAUDE.md

<interfaces>
Custom-line object shape posted by the client (no item_id):
  { custom: true, description: string, note: string, quantity: number, rate: number, taxable: boolean }

Catalog-line shape (unchanged): { item_id, name, sku, quantity, rate, product_type, cf_type }

Server-resolved line for computeTax: a custom line carries `tax_percentage` (5 when taxable, 0 when exempt) so the existing tax math works once the catalog lookup is skipped.

Zoho invoice line for a custom line (canonical shape from /api/pos/sale L629-634):
  { description: "<Description> — <Note>" | "<Description>", rate, quantity, tax_id? }
  tax_id present ONLY when taxable; never item_id/sku.

Tax rule constant: ZOHO_TAX_SERVICES_RULE (default '109900000000033417') = 5% GST. `_TAX_RULE_PCT` maps it to 5 (pos.js ~L26-30).

GST tax_id resolution order (D-02): (1) process.env.KIOSK_GST_TAX_ID; (2) auto-discover — scan KIOSK_PRODUCTS_CACHE_KEY catalog for an item whose `sales_tax_rule_id === process.env.ZOHO_TAX_SERVICES_RULE` and reuse its `tax_id`; (3) fail-closed — reject the sale.

Current export tail (pos.js L2207-2210):
  module.exports = router;
  module.exports.resolveDiscount = resolveDiscount;
  module.exports.computeTax = computeTax;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing custom-line regression suite</name>
  <files>zoho-middleware/__tests__/pos-custom-line.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/pos-tax.test.js (full mock harness + terminal-push assertion pattern — copy verbatim)
    - zoho-middleware/__tests__/pos-discount.test.js (resolveDiscount direct unit-test pattern, ~L28-44)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md (test section ~L509-676: mock harness, getHandlers, mockRes, CATALOG_WITH_GST)
    - zoho-middleware/routes/pos.js (L41-139 resolveDiscount/computeTax; L190-312 /api/kiosk/sale; L409-545 confirm)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md (D-01..D-08)
  </read_first>
  <behavior>
    - taxable custom line on /api/kiosk/sale: terminalPurchase called with grandTotal = rate*qty*1.05 (e.g. rate 100 qty 1 -> 105), sale not rejected by catalog check
    - tax-exempt custom line (taxable:false): grandTotal = rate*qty (no tax), no tax_id on the resulting Zoho line
    - fail-closed: taxable custom line + KIOSK_GST_TAX_ID unset + catalog with NO 5% item -> handler returns res.status(400) with an actionable error mentioning tax-exempt or KIOSK_GST_TAX_ID; terminalPurchase NOT called
    - GST auto-discovery: taxable custom line + KIOSK_GST_TAX_ID unset + CATALOG_WITH_GST (item with sales_tax_rule_id === ZOHO_TAX_SERVICES_RULE) -> /api/kiosk/sale/confirm posts a Zoho line whose tax_id === that item's tax_id (assert via zohoPost mock call payload)
    - env override: KIOSK_GST_TAX_ID set -> resolved tax_id equals the env value, no catalog scan needed
    - description shaping: custom line with description 'Repair' + note 'broke airlock' -> Zoho line description === 'Repair — broke airlock'; blank note -> description === 'Repair'
    - negative rate: a custom line rate -10 combined with catalog items keeping grandTotal>0 is accepted; a cart whose grandTotal nets <= 0 is rejected by the existing 'greater than zero' guard
    - large rate: server rejects abs(rate) > 10000 in the structural validation loop with a clear error
    - validation: missing/empty description rejected; quantity 0 or 101 rejected
    - discount skip (D-08): resolveDiscount with a type-scope preset over [catalog line, custom line] leaves the custom line's `discount` undefined and does not add its subtotal to matchedSubtotal
  </behavior>
  <action>Create `pos-custom-line.test.js` mirroring the pos-tax.test.js mock harness exactly (jest.mock for express, helcim, zoho-api, cache, logger, eventLog, mailer, inventory-ledger, brewpad-integration, discount-match, constants). Reuse `getHandlers()`, `mockRes()`, and the terminal-push `res.json.mockImplementation` assertion idiom. In beforeEach set `process.env.ZOHO_TAX_SERVICES_RULE` to its default and `process.env.KIOSK_CONTACT_ID`; in afterEach delete `KIOSK_GST_TAX_ID`, `ZOHO_TAX_SERVICES_RULE`, `KIOSK_TAX_RATE`. Define a `CATALOG_WITH_GST` fixture (one item with `sales_tax_rule_id === process.env.ZOHO_TAX_SERVICES_RULE` and `tax_id:'tax-gst-5'`) and a `CATALOG_NO_GST` fixture (no such item) and wire them via `cache.get.mockResolvedValue(...)`. Add a direct `resolveDiscount` unit test importing `require('../routes/pos').resolveDiscount` for the D-08 skip. Tests describe the contract; they MUST fail against the current pos.js (which crashes on the absent item_id). Do not implement pos.js yet.</action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/pos-custom-line.test.js 2>&1 | grep -Eq "fail|Cannot read|TypeError|●" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - File `zoho-middleware/__tests__/pos-custom-line.test.js` exists and is loaded by jest (appears in `npx jest __tests__/pos-custom-line.test.js` output)
    - The suite contains explicit cases for: taxable 5% grandTotal, exempt 0% + no tax_id, fail-closed rejection, GST auto-discovery tax_id match, KIOSK_GST_TAX_ID env override, 'Description — Note' shaping, negative-rate accept + net-zero reject, large-rate (>10000) reject, description/quantity validation, resolveDiscount D-08 skip
    - Running the suite now FAILS (RED) — it is written against behavior pos.js does not yet have
    - No production file modified in this task
  </acceptance_criteria>
  <done>pos-custom-line.test.js exists with all listed cases and fails against the current pos.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the custom-line path in routes/pos.js</name>
  <files>zoho-middleware/routes/pos.js</files>
  <read_first>
    - zoho-middleware/routes/pos.js (L1-18 imports, L26-30 _TAX_RULE_PCT, L41-113 resolveDiscount, L116-139 computeTax, L190-312 /api/kiosk/sale incl validation L227-236, catalog-reject L251-258, lineItems builder L264-280, grandTotal guards L307-312, L409-545 confirm incl catalog-reject L423-426 + lineItems builder L430-440, L629-634 /api/pos/sale ad-hoc line shape, L2207-2210 exports)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md (every pos.js guard excerpt, L24-222)
    - zoho-middleware/__tests__/pos-custom-line.test.js (the contract this task must satisfy)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md (D-01..D-08, fail-closed copy ~L91)
  </read_first>
  <action>
    Extend `routes/pos.js` (ES5 / var / named functions only) to satisfy the suite:
    1. Add a top-level `resolveGstTaxId(catalogMap)` helper: return `process.env.KIOSK_GST_TAX_ID` if set; else scan the catalog values for the first item whose `sales_tax_rule_id === process.env.ZOHO_TAX_SERVICES_RULE` (default '109900000000033417') and return its `tax_id`; else return `null` (caller fail-closes). Export it: `module.exports.resolveGstTaxId = resolveGstTaxId;`.
    2. `computeTax` (L116): at the top of the forEach, `if (li.custom) { var lt = li.quantity * li.rate; ...apply li.discount the same way... lt = Math.max(lt,0); taxTotal += lt * ((li.tax_percentage || 0)/100); return; }` before touching `catalogMap[li.item_id]`.
    3. `resolveDiscount` (L41): in the type-scope `lineItems.map` add `if (li.custom) return false;` before `classifyCatalogItem`; in any cart-scope distribution add an equivalent skip so custom lines get no discount share and never receive `li.discount` (D-08).
    4. Structural validation loop in /api/kiosk/sale (L227-236): branch `if (vi.custom) { validate description (string, trim length 1-100), rate (isFinite, abs(rate) <= 10000), quantity (integer 1-100); on failure return res.status(400) with a clear per-field error; continue; }` before the existing item_id check.
    5. Catalog-rejection loops in BOTH /api/kiosk/sale (L251-258) and /sale/confirm (L423-426): `if (cItem.custom) continue;` at loop top.
    6. lineItems builders in BOTH handlers (L264-280, L430-440): branch `if (item.custom) { var rate = Number(item.rate); var qty = Number(item.quantity)||1; subtotal += qty*rate; var taxable = item.taxable !== false; var desc = String(item.description||'').trim(); var note = String(item.note||'').trim(); var fullDesc = note ? (desc + ' — ' + note) : desc; var li = { description: fullDesc, rate: rate, quantity: qty }; li.custom = true; li.tax_percentage = taxable ? 5 : 0; if (taxable) { var gid = resolveGstTaxId(catalogMap); if (!gid) { /* fail-closed */ return res.status(400).json({ error: 'Cannot tax this custom line: no GST tax rate configured. Mark the line tax-exempt or set KIOSK_GST_TAX_ID.' }); } li.tax_id = gid; } return li; }`. NOTE: returning res inside a `.map` callback does not stop the handler — instead pre-resolve the GST tax_id ONCE before building lineItems (compute `gstTaxId = null; needGst = body.items.some(custom && taxable)`; if needGst, `gstTaxId = resolveGstTaxId(catalogMap)`; if needGst && !gstTaxId return res.status(400) fail-closed) and reference `gstTaxId` inside the map. Apply the SAME pre-resolution in both /sale and /sale/confirm.
    7. Sanitize description/note: strip control chars and cap to 100 chars after trim before building `fullDesc` (injection mitigation T-43-04).
    8. Leave grandTotal guards (L307-312) UNCHANGED — net-negative/zero still rejected.
    9. Confirm `detectKitItems` / `createBatchesFromSale` and `decrementStock` tolerate the no-item_id custom line; per 43-PATTERNS they already self-guard — if `detectKitItems` could include a custom line when a Maker's Fee is present, add `if (item.custom) return;` inside its iterator to be safe.
    Do not add fenced code to the file beyond normal ES5; the snippets above are guidance for identifiers/shape, not literal blocks to paste.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/pos-custom-line.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/routes/pos.js` defines `resolveGstTaxId` and exports it (`grep -q "module.exports.resolveGstTaxId" zoho-middleware/routes/pos.js`)
    - `computeTax` has an `if (li.custom)` branch that returns before `catalogMap[li.item_id]` is dereferenced (`grep -n "li.custom" zoho-middleware/routes/pos.js` shows a hit inside computeTax)
    - Both catalog-rejection loops skip custom lines (`grep -c "\.custom) continue" zoho-middleware/routes/pos.js` >= 1; both /sale and /confirm covered)
    - resolveDiscount returns false / assigns no discount for custom lines (D-08) — verified by the resolveDiscount unit test passing
    - `cd zoho-middleware && npx jest __tests__/pos-custom-line.test.js` exits 0 (GREEN) — every case from Task 1 passes
    - `cd zoho-middleware && npm test` exits 0 — no existing pos-tax / pos-discount / confirm test regressed
    - grandTotal guards at the existing line region are unchanged (the 'greater than zero' and 'exceeds maximum' strings still present and unmodified)
  </acceptance_criteria>
  <done>All pos-custom-line cases pass and the full middleware suite is green; fail-closed GST behavior and D-08 discount-skip verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk client -> /api/kiosk/sale(/confirm) | Staff-entered custom-line price/description/note crosses here; the ONE trusted-price path |
| middleware -> Helcim terminal | grandTotal pushed to the physical terminal — must equal the Zoho invoice total |
| middleware -> Zoho Books | invoice line description + tax_id written from staff input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | custom line `rate` on /api/kiosk/sale | mitigate | server caps `abs(rate) <= 10000` in the structural validation loop; existing grandTotal>0 and grandTotal<=10000 guards remain (net-negative/zero rejected) |
| T-43-02 | Information Disclosure / Integrity | tax divergence between terminal charge and Zoho invoice | mitigate | computeTax taxes a taxable custom line at 5%; lineItems builder attaches the resolved 5% GST `tax_id`; fail-closed (reject) when no GST tax_id resolvable — never silently undertax (D-02) |
| T-43-03 | Tampering | client-supplied `item_id` / `tax_id` injection on a custom line | mitigate | custom branch ignores any client item_id/tax_id; tax_id is server-resolved via resolveGstTaxId only; description-only Zoho line (no item_id/sku) |
| T-43-04 | Tampering / XSS-at-rest | description/note free-text into Zoho line + later receipt render | mitigate | server strips control chars and caps to 100 chars; render-side escapeHTML handled in plan 02 |
| T-43-05 | Repudiation / Replay | sale containing custom lines replayed | mitigate | existing idempotency_key + replay guard cover the whole /sale + /confirm flow unchanged — custom lines add no new bypass; regression suite asserts handlers still gate on it |
| T-43-SC | Tampering | npm/pip installs | accept | no new packages added in this plan; nothing to vet |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` exits 0 (incl. new pos-custom-line.test.js and all prior pos suites)
- `cd zoho-middleware && npm run lint` (or root `npm run lint` if middleware shares config) exits 0
- Manual grep gates in acceptance_criteria confirm the guards exist at the right call sites
</verification>

<success_criteria>
- /api/kiosk/sale and /api/kiosk/sale/confirm accept a `{custom:true,...}` line with no item_id
- Taxable custom line: terminal charge includes 5% GST AND the Zoho line carries the resolved GST tax_id (charge == invoice tax)
- Fail-closed when a taxable custom line cannot resolve a GST tax_id
- Custom lines excluded from all discount presets
- grandTotal guards unchanged; full middleware suite + lint green
</success_criteria>

<output>
Create `.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-01-SUMMARY.md` when done
</output>
