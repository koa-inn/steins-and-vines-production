---
phase: 43-kiosk-manual-custom-line-item-with-notes
plan: 02
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - js/kiosk.js
  - js/admin.js
  - js/kiosk.min.js
  - js/admin.min.js
autonomous: false
requirements: [KIOSK-02]

must_haves:
  truths:
    - "KIOSK-02: an 'Add custom item' button in the cart area opens a focused modal on BOTH kiosk surfaces (D-06)"
    - "D-05: the modal collects Description (required), Note (optional), Price, Qty (default 1), and a Tax-exempt toggle (default off = taxable)"
    - "D-03: the modal requires explicit confirmation when the entered amount is > $2000 or negative before the line enters the cart"
    - "D-07: a custom line shows in the cart with qty +/- and remove, reusing existing cart controls, on both surfaces"
    - "D-04/D-08: the items array posted to the server forwards { custom:true, description, note, quantity, rate, taxable } (no item_id); custom lines carry tax_percentage 5/0 so kioskCalcTotals needs no math change and are skipped by frontend discount resolution"
    - "Fork parity: kiosk.js and admin.js carry identical custom-line logic; only the modal mechanism differs (kiosk.js inline style.display overlay vs admin.js openModal/closeModal)"
  artifacts:
    - path: "js/kiosk.js"
      provides: "custom-item modal + _kioskCustomCounter + custom-line items-array branch + discount skip (standalone surface)"
      contains: "custom"
    - path: "js/admin.js"
      provides: "custom-item modal via openModal + _kioskCustomCounter + custom-line items-array branch + discount skip (admin-embedded surface)"
      contains: "custom"
  key_links:
    - from: "Add-custom-item modal submit"
      to: "_kioskCart['custom-N']"
      via: "{ item: { custom:true, description, note, name, rate, tax_percentage, taxable }, qty }"
      pattern: "custom-"
    - from: "kioskProceedToPayment items mapper"
      to: "POST /api/kiosk/sale body.items[]"
      via: "if (entry.item.custom) return { custom:true, description, note, quantity, rate, taxable }"
      pattern: "custom: true"
---

<objective>
Build the staff-facing "Add custom item" affordance on BOTH forked kiosk surfaces — standalone `js/kiosk.js` and admin-embedded `js/admin.js` (`admin.html?tab=kiosk`, the prod kiosk) — and forward the custom line to the server contract finalized in plan 43-01.

Purpose: D-06 places the affordance as a cart-area button opening a focused modal; the shared cart/totals/items-mapping logic must be equivalent across the two files, with one intentional divergence: kiosk.js has no `openModal` (uses an inline `style.display` overlay) while admin.js uses the global `openModal`/`closeModal`. Custom lines carry `tax_percentage` (5/0) so the existing `kioskCalcTotals` per-line tax math is unchanged.

Output: custom-item modal + cart wiring + items-array branch + frontend discount-skip in kiosk.js AND admin.js, minified bundles rebuilt, both full test suites + lint green, and a human-verify checkpoint on the iPad-target UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md
@.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md
@.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-01-SUMMARY.md
@CLAUDE.md

<interfaces>
Server contract (from 43-01, the shape the UI must post per line):
  custom line:  { custom: true, description, note, quantity, rate, taxable }   // NO item_id
  catalog line: { item_id, name, sku, quantity, rate, product_type, cf_type } // unchanged

Cart entry object (in _kioskCart, keyed 'custom-N'):
  { item: { custom:true, description, note, name:(=description), rate, tax_percentage:(5|0), taxable }, qty }

kiosk.js anchors (verify exact lines before editing — offsets drift):
  escapeHTML ~L527; _kioskCart ~L689; kioskCalcTotals ~L1187 (per-line tax via entry.item.tax_percentage, default KIOSK_TAX_RATE_DEFAULT); kioskShowView ~L1289; recipe-prompt overlay pattern ~L1544; kioskAddToCart ~L2349; kioskSetQty/kioskRemoveFromCart ~L2569; kioskRenderCart ~L2618; kioskProceedToPayment items mapper ~L2958-2968.

admin.js anchors (the EXACT-DUPLICATE fork; prod kiosk):
  openModal/closeModal ~L1282-1323; escapeHTML ~L5318; kiosk section start ~L9783; state vars ~L9785-9793 (add var _kioskCustomCounter = 0;); kioskItemTax ~L9829; kioskCalcTotals ~L9847; kioskRenderCart ~L10099 (uses span.kiosk-qty-val, no remove btn — qty-dec to zero removes); kioskAddToCart ~L10060; kioskSetQty ~L10071; kioskProceedToPayment items mapper ~L10352-10436.

Fork parity table + modal divergence: 43-PATTERNS.md L734-749.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Custom-item modal + cart wiring in js/kiosk.js (standalone surface)</name>
  <files>js/kiosk.js</files>
  <read_first>
    - js/kiosk.js (escapeHTML ~L527; _kioskCart ~L689; kioskCalcTotals ~L1187 incl. KIOSK_TAX_RATE_DEFAULT + discount block; kioskShowView ~L1289; recipe-prompt overlay ~L1544; kioskAddToCart ~L2349; kioskSetQty/kioskRemoveFromCart ~L2569; kioskRenderCart ~L2618; kioskProceedToPayment ~L2950)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md (kiosk.js section L226-383, fork table L734-749)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md (D-03 confirm-on-large/negative, D-05 field rules, D-06 modal placement, D-07 cart controls, D-08 discount skip)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-01-SUMMARY.md (server-accepted items[] shape)
  </read_first>
  <action>
    In js/kiosk.js (ES5/var/named functions; match surrounding style):
    1. Add `var _kioskCustomCounter = 0;` near the other kiosk state vars (~L689 region).
    2. Add an "Add custom item" button in the cart area. Find where the cart sidebar/footer is rendered (near kioskRenderCart ~L2618) and inject a button with a stable id/class (e.g. kiosk-add-custom-btn) that calls kioskShowCustomItemModal(). Use escapeHTML for any dynamic content.
    3. Build kioskShowCustomItemModal() using the existing inline-overlay pattern (the recipe-prompt approach: a fixed-position overlay div toggled with style.display, NOT a new kioskShowView entry). Fields: Description (text, required), Note (text, optional), Price (number, inputmode=decimal), Qty (number, step=1, min=1, default 1), Tax-exempt toggle (checkbox, unchecked = taxable per D-05 default-on). Include a Cancel and an Add button.
    4. Build kioskSubmitCustomItem() (the Add handler): trim+validate Description (1-100 chars, required) and Qty (integer >= 1); parse rate. D-03: if rate > 2000 || rate < 0, require an explicit confirm step (window.confirm or an in-modal confirm affordance) showing "You entered <amount> — confirm this custom charge" before proceeding. On confirm, create the cart entry _kioskCart['custom-' + (++_kioskCustomCounter)] = { item: { custom:true, description:desc, note:note, name:desc, rate:rate, tax_percentage:(taxExempt?0:5), taxable:!taxExempt }, qty:qty }; then close the overlay and call kioskRenderCart().
    5. Discount skip (D-08): in the kioskCalcTotals discount-resolution block, skip lines where entry.item.custom === true so no preset discounts a custom line (parallel to server resolveDiscount).
    6. items mapper in kioskProceedToPayment (~L2958): add the custom branch returning { custom:true, description:entry.item.description||'', note:entry.item.note||'', quantity:entry.qty, rate:parseFloat(entry.item.rate)||0, taxable:entry.item.taxable !== false } before the catalog mapping, per 43-PATTERNS L298-320.
    7. Cart render/qty/remove (D-07): confirm kioskRenderCart, kioskSetQty, kioskRemoveFromCart already key off the synthetic custom-N key and need no special-case (PATTERNS L323-382). Use escapeHTML on item.name (= description) in the cart line.
    Do NOT touch kioskCalcTotals' tax math (custom carries tax_percentage already). Do NOT add a 'custom-item' entry to the kioskShowView views array.
  </action>
  <verify>
    <automated>grep -q "_kioskCustomCounter" js/kiosk.js && grep -q "kioskShowCustomItemModal" js/kiosk.js && grep -q "custom: true" js/kiosk.js && echo KIOSK_OK</automated>
  </verify>
  <acceptance_criteria>
    - js/kiosk.js defines _kioskCustomCounter, kioskShowCustomItemModal, and a custom-line submit handler (grep finds all three)
    - The cart area renders an "Add custom item" button wired to open the modal
    - The modal collects Description (required), Note, Price, Qty (default 1), Tax-exempt toggle (default taxable) — D-05
    - Entering rate > 2000 or < 0 triggers an explicit confirm before the line is added — D-03
    - kioskProceedToPayment items mapper emits { custom:true, description, note, quantity, rate, taxable } with NO item_id for custom entries
    - kioskCalcTotals discount block skips entry.item.custom === true lines — D-08
    - Description/note are escaped via escapeHTML on render (no raw user input to innerHTML)
    - No change to kioskCalcTotals per-line tax math; no new entry in the kioskShowView views array
  </acceptance_criteria>
  <done>kiosk.js custom-item modal + cart wiring + items mapper + discount skip complete and the grep gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Duplicate custom-item logic into js/admin.js (admin-embedded fork) via openModal</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (openModal/closeModal ~L1282-1323; escapeHTML ~L5318; kiosk section ~L9783; state vars ~L9785-9793; kioskItemTax ~L9829; kioskCalcTotals ~L9847; kioskAddToCart ~L10060; kioskSetQty ~L10071; kioskRenderCart ~L10099; kioskProceedToPayment ~L10352-10436)
    - js/kiosk.js (the just-completed custom-item functions from Task 1 — port the shared logic identically)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md (admin.js section L386-506, fork table + modal-divergence note L734-749)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-CONTEXT.md (D-03..D-08)
  </read_first>
  <action>
    Duplicate every custom-line change from Task 1 into js/admin.js, keeping the SHARED logic identical (cart key scheme custom-N, the cart-entry item shape with tax_percentage 5/0, the items-array mapper branch, the discount skip, the D-03 confirm threshold, the D-05 field rules). The ONE intentional divergence is the modal mechanism:
    1. Add `var _kioskCustomCounter = 0;` to the admin kiosk state vars (~L9785-9793).
    2. Modal mechanism: instead of an inline overlay, build the modal body HTML string and open it with the global openModal('Add custom item', html); the Add handler closes via closeModal() (PATTERNS L485-506). Use admin.js's in-scope escapeHTML for field values.
    3. Add the "Add custom item" button to the admin kiosk cart area (near kioskRenderCart ~L10099) wired to the openModal-based show function.
    4. items mapper in admin's kioskProceedToPayment (~L10352-10436): add the SAME custom branch as kiosk.js — { custom:true, description, note, quantity, rate, taxable }, no item_id. Note admin's catalog mapper omits sku/cf_type (PATTERNS L450-462) — keep that difference for catalog lines only; the custom branch is identical to kiosk.js.
    5. Discount skip (D-08) in admin's kioskCalcTotals discount block — same entry.item.custom guard.
    6. Cart render: admin's kioskRenderCart uses span.kiosk-qty-val and removes via qty-dec to zero (no remove button) — custom lines reuse this unchanged; ensure kioskSetQty deletes the custom-N entry at qty <= 0 (PATTERNS L464-483).
    Walk the Fork Parity Checklist (PATTERNS L734-749): for every kiosk.js custom-line touch, confirm the admin.js parallel exists.
  </action>
  <verify>
    <automated>grep -q "_kioskCustomCounter" js/admin.js && grep -q "Add custom item" js/admin.js && grep -q "custom: true" js/admin.js && echo ADMIN_OK</automated>
  </verify>
  <acceptance_criteria>
    - js/admin.js defines _kioskCustomCounter and a custom-item modal opened via openModal('Add custom item', ...) closed via closeModal() (grep finds both)
    - The admin kiosk cart area renders an "Add custom item" button wired to the openModal-based show function
    - admin's kioskProceedToPayment items mapper emits { custom:true, description, note, quantity, rate, taxable } with NO item_id for custom entries — byte-equivalent shared logic to kiosk.js
    - admin's kioskCalcTotals discount block skips entry.item.custom === true lines — D-08
    - admin's kioskSetQty deletes the custom-N entry at qty <= 0
    - The D-03 confirm threshold and D-05 field rules match kiosk.js exactly (only the modal open/close mechanism differs)
    - Description/note escaped via escapeHTML; the Fork Parity Checklist is satisfied for every custom-line touch
  </acceptance_criteria>
  <done>admin.js carries the identical custom-line logic via openModal/closeModal; the grep gate passes and parity with kiosk.js holds.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild bundles and run the full gate (build + both suites + lint)</name>
  <files>js/kiosk.min.js, js/admin.min.js</files>
  <read_first>
    - CLAUDE.md (build + pre-commit rules: npm run build after JS changes; npm test AND cd zoho-middleware && npm test AND npm run lint must pass)
    - .planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-PATTERNS.md (kiosk.min.js IS built from kiosk.js — regenerate via npm run build, L228)
    - package.json (confirm the build script minifies kiosk.js -> kiosk.min.js and admin.js -> admin.min.js)
  </read_first>
  <action>
    Run `npm run build` from the repo root to regenerate js/kiosk.min.js and js/admin.min.js from the edited sources (admin.html cache-busting ?v= stamps update automatically per CLAUDE.md). Then run the full pre-commit gate: `npm test` (frontend jsdom), `cd zoho-middleware && npm test` (middleware — includes 43-01's pos-custom-line suite), and `npm run lint` from root. Fix any lint/style violations introduced in kiosk.js/admin.js (ES5/var; no let/const/arrow/template-literal). Do NOT edit the .min.js files by hand — they are build artifacts. Do NOT modify existing tests (CLAUDE.md rule 10). This phase ships to staging only — do not push to production.
  </action>
  <verify>
    <automated>npm run build && npm test && (cd zoho-middleware && npm test) && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - js/kiosk.min.js and js/admin.min.js are regenerated (newer mtime than the source edits; contain the minified custom-line logic)
    - `npm test` (frontend) exits 0
    - `cd zoho-middleware && npm test` exits 0 (incl. pos-custom-line.test.js from 43-01)
    - `npm run lint` exits 0
    - No .min.js file was hand-edited; no existing test file was modified
  </acceptance_criteria>
  <done>Bundles rebuilt; both full suites and lint are green; nothing pushed to production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The "Add custom item" affordance on both kiosk surfaces: a cart-area button opening a focused modal (Description, Note, Price, Qty, Tax-exempt toggle) with a confirm step for amounts over $2000 or negative; the custom line rings through the terminal-charge -> Zoho-invoice -> payment path with correct 5% GST (or 0% when exempt) and the note in the invoice line description.</what-built>
  <how-to-verify>
    Test on BOTH surfaces (standalone kiosk.js page AND admin.html?tab=kiosk — the prod kiosk):
    1. Open the kiosk cart, tap "Add custom item". Confirm the modal shows Description, Note, Price, Qty, Tax-exempt toggle with large iPad-friendly touch targets.
    2. Add a taxable custom line (e.g. Description "Equipment rental", Note "weekend", Price 50, Qty 1, tax-exempt OFF). Confirm the cart shows the line, qty +/- and remove work, and the cart total reflects 50 + 5% GST = 52.50.
    3. Add a tax-exempt line (toggle ON) and confirm no tax is added to that line.
    4. Enter a Price of 2500 (or a negative like -10) and confirm an explicit "confirm this custom charge" prompt appears before the line is added.
    5. Run a real (or terminal-test) sale containing a taxable custom line; confirm the Zoho invoice line description reads "Description — Note" and the invoice tax matches the amount charged on the terminal.
    6. If a discount preset is active, confirm the custom line is NOT discounted.
    Verify steps 1-4 and 6 on admin.html?tab=kiosk as well (the production kiosk surface).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (per surface) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff input -> kiosk DOM | Description/Note free-text entered in the modal, rendered into the cart and (server-side) the Zoho line |
| kiosk client -> /api/kiosk/sale(/confirm) | the custom line items[] payload crosses to the server (price trusted, bounded server-side in 43-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Tampering / XSS-at-rest | description/note rendered in kioskRenderCart and the modal | mitigate | escapeHTML on every user field before innerHTML on BOTH surfaces; server (43-01) strips control chars + caps length |
| T-43-06 | Tampering | client posting a custom line with item_id/tax_id to bypass server price/tax | mitigate | UI never sends item_id/tax_id on a custom line; server (43-01) ignores them and resolves tax_id itself; UI is convenience only — server is authoritative |
| T-43-07 | Elevation / scope-bypass | a custom line slipping into a discount preset on the client | mitigate | frontend kioskCalcTotals skips entry.item.custom in the discount block on BOTH surfaces (mirrors server resolveDiscount) |
| T-43-08 | Tampering | fork drift — feature lands on one surface only (the recurring #14 blind spot) | mitigate | Task 2 duplicates into admin.js with per-file grep gates; the human-verify checkpoint tests BOTH surfaces |
| T-43-SC | Tampering | npm/pip installs | accept | no new packages added in this plan; nothing to vet |
</threat_model>

<verification>
- `npm test` (frontend) and `cd zoho-middleware && npm test` (middleware) both exit 0
- `npm run lint` exits 0
- grep gates confirm custom-line logic present in BOTH js/kiosk.js and js/admin.js
- Human-verify checkpoint confirms the affordance works on both surfaces incl. the 5% GST / note-in-description / discount-skip behaviors
</verification>

<success_criteria>
- "Add custom item" modal works identically (shared logic) on kiosk.js and admin.js, with the one intentional modal-mechanism divergence
- Custom line posts { custom:true, description, note, quantity, rate, taxable } with no item_id
- D-03 confirm-on-large/negative enforced; D-05 field rules enforced; D-07 cart controls reuse; D-08 client discount-skip
- Bundles rebuilt; full frontend + middleware suites + lint green; staging-only
</success_criteria>

<output>
Create `.planning/phases/43-kiosk-manual-custom-line-item-with-notes/43-02-SUMMARY.md` when done
</output>
