---
phase: 35-batch-scaling-engine
plan: 06
type: execute
wave: 5
depends_on: [35-03, 35-04, 35-05]
gap_closure: true
files_modified:
  - zoho-middleware/routes/pos-recipe.js
  - zoho-middleware/__tests__/pos-recipe.test.js
  - admin.html
  - js/admin.js
autonomous: true
requirements: [SCALE-01, SCALE-05]
must_haves:
  truths:
    - "A read-only GET /api/kiosk/recipe-quote returns the SAME server-authoritative scaled total + scaled ingredient quantities as the recipe-sale charge, with NO terminal charge, lock, or invoice (SCALE-01)"
    - "The admin Kiosk Sale recipe prompt shows the scaled total on the Add-to-Cart button and scaled ingredient quantities in the cart, matching what the terminal will charge (SCALE-01)"
    - "Changing the target volume re-quotes (debounced) and updates the displayed price + quantities live"
  artifacts:
    - path: "zoho-middleware/routes/pos-recipe.js"
      provides: "GET /api/kiosk/recipe-quote (dry-run quote) sharing the recipe-sale scale+price+stock computation"
      contains: "recipe-quote"
    - path: "js/admin.js"
      provides: "Debounced quote fetch on volume change + prompt open; cart + Add-to-Cart price + receipt built from the scaled quote"
      contains: "recipe-quote"
---

<objective>
Phase 35 UAT defect: the admin Kiosk Sale recipe prompt charges the correct server-scaled amount, but the CLIENT display (Add-to-Cart price, cart line-item quantities, receipt) shows base/1x values — e.g. at 6x it shows $109.20 and base quantities while the terminal charges ~6x. `_kioskScaleFactor` is preview-only and the cart-builder uses base `ing.quantity`.

Fix (server-quote approach, owner-chosen): add a read-only quote endpoint that runs the SAME scale+price computation as the real sale (reusing lib/recipe-scaling.js + INGREDIENTS_ALL) but with no terminal/lock/invoice, and wire the admin client to display the quote's scaled total + scaled quantities. Guarantees displayed price == charged price (no client-side drift).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Middleware — GET /api/kiosk/recipe-quote (dry-run) + tests</name>
  <files>zoho-middleware/routes/pos-recipe.js, zoho-middleware/__tests__/pos-recipe.test.js</files>
  <action>
    In pos-recipe.js, extract the shared computation from the recipe-sale QUOTE handler (recipe fetch via callAppsScriptPost get_recipe → validate target_volume_l [batch_size_l>0, target>0, <=10x base per existing rules] → cache.get(INGREDIENTS_ALL) → build catalogMap → scaling.scaleIngredients → scaling.checkScaledStock → scaling.computeScaledRecipeTotal) into a reusable helper, and call it from BOTH the existing POST /api/kiosk/recipe-sale handler (no behavior change there) AND a NEW read-only `GET /api/kiosk/recipe-quote`.

    GET /api/kiosk/recipe-quote?recipe_id=<id>&target_volume_l=<num>:
    - Performs scale + price + stock computation ONLY. NO helcimLib.terminalPurchase, NO cache.acquireLock, NO invoice, NO cache busting.
    - 200 JSON: { ok:true, recipe_id, base_volume_l, target_volume_l, scale_factor, pricing_mode, total, ingredients: [{ item_id, item_name, unit, base_quantity, quantity (scaled), rate, line_total }], stock: { ok:boolean, conflicts:[...] } }.
    - 400 on the same validation failures as recipe-sale (batch_size_l unset, target<=0, >10x base). 404 if recipe not found. 503 if INGREDIENTS_ALL catalog cold.
    - Mirror any feature gate the recipe-sale handler uses (e.g. BEER_SALES_ENABLED) if present; otherwise leave ungated (read-only preview). Keep the existing API-key middleware.

    Extend pos-recipe.test.js: recipe-quote returns the SAME `total` and scaled quantities as the recipe-sale handler for the same inputs; does NOT call helcim.terminalPurchase or cache.acquireLock; returns stock.ok=false + conflicts when scaled qty oversells; 400 on invalid target_volume_l. Do NOT weaken existing recipe-sale tests.
  </action>
  <verify><automated>cd zoho-middleware && npm test 2>&1 | tail -8</automated></verify>
  <acceptance_criteria>
    - GET /api/kiosk/recipe-quote exists, shares the recipe-sale compute, has no side effects (no terminal/lock/invoice).
    - quote.total === recipe-sale grandTotal for identical inputs (assert in test).
    - Full middleware suite green; npm run lint clean.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Frontend — quote-driven price + scaled cart + receipt; rebuild</name>
  <files>admin.html, js/admin.js</files>
  <action>
    In js/admin.js (ES5 only — var, function decls, no arrow/template/const/let):
    - Add a debounced (~350ms) helper `kioskFetchRecipeQuote()` that GETs /api/kiosk/recipe-quote with the current recipe_id + _kioskTargetVolumeL (x-api-key header like other kiosk calls). Store the result in `var _kioskQuote = null;`.
    - Call it (a) at the end of kioskShowRecipePrompt once baseVol/value are set, and (b) inside volInput.oninput after updating the factor readout. On success, update the Add-to-Cart button label to the quote total (e.g. 'Add to Cart — $' + total.toFixed(2)) and keep the factor readout. On error, fall back to the base price and (optionally) note 'price confirmed at terminal'.
    - In processRecipeData / the cart build (both dynamic and locked branches): when `_kioskQuote` is present, build cart line items from `_kioskQuote.ingredients` (scaled `quantity` in the name, scaled `rate`/`line_total` for dynamic; for locked use `_kioskQuote.total` for the package-price line and show scaled quantities on the info-only ingredient lines). Fall back to base only if no quote.
    - Update the receipt (kioskShowReceipt call path) so it reflects the scaled quote totals/quantities rather than the base client `totals`/`items` for recipe sales.
    - Reset `_kioskQuote` when leaving/clearing the recipe prompt.
    Then run `npm run build` to regenerate js/admin.min.js + cache-bust stamps.
    Add/extend a frontend jsdom test (tests/frontend) asserting: a volume change triggers a quote fetch, and the Add-to-Cart price + cart quantities reflect the mocked quote (scaled), not base.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - js/admin.js references /api/kiosk/recipe-quote, _kioskQuote, and builds the cart + Add-to-Cart price + receipt from the scaled quote.
    - Changing target volume updates the displayed price and cart quantities to the scaled values.
    - Frontend Jest + middleware Jest + lint all green; bundle rebuilt.
  </acceptance_criteria>
</task>

</tasks>

<output>
Create `.planning/phases/35-batch-scaling-engine/35-06-SUMMARY.md`. Record the new quote endpoint, the client wiring, the tests, and that deploy requires BOTH a Railway middleware `railway up` AND a staging frontend `git push origin main` before the Phase 35 UAT can be completed.
</output>
