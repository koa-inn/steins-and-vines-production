---
phase: 35-batch-scaling-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: [SCALE-01]
must_haves:
  truths:
    - "The Apps Script get_recipe response is confirmed to include batch_size_l (or the gap is fixed) before any scaling code relies on it (SCALE-01, D-10, D-11)"
  artifacts:
    - path: ".planning/phases/35-batch-scaling-engine/35-01-SUMMARY.md"
      provides: "Recorded verdict: batch_size_l present in get_recipe response (yes/no) + remediation taken if no"
  key_links:
    - from: "admin recipe-sale flow"
      to: "recipe.batch_size_l"
      via: "GET /api/recipes/:id Apps Script get_recipe response"
      pattern: "batch_size_l"
---

<objective>
Verify the open LOW-confidence assumption (RESEARCH A2 / Open Question 1) that the Apps Script `get_recipe` action returns `batch_size_l` on the recipe object. Every downstream task (server scaling math, admin UI factor preview) computes `scale_factor = target_volume_l / recipe.batch_size_l` and is wrong if the field is absent.

Purpose: De-risk the phase. If `batch_size_l` is missing from the live response, an Apps Script edit + manual redeploy is required (NOT in CI) before middleware wiring can rely on the base volume. This is a known project blocker class (STATE.md: "Apps Script changes require manual redeploy").
Output: A recorded verdict (present / absent) and, if absent, a remediated + redeployed Apps Script `get_recipe` that includes the field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-batch-scaling-engine/35-RESEARCH.md
@.planning/phases/35-batch-scaling-engine/35-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Probe staging get_recipe response for batch_size_l</name>
  <files>(read-only probe — no files modified)</files>
  <read_first>
    - .planning/phases/35-batch-scaling-engine/35-RESEARCH.md (§6 "batch_size_l Source", Open Question 1, Pitfall 4 + Pitfall 6)
    - zoho-middleware/routes/recipes.js (the GET /api/recipes/:id handler — confirm which field passes through from the Apps Script get_recipe response)
  </read_first>
  <action>
    Identify a real recipe ID that has a base batch size set in the admin recipe editor (a recipe whose `recipe-batch-size` form field is non-empty). Call `GET /api/recipes/:id` against the live middleware (the staging site calls the PROD Railway middleware per STATE.md — there is no separate staging middleware) using a browser-UA curl or `gh api`-equivalent authenticated request as the project does for middleware probes. Inspect the returned recipe object's JSON for a top-level `batch_size_l` field with a numeric value > 0. If `recipes.js` strips or renames the field, trace it back to the raw Apps Script `get_recipe` payload it proxies. Record the literal field name and value observed. Do NOT write any production code in this task.
  </action>
  <verify>
    <automated>MISSING — manual probe; the verdict is recorded in 35-01-SUMMARY.md. The Task 2 checkpoint gates on the recorded result.</automated>
  </verify>
  <acceptance_criteria>
    - 35-01-SUMMARY.md records the exact `GET /api/recipes/:id` URL probed, the recipe ID, and whether `batch_size_l` was present as a numeric > 0 on the recipe object.
    - If absent or zero for a recipe known to have a base batch size set, the SUMMARY records "batch_size_l MISSING from get_recipe response — Apps Script remediation required".
  </acceptance_criteria>
  <done>The presence/absence of `batch_size_l` in the live `get_recipe` response is recorded with the probed recipe ID and URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm batch_size_l availability or remediate Apps Script</name>
  <what-built>A probe of the live `GET /api/recipes/:id` response checking for `batch_size_l` on the recipe object.</what-built>
  <how-to-verify>
    Review the recorded verdict from Task 1.
    - CASE A — `batch_size_l` IS present (numeric > 0): Reply "approved — batch_size_l present". No code change needed; the downstream middleware can rely on the field. Resume the phase.
    - CASE B — `batch_size_l` is ABSENT / null / 0 for a recipe that clearly has a base batch size set in the editor: The Apps Script `get_recipe` action must be edited to include `batch_size_l` in its return payload, then manually redeployed via the Google Apps Script editor (Deploy > Manage Deployments > New Deployment) — this is NOT handled by CI. After redeploy, re-run the Task 1 probe and confirm the field now appears. Reply "approved — Apps Script remediated and redeployed" once verified.
  </how-to-verify>
  <resume-signal>Reply "approved — batch_size_l present" OR "approved — Apps Script remediated and redeployed", or describe the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → Apps Script | The recipe object (including `batch_size_l`) is sourced from the Apps Script Google Sheet; the middleware trusts but does not validate its shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Information disclosure | GET /api/recipes/:id probe | accept | Read-only probe of an existing authenticated endpoint; no new surface introduced |
| T-35-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify |
</threat_model>

<verification>
The verdict is recorded in 35-01-SUMMARY.md and the blocking checkpoint is approved. If CASE B occurred, the re-probe confirms `batch_size_l` now appears in the response.
</verification>

<success_criteria>
- `batch_size_l` is confirmed present in the live `get_recipe` response (CASE A), OR the Apps Script has been remediated and manually redeployed and a re-probe confirms the field (CASE B).
- Downstream plans (35-02, 35-03, 35-04) can safely assume `recipe.batch_size_l` is available server-side.
</success_criteria>

<output>
Create `.planning/phases/35-batch-scaling-engine/35-01-SUMMARY.md` when done. Record the probed recipe ID, URL, the literal presence/value of `batch_size_l`, and which CASE (A or B) applied. If CASE B, record the Apps Script change made and the redeploy confirmation.
</output>
