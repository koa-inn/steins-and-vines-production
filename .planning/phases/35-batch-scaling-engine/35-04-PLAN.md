---
phase: 35-batch-scaling-engine
plan: 04
type: execute
wave: 3
depends_on: [35-03]
files_modified:
  - admin.html
  - js/admin.js
autonomous: false
requirements: [SCALE-01, SCALE-05]
must_haves:
  truths:
    - "After a recipe is selected on the admin Kiosk Sale tab, a 'Target volume (L)' input appears pre-filled with the recipe's base batch_size_l and a factor readout shows e.g. '1.5x base 20 L' before commit (SCALE-01, D-10)"
    - "Entering a volume updates the displayed scale factor live; the input steps by 0.5 L (SCALE-01, D-10)"
    - "A recipe with no base batch_size_l disables scaling with a prompt to set the base first (does not assume factor 1) (SCALE-01, D-11)"
    - "target_volume_l (and override) are sent in BOTH recipeSaleBody and confirmBody (SCALE-01, SCALE-05)"
    - "A 409 stock conflict surfaces the short ingredients with a manager-override button that re-submits the sale (SCALE-05, D-08)"
  artifacts:
    - path: "admin.html"
      provides: "Target-volume input + scale-factor readout + stock-conflict/override block inside #kiosk-recipe-prompt"
      contains: "kiosk-target-volume"
    - path: "js/admin.js"
      provides: "Volume input wiring, factor readout, _kioskRecipeContext extension, target_volume_l/override in recipeSaleBody+confirmBody, 409 conflict handling + override re-submit"
      contains: "kiosk-target-volume"
  key_links:
    - from: "admin.html #kiosk-target-volume input"
      to: "js/admin.js volume change handler -> factor readout"
      via: "oninput recompute target/base"
      pattern: "kiosk-scale-factor-readout"
    - from: "js/admin.js recipeSaleBody + confirmBody"
      to: "POST /api/kiosk/recipe-sale(/confirm) target_volume_l"
      via: "request body field"
      pattern: "target_volume_l"
    - from: "js/admin.js 409 handler"
      to: "manager override re-submit"
      via: "override button onclick -> _kioskStockOverride = true -> re-run sale"
      pattern: "kiosk-stock-override"
---

<objective>
Wire the admin Kiosk Sale recipe-selection UI to the Plan 03 scaling endpoints: add the "Target volume (L)" input + live scale-factor readout inside `#kiosk-recipe-prompt`, send `target_volume_l` + `override` in both the recipe-sale and confirm request bodies, and surface 409 stock conflicts with a manager-override button (D-08). Then rebuild the JS bundle and run a staging UAT that explicitly flags the D-06 locked-pricing behavior change.

Purpose: Deliver SCALE-01 (the visible scaling control + factor preview) and the SCALE-05 client surface (conflict display + override). The client only previews the factor; the charged price and stock verdict come from the server (Plan 03).
Output: Modified `admin.html` + `js/admin.js`, rebuilt `js/main.js`/`js/main.min.js` (via `npm run build`), and a recorded staging UAT confirming the flow and the D-06 price shift.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-batch-scaling-engine/35-RESEARCH.md
@.planning/phases/35-batch-scaling-engine/35-PATTERNS.md
@.planning/phases/35-batch-scaling-engine/35-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Server contract from Plan 03 (zoho-middleware/routes/pos-recipe.js): -->
POST /api/kiosk/recipe-sale   body adds: target_volume_l (number), override (bool)
  -> 202 { pending, reference, total, scale_factor, target_volume_l }
  -> 400 when batch_size_l unset / target_volume_l <= 0 / > 10x base
  -> 409 { error, conflicts: [{ item_id, item_name, needed, stock, unit }] } when scaled qty oversells (no override)
POST /api/kiosk/recipe-sale/confirm  body adds: target_volume_l (number), override (bool)

<!-- Existing admin.js landmarks (verified line numbers, 35-PATTERNS / 35-RESEARCH §7): -->
- line 9778: var _kioskSelectedRecipe = null;
- line 9782: var _kioskRecipeContext = null;
- line 10380: var isRecipeSale = !!_kioskRecipeContext;
- lines 10391-10399: recipeSaleBody object  [ADD target_volume_l, override]
- line 10400: var saleBody = isRecipeSale ? recipeSaleBody : existingSaleBody;
- lines 10415-10423: confirmBody object  [ADD target_volume_l, override]
- function kioskShowRecipePrompt(recipe) at line 10900  [WIRE volume input + readout + reset state]
- _kioskRecipeContext = {...} assembled around line 11235  [ADD target_volume_l]

<!-- admin.html insertion point (35-PATTERNS / 35-RESEARCH §7): -->
- #kiosk-recipe-prompt block ~lines 625-642; insert volume input between #kiosk-recipe-summary and #kiosk-avail-banner; insert stock-conflict/override block alongside.
- ES5 only: var, function declarations — NO arrow functions, template literals, const/let.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin.html — target-volume input + stock-conflict/override markup</name>
  <files>admin.html</files>
  <read_first>
    - admin.html (lines 600-650 — the #kiosk-recipe-prompt block: #kiosk-recipe-summary, #kiosk-avail-banner, sale-type buttons, #kiosk-milling-toggle for the existing input/label/checkbox styling to match)
    - .planning/phases/35-batch-scaling-engine/35-PATTERNS.md (§admin.html — exact markup to insert and the insertion point)
  </read_first>
  <action>
    Inside `#kiosk-recipe-prompt`, between the `#kiosk-recipe-summary` div and the `#kiosk-avail-banner` div, insert a hidden-by-default volume wrap: a `#kiosk-recipe-volume-wrap` div containing a labelled `<input type="number" id="kiosk-target-volume" min="0.5" step="0.5" inputmode="decimal">` and a `#kiosk-scale-factor-readout` div. Below it, insert a hidden-by-default `#kiosk-stock-conflict` div containing a `.kiosk-stock-conflict-msg` element and a `#kiosk-stock-override-btn` button labelled "Manager Override — Proceed Anyway". Match the class/inline-style conventions of the adjacent `#kiosk-milling-toggle` block. Do not wire JS here (Task 2). Do NOT touch js/main.js or js/main.min.js.
  </action>
  <verify>
    <automated>grep -c "kiosk-target-volume" admin.html | grep -qv '^0$' && grep -c "kiosk-stock-override-btn" admin.html | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - admin.html contains `#kiosk-recipe-volume-wrap`, `#kiosk-target-volume` (type number, step 0.5, min 0.5), `#kiosk-scale-factor-readout`, `#kiosk-stock-conflict`, and `#kiosk-stock-override-btn`.
    - The new markup sits between `#kiosk-recipe-summary` and `#kiosk-avail-banner` inside `#kiosk-recipe-prompt`.
  </acceptance_criteria>
  <done>The target-volume input and stock-conflict/override markup exist in the recipe prompt, hidden by default.</done>
</task>

<task type="auto">
  <name>Task 2: js/admin.js — wire volume input, factor readout, body fields, 409 override; rebuild</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (lines 9775-9790 kiosk state vars; lines 10380-10430 sale/confirm body assembly; function kioskShowRecipePrompt ~line 10900; _kioskRecipeContext assembly ~line 11235)
    - .planning/phases/35-batch-scaling-engine/35-PATTERNS.md (§js/admin.js — new state vars, kioskShowRecipePrompt modifications, oninput handler, recipeSaleBody/confirmBody additions, 409 conflict handler, _kioskRecipeContext extension; all in ES5 style)
    - CLAUDE.md (§Build Artifacts — never edit js/main.js directly; run npm run build after module change. NOTE: admin.js is a standalone file, not a js/modules/* concatenated module — confirm whether the build minifies admin.js to admin.min.js and rebuild accordingly.)
  </read_first>
  <action>
    Add ES5 state vars after the existing kiosk vars (~line 9782): `var _kioskTargetVolumeL = null;`, `var _kioskScaleFactor = 1.0;`, `var _kioskStockOverride = false;`. In `kioskShowRecipePrompt(recipe)` (~line 10900): resolve `var baseVol = Number(recipe.batch_size_l) || 0;`, reset `_kioskTargetVolumeL`/`_kioskScaleFactor`/`_kioskStockOverride`, hide `#kiosk-stock-conflict`. Show `#kiosk-recipe-volume-wrap`; if `baseVol > 0` set the input value to baseVol, `max = baseVol * 10`, enabled, and the readout to `'1.0\xd7 base ' + baseVol.toFixed(1) + ' L'`; else disable the input and set the readout to the "Scaling disabled — set base batch size in recipe editor first." prompt (D-11 — do NOT assume factor 1). Wire `volInput.oninput` to recompute `_kioskTargetVolumeL`, the live factor (`val / baseVol`), update the readout to `factor.toFixed(2) + '\xd7 base ' + baseVol.toFixed(1) + ' L'`, and reset the override/conflict state on change. Extend the `_kioskRecipeContext` object (~line 11235) with `target_volume_l: _kioskTargetVolumeL`. Add `target_volume_l` (falling back to the recipe's batch_size_l) and `override: _kioskStockOverride || false` to BOTH `recipeSaleBody` (lines 10391-10399) AND `confirmBody` (lines 10415-10423) — RESEARCH Pitfall 2: missing it on confirm silently defaults the server scaleFactor to 1.0. Add a 409 branch in the sale-result handling (~line 10413): when `result.status === 409` with `result.data.conflicts`, populate `.kiosk-stock-conflict-msg` with the short-ingredient list, show `#kiosk-stock-conflict`, and wire `#kiosk-stock-override-btn` onclick to set `_kioskStockOverride = true`, hide the conflict block, and re-trigger the sale. All new code ES5 (`var`, function declarations, no arrow/template/const/let). After editing, run the build to regenerate bundles.
  </action>
  <verify>
    <automated>npm run build && grep -c "kiosk-target-volume\|target_volume_l\|_kioskStockOverride" js/admin.js | grep -qv '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - js/admin.js references `#kiosk-target-volume`, `#kiosk-scale-factor-readout`, `#kiosk-stock-conflict`, `#kiosk-stock-override-btn`.
    - `target_volume_l` appears in both the recipeSaleBody and confirmBody object literals (grep finds >= 2 occurrences in the body-assembly region).
    - `_kioskRecipeContext` includes `target_volume_l`.
    - The 409 handler sets `_kioskStockOverride = true` and re-submits on override.
    - No ES6 syntax introduced: `grep -nE "=>|\`|\\b(const|let)\\b" ` over the changed regions returns nothing new (manual confirm during review).
    - `npm run build` completes without errors and regenerates the minified bundle(s).
  </acceptance_criteria>
  <done>The volume input drives a live factor readout, target_volume_l/override flow in both bodies, 409 conflicts surface with a working override, and the bundle is rebuilt.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend + middleware test + lint gate; deploy to staging</name>
  <files>(verification + deploy — no new source files)</files>
  <read_first>
    - CLAUDE.md (§Non-Negotiable Rules — Rule 1: run `npm test` AND `cd zoho-middleware && npm test` before every commit; §Commands, §Deployment — npm run lint must pass; staging deploy is `git push origin main`; ALL changes go to staging first; verify CNAME handling per project rules)
  </read_first>
  <action>
    Run BOTH the root-level frontend Jest suite AND the middleware Jest suite (CLAUDE.md Rule 1 — both suites must pass before any commit), plus lint. Then push to staging (`git push origin main`) so the admin Kiosk Sale tab on staging serves the new scaling UI (the staging site calls the PROD middleware, which already carries the Plan 03 scaling endpoints once that wave deployed). Do NOT push to production — staging only, pending human UAT in Task 4.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>cd zoho-middleware && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` (root-level frontend Jest) exits 0.
    - `cd zoho-middleware && npm test` (middleware Jest) exits 0 — CLAUDE.md Rule 1 both-suite gate before commit.
    - `npm run lint` reports no new errors.
    - The change is pushed to the staging remote (`origin`), not production.
  </acceptance_criteria>
  <done>Frontend tests + middleware tests + lint pass; the scaling UI is live on staging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Staging UAT — scaling flow + D-06 price-change confirmation</name>
  <what-built>The admin Kiosk Sale recipe-scaling UI on staging: target-volume input, live factor readout, no-base-size disable state, scaled server pricing, and the manager-override stock-conflict path.</what-built>
  <how-to-verify>
    On staging.steinsandvines.ca, open the admin Kiosk Sale tab (`admin.html?tab=kiosk`), authenticate, and select a recipe that has a base batch size set.
    1. SCALE-01: Confirm a "Target volume (L)" input appears, pre-filled with the recipe's base `batch_size_l`, and a readout shows "1.0x base N L". Change the volume (0.5 L steps) and confirm the readout updates live (e.g. "1.5x base 20 L").
    2. D-11: Select a recipe with NO base batch size — confirm scaling is disabled with the "set base batch size first" prompt (it must NOT silently price at 1x).
    3. SCALE-03 / D-06 PRICE CHANGE (CRITICAL — owner awareness): Run a recipe sale at 1.0x on a LOCKED-price recipe and confirm the charged total is now `locked_price + service_fee + materials_fee` — i.e. HIGHER than before this phase, when locked recipes charged a flat locked_price with no fees. Confirm the owner is aware that EVERY existing locked recipe's price rises by (service_fee + materials_fee) even at 1x scale. This is the intended, owner-approved D-04/D-05/D-06 change. Then run at 1.5x and confirm the locked portion scales (`locked_price * 1.5 + fees`) while fees stay flat.
    4. SCALE-02: Confirm the scaled batch's invoice/quantities reflect linear weight scaling and round-up pcs (spot-check one kg ingredient and one pcs ingredient).
    5. SCALE-05 / D-08: Enter a target volume large enough to oversell an ingredient; confirm the sale is hard-blocked with the short-ingredient list and a "Manager Override — Proceed Anyway" button, and that clicking it proceeds.
    6. No console errors on iPad Safari (the kiosk target device).
  </how-to-verify>
  <resume-signal>Reply "approved — UAT passed, owner aware of D-06 locked-price increase", or describe defects (which become a gap-closure plan).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → middleware | The client sends target_volume_l + override; it is a preview/intent surface only. The server (Plan 03) recomputes price, scaled quantities, and the stock verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Tampering | client-displayed scale factor / price preview | mitigate | The readout is display-only; the charged total comes from the server 202 response, not the client computation. Client preview drift does not affect the charge. |
| T-35-04-02 | Tampering | override button set client-side | accept | D-08 intent — the override is a UI confirm within the already Google-OAuth-authenticated admin session; the server enforces the hard-block unless override is present; no extra auth in scope (state explicitly in UAT) |
| T-35-04-03 | Information disclosure | stock conflict list shown in UI | accept | Stock counts are internal staff data shown only inside the authenticated admin kiosk surface |
| T-35-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs — UI wiring + existing build pipeline only. Nothing to verify. |
</threat_model>

<verification>
- Frontend Jest + middleware Jest + lint pass; bundle rebuilt via npm run build.
- Staging UAT confirms: factor preview (SCALE-01), no-base disable (D-11), scaled server pricing incl. the D-06 locked-price increase (SCALE-03), linear/round-up quantities (SCALE-02), and the override stock-block (SCALE-05/D-08), with no iPad Safari console errors.
</verification>

<success_criteria>
- The admin Kiosk Sale tab shows a pre-filled "Target volume (L)" input with a live factor readout after a recipe is selected (SCALE-01, D-10); recipes with no base size disable scaling with a prompt (D-11).
- target_volume_l + override flow in both the recipe-sale and confirm bodies; 409 conflicts surface with a working manager override (SCALE-05, D-08).
- The build is regenerated, frontend + middleware tests + lint pass, the feature is verified on staging, and the owner has explicitly acknowledged the D-06 locked-price increase.
</success_criteria>

<output>
Create `.planning/phases/35-batch-scaling-engine/35-04-SUMMARY.md` when done. Record the UAT outcome and the explicit owner acknowledgement of the D-06 locked-price behavior change.
</output>
</output>
</content>
