---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/deploy-production.yml
  - _config.yml
  - .gitignore
  - CNAME
autonomous: true
requirements: []

must_haves:
  truths:
    - "`.planning/` paths 404 on staging AND prod live sites while remaining tracked in git"
    - "`.planning/` files are still present in the repository (not untracked, not removed)"
    - "Prod publishes via upload-pages-artifact with `.planning/` stripped from the artifact at build time (not relying on Jekyll exclude, which is bypassed by the auto-injected .nojekyll)"
    - "CNAME is no longer tracked by git, and enforce-cname.yml continues to self-heal the domain"
    - "Prod deploy still pins the correct domain after CNAME is untracked"
    - "D-01: `.planning/` is excluded at the deploy layer (build-time artifact strip on prod, Jekyll exclude on staging) while kept tracked in git — never `git rm --cached`/untracked"
  artifacts:
    - path: ".github/workflows/deploy-production.yml"
      provides: "Build-time strip step that removes .planning/ from the published Pages artifact (governs the PROD outcome) before upload-pages-artifact@v3"
      contains: "rm -rf .planning"
    - path: "_config.yml"
      provides: "Defense-in-depth Jekyll exclude removing .planning/ from the staging classic-Jekyll build (staging path only)"
      contains: "exclude"
    - path: ".gitignore"
      provides: "CNAME ignored so it stays untracked after removal"
      contains: "CNAME"
  key_links:
    - from: ".github/workflows/deploy-production.yml"
      to: ".planning/"
      via: "rm -rf .planning step before upload-pages-artifact@v3"
      pattern: "rm -rf .planning"
---

<objective>
Repo hygiene (items #15, #16): stop `.planning/` from serving publicly via a DEPLOY-LAYER exclusion while KEEPING IT TRACKED IN GIT (D-01), and untrack the tracked `CNAME` file (item #16) while preserving the prod-deploy domain pinning.

Purpose: Remove internal planning history and the CNAME file from the published surface / git tracking without destroying version-controlled planning history (GSD commit_docs=true stays intact).
Output: A build-time strip step in the production deploy workflow (primary prod mechanism), a `_config.yml` Jekyll exclude (staging defense-in-depth), a `.gitignore` update, and CNAME untracked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- GitHub Pages serves the repo root. No _config.yml exists today (default Jekyll processing). -->
<!-- PROD publishes via .github/workflows/deploy-production.yml: actions/upload-pages-artifact@v3 with path: . (line 47). -->
<!--   upload-pages-artifact AUTO-INJECTS .nojekyll into the artifact, which BYPASSES Jekyll on prod. -->
<!--   => A Jekyll _config.yml `exclude:` has NO effect on the prod artifact. The artifact must be stripped at build time. -->
<!--   Staging uses classic branch-deploy (deploy-production.yml aborts when not the -production repo) and DOES run Jekyll, -->
<!--   so _config.yml works there only. -->
<!-- enforce-cname.yml auto-pins the custom domain after every push to either repo. -->
<!-- CNAME is currently TRACKED (git ls-files CNAME returns it) despite repo CLAUDE.md claiming it is gitignored. -->
<!-- Prod deploy is `git push production main --force`; enforce-cname.yml restores the correct domain. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Exclude .planning/ from the published site at the deploy artifact layer (#15, D-01)</name>
  <read_first>
    - .github/workflows/deploy-production.yml (the prod publish workflow; upload-pages-artifact@v3 at line 45, path: . at line 47)
    - .gitignore (current entries)
    - .github/workflows/enforce-cname.yml (to confirm staging Pages build mechanism — classic Jekyll)
    - CLAUDE.md (deploy section)
  </read_first>
  <action>
    Implement D-01: keep `.planning/` TRACKED IN GIT but EXCLUDE it from the published GitHub Pages site on BOTH staging and prod. Do NOT `git rm --cached .planning` and do NOT untrack it (D-01 explicitly rejects the QUICK-WINS #15 `git rm -r --cached .planning` approach).

    PRIMARY prod mechanism (this is what actually governs prod): edit `.github/workflows/deploy-production.yml`. The `actions/upload-pages-artifact@v3` step (line 45) publishes `path: .` (line 47) and auto-injects a `.nojekyll` file, which BYPASSES Jekyll on prod — so a Jekyll `_config.yml exclude:` has NO effect on the production artifact. Add a step in the `deploy` job AFTER `actions/checkout@v4` and BEFORE the `actions/upload-pages-artifact@v3` step that strips `.planning/` from the checked-out working tree at build time, e.g. `run: rm -rf .planning` (a `find`-based equivalent is acceptable). This removes `.planning/` from the published artifact WITHOUT removing it from git — the repo still tracks every `.planning/` file (D-01 satisfied), only the build artifact loses it.

    DEFENSE-IN-DEPTH for staging (classic Jekyll path only): you MAY also create a root `_config.yml` with a Jekyll `exclude:` list that includes `.planning/`. Staging uses classic branch-deploy + Jekyll, so the exclude works there. This is supplementary — it MUST NOT be the sole/primary mechanism, because it is bypassed on prod. Do not exclude files the site actually needs (content/, css/, js/, images/, *.html).
  </action>
  <verify>
    <automated>grep -q "rm -rf .planning" .github/workflows/deploy-production.yml &amp;&amp; git ls-files .planning/STATE.md | grep -q "STATE.md" &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/deploy-production.yml` has a build-time strip step (`rm -rf .planning` or equivalent) AFTER `actions/checkout@v4` and BEFORE `actions/upload-pages-artifact@v3`.
    - `.planning/` files remain tracked in git: `git ls-files .planning | head` lists tracked files and `git ls-files .planning/STATE.md` returns the path.
    - (Defense-in-depth, optional) `_config.yml` exists at repo root with an `exclude:` list containing `.planning/` for the staging Jekyll path.
    - HUMAN checkpoint verifies the live PROD outcome (`https://steinsandvines.ca/.planning/STATE.md` → 404) and the live STAGING outcome (`https://staging.steinsandvines.ca/.planning/STATE.md` → 404), per D-01 requiring 404 on BOTH.
    - The strip step / `_config.yml` does not remove or exclude `content/`, `css/`, `js/`, `images/`, or any `*.html`.
  </acceptance_criteria>
  <done>Build-time artifact strip governs prod 404; staging Jekyll exclude as defense-in-depth; `.planning/` still tracked in git.</done>
</task>

<task type="auto">
  <name>Task 2: Untrack CNAME with safe sequencing (#16)</name>
  <read_first>
    - CNAME (current value)
    - .gitignore
    - .github/workflows/enforce-cname.yml
    - CLAUDE.md (deploy + CNAME notes)
  </read_first>
  <action>
    Item #16: stop tracking the `CNAME` file. First confirm enforce-cname.yml self-heals the domain on push to each repo (it does, per repo memory). Add `CNAME` to `.gitignore` (CLAUDE.md claims it is already ignored — verify and add if missing). Then `git rm --cached CNAME` so the file is removed from version control but remains on disk locally. Sequence this so it does NOT strand a wrong CNAME mid-deploy: do the untrack on the staging branch first, push to origin, confirm enforce-cname.yml pins `staging.steinsandvines.ca`, then allow promotion. After untracking, the legacy CNAME-swap deploy dance (set steinsandvines.ca → push production → restore) is retired: prod deploy becomes plain `git push production main --force` with enforce-cname.yml pinning `steinsandvines.ca`. Note this retirement in the SUMMARY so CLAUDE.md / memory can be updated. Do not delete the local CNAME file from disk.
  </action>
  <verify>
    <automated>! git ls-files --error-unmatch CNAME 2>/dev/null &amp;&amp; grep -q "CNAME" .gitignore &amp;&amp; test -f CNAME &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files CNAME` returns nothing (untracked).
    - `.gitignore` contains `CNAME`.
    - Local `CNAME` file still exists on disk.
    - SUMMARY documents that the CNAME-swap deploy dance is retired in favor of enforce-cname.yml auto-pinning.
  </acceptance_criteria>
  <done>CNAME untracked and gitignored; deploy process documented as simplified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>`.planning/` build-time artifact strip + staging Jekyll exclude and CNAME untracking, deployed to staging then prod.</what-built>
  <how-to-verify>
    0. Run the full test gate BEFORE any push (CLAUDE.md non-negotiable pre-commit rule): `npm test` (frontend, green) AND `cd zoho-middleware && npm test` (middleware, green) AND `npm run lint` (clean). Do not proceed to push if any fail.
    1. Confirm local CNAME reads `staging.steinsandvines.ca`; `git push origin main`.
    2. Wait for the Pages build, then confirm `https://staging.steinsandvines.ca/.planning/STATE.md` returns 404 (NOT 200) — staging Jekyll-exclude path.
    3. Confirm the staging site itself still loads (home, a product page) and the custom domain is intact (enforce-cname.yml ran).
    4. Promote to prod: `git push production main --force`. Confirm `https://steinsandvines.ca/.planning/STATE.md` returns 404 (build-time strip path) and `steinsandvines.ca` resolves correctly (domain pinned).
    5. Confirm `.planning/` is still git-tracked: `git ls-files .planning | head` lists tracked files (files retained in repo, only the published artifact lost them).
  </how-to-verify>
  <resume-signal>Type "approved" once BOTH sites 404 on `.planning/` paths, `.planning/` is still git-tracked, and domains are intact, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages → public | Repo root published; `.planning/` currently exposes 323 internal files |
| Git history → repo | `.planning/` must remain tracked (no information destroyed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02-I | Information Disclosure | `.planning/` served publicly on staging + prod | mitigate | Build-time `rm -rf .planning` strips it from the prod Pages artifact (Jekyll exclude is bypassed by auto-injected .nojekyll); `_config.yml exclude:` covers the staging classic-Jekyll path; files remain tracked (D-01) |
| T-30-02-D | Denial of Service | Domain pinning during CNAME untrack | mitigate | Sequence on staging first; rely on enforce-cname.yml self-heal; never strand a wrong CNAME |
| T-30-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `.github/workflows/deploy-production.yml` strips `.planning/` from the artifact before `upload-pages-artifact@v3`; `_config.yml` excludes `.planning/` for staging; files still tracked (`git ls-files .planning` non-empty).
- CNAME untracked + gitignored; local file retained.
- Pre-push test gate green: `npm test`, `cd zoho-middleware && npm test`, `npm run lint`.
- Staging AND prod: `.planning/` paths 404; domains intact.
</verification>

<success_criteria>
Items #15 (per D-01) and #16 complete; `.planning/` 404s publicly on BOTH staging and prod (via build-time artifact strip on prod, Jekyll exclude on staging) but stays git-tracked; CNAME untracked without breaking domain pinning.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-02-SUMMARY.md` when done
</output>
