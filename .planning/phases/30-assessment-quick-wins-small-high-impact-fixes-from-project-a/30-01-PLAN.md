---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - images/products/unmatched/
  - test-subpage.html
  - docs-validation-report.md
  - sw.js
  - "404.html"
  - zoho-middleware/lib/gp.js
  - zoho-middleware/jest.config.js
  - package.json
  - content/products.csv
  - content/ingredients.csv
  - content/services.csv
  - content/admin.json
  - content/contact.json
  - content/ingredients.json
  - content/products.json
  - content/reservation.json
  - content/services.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Unreferenced 26 MB image directory no longer ships publicly"
    - "Dead HTML/report files no longer ship publicly"
    - "Dead Global Payments lib and its jest exclusion are gone"
    - "Nine dead content/ files are deleted with zero remaining references"
    - "Self-destruct service worker, its 404.html registration, and the dead stamp:sw script are gone"
    - "Both test suites pass after deletions"
  artifacts:
    - path: "package.json"
      provides: "stamp:pages with test-subpage.html removed; stamp:sw script removed"
      contains: "stamp:pages"
    - path: "zoho-middleware/jest.config.js"
      provides: "gp.js exclusion removed from coverage config"
    - path: "404.html"
      provides: "service worker registration line removed"
  key_links:
    - from: "package.json"
      to: "stamp:pages"
      via: "page list no longer contains test-subpage.html"
      pattern: "stamp:pages"
---

<objective>
Remove dead weight (items #10–14): unreferenced/dead files that ship publicly via GitHub Pages, plus their build-script and config references. This is the lowest-risk batch (D-04 group a) and ships first.

Purpose: Stop ~26 MB of unreferenced assets and several dead files from being served publicly; clean dead lib + scripts.
Output: Deleted files, cleaned package.json and jest.config.js, regenerated test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unreferenced assets and dead files (#10, #11, #14)</name>
  <read_first>
    - 404.html (line 96 service-worker registration)
    - sw.js
    - package.json (scripts: stamp:sw at line 10, stamp:pages at line 15)
    - test-subpage.html
    - docs-validation-report.md
  </read_first>
  <action>
    Delete `images/products/unmatched/` (26 MB unreferenced bottle mockups, item #10). Delete `test-subpage.html` and remove the `'test-subpage.html'` entry from the page list array in the `stamp:pages` npm script in package.json (item #11). Delete root `docs-validation-report.md` (diverged duplicate of the docs/archive copy, item #11). Delete `sw.js`, remove the service-worker registration statement at 404.html:96 (`navigator.serviceWorker.register('/sw.js')` and its wrapping `if('serviceWorker' in navigator)...` block), and remove the entire `stamp:sw` npm script (package.json:10) plus any reference to it from the `build` script chain if present (item #14). Verify with grep that nothing else references `sw.js`, `stamp:sw`, `test-subpage.html`, or `docs-validation-report.md` before finalizing. Do NOT run `npm run build` in this task (no JS module source changed; stamp:sw removal is the only build-script delta and build runs in verify).
  </action>
  <verify>
    <automated>test ! -d images/products/unmatched &amp;&amp; test ! -f sw.js &amp;&amp; test ! -f test-subpage.html &amp;&amp; test ! -f docs-validation-report.md &amp;&amp; ! grep -q "serviceWorker" 404.html &amp;&amp; ! grep -q "stamp:sw" package.json &amp;&amp; ! grep -q "test-subpage.html" package.json &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `images/products/unmatched/` directory does not exist.
    - `sw.js`, `test-subpage.html`, `docs-validation-report.md` do not exist.
    - 404.html contains no `serviceWorker` registration.
    - package.json `stamp:pages` page list no longer contains `test-subpage.html`; no `stamp:sw` script; `build` script references no `stamp:sw`.
    - `grep -rn "sw.js\|stamp:sw\|test-subpage" --include=*.js --include=*.html --include=*.json . | grep -v node_modules | grep -v .min.js` returns no live references.
  </acceptance_criteria>
  <done>All five targets removed and no dangling references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Delete dead Global Payments lib and 9 dead content files (#12, #13)</name>
  <read_first>
    - zoho-middleware/lib/gp.js
    - zoho-middleware/jest.config.js (line 7 `!lib/gp.js` exclusion)
    - content/ (the 9 candidate files)
  </read_first>
  <action>
    Delete `zoho-middleware/lib/gp.js` (dead Global Payments lib, zero requires — item #12) and remove the `'!lib/gp.js'` entry (jest.config.js:7) from the coverage `collectCoverageFrom` array. Do NOT touch the `'!lib/mailer.js'` exclusion here — that belongs to item #21 (Plan 06). For item #13, before deleting each, run grep across the repo (excluding node_modules and *.min.js) to confirm ZERO references, then delete these 9 dead content files: `content/products.csv`, `content/ingredients.csv`, `content/services.csv`, `content/admin.json`, `content/contact.json`, `content/ingredients.json`, `content/products.json`, `content/reservation.json`, `content/services.json`. Do NOT delete the live files (timeslots.csv, home.json, about.json, reviews.json, shared.json, email-templates.json, zoho-snapshot.json). If grep finds any reference to a candidate file, leave that file and note it in the SUMMARY.
  </action>
  <verify>
    <automated>cd zoho-middleware &amp;&amp; npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/lib/gp.js` does not exist.
    - jest.config.js no longer contains `!lib/gp.js`; still contains `!lib/mailer.js`.
    - All 9 dead content files removed; `content/zoho-snapshot.json`, `content/home.json`, `content/about.json`, `content/shared.json`, `content/reviews.json`, `content/email-templates.json`, `content/timeslots.csv` still present.
    - `grep -rn "products.csv\|admin.json\|reservation.json" --include=*.js --include=*.html . | grep -v node_modules | grep -v .min.js` returns no live references.
    - `cd zoho-middleware &amp;&amp; npm test` passes.
  </acceptance_criteria>
  <done>Dead lib + jest exclusion removed; 9 verified-dead content files removed; middleware tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dead-weight removal batch (#10–14) deployed to staging.</what-built>
  <how-to-verify>
    1. Run `npm test` AND `cd zoho-middleware &amp;&amp; npm test` AND `npm run lint` locally — all green.
    2. Confirm CNAME reads `staging.steinsandvines.ca`, then `git push origin main`.
    3. On staging.steinsandvines.ca: site loads normally, no console errors; visit a known product page and confirm assets load.
    4. Confirm `staging.steinsandvines.ca/sw.js` and `staging.steinsandvines.ca/test-subpage.html` return 404.
    5. Approve to promote to production per the staging-first workflow.
  </how-to-verify>
  <resume-signal>Type "approved" once staging verified and (optionally) promoted to prod, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages → public | Repo root served publicly; deleting files removes them from the served surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-I | Information Disclosure | Publicly served dead files (unmatched images, test-subpage, docs report) | mitigate | Deletion removes them from the published surface |
| T-30-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm test` and `cd zoho-middleware && npm test` green.
- No live references to any deleted file.
- Staging serves cleanly; deleted paths 404.
</verification>

<success_criteria>
Items #10, #11, #12, #13, #14 complete; both test suites pass; staging verified.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-01-SUMMARY.md` when done
</output>
