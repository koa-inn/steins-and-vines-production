---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 05
type: execute
wave: 4
depends_on: [30-04]
files_modified:
  - js/kiosk.js
  - js/admin.js
  - js/brewpad.js
  - js/modules/02-utils.js
  - js/modules/07-catalog-kits.js
  - package.json
  - js/kiosk.min.js
  - js/admin.min.js
  - js/brewpad.min.js
  - js/main.js
  - js/main.min.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Customer-controlled Zoho contact name/email are escaped before being written into innerHTML in staff POS/admin contexts"
    - "The main bundle resolves exactly ONE canonical escapeHTML (with apostrophe escaping) — no weak shadow copies override it"
    - "Custom-field flattening in 07-catalog-kits.js is guarded against __proto__/constructor/prototype pollution"
    - "The full frontend test suite passes after the concat:js change"
  artifacts:
    - path: "js/kiosk.js"
      provides: "escapeHTML wrapping the contact name/email innerHTML sinks (~lines 2240-2241, 2342-2343)"
      contains: "escapeHTML"
    - path: "js/admin.js"
      provides: "escapeHTML wrapping the contact name/email innerHTML sinks"
      contains: "escapeHTML"
    - path: "js/modules/07-catalog-kits.js"
      provides: "prototype-pollution guard in custom-field flattening (lines 87-93)"
      contains: "__proto__"
    - path: "package.json"
      provides: "concat:js including js/lib/utils.js, producing exactly one canonical escapeHTML"
      contains: "js/lib/utils.js"
  key_links:
    - from: "js/modules/07-catalog-kits.js"
      to: "prototype-pollution guard"
      via: "key === __proto__ / constructor / prototype skip (copied from 17-search-overlay.js:176)"
      pattern: "__proto__|constructor|prototype"
    - from: "package.json concat:js"
      to: "js/lib/utils.js"
      via: "concatenation ordering yields single canonical escapeHTML"
      pattern: "js/lib/utils.js"
---

<objective>
Security hardening batch (items #7, #8, #9, D-04 group c — payment-adjacent staff UI). Escape customer-controlled Zoho contact data at innerHTML sinks, fix the weak/shadowing escapeHTML so the canonical apostrophe-escaping version is canonical in the bundle, and add the prototype-pollution guard to catalog custom-field flattening. Items #7/#8 touch kiosk/admin staff POS UI and MUST be human-verified on the staging kiosk after deploy.

Purpose: Close XSS and prototype-pollution sinks in staff-facing payment-adjacent code.
Output: Escaped sinks, single canonical escapeHTML, proto guard, rebuilt bundles, staging-kiosk verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Canonical escapeHTML (apostrophe-escaping), js/lib/utils.js:6 -> exported {escapeHTML, formatCurrency}. THIS is the target. -->
<!-- Weak/shadow copies: js/modules/02-utils.js:97 (no apostrophe escaping, currently first in concat),
     js/brewpad.js:6 (local, overrides canonical), js/kiosk.js:526 (local copy). -->
<!-- concat:js (package.json:7) currently: cat js/lib/constants.js js/modules/01-config.js js/modules/02-utils.js ... > js/main.js
     (note: js/lib/utils.js is NOT in the concat today, so the bundle uses 02-utils.js:97's weak copy). -->
<!-- XSS sinks (customer-controlled Zoho contact name/email into innerHTML, staff context):
     js/kiosk.js selectedEl.innerHTML (~2240-2241): '<span>' + (c.name||'') + (c.email ? ' — ' + c.email : '') + '</span>' ...
     js/kiosk.js result-row innerHTML (~2342-2343): '<strong>' + (c.contact_name||c.name||'') + '</strong>' + (c.email ? ' <span...>' + c.email + '</span>' : '')
     js/admin.js mirrors these at the contact-search sinks (~10092 and ~10186-10187 in current file; QUICK-WINS cites 9857 and 9950-9953 — line numbers drifted, locate by the same code pattern). -->
<!-- Prototype-pollution guard pattern, js/modules/17-search-overlay.js:176:
     if (key === '__proto__' || key === 'constructor' || key === 'prototype') return;  (also in 16-catalog-subpage.js) -->
<!-- Target flatten loop, js/modules/07-catalog-kits.js:87-93: z.custom_fields.forEach builds key from cf.label,
     then obj[key] = String(cf.value)  -- no guard today. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Escape XSS sinks (#7) and add prototype-pollution guard (#9)</name>
  <files>js/kiosk.js, js/admin.js, js/modules/07-catalog-kits.js, tests/frontend/catalog-kits-proto-guard.test.js</files>
  <read_first>
    - js/kiosk.js (contact-render innerHTML sinks ~2240-2241 and ~2342-2343; local escapeHTML at :526)
    - js/admin.js (contact-search innerHTML sinks — locate by the `selectedEl.innerHTML = '<span>' + (c.name...` and `'<strong>' + (c.contact_name||c.name...` patterns; current lines ~10092 and ~10186-10187)
    - js/modules/17-search-overlay.js (line 176 prototype-pollution guard pattern to copy)
    - js/modules/07-catalog-kits.js (lines 87-93 custom-field flattening loop)
    - tests/frontend/ (existing jsdom test patterns)
  </read_first>
  <behavior>
    - Test 1 (RED first, #9): flattening custom fields whose label normalizes to `__proto__`, `constructor`, or `prototype` does NOT pollute Object.prototype (e.g. `({}).polluted` stays undefined) and the malicious key is skipped.
    - Test 2 (#9): legitimate custom-field labels still flatten normally onto the object.
  </behavior>
  <action>
    Item #7: wrap the customer-controlled Zoho contact name/email values in `escapeHTML(...)` at the innerHTML sinks: js/kiosk.js ~2240-2241 (`c.name`, `c.email` in `selectedEl.innerHTML`) and ~2342-2343 (`c.contact_name || c.name`, `c.email` in the result-row innerHTML); and the mirrored sinks in js/admin.js (the `selectedEl.innerHTML = '<span>' + (c.name...` sink and the `'<strong>' + (c.contact_name||c.name...` + `c.email` result-row sink — current lines ~10092 and ~10186-10187; locate by code pattern, do NOT trust the drifted QUICK-WINS line numbers). Use each file's available escapeHTML (kiosk.js has a local copy at :526; admin.js — confirm it has access to escapeHTML, else use the canonical one). Item #9: copy the 3-line `if (key === '__proto__' || key === 'constructor' || key === 'prototype') return;` guard from js/modules/17-search-overlay.js:176 into the custom-field flattening loop in js/modules/07-catalog-kits.js:87-93, placed before the `obj[key] = String(cf.value)` assignment. Write the proto-guard regression test FIRST (RED), then implement (GREEN). Do NOT run the build in this task — Task 2 changes concat:js, so the single rebuild happens there.
  </action>
  <verify>
    <automated>npm test -- catalog-kits-proto-guard 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - js/kiosk.js: both contact-render innerHTML sinks wrap name AND email in escapeHTML.
    - js/admin.js: both mirrored contact-search innerHTML sinks wrap name AND email in escapeHTML.
    - js/modules/07-catalog-kits.js: flatten loop skips `__proto__`/`constructor`/`prototype` keys before assignment.
    - `tests/frontend/catalog-kits-proto-guard.test.js` exists, asserts no prototype pollution + normal flatten still works, and passes.
  </acceptance_criteria>
  <done>XSS sinks escaped; proto guard added with passing regression test (build deferred to Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Canonicalize escapeHTML in the bundle (#8) and rebuild</name>
  <read_first>
    - js/lib/utils.js (line 6 canonical escapeHTML with apostrophe escaping)
    - js/modules/02-utils.js (line 97 weak escapeHTML; line 303 module.exports — confirm whether removing the function breaks exports)
    - js/brewpad.js (line 6 local escapeHTML to delete)
    - js/kiosk.js (line 526 local escapeHTML copy to reconcile)
    - package.json (line 7 concat:js order; line 8 minify:js terser targets)
  </read_first>
  <action>
    Item #8: make the canonical apostrophe-escaping `escapeHTML` (js/lib/utils.js:6) the single definition that the main bundle resolves. Steps: (1) Add `js/lib/utils.js` to the `concat:js` script (package.json:7) — place it so its `escapeHTML` is the canonical one in the concatenated `js/main.js`. CRITICAL build-risk (per CONTEXT specifics + Item #8): the concat must NOT double-define `escapeHTML`. Since `js/modules/02-utils.js:97` also defines `escapeHTML`, you must remove/reconcile the weak `02-utils.js` copy so the bundle contains EXACTLY ONE canonical `escapeHTML`; preserve the `02-utils.js` module.exports (re-export the canonical one or keep other exports intact). Decide concat ordering deliberately (e.g. utils.js before 02-utils.js, with 02-utils.js's local definition deleted) and confirm the resulting bundle has one canonical definition. (2) Delete the local `escapeHTML` in js/brewpad.js:6 so it uses the canonical version (confirm brewpad.js loads a source that provides escapeHTML, or keep a single canonical copy if brewpad is standalone — do not leave it referencing an undefined function). (3) Reconcile/remove the js/kiosk.js:526 local copy similarly (kiosk.js is a standalone bundle, not part of concat:js — keep exactly one canonical copy there, replacing the weak one with the apostrophe-escaping logic). Then run `npm run build` (regenerates js/main.js + all *.min.js, including kiosk/admin/brewpad bundles which carry the Task 1 sink fixes). Run the FULL frontend test suite (`npm test`) and `npm run lint`. Verify the concatenated js/main.js contains exactly one `function escapeHTML`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tail -3 &amp;&amp; [ "$(grep -c 'function escapeHTML' js/main.js)" -eq 1 ] &amp;&amp; npm test 2>&amp;1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `concat:js` includes `js/lib/utils.js`.
    - Concatenated `js/main.js` contains exactly ONE `function escapeHTML` (the canonical apostrophe-escaping one).
    - js/brewpad.js no longer defines a local weak escapeHTML; js/kiosk.js has exactly one canonical (apostrophe-escaping) escapeHTML.
    - js/modules/02-utils.js exports are intact (other code importing from it still works; tests pass).
    - `npm run build` succeeds; all `*.min.js` regenerated and committed.
    - FULL `npm test` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Bundle resolves a single canonical escapeHTML; full suite green; all bundles rebuilt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Security batch (#7 XSS escaping, #8 canonical escapeHTML, #9 proto guard) deployed to staging — PAYMENT-ADJACENT staff UI.</what-built>
  <how-to-verify>
    1. Run `npm test` AND `cd zoho-middleware &amp;&amp; npm test` AND `npm run lint` locally — all green. Confirm `grep -c 'function escapeHTML' js/main.js` is exactly 1.
    2. Confirm CNAME reads `staging.steinsandvines.ca`; `git push origin main`.
    3. STAGING KIOSK verification (REQUIRED for #7/#8): open the staging kiosk POS, run the contact/customer search, and confirm contact name + email render correctly (no broken markup, no double-escaping artifacts). If a Zoho test contact with HTML-special characters (e.g. `<`, `&`, `'`) in the name is available, confirm it renders as literal text, not executed/markup. Note the PWA service-worker cache gotcha — use the in-app cache-clear / hard refresh so you are testing the freshly deployed bundle, not a stale SW cache.
    4. STAGING ADMIN verification: same contact-search render check in the admin POS context.
    5. Confirm catalog kit pages still load and render products normally (proto-guard change did not break flattening).
    6. Approve to promote to production only after the staging kiosk + admin checks pass.
  </how-to-verify>
  <resume-signal>Type "approved" once the staging kiosk AND admin contact renders are verified safe and catalog loads cleanly, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Zoho contact data → staff POS/admin DOM | Customer-controlled contact name/email (set at checkout) is rendered into innerHTML in staff payment-adjacent UI |
| Zoho catalog custom fields → client object map | Untrusted custom-field labels become object keys during flattening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-XSS1 | Tampering/Elevation (Stored XSS) | js/kiosk.js contact-render innerHTML sinks (~2240-2241, ~2342-2343) | mitigate | Wrap customer-controlled name/email in escapeHTML before innerHTML |
| T-30-05-XSS2 | Tampering/Elevation (Stored XSS) | js/admin.js contact-search innerHTML sinks (~10092, ~10186-10187) | mitigate | Wrap customer-controlled name/email in escapeHTML before innerHTML |
| T-30-05-ESC | Tampering (incomplete escaping) | Weak escapeHTML shadow (02-utils.js:97 / brewpad.js:6 / kiosk.js:526) lacking apostrophe escaping | mitigate | Canonicalize on js/lib/utils.js:6; ensure single definition in each bundle |
| T-30-05-PP | Tampering (Prototype Pollution) | js/modules/07-catalog-kits.js:87-93 custom-field flattening | mitigate | Skip `__proto__`/`constructor`/`prototype` keys (guard copied from 17-search-overlay.js:176) |
| T-30-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm test` (incl. proto-guard regression) and `cd zoho-middleware && npm test` green; `npm run lint` clean.
- `js/main.js` contains exactly one canonical escapeHTML.
- Staging kiosk + admin contact renders verified safe; catalog loads.
- All bundles rebuilt and committed.
</verification>

<success_criteria>
Items #7, #8, #9 complete; XSS sinks escaped, single canonical escapeHTML, proto guard in place; staging-kiosk + admin human verification passed.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-05-SUMMARY.md` when done
</output>
