---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 06
type: execute
wave: 5
depends_on: [30-01]
files_modified:
  - railway.toml
  - zoho-middleware/package.json
  - zoho-middleware/package-lock.json
  - zoho-middleware/server.js
  - zoho-middleware/jest.config.js
  - zoho-middleware/__tests__/checkout-fallback-email.test.js
autonomous: false
user_setup:
  - service: railway
    why: "Verify production middleware env vars are set (item #17)"
    env_vars:
      - name: REDIS_ENCRYPTION_KEY
        source: "Railway → zoho-middleware service → Variables (closes issue #106; without it the Zoho refresh token is stored plaintext)"
      - name: HELCIM_WEBHOOK_SECRET
        source: "Railway → zoho-middleware service → Variables"
      - name: RECAPTCHA_SECRET_KEY
        source: "Railway → zoho-middleware service → Variables"
      - name: SENTRY_DSN
        source: "Railway → zoho-middleware service → Variables"
  - service: uptime-monitor
    why: "Stand up an external uptime monitor on middleware /health (item #18)"
    dashboard_config:
      - task: "Point UptimeRobot/Better Stack at the middleware /health endpoint; alert on authenticated:false or redis:false"
        location: "UptimeRobot or Better Stack dashboard"
requirements: []

must_haves:
  truths:
    - "Frontend-only pushes no longer trigger middleware rebuilds on Railway (watchPatterns scoped to zoho-middleware/**)"
    - "node-cron is upgraded to 4.2.1, the 2 moderate npm audit findings are cleared, and the scheduled warm-up still registers and runs"
    - "Stale jest coverage exclusion for mailer.js is removed and the misleading test header is corrected"
    - "Railway env vars are human-verified present; an uptime monitor watches /health"
    - "D-03: items #17 (Railway env vars) and #18 (uptime monitor) are tracked as human-action tasks (autonomous: false), not auto-completable code tasks; #19/#20/#21 execute normally"
  artifacts:
    - path: "railway.toml"
      provides: "watchPatterns scoped to middleware dir"
      contains: "watchPatterns"
    - path: "zoho-middleware/package.json"
      provides: "node-cron pinned to 4.2.1"
      contains: "node-cron"
    - path: "zoho-middleware/jest.config.js"
      provides: "mailer.js coverage exclusion removed"
  key_links:
    - from: "railway.toml"
      to: "zoho-middleware/**"
      via: "watchPatterns build trigger scope"
      pattern: "watchPatterns"
---

<objective>
Config/infra + test-config cleanup (items #19, #20, #21) executed normally, plus the two tracked HUMAN-ACTION items (#17, #18) per D-03. node-cron is a semver-major upgrade (#20), so it gets an explicit human checkpoint before merge.

Purpose: Stop spurious middleware rebuilds, clear npm audit findings, tidy jest config, and ensure prod env + monitoring are in place.
Output: railway.toml watchPatterns, node-cron@4.2.1, jest cleanup, and a completed human checklist for env vars + uptime monitor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- railway.toml is small; root config that drives the Railway build for the middleware service. -->
<!-- node-cron currently "^3.0.3" in zoho-middleware/package.json. cron.schedule() options used at
     zoho-middleware/server.js:439-459 (scheduled warm-up: 05:00 & 13:00 UTC). v4 is semver-major. -->
<!-- jest.config.js collectCoverageFrom currently excludes '!lib/gp.js' (removed in Plan 30-01) and '!lib/mailer.js' (remove here). -->
<!-- __tests__/checkout-fallback-email.test.js:4-5 has a misleading header to correct. -->
<!-- Staging calls the PROD middleware (no separate staging middleware) — #19/#20 affect prod immediately on Railway deploy. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: railway.toml watchPatterns (#19) and jest cleanup (#21)</name>
  <read_first>
    - railway.toml (current content)
    - zoho-middleware/jest.config.js (collectCoverageFrom; the `!lib/mailer.js` exclusion at line 8)
    - zoho-middleware/__tests__/checkout-fallback-email.test.js (lines 4-5 misleading header)
  </read_first>
  <action>
    Item #19: add `watchPatterns = ["zoho-middleware/**"]` to root `railway.toml` so frontend-only pushes do not trigger middleware rebuilds (place it in the appropriate build/service section per the railway.toml schema). Item #21: remove the stale `'!lib/mailer.js'` coverage exclusion from `zoho-middleware/jest.config.js` (mailer now has tests). Plan 30-01 already removed the `'!lib/gp.js'` line; if it is somehow still present, remove it too. Fix the misleading header comment at `zoho-middleware/__tests__/checkout-fallback-email.test.js:4-5` to accurately describe the test; do NOT change the test assertions (CLAUDE.md rule 10).
  </action>
  <verify>
    <automated>grep -q "watchPatterns" railway.toml &amp;&amp; grep -q "zoho-middleware" railway.toml &amp;&amp; ! grep -q "mailer.js" zoho-middleware/jest.config.js &amp;&amp; cd zoho-middleware &amp;&amp; npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `railway.toml` contains `watchPatterns = ["zoho-middleware/**"]`.
    - `zoho-middleware/jest.config.js` has no `mailer.js` (and no `gp.js`) coverage exclusion.
    - `checkout-fallback-email.test.js` header (lines 4-5) accurately describes the test; assertions unchanged.
    - `cd zoho-middleware && npm test` passes.
  </acceptance_criteria>
  <done>watchPatterns added; jest exclusions cleaned; test header corrected; middleware tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade node-cron to 4.2.1 (#20)</name>
  <read_first>
    - zoho-middleware/package.json (node-cron `^3.0.3`)
    - zoho-middleware/server.js (lines 439-459 cron.schedule() warm-up registration + options)
  </read_first>
  <action>
    Item #20: from the middleware dir run `cd zoho-middleware && npm i node-cron@4.2.1` (semver-major; clears the 2 moderate npm audit findings). Then verify the `cron.schedule()` call(s) at zoho-middleware/server.js:439-459 still use options valid in node-cron v4 (the v4 API changed option names/signatures vs v3 — check the scheduled-options object, e.g. timezone handling, and adjust the call to the v4-correct form if needed without changing the schedule semantics: 05:00 & 13:00 UTC daily warm-up). Run `cd zoho-middleware && npm test` and `cd zoho-middleware && npm audit` to confirm the moderate findings are gone. Commit the updated package.json + package-lock.json. NOTE: this is a [SUS-by-major-bump] dependency change verified via the legitimacy checkpoint below before merge; do not push to Railway until the checkpoint approves (staging calls the PROD middleware, so a node-cron regression would hit prod).
  </action>
  <verify>
    <automated>cd zoho-middleware &amp;&amp; grep -q '"node-cron": "[\^~]*4\.2\.1"' package.json &amp;&amp; npm test 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/package.json` pins node-cron to `4.2.1`.
    - `cron.schedule()` at server.js:439-459 uses v4-valid options; the warm-up still registers (startup log `[cron] Scheduled warm-up registered`).
    - `cd zoho-middleware && npm audit` no longer reports the 2 prior moderate findings.
    - `cd zoho-middleware && npm test` passes; package-lock.json committed.
  </acceptance_criteria>
  <done>node-cron at 4.2.1; cron options v4-valid; audit clean; middleware tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>node-cron@4.2.1 semver-major upgrade (#20) — payment/prod-adjacent (staging uses the PROD middleware).</what-built>
  <how-to-verify>
    1. Verify on npmjs.com/package/node-cron that 4.2.1 is the legitimate published version and not typosquatted.
    2. Confirm `cd zoho-middleware && npm audit` shows the 2 prior moderate findings cleared with no new high/critical introduced.
    3. Confirm `cd zoho-middleware && npm test` passes and the cron warm-up registers at startup (run the middleware locally and check the `[cron] Scheduled warm-up registered` log line, or inspect server.js:439-459 logic).
    4. Approve before the change is deployed to Railway (prod middleware).
  </how-to-verify>
  <resume-signal>Type "approved" once node-cron@4.2.1 is verified legitimate and the cron warm-up still registers, or describe issues.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>Tracked human/dashboard checklist (items #17, #18 per D-03) — no code; Claude cannot perform these.</what-built>
  <how-to-verify>
    Item #17 — In the Railway dashboard (zoho-middleware service → Variables), confirm these env vars are SET in PRODUCTION:
      - `REDIS_ENCRYPTION_KEY` (closes issue #106 — without it the Zoho refresh token is stored plaintext)
      - `HELCIM_WEBHOOK_SECRET`
      - `RECAPTCHA_SECRET_KEY`
      - `SENTRY_DSN`
    Item #18 — Stand up an external uptime monitor (UptimeRobot or Better Stack) pointed at the middleware `/health` endpoint; configure it to alert when the health payload reports `authenticated:false` or `redis:false`.
    Record in the SUMMARY which env vars were already set vs. newly added, and the uptime-monitor URL/check that was created.
  </how-to-verify>
  <resume-signal>Type "done" once the four Railway env vars are confirmed/added and the /health uptime monitor is live, or note any var that could not be set.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Config/infra cleanup (#19, #21) and node-cron upgrade (#20) deployed to staging then Railway prod.</what-built>
  <how-to-verify>
    1. `cd zoho-middleware && npm test` and `npm test` (frontend) and `npm run lint` green locally.
    2. Confirm CNAME reads `staging.steinsandvines.ca`; `git push origin main`. Confirm a frontend-only push no longer triggers a middleware rebuild on Railway (watchPatterns working).
    3. Deploy the middleware change to Railway prod (the node-cron + jest/config changes). Confirm the middleware boots, `/health` returns healthy (authenticated + redis ok), and the `[cron] Scheduled warm-up registered` log appears.
    4. Promote frontend config (railway.toml lives at repo root) to production per staging-first workflow.
  </how-to-verify>
  <resume-signal>Type "approved" once the middleware is healthy on Railway with node-cron@4.2.1 and watchPatterns confirmed, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → middleware deps | Semver-major node-cron upgrade pulls new dependency code into the prod middleware |
| Railway env → middleware secrets | Missing REDIS_ENCRYPTION_KEY leaves the Zoho refresh token stored plaintext |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-06-I | Information Disclosure | Plaintext Zoho refresh token when REDIS_ENCRYPTION_KEY unset | mitigate | Human-verify REDIS_ENCRYPTION_KEY set in Railway prod (#17, closes #106) |
| T-30-06-D | Denial of Service | Undetected middleware outage | mitigate | Uptime monitor on /health alerting on authenticated:false / redis:false (#18) |
| T-30-06-SC | Tampering | node-cron semver-major install (#20) | mitigate | Legitimacy checkpoint (npmjs.com verify) + npm audit + cron-registration check before Railway deploy |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` and frontend `npm test` + `npm run lint` green.
- watchPatterns scoped to middleware; node-cron@4.2.1; audit clean; jest exclusions removed.
- Human checklist (#17 env vars, #18 uptime monitor) completed and recorded.
- Middleware healthy on Railway post-deploy.
</verification>

<success_criteria>
Items #19, #20, #21 complete and deployed; #17 and #18 human actions completed and recorded per D-03.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-06-SUMMARY.md` when done
</output>
