---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 04
type: execute
wave: 3
depends_on: [30-03]
files_modified:
  - js/modules/12-checkout.js
  - js/modules/13-init.js
  - tests/frontend/checkout-waitlist.test.js
  - tests/frontend/kiosk-attract-reset.test.js
  - js/main.js
  - js/main.min.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Beer waitlist signups are captured via /api/contact (no longer discarded to a placeholder Google Form)"
    - "The waitlist no longer shows fake success when the submission did not actually send"
    - "Kiosk idle/attract reset clears BOTH dual carts and milled-item state, not just the legacy reservation key"
    - "A regression test exists and passes for each fix"
  artifacts:
    - path: "js/modules/12-checkout.js"
      provides: "waitlist form routed through /api/contact"
      contains: "/api/contact"
    - path: "js/modules/13-init.js"
      provides: "attract-screen reset clearing sv-cart-ferment, sv-cart-ingredients, _milledItemKeys"
      contains: "sv-cart-ferment"
    - path: "js/main.js"
      provides: "rebuilt concatenated bundle including the two module fixes"
  key_links:
    - from: "js/modules/12-checkout.js"
      to: "/api/contact"
      via: "fetch POST replacing the Google Form POST"
      pattern: "api/contact"
    - from: "js/modules/13-init.js"
      to: "localStorage carts"
      via: "removeItem on both cart keys + milled state"
      pattern: "sv-cart-ferment|sv-cart-ingredients|_milledItemKeys"
---

<objective>
User-facing JS bug fixes that REQUIRE a rebuild (items #2, #4): route the beer waitlist through `/api/contact` (D-02) and fix the kiosk idle-reset cart leak. Per CLAUDE.md, write a regression test FIRST for each, then fix, then `npm run build`.

Purpose: Stop silently discarding waitlist signups; stop leaking carts between kiosk customers.
Output: Fixed checkout/init modules, regression tests, rebuilt bundles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existing contact-form client wiring (the reuse target for D-02), js/modules/12-checkout.js:2192-2202:
     setupContactSubmit() POSTs JSON {name, email, message} to `${MIDDLEWARE_URL}/api/contact`,
     reads JSON {success, error}, shows success on d.success, showToast(err) on failure.
     MIDDLEWARE_URL via SHEETS_CONFIG.MIDDLEWARE_URL. -->
<!-- Server handler: zoho-middleware/server.js:155 app.post('/api/contact', contactLimiter, ...) -
     existing reCAPTCHA/validation/mailer plumbing. -->
<!-- Current broken waitlist, js/modules/12-checkout.js:1689-1696 setupBeerWaitlistForm():
     builds a hidden form POSTing to docs.google.com/.../YOUR_BEER_WAITLIST_FORM_ID with
     entry.YOUR_EMAIL_ENTRY_ID, then ALWAYS reveals #beer-waitlist-confirm (fake success). -->
<!-- index.html:227-231 #beer-waitlist-form (#beer-waitlist-email input) + #beer-waitlist-confirm. -->
<!-- Kiosk attract reset, js/modules/13-init.js:468-488 initKioskAttractScreen():
     on idle reset does localStorage.removeItem(RESERVATION_KEY) only. -->
<!-- Cart keys (from CLAUDE.md): sv-cart-ferment, sv-cart-ingredients (legacy sv-reservation). _milledItemKeys state. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route beer waitlist through /api/contact (#2, D-02)</name>
  <files>tests/frontend/checkout-waitlist.test.js, js/modules/12-checkout.js</files>
  <read_first>
    - js/modules/12-checkout.js (setupBeerWaitlistForm() at lines 1689-1696; setupContactSubmit() at 2192-2202 as the reuse pattern)
    - index.html (lines 227-231 waitlist form + confirm element)
    - tests/frontend/ (existing checkout test patterns, e.g. checkout.test.js, for jsdom + fetch-mock setup)
    - zoho-middleware/server.js (line 155 /api/contact contract)
  </read_first>
  <behavior>
    - Test 1 (RED first): submitting the waitlist form with a valid email POSTs to `${MIDDLEWARE_URL}/api/contact` with a JSON body carrying the email (and a synthesized name/message identifying it as a beer-waitlist signup) and the confirm element is shown ONLY after a `{success:true}` response.
    - Test 2: on a failed/`{success:false}` response or network error, the confirm element is NOT shown and an error is surfaced (no fake success).
    - Test 3: empty/blank email does not fire a request.
  </behavior>
  <action>
    Implement D-02: rewrite `setupBeerWaitlistForm()` (js/modules/12-checkout.js:1689-1696) to route through the existing `/api/contact` endpoint, mirroring `setupContactSubmit()` (12-checkout.js:2192-2202). Remove the placeholder Google Form POST (the hidden form with `action=https://docs.google.com/forms/d/e/YOUR_BEER_WAITLIST_FORM_ID/formResponse` and `entry.YOUR_EMAIL_ENTRY_ID`). Build a JSON body `{ name, email, message }` where `email` is the entered waitlist email, `name` is a fixed label like `Beer Waitlist Signup`, and `message` identifies the source (e.g. `Beer waitlist signup`); POST to `${SHEETS_CONFIG.MIDDLEWARE_URL}/api/contact` with `Content-Type: application/json` (match setupContactSubmit headers — it does not send an x-api-key or recaptcha_token, so reuse the same minimal contract and the server's existing reCAPTCHA/validation/mailer plumbing handles it). Reveal `#beer-waitlist-confirm` and hide the form ONLY on `{success:true}`; on failure restore the button and show an error toast (no fake-success path). Do NOT introduce new infra or env vars (D-02). Write the regression test FIRST (RED), then implement (GREEN). After the module change, run `npm run build` to regenerate `js/main.js` + `js/main.min.js`.
  </action>
  <verify>
    <automated>npm test -- checkout-waitlist 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/frontend/checkout-waitlist.test.js` exists and asserts the three behaviors above; passes.
    - js/modules/12-checkout.js contains no `YOUR_BEER_WAITLIST_FORM_ID` / `YOUR_EMAIL_ENTRY_ID` / `docs.google.com/forms` reference.
    - Waitlist submit POSTs to `/api/contact`; confirm shown only on success.
    - `js/main.js` and `js/main.min.js` regenerated (contain the new waitlist logic; no Google Form reference).
    - `npm test` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Waitlist captured via /api/contact; fake success removed; tests green; bundles rebuilt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix kiosk idle-reset cart leak (#4)</name>
  <files>tests/frontend/kiosk-attract-reset.test.js, js/modules/13-init.js</files>
  <read_first>
    - js/modules/13-init.js (initKioskAttractScreen() at 468-488; the `localStorage.removeItem(RESERVATION_KEY)` at line 488)
    - js/lib/constants.js (CART_KEYS for sv-cart-ferment / sv-cart-ingredients canonical names)
    - tests/frontend/ (existing jsdom localStorage test patterns)
  </read_first>
  <behavior>
    - Test 1 (RED first): after seeding `sv-cart-ferment`, `sv-cart-ingredients`, the legacy reservation key, and `_milledItemKeys` state, invoking the attract/idle reset clears ALL of them.
    - Test 2: the reset does not throw when one or more keys are already absent.
  </behavior>
  <action>
    Item #4: `initKioskAttractScreen()` (js/modules/13-init.js:468-488) currently clears only the legacy `RESERVATION_KEY` on idle reset, leaking the dual carts between customers. Extend the reset to also clear `sv-cart-ferment` and `sv-cart-ingredients` (use the canonical CART_KEYS constants from js/lib/constants.js if available, else the literal keys) and reset the `_milledItemKeys` state (clear the in-memory set/object and any persisted copy). Keep clearing the legacy reservation key for backward compatibility. Write the regression test FIRST (RED) seeding all keys and asserting they are cleared, then implement (GREEN). After the module change, run `npm run build` to regenerate `js/main.js` + `js/main.min.js`.
  </action>
  <verify>
    <automated>npm test -- kiosk-attract-reset 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tests/frontend/kiosk-attract-reset.test.js` exists and asserts both behaviors; passes.
    - js/modules/13-init.js attract reset clears `sv-cart-ferment`, `sv-cart-ingredients`, the legacy reservation key, and `_milledItemKeys`.
    - `js/main.js` and `js/main.min.js` regenerated.
    - `npm test` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Kiosk idle reset clears both carts + milled state; regression test green; bundles rebuilt.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Waitlist /api/contact routing and kiosk cart-leak fix (#2, #4) deployed to staging.</what-built>
  <how-to-verify>
    1. Run `npm test` and `cd zoho-middleware &amp;&amp; npm test` and `npm run lint` locally (all green); `npm run build` ran (bundles committed).
    2. Confirm CNAME reads `staging.steinsandvines.ca`; `git push origin main`.
    3. On staging homepage: submit the beer waitlist with a real test email; confirm the confirm message appears AND that a contact email is actually received via the /api/contact mailer path (check the staff inbox the contact form delivers to). Note the BrewPad/PWA service-worker cache gotcha if the page seems stale — hard refresh.
    4. On the staging kiosk attract/idle flow: add items to both carts, trigger idle reset, confirm BOTH carts and milled state are empty for the next customer.
    5. Approve to promote to production.
  </how-to-verify>
  <resume-signal>Type "approved" once the waitlist email is received and the kiosk reset clears both carts, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/contact | Waitlist email now crosses to the existing contact endpoint (untrusted user input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-T | Tampering/Injection | Waitlist email into /api/contact | mitigate | Reuse the existing /api/contact validation + reCAPTCHA + mailer plumbing (no new path); no raw email interpolation into HTML on the client |
| T-30-04-I | Information Disclosure | Kiosk cart leak between customers | mitigate | Idle reset now clears both carts + milled state |
| T-30-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `npm test` (incl. new regression tests) green; `npm run lint` clean.
- No Google Form placeholder remains; waitlist POSTs to /api/contact.
- Attract reset clears both carts + milled state.
- Bundles rebuilt and committed.
</verification>

<success_criteria>
Items #2 (per D-02) and #4 complete; regression tests pass; verified on staging incl. real waitlist email delivery.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-04-SUMMARY.md` when done
</output>
