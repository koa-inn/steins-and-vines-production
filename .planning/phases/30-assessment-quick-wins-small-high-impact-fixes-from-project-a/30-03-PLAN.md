---
phase: 30-assessment-quick-wins-small-high-impact-fixes-from-project-a
plan: 03
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - content/home.json
  - "404.html"
  - css/labels.css
  - css/styles.css
  - "index.html"
  - about.html
  - content/about.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Homepage hero shows the strong subtitle, not the weak `Right Here In Squamish.` tagline"
    - "404.html on a nested URL (e.g. /products/anything-bad) loads its CSS and JS correctly"
    - "Muted/placeholder text meets WCAG 4.5:1 contrast (no hardcoded #777, no .hero p opacity dimming)"
    - "The About page no longer renders an empty story paragraph"
  artifacts:
    - path: "content/home.json"
      provides: "hero-subtitle resolved (removed or updated to strong copy)"
    - path: "404.html"
      provides: "root-absolute asset paths using styles.min.css and main.min.js"
      contains: "/css/styles.min.css"
    - path: "css/labels.css"
      provides: "var(--color-muted) replacing hardcoded #777"
      contains: "var(--color-muted)"
  key_links:
    - from: "content/home.json"
      to: "index.html hero"
      via: "13-init.js content loader overwrites HTML copy at runtime"
      pattern: "hero-subtitle"
---

<objective>
User-facing presentation bug fixes that need NO JS module rebuild (items #1, #3, #5, #6): hero subtitle regression, 404.html nested-URL breakage, contrast (H14 remainder), and the empty About story paragraph. These touch content JSON, CSS, and static HTML only.

Purpose: Fix visible live bugs in copy, error-page rendering, and accessibility contrast.
Output: Corrected content/home.json, 404.html, css/labels.css, css/styles.css, index.html critical CSS, about.html/content/about.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/QUICK-WINS.md
@.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Content loader: js/modules/13-init.js (lines ~227-233) reads content/*.json and overwrites
     elements with matching data-content attributes at runtime. So the JSON value WINS over inline HTML. -->
<!-- index.html:169 has strong hero HTML; content/home.json:10 hero-subtitle overwrites it with the weak tagline. -->
<!-- about.html:177 <p data-content="story-text"></p> ; content/about.json:4 "story-text": "" -> renders empty. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hero subtitle (#1) and empty story paragraph (#6)</name>
  <read_first>
    - content/home.json (line 10 `hero-subtitle`)
    - index.html (line 169 hero HTML)
    - js/modules/13-init.js (lines 227-233 content-loader overwrite logic)
    - about.html (line 177 empty `<p data-content="story-text">`)
    - content/about.json (line 4 `story-text: ""`, line 5 `story-text-2`)
  </read_first>
  <action>
    Item #1: the content loader (13-init.js:227-233) overwrites index.html:169's strong hero copy with the weak `content/home.json:10` `hero-subtitle` value (`"Your brew.  Your vintage.  Right Here In Squamish."`). Resolve so the strong copy wins: either remove the `hero-subtitle` key from content/home.json (so the loader leaves the HTML copy intact) OR set `hero-subtitle` to the strong copy matching index.html:169. Verify which path the loader takes when the key is absent (read 13-init.js:227-233) and choose the one that yields the strong subtitle at runtime. Item #6: at about.html:177 the `<p data-content="story-text">` renders empty because content/about.json:4 `story-text` is `""`. Remove the empty `<p data-content="story-text">` element from about.html (the meaningful copy lives in `story-text-2`); if removing the element, also remove the now-orphaned `story-text` key from content/about.json. Do NOT alter `story-text-2`. No JS source changed, so no `npm run build` needed.
  </action>
  <verify>
    <automated>! grep -q "Right Here In Squamish" content/home.json &amp;&amp; ! grep -q 'data-content="story-text"[^-]' about.html &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - content/home.json no longer carries the weak `Right Here In Squamish.` subtitle (key removed or replaced with strong copy).
    - At runtime the homepage hero shows the strong subtitle (verified in human-check on staging).
    - about.html has no `<p data-content="story-text">` empty element (only `story-text-2` remains).
    - content/about.json `story-text-2` is unchanged.
  </acceptance_criteria>
  <done>Hero subtitle resolved to strong copy; empty story paragraph removed.</done>
</task>

<task type="auto">
  <name>Task 2: Fix 404.html nested-URL paths (#3) and contrast (#5)</name>
  <read_first>
    - 404.html (asset paths: css/styles.css line 20, js/main.js line 94, nav links 27/32/57)
    - css/labels.css (lines 141, 157, 225 `#777`)
    - css/styles.css (lines 737-744 `.hero p` with `opacity:.9`; line ~1435 waitlist placeholder `rgba(229,222,193,0.6)`)
    - index.html (line 59 inline critical CSS `.hero p{...opacity:.9}`)
  </read_first>
  <action>
    Item #3: 404.html uses relative asset/nav paths that break on nested URLs like `/products/anything-bad`. Convert to ROOT-ABSOLUTE paths (leading `/`): change `css/styles.css` to `/css/styles.min.css`, `js/main.js` to `/js/main.min.js` (switch to the minified production bundles per item #3), and the nav/CTA links at 404.html:27/32/57 to root-absolute (`/index.html`, `/products/ferment-in-store.html`, `/about.html`, `/contact.html`, etc.). Note: Plan 30-01 already removed the sw.js registration from 404.html — do not re-add it. Item #5 contrast: replace `#777` with `var(--color-muted)` at css/labels.css:141, :157, :225. Remove `opacity:.9` from the `.hero p` rule in css/styles.css:737-744 AND from the duplicated inline critical CSS at index.html:59 (`.hero p{...opacity:.9}`). Change the waitlist email placeholder color `rgba(229,222,193,0.6)` at css/styles.css ~:1435 (`.beer-waitlist-form input[type="email"]::placeholder`) to a value meeting ≥4.5:1 contrast against its background (raise alpha / use a darker token). Run `npm run build` ONLY if a *.min.css regeneration is needed for the CSS edits to take effect on the built site (styles edits require `npm run minify:css` via build) — run `npm run build` and commit regenerated min CSS.
  </action>
  <verify>
    <automated>grep -q "/css/styles.min.css" 404.html &amp;&amp; grep -q "/js/main.min.js" 404.html &amp;&amp; [ "$(grep -v '^[[:space:]]*/\*' css/labels.css | grep -c '#777')" -eq 0 ] &amp;&amp; ! grep -q "opacity:.9" index.html &amp;&amp; echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 404.html references `/css/styles.min.css` and `/js/main.min.js` (root-absolute, minified) and root-absolute nav/CTA links.
    - css/labels.css has zero `#777` occurrences; uses `var(--color-muted)` at the three former locations.
    - `.hero p` rule in css/styles.css and the inline critical CSS in index.html no longer contain `opacity:.9`.
    - Waitlist placeholder color meets ≥4.5:1 contrast (value changed from `rgba(229,222,193,0.6)`).
    - `npm run build` succeeds and regenerated `*.min.css` committed (if CSS changes require it).
    - `npm test` passes.
  </acceptance_criteria>
  <done>404.html works on nested URLs with minified assets; contrast fixes applied and built.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Content/CSS/404 presentation fixes (#1, #3, #5, #6) deployed to staging.</what-built>
  <how-to-verify>
    1. Run `npm test` and `npm run lint` locally (green); `npm run build` ran for CSS.
    2. Confirm CNAME reads `staging.steinsandvines.ca`; `git push origin main`.
    3. On staging: homepage hero shows the strong subtitle (no `Right Here In Squamish.`).
    4. Visit `staging.steinsandvines.ca/products/anything-bad` — the 404 page renders with full styling and working nav (CSS + JS loaded).
    5. About page shows no empty gap where the first story paragraph was.
    6. Visually confirm muted label text and the beer-waitlist email placeholder are legible (improved contrast).
    7. Approve to promote to production.
  </how-to-verify>
  <resume-signal>Type "approved" once staging verified, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Presentation-only changes (content JSON, CSS, static HTML); no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-00 | n/a | content/CSS/404/about edits | accept | No new attack surface; presentation-only |
| T-30-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
- Hero subtitle strong; About empty paragraph gone.
- 404.html loads on nested URLs with minified root-absolute assets.
- No `#777`, no `.hero p` opacity dimming; placeholder contrast raised.
- `npm test` + `npm run lint` green; build regenerated CSS.
</verification>

<success_criteria>
Items #1, #3, #5, #6 complete; verified on staging.
</success_criteria>

<output>
Create `.planning/phases/30-assessment-quick-wins-small-high-impact-fixes-from-project-a/30-03-SUMMARY.md` when done
</output>
