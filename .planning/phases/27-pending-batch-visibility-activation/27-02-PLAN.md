---
phase: 27-pending-batch-visibility-activation
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - js/admin.js
autonomous: true
requirements: [BATCH-02]

must_haves:
  truths:
    - "A pending batch row shows an inline Activate action that flips it to Primary in one click from the list (D-05)"
    - "The batch detail modal shows the same Activate action for pending batches (D-05)"
    - "One-click Activate shows a showConfirm() dialog warning that no schedule will be attached before flipping (D-06)"
    - "On confirm the batch transitions to Primary via update_batch with updates.status='primary', start date = today (D-07)"
    - "After activation the list and detail views reflect Primary status + start date with no page reload (D-11)"
  artifacts:
    - path: "js/admin.js"
      provides: "inline + modal one-click activate buttons with confirm + update_batch flip"
      contains: "batch-activate"
  key_links:
    - from: "pending row Activate button"
      to: "update_batch action"
      via: "adminApiPost update_batch updates.status primary with expectedVersion"
      pattern: "update_batch"
    - from: "activation success"
      to: "live refresh"
      via: "loadBatchesData / refreshUpcomingCache / loadBatchDashboardSummary / openBatchDetail"
      pattern: "loadBatchesData"
---

<objective>
Add a genuine one-click "Activate" action for pending batches (BATCH-02) — both inline on the pending row in the list and inside the batch detail modal. A confirmation dialog warns that no schedule will be attached, then the batch flips to Primary with start date = today. Views refresh live.

Purpose: The most common case is "this batch is starting today, no special schedule needed" — staff should activate it from the list without opening a form. The empty-schedule footgun (pending batches have no schedule/tasks) is surfaced in the confirm copy.
Output: Pending rows and the detail modal gain an Activate button that promotes to Primary in one confirmed click and refreshes the UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-pending-batch-visibility-activation/27-CONTEXT.md
@CLAUDE.md
@.planning/phases/27-pending-batch-visibility-activation/27-01-PLAN.md

<interfaces>
<!-- Extracted from the codebase. Use directly; do NOT re-explore. -->

js/admin.js — renderBatchList row HTML (admin.js:5662–5680). The action cell today is:
  td with a button class "btn-secondary admin-btn-sm batch-qr-btn" data-batch-id, label "QR".
  Row click opens detail (5685–5690); the QR handler calls e.stopPropagation() (5694–5695) so the
  detail modal does not open. New inline buttons MUST also stopPropagation.

js/admin.js — showConfirm(message, onConfirm, onCancel) (admin.js:138). Overlay with Cancel/Confirm;
  calls onConfirm on Confirm. Used by Regenerate token / Delete.

js/admin.js — status mutation pattern (admin.js:5914–5926, the detail-modal Change Status handler):
  adminApiPost('update_batch', { batch_id, expectedVersion: batchVersion, updates: { status: newStatus } })
    .then -> showToast; openBatchDetail(batchId); loadBatchesData(); refreshUpcomingCache();
  This is the exact reuse target. update_batch already stamps fermentation_started_at = now on the
  pending->active transition (adminApi.gs:2202) — correct for the one-click "today" case (D-07).

js/admin.js — detail-modal actions block (admin.js:5825–5837, the .batch-detail-actions div) and its
  binding region (5841 onward). In that scope: batchId, batchVersion (= b.last_updated), batchToken,
  and the full batch object b (b.status) are available.

js/admin.js — refresh helpers: loadBatchesData() (5580), refreshUpcomingCache() (5531),
  loadBatchDashboardSummary() (5605), openBatchDetail(id) (5740).

js/admin.js — BATCH_STATUSES.pending added in 27-01. Gate buttons with
  String(b.status).toLowerCase() === 'pending'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inline Activate button on pending rows with confirm + flip</name>
  <read_first>
    - js/admin.js:5662–5708 (renderBatchList row HTML, action cell, row-click + QR handlers)
    - js/admin.js:138–180 (showConfirm signature and behaviour)
    - js/admin.js:5914–5926 (Change Status mutation + refresh pattern to mirror)
  </read_first>
  <files>js/admin.js</files>
  <action>In renderBatchList (admin.js:5662–5680), for rows where String(b.status).toLowerCase() === 'pending', render an inline Activate button in the action cell — class "btn admin-btn-sm batch-activate-btn", with data-batch-id and a data-version attribute carrying b.last_updated (required for optimistic locking). Keep the existing QR button on the row. Non-pending rows are unchanged. After the tbody.innerHTML assignment, bind a click handler on .batch-activate-btn that: (1) calls e.stopPropagation() so the row-click detail open does not fire (mirror the QR handler at 5694–5695); (2) calls showConfirm with copy that warns no schedule is attached, e.g. "Activate this batch now? It has no fermentation schedule, so no tasks will be created and the start date is set to today. Use 'Schedule & activate' if you need a schedule." (D-06); (3) on confirm, disables the button, then calls adminApiPost('update_batch', { batch_id: bid, expectedVersion: ver, updates: { status: 'primary' } }) (D-07, exact status string 'primary'); (4) on success showToast success and run live refresh loadBatchesData(), refreshUpcomingCache(), loadBatchDashboardSummary() (D-11); (5) on error (incl. version_conflict) showToast the error message and re-enable the button.</action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -c "batch-activate-btn" js/admin.js && grep -ci "no fermentation schedule\|no tasks will be created" js/admin.js && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - renderBatchList emits a .batch-activate-btn only for pending rows, carrying data-batch-id and data-version
    - The handler calls e.stopPropagation() before showConfirm
    - The confirm copy explicitly warns no schedule/tasks are attached (D-06)
    - The mutation uses updates.status === 'primary' and passes expectedVersion (D-07)
    - Success path calls loadBatchesData, refreshUpcomingCache, loadBatchDashboardSummary (D-11)
    - npm run lint passes
  </acceptance_criteria>
  <done>Staff can activate a pending batch in one confirmed click directly from the list; the list and dashboard refresh without reload.</done>
</task>

<task type="auto">
  <name>Task 2: Activate button in the batch detail modal for pending batches</name>
  <read_first>
    - js/admin.js:5825–5841 (.batch-detail-actions block — where buttons are emitted)
    - js/admin.js:5841–5927 (binding region; batchId/batchVersion/b in scope; Change Status handler to mirror)
    - The Task 1 confirm+flip logic in this same plan (reuse identical copy and refresh, plus re-openBatchDetail)
  </read_first>
  <files>js/admin.js</files>
  <action>In renderBatchDetailModal: when String(b.status).toLowerCase() === 'pending', add an Activate button (id "batch-activate-detail", class "btn admin-btn-sm") into the .batch-detail-actions block (admin.js:5826–5837), placed before the existing secondary actions. In the binding region (after openModal, where batchId and batchVersion are in scope), wire its click to the SAME one-click flow as Task 1: showConfirm with the identical no-schedule warning copy (D-06), then adminApiPost('update_batch', { batch_id: batchId, expectedVersion: batchVersion, updates: { status: 'primary' } }) (D-07); on success showToast, then live refresh — re-openBatchDetail(batchId) (so the modal reflects Primary + start date), loadBatchesData(), refreshUpcomingCache(), loadBatchDashboardSummary() (D-11). Only render/bind this for pending batches (the existing Change Status dropdown already covers non-pending status changes). Factor the confirm+flip into a small shared helper if it reduces duplication with Task 1, but do not change the Task 1 inline behaviour.</action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -c "batch-activate-detail" js/admin.js && grep -c "openBatchDetail" js/admin.js && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The detail modal renders #batch-activate-detail only when b.status is pending
    - Its handler reuses the same confirm copy + update_batch primary flip + expectedVersion
    - Success path re-calls openBatchDetail(batchId) plus loadBatchesData/refreshUpcomingCache/loadBatchDashboardSummary (D-11)
    - npm run lint passes
  </acceptance_criteria>
  <done>The detail modal offers the same one-click Activate for pending batches and refreshes the modal + list on success.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild frontend bundle</name>
  <read_first>
    - CLAUDE.md (Build Artifacts rules 8/9 — run npm run build after JS module changes)
  </read_first>
  <files>js/main.js, js/main.min.js, js/admin.min.js</files>
  <action>Run `npm run build` from the project root to regenerate the minified artifacts after the js/admin.js edits. Do not hand-edit build artifacts.</action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build 2>&1 | tail -5 && grep -c "batch-activate-btn\|batch-activate-detail" js/admin.min.js</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits 0
    - The built admin artifact contains the new activate button class/id strings
  </acceptance_criteria>
  <done>Built artifacts regenerated with the one-click activation changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser → adminApi.gs (token-auth) | Authenticated staff issue the status-flip mutation; existing Apps Script token auth + allowedFields whitelist enforce the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Elevation of Privilege | New Activate action triggering update_batch status flip | accept | Status mutation reuses the existing update_batch action already exposed to authenticated admins via the Change Status dropdown; no new privilege or endpoint is introduced — same admin trust boundary |
| T-27-04 | Tampering | Concurrent edit between list load and activate (stale version) | mitigate | Activate passes expectedVersion (b.last_updated) so the backend optimistic-lock check (adminApi.gs:2090–2094) rejects stale writes with version_conflict; handler surfaces the conflict via showToast |
| T-27-05 | Tampering | data-version attribute injected into row HTML | mitigate | b.last_updated is a server-generated ISO timestamp; render via the existing escaping path and treat as opaque — the backend re-validates it against the sheet, so a tampered value only yields a version_conflict, never an unguarded write |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — existing build toolchain only |
</threat_model>

<verification>
- Pending row Activate → confirm (with no-schedule warning) → row flips to Primary, disappears from pending filter, start date = today; list/dashboard update without reload.
- Detail-modal Activate for a pending batch behaves identically and refreshes the modal.
- Stale version triggers a version_conflict toast, not a silent overwrite.
- npm run lint clean; npm run build succeeds.
</verification>

<success_criteria>
ROADMAP success criterion #2 satisfied: a pending batch row/detail shows an Activate action that, in one click (plus a safety confirm), flips the batch to Primary with the fermentation start date set to today. Criterion #4 (live refresh) satisfied for this path.
</success_criteria>

<output>
Create `.planning/phases/27-pending-batch-visibility-activation/27-02-SUMMARY.md` when done.
</output>
