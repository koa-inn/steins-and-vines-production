---
phase: 76-brewpad-session-expiry-hardening
plan: 03
type: execute
wave: 3
depends_on: [76-02]
files_modified:
  - js/brewpad.js
  - js/brewpad.min.js
  - tests/frontend/brewpad-session-auth.test.js
autonomous: true
requirements: [STAFF-AUTH]

must_haves:
  truths:
    - "A Google-token / Apps-Script 401 (or an 'unauthorized' response body) does NOT remove sv_session_token from localStorage"
    - "adminApiGet/adminApiPost call the middleware proxy (mwUrl() + '/api/batch/admin-proxy') with no Google token in the body; the x-session-token header is attached by the existing fetch-wrapper"
    - "A full re-login (wipe sv_session + login modal) fires ONLY on a real middleware HTTP 401 — a single global interceptor keyed on res.status, never on an upstream body substring"
    - "The dual-token machinery (_tokenRefreshTimer, _silentRefreshTimer, handleUnauthorized-on-Apps-Script-401, isUnauthorizedError) is deleted, not hardened (D-02)"
    - "Explicit staff sign-out (bpSignOut) and multi-tab sign-out propagation still work; the login-time Google exchange still mints sv_session"
  artifacts:
    - path: "js/brewpad.js"
      provides: "Repointed adminApiGet/adminApiPost + global middleware-401 interceptor + dual-token machinery removed"
      contains: "/api/batch/admin-proxy"
    - path: "js/brewpad.min.js"
      provides: "Rebuilt minified artifact (terser) reflecting the source change"
    - path: "tests/frontend/brewpad-session-auth.test.js"
      provides: "Regression coverage: Apps-Script 401 does not clear sv_session; real middleware 401 does"
  key_links:
    - from: "js/brewpad.js adminApiGet/adminApiPost"
      to: "middleware /api/batch/admin-proxy"
      via: "fetch(mwUrl() + '/api/batch/admin-proxy')"
      pattern: "/api/batch/admin-proxy"
    - from: "js/brewpad.js fetch-wrapper IIFE"
      to: "clearSession()/login modal"
      via: "exported _handleMiddlewareResponse (res.status===401) called from the IIFE response path"
      pattern: "_handleMiddlewareResponse"
---

<objective>
Complete the single-credential migration on the frontend (D-01): repoint `adminApiGet`/`adminApiPost` in `js/brewpad.js` from the direct Apps-Script call (`SHEETS_CONFIG.ADMIN_API_URL` + Google `token` body field) to the new middleware proxy (`mwUrl() + '/api/batch/admin-proxy'`), keeping the ~50 call-site signatures unchanged. Add ONE global "middleware 401 → logout" interceptor (extending the existing fetch-wrapper IIFE) as the SOLE re-login trigger, keyed on the real middleware `res.status === 401` (D-03) — never an upstream body substring. Then DELETE the dual-token machinery that causes the bug (D-02): `_tokenRefreshTimer`, `_silentRefreshTimer`, `handleUnauthorized()`-on-Apps-Script-401, and `isUnauthorizedError`. Keep only the login-time Google exchange, explicit `bpSignOut`, and multi-tab sign-out (re-routed to the new handler). Regression tests go RED first (CLAUDE.md #3), then GREEN.

Purpose: This is the payload that eliminates the "Session expired while sv_session is valid" bug — after this, no Google/Apps-Script failure can clear `sv_session`; only a real middleware rejection can.
Output: A migrated, slimmed `js/brewpad.js` (+ rebuilt `brewpad.min.js`), new regression tests, and both suites + both lints green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-CONTEXT.md
@.planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-RESEARCH.md
@.planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-PATTERNS.md

<interfaces>
<!-- Verified live in js/brewpad.js (2026-08-27). Use directly. -->
- Fetch-wrapper IIFE (attaches x-session-token on MIDDLEWARE_URL requests): js/brewpad.js:9-38. Guarded by window.__svSessionFetchWrapped. This is the extension point for the global 401 response interceptor (no response-side 401 check exists anywhere in the file yet — new logic to design, not copy).
- adminApiGet: js/brewpad.js:1443-1469 (currently POSTs {action, token: accessToken} to SHEETS_CONFIG.ADMIN_API_URL; calls isUnauthorizedError→handleUnauthorized at :1464).
- adminApiPost: js/brewpad.js:1471-1488 (same shape; unauthorized branch at :1483).
- mwUrl(): js/brewpad.js:1490-1491 → SHEETS_CONFIG.MIDDLEWARE_URL.
- Migration analog (already-shipped, real-status error handling): postBottlingInvite js/brewpad.js:1736-1748; bpSaveAsNewRecipe js/brewpad.js:1704-1731.
- Deletion inventory (PATTERNS.md:258-267): _tokenRefreshTimer decl :903 (set :1312, cleared :1311/:1365/:1409); _silentRefreshTimer decl :905 (set :1110, refs :1111/:1123-1124/:1215/:9261); handleUnauthorized() :1406-1420 (callers :1086 GIS error_callback, :1210 tryRefreshToken catch, :1221 onTokenResponse refresh-fail, :1344 storage listener, :1464/:1483 adminApiGet/Post); isUnauthorizedError() :1438-1441 (callers :1464/:1483 only).
- KEEP: clearSession() :1023 — its callers at :1371 (bpSignOut, explicit sign-out) MUST survive; the multi-tab storage listener :1341-1346 should still sign this tab out (re-route to the new real-401/sign-out handler, do not orphan it).
- Client 7-day cliff: isSessionExpired :528, used at :1014 (loadSession, anchored to original login_at). Test seam: _adminApiGetForTest :~9260s, isSessionExpired exported :9355.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write RED regression tests (Apps-Script 401 must NOT clear sv_session; real middleware 401 must)</name>
  <files>tests/frontend/brewpad-session-auth.test.js</files>
  <read_first>
    - tests/frontend/brewpad-session-auth.test.js — the existing D-46-09 harness: the `document`/`window`/`navigator`/`google` stubs + `SHEETS_CONFIG.MIDDLEWARE_URL` setup (lines ~14-35), the `global.fetch = jest.fn()` + `mockResolvedValue` + assert-on-`global.fetch.mock.calls[0]` idiom (~57-70), and the existing "no x-api-key header" assertion (~151-160) to mirror for the new transport assertion. Do NOT modify its existing 5 describe blocks (CLAUDE.md #10) — ADD new tests.
    - js/brewpad.js:1443-1488 — current adminApiGet/adminApiPost (the code these tests pin the NEW behavior against).
    - .planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-PATTERNS.md:289-296 — the three required regression assertions.
  </read_first>
  <behavior>
    - Test 1 (the mandated reproduction): given a valid sv_session_token in localStorage, when adminApiGet/adminApiPost receives an Apps-Script/Google-shaped failure — a response whose body contains "unauthorized"/"not authorized" but which is NOT an HTTP 401 (or a rejected Google-token refresh) — sv_session_token in localStorage is NOT removed. (RED against today's code, which calls isUnauthorizedError→handleUnauthorized→clearSession.)
    - Test 2 (transport): adminApiGet('get_batches', ...) and adminApiPost('update_batch', ...) issue their fetch to mwUrl() + '/api/batch/admin-proxy'; the request body does NOT contain a `token` field; and they do NOT target SHEETS_CONFIG.ADMIN_API_URL. (RED — today they hit ADMIN_API_URL with a token field.)
    - Test 3 (real 401 → logout): a fetch to a MIDDLEWARE_URL path returning HTTP 401 triggers the logout path (sv_session_token removed / login modal shown) exactly once. (RED — no global interceptor exists yet.)
  </behavior>
  <action>
    Extend `tests/frontend/brewpad-session-auth.test.js` with a NEW describe block (`Phase 76 — single-credential migration`) containing the three behaviors above, reusing the file's existing stub/harness setup and the `_adminApiGetForTest`-style test seam already exported for adminApiGet (add a parallel seam for adminApiPost and, if needed, for the logout path, mirroring the existing export style at brewpad.js:9244-9267 — but only in Task 2 when you touch source; in THIS task, write the tests against the seams/exports you will add, and expect them RED). Run the file and record the RED failures. Add nothing to the source in this task.
  </action>
  <verify>
    <automated>npx jest tests/frontend/brewpad-session-auth.test.js 2>&1 | grep -E "✕|fail|Expected|is not a function" | head</automated>
  </verify>
  <acceptance_criteria>
    - `tests/frontend/brewpad-session-auth.test.js` gains a new describe block with the 3 tests; existing describe blocks are byte-for-byte unchanged.
    - The new tests fail RED against current source (Apps-Script-401 currently DOES clear the token; transport currently hits ADMIN_API_URL with a token field; no 401 interceptor exists) — executor records the RED run.
  </acceptance_criteria>
  <done>Three RED regression tests encode D-02/D-03: Apps-Script 401 must not clear sv_session, transport must be the proxy without a token field, and a real middleware 401 must trigger logout.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint adminApiGet/adminApiPost to the proxy + add the global middleware-401 interceptor + drop the client 7-day cliff</name>
  <files>js/brewpad.js</files>
  <read_first>
    - js/brewpad.js:1443-1491 — adminApiGet/adminApiPost/mwUrl (the internals to rewrite; keep signatures).
    - js/brewpad.js:1704-1748 — bpSaveAsNewRecipe / postBottlingInvite: the already-shipped analog that checks real HTTP status (`r.ok`, `r.status`) not a body substring — copy this discipline.
    - js/brewpad.js:9-38 — the fetch-wrapper IIFE to EXTEND with the response-side 401 interceptor (it already scopes to MIDDLEWARE_URL-prefixed requests on the way out).
    - js/brewpad.js:1341-1346 — the multi-tab storage listener that must keep signing this tab out; re-route it to the new handler.
    - js/brewpad.js:528, 1005-1019 — isSessionExpired + loadSession's client login_at 7-day check (drop-and-trust-server-401 per D-03/RESEARCH.md lean).
    - .planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-RESEARCH.md:404-414 — Open Question #2 (global interceptor is the recommended single-source-of-truth shape).
  </read_first>
  <action>
    Rewrite the BODIES of `adminApiGet(action, params)` and `adminApiPost(action, payload)` in `js/brewpad.js` to POST to `mwUrl() + '/api/batch/admin-proxy'` with `Content-Type: application/json` and a JSON body of `{action: action, ...params-or-payload}` — DROP the `token: accessToken` field entirely (the fetch-wrapper IIFE at :9-38 attaches `x-session-token` automatically because the URL is MIDDLEWARE_URL-prefixed). Keep both function names/signatures/return-contract (resolve to the parsed data on `data.ok`, reject on failure) so the ~50 call sites are untouched. Handle errors by REAL status (mirror postBottlingInvite: check `r.ok`/`r.status`, then body `data.ok`), and REMOVE the per-call `isUnauthorizedError(...)→handleUnauthorized()` branches (:1464, :1483). Then add the single response-side 401 interceptor as a STANDALONE, NAMED, EXPORTED function — do NOT bury the logic inside the fetch-wrapper IIFE body. IMPORTANT: the IIFE at :9-38 begins `if (typeof module !== 'undefined' || ...) return;`, so under Jest/CommonJS (`require('../../js/brewpad')`) the IIFE never runs — any interceptor logic placed inside it is dead code under test and Task 1 Test 3 could never reach it. Instead define a module-scope function e.g. `function _handleMiddlewareResponse(url, response) { ... }` that: for a MIDDLEWARE_URL-prefixed `url`, when `response.status === 401`, invokes the SINGLE logout path (wipe `sv_session_token` + show the login modal) exactly once — this is the ONLY place a full re-login is triggered (D-03); keyed strictly on the middleware Response status, never on an upstream/Apps-Script body substring. Call `_handleMiddlewareResponse(url, resolvedResponse)` from inside the wrapped `fetch` in production (in the IIFE's response `.then`), AND export it via the existing `module.exports` test-seam pattern (mirror :9244-9267) so Task 1 Test 3 invokes it directly. Re-route the multi-tab `storage` listener (:1341-1346) to this same "session ended" logout path so cross-tab sign-out still propagates. Remove `loadSession`'s client-side 7-day `isSessionExpired(data.login_at, ...)` gate (:1014) and trust the server 401 as the single source of truth (sliding-expiry now lives server-side via touchSession, Plan 76-02) — keep the `isSessionExpired` function/export itself if other code references it, but stop using it to nuke a still-valid session. Add/extend the module.exports test seams (mirror :9244-9267) needed by Task 1 (adminApiPost seam, logout-path seam). ES5 ONLY — `var`, `function () {}`, string concatenation; NO arrow functions/let/const/template literals/classes. Do NOT touch `js/brewpad.min.js` by hand, `js/admin-config.js`, `ADMIN_API_URL`'s definition, or any admin/kiosk path (Pitfall 4). Run Task-1 tests to GREEN. Do NOT run the full build/lint gate here — that is Task 3.
  </action>
  <verify>
    <automated>npx jest tests/frontend/brewpad-session-auth.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/batch/admin-proxy" js/brewpad.js` >= 1 and adminApiGet/adminApiPost no longer reference `SHEETS_CONFIG.ADMIN_API_URL` (`grep -n "ADMIN_API_URL" js/brewpad.js` shows no hit inside the adminApiGet/adminApiPost bodies).
    - The rewritten helpers send no `token` body field (Task 1 Test 2 GREEN).
    - A standalone exported `_handleMiddlewareResponse` (or equivalently-named) function contains the `status === 401` → logout logic and is exported via `module.exports`: `grep -n "_handleMiddlewareResponse" js/brewpad.js` returns both its definition and its export, and it is called from inside the fetch-wrapper IIFE's response path. Task 1 Test 3 drives that exported function directly (NOT logic buried inside the Jest-skipped IIFE body) and passes GREEN.
    - Apps-Script-401 no longer clears sv_session (Task 1 Test 1 GREEN).
    - `npx jest tests/frontend/brewpad-session-auth.test.js` exits 0.
  </acceptance_criteria>
  <done>adminApiGet/adminApiPost hit the proxy with no Google token; a single global res.status===401 interceptor is the sole logout trigger; the client 7-day cliff is dropped; all Task-1 tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the dual-token machinery (D-02) + rebuild + full both-suite + both-lint gate</name>
  <files>js/brewpad.js, js/brewpad.min.js</files>
  <read_first>
    - .planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-PATTERNS.md:258-267 — the verified deletion inventory (every symbol + every call site).
    - .planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-RESEARCH.md:320-325 — Pitfall 3 (which callers must survive: bpSignOut clearSession, multi-tab storage, login-time Google exchange).
    - js/brewpad.js:903-905, 1081-1131, 1197-1224, 1312, 1364-1371, 1406-1441 — the timers, GIS error_callback, tryRefreshToken, onTokenResponse, bpSignOut, handleUnauthorized, isUnauthorizedError regions.
    - package.json:11,15,18 — the brewpad build path (`stamp:brewpad` + `terser js/brewpad.js -o js/brewpad.min.js` inside `minify:js`, both run by `npm run build`).
  </read_first>
  <action>
    Delete the now-orphaned dual-token machinery per the PATTERNS.md inventory: remove `_tokenRefreshTimer` and `_silentRefreshTimer` (declarations :903/:905 and all set/clear sites) and their driving code (the ~1hr refresh interval at :1312 and the `doSilentRefreshOnLoad` timer at :1098-1131 to the extent it exists solely to keep a runtime Google token alive), `handleUnauthorized()` (:1406-1420) and `isUnauthorizedError()` (:1438-1441). Remove or re-point every caller so no dangling reference remains: the GIS `error_callback` (:1081/1086), `tryRefreshToken` catch (:1210), and `onTokenResponse` refresh-failure branch (:1221) no longer call the deleted `handleUnauthorized` — for the login-time Google exchange, a failed mint just means "show the sign-in button", NOT "clear a still-valid session" (D-03). KEEP `clearSession()` itself and its legitimate caller `bpSignOut` (:1371, explicit staff sign-out) and the `_resetAuthStateForTest` seam (adjust it if it references a deleted timer). Decide and apply the `doSilentRefreshOnLoad` simplification per RESEARCH.md Open Question #1: since runtime API traffic no longer needs a live Google token, simplify `initGoogleAuth`/`checkAuthorization` to "have sv_session_token? verify once via /auth/google; fails? show sign-in button" — remove the silent-refresh-on-load branch if it is now dead; record the choice in the SUMMARY. ES5 ONLY. Then rebuild: `npm run build` (regenerates `js/brewpad.min.js` via terser + stamps `brewpad.html`) — NEVER hand-edit the min file. Finally run the FULL gate and fix any regression: `npm run lint` (frontend, --max-warnings 0), `npm test` (frontend), `cd zoho-middleware && npm test`, `cd zoho-middleware && npm run lint`. Confirm the deletions via grep (see acceptance).
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm test && (cd zoho-middleware && npm test && npm run lint)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isUnauthorizedError" js/brewpad.js` returns 0.
    - `grep -c "handleUnauthorized" js/brewpad.js` returns 0.
    - `grep -c "_tokenRefreshTimer\|_silentRefreshTimer" js/brewpad.js` returns 0.
    - `grep -c "function bpSignOut" js/brewpad.js` returns 1 and bpSignOut still calls `clearSession()` (explicit sign-out preserved); the multi-tab storage listener still signs the tab out.
    - `js/brewpad.min.js` regenerated by `npm run build` (never hand-edited); `brewpad.html` cache `?v=` stamp updated.
    - `npm run lint` exits 0; `npm test` exits 0; `cd zoho-middleware && npm test` exits 0; `cd zoho-middleware && npm run lint` exits 0.
  </acceptance_criteria>
  <done>The dual-token machinery is gone with no dangling references, explicit + multi-tab sign-out and the login-time Google exchange survive, the min artifact is rebuilt, and all four gates (2 tests + 2 lints) are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrewPad browser session state | `sv_session_token` in localStorage is the sole credential; the decision "should this session be destroyed?" is the security-relevant control being rewritten. |
| browser → middleware | The only signal that may destroy the session is a real middleware HTTP 401. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-03-01 | Denial of Service (self-inflicted logout — the root-cause class) | logout trigger | mitigate | Single global interceptor keys on the MIDDLEWARE Response `status === 401` only; `isUnauthorizedError`'s loose upstream-body substring match is DELETED. No Google/Apps-Script failure can clear sv_session (D-03). Tested (Task 1 Tests 1 & 3). |
| T-76-03-02 | Denial of Service (un-signaled hard logout) | client 7-day login_at cliff | mitigate | Client cliff dropped; server sliding-expiry (touchSession, Plan 76-02) keeps an active session alive; the server 401 is the single source of truth. Trades a hard client cliff for a sliding server window. |
| T-76-03-03 | Spoofing | residual Google token as runtime identity | mitigate | Google token is used ONLY at login-time `/auth/google`; adminApiGet/Post send no `token` field; the deleted refresh timers remove the last runtime Google-token dependency. No client-held token authorizes any data call. |
| T-76-03-04 | Tampering | stale build artifact shipped | mitigate | `npm run build` regenerates `js/brewpad.min.js` (terser) + stamps `brewpad.html`; min file never hand-edited (CLAUDE.md #8/#9). Grep gate confirms deletions in source, build gate confirms artifact refresh. |
| T-76-03-05 | Elevation of Privilege | scope leak into admin/kiosk shared paths | mitigate | Only `js/brewpad.js` internals + BrewPad-local state change; `js/admin-config.js`/`ADMIN_API_URL`/admin.js/kiosk.js untouched (Pitfall 4). Both suites run to prove no cross-surface regression. |
| T-76-03-SC | Tampering | package installs | mitigate | No npm/pip/cargo installs — RESEARCH.md Package Legitimacy Audit: not applicable. |
</threat_model>

<verification>
- Apps-Script/Google 401 does not clear sv_session; real middleware 401 does (regression tests GREEN).
- adminApiGet/adminApiPost transport = proxy, no token body field.
- Global 401 interceptor is the sole logout trigger; multi-tab + explicit sign-out preserved.
- Dual-token machinery deleted (greps == 0); no dangling references (lint/tests pass).
- `brewpad.min.js` rebuilt; all four gates green.
</verification>

<success_criteria>
BrewPad runs on ONE credential end-to-end: Google only at login, all batch/dashboard/reading/schedule traffic via `x-session-token` through the middleware proxy, a real middleware 401 the sole re-login trigger, and the dual-token machinery deleted. The "Session expired while sv_session is valid" bug (STAFF-AUTH) is eliminated.
</success_criteria>

<output>
Create `.planning/phases/76-brewpad-session-expiry-hardening-decouple-the-durable-7-day-/76-03-SUMMARY.md` when done, recording the `doSilentRefreshOnLoad` simplification decision (Open Question #1) and the client-cliff drop.
</output>
