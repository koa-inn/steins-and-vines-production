---
phase: 33-deploy-safety-monitoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/gated-deploy.yml
  - docs/RUNBOOK.md
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "A manual workflow_dispatch on the staging repo runs the full frontend + middleware test suite and refuses to deploy if either fails"
    - "On green tests, the workflow swaps CNAME to steinsandvines.ca, force-pushes production, smoke-checks /health, tags prod-YYYYMMDD-N, appends a runbook entry, and always restores the staging CNAME"
    - "Any deploy that force-pushes production is recorded (prod-YYYYMMDD-N tag + RUNBOOK row pairing SHA and Railway deploy ID) even when the post-deploy /health smoke-check fails — the record is written before/independently of the smoke-check (DEPLOY-02)"
    - "The smoke check hard-fails on non-200 or redis:false and soft-warns on authenticated:false"
    - "docs/RUNBOOK.md documents both rollback paths (GitHub Pages revert + Railway dashboard/GraphQL) and the human setup prerequisites"
    - "Implements locked CONTEXT decisions D-01 (GitHub Actions deploy gate), D-02 (manual workflow_dispatch trigger), D-03 (gate both frontend + middleware surfaces), D-04 (post-deploy /health smoke check), D-05 (smoke-check semantics: hard on redis:false/non-200, soft on authenticated:false), D-06 (scripted prod-YYYYMMDD-N tag + runbook entry pairing SHA and Railway deploy ID), D-07 (tracked RUNBOOK doc with both rollback procedures)"
  artifacts:
    - path: ".github/workflows/gated-deploy.yml"
      provides: "Test-gated workflow_dispatch production deploy with health smoke-check, tagging, runbook append, CNAME swap+restore"
      contains: "workflow_dispatch"
    - path: "docs/RUNBOOK.md"
      provides: "Deploy history table + rollback procedures + human prerequisites"
      contains: "## Rollback"
  key_links:
    - from: ".github/workflows/gated-deploy.yml"
      to: "deploy job"
      via: "needs: [test-middleware, test-frontend]"
      pattern: "needs:.*test-middleware.*test-frontend"
    - from: ".github/workflows/gated-deploy.yml"
      to: "svmiddleware-production.up.railway.app/health"
      via: "curl retry smoke-check"
      pattern: "health"
    - from: ".github/workflows/gated-deploy.yml"
      to: "docs/RUNBOOK.md"
      via: "append deploy-history row then commit/push"
      pattern: "RUNBOOK.md"
---

<objective>
Build the blessed, test-gated production deploy path (DEPLOY-01) with automatic tagging and a rollback runbook (DEPLOY-02).

Purpose: Replace the ungated `git push production main --force` ritual with a manual-trigger GitHub Actions workflow that runs both test suites first, deploys both surfaces only on green, smoke-checks /health, and records every deploy (git SHA <-> Railway deploy ID) so a rollback can start from either end within minutes.

Output: A new `.github/workflows/gated-deploy.yml` (workflow_dispatch, lives on the staging repo, deploys to production) and a new `docs/RUNBOOK.md` it appends to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md
@.planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md
@.github/workflows/tests.yml
@.github/workflows/deploy-production.yml
@docs/DEPLOYMENT.md

<interfaces>
<!-- tests.yml job structure to mirror: test-middleware (npm install + npm test + npm run lint, working-directory zoho-middleware)
     and test-frontend (npm install + npm test + npm run lint at root). Do NOT reuse the E2E job (it hits live staging). -->
<!-- /health response shape (server.js:103): {status:'ok', authenticated, redis, uptime}. Always HTTP 200.
     redis:false OR HTTP!=200 = hard fail; authenticated:false = soft warn (Zoho OAuth drops on every restart). -->
<!-- CNAME swap ritual: local CNAME is `staging.steinsandvines.ca`; prod needs `steinsandvines.ca`.
     Production deploy is `git push production main --force` to koa-inn/steins-and-vines-production via PROD_DEPLOY_TOKEN. -->
<!-- Railway deploy ID: railway deployment list --service sv_middleware --json --limit 1 | jq -r '.[0].id' (needs RAILWAY_TOKEN). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gated-deploy.yml (test gate + CNAME swap + force-push + smoke-check)</name>
  <files>.github/workflows/gated-deploy.yml</files>
  <read_first>
    - .github/workflows/tests.yml (mirror test-middleware + test-frontend jobs exactly; do NOT include the E2E job)
    - .github/workflows/deploy-production.yml (the production Pages workflow that the force-push triggers; do not modify it)
    - .planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md (Target 2 workflow shape + Railway deploy ID + rollback; Target 4 smoke-check retry loop; Target 5 CNAME swap ritual + PROD_DEPLOY_TOKEN; Pitfalls 2/3/4/5)
    - .planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md (gated-deploy.yml construction notes + reused patterns: needs gate, curl status check, [skip ci], if: always() restore)
    - CLAUDE.md (Deployment section — two-repo model, staging-first, CNAME tracked despite .gitignore)
  </read_first>
  <action>
    Create .github/workflows/gated-deploy.yml on the staging repo. Trigger: workflow_dispatch ONLY (D-02) with an optional `reason` string input. Structure:
    (1) Two parallel test jobs `test-middleware` and `test-frontend` copied from tests.yml (npm install + npm test + npm run lint; middleware uses working-directory zoho-middleware). Do NOT copy the E2E job.
    (2) A `deploy` job with `needs: [test-middleware, test-frontend]` (D-03 — either suite failing blocks the whole deploy). Steps in order:
        a. Checkout with `token: ${{ secrets.PROD_DEPLOY_TOKEN }}` and `fetch-depth: 0` (tags needed).
        b. CNAME pre-flight guard: read `cat CNAME`; if it already equals `steinsandvines.ca`, exit 1 with an explicit abort message (Pitfall 3 guard).
        c. Set CNAME: write `steinsandvines.ca` to CNAME, git config the bot identity, commit `chore: set CNAME to steinsandvines.ca for production deploy [skip ci]` (Pitfall 5 — include [skip ci]).
        d. Force-push to production: `git push https://x-access-token:${{ secrets.PROD_DEPLOY_TOKEN }}@github.com/koa-inn/steins-and-vines-production.git HEAD:main --force`. This triggers deploy-production.yml on the production repo.
        e. Railway gating note: this phase uses Approach A ("Wait for CI") — Railway auto-deploys on the watchPatterns match from the force-push and holds for CI; the workflow does NOT run `railway up`. Add a step comment documenting Approach B (disable auto-deploy + `railway up --service sv_middleware --ci`) as the fallback if Approach A proves flaky (researcher Open Question #2, RESOLVED: A primary, B documented).

        CRITICAL ORDERING (DEPLOY-02 — the deploy record MUST survive a failed smoke-check). The moment step (d) force-pushes, production is updated; therefore the tag + runbook row must be written BEFORE and INDEPENDENTLY of the smoke-check, so a deploy that reaches production but then fails its health check is still recorded. Order the remaining steps exactly:
        f. Capture metadata: record the short git SHA (of the commit force-pushed in step d) and fetch the Railway deploy ID via `railway deployment list --service sv_middleware --json --limit 1 | jq -r '.[0].id'` with `RAILWAY_TOKEN` in env; if RAILWAY_TOKEN is absent or the call fails, set the ID to `unknown` and continue (never fail the deploy on missing runbook-metadata token). Give this step `id: meta` and mark it `if: success() || failure()` so it runs whenever the deploy job got as far as the force-push.
        g. Tag: compute `prod-$(date -u +%Y%m%d)-N` where N is the next integer for today (count existing tags matching today's date prefix + 1); create the annotated tag (pointing at the captured SHA) and push it to BOTH remotes (origin + production via token). Mark this step `if: success() || failure()` — every deploy that reached production gets a tag even if the later smoke-check fails.
        h. Runbook append: append one row to docs/RUNBOOK.md deploy-history table: `| <UTC date> | <short git SHA> | <railway deploy id or unknown> | <workflow run URL> | <reason input> |`, then commit + push the RUNBOOK.md change to origin (message `chore: append deploy runbook entry [skip ci]`). Mark this step `if: success() || failure()` so the row is written for any deploy that reached production, including one whose smoke-check later fails.
        i. Smoke-check (runs AFTER the record is written): a grace-wait retry loop (5 iterations x 20s sleep) curling `https://svmiddleware-production.up.railway.app/health`. HARD fail (exit 1) if HTTP != 200 after all retries OR if `.redis` (parsed via `jq -r '.redis'`) is not `true` — this exit 1 marks the workflow failed and alerts the operator, but the tag + runbook row from steps g/h are already committed. SOFT warn only (echo, do not fail) if `.authenticated` is `false` — print the re-auth URL `https://svmiddleware-production.up.railway.app/auth/zoho` (D-05, Pitfall 2). Use `jq` (preinstalled on ubuntu-latest), not python3.
        j. Restore CNAME: write `staging.steinsandvines.ca` back to CNAME, commit `chore: restore CNAME to staging.steinsandvines.ca after prod promote [skip ci]`, push to origin (staging). This step MUST be `if: always()` so a failed smoke-check or Railway step never leaves staging holding the production CNAME (Pitfall 3).
        Rationale (Blocker 3 / DEPLOY-02): a force-push that succeeds but is followed by a transient `redis:false` smoke-check must NOT leave production updated with no git tag and no runbook entry. By writing the tag and runbook row with `if: success() || failure()` BEFORE the smoke-check (which may exit 1), every deploy that reached production is recorded; the failed smoke-check still fails the workflow and alerts the operator, and the CNAME restore still runs via `if: always()`.
    Secret handling: reference PROD_DEPLOY_TOKEN and RAILWAY_TOKEN only as `${{ secrets.* }}`; never echo them. The gated workflow is the BLESSED path; document inline that the ungated `git push production main --force` still exists as break-glass.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gated-deploy.yml'))" && grep -q "workflow_dispatch" .github/workflows/gated-deploy.yml && grep -Eq "needs:.*test-middleware" .github/workflows/gated-deploy.yml && grep -q "if: always()" .github/workflows/gated-deploy.yml && grep -Eq "if:[[:space:]]*success\(\)[[:space:]]*\|\|[[:space:]]*failure\(\)" .github/workflows/gated-deploy.yml && grep -q "auth/zoho" .github/workflows/gated-deploy.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - gated-deploy.yml YAML parses: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gated-deploy.yml'))"` exits 0
    - File contains `workflow_dispatch` and does NOT contain a `push:` trigger
    - The deploy job declares `needs:` including both `test-middleware` and `test-frontend`
    - Contains a CNAME pre-flight guard comparing against `steinsandvines.ca` and an `exit 1`
    - Contains a smoke-check loop curling `/health`, an `exit 1` on `redis` != true / non-200, and a soft-warn branch printing `auth/zoho`
    - Contains `railway deployment list --service sv_middleware --json --limit 1` and `jq -r '.[0].id'`
    - The tag step (producing `prod-` + UTC date) AND the RUNBOOK.md append/commit step are both marked `if: success() || failure()` and are positioned BEFORE the smoke-check step in the deploy job
    - Observable: a gated-deploy run that force-pushes production but then fails the smoke-check still produces a `prod-YYYYMMDD-N` tag on both remotes and a RUNBOOK row pairing that SHA with the Railway deploy ID (record survives a failed smoke-check)
    - The smoke-check step still exits 1 (fails the workflow + alerts) on non-200 / redis:false, but runs only after the tag + runbook row are written
    - The CNAME restore step is marked `if: always()`
    - No echo/cat of PROD_DEPLOY_TOKEN or RAILWAY_TOKEN (grep returns nothing)
    - CNAME swap commit messages include `[skip ci]`
  </acceptance_criteria>
  <done>gated-deploy.yml is a valid workflow_dispatch workflow that gates on both test suites, swaps/restores CNAME safely, force-pushes production, smoke-checks /health with correct hard/soft semantics, fetches the Railway deploy ID, tags, and appends to the runbook.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/RUNBOOK.md with deploy history + rollback + prerequisites</name>
  <files>docs/RUNBOOK.md</files>
  <read_first>
    - docs/DEPLOYMENT.md (match its doc style: ## headers, tables, fenced code blocks, numbered steps)
    - .planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md (Target 2 Railway rollback commands + GraphQL mutation; Target 5 CNAME swap + PROD_DEPLOY_TOKEN setup; Target 6 Phase 32 UAT human checklist)
    - .planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md (RUNBOOK.md required sections + append row format)
  </read_first>
  <action>
    Create docs/RUNBOOK.md with these sections:
    (1) Overview — one short table: the blessed deploy path = the gated-deploy.yml workflow_dispatch button; break-glass = manual `git push production main --force`.
    (2) Deploy History — a markdown table with header `| Date | Git SHA | Railway Deploy ID | Deploy URL | Notes |` and a separator row, followed by an HTML comment marker the workflow appends BELOW. Seed with no data rows.
    (3) ## Rollback — two subsections:
        - GitHub Pages (frontend): `git revert --no-edit HEAD` (or `HEAD~N..HEAD`) then `git push production main --force` (note --force required because the repos diverge), preserving CNAME=steinsandvines.ca on production.
        - Railway (middleware): true rollback via dashboard (Deployments tab -> 3-dot menu -> Rollback) OR the `deploymentRollback(id)` GraphQL mutation (note `canRollback: true` constraint). Document that CLI `railway deployment redeploy` only re-runs the CURRENT deploy and is NOT a rollback.
    (4) ## Smoke-check semantics — restate: hard-fail on non-200 or redis:false; authenticated:false is a soft warning (Zoho OAuth drops on every restart; re-auth at /auth/zoho).
    (5) ## Human prerequisites (one-time) — checklists for: PROD_DEPLOY_TOKEN (fine-grained PAT, koa-inn/steins-and-vines-production, Contents:write, max expiry — Pitfall 6); RAILWAY_TOKEN (project token scoped to sv-middleware + production); UptimeRobot keyword monitor on /health (`"redis":true` exists check, 5-min, email); Railway "Wait for CI" toggle (Approach A) with Approach B fallback noted; Phase 32 UAT close (NODE_ENV=production + confirm RECAPTCHA_SECRET_KEY / HELCIM_WEBHOOK_SECRET / CALCOM_WEBHOOK_SECRET / REDIS_ENCRYPTION_KEY / SENTRY_DSN present in Railway).
    (6) ## CNAME reference — the swap ritual and the warning that enforce-cname.yml is broken (403) so CNAME must be managed manually by the workflow; never push steinsandvines.ca to origin or staging.steinsandvines.ca to production.
    These human checklists are the source-of-truth that Plan 03's HUMAN-UAT mirrors.
  </action>
  <verify>
    <automated>test -f docs/RUNBOOK.md && grep -q "## Rollback" docs/RUNBOOK.md && grep -q "Date | Git SHA | Railway Deploy ID | Deploy URL | Notes" docs/RUNBOOK.md && grep -q "deploymentRollback" docs/RUNBOOK.md && grep -q "PROD_DEPLOY_TOKEN" docs/RUNBOOK.md && grep -q "auth/zoho" docs/RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/RUNBOOK.md exists
    - Contains the deploy-history table header `| Date | Git SHA | Railway Deploy ID | Deploy URL | Notes |`
    - Contains a `## Rollback` section with both a GitHub Pages revert+force-push path and a Railway `deploymentRollback` / dashboard path
    - States that `railway deployment redeploy` is NOT a true rollback
    - Contains the smoke-check semantics (hard redis/200, soft authenticated)
    - Contains the human-prerequisites checklist naming PROD_DEPLOY_TOKEN, RAILWAY_TOKEN, UptimeRobot, Railway "Wait for CI", and the Phase 32 secrets
    - Contains the CNAME swap reference and the enforce-cname.yml-broken warning
  </acceptance_criteria>
  <done>docs/RUNBOOK.md exists with deploy-history table (workflow-appendable), both rollback procedures, smoke-check semantics, human prerequisites, and CNAME reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer (workflow_dispatch) -> CI | Only a human with repo access can trigger a prod deploy |
| CI runner -> production repo | Force-push crosses the staging/production repo boundary with a privileged token |
| CI runner -> Railway API | RAILWAY_TOKEN reads deploy metadata |
| CI runner -> live /health | Untrusted network response gates deploy success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Information Disclosure | PROD_DEPLOY_TOKEN / RAILWAY_TOKEN in workflow logs | mitigate | Tokens referenced only via `${{ secrets.* }}` (Actions auto-masks), embedded inline in push URL; never echoed. Acceptance grep checks for `echo.*_TOKEN`. |
| T-33-05 | Elevation of Privilege | PROD_DEPLOY_TOKEN over-scope | mitigate | Documented in RUNBOOK as fine-grained PAT scoped to production repo Contents:write only; RAILWAY_TOKEN scoped to sv-middleware + production. |
| T-33-06 | Tampering | Deploy-gate bypass (broken code reaching prod) | mitigate | Deploy job has `needs: [test-middleware, test-frontend]`; smoke-check is a mandatory non-optional step (hard exit 1). Break-glass `git push production main --force` documented as the only bypass, retained intentionally. |
| T-33-07 | Spoofing | CNAME poisoning (staging served as steinsandvines.ca) | mitigate | Pre-flight CNAME guard aborts if already production value; restore step is `if: always()` so a mid-deploy failure never leaves staging holding the prod CNAME (Pitfall 3). |
| T-33-08 | Tampering | Smoke check skipped / fail-open | mitigate | redis:false and non-200 hard-fail with exit 1 after retries; authenticated:false is the ONLY soft condition (documented Zoho-restart behavior). |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (33-RESEARCH Package Legitimacy Audit: none). Workflow only runs existing `npm install`/`npm test` from tests.yml. |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/gated-deploy.yml'))"` exits 0
- grep confirms: workflow_dispatch (no push trigger), needs both test jobs, CNAME guard, /health smoke loop with hard/soft branches, railway deployment list, tag step, runbook append, `if: always()` restore
- docs/RUNBOOK.md exists with all required sections
- No token values echoed in the workflow
</verification>

<success_criteria>
- DEPLOY-01: a workflow_dispatch deploy that would fail frontend OR middleware tests is blocked before any push to production; a healthy deploy passes the /health smoke gate.
- DEPLOY-02: every deploy that reaches production (including one whose smoke-check later fails) produces a `prod-YYYYMMDD-N` tag and a RUNBOOK.md row pairing git SHA with Railway deploy ID; both rollback paths are documented.
</success_criteria>

<output>
Create `.planning/phases/33-deploy-safety-monitoring/33-02-SUMMARY.md` when done
</output>
