---
phase: 33-deploy-safety-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/update-snapshot.yml
  - zoho-middleware/lib/validateEnv.js
  - zoho-middleware/__tests__/validateEnv.test.js
autonomous: true
requirements: [DEPLOY-03, MONITOR-02]
must_haves:
  truths:
    - "After a production force-push deploy, steinsandvines.ca/content/zoho-snapshot.json generated_at is within 25 hours (user-observable outcome of DEPLOY-03)"
    - "The nightly snapshot job runs only on the staging repo and lands the snapshot commit on BOTH repos so a production force-push never reverts it (mechanism)"
    - "A production middleware booted without SENTRY_DSN fails closed (process.exit) instead of warning and continuing"
    - "The validateEnv test suite asserts SENTRY_DSN is enforced in prod and passes"
    - "Implements locked CONTEXT decisions D-09 (MONITOR-02 secrets verification — fail-closed code half) and D-10 (DEPLOY-03 snapshot staleness fix: repo-guarded nightly + cross-repo push)"
  artifacts:
    - path: ".github/workflows/update-snapshot.yml"
      provides: "Repo-guarded nightly snapshot with cross-repo push to production"
      contains: "github.repository == 'koa-inn/steins-and-vines-staging'"
    - path: "zoho-middleware/lib/validateEnv.js"
      provides: "SENTRY_DSN + HELCIM_API_TOKEN promoted to REQUIRED_IN_PROD (removed from OPTIONAL); full SC#5 set (live Helcim/Cal.com/REDIS_ENCRYPTION_KEY) covered; dead Global Payments vars confirmed absent"
      contains: "SENTRY_DSN"
    - path: "zoho-middleware/__tests__/validateEnv.test.js"
      provides: "Regression test for SENTRY_DSN prod enforcement"
      contains: "SENTRY_DSN"
  key_links:
    - from: ".github/workflows/update-snapshot.yml"
      to: "koa-inn/steins-and-vines-production"
      via: "git push via PROD_DEPLOY_TOKEN"
      pattern: "PROD_DEPLOY_TOKEN.*steins-and-vines-production"
    - from: "zoho-middleware/lib/validateEnv.js"
      to: "process.exit(1)"
      via: "REQUIRED_IN_PROD missing-secret loop"
      pattern: "REQUIRED_IN_PROD"
---

<objective>
Fix DEPLOY-03 (nightly snapshot staleness) and complete the MONITOR-02 code half by promoting SENTRY_DSN to a prod-required secret.

Purpose: DEPLOY-03 — the two-repo `git push production main --force` model overwrites snapshot commits that landed on production after the last force-push, leaving `steinsandvines.ca/content/zoho-snapshot.json` stale. MONITOR-02 — SENTRY_DSN currently only warns when missing in prod; the requirement says its absence must fail closed.

Output: A repo-guarded `update-snapshot.yml` that cross-pushes the snapshot to both repos, and a `validateEnv.js` that hard-fails boot in prod when SENTRY_DSN is missing, with a regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md
@.planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md
@.github/workflows/update-snapshot.yml
@zoho-middleware/lib/validateEnv.js

<interfaces>
<!-- update-snapshot.yml current "Commit and push if changed" step uses a bare `git push`.
     The fix replaces that single push with: push origin main, then push to production via token. -->
<!-- validateEnv.js current REQUIRED_IN_PROD (lines 13-18) has 4 entries:
     RECAPTCHA_SECRET_KEY, HELCIM_WEBHOOK_SECRET, CALCOM_WEBHOOK_SECRET, REDIS_ENCRYPTION_KEY.
     SENTRY_DSN currently lives in OPTIONAL at line 29.
     The missing-prod-secret loop (lines 106-117) already iterates REQUIRED_IN_PROD and calls process.exit(1). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-guard update-snapshot.yml and cross-push to production</name>
  <files>.github/workflows/update-snapshot.yml</files>
  <read_first>
    - .github/workflows/update-snapshot.yml (the file being modified — read in full)
    - .github/workflows/tests.yml (confirm the `[skip ci]` convention and checkout token pattern)
    - .planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md (Target 1 root cause + recommended Option A fix; Pitfall 1 and Pitfall 6)
    - .planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md (update-snapshot.yml section: exact repo-guard + cross-repo push additions)
  </read_first>
  <action>
    Make two targeted edits to .github/workflows/update-snapshot.yml; do NOT touch the fetch/validate/write-file steps.
    (1) Add a repo guard to the `update-snapshot` job so it runs ONLY on the staging repo: add the job-level condition `if: github.repository == 'koa-inn/steins-and-vines-staging'`. This stops the production repo (which receives the file via force-push) from running its own divergent nightly job.
    (2) Replace the single `git push` inside the "Commit and push if changed" step with two pushes: `git push origin main` (staging), then a cross-repo push to production using the PROD_DEPLOY_TOKEN secret via the `x-access-token` URL form targeting `github.com/koa-inn/steins-and-vines-production.git HEAD:main`. Keep the commit message exactly `chore: update Zoho snapshot [skip ci]`. Keep the `git diff --staged --quiet` no-op guard. Rename the step to reflect the dual push. NEVER echo the token value; embed it inline in the push URL only (it is masked by Actions when referenced as a secret).
    Rationale (per 33-RESEARCH Target 1, Option A): committing the snapshot to BOTH repos in the same nightly run means the next `git push production main --force` from the deploy workflow carries the same snapshot commit (since staging has it too), so production never reverts to a stale snapshot. PROD_DEPLOY_TOKEN is a human-created fine-grained PAT (created in Plan 03) — until it exists the cross-push step will fail loudly, which is the correct alarm, not a silent staleness.
  </action>
  <verify>
    <automated>grep -q "github.repository == 'koa-inn/steins-and-vines-staging'" .github/workflows/update-snapshot.yml && grep -q "PROD_DEPLOY_TOKEN" .github/workflows/update-snapshot.yml && grep -q "steins-and-vines-production.git HEAD:main" .github/workflows/update-snapshot.yml && grep -q "git push origin main" .github/workflows/update-snapshot.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/update-snapshot.yml'))" && echo YAML_OK && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - update-snapshot.yml contains `if: github.repository == 'koa-inn/steins-and-vines-staging'` on the update-snapshot job
    - update-snapshot.yml contains a `git push origin main` line AND a cross-repo push line referencing `secrets.PROD_DEPLOY_TOKEN` and `koa-inn/steins-and-vines-production.git HEAD:main`
    - The commit message string `chore: update Zoho snapshot [skip ci]` is unchanged
    - The token value is never passed to `echo`/`cat` (grep for `echo.*PROD_DEPLOY_TOKEN` returns nothing)
    - YAML remains valid: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/update-snapshot.yml'))"` exits 0
  </acceptance_criteria>
  <done>update-snapshot.yml is repo-guarded to staging and pushes the snapshot commit to both staging and production remotes; YAML parses clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote SENTRY_DSN to REQUIRED_IN_PROD with a regression test</name>
  <files>zoho-middleware/lib/validateEnv.js, zoho-middleware/__tests__/validateEnv.test.js</files>
  <read_first>
    - zoho-middleware/lib/validateEnv.js (the file being modified — read in full; REQUIRED_IN_PROD at lines 13-18, OPTIONAL SENTRY_DSN at line 29)
    - zoho-middleware/__tests__/validateEnv.test.js (existing test patterns for REQUIRED_IN_PROD; reuse the prod-missing-secret assertion shape)
    - .planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md (Target 6 gap analysis — SENTRY_DSN must hard-fail in prod; Open Questions #1 (RESOLVED) HELCIM_API_TOKEN; grounded in ROADMAP Success Criterion #5)
    - .planning/phases/33-deploy-safety-monitoring/33-PATTERNS.md (validateEnv.js section: exact REQUIRED_IN_PROD addition + remove duplicate from OPTIONAL)
  </read_first>
  <behavior>
    - Test: with NODE_ENV=production and all other REQUIRED_IN_PROD secrets set but SENTRY_DSN unset, validateEnv() calls process.exit(1) (per MONITOR-02, D-09).
    - Test: with NODE_ENV=production and ALL REQUIRED_IN_PROD secrets including SENTRY_DSN set, validateEnv() does NOT exit.
    - Test: with NODE_ENV unset/non-prod and SENTRY_DSN unset, validateEnv() does NOT exit on SENTRY_DSN (prod-only gate).
  </behavior>
  <action>
    AUTHORITY (READ FIRST): ROADMAP **Success Criterion #5** is the LOCKED source for this task. It states verbatim: "`HELCIM_WEBHOOK_SECRET`, `RECAPTCHA_SECRET_KEY`, and `SENTRY_DSN` are confirmed present in Railway production, and `validateEnv.js` startup check covers the live Helcim/Cal.com/`REDIS_ENCRYPTION_KEY` variables while dead Global Payments vars are removed." Promoting the live Helcim/Cal.com/REDIS_ENCRYPTION_KEY vars to REQUIRED_IN_PROD is therefore ROADMAP-LOCKED scope, NOT scope creep beyond REQUIREMENTS.md MONITOR-02. Resolves 33-RESEARCH Open Question #1 (now marked RESOLVED).
    CURRENT STATE (verified, validateEnv.js): REQUIRED_IN_PROD already contains RECAPTCHA_SECRET_KEY, HELCIM_WEBHOOK_SECRET, CALCOM_WEBHOOK_SECRET, and REDIS_ENCRYPTION_KEY (lines 13-18). SENTRY_DSN and HELCIM_API_TOKEN are the only SC#5-relevant vars still in OPTIONAL. Dead Global Payments (GP_* / GLOBAL_PAYMENT*) vars are already ABSENT from the file (33-RESEARCH Target 6, HARDEN-04 complete) — this task must NOT re-introduce them and must assert their absence.
    EDIT zoho-middleware/lib/validateEnv.js:
    (1) Append a SENTRY_DSN entry to REQUIRED_IN_PROD with desc referencing MONITOR-02 / ROADMAP SC#5 / phase 33, and REMOVE the now-duplicate SENTRY_DSN entry from OPTIONAL (line 29) so startup logs do not falsely report it as an unset optional after the hard gate enforces it.
    (2) Append a HELCIM_API_TOKEN entry to REQUIRED_IN_PROD with desc referencing the live-Helcim clause of ROADMAP SC#5, and REMOVE HELCIM_API_TOKEN from OPTIONAL (line 34) to avoid duplication.
    (3) Verify (do NOT re-add) that the live Cal.com webhook var (CALCOM_WEBHOOK_SECRET) and REDIS_ENCRYPTION_KEY remain present in REQUIRED_IN_PROD — they already are; this completes the FULL SC#5 set so the task does not under-cover it. If for any reason either is missing on read, add it.
    (4) Do NOT change the validateEnv() function body — the existing REQUIRED_IN_PROD loop (lines 106-117) already handles new entries.
    NOTE: SENTRY_DSN, HELCIM_API_TOKEN, CALCOM_WEBHOOK_SECRET, and REDIS_ENCRYPTION_KEY are all confirmed present in Railway production today (payments + bookings are live), so this fail-closed boot gate will NOT strand a currently-working deploy — a missing var surfacing as a loud boot failure is the intended alarm.
    Write the regression tests FIRST in validateEnv.test.js (RED), following the existing prod-missing-secret test shape: mock process.exit, set NODE_ENV=production plus the full secret set minus the var under test, assert process.exit called with 1; then a positive case with the full set asserting no exit; then a non-prod case. Then make them pass (GREEN).
    Per CLAUDE.md, run `cd zoho-middleware && npm test` and `cd zoho-middleware && npm run lint` before finishing.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- validateEnv 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - validateEnv.js REQUIRED_IN_PROD contains, at minimum, the FULL ROADMAP SC#5 set: SENTRY_DSN, HELCIM_API_TOKEN, HELCIM_WEBHOOK_SECRET, RECAPTCHA_SECRET_KEY, CALCOM_WEBHOOK_SECRET, and REDIS_ENCRYPTION_KEY
    - validateEnv.js OPTIONAL array no longer contains SENTRY_DSN or HELCIM_API_TOKEN (grep: each name appears exactly once in the file's var entries)
    - Dead Global Payments vars are absent: `grep -Ei 'GP_|GLOBAL_PAYMENT' zoho-middleware/lib/validateEnv.js` returns nothing
    - `cd zoho-middleware && npm test -- validateEnv` passes including a test that asserts process.exit(1) when SENTRY_DSN is the only missing prod secret
    - `cd zoho-middleware && npm run lint` exits 0
  </acceptance_criteria>
  <done>SENTRY_DSN and HELCIM_API_TOKEN are enforced in prod, removed from OPTIONAL, and a passing regression test proves SENTRY_DSN absence fails boot in prod.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → production repo | Cross-repo push carries a privileged token across a repo boundary |
| Railway boot → process env | Prod secrets gate whether the service is allowed to start |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | PROD_DEPLOY_TOKEN in update-snapshot.yml logs | mitigate | Token referenced only as `${{ secrets.PROD_DEPLOY_TOKEN }}` embedded inline in the push URL (Actions auto-masks); never passed to echo/cat. Acceptance criteria greps for `echo.*PROD_DEPLOY_TOKEN`. |
| T-33-02 | Elevation of Privilege | PROD_DEPLOY_TOKEN over-scope | mitigate | Token created (Plan 03) as a fine-grained PAT scoped to `koa-inn/steins-and-vines-production` Contents:write only — documented in RUNBOOK + HUMAN-UAT. |
| T-33-03 | Tampering | Snapshot reverted by force-push leaving stale catalog | mitigate | Cross-repo push lands snapshot on production directly so the next force-push from staging already carries it (Option A, 33-RESEARCH Target 1). |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (33-RESEARCH Package Legitimacy Audit: none). No install tasks. |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/update-snapshot.yml'))"` exits 0 (valid YAML)
- `cd zoho-middleware && npm test` full suite passes
- `cd zoho-middleware && npm run lint` exits 0
- grep confirms repo guard + dual push in update-snapshot.yml; the full SC#5 set in REQUIRED_IN_PROD (SENTRY_DSN, HELCIM_API_TOKEN, HELCIM_WEBHOOK_SECRET, RECAPTCHA_SECRET_KEY, CALCOM_WEBHOOK_SECRET, REDIS_ENCRYPTION_KEY); SENTRY_DSN/HELCIM_API_TOKEN gone from OPTIONAL; no GP_/GLOBAL_PAYMENT vars
</verification>

<success_criteria>
- DEPLOY-03: update-snapshot.yml runs only on staging and pushes the snapshot to both repos (the force-push staleness mechanism is broken).
- MONITOR-02 / ROADMAP SC#5 (code half): the full SC#5 set (SENTRY_DSN, HELCIM_API_TOKEN, HELCIM_WEBHOOK_SECRET, RECAPTCHA_SECRET_KEY, CALCOM_WEBHOOK_SECRET, REDIS_ENCRYPTION_KEY) fails closed in prod; dead Global Payments vars absent; regression test green.
</success_criteria>

<output>
Create `.planning/phases/33-deploy-safety-monitoring/33-01-SUMMARY.md` when done
</output>
