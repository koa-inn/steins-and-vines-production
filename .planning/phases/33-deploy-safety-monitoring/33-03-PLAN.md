---
phase: 33-deploy-safety-monitoring
plan: 03
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified:
  - .planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md
autonomous: false
requirements: [MONITOR-01, MONITOR-02, DEPLOY-01, DEPLOY-02, DEPLOY-03]
user_setup:
  - service: github
    why: "Cross-repo force-push + snapshot push from the gated workflow"
    env_vars:
      - name: PROD_DEPLOY_TOKEN
        source: "GitHub -> Settings -> Developer Settings -> Fine-grained PAT (resource owner koa-inn, repo koa-inn/steins-and-vines-production, Contents: Read+Write), stored as Actions secret on the staging repo"
  - service: railway
    why: "Fetch the Railway deploy ID for the runbook during the gated deploy"
    env_vars:
      - name: RAILWAY_TOKEN
        source: "Railway -> Project Settings -> Service Tokens -> scope to sv-middleware service + production environment, stored as Actions secret on the staging repo"
    dashboard_config:
      - task: "Enable 'Wait for CI' on the sv_middleware service (Approach A)"
        location: "Railway Dashboard -> svmiddleware-production service -> Settings"
      - task: "Confirm NODE_ENV=production and all prod secrets present"
        location: "Railway Dashboard -> svmiddleware-production service -> Variables"
  - service: uptimerobot
    why: "External 5-min uptime + keyword monitor on /health (MONITOR-01)"
    dashboard_config:
      - task: "Create Keyword monitor on /health asserting body contains \"redis\":true, 5-min interval, email alert"
        location: "uptimerobot.com -> Add New Monitor"
must_haves:
  truths:
    - "UptimeRobot polls https://svmiddleware-production.up.railway.app/health every 5 minutes and emails an alert when the body lacks \"redis\":true or returns non-200"
    - "PROD_DEPLOY_TOKEN and RAILWAY_TOKEN secrets exist on the staging repo and the first gated deploy runs green end-to-end"
    - "NODE_ENV=production and all REQUIRED_IN_PROD secrets (incl. SENTRY_DSN, HELCIM_API_TOKEN) are confirmed present in Railway (proven by a healthy post-deploy /health boot)"
    - "The pending Phase 32 human UAT (NODE_ENV + 4 prod secrets) is closed alongside MONITOR-02"
    - "content/zoho-snapshot.json is current at steinsandvines.ca after the first gated deploy"
    - "Implements locked CONTEXT decisions D-08 (UptimeRobot free-tier keyword monitor on /health every 5 min, email alert) and D-09 (MONITOR-02 secrets verification — human Railway confirmation, proven by a healthy post-deploy boot)"
  artifacts:
    - path: ".planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md"
      provides: "Recorded human verification of monitor, secrets, first gated deploy, and snapshot freshness"
      contains: "UptimeRobot"
  key_links:
    - from: "UptimeRobot keyword monitor"
      to: "svmiddleware-production.up.railway.app/health"
      via: "5-min keyword check on \"redis\":true"
      pattern: "redis.*true"
---

<objective>
Stand up external uptime monitoring (MONITOR-01), wire the deploy secrets, run the first gated deploy end-to-end, and verify+close the secrets posture (MONITOR-02) including the carried-over Phase 32 UAT.

Purpose: The code (Plans 01 and 02) is inert until the human creates the tokens, enables Railway "Wait for CI", sets up UptimeRobot, and exercises the gated deploy once to confirm the whole chain works (test gate -> CNAME swap -> force-push -> /health smoke -> tag -> runbook -> snapshot freshness). MONITOR-02 is proven by a healthy boot (the validateEnv prod gate passed) plus the one-time Railway dashboard confirmation.

Output: A completed `33-HUMAN-UAT.md` recording each verification, and the closure of the pending `32-HUMAN-UAT.md` items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-deploy-safety-monitoring/33-RESEARCH.md
@.planning/phases/33-deploy-safety-monitoring/33-CONTEXT.md
@.planning/phases/32-fail-closed-hardening-access-control/32-HUMAN-UAT.md
@docs/RUNBOOK.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create deploy secrets and enable Railway "Wait for CI"</name>
  <files>.planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md</files>
  <action>HUMAN ACTION (no executor automation): create the PROD_DEPLOY_TOKEN fine-grained PAT (koa-inn/steins-and-vines-production, Contents:write) and RAILWAY_TOKEN (scoped to sv-middleware + production), add both as Actions secrets on the STAGING repo, and enable "Wait for CI" on the svmiddleware-production Railway service. Record names/scopes/dates (never values) in 33-HUMAN-UAT.md. See how-to-verify for exact steps.</action>
  <what-built>Plans 01 and 02 wrote update-snapshot.yml (cross-repo push) and gated-deploy.yml (force-push + Railway deploy ID lookup). Both depend on two human-created secrets and one Railway toggle that an executor cannot create.</what-built>
  <how-to-verify>
    1. Create PROD_DEPLOY_TOKEN: GitHub -> Settings -> Developer Settings -> Personal access tokens -> Fine-grained tokens. Resource owner: koa-inn. Repository access: koa-inn/steins-and-vines-production only. Permissions: Contents = Read and Write. Set expiry to max (1 year) and add a renewal reminder (Pitfall 6). Add it on the STAGING repo: Settings -> Secrets and variables -> Actions -> New repository secret named PROD_DEPLOY_TOKEN.
    2. Create RAILWAY_TOKEN: Railway Dashboard -> Project Settings (sv-middleware) -> Service Tokens (or Tokens) -> generate scoped to the sv-middleware service + production environment. Add it on the STAGING repo as Actions secret RAILWAY_TOKEN.
    3. Enable Approach A: Railway Dashboard -> svmiddleware-production service -> Settings -> enable "Wait for CI". (If later flaky per Pitfall 4, switch to Approach B per RUNBOOK.)
    4. Record completion (token names, scopes, date) in 33-HUMAN-UAT.md — do NOT paste the token values anywhere.
  </how-to-verify>
  <acceptance_criteria>
    - PROD_DEPLOY_TOKEN exists as an Actions secret on koa-inn/steins-and-vines-staging (fine-grained, production repo, Contents:write)
    - RAILWAY_TOKEN exists as an Actions secret on the staging repo, scoped to sv-middleware + production
    - Railway "Wait for CI" is enabled on svmiddleware-production
    - 33-HUMAN-UAT.md records the setup with dates and scopes, no secret values
  </acceptance_criteria>
  <resume-signal>Type "secrets ready" once both Actions secrets exist and Railway "Wait for CI" is enabled.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create the UptimeRobot /health keyword monitor (MONITOR-01)</name>
  <files>.planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md</files>
  <action>HUMAN ACTION (no executor automation): curl /health to confirm the exact "redis":true serialization, then create an UptimeRobot Keyword monitor on https://svmiddleware-production.up.railway.app/health (keyword exists check on "redis":true, 5-min interval, email alert). Optionally add an informational "authenticated":false monitor. Record the config + a successful test check in 33-HUMAN-UAT.md. See how-to-verify for exact steps.</action>
  <what-built>MONITOR-01 requires an external monitor independent of CI. The /health endpoint already returns the right shape; this is pure dashboard setup.</what-built>
  <how-to-verify>
    Before creating the monitor, confirm the exact serialization (Assumption A1): run `curl -s https://svmiddleware-production.up.railway.app/health` and note whether the body shows `"redis":true` (no space) — use the exact substring observed as the keyword.
    1. Create a free account at uptimerobot.com (no credit card).
    2. Add New Monitor -> Type: Keyword. Friendly name: `sv-middleware /health Redis`. URL: https://svmiddleware-production.up.railway.app/health. Keyword: the observed `"redis":true` substring. Keyword type: "exists" (alert when the keyword is ABSENT). Interval: 5 minutes (free-tier max). Alert contact: site/ops email.
    3. (Optional, informational) Add a SECOND keyword monitor on the same URL with keyword `"authenticated":false` (alert when present) — note it fires after every deploy and is expected; treat as a re-auth reminder, not an outage. Researcher open question #3 resolved: redis is the hard alert; authenticated is optional/informational.
    4. Trigger a test/manual check in UptimeRobot and confirm it reports UP while the middleware is healthy.
    5. Record monitor name(s), URL, keyword, interval, and the test result in 33-HUMAN-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - A UptimeRobot keyword monitor polls https://svmiddleware-production.up.railway.app/health at a 5-minute interval
    - The monitor alerts via email when the `"redis":true` keyword is absent (or HTTP != 200)
    - The exact keyword string used matches the live /health serialization (verified by curl)
    - 33-HUMAN-UAT.md records the monitor configuration and a successful test check
  </acceptance_criteria>
  <resume-signal>Type "monitor live" once the UptimeRobot keyword monitor is created and reporting UP.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the first gated deploy and verify the full chain + MONITOR-02 secrets</name>
  <files>.planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md, .planning/phases/32-fail-closed-hardening-access-control/32-HUMAN-UAT.md</files>
  <action>HUMAN ACTION (executor confirms Railway secrets present, human triggers the deploy): confirm NODE_ENV=production + all REQUIRED_IN_PROD secrets in Railway, trigger gated-deploy.yml via workflow_dispatch, then verify the test gate, /health smoke (200+redis:true), prod-YYYYMMDD-N tag, RUNBOOK row, CNAME restore, and snapshot freshness. Close the pending 32-HUMAN-UAT.md items. Record all results in 33-HUMAN-UAT.md. See how-to-verify for exact steps.</action>
  <what-built>gated-deploy.yml (workflow_dispatch), the cross-repo snapshot push, and the prod-secret boot gate. This checkpoint exercises them once end-to-end and closes the Phase 32 secrets UAT.</what-built>
  <how-to-verify>
    Pre-req: ensure staging is in the desired-to-ship state and approved (staging-first rule). Confirm in Railway -> svmiddleware-production -> Variables that NODE_ENV=production and that RECAPTCHA_SECRET_KEY, HELCIM_WEBHOOK_SECRET, CALCOM_WEBHOOK_SECRET, REDIS_ENCRYPTION_KEY, SENTRY_DSN, and HELCIM_API_TOKEN are all present (these now hard-fail boot per Plan 01). If SENTRY_DSN or HELCIM_API_TOKEN is not yet set, set it now — otherwise the middleware will refuse to boot after deploy (that is the intended fail-closed alarm).
    1. Trigger the gated deploy: GitHub -> staging repo -> Actions -> "Deploy to prod" (gated-deploy.yml) -> Run workflow, supplying a reason.
    2. Confirm the test jobs run and pass; if either fails, confirm the deploy job does NOT run (DEPLOY-01).
    3. After the deploy job completes green, verify:
       - https://steinsandvines.ca loads (Pages updated).
       - The smoke-check step shows HTTP 200 + redis:true; if it logged an `authenticated:false` soft warning, visit https://svmiddleware-production.up.railway.app/auth/zoho to re-authenticate Zoho (expected on every restart).
       - A new git tag `prod-YYYYMMDD-N` exists on both repos.
       - docs/RUNBOOK.md has a new deploy-history row pairing the git SHA with the Railway deploy ID.
       - CNAME on staging is restored to staging.steinsandvines.ca (cat CNAME) and steinsandvines.ca still resolves (not 404).
    4. Verify DEPLOY-03: `curl -s https://steinsandvines.ca/content/zoho-snapshot.json | python3 -c "import sys,json; print(json.load(sys.stdin)['generated_at'])"` shows a generated_at within 25 hours; after the next nightly run, re-check that a subsequent prod deploy does NOT revert it.
    5. Record all results in 33-HUMAN-UAT.md and mark the pending 32-HUMAN-UAT.md items (NODE_ENV=production, RAILWAY_ENVIRONMENT present, the 4 prod secrets) as closed (a healthy boot proves the REQUIRED_IN_PROD gate passed — MONITOR-02 / D-09).
  </how-to-verify>
  <acceptance_criteria>
    - The gated deploy ran via workflow_dispatch; the deploy job only ran because both test jobs passed (DEPLOY-01)
    - steinsandvines.ca loads and /health returns 200 + redis:true post-deploy
    - A `prod-YYYYMMDD-N` tag exists and docs/RUNBOOK.md has a row pairing git SHA + Railway deploy ID (DEPLOY-02)
    - CNAME restored to staging.steinsandvines.ca on staging; steinsandvines.ca does not 404 (CNAME intact on production)
    - steinsandvines.ca/content/zoho-snapshot.json generated_at is within 25 hours (DEPLOY-03)
    - Railway confirmed to have NODE_ENV=production + all REQUIRED_IN_PROD secrets; 32-HUMAN-UAT.md pending items marked closed (MONITOR-02)
    - 33-HUMAN-UAT.md records every result above
  </acceptance_criteria>
  <resume-signal>Type "deploy verified" with the prod tag name and snapshot generated_at, or describe any failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human operator -> GitHub/Railway secret stores | One-time creation of privileged tokens |
| External monitor -> live /health | UptimeRobot reads the public health endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-09 | Elevation of Privilege | PROD_DEPLOY_TOKEN / RAILWAY_TOKEN scope | mitigate | Fine-grained PAT scoped to production repo Contents:write; Railway token scoped to sv-middleware + production only. Verified in Task 1 acceptance. |
| T-33-10 | Information Disclosure | Token values pasted into UAT/runbook | mitigate | Tasks explicitly forbid pasting token values; only names/scopes/dates recorded. |
| T-33-11 | Denial of Service | Missing prod secret blocks boot after deploy | accept | Intended fail-closed behavior (MONITOR-02). Operator sets SENTRY_DSN/HELCIM_API_TOKEN before deploy; the loud boot failure is the correct alarm, not a regression. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed in this plan (human-action + verification only). |
</threat_model>

<verification>
- UptimeRobot monitor reporting UP at 5-min interval on /health with `"redis":true` keyword
- First gated deploy ran green end-to-end; prod tag + runbook row created; CNAME restored; snapshot fresh
- Railway secrets posture confirmed; 32-HUMAN-UAT pending items closed
- 33-HUMAN-UAT.md records all results
</verification>

<success_criteria>
- MONITOR-01: external 5-min uptime monitor live on /health, alerting on redis-down / non-200.
- MONITOR-02: prod secrets confirmed present in Railway (healthy boot proves the gate); Phase 32 UAT closed.
- DEPLOY-01/02/03 proven live by the first gated deploy: tests gated, tag+runbook recorded, snapshot fresh.
</success_criteria>

<output>
Create `.planning/phases/33-deploy-safety-monitoring/33-03-SUMMARY.md` when done.
Create/complete `.planning/phases/33-deploy-safety-monitoring/33-HUMAN-UAT.md` with the recorded verifications.
</output>
