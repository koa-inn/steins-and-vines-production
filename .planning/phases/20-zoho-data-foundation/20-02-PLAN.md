---
phase: 20-zoho-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Every ingredient item in Zoho Inventory has its Subcategory custom field set to one of: Grain, Yeast, Additive, Packaging, Equipment, Cleaning, or Hops"
    - "The nightly snapshot pipeline captures subcategory for each ingredient item without any code changes"
    - "Verification script reports 100% coverage (exit code 0)"
  artifacts: []
  key_links:
    - from: "zoho-middleware/scripts/tag-subcategories.js"
      to: "Zoho Inventory items"
      via: "inventoryPut custom_fields update"
      pattern: "Subcategory"
    - from: "zoho-middleware/scripts/verify-subcategories.js"
      to: "http://localhost:3001/api/ingredients"
      via: "http.get pipeline verification"
      pattern: "api/ingredients"
---

<objective>
Execute the Zoho subcategory tagging workflow: run the bulk tagger, manually tag ambiguous items in the Zoho UI, and verify 100% coverage via the verification script.

Purpose: This plan applies the tooling created in Plan 01 to actually tag all ~219 ingredient items in Zoho Inventory, completing DATA-01 and DATA-02. The tagging script handles ~185 items automatically; the remaining ~13 ambiguous items require human judgment in the Zoho UI.

Output: All Zoho ingredient items tagged with Subcategory CF; verification script confirms 100% coverage
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-zoho-data-foundation/20-01-SUMMARY.md
@.planning/phases/20-zoho-data-foundation/20-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run tagging script and manually tag ambiguous items</name>
  <files>zoho-middleware/scripts/tag-subcategories.js</files>
  <action>
    Human executes the tagging workflow created in Plan 01. Three steps: (1) dry-run to preview, (2) live run to auto-tag ~185 items, (3) manually tag ~13 ambiguous items in Zoho UI.

    Dry run: cd zoho-middleware and node scripts/tag-subcategories.js --dry-run
    Live run: node scripts/tag-subcategories.js
    Manual tagging: Zoho Inventory UI for items listed in MANUAL REVIEW REQUIRED output — candles and soaps as Equipment (per D-03), Falconer's Flight variants as Hops, Fermenting Starter Kit as Equipment.

    If the Subcategory custom field does not exist in Zoho yet, create it first: Zoho Inventory > Settings > Preferences > Items > Custom Fields > Add Custom Field > Type: Text, Label: "Subcategory" (capital S).
  </action>
  <verify>
    <automated>echo "Human action required — tagging script run and manual Zoho UI edits. No automated verification for this step."</automated>
  </verify>
  <done>All ~219 ingredient items have Subcategory custom field set in Zoho Inventory (both auto-tagged and manually tagged)</done>
  <what-built>
    Plan 01 created two scripts:
    - zoho-middleware/scripts/tag-subcategories.js — bulk tagger with --dry-run mode and pre-flight CF label inspection
    - zoho-middleware/scripts/verify-subcategories.js — coverage checker

    The tagging script auto-classifies ~185 ingredient items by name keywords and outputs a list of ~13 ambiguous items that need manual tagging in the Zoho UI.
  </what-built>
  <how-to-verify>
    **Step 1 — Dry run (preview only, no Zoho changes):**
    ```
    cd zoho-middleware
    node scripts/tag-subcategories.js --dry-run
    ```
    Review the output. The pre-flight check will confirm the Subcategory CF label exists. Confirm the keyword mappings look correct. Check the MANUAL REVIEW list.

    **Step 2 — Live run (tags items in Zoho):**
    ```
    node scripts/tag-subcategories.js
    ```
    This takes ~2-3 minutes (700ms delay between API calls). Watch for errors in the output. If the pre-flight check fails with "ABORT: No custom field with label Subcategory found", create the CF first (see below).

    **Step 3 — Manually tag ambiguous items in Zoho UI:**
    The script will output a MANUAL REVIEW REQUIRED section listing ~13 items (candles, soaps, Falconer's Flight, Fermenting Starter Kit).

    For each item:
    1. Go to Zoho Inventory (inventory.zoho.com)
    2. Search for the item by SKU
    3. Edit the item, set the Subcategory custom field:
       - Candles and soaps: "Equipment" (per D-03 catch-all)
       - Falconer's Flight variants: "Hops" (it is a hop blend)
       - Fermenting Starter Kit: "Equipment" (per D-03)
    4. Save each item

    **If the Subcategory custom field does not exist in Zoho:**
    Create it first: Zoho Inventory > Settings > Preferences > Items > Custom Fields > Add Custom Field > Type: Text, Label: "Subcategory" (capital S, no space). Then re-run the tagging script.
  </how-to-verify>
  <resume-signal>Type "tagged" when all items have been tagged in Zoho (both auto-tagged and manually tagged)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify 100% subcategory coverage</name>
  <files>zoho-middleware/scripts/verify-subcategories.js</files>
  <action>
    Human starts local middleware, authenticates with Zoho, runs the verification script, and confirms 100% coverage. Optionally triggers the nightly snapshot workflow to confirm end-to-end pipeline.
  </action>
  <verify>
    <automated>echo "Human verification required — run: cd zoho-middleware && node scripts/verify-subcategories.js — script exits 0 on 100% coverage (all items tagged), exits 1 if any items are missing subcategory. Type 'verified' only when exit code is 0."</automated>
  </verify>
  <done>verify-subcategories.js exits with code 0 showing 100% coverage across all 7 subcategory values</done>
  <what-built>
    All ingredient items should now have the Subcategory custom field set in Zoho. The verification script will confirm this by checking the live middleware API response.
  </what-built>
  <how-to-verify>
    **Step 1 — Start local middleware (if not already running):**
    ```
    cd zoho-middleware
    node server.js
    ```
    Then visit http://localhost:3001/auth/zoho to authenticate.

    **Step 2 — Run verification:**
    ```
    node scripts/verify-subcategories.js
    ```

    **Exit code contract:** The script exits 0 on 100% coverage (all items tagged) and exits 1 if any items are missing subcategory. Only type "verified" when the script exits with code 0.

    **Expected output:**
    ```
    === Subcategory Coverage Report ===
    Total ingredients: ~219
    Tagged:   ~219
    Missing:  0
    Coverage: 100%

    Breakdown:
      Additive: ~55
      Cleaning: ~4
      Equipment: ~33
      Grain: ~18
      Hops: ~45
      Packaging: ~19
      Yeast: ~15
    ```

    **If coverage is not 100%:** The script will list items still missing subcategory and exit with code 1. Tag those items in Zoho UI and re-run the verification.

    **Step 3 — Confirm snapshot pipeline (optional, can wait for nightly):**
    Either wait for the nightly update-snapshot.yml run (6 AM UTC) or manually trigger it:
    - GitHub repo > Actions > update-snapshot > Run workflow

    After the snapshot updates, check content/zoho-snapshot.json for subcategory values on a few items to confirm the pipeline works end-to-end (per D-08).
  </how-to-verify>
  <resume-signal>Type "verified" when verify-subcategories.js exits with code 0 (100% coverage)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human -> Zoho UI | Human edits item custom fields directly in Zoho web interface |
| Script -> Zoho API | Tagging script writes to live production Zoho data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Tampering | Human manual tagging in Zoho UI | accept | Verification script catches any mistagged or missing items; human is the trusted actor |
| T-20-06 | Tampering | Live Zoho data modification by script | mitigate | Dry-run preview before live run; VALID_SUBCATEGORIES validation in script prevents invalid values; pre-flight CF label check prevents silent no-ops |
</threat_model>

<verification>
1. verify-subcategories.js exits with code 0
2. Coverage report shows 100% (0 missing items)
3. All 7 subcategory values appear in the breakdown (Grain, Yeast, Additive, Packaging, Equipment, Cleaning, Hops)
</verification>

<success_criteria>
Verification script reports 100% subcategory coverage across all ingredient items. All items are tagged with one of the 7 valid subcategory values. The snapshot pipeline will capture these values on its next nightly run without any code changes.
</success_criteria>

<output>
Create `.planning/phases/20-zoho-data-foundation/20-02-SUMMARY.md` when done
</output>
