---
phase: 29.3-pull-non-kiosk-batch-sales-into-brewpad
plan: "02"
subsystem: api
tags: [apps-script, google-sheets, idempotency, dedup, batch-tracking]

# Dependency graph
requires:
  - phase: 29.3-01
    provides: middleware pre-check for zoho_so_number dedup (D-10 first line)
provides:
  - zoho_so_number idempotency guard in Apps Script createBatch (D-10 backstop)
  - duplicate_so_number rejection in { ok:false, error, message } shape
affects: [29.3-03, any caller of createBatch who sends zoho_so_number]

# Tech tracking
tech-stack:
  added: []
  patterns: [idempotency guard via pre-scan before lock acquisition, Google Apps Script error shape]

key-files:
  created: []
  modified:
    - apps-script/adminApi.gs

key-decisions:
  - "Guard placed after missing_fields check and before lock acquisition — duplicates rejected cheapest possible (no sheet append, no lock)"
  - "sheetToObjects(BATCHES_SHEET_NAME) used for the scan — consistent with how getBatches reads the sheet"
  - "Trim + String() coercion matches the robustness pattern already used across the codebase for sheet value comparisons"

patterns-established:
  - "Idempotency guard pattern: scan sheetToObjects before sheet append, return { ok:false, error:'...' } on match"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-06-14
---

# Phase 29.3 Plan 02: Apps Script createBatch zoho_so_number Idempotency Guard Summary

**zoho_so_number idempotency guard inserted into Apps Script createBatch — rejects duplicate SO/invoice creates with { ok:false, error:'duplicate_so_number' } before lock acquisition**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-06-14T01:38:00Z
- **Completed:** 2026-06-14T01:40:49Z
- **Tasks:** 1/1
- **Files modified:** 1

## Accomplishments
- Added D-10.2 backstop guard to `createBatch` in `apps-script/adminApi.gs`
- Guard scans all existing BATCHES rows via `sheetToObjects(BATCHES_SHEET_NAME)` before any lock acquisition or sheet append
- Returns `{ ok:false, error:'duplicate_so_number', message:'A batch for SO/invoice <N> already exists: SV-B-XXXXXX' }` on match
- Guard is wrapped in `if (payload.zoho_so_number)` — batches without an SO number are completely unaffected
- Includes regression comment documenting the bug fixed and expected behavior

## Task Commits

1. **Task 1: Add zoho_so_number idempotency guard to createBatch** - `5d8b0ff` (fix)

**Plan metadata:** (pending — docs commit below)

## Files Created/Modified
- `apps-script/adminApi.gs` — zoho_so_number idempotency guard inserted at lines 1905-1922

## Decisions Made
- Guard placed immediately after `missing_fields` validation (line 1903) and before `var ss = SpreadsheetApp.getActiveSpreadsheet()` (line 1938) — duplicates are rejected at the earliest possible point, before any expensive lock or sheet I/O.
- Variable named `di` (dedup index) to avoid shadowing any outer `i` variable in the function scope.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## MANUAL REDEPLOY REQUIRED

**This plan's changes are NOT live until the user manually redeploys `adminApi.gs` to Google Apps Script.**

The file `apps-script/adminApi.gs` is hand-deployed to Google (not part of any automated build or CI pipeline). The guard added in this plan will have no effect in production until the following manual step is completed:

1. Open the Google Apps Script project linked to the Steins & Vines Google Sheet.
2. Replace the existing `adminApi.gs` content with the updated file from `apps-script/adminApi.gs`.
3. Save and deploy a new version (or update the existing web app deployment).
4. Verify the guard is active by checking that a duplicate `zoho_so_number` create returns `{ ok: false, error: 'duplicate_so_number' }`.

**Do NOT mark Phase 29.3 as live / ship to production until this manual redeploy is acknowledged.**

## Verification

- Grep gate: `grep -v '^[[:space:]]*//' apps-script/adminApi.gs | grep -c "duplicate_so_number"` returns `1` (passing)
- Guard position confirmed: after `missing_fields` return (line 1903), before `var ss = SpreadsheetApp.getActiveSpreadsheet()` (line 1938)
- `if (payload.zoho_so_number)` wrapper confirmed — batches without SO number bypass the guard entirely

## Known Stubs

None — the guard is complete code (no placeholders). Live behavior pending manual redeploy to Google Apps Script.

## Threat Flags

No new security surface introduced. This plan closes T-29.3-07 (Tampering: duplicate/replayed createBatch) and T-29.3-08 (Repudiation: silent double-create) per the plan's threat register.

## Next Phase Readiness

- Plan 29.3-03 (middleware bulk-create route + BrewPad UI) can proceed immediately
- The Apps Script guard is a backstop — the middleware pre-check (29.3-01) is the primary dedup path; both are now in place
- The `.gs` changes must be user-deployed to Google before the end-to-end flow is fully protected

---
*Phase: 29.3-pull-non-kiosk-batch-sales-into-brewpad*
*Completed: 2026-06-14*
