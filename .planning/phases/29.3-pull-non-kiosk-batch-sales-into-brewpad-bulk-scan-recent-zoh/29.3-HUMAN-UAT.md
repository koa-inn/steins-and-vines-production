---
status: partial
phase: 29.3-pull-non-kiosk-batch-sales-into-brewpad
source: [29.3-VERIFICATION.md, 29.3-REVIEW.md]
started: 2026-06-14
updated: 2026-06-14
---

## Current Test

[awaiting human testing + mandatory adminApi.gs redeploy — code fixes applied, see ## Blocking Issues]

## Blocking Issues (from 29.3-REVIEW.md — must resolve before production)

- [x] **CR-01 (BLOCKER — CODE FIXED, REDEPLOY REQUIRED)** `apps-script/adminApi.gs:1910-1922` — `createBatch` dedup guard now scopes to `zoho_so_number AND product_sku` (commit `7bb8ce5`). Multi-kit invoices can now create one batch per kit SKU; replay of the same invoice+SKU pair is still blocked. **WARNING: adminApi.gs has no Jest harness — this fix has NO effect in production until you manually redeploy the Apps Script to Google. See MANDATORY step below.**
- [x] **CR-02 (BLOCKER — FIXED)** `zoho-middleware/routes/pos.js:1508` — single-invoice mode now uses search-then-detail (commit `bac9133`). Calls `/invoices?invoice_number=INV-XXXXXX` first to resolve the numeric internal ID, then `/invoices/<numericId>`. SO-prefixed numbers route to `/salesorders` correctly. Regression tests added (commit `343caee`). No more 502 on single-import.
- [x] **WR-01 (warning — FIXED)** `pos.js:1652-1664` — `invoice_ids` now validated against `/^\d{15-20}$/` regex and capped at 200 entries (commit `bac9133`). Path-traversal strings and human-readable INV/SO numbers are rejected with 400. Regression tests added.
- [x] **WR-02 (warning — FIXED)** `js/brewpad.js:2283` — `removeEventListener` guard added before `addEventListener` in `openPullFromZohoSheet` (commit `ca506de`). Structural regression test added. `js/brewpad.min.js` rebuilt.

## Tests

- [ ] **Pull sheet opens with scan results** — open BrewPad, click "Pull from Zoho", confirm the candidate confirm sheet renders with scanned invoices (live middleware + Zoho).
- [ ] **DRAFT badge renders** — confirm a draft-status candidate shows the orange DRAFT badge in the confirm sheet.
- [ ] **Single-invoice import validates and resolves** — type a known INV/SO number, confirm it resolves and creates a pending batch (CR-02 is now fixed; requires live Zoho).
- [ ] **Multi-kit invoice creates a batch per kit** — use a test invoice with 2+ kit line items and confirm all get pending batches (CR-01 fix is in code; only effective after adminApi.gs redeploy).
- [ ] **MANDATORY: Redeploy `adminApi.gs` to Google Apps Script** — the CR-01 fix (scoped dedup guard) is in `apps-script/adminApi.gs` but has NO effect in production until manually redeployed to the Steins & Vines Google Apps Script project. Do not test multi-kit behaviour or ship to production until the redeploy is complete.

## Notes

- Code fixes: CR-01 (adminApi.gs commit 7bb8ce5), CR-02 (pos.js commit bac9133), WR-01 (pos.js commit bac9133), WR-02 (brewpad.js commit ca506de).
- Regression tests added for CR-02 and WR-01 in `zoho-middleware/__tests__/batch-scan-invoices.test.js` and a structural test for WR-02 in `tests/frontend/brewpad-pull-from-zoho.test.js`.
- Both test suites green: 579 frontend tests, 648 middleware tests. Lint: 0 errors.
- Per project rule: all changes go to **staging first** and require human approval before production. Nothing has been pushed.
