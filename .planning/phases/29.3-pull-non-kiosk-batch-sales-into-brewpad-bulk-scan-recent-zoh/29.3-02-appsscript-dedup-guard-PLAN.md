---
phase: 29.3-pull-non-kiosk-batch-sales-into-brewpad
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps-script/adminApi.gs
autonomous: true
requirements: []
must_haves:
  truths:
    - "createBatch rejects a second batch carrying a zoho_so_number that already maps to an existing batch (D-10.2)"
    - "The rejection returns { ok:false, error:'duplicate_so_number', message:... } matching the existing Apps Script error shape"
    - "Batches with no zoho_so_number are unaffected (guard only fires when zoho_so_number is present)"
    - "The guard runs before lock acquisition / sheet append so a concurrent duplicate cannot land"
  artifacts:
    - path: "apps-script/adminApi.gs"
      provides: "zoho_so_number idempotency guard at the top of createBatch"
      contains: "duplicate_so_number"
  key_links:
    - from: "apps-script/adminApi.gs createBatch"
      to: "BATCHES sheet existing zoho_so_number values"
      via: "sheetToObjects scan before create"
      pattern: "zoho_so_number"
---

<objective>
Add a zoho_so_number idempotency guard to Apps Script `createBatch` so a duplicate or
concurrent create for an invoice that already has a batch is rejected instead of landing twice.

Purpose: D-10 belt-and-suspenders. `createBatch` (~:1899) currently has NO dedup by
zoho_so_number — a latent duplicate-risk bug worth fixing regardless of the scan feature.
The middleware pre-check (plan 29.3-01) is the first line; this guard is the backstop that
holds even under a concurrent/replayed create.

Output: A guard block at the top of createBatch in apps-script/adminApi.gs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.3-pull-non-kiosk-batch-sales-into-brewpad-bulk-scan-recent-zoh/29.3-CONTEXT.md
@.planning/phases/29.3-pull-non-kiosk-batch-sales-into-brewpad-bulk-scan-recent-zoh/29.3-PATTERNS.md

<interfaces>
From apps-script/adminApi.gs:
  function createBatch(payload, userEmail)  // ~:1899; returns { ok:false, error, message } on reject, { ok:true, batch_id, ... } on success
  Existing validation block ~:1900-1902 returns { ok:false, error:'missing_fields', message:... }
  sheetToObjects(SHEET_NAME) => array of row objects (BATCHES_SHEET_NAME holds zoho_so_number column, written ~:1971)
  get_batches doGet action ~:141 returns { ok:true, data:{ batches:[{ zoho_so_number, batch_id }], ... } }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add zoho_so_number idempotency guard to createBatch (regression-test-first)</name>
  <files>apps-script/adminApi.gs</files>
  <read_first>
    - apps-script/adminApi.gs lines 1899-1974 (createBatch — full function, validation block, lock acquisition, sheet append, return shape)
    - apps-script/adminApi.gs lines 141-144 (get_batches dispatch), 1310-1365 (getBatches return shape), 1971 (where zoho_so_number is written to the sheet)
    - apps-script/adminApi.gs — find sheetToObjects and BATCHES_SHEET_NAME definitions (grep) to confirm exact identifiers
    - .planning/phases/29.3-pull-non-kiosk-batch-sales-into-brewpad-bulk-scan-recent-zoh/29.3-PATTERNS.md (section 4 — exact guard placement + error shape)
  </read_first>
  <action>
    Per CLAUDE.md "write a regression test FIRST when fixing bugs": this is a latent duplicate-risk
    fix. Apps Script (.gs) has no Jest harness in this repo, so the regression check is encoded as a
    deterministic, in-file documented assertion plus a source-level grep gate (the .gs runs only in
    Google's runtime). Add a short comment block above the guard documenting the bug being fixed
    (no dedup by zoho_so_number) and the expected behavior (reject duplicate). The verifiable
    regression artifact is the guard + its grep-checkable error contract.

    Insert the guard in createBatch AFTER the existing field-validation block (~:1900-1902) and BEFORE
    lock acquisition / `var ss = ...` (~:1919), exactly per PATTERNS.md section 4:
    - Only run when payload.zoho_so_number is truthy.
    - Read existing rows via sheetToObjects(BATCHES_SHEET_NAME) (confirm the exact constant name from
      the file; use whatever the file actually defines).
    - If any existing row's zoho_so_number (trimmed, String()) equals payload.zoho_so_number (trimmed,
      String()), return { ok:false, error:'duplicate_so_number', message:'A batch for SO/invoice ' +
      payload.zoho_so_number + ' already exists: ' + existing.batch_id }.
    - Match the existing { ok:false, error:'string', message:'string' } shape used at lines 1902/1915/1924.
    Make ONE logical change per commit (CLAUDE.md). Do not alter the create path beyond inserting the guard.

    Note for the executor: this file is deployed to Google Apps Script manually by the user (STATE.md
    notes adminApi.gs is deployed by hand). Record in the SUMMARY that the user must redeploy adminApi.gs
    for the guard to take effect — do NOT mark the phase live until that manual step is acknowledged.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*//' apps-script/adminApi.gs | grep -c "duplicate_so_number"</automated>
  </verify>
  <acceptance_criteria>
    - adminApi.gs createBatch contains a guard returning error:'duplicate_so_number' when an existing batch shares the zoho_so_number.
    - The grep gate (comments stripped) returns >= 1 for "duplicate_so_number".
    - The guard is positioned after the missing_fields validation and before lock acquisition / sheet append (verify by reading the surrounding lines).
    - The guard is wrapped in `if (payload.zoho_so_number)` so batches without an SO number are unaffected.
    - SUMMARY notes the manual Apps Script redeploy requirement.
  </acceptance_criteria>
  <done>createBatch rejects duplicate zoho_so_number with the standard error shape; redeploy note recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → Apps Script create_batch | server_token-authenticated write into the BATCHES sheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.3-07 | Tampering | createBatch duplicate/replayed create | mitigate | zoho_so_number idempotency guard scans existing rows and rejects duplicates before sheet append (D-10.2); backstops the middleware pre-check. |
| T-29.3-08 | Repudiation | silent double-create | mitigate | Explicit { ok:false, error:'duplicate_so_number', message } surfaced to caller so the bulk-create per-row result records the rejection rather than failing silently. |
| T-29.3-SC | Tampering | dependency installs | accept | No package installs; .gs edit only. |
</threat_model>

<verification>
- Reading createBatch shows the guard before lock acquisition.
- A create with a zoho_so_number already present returns duplicate_so_number.
- A create with no zoho_so_number is unaffected.
</verification>

<success_criteria>
- createBatch is idempotent on zoho_so_number; duplicate creates are rejected with the standard error shape; redeploy note captured.
</success_criteria>

<output>
Create `.planning/phases/29.3-pull-non-kiosk-batch-sales-into-brewpad-bulk-scan-recent-zoh/29.3-02-SUMMARY.md` when done
</output>
