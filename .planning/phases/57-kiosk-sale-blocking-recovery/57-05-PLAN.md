---
phase: 57-kiosk-sale-blocking-recovery
plan: 05
type: execute
wave: 4
depends_on: [57-03, 57-04]
files_modified: []
autonomous: false

requirements: [REVIEW-01]

must_haves:
  truths:
    - "The 57-03 client fix and 57-04 server safety net are deployed (frontend via staging→prod, middleware to prod) and the full gate is green before deploy"
    - "On the REAL iPad, a sale of a previously-stale item completes WITHOUT a manual 'Refresh the product list' — SC#4, verified live, not merely a green suite"
    - "The client-error beacon now captures the sale server-error class with a READABLE item_id (both 57-DIAGNOSIS beacon findings closed end-to-end)"
  artifacts:
    - path: ".planning/phases/57-kiosk-sale-blocking-recovery/57-05-SUMMARY.md"
      provides: "Live-iPad verification record for SC#4"
  key_links:
    - from: "prod kiosk (live iPad)"
      to: "a sale that self-heals a stale catalog"
      via: "no manual refresh required"
      pattern: "verified"
---

<objective>
Prove the fix on the LIVE device. Phase 57 SC#4 is explicit that a green suite proves nothing here — `7cbf856` passed its tests and still failed in the store. This plan runs the full gate, deploys (frontend staging→prod, middleware prod-only — there is no staging middleware), and gates on a human confirming on the real iPad that a previously-stale item now sells WITHOUT a manual refresh, and that the beacon captured the occurrence with a readable item_id.

Purpose: Satisfy Phase 57 SC#4 (live-iPad verification against the prod middleware).
Output: A live confirmation + 57-05-SUMMARY.md recording it.

## Ordering note (deployment reality)
57-03 (client) is the fix for the CONFIRMED cause; 57-04 (server) is the safety net that touches the money path with no staging middleware. Deploy 57-03 first and confirm the frontend self-heal, then deploy 57-04's middleware change in the same low-traffic window and re-confirm. If 57-04 cannot be safely deployed now, the phase may ship on 57-03 alone with 57-04 held for a follow-up window — record that decision in the summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md
@.planning/phases/57-kiosk-sale-blocking-recovery/57-03-PLAN.md
@.planning/phases/57-kiosk-sale-blocking-recovery/57-04-PLAN.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate + deploy</name>
  <files></files>
  <read_first>
    - CLAUDE.md (before every commit: `npm test`, `cd zoho-middleware && npm test`, `npm run lint`; ALL changes to staging first; prod is `git push production main --force`; enforce-cname workflow auto-corrects the domain)
    - docs/RUNBOOK.md (prod deploy procedure, if present)
    - .planning/STATE.md (anti-pattern: a green suite proved nothing last time)
  </read_first>
  <action>
    1. Run the FULL gate and confirm green: `npm test`, `cd zoho-middleware && npm test`, `npm run lint`. Confirm 57-03's artifacts (js/main.js, js/main.min.js, js/kiosk-core.min.js) were rebuilt and committed.
    2. Deploy the FRONTEND to staging: `git push origin main` → staging.steinsandvines.ca. Sanity-check the kiosk loads there.
    3. Deploy the MIDDLEWARE (57-04) to the prod Railway instance — there is no staging middleware, so this goes live for real customers on deploy. Do this in a low-traffic window per docs/RUNBOOK.md.
    4. Deploy the FRONTEND to production: `git push production main --force` → steinsandvines.ca.
    Do NOT proceed to the live-iPad checkpoint until all deploys report success.
  </action>
  <verify>
    <automated>npm test && cd zoho-middleware && npm test && cd .. && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Full frontend + middleware suites and both lints are green before any push.
    - Frontend is live on staging then production; middleware is live on prod Railway.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-iPad verification (SC#4)</name>
  <read_first>
    - .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md (the captured symptom: "Item not found in current catalog: <id>. Refresh the product list and try again.")
    - .planning/STATE.md (7cbf856 passed its tests and still failed in the store — the live device is the only proof)
  </read_first>
  <action>
    This is a blocking human-verify checkpoint: after Task 1 has deployed the fix, hand the verification steps below to the operator on the live iPad and PAUSE. Do not mark the phase complete or proceed until the operator returns the resume-signal. If they report a residual failure, capture it as a new 57-DIAGNOSIS occurrence rather than retrying blindly.
  </action>
  <what-built>
    57-03: the kiosk now re-fetches its catalog on wake/staleness (dropping phantom items) and blocks a stale cart line before the sale POST, and beacons the sale server-error branch with a readable item_id. 57-04: on a catalog-miss the server auto-reconciles once before rejecting, and logs the item_id un-redacted. Both are deployed (frontend prod, middleware prod).
  </what-built>
  <how-to-verify>
    On the REAL iPad in the shop (hard-reload once to pick up the new bundle):
    1. Reproduce the staleness condition as closely as possible: leave the kiosk open/idle so the catalog ages past the staleness bound (or sleep the iPad and wake it), then return to the product grid.
    2. Confirm the grid self-refreshes on wake and no longer offers any item that was removed/changed in Zoho (the phantom class from the diagnosis).
    3. Attempt a real sale (or a low-value test sale) of an item and confirm it can be STARTED and pushed to the terminal WITHOUT tapping "Refresh the product list" first — i.e. the failure the owner photographed no longer requires a manual refresh.
    4. If you can still trigger a catalog-miss for a CURRENT item, confirm the server auto-reconcile lets the sale through without staff action.
    5. In Sentry / middleware logs, confirm any `source: kiosk-client` beacon for this class now shows the endpoint `/api/kiosk/sale`, the real status, and a READABLE item_id (not `[REDACTED]`).
  </how-to-verify>
  <resume-signal>Type "verified" once a previously-stale item sells on the live iPad WITHOUT a manual refresh (and the beacon shows a readable item_id), or describe the residual failure — which reopens diagnosis (a new 57-DIAGNOSIS occurrence) rather than silently retrying.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → prod kiosk (live customers) | Middleware has no staging; a bad deploy is live immediately on the money surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-05-01 | Denial of Service | prod middleware deploy | mitigate | Deploy in a low-traffic window; the 57-04 change is bounded (one rebuild/sale) and reversible; frontend goes to staging first. |
| T-57-05-02 | Tampering | live money path | mitigate | Live verification uses a low-value/real sale and confirms price-anchoring still holds (a phantom still rejects); any residual failure reopens diagnosis rather than being papered over. |
| T-57-05-SC | Tampering (supply chain) | npm installs | accept | No installs in this plan. |
</threat_model>

<verification>
- Full suites + lints green before deploy.
- Human-confirmed on the live iPad: a previously-stale item sells without a manual refresh; beacon shows a readable item_id.
</verification>

<success_criteria>
Satisfies Phase 57 SC#4: verified on the live iPad against the prod middleware — not merely a green suite.
</success_criteria>

<output>
Create `.planning/phases/57-kiosk-sale-blocking-recovery/57-05-SUMMARY.md` when done, recording the live-iPad outcome and whether 57-04's middleware change shipped now or was deferred.
</output>
