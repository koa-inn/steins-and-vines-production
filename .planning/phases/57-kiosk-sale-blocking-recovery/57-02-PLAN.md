---
phase: 57-kiosk-sale-blocking-recovery
plan: 02
type: execute
wave: 2
depends_on: [57-01]
files_modified:
  - .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md
autonomous: false

requirements: [REVIEW-01]

must_haves:
  truths:
    - "57-01 is deployed to the PROD middleware and the live kiosk (the only place the beacon can be exercised — there is no staging middleware)"
    - "A REAL kiosk failure occurrence is captured through the beacon on the live iPad (forced or observed), not inferred"
    - "The confirmed cause is recorded in 57-DIAGNOSIS.md BEFORE any fix is written — with the real error text, HTTP status, endpoint, and auth_state from the captured beacon"
    - "The diagnosis names which of the four hypotheses won and specifies the fix 57-03 must make"
  artifacts:
    - path: ".planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md"
      provides: "The recorded, evidence-backed root cause + the prescribed 57-03 fix branch"
      contains: "Confirmed cause"
  key_links:
    - from: "live iPad beacon"
      to: "Sentry captured occurrence"
      via: "POST /api/kiosk/client-error on the prod middleware"
      pattern: "kiosk-client"
---

<objective>
Look through the lens 57-01 built. Deploy it to production (the beacon can only be exercised against the prod middleware — there is no staging middleware), force or await a REAL sale-blocking failure on the live iPad, capture the beacon report from Sentry, and RECORD the diagnosis in `57-DIAGNOSIS.md` before a single line of fix is written.

This plan is deliberately a checkpoint gate, not code. It exists to stop the phase from guessing a second time: `7cbf856` was inferred from symptoms, never reproduced, and this recurrence proves the guess was incomplete. 57-03 (the fix) is BLOCKED on the artifact this plan produces.

Purpose: Satisfy Success Criterion #2 — the real cause is diagnosed from a captured occurrence, not inferred, and recorded before any fix.
Output: `57-DIAGNOSIS.md` naming the confirmed cause and the exact fix 57-03 must make.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/kiosk-sale-requires-refresh-recurring.md
@docs/RUNBOOK.md
@.planning/phases/57-kiosk-sale-blocking-recovery/57-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deploy 57-01 to the live kiosk + prod middleware</name>
  <read_first>
    - docs/RUNBOOK.md (the prod deploy / gated-deploy procedure + Railway middleware deploy notes)
    - .planning/STATE.md (deployment reality: no separate staging middleware; a prod force-push IS a middleware deploy; staging site calls the prod middleware)
  </read_first>
  <what-built>
    57-01 shipped the client-error beacon (frontend) and the device-token-gated, rate-limited `POST /api/kiosk/client-error` endpoint (middleware) with PII scrub + Sentry capture. The endpoint is a no-side-effect 204 telemetry sink — but it is a NEW public (device-gated) route that goes live for real customers the moment the middleware deploys.
  </what-built>
  <how-to-verify>
    1. Push the frontend + middleware changes to staging first (staging front-end + build artifacts), confirm the staging kiosk loads with no console errors.
    2. Deploy to production per docs/RUNBOOK.md (gated-deploy / prod force-push → Railway redeploy). Confirm the middleware redeployed (Railway shows the new deploy; `/health` returns 200 with redis:true).
    3. Fire one deliberate test beacon from the live kiosk (or curl through the browser session with the device token) and confirm ONE `source: kiosk-client` event lands in Sentry. This proves the pipe works end to end BEFORE relying on it to catch the real bug.
    4. Watch Sentry briefly for any unexpected volume (DoS/limiter sanity).
  </how-to-verify>
  <resume-signal>Type "deployed" once the prod middleware is live and a test beacon was confirmed in Sentry, or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture a REAL sale-blocking occurrence on the live iPad</name>
  <read_first>
    - .planning/todos/pending/kiosk-sale-requires-refresh-recurring.md (the four hypotheses + the discriminating fields)
  </read_first>
  <what-built>
    The live beacon now captures {message, http_status, endpoint, auth_state, timestamp, user_agent} on every kiosk failure.
  </what-built>
  <how-to-verify>
    Reproduce (or await) the "sale won't work until refresh" failure on the real iPad, then read the captured beacon in Sentry. Try to FORCE it:
    1. Let the iPad sleep / background the tab for an extended period, kill wifi, then return and immediately try to start a sale (the reported real-world trigger).
    2. Separately, exercise a STALE-TOKEN / stale-session scenario (leave the kiosk idle long enough that the device token/session may have expired) and try to start a sale.
    3. When the grid/sale fails, DO NOT reflexively hit Retry until you have noted whether Retry vs a full refresh is what recovers it.
    4. In Sentry, open the `source: kiosk-client` occurrence and copy out the exact fields: message, http_status, endpoint, auth_state, user_agent, timestamp — plus note observationally which recovery worked (in-place Retry vs manual page reload).
    Paste the captured fields + the Retry-vs-refresh observation as the resume evidence.
  </how-to-verify>
  <resume-signal>Paste the captured beacon fields (message / http_status / endpoint / auth_state) and whether Retry or a full refresh recovered it. If it could not be reproduced this session, say so and the checkpoint stays open.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the confirmed cause in 57-DIAGNOSIS.md</name>
  <files>.planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md</files>
  <read_first>
    - .planning/todos/pending/kiosk-sale-requires-refresh-recurring.md (the four hypotheses, each with file:line, so the diagnosis maps evidence → winning hypothesis)
    - js/kiosk-core.js (kioskLoadProducts + wake listeners ~770-860, and the sale-submit path — to tie the captured endpoint/status to the actual code path)
    - zoho-middleware/lib/authTiers.js (device-token tier — relevant if the captured http_status is 401/403)
  </read_first>
  <action>
    Using ONLY the captured evidence from Task 2 (not inference), author 57-DIAGNOSIS.md with: (1) the raw captured beacon fields; (2) the Retry-vs-refresh observation; (3) which of the four hypotheses the evidence confirms — H1 auth/token expiry (401/403 on /api/kiosk/products, auth_state stale), H2 wake-retry listeners dead on the real iPad (fetch reject / TypeError, endpoint catalog, and Retry works but auto-retry never fired), H3 wrong surface (endpoint is /api/kiosk/sale not products), or H4 persistent server-side condition (5xx where Retry also fails); (4) the "Confirmed cause" stated in one sentence; (5) the prescribed 57-03 fix branch — the specific code change and the regression test that must reproduce the cause. If the evidence contradicts all four hypotheses, record the actual observed cause and prescribe the fix accordingly. If Task 2 could not reproduce, record "not yet captured" and keep 57-03 blocked — do NOT invent a cause.
  </action>
  <verify>
    <automated>test -f .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md && grep -q "Confirmed cause" .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - 57-DIAGNOSIS.md exists and contains the raw captured beacon fields, the named winning hypothesis, a one-sentence "Confirmed cause", and the prescribed 57-03 fix branch + the regression test it must write.
    - No claim in the file is inference-only; every conclusion cites a captured field or a live observation.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → prod | 57-01's endpoint goes live for real customers on deploy (no staging middleware). The deploy itself crosses into the live money surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Information Disclosure | live client-error endpoint | mitigate | The endpoint's scrub/whitelist/rate-limit (T-57-01/02/03 from 57-01) is already in place before this deploy; the deploy is preceded by a Sentry-volume watch so a misbehaving beacon is caught early. |
| T-57-02-02 | Denial of Service | prod middleware post-deploy | accept | clientErrorLimiter + apiLimiter bound the new route; owner watches `/health` and Sentry volume for the first period after cutover. |
| T-57-02-SC | Tampering (supply chain) | deploy | accept | No package changes deploy in this plan — it ships the already-audited 57-01 code. |
</threat_model>

<verification>
- 57-DIAGNOSIS.md exists with a "Confirmed cause" line backed by captured beacon fields.
- A `source: kiosk-client` occurrence is visible in Sentry from the live iPad.
- The prod `/health` endpoint returns 200 (redis:true) after the deploy.
</verification>

<success_criteria>
Satisfies Phase 57 SC#2 (real cause diagnosed from a captured occurrence, recorded before any fix) and the deploy-half of SC#4 (57-01 verified reachable on the live iPad against the prod middleware). SC#3 (the fix) is 57-03, which is BLOCKED on 57-DIAGNOSIS.md.
</success_criteria>

<output>
Create `.planning/phases/57-kiosk-sale-blocking-recovery/57-02-SUMMARY.md` when done, and ensure `57-DIAGNOSIS.md` is committed.
</output>
