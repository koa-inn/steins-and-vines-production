---
phase: 57-kiosk-sale-blocking-recovery
plan: 03
type: execute
wave: 3
depends_on: [57-02]
files_modified:
  - js/kiosk-core.js
  - tests/frontend/kiosk-sale-recovery.test.js
  - js/main.js
  - js/main.min.js
  - js/kiosk-core.min.js
autonomous: false

requirements: [REVIEW-01]

must_haves:
  truths:
    - "A regression test written FIRST (RED) reproduces the CONFIRMED cause from 57-DIAGNOSIS.md before any fix code exists"
    - "The fix targets the cause 57-02 confirmed — not any of the four un-selected hypotheses"
    - "On the real iPad, a kiosk sale can be started without a manual page refresh after the failure condition recurs"
  artifacts:
    - path: "js/kiosk-core.js"
      provides: "The confirmed-cause fix (branch selected by 57-DIAGNOSIS.md)"
    - path: "tests/frontend/kiosk-sale-recovery.test.js"
      provides: "RED-first regression test reproducing the confirmed cause"
  key_links:
    - from: ".planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md"
      to: "the fix branch executed here"
      via: "confirmed-cause selection"
      pattern: "Confirmed cause"
---

<objective>
Fix the confirmed cause — the one 57-02 captured and recorded in `57-DIAGNOSIS.md` — with a regression test written first, and prove the auto-recovery works on the real iPad so staff no longer need a manual refresh.

## ⚠ THIS PLAN IS A SCAFFOLD — FINALIZE IT AFTER 57-02

Per the phase's honest debug structure, the fix cannot be fully specified before the diagnosis exists. This file lays out ONE branch per hypothesis, each clearly marked "select per 57-02 diagnosis." Do NOT execute more than one branch.

**Preferred path:** once `57-DIAGNOSIS.md` lands, re-run `/gsd:plan-phase 57` to collapse this plan to the single confirmed branch (a clean, un-branched fix plan). If executing directly instead, Task 0 gates on the diagnosis and selects the branch; execute only that branch.

Purpose: Satisfy Success Criteria #3 and #4 — the confirmed cause is fixed RED-first and the recovery actually works on the live device.
Output: A regression test + the targeted fix + a live-iPad confirmation that no manual refresh is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/kiosk-sale-requires-refresh-recurring.md
@.planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md
@CLAUDE.md

<interfaces>
js/kiosk-core.js is ES5. Relevant seams for each hypothesis:
  - H1 (auth/token expiry): `_kcEnv.buildAuthOptions()` (auth seam), `kioskLoadProducts` catch on HTTP 401/403 (~801). A re-auth/token-refresh on 401 before retry would live around the catch.
  - H2 (wake-retry dead): `kioskRetryStalledLoads` + the visibilitychange/online listeners (~842-860) — the self-heal that may never fire on a real iPad (bfcache/full reload discards listeners).
  - H3 (wrong surface): the failing call is the sale POST, not the catalog. The exact endpoint + catch site MUST be taken from `57-DIAGNOSIS.md` (the beacon records the real endpoint), NOT re-derived from a grep. The candidate sale catches are kiosk-core.js:2596 (`/api/kiosk/sale/confirm`) and 2570 (`/api/kiosk/recipe-sale/confirm`) — the diagnosis names which one actually fired.
  - H4 (server-side): a persistent 5xx where Retry also fails — fix may be middleware-side (routes/pos.js or routes/catalog.js), which would change this plan's files_modified.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Confirm diagnosis exists and select the fix branch</name>
  <read_first>
    - .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md (the "Confirmed cause" + prescribed fix branch — this MUST exist; if it does not, STOP: 57-02 is not complete)
  </read_first>
  <decision>Which confirmed-cause branch does 57-DIAGNOSIS.md prescribe?</decision>
  <context>
    57-03 must fix the cause 57-02 captured, not a guess. If 57-DIAGNOSIS.md is absent or records "not yet captured", this plan cannot proceed — return to 57-02. If it exists, its prescribed fix branch selects exactly one of the options below (or an "other/actual observed cause" branch the diagnosis wrote itself).
  </context>
  <options>
    <option id="h1-auth">
      <name>H1 — auth/token expiry (lead suspect)</name>
      <pros>Explains why a page refresh (re-runs init, re-presents the token) fixes it but in-place Retry does not.</pros>
      <cons>The fix touches the auth seam; must NOT weaken the device-token gate or broaden KIOSK_ROUTES.</cons>
    </option>
    <option id="h2-wake">
      <name>H2 — wake-retry never fires on the real iPad</name>
      <pros>Explains why only a manual reload works if iOS discarded the tab/listeners.</pros>
      <cons>Recovery must be re-armed in a way that survives bfcache/full reload (e.g. pageshow, focus, a poll on interaction).</cons>
    </option>
    <option id="h3-surface">
      <name>H3 — failing surface is the sale POST, not the catalog</name>
      <pros>Matches "a sale won't work" being broader than "the grid is empty".</pros>
      <cons>Fix + regression test target the sale-submit path, not kioskLoadProducts.</cons>
    </option>
    <option id="h4-server">
      <name>H4 — persistent server-side condition (Railway cold-start / Redis / cache miss)</name>
      <pros>Explains cases where Retry also fails.</pros>
      <cons>Fix is middleware-side — this plan's files_modified and threat_model must be re-planned to the middleware.</cons>
    </option>
  </options>
  <resume-signal>State the branch id from 57-DIAGNOSIS.md (h1-auth / h2-wake / h3-surface / h4-server / other), or "blocked — no diagnosis" to return to 57-02.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: RED-first regression test + targeted fix for the confirmed cause</name>
  <files>js/kiosk-core.js, tests/frontend/kiosk-sale-recovery.test.js</files>
  <read_first>
    - .planning/phases/57-kiosk-sale-blocking-recovery/57-DIAGNOSIS.md (the confirmed cause + prescribed fix)
    - js/kiosk-core.js (the code path named by the selected branch — see the <interfaces> map above)
    - tests/frontend/kiosk-load-recovery.test.js (harness for simulating fetch reject / non-ok status / visibilitychange / online)
    - CLAUDE.md (regression test FIRST, then fix; ES5 only; one logical change per commit; do not edit existing tests)
  </read_first>
  <behavior>
    RED first — author tests/frontend/kiosk-sale-recovery.test.js that reproduces the CONFIRMED cause from 57-DIAGNOSIS.md and FAILS against current code. Branch guidance (execute only the selected one):
    - h1-auth: simulate the catalog/sale fetch returning 401/403 with a stale auth_state; assert the kiosk re-establishes auth (re-presents/refreshes the token via the auth seam) and the follow-up request succeeds WITHOUT a full page reload.
    - h2-wake: simulate the wake path that the real iPad actually fires (per the diagnosis — e.g. pageshow/focus) after a failed load; assert a stalled load auto-retries on that event, not only on visibilitychange/online.
    - h3-surface: simulate the sale-submit fetch failing; assert the sale path surfaces a recoverable retry and self-heals without a manual refresh.
    - h4-server: (middleware) reproduce the persistent 5xx/cache-miss condition in a middleware test and assert the server now serves a good response (this changes files_modified to the middleware — re-plan).
    Then GREEN — implement the minimal fix the diagnosis prescribes until the test passes.
  </behavior>
  <action>
    Write the failing regression test FIRST (RED) reproducing the confirmed cause, commit it (`test(57-03): reproduce <confirmed cause>`). Then implement ONLY the prescribed fix (GREEN), ES5, one logical change, commit separately (`fix(57-03): <confirmed cause>`). Do not implement any un-selected branch. Do not weaken the device-token auth gate, the KIOSK_ROUTES allowlist, or the v4.2/Phase-48 money-path hardening. Do not edit existing tests.
  </action>
  <verify>
    <automated>npx jest tests/frontend/kiosk-sale-recovery.test.js && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - tests/frontend/kiosk-sale-recovery.test.js exists, failed before the fix (RED), passes after (GREEN), and reproduces the exact confirmed cause named in 57-DIAGNOSIS.md.
    - The fix touches only the code path of the selected branch; no un-selected hypothesis is implemented.
    - `npm run lint` (ES5) passes; existing kiosk tests still green.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Rebuild, deploy, and re-verify on the live iPad (SC#4)</name>
  <read_first>
    - CLAUDE.md (npm run build regenerates artifacts as a separate commit; run both suites + lint before commit; all changes to staging first)
    - docs/RUNBOOK.md (prod deploy procedure)
    - .planning/STATE.md (anti-pattern: a green suite proved nothing last time — 7cbf856 passed its tests and still failed in the store; verify against the live iPad)
  </read_first>
  <what-built>
    The confirmed-cause fix + its RED-first regression test. `npm run build` regenerated js/main.min.js and js/kiosk-core.min.js.
  </what-built>
  <how-to-verify>
    1. Run the full gate: `npm run build`, `npm test`, `cd zoho-middleware && npm test`, `npm run lint` — all green; commit the rebuilt artifacts separately.
    2. Deploy to staging, then production per docs/RUNBOOK.md (prod middleware only if the branch was h4-server).
    3. On the REAL iPad, recreate the failure condition that 57-02 captured (sleep + wifi-kill + return, and/or the stale-token idle scenario).
    4. Confirm a kiosk sale can now be STARTED without a manual page refresh — the auto-recovery (re-auth / wake-retry / self-heal, per the branch) fires on its own.
    5. Confirm no new `source: kiosk-client` beacon fires for the old cause on that occurrence (the lens should now go quiet for this failure).
  </how-to-verify>
  <resume-signal>Type "verified" once the live iPad starts a sale without a manual refresh under the reproduced condition, or describe the residual failure (which reopens diagnosis).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk browser → prod middleware | The money surface. Any fix to the auth/retry path must not weaken the device-token gate or the v4.2/Phase-48 money-path hardening. |

## STRIDE Threat Register

> Finalize this register against the selected branch once 57-DIAGNOSIS.md lands (h4-server moves the surface to the middleware).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-03-01 | Elevation of Privilege | auth-seam / retry fix (h1/h2) | mitigate | A re-auth-on-401 or re-armed wake-retry must re-present the EXISTING device token/session only — never broaden KIOSK_ROUTES, never expose or log the token value. Regression test asserts auth is still required. |
| T-57-03-02 | Tampering | money path | mitigate | The fix must not alter the terminal-charge → Zoho invoice → void-on-failure path hardened in v4.2/Phase 48; scope the change to load/auth-recovery only. |
| T-57-03-03 | Denial of Service | retry loop | mitigate | Any added auto-retry must be guarded (bounded, in-flight-guarded like kioskRetryStalledLoads) so a persistent failure cannot stampede the middleware. |
| T-57-03-SC | Tampering (supply chain) | npm installs | accept | No new packages expected; if the confirmed fix requires one, run the Package Legitimacy Gate before install. |
</threat_model>

<verification>
- tests/frontend/kiosk-sale-recovery.test.js reproduces the confirmed cause (RED→GREEN).
- Full `npm test`, `cd zoho-middleware && npm test`, `npm run lint` green.
- Live iPad: the reproduced failure condition no longer requires a manual refresh to start a sale.
</verification>

<success_criteria>
Satisfies Phase 57 SC#3 (confirmed cause fixed, regression test first, auto-recovery works on the real device) and SC#4 (verified on the live iPad against the prod middleware — not merely a green suite).
</success_criteria>

<output>
Create `.planning/phases/57-kiosk-sale-blocking-recovery/57-03-SUMMARY.md` when done.
</output>
