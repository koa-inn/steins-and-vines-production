---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/validateEnv.js
autonomous: false
requirements: [GIFTCARD-01]
user_setup:
  - service: zoho-books
    why: "Gift-card sale must post to a dedicated income account (deferred to liability via a periodic manual journal) per D-04/R-01; the dedicated Zoho item drives this."
    env_vars:
      - name: KIOSK_GIFT_CARD_ITEM_ID
        source: "Zoho Books → Items → 'Gift Certificate' item → item_id (from item URL or GET /items)"
    dashboard_config:
      - task: "Create income account 'Gift Card Sales' (type: Income)"
        location: "Zoho Books → Chart of Accounts → New Account"
      - task: "Create item 'Gift Certificate' (Sales type, NOT inventory-tracked) mapped to 'Gift Card Sales' income account, tax = 0% / exempt"
        location: "Zoho Books → Items → New Item"
      - task: "Confirm 'Gift Cards Sold' Current-Liability account exists (owner reports already created) — it is the periodic deferral-journal target, NOT mapped on the item"
        location: "Zoho Books → Chart of Accounts"
      - task: "Set KIOSK_GIFT_CARD_ITEM_ID env var to the new item's item_id"
        location: "Railway dashboard → zoho-middleware service → Variables"

must_haves:
  truths:
    - "D-04: a gift-card sale posts to the dedicated 'Gift Card Sales' income account (via KIOSK_GIFT_CARD_ITEM_ID), isolating it for the periodic deferral journal that moves unredeemed balance to the 'Gift Cards Sold' liability"
    - "D-03: the 'Gift Certificate' Zoho item is set to 0% / exempt tax so a sale carries no GST/PST"
    - "R-01: KIOSK_GIFT_CARD_ITEM_ID is set in Railway production so the issue/redeem routes do not fail-closed (503)"
    - "Open Question 2: a POST /customerpayments with payment_mode:'others' posts cleanly against a test invoice in S&V's Zoho (no API error)"
    - "Open Question 1: whether ZOHO_TAX_ZERO_ID is required, or the item's own 0% tax suffices, is determined and recorded"
    - "R-01: the recurring (~monthly) deferral-journal practice is established with the owner/bookkeeper"
  artifacts:
    - path: "zoho-middleware/lib/validateEnv.js"
      provides: "KIOSK_GIFT_CARD_ITEM_ID registered as an OPTIONAL env var with a startup warning"
      contains: "KIOSK_GIFT_CARD_ITEM_ID"
  key_links:
    - from: "Railway env KIOSK_GIFT_CARD_ITEM_ID"
      to: "zoho-middleware/routes/gift-cards.js issue route fail-closed guard"
      via: "process.env.KIOSK_GIFT_CARD_ITEM_ID"
      pattern: "KIOSK_GIFT_CARD_ITEM_ID"
---

<objective>
Establish the one-time owner setup and runtime environment that all gift-card accounting depends on, and verify the two Zoho behaviours the middleware design assumes. Without this plan, every downstream gift-card route fails closed (503) or posts to the wrong account.

Purpose: Lock the R-01 accounting mechanism (income-mapped item + periodic deferral journal) and de-risk the two open Zoho questions (payment_mode:'others' acceptance; zero-tax mechanism) before any code calls Zoho.
Output: Zoho "Gift Card Sales" income account + "Gift Certificate" item, KIOSK_GIFT_CARD_ITEM_ID set on Railway, validateEnv registration, and recorded answers to Open Questions 1 & 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-CONTEXT.md

<interfaces>
<!-- validateEnv OPTIONAL array shape — extracted from zoho-middleware/lib/validateEnv.js line 64 -->
OPTIONAL entries look like:
  { name: 'KIOSK_CONTACT_ID', desc: 'Zoho contact ID for kiosk walk-in sales' }
Add a sibling entry for KIOSK_GIFT_CARD_ITEM_ID after line 64.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Owner Zoho Books setup + Railway env var</name>
  <files>(no repo files — Zoho Books UI + Railway dashboard)</files>
  <read_first>
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md (R-01 ⚠ DECISION UPDATE block, lines 70-82, and "Human setup required" lines 176-186 — these are authoritative and SUPERSEDE the original liability-item approach)
  </read_first>
  <what-built>
    This step is owner-only Zoho/Railway configuration (no CLI/API path Claude can drive — it is dashboard work in the owner's authenticated Zoho Books + Railway accounts).
  </what-built>
  <action>
    Owner performs, in order: (1) Zoho Books → Chart of Accounts → create income account "Gift Card Sales" (type: Income). (2) Confirm the existing "Gift Cards Sold" Current-Liability account is present (owner reports already created) — it stays as the periodic-journal target and is NOT mapped on the item. (3) Zoho Books → Items → New Item "Gift Certificate": item type = Sales (NOT inventory-tracked), Account = "Gift Card Sales" (income — a liability account is not selectable on a sales item in this org), tax = 0% / exempt (satisfies D-03). (4) Copy the new item's item_id. (5) Railway → zoho-middleware service → Variables → set KIOSK_GIFT_CARD_ITEM_ID to that item_id. Per D-04, the item maps to INCOME (not liability); D-04 is satisfied on an accrual basis by the periodic deferral journal in Task 3, not by the item's account.
  </action>
  <acceptance_criteria>
    - A "Gift Card Sales" income account exists in Zoho Books Chart of Accounts.
    - A "Gift Certificate" Sales item exists, mapped to "Gift Card Sales", with tax set to 0% / exempt, not inventory-tracked.
    - KIOSK_GIFT_CARD_ITEM_ID is set in Railway production to the item's item_id (owner confirms the value is present, non-empty).
    - Owner confirms the "Gift Cards Sold" Current-Liability account exists for the deferral journal target.
  </acceptance_criteria>
  <resume-signal>Type "setup done" and paste the KIOSK_GIFT_CARD_ITEM_ID value (or confirm it is set), or describe any blocker (e.g. liability account selectable on item — would change R-01).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Wave-0 Zoho API probes (others tender + zero-tax mechanism)</name>
  <files>(no repo files — Zoho API probe against a test invoice)</files>
  <read_first>
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md (Open Questions 1 & 2, lines 733-741; Assumptions A1/A2, lines 720-721)
    - zoho-middleware/routes/pos.js (zohoPost('/customerpayments', ...) usage, lines 626-666 — the surface the probe exercises)
  </read_first>
  <what-built>
    Two runtime confirmations that the R-01/R-03 design assumptions hold in S&V's actual Zoho Books account. These cannot be unit-tested (they exercise the live Zoho tenant) and must be answered before the redemption split-tender (44-04) is implemented.
  </what-built>
  <action>
    With the owner's authenticated Zoho session (owner-driven, or Claude-driven if Zoho MCP/credentials are available in-session): (1) Create or pick a throwaway test invoice, then POST /customerpayments with payment_mode:'others' and a small amount_applied against it; confirm Zoho accepts it (200/201) and records the payment without erroring, and note which clearing account "others" posts to (A1). (2) Determine whether a zero-tax invoice line requires ZOHO_TAX_ZERO_ID or whether the "Gift Certificate" item's own 0%/exempt tax produces a $0 tax line on its own (A2/Open Question 1): create a one-line draft invoice using KIOSK_GIFT_CARD_ITEM_ID with no explicit tax_id and confirm tax = $0. Record both answers in the plan SUMMARY so 44-03/44-04 can hard-wire the correct approach. Delete/void the test invoice(s) afterward.
  </action>
  <acceptance_criteria>
    - Recorded answer: payment_mode:'others' against an invoice returns success (not an error) in S&V's Zoho — or, if it errors, the fallback mode is documented (blocks 44-04 design).
    - Recorded answer: zero-tax at sale is achieved by the item's own 0% tax (ZOHO_TAX_ZERO_ID not required) OR ZOHO_TAX_ZERO_ID is confirmed present in Railway — exactly one path is chosen and written down.
    - Any test invoices created for the probe are voided/deleted.
  </acceptance_criteria>
  <resume-signal>Type "probes done" with the two recorded answers (others: accepted/rejected; zero-tax: item-own / needs ZOHO_TAX_ZERO_ID), or describe a blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Register KIOSK_GIFT_CARD_ITEM_ID in validateEnv + record deferral-journal cadence</name>
  <files>zoho-middleware/lib/validateEnv.js</files>
  <read_first>
    - zoho-middleware/lib/validateEnv.js (the OPTIONAL array; the KIOSK_CONTACT_ID entry at line 64 is the exact shape to mirror)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md ("validateEnv.js — OPTIONAL entry for new env var", lines 750-757)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md (Pitfall 4, lines 611-619 — fail-closed rationale)
  </read_first>
  <action>
    Add KIOSK_GIFT_CARD_ITEM_ID to the OPTIONAL env array in validateEnv.js (sibling to KIOSK_CONTACT_ID at line 64), desc: "Zoho item ID for gift certificate sales (maps to Gift Card Sales income account); gift-card issue/redeem routes fail-closed 503 without it". Keep it OPTIONAL (startup warning only, not REQUIRED — the routes enforce fail-closed at request time per R-01). In the plan SUMMARY, document the recurring (~monthly) manual deferral-journal practice the owner/bookkeeper must run: Dr "Gift Card Sales" income, Cr "Gift Cards Sold" liability, for the unredeemed balance taken from the GiftCards sheet (this is the human accounting cadence that satisfies D-04 on an accrual basis; it is NOT code in v1).
  </action>
  <acceptance_criteria>
    - `grep -n KIOSK_GIFT_CARD_ITEM_ID zoho-middleware/lib/validateEnv.js` returns a line inside the OPTIONAL array.
    - `cd zoho-middleware && npm test` exits 0 (no validateEnv test regressions).
    - `cd zoho-middleware && npm run lint` is clean.
    - Plan SUMMARY documents the monthly deferral-journal cadence (account names + direction + source = GiftCards sheet outstanding balance).
  </acceptance_criteria>
  <verify>
    <automated>cd zoho-middleware && npm test 2>&1 | tail -5 && grep -c KIOSK_GIFT_CARD_ITEM_ID lib/validateEnv.js</automated>
  </verify>
  <done>KIOSK_GIFT_CARD_ITEM_ID is a registered OPTIONAL env var; middleware tests + lint green; deferral-journal cadence recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware → Zoho Books | Gift-card sale/redeem money posting crosses here; misconfiguration silently breaks liability accounting |
| owner dashboard → runtime env | KIOSK_GIFT_CARD_ITEM_ID is the single config that gates all gift-card money flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Gift-card sale posts to wrong/generic income account | mitigate | Dedicated KIOSK_GIFT_CARD_ITEM_ID item isolates gift sales; fail-closed 503 (44-03) if unset (Pitfall 4) |
| T-44-02 | Information disclosure | ZOHO_TAX_ZERO_ID / item misconfig charges tax at sale (violates D-03) | mitigate | Task 2 probe confirms zero-tax mechanism before any sale route ships |
| T-44-03 | Repudiation | Liability never recognized (D-04) if income isolation breaks | accept | Periodic manual deferral journal by owner/bookkeeper; GiftCards sheet is the outstanding-balance source of truth |
| T-44-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase (RESEARCH: "no new npm dependencies") |
</threat_model>

<verification>
- `grep -n KIOSK_GIFT_CARD_ITEM_ID zoho-middleware/lib/validateEnv.js` → present in OPTIONAL array.
- Owner confirms Zoho item + income account + Railway env var.
- Open Questions 1 & 2 answered and recorded in SUMMARY.
- `cd zoho-middleware && npm test && npm run lint` clean.
</verification>

<success_criteria>
- KIOSK_GIFT_CARD_ITEM_ID set in Railway and registered in validateEnv.
- Zero-tax-at-sale mechanism and payment_mode:'others' acceptance are confirmed and documented.
- Deferral-journal cadence established with the owner.
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-01-SUMMARY.md` when done. MUST record the answers to Open Questions 1 & 2 and the deferral-journal cadence (downstream plans depend on them).
</output>
