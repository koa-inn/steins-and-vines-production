---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: 05
type: execute
wave: 3
depends_on: ["44-03"]
files_modified:
  - zoho-middleware/routes/gift-cards.js
  - zoho-middleware/__tests__/gift-cards.test.js
autonomous: true
requirements: [GIFTCARD-01]

must_haves:
  truths:
    - "D-06: reload adds value to an existing cert — POST /api/kiosk/gift-card/reload increments the balance atomically (Apps Script) with the same liability+zero-tax accounting as a sale (D-03/D-04)"
    - "D-06: void cancels a cert — POST /api/kiosk/gift-card/void sets status='void' so a voided cert returns invalid_status on any later redeem"
    - "D-04: reload posts via KIOSK_GIFT_CARD_ITEM_ID to the Gift Card Sales income account; reload fails-closed 503 if KIOSK_GIFT_CARD_ITEM_ID is unset"
    - "D-05: reload amount is server-validated (bounds + sane max balance); the client never sets the new balance directly"
  artifacts:
    - path: "zoho-middleware/routes/gift-cards.js"
      provides: "reload + void routes appended to the existing router"
      contains: "reload_gift_card"
  key_links:
    - from: "gift-cards.js reload route"
      to: "Apps Script reload_gift_card + zohoPost invoice+payment"
      via: "axios.post + zohoPost"
      pattern: "reload_gift_card"
    - from: "gift-cards.js void route"
      to: "Apps Script void_gift_card"
      via: "axios.post"
      pattern: "void_gift_card"
---

<objective>
Complete the lifecycle middleware with reload (add value to an existing cert — same liability + zero-tax accounting as a sale) and void (cancel a cert). This is GIFTCARD-01d (reload) + 01e (void) at the middleware tier. Reload mirrors the issue money flow but increments an existing balance; void is a status-only state change.

Purpose: Full lifecycle (D-06) — staff can top up and cancel certificates with the same server-authoritative, fail-closed guarantees as issuance.
Output: reload + void routes appended to gift-cards.js, with tests added to gift-cards.test.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-03-SUMMARY.md

<interfaces>
<!-- Apps Script contract (from 44-02) -->
reload_gift_card {cert_number, amount, transaction_ref} -> {ok:true, new_balance, status} | {ok:false, error:'not_found'|'invalid_status'}
void_gift_card   {cert_number, reason} -> {ok:true} | {ok:false, error:'not_found'}

<!-- gift-cards.js (from 44-03) already has: imports, fail-closed env-guard helper, axios Apps Script POST helper, zohoPost invoice+payment pattern, eventLog, log prefixes, module.exports=router -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gift-cards.js — reload + void routes</name>
  <files>zoho-middleware/routes/gift-cards.js</files>
  <behavior>
    - reload: valid cert + amount in (0, 2000] → Apps Script reload_gift_card, then zohoPost invoice (KIOSK_GIFT_CARD_ITEM_ID, zero tax, reference=cert#) + customerpayment(creditcard, amount) → 200 {ok, cert_number, new_balance}
    - reload: KIOSK_GIFT_CARD_ITEM_ID unset → 503 (fail-closed, same as issue)
    - reload: unknown cert (Apps Script not_found) → 404; voided cert (invalid_status) → 409
    - reload: amount ≤ 0 / > 2000 / NaN → 400
    - reload: Zoho fails after the balance increment → log CRITICAL (the increment already happened; do NOT auto-reverse — surface for manual fix) and return 502 with a warning flag
    - void: known cert → 200 {ok}; unknown → 404; requires a reason string (sanitized)
  </behavior>
  <read_first>
    - zoho-middleware/routes/gift-cards.js (the issue route from 44-03 — reuse its env-guard, axios Apps Script POST helper, zohoPost invoice+payment, error handling)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md (Specific Ideas line 92 "Reload = add value ... same accounting as a sale: liability up, no tax"; actions table lines 221-232; Pitfall 4 fail-closed)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md (Apps Script POST pattern lines 117-142; zohoPost invoice+payment lines 146-173)
  </read_first>
  <action>
    Append two routes to gift-cards.js. POST /api/kiosk/gift-card/reload: fail-closed 503 if !KIOSK_GIFT_CARD_ITEM_ID; validate cert /^GC-\d{6}$/ + amount in (0, 2000]; call Apps Script reload_gift_card (map not_found→404, invalid_status→409); on ok, zohoPost('/invoices', ...) with KIOSK_GIFT_CARD_ITEM_ID zero-tax line (reference=cert#, name noting 'reload') + zohoPost('/customerpayments', creditcard, amount); on Zoho failure AFTER the increment, log '[gift-cards/reload] CRITICAL' (the balance is already up — do not silently reverse; return 502 with a needs_manual_review flag so staff can reconcile). eventLog.logEvent('kiosk.gift_card_reloaded', {certNumber, amount, newBalance}). NOTE the ordering asymmetry vs redemption: reload increments FIRST (so the customer's added value is never lost) then records Zoho; document this choice in the SUMMARY. POST /api/kiosk/gift-card/void: validate cert; require a non-empty reason (sanitized/sliced); call Apps Script void_gift_card (not_found→404); 200 {ok} on success. Use log prefixes [gift-cards/reload] and [gift-cards/void].
  </action>
  <acceptance_criteria>
    - `grep -c "gift-card/reload\|gift-card/void" zoho-middleware/routes/gift-cards.js` ≥ 2.
    - reload handler contains the `status(503)` fail-closed guard (grep).
    - `grep -n "reload_gift_card\|void_gift_card" zoho-middleware/routes/gift-cards.js` both present.
    - `cd zoho-middleware && node -e "require('./routes/gift-cards.js')"` loads without throwing.
  </acceptance_criteria>
  <verify>
    <automated>cd zoho-middleware && node -e "require('./routes/gift-cards.js'); console.log('loads')" && grep -c "gift-card/reload\|gift-card/void" routes/gift-cards.js</automated>
  </verify>
  <done>reload (increment + sale accounting, fail-closed) and void (status change) routes appended and load cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend gift-cards.test.js with reload + void cases</name>
  <files>zoho-middleware/__tests__/gift-cards.test.js</files>
  <behavior>
    - reload valid → 200, calls reload_gift_card then zohoPost invoice + payment (zero-tax, amount)
    - reload with item id unset → 503, no Apps Script/Zoho call
    - reload unknown cert → 404; reload voided cert (invalid_status) → 409
    - reload amount 0 / 2001 / 'x' → 400
    - reload where zohoPost rejects after increment → 502 with needs_manual_review and CRITICAL logged (increment NOT reversed)
    - void known → 200; void unknown → 404; void missing reason → 400
  </behavior>
  <read_first>
    - zoho-middleware/__tests__/gift-cards.test.js (the issue/lookup/next-number suite from 44-03 — extend it, do not duplicate the harness/mocks)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-RESEARCH.md (Validation Architecture test map lines 782-802)
  </read_first>
  <action>
    Extend the existing gift-cards.test.js (added in 44-03) with describe blocks for reload and void, reusing the same axios + zoho-api mocks. Cover all behaviors above. For the reload-Zoho-failure case, assert the response is 502 with a needs_manual_review flag and that the reload_gift_card mock WAS called (increment happened first) — documenting the deliberate ordering asymmetry vs redemption. Keep everything under the `gift-card` testPathPattern. Do NOT modify the 44-03 cases (CLAUDE.md: don't modify existing tests unless asked) — only add.
  </action>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- gift-card` exits 0 with the new reload/void cases passing alongside the 44-03 cases.
    - The reload 503 case asserts no Apps Script/Zoho call.
    - The reload-Zoho-failure case asserts reload_gift_card was called (increment-first) and the response carries needs_manual_review.
    - `cd zoho-middleware && npm test && npm run lint` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd zoho-middleware && npm test -- gift-card 2>&1 | tail -15</automated>
  </verify>
  <done>reload + void fully covered; full middleware suite + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk/admin client → /api/kiosk/gift-card/reload|void | untrusted amount + cert + reason cross here |
| middleware → Zoho + Apps Script | reload money posting + balance mutation cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-18 | Tampering | Reload with negative/huge amount inflates balance | mitigate | amount bounds (0, 2000] fail-closed at route entry; Apps Script increments only the validated amount |
| T-44-19 | Tampering | Reload to a voided/unknown cert | mitigate | Apps Script invalid_status→409 / not_found→404; status checked under lock |
| T-44-20 | Repudiation | Reload increment without Zoho record | accept | Increment-first ordering keeps customer value safe; Zoho failure logs CRITICAL + needs_manual_review for staff reconciliation (low volume, owner-visible sheet) |
| T-44-21 | Elevation of privilege | Unauthorized void cancels a live cert | mitigate | void route behind MW_API_KEY (/api/kiosk/*); Apps Script void_gift_card also staff/server-token gated; reason recorded for audit |
| T-44-22 | Tampering | Reload not configured posts to wrong account | mitigate | Same fail-closed 503 KIOSK_GIFT_CARD_ITEM_ID guard as issue (Pitfall 4) |
| T-44-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase |
</threat_model>

<verification>
- `cd zoho-middleware && npm test -- gift-card` green (issue/lookup/next from 44-03 + reload/void).
- `cd zoho-middleware && npm test && npm run lint` clean.
- grep confirms reload fail-closed 503 + both Apps Script actions wired.
</verification>

<success_criteria>
- Reload (add value, sale accounting, fail-closed) and void (cancel) routes live and tested.
- Full lifecycle (D-06) is now complete at the middleware tier; balances remain server-authoritative (D-05).
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-05-SUMMARY.md` when done. Record the reload/void request+response shapes (44-07 UI consumes them) and the deliberate increment-first ordering rationale.
</output>
