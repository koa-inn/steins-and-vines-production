---
status: partial
phase: 44-kiosk-gift-card-certificate-lifecycle
source: [44-08-PLAN.md Task 2 — iPad Safari UAT]
started: 2026-06-29
updated: 2026-06-29
---

## Summary

ROUND 1 (2026-06-29): UAT halted on step 1 — blocking money-path defect G-44-01 (issuance
recorded a phantom paid Zoho invoice with no real Helcim charge). Owner chose the
Cart + terminal checkout fix model. → gap-closure plans 44-09 + 44-10.

ROUND 2 (2026-06-29, after 44-09/44-10 deployed to staging + prod middleware): owner re-tested
the visible/no-charge behavior on staging and it looks good. G-44-01 RESOLVED. The remaining
items require an ACTUAL terminal sale (real card charge) and are DEFERRED by the owner to a
later date — see "Deferred (live terminal sale)" below. Backend re-verified live post-deploy:
phantom `/issue` + `/reload` routes now return 404 (decommissioned); `next-number` → GC-000001.

total: 7
passed: visible/non-charge behavior (issue-to-cart UI, abandon-no-orphan, lookup, void, D-08 parity) + backend route checks
deferred: 4  (live terminal sale: issue, reload, partial redeem, full redeem — real card charge + Zoho accounting + cert activation-on-payment)
issues: 0 open  (G-44-01 resolved by 44-09/44-10)

## Deferred (live terminal sale) — owner to run at a later date
These need a real card (or Helcim test card) on the iPad and a Zoho check afterward. Until they
pass, the phase is NOT production-promoted and GIFTCARD-01a/01c/01d remain "live-UAT pending".
- [ ] ISSUE via cart → terminal charge → cert activates on payment success; Zoho cart invoice has the gift-cert line at $0 tax, paid by the REAL terminal payment (no phantom; nothing in Undeposited Funds)
- [ ] RELOAD via cart → terminal charge → balance increments only on payment success
- [ ] PARTIAL REDEEM → terminal charges only the remainder; two payments sum to total; goods tax unchanged
- [ ] FULL REDEEM (gift covers 100%) → terminal skipped; invoice closed by gift payment only
- [ ] (if a charge ever fails post-payment) gift_card_activation_failed staff alert shows on both surfaces

## Gaps

### G-44-01 — Issuance & reload mark Zoho invoice paid without charging the card  [BLOCKING]
status: resolved (44-09 + 44-10, deployed to staging + prod middleware 2026-06-29; live terminal-sale confirmation deferred — see above)
requirements: [GIFTCARD-01a (issue), GIFTCARD-01d (reload)]
surfaced_by: owner UAT (issued GC-000001 $50 on staging kiosk)
files: [zoho-middleware/routes/gift-cards.js (issue, reload), js/kiosk.js, js/admin.js, zoho-middleware/routes/pos.js (cart/checkout)]

**Observed:** Issuing GC-000001 ($50) wrote the sheet row correctly and showed a success
toast, but created INV-000124 in Zoho and immediately recorded a $50 `creditcard` payment
(deposited to Undeposited Funds) — with NO Helcim terminal charge ever happening. The card
holder never paid through the system, yet Zoho reported the invoice `paid`. The issue/reload
routes hard-record a `creditcard` customerpayment assuming the terminal was used, but the
44-06 modal never invokes the terminal. Nothing enters the kiosk cart.

**Why it matters:** Phantom revenue — Zoho overstates collected cash by the face value, and
no payment is actually taken. The reload route mirrors issue and has the identical defect.
(The redeem path is correct — it genuinely charges the terminal for the remainder.)

**Owner decision (2026-06-29): Cart + terminal checkout model.**
Issuing/reloading a gift card must add a gift-certificate line to the kiosk cart and be paid
through the normal terminal checkout flow (real Helcim charge); the certificate is
created/activated on payment SUCCESS — not pre-recorded as paid. This reuses the existing
redeem terminal path and produces ONE invoice (the cart's) rather than a standalone
pre-paid invoice.

**Implied rework:**
- js/kiosk.js + js/admin.js (D-08): issue/reload affordance adds a cart line instead of
  calling /issue|/reload directly + toast.
- zoho-middleware/routes/pos.js: on successful kiosk sale containing a gift-cert issue/reload
  line, call Apps Script issue_gift_card / reload_gift_card to activate the cert (post-payment).
- zoho-middleware/routes/gift-cards.js: issue/reload routes must stop self-creating the
  invoice + phantom creditcard payment; activation logic moves to (or is driven by) the
  paid-checkout flow. Preserve cert-number uniqueness (D-02), server-authoritative balance
  (D-05), zero-tax on the gift-cert line (D-03), and void-on-failure semantics.
- Decide cert lifecycle for the cart model: reserve cert number at add-to-cart vs activate at
  payment success; handle abandoned carts (no orphan active certs).

## Test cleanup performed
- Zoho: deleted phantom payment (id 109900000000873279) and INV-000124 (id 109900000000873261).
- GiftCards sheet: GC-000001 row to be deleted by owner (manual).

## Next step
G-44-01 closed and on staging. Remaining: owner runs the DEFERRED live terminal-sale checks
above (real card), confirms Zoho accounting, then this UAT flips to `status: passed`. Only after
that should Phase 44 be production-promoted (`git push production main --force` + `railway up`),
which is a separate owner-gated step (staging-first rule). These deferred items are tracked and
will surface in `/gsd:progress` and `/gsd:audit-uat`.
