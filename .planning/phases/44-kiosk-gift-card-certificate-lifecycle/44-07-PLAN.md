---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: 07
type: execute
wave: 5
depends_on: ["44-04", "44-05", "44-06"]
files_modified:
  - js/kiosk.js
  - js/admin.js
  - js/kiosk.min.js
  - js/admin.min.js
autonomous: true
requirements: [GIFTCARD-01]

must_haves:
  truths:
    - "D-01: redemption is a tender in the payment view — staff enter a cert number, the UI shows the live server balance (GET lookup), staff enter an amount to apply (≤ min(balance, amount due)), and the remainder goes to the Helcim terminal"
    - "D-05: the applied amount is clamped client-side to min(balance, total) AND re-clamped server-side; the sale body sends gift_card:{cert_number, amount_applied}"
    - "D-08: the redeem tender exists IDENTICALLY on both forked surfaces (kiosk.js inline + admin.js); the void/lookup management view is admin.js-only (staff management, not a kiosk POS action)"
    - "D-06: from the admin gift-card management view staff can look up any cert balance and void a cert (01b lookup, 01e void)"
  artifacts:
    - path: "js/kiosk.js"
      provides: "redeem tender panel in payment view + _kioskGiftCard state + gift_card in saleBody"
      contains: "_kioskGiftCard"
    - path: "js/admin.js"
      provides: "paired redeem tender + admin gift-card management view (lookup + void)"
      contains: "_kioskGiftCard"
  key_links:
    - from: "js/kiosk.js payment view redeem panel"
      to: "/api/kiosk/gift-card/lookup then /api/kiosk/sale (gift_card in body)"
      via: "fetch lookup + saleBody.gift_card"
      pattern: "gift_card"
    - from: "js/admin.js management view"
      to: "/api/kiosk/gift-card/lookup, /void"
      via: "fetch with MW_API_KEY"
      pattern: "gift-card/void"
---

<objective>
Add the redemption tender to the kiosk payment view on BOTH forked surfaces (lookup live balance → apply amount → split the remainder onto the terminal via the 44-04 server path), plus an admin-only gift-card management view (lookup balance + void). This is GIFTCARD-01b (lookup) + 01c (redeem/partial/split) + 01e (void) at the UI tier. Per D-08, the redeem tender is paired across kiosk.js + admin.js; the management view is admin-only.

Purpose: Complete the lifecycle UI — staff redeem as a tender (D-01) with server-authoritative balances (D-05) and manage certs from the admin panel.
Output: redeem tender on js/kiosk.js + js/admin.js, admin management view in js/admin.js, rebuilt bundles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-04-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Endpoints (from 44-04 / 44-05 SUMMARYs) -->
GET  /api/kiosk/gift-card/lookup?cert_number=GC-000001 -> 200 {ok, data:{current_balance, status, face_value}} | 404
POST /api/kiosk/gift-card/void {cert_number, reason}    -> 200 {ok} | 404 | 400
POST /api/kiosk/sale (+ confirm) — sale body now accepts: gift_card: { cert_number, amount_applied }  (server clamps + splits tender — 44-04)

<!-- UI injection points (from PATTERNS.md) -->
kiosk.js: payment view / kioskStartCheckout new-sale branch lines 3215-3292; standardSaleBody line 3286; _kioskDiscount/_kioskCustomer module vars (mirror for _kioskGiftCard); cleared in kioskClearCart
admin.js: payment view block ~lines 10529-10601; existingSaleBody ~line 10575; openModal/closeModal for the management view + void confirm
// _kioskGiftCard = { cert_number, amount_applied, balance } — module-scoped var on BOTH surfaces; cleared on cart clear.
// BUILD RULE: edit js/kiosk.js + js/admin.js source; npm run build regenerates min bundles.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: js/kiosk.js — redeem tender in payment view</name>
  <files>js/kiosk.js</files>
  <read_first>
    - js/kiosk.js (kioskStartCheckout new-sale branch lines 3215-3292; standardSaleBody line 3286; _kioskDiscount/_kioskCustomer patterns; kioskClearCart; handleSaleResult line 3297 — unchanged)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md ("Redeem tender — payment view injection point" lines 619-657)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-04-SUMMARY.md (the exact gift_card sale-body shape the server expects)
  </read_first>
  <action>
    Add an "Apply Gift Card" sub-panel in the payment view BEFORE the terminal push (around line 3215). Flow: staff tap "Apply Gift Card" → cert input (kgcr-cert) + "Look up" button → fetch GET /api/kiosk/gift-card/lookup → on 404 show "Certificate not found", on inactive status show the status, else display the live balance → amount-to-apply input (kgcr-amount, font-size:1rem) clamped client-side to min(balance, amountDue) → on confirm set module var `var _kioskGiftCard = { cert_number, amount_applied, balance }`, update the displayed split ("Gift Card: $Y", "Terminal: $Z = total - Y"). Add `gift_card: _kioskGiftCard ? { cert_number: _kioskGiftCard.cert_number, amount_applied: _kioskGiftCard.amount_applied } : undefined` to standardSaleBody (line 3286). Clear _kioskGiftCard in kioskClearCart() and after a completed sale. Leave handleSaleResult + the confirm fetch unchanged (the split is transparent once the body carries gift_card). Use ES5/var, ids prefixed 'kgcr-'.
  </action>
  <acceptance_criteria>
    - `grep -c "_kioskGiftCard\|gift-card/lookup\|kgcr-cert" js/kiosk.js` shows all present.
    - standardSaleBody includes a `gift_card:` field (grep shows `gift_card:` near line 3286).
    - `_kioskGiftCard` is cleared in kioskClearCart (grep shows the reset).
    - `npm run lint` clean; no NEW const/let/arrow in the added block.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "_kioskGiftCard\|gift-card/lookup\|kgcr-cert" js/kiosk.js && grep -n "gift_card:" js/kiosk.js | head && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>Redeem tender panel in kiosk.js payment view: lookup balance, clamp applied amount, split terminal remainder, send gift_card in saleBody.</done>
</task>

<task type="auto">
  <name>Task 2 (PAIRED, D-08): js/admin.js — redeem tender + admin gift-card management view</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (payment view block ~lines 10529-10601; existingSaleBody ~line 10575; openModal/closeModal lines 1301-1319; an existing admin list/management view + a confirm pattern to mirror for void)
    - js/kiosk.js (the redeem tender added in Task 1 — admin mirrors its lookup/clamp/saleBody logic exactly)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md ("Payment view injection point" lines 722-738; openModal/closeModal lines 666-682)
  </read_first>
  <action>
    Part A (PAIRED redeem tender, D-08): mirror Task 1 EXACTLY in the admin-embedded kiosk payment view — same _kioskGiftCard module var, same lookup/clamp/split logic, same kgcr-* IDs — and add `gift_card: _kioskGiftCard ? {...} : undefined` to existingSaleBody (~line 10575). Part B (admin-only management view): add a gift-card management affordance in the admin panel (a small section or modal via openModal) where staff can (1) look up any cert by number → GET /api/kiosk/gift-card/lookup → show balance/status/face_value, and (2) void a cert → a confirm dialog naming the cert ("Void GC-NNNNNN? This cannot be undone.") → POST /api/kiosk/gift-card/void {cert_number, reason} → refresh the lookup display. The management view is admin.js-only (not added to kiosk.js) because void is staff management, not a POS sale action. ES5/var; admin button classes.
  </action>
  <acceptance_criteria>
    - `grep -c "_kioskGiftCard\|gift-card/lookup\|kgcr-cert" js/admin.js` shows the paired redeem tender present with matching IDs (D-08).
    - existingSaleBody includes a `gift_card:` field (grep).
    - `grep -c "gift-card/void" js/admin.js` ≥ 1 (management void) and a confirm dialog string naming the cert is present.
    - `grep -c "gift-card/void" js/kiosk.js` === 0 (void is admin-only, not on the POS surface).
    - `npm run lint` clean; no NEW const/let/arrow in the added block.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "_kioskGiftCard\|gift-card/lookup\|gift-card/void" js/admin.js && echo "kiosk void (want 0):" && grep -c "gift-card/void" js/kiosk.js; npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>Paired redeem tender on admin.js (identical IDs/logic) + admin-only lookup/void management view; void absent from kiosk.js.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild kiosk.min.js + admin.min.js</name>
  <files>js/kiosk.min.js, js/admin.min.js</files>
  <read_first>
    - CLAUDE.md (build rules); package.json (build script)
  </read_first>
  <action>
    Run `npm run build` to regenerate js/kiosk.min.js + js/admin.min.js and re-stamp cache-bust versions. Do not hand-edit min files.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -c "gift-card/lookup" js/kiosk.min.js` ≥ 1 and `grep -c "gift-card/void" js/admin.min.js` ≥ 1.
    - `npm test` (frontend) exits 0 — no regression.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -c "gift-card/lookup" js/kiosk.min.js && grep -c "gift-card/void" js/admin.min.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Min bundles regenerated with redeem tender + management view; frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPad Safari kiosk/admin → /api/kiosk/gift-card/lookup + /sale | client supplies cert + amount_applied; server is the authority for balance + split |
| admin panel → /api/kiosk/gift-card/void | staff-only management action crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-26 | Tampering | Client over-applies (amount_applied > balance/total) | mitigate | UI clamps to min(balance, due); server re-clamps to grandTotal + validates balance (44-04/Apps Script) |
| T-44-27 | Information disclosure | Cert balance shown without auth | mitigate | lookup requires MW_API_KEY (/api/kiosk/*); no customer-facing surface (D-07); admin view behind staff auth |
| T-44-28 | Elevation of privilege | Void exposed on the POS surface to non-management flow | mitigate | void affordance is admin.js-only; acceptance criteria assert it is absent from kiosk.js |
| T-44-29 | Repudiation | Fork drift on the redeem tender (D-08) | mitigate | Paired Task 1/Task 2 with identical kgcr-* IDs; grep both files (Pitfall 5) |
| T-44-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase |
</threat_model>

<verification>
- grep confirms paired redeem tender (matching IDs) on both files; void only on admin.js.
- `npm run build && npm test && npm run lint` all clean.
- Functional verification deferred to 44-08 staging iPad Safari UAT.
</verification>

<success_criteria>
- Redeem tender (lookup → apply → split terminal) on both forked surfaces (D-01, D-05, D-08); admin lookup + void management view (D-06); bundles rebuilt; tests green.
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-07-SUMMARY.md` when done.
</output>
