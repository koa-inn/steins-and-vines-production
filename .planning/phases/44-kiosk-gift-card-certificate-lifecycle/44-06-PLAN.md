---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: 06
type: execute
wave: 4
depends_on: ["44-03", "44-05"]
files_modified:
  - js/kiosk.js
  - js/admin.js
  - js/kiosk.min.js
  - js/admin.min.js
autonomous: true
requirements: [GIFTCARD-01]

must_haves:
  truths:
    - "D-02: the Issue Gift Card modal pre-fills the suggested next cert number from GET /api/kiosk/gift-card/next-number; staff may override; the server rejects duplicates (409 surfaced as an error)"
    - "D-06: staff can issue a new certificate and reload an existing one from the kiosk POS 'add value' affordance"
    - "D-07: paper-certificate workflow — staff manually enter/override the cert number; no barcode, digital code, or customer-facing lookup in v1"
    - "D-08: the modal exists IDENTICALLY on both forked surfaces — js/kiosk.js (inline overlay) and js/admin.js (openModal/closeModal) — with the same field IDs and fetch logic"
    - "D-03: zero tax at sale is enforced server-side (44-03); the UI never adds tax to a gift-card issue/reload"
  artifacts:
    - path: "js/kiosk.js"
      provides: "kioskShowGiftCardIssueModal (issue + reload) + button wire-up in kioskRenderCart"
      contains: "kioskShowGiftCardIssueModal"
    - path: "js/admin.js"
      provides: "kioskShowAdminGiftCardIssueModal (openModal/closeModal) — paired with kiosk.js"
      contains: "kioskShowAdminGiftCardIssueModal"
  key_links:
    - from: "js/kiosk.js issue/reload modal"
      to: "/api/kiosk/gift-card/issue, /reload, /next-number"
      via: "fetch with x-api-key MW_API_KEY"
      pattern: "gift-card/issue"
    - from: "js/admin.js issue/reload modal"
      to: "/api/kiosk/gift-card/issue, /reload, /next-number"
      via: "fetch with x-api-key MW_API_KEY"
      pattern: "gift-card/issue"
---

<objective>
Add the kiosk POS "add value" affordance — Issue a new gift certificate and Reload an existing one — on BOTH forked surfaces (kiosk.js inline overlay; admin.js openModal/closeModal). This is the GIFTCARD-01a (issue) + 01d (reload) UI. Per D-08, every kiosk.js change is paired with the identical admin.js change.

Purpose: Staff can sell and top up paper certificates at the POS; the suggested cert number (D-02) is pre-filled and server-validated; ES5/var, iPad-Safari-safe.
Output: Issue/Reload modal + button on js/kiosk.js and js/admin.js, rebuilt min bundles.

NOTE: whether issuance/reload routes the face_value charge through the Helcim terminal before calling /issue (or records it as collected per RESEARCH Pattern 3) is the one open flow detail — implement per RESEARCH Pattern 3 (modal → /issue) and flag it for owner confirmation at the 44-08 staging UAT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-03-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Middleware endpoints (from 44-03 / 44-05 SUMMARYs) -->
GET  /api/kiosk/gift-card/next-number          -> { suggested:'GC-000042' }
POST /api/kiosk/gift-card/issue  {cert_number, face_value} -> 201 {ok, cert_number, face_value, zoho_invoice_number} | 409 dup | 400 | 503
POST /api/kiosk/gift-card/reload {cert_number, amount}     -> 200 {ok, cert_number, new_balance} | 404 | 409 | 400 | 503

<!-- UI analogs (from PATTERNS.md) -->
kiosk.js: kioskShowCustomItemModal (inline overlay build-once) line 2629; kioskRenderCart button wire-up lines 2800-2857; SHEETS_CONFIG.MW_API_KEY header line 3181; kioskClearCart for state reset
admin.js: kioskShowAdminCustomItemModal line 10106; openModal/closeModal lines 1301-1319; button classes 'btn btn-default'/'btn btn-primary'
// Use field ID prefix 'kgci-' (cert/value/error/mode) to avoid collision with the custom-item modal 'kci-' prefix.
// BUILD ARTIFACT RULE (CLAUDE.md): js/kiosk.js and js/admin.js are SOURCE (edit directly); never edit main.js/main.min.js. Run npm run build to regenerate kiosk.min.js + admin.min.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: js/kiosk.js — Issue/Reload modal (inline overlay) + button</name>
  <files>js/kiosk.js</files>
  <read_first>
    - js/kiosk.js (kioskShowCustomItemModal lines 2631-2713 — the build-once inline-overlay pattern to mirror; kioskRenderCart button block lines 2800-2817 + wire-up 2854-2857; fetch with 'x-api-key': SHEETS_CONFIG.MW_API_KEY line 3181; kioskClearCart for state reset)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md ("js/kiosk.js (MODIFY)" lines 550-657 — exact overlay + button patterns)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-03-SUMMARY.md + 44-05-SUMMARY.md (endpoint request/response shapes)
  </read_first>
  <action>
    Add kioskShowGiftCardIssueModal() following the build-once inline-overlay pattern (id 'kiosk-gift-card-issue-overlay', z-index 1200, backdrop-click close). Modal supports two modes via a toggle/segmented control: Issue (new cert) and Reload (existing cert). Fields (ES5/var, ids prefixed 'kgci-'): kgci-mode (issue|reload), kgci-cert (text; in Issue mode pre-filled by fetching GET /api/kiosk/gift-card/next-number; in Reload mode staff type an existing cert), kgci-value (number, $0.01-$2000, font-size:1rem to avoid iOS zoom), kgci-error, kgci-cancel, kgci-issue. On submit: validate cert /^GC-\d{6}$/ + value bounds client-side, then fetch POST to /issue (Issue) or /reload (Reload) with the MW_API_KEY header and a loading state; on 409 show "Certificate number already in use", on 503 show "Gift card accounting not configured", on 201/200 hide overlay and surface confirmation (cert_number + value, and new_balance for reload) toward the receipt/cart. Render an "+ Issue / Reload Gift Card" button in kioskRenderCart()'s non-empty cart block (mirror the "Add custom item" button at lines 2854-2857) wired to kioskShowGiftCardIssueModal(). Reset modal fields + clear any transient state on open and in kioskClearCart().
  </action>
  <acceptance_criteria>
    - `grep -c "kioskShowGiftCardIssueModal\|kgci-cert\|gift-card/issue\|gift-card/reload\|gift-card/next-number" js/kiosk.js` shows all identifiers present.
    - `grep -n "kiosk-add-gc-btn\|Issue.*Gift Card\|Gift Card" js/kiosk.js` shows the button in kioskRenderCart.
    - `npm run lint` clean (ES5; no const/let/arrow introduced — `grep -nE "=>|\\bconst\\b|\\blet\\b" js/kiosk.js` shows no NEW occurrences in the added block).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "kioskShowGiftCardIssueModal\|gift-card/issue\|gift-card/reload\|gift-card/next-number" js/kiosk.js && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>Issue/Reload modal + button live in kiosk.js, ES5, pre-fills suggested cert, handles 409/503/success.</done>
</task>

<task type="auto">
  <name>Task 2 (PAIRED, D-08): js/admin.js — identical Issue/Reload modal (openModal/closeModal)</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js (kioskShowAdminCustomItemModal lines 10106-10155 — the direct template; openModal/closeModal lines 1301-1319; button classes 'btn btn-default'/'btn btn-primary' lines 10137-10138)
    - .planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-PATTERNS.md ("js/admin.js (MODIFY)" lines 660-738 — openModal divergence from the kiosk inline overlay)
    - js/kiosk.js (the kioskShowGiftCardIssueModal added in Task 1 — admin.js mirrors its validation + fetch logic exactly)
  </read_first>
  <action>
    Add kioskShowAdminGiftCardIssueModal() and kioskSubmitAdminGiftCardIssue() mirroring Task 1's logic EXACTLY (same field IDs kgci-*, same Issue/Reload modes, same endpoints, same 409/503/success handling, same client validation) but using openModal('Issue / Reload Gift Certificate', html) for the container (no inline overlay) and closeModal() to dismiss (cancel button and on success). Use admin button classes 'btn btn-default' (cancel) / 'btn btn-primary' (submit). Wire the kgci-cert focus + buttons after openModal. Add the "+ Issue / Reload Gift Card" button to the admin-embedded kiosk cart render (same placement as kiosk.js Task 1, admin's kioskRenderCart equivalent). On success: closeModal() then refresh the cart/receipt view. This is the D-08 paired surface — logic identical, only modal mechanism + button classes differ.
  </action>
  <acceptance_criteria>
    - `grep -c "kioskShowAdminGiftCardIssueModal\|kgci-cert\|gift-card/issue\|gift-card/reload\|gift-card/next-number" js/admin.js` shows all identifiers present.
    - The admin modal uses openModal/closeModal (grep shows `openModal(` and `closeModal(` in the new functions), NOT an inline overlay.
    - Field IDs match kiosk.js exactly (kgci-cert, kgci-value, kgci-error) — D-08 parity.
    - `npm run lint` clean; no NEW const/let/arrow in the added block.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "kioskShowAdminGiftCardIssueModal\|gift-card/issue\|gift-card/reload" js/admin.js && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>Paired admin.js modal exists with identical logic + IDs; openModal/closeModal mechanism; D-08 fork parity maintained.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild kiosk.min.js + admin.min.js</name>
  <files>js/kiosk.min.js, js/admin.min.js</files>
  <read_first>
    - CLAUDE.md ("After any JS module change, run npm run build"; "Never edit *.min.js directly")
    - package.json (build script: stamp + minify:js terser kiosk.js/admin.js)
  </read_first>
  <action>
    Run `npm run build` to stamp and regenerate js/kiosk.min.js and js/admin.min.js (and the cache-bust ?v= stamps on kiosk.html/admin.html). Do not hand-edit the min files. Confirm the new gift-card identifiers are present in the minified output.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0.
    - `grep -c "gift-card/issue" js/kiosk.min.js` ≥ 1 and `grep -c "gift-card/issue" js/admin.min.js` ≥ 1 (the new code is in the bundles).
    - `npm test` (frontend) exits 0 — no regression.
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -3 && grep -c "gift-card/issue" js/kiosk.min.js js/admin.min.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Min bundles regenerated with the gift-card UI; frontend tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPad Safari kiosk UI → /api/kiosk/gift-card/* | client sends cert/value; server is the authority (validation + dup-reject server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-23 | Tampering | Client-side validation bypass (bad cert/value) | mitigate | Server re-validates format + bounds + duplicate (44-03/44-05); UI checks are convenience only |
| T-44-24 | Spoofing | Request without API key | mitigate | UI sends x-api-key: SHEETS_CONFIG.MW_API_KEY; server enforces on /api/kiosk/* |
| T-44-25 | Repudiation | Fork drift — feature on one surface only (D-08) | mitigate | Paired Task 1/Task 2 with identical IDs; acceptance criteria grep BOTH files (Pitfall 5) |
| T-44-SC | Tampering | npm/pip/cargo installs | N/A | No package installs; build uses existing terser/cleancss devDeps |
</threat_model>

<verification>
- grep confirms the modal + endpoints on BOTH js/kiosk.js and js/admin.js with matching field IDs (D-08).
- `npm run build && npm test && npm run lint` all clean.
- Functional verification deferred to 44-08 staging iPad Safari UAT.
</verification>

<success_criteria>
- Issue + Reload modal available identically on both forked surfaces; suggested cert pre-filled (D-02); 409/503/success handled; ES5/iPad-safe; bundles rebuilt; tests green.
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-06-SUMMARY.md` when done. Note the open issuance terminal-charge flow question for the 44-08 UAT.
</output>
