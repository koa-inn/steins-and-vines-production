---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: 08
type: execute
wave: 6
depends_on: ["44-07"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [GIFTCARD-01]

must_haves:
  truths:
    - "D-06: the full lifecycle works end-to-end on staging — issue, balance lookup, partial redemption (split tender), full redemption (gift covers 100%), reload, and void"
    - "D-01: redemption applies as a tender and the Helcim terminal charges only the remainder; D-03: a gift-card sale carries no tax while the redeemed goods invoice keeps its tax"
    - "D-08: every gift-card affordance behaves identically on both forked surfaces (standalone kiosk and admin-embedded kiosk) on iPad Safari"
    - "GIFTCARD-01 (a-e) is verified and the REQUIREMENTS.md traceability row is updated"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "GIFTCARD-01 traceability updated to Phase 44 with verification status"
      contains: "GIFTCARD-01"
  key_links:
    - from: "staging deploy (git push origin main)"
      to: "iPad Safari kiosk + admin kiosk full-lifecycle UAT"
      via: "staging.steinsandvines.ca"
      pattern: "GIFTCARD-01"
---

<objective>
Deploy the full gift-card lifecycle to staging and verify it end-to-end on iPad Safari across both forked surfaces, confirm the accounting/tax behaviour in Zoho, resolve the open issuance terminal-charge flow question, and update REQUIREMENTS traceability. This is the phase gate before production.

Purpose: Prove GIFTCARD-01 (a-e) holds against the v4.2-hardened money path without weakening it, on the real kiosk hardware path.
Output: staging deploy + a recorded UAT pass (or a gap list) + updated REQUIREMENTS.md. Production promotion stays a separate, owner-gated step (staging-first rule).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-03-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-04-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-06-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate (build + both test suites + lint) and deploy to staging</name>
  <files>(deploy — git push origin main)</files>
  <read_first>
    - CLAUDE.md (Before Every Commit: npm test AND cd zoho-middleware && npm test; npm run lint; staging deploy = git push origin main; staging calls PROD middleware)
    - .planning/STATE.md (Blocker: middleware deploys to the prod Railway instance — staging site calls prod middleware; so the gift-card routes must be deployed to Railway too)
  </read_first>
  <action>
    Run the full gate: `npm run build`, `npm test` (frontend), `cd zoho-middleware && npm test` (middleware), `npm run lint` — all must be green. Confirm the gift-card middleware routes are deployed to the Railway production instance (staging site calls prod middleware per the v4.2 architecture) — deploy the middleware if not already live. Push the frontend to staging via `git push origin main`. Confirm KIOSK_GIFT_CARD_ITEM_ID + APPS_SCRIPT_URL/token are set in Railway and the GiftCards Apps Script is the redeployed version (from 44-02). Verify the staging kiosk + admin pages load with no console errors before handing to the human UAT.
  </action>
  <acceptance_criteria>
    - `npm test` and `cd zoho-middleware && npm test` both exit 0; `npm run lint` clean; `npm run build` clean.
    - Staging deploy pushed (git push origin main) and the gift-card middleware routes respond on the Railway instance (e.g. GET /api/kiosk/gift-card/next-number with the API key returns a suggested number).
    - kiosk + admin staging pages load with no console errors.
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -5 && cd zoho-middleware && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>All gates green; gift-card routes live on Railway; frontend on staging; no console errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPad Safari full-lifecycle UAT on both surfaces</name>
  <what-built>
    The complete gift-card lifecycle on both forked kiosk surfaces: Issue/Reload modal, redeem tender (lookup + partial/full split), and admin lookup/void management — all server-authoritative with the v4.2 money path intact.
  </what-built>
  <action>
    Hand the staging build to the owner and walk the full-lifecycle UAT script below on an iPad (Safari), exercising every step on BOTH the standalone kiosk and the admin-embedded kiosk, and confirm the Zoho accounting/tax outcomes. This is a verification checkpoint — no code is written; capture pass/fail per step plus the issuance-charge decision.
  </action>
  <how-to-verify>
    On an iPad (Safari), test BOTH the standalone kiosk (staging kiosk URL) AND the admin-embedded kiosk (admin.html?tab=kiosk), confirming identical behaviour (D-08):
    1. ISSUE: open "Issue / Reload Gift Card" → confirm the cert number is pre-filled with a suggested GC-NNNNNN → issue a $50 cert. Confirm the cert + value surface on the receipt. In Zoho, confirm the sale invoice posts to the "Gift Card Sales" income account with ZERO tax (D-03/D-04).
    2. DUPLICATE: try issuing the same cert number again → confirm it is rejected ("already in use").
    3. LOOKUP: in the admin management view, look up the cert → confirm balance $50, status active.
    4. PARTIAL REDEEM: start a normal taxed sale (e.g. $30 of goods incl. tax) → Apply Gift Card → enter the cert → confirm the live balance shows → apply $20 → confirm the terminal is asked to charge ONLY the remainder ($10) → complete on the terminal. In Zoho, confirm the goods invoice is fully paid by TWO payments (terminal + gift 'others') and the goods tax is unchanged (D-01/D-03). Confirm the cert balance is now $30.
    5. FULL REDEEM: a sale fully covered by the gift card → confirm the terminal is skipped and the invoice closes with only the gift payment.
    6. RELOAD: reload the cert by $25 → confirm the balance becomes $55 and the reload posts as a zero-tax sale.
    7. VOID: void a (different, test) cert from the admin management view with the confirm dialog → confirm a later redeem of that cert is refused.
    8. Confirm NO console errors throughout on iPad Safari, and that every step behaves identically on the standalone kiosk and the admin-embedded kiosk.
    9. ISSUANCE CHARGE QUESTION: confirm with the owner whether issuance/reload should charge the Helcim terminal as part of the modal flow (vs the current record-as-collected behaviour) — record the decision; if a change is needed, log it as a gap.
  </how-to-verify>
  <resume-signal>Type "UAT passed" (optionally noting the issuance-charge decision), or list defects/gaps to feed a gap-closure plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS traceability for GIFTCARD-01</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the Traceability table + v1 Requirements list — note GIFTCARD-01 is owner-added for Phase 44 and may not yet have a row)
  </read_first>
  <action>
    Add/update the GIFTCARD-01 entry in REQUIREMENTS.md: map it to Phase 44 with the sub-requirement breakdown (01a issue, 01b lookup, 01c redeem/split-tender, 01d reload, 01e void) and set status to Verified-on-staging (or note the gap list if UAT found defects). Update the Traceability table and coverage counts accordingly. Do not alter unrelated requirement rows.
  </action>
  <acceptance_criteria>
    - `grep -n "GIFTCARD-01" .planning/REQUIREMENTS.md` shows the requirement mapped to Phase 44 with a verification status.
    - The sub-requirement breakdown (a-e) is recorded so each lifecycle capability is traceable to GIFTCARD-01.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "GIFTCARD-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>GIFTCARD-01 traceable to Phase 44 with verified status (or a recorded gap list).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging frontend → prod Railway middleware | staging calls the prod middleware; gift-card routes must be live there before UAT |
| iPad Safari kiosk → money path | the real redemption split-tender exercises Helcim + Zoho + Apps Script together |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-30 | Tampering | A regression weakens the v4.2 money path (void/idempotency) | mitigate | Full middleware suite (incl. existing pos/checkout tests) must pass before deploy; UAT exercises partial/full/void ordering |
| T-44-31 | Information disclosure | Tax incorrectly applied at sale or dropped at redemption | mitigate | UAT step 1 (zero tax at sale) + step 4 (goods tax unchanged at redemption) verified in Zoho |
| T-44-32 | Repudiation | Fork drift surfaces only at runtime (D-08) | mitigate | UAT runs every step on BOTH the standalone and admin-embedded kiosk |
| T-44-SC | Tampering | npm/pip/cargo installs | N/A | No package installs in this phase |
</threat_model>

<verification>
- Full gate green (frontend + middleware tests + lint + build).
- Human UAT pass on iPad Safari across both surfaces, including Zoho accounting/tax confirmation.
- REQUIREMENTS.md traceability updated.
</verification>

<success_criteria>
- Full gift-card lifecycle (issue/lookup/partial/full redeem/reload/void) verified on staging iPad Safari on both forked surfaces, with correct liability+zero-tax accounting and an intact v4.2 money path.
- GIFTCARD-01 (a-e) marked verified; production promotion left as a separate owner-gated step (staging-first).
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-08-SUMMARY.md` when done. Record the UAT outcome, the issuance-charge decision, and any gaps (feed `/gsd:plan-phase 44 --gaps` if defects found).
</output>
