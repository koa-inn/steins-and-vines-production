---
phase: 44-kiosk-gift-card-certificate-lifecycle
plan: "10"
type: execute
gap_closure: true
wave: 2
depends_on: ["44-09"]
files_modified:
  - js/kiosk.js
  - js/admin.js
  - js/kiosk.min.js
  - js/admin.min.js
autonomous: true
requirements: [GIFTCARD-01]
user_setup: []

must_haves:
  truths:
    - "Clicking Issue/Reload in the kiosk gift-card modal ADDS a gift-cert line to the cart instead of calling /issue|/reload directly (G-44-01 closed; no immediate phantom invoice)"
    - "The gift-cert cart line flows through the normal terminal checkout so a REAL Helcim charge is taken for the face value (D-05 server-authoritative)"
    - "The gift-cert cart line is shown, removable, never discounted, and contributes $0 tax client-side (D-03); the server is still authoritative"
    - "Both forked surfaces (js/kiosk.js + js/admin.js) behave identically — same gift_cert line shape, same cart wiring (D-08)"
    - "Reload mode confirms the cert exists + is active (via lookup) before adding the line, so staff get immediate feedback rather than a post-payment failure"
    - "When confirm returns gift_card_activation_failed, both surfaces show a prominent alert (payment taken, cert NOT active — record number + activate manually) instead of a plain success toast (D-08)"
    - "Built bundles js/kiosk.min.js + js/admin.min.js are regenerated via npm run build (never hand-edited)"
  artifacts:
    - path: "js/kiosk.js"
      provides: "modal submit → cart line; gift_cert render + totals/tax/discount-skip; proceed-to-payment item shape; activation-failure alert in _kioskPushToTerminal"
      contains: "gift_cert"
    - path: "js/admin.js"
      provides: "D-08 parity of the same gift_cert cart wiring + activation-failure alert in _adminDoSale"
      contains: "gift_cert"
  key_links:
    - from: "js/kiosk.js kioskSubmitGiftCardIssue"
      to: "_kioskCart gift_cert line"
      via: "add cart entry then kioskRenderCart (no direct /issue fetch)"
      pattern: "gift_cert"
    - from: "js/kiosk.js kioskProceedToPayment items map"
      to: "/api/kiosk/sale body.items"
      via: "emits { gift_cert, gift_action, cert_number, quantity:1, rate }"
      pattern: "gift_cert"
    - from: "js/kiosk.js _kioskPushToTerminal confirm handler"
      to: "staff alert on response.gift_card_activation_failed"
      via: "checks confirm 201 flag before success toast"
      pattern: "gift_card_activation_failed"
---

<objective>
Close G-44-01 on the frontend. The 44-06 issue/reload modal currently POSTs straight to
`/api/kiosk/gift-card/issue` or `/reload` and toasts success — which (pre-44-09) recorded a
phantom paid invoice with no terminal charge. Rework BOTH forked kiosk surfaces so the modal
ADDS a gift-cert LINE to the kiosk cart, and the existing terminal checkout takes the real
Helcim charge; the certificate is activated server-side on payment success (44-09).

Purpose: route gift-cert money-in through the same real, hardened terminal flow used for every
other kiosk sale. No standalone pre-paid invoice, no phantom revenue.
Output: reworked js/kiosk.js + js/admin.js (D-08), regenerated min bundles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-08-UAT.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-06-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-07-SUMMARY.md
@.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-09-PLAN.md
@CLAUDE.md

<interfaces>
<!-- gift_cert LINE contract — must match 44-09 exactly. -->

Cart entry stored in _kioskCart (model both surfaces):
  _kioskCart['giftcert-' + n] = {
    item: { gift_cert: true, gift_action: 'issue'|'reload', cert_number: 'GC-000042',
            name: 'Gift Certificate GC-000042' (issue) | 'Gift Cert Reload GC-000042' (reload),
            rate: <amount>, tax_percentage: 0, taxable: false },
    qty: 1 }

items[] element emitted by kioskProceedToPayment (sent to /api/kiosk/sale + /sale/confirm):
  { gift_cert: true, gift_action: 'issue'|'reload', cert_number: 'GC-000042', quantity: 1, rate: <amount> }

Confirm 201 response flag (from 44-09): when issue/reload fails AFTER the terminal charge
succeeded, the /api/kiosk/sale/confirm 201 body contains
  { ...saleResult, gift_card_activation_failed: true, needs_manual_review: true }
The UI must detect this and warn staff instead of showing a plain success.

This is SEPARATE from the 44-07 redeem TENDER (_kioskGiftCard → body.gift_card). Do not touch
the redeem tender wiring.

Existing analog to mirror (custom line):
  - kiosk.js custom add (~2765-2776): _kioskCart['custom-'+n] = { item:{custom:true,...}, qty }
  - proceed-to-payment items map (~3355-3373): `if (entry.item.custom) return {custom:true,...}`
  - discount skip (~1224): `if (entry.item.custom) return;` — gift_cert must also be skipped
  - kioskItemTax (~1119): uses item.tax_percentage → tax_percentage:0 yields $0 automatically
  - kioskCalcTotals (~1195): sums rate*qty for all cart entries → gift_cert face value included
  - _kioskPushToTerminal: posts to /api/kiosk/sale/confirm and handles the 201 result / success toast

Lookup endpoint for reload pre-check:
  GET /api/kiosk/gift-card/lookup?cert_number=GC-NNNNNN
    → 200 { ok, data:{ current_balance, status, face_value } } | 404 not found
  next-number (issue pre-fill) already wired in the modal: GET /api/kiosk/gift-card/next-number.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: kiosk.js — modal adds gift_cert cart line + cart/totals wiring + activation-failure alert</name>
  <files>js/kiosk.js</files>
  <read_first>
    - js/kiosk.js kioskSubmitGiftCardIssue (~2890-2960) — the function being reworked (it currently
      fetches /issue|/reload directly). kioskShowGiftCardIssueModal (~2783-2889) stays (mode toggle,
      next-number pre-fill) — only the SUBMIT changes.
    - The custom-line analog: custom add (~2765-2776), kioskProceedToPayment items map (~3355-3373),
      discount skip (~1224), kioskItemTax (~1119), kioskCalcTotals (~1195), kioskRenderCart
      (~2962+) and how it lists custom lines / wires remove buttons, _kioskCustomCounter (~703).
    - _kioskPushToTerminal — the function that POSTs /api/kiosk/sale/confirm and shows the
      "Sale complete" success toast; grep for it. This is where the gift_card_activation_failed
      alert must be added (before/instead of the normal success toast).
  </read_first>
  <action>
    Rework kioskSubmitGiftCardIssue so it NO LONGER fetches /api/kiosk/gift-card/issue or /reload.
    Keep the client-side validation (cert /^GC-\d{6}$/, amount finite in (0, 2000]). On valid
    input:
      - Issue mode: add a cart line _kioskCart['giftcert-' + (++counter)] = { item: { gift_cert:
        true, gift_action:'issue', cert_number: cert, name: 'Gift Certificate ' + cert, rate: val,
        tax_percentage: 0, taxable: false }, qty: 1 }, close the modal, call kioskRenderCart().
      - Reload mode: first GET /api/kiosk/gift-card/lookup?cert_number=cert (x-api-key header). On
        404 show the modal error 'Certificate not found'; on a non-active status show 'Certificate
        is not active'; on success add the line with gift_action:'reload', name:'Gift Cert Reload '
        + cert, rate: val. Then close modal + kioskRenderCart(). (Lookup is a UX pre-check only;
        the server re-validates and is authoritative — D-05.)
    Use a counter var (reuse the existing _kioskCustomCounter or add _kioskGiftCertCounter) for
    unique keys. Do NOT toast 'issued/reloaded' — the cert is only real after terminal payment.

    Wire the gift_cert line into the cart model (mirror custom lines), D-03/D-05:
      - Discount skip (~1224): change the guard to `if (entry.item.custom || entry.item.gift_cert)
        return;` so gift-cert lines are never discounted.
      - Confirm gift_cert lines are zero-tax: since item.tax_percentage:0, kioskItemTax already
        returns 0 — verify, and do not add tax for gift_cert anywhere.
      - kioskRenderCart: render gift-cert lines with their name + kioskFmt(rate) and a remove
        control, like custom lines (qty fixed at 1; no qty stepper needed). Ensure the existing
        "+ Issue / Reload Gift Card" button still opens kioskShowGiftCardIssueModal.

    In kioskProceedToPayment items map (~3355-3373), add a branch BEFORE the catalog branch:
    `if (entry.item.gift_cert) return { gift_cert:true, gift_action: entry.item.gift_action,
    cert_number: entry.item.cert_number, quantity: 1, rate: parseFloat(entry.item.rate)||0 };`
    Leave the custom and catalog branches and the body.gift_card redeem tender (44-07) untouched.

    Activation-failure alert (T-44-G11, WARNING 5): in _kioskPushToTerminal, after the
    /api/kiosk/sale/confirm 201 response is parsed and BEFORE the normal "Sale complete" success
    toast, check `if (response && response.gift_card_activation_failed)`. If true, show a prominent
    blocking alert/modal (not a transient toast) with copy like 'Gift cert activation FAILED —
    payment was taken. Record the certificate number(s) and activate manually before the customer
    leaves.' Include the cert_number(s) from the gift_cert cart lines so staff can write them down.
    Only after the staff dismisses this alert proceed with the normal post-sale cleanup. When the
    flag is absent/false, behavior is unchanged (normal success toast). This handling is required
    on BOTH surfaces (D-08 — admin.js mirrors it in Task 2).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - source: `grep -n "gift_cert" js/kiosk.js` shows the cart line add, the discount-skip guard, and the proceed-to-payment branch
    - source: kioskSubmitGiftCardIssue no longer contains a fetch to '/api/kiosk/gift-card/issue' or '/api/kiosk/gift-card/reload'
    - source: `grep -n "gift_card_activation_failed" js/kiosk.js` shows the alert branch inside _kioskPushToTerminal's confirm handler
    - behavior: issuing in the modal adds a removable, non-discounted, zero-tax line to the cart and does NOT toast a success message before payment
    - behavior: reload mode rejects an unknown/inactive cert (lookup 404/non-active) with a modal error and adds NO line
    - behavior: when the confirm 201 response has gift_card_activation_failed:true, a prominent alert/modal (payment taken, cert NOT active, record number) is shown instead of the plain success toast; when absent, the normal success toast shows
    - test-command: `npm test` passes (existing frontend suite green)
  </acceptance_criteria>
  <done>kiosk.js modal builds gift_cert cart lines that flow to terminal checkout; activation-failure alert surfaces in _kioskPushToTerminal; no direct issue/reload fetch.</done>
</task>

<task type="auto">
  <name>Task 2: admin.js — D-08 parity of gift_cert cart wiring + activation-failure alert</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js kioskSubmitAdminGiftCardIssue (the admin paired submit from 44-06) and the admin
      kioskShowAdminGiftCardIssueModal; the admin custom-line add, admin kioskProceedToPayment /
      _adminDoSale items map, admin discount skip, admin kioskItemTax, admin kioskCalcTotals, admin
      kioskRenderCart. Use grep to locate each (the admin fork mirrors kiosk.js names).
    - _adminDoSale — the admin function that POSTs /api/kiosk/sale/confirm and shows the success
      result; grep for it. This is where the gift_card_activation_failed alert must be added.
    - js/kiosk.js after Task 1 (the reference implementation to replicate identically, D-08).
  </read_first>
  <action>
    Apply the exact same rework as Task 1 to js/admin.js (D-08 parity): kioskSubmitAdminGiftCardIssue
    adds a gift_cert cart line (issue, or reload after a successful lookup using the admin
    apiKey/x-api-key header) instead of POSTing to /issue|/reload; the admin discount-skip guard
    gains `|| entry.item.gift_cert`; admin kioskRenderCart renders + allows removal of gift-cert
    lines; the admin proceed-to-payment / _adminDoSale items map emits the identical
    { gift_cert, gift_action, cert_number, quantity:1, rate } shape. Keep the admin redeem tender
    (kgcr-*) and management modal (kgcm-*) from 44-07 untouched. Field IDs and the gift_cert line
    shape MUST be identical to kiosk.js (D-08).

    Activation-failure alert (T-44-G11, WARNING 5, D-08): in _adminDoSale, after the
    /api/kiosk/sale/confirm 201 response is parsed and BEFORE the normal success message, check
    `if (response && response.gift_card_activation_failed)` and show the same prominent blocking
    alert/modal as kiosk.js ('Gift cert activation FAILED — payment was taken. Record the
    certificate number(s) and activate manually.'), including the cert_number(s). Copy and behavior
    MUST match the kiosk.js implementation exactly.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - source: `grep -c "gift_cert" js/admin.js` matches the same wiring points present in js/kiosk.js (cart add, discount skip, proceed map)
    - source: kioskSubmitAdminGiftCardIssue no longer fetches '/api/kiosk/gift-card/issue' or '/api/kiosk/gift-card/reload'
    - source: `grep -n "gift_card_activation_failed" js/admin.js` shows the alert branch inside _adminDoSale's confirm handler (D-08 parity with kiosk.js)
    - behavior: admin kiosk surface issues/reloads via the cart + terminal flow identically to kiosk.js (D-08)
    - behavior: when the confirm 201 response has gift_card_activation_failed:true, the same prominent alert is shown as on the kiosk surface
    - test-command: `npm test` passes
  </acceptance_criteria>
  <done>admin.js mirrors kiosk.js gift_cert cart wiring + activation-failure alert exactly (D-08); no direct issue/reload fetch.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild bundles + full frontend + middleware gate</name>
  <files>js/kiosk.min.js, js/admin.min.js</files>
  <read_first>
    - CLAUDE.md build rules: edit source then `npm run build`; never hand-edit .min.js; run
      `npm test` (frontend) AND `cd zoho-middleware && npm test` (middleware) + `npm run lint`
      before commit (CLAUDE.md Rule 1: both suites green before every commit).
  </read_first>
  <action>
    Run `npm run build` to regenerate js/kiosk.min.js + js/admin.min.js (and the ?v= cache stamps
    the build touches) from the reworked sources. Run `npm test` and `npm run lint` and fix any
    failures. Do NOT hand-edit any .min.js. Confirm both bundles contain the gift_cert wiring and
    no longer reference a direct issue/reload POST from the modal submit path. Per CLAUDE.md Rule 1,
    BOTH the frontend suite and the middleware suite must pass before this work is committed —
    run `cd zoho-middleware && npm test` as the pre-commit gate.
  </action>
  <verify>
    <automated>npm run build && npm test && npm run lint && cd zoho-middleware && npm test</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `npm run build` exits 0; js/kiosk.min.js and js/admin.min.js regenerated
    - source: `grep -c gift_cert js/kiosk.min.js` >= 1 and `grep -c gift_cert js/admin.min.js` >= 1
    - test-command: `npm test` passes, `npm run lint` reports 0 errors, AND `cd zoho-middleware && npm test` passes (both suites green before commit — CLAUDE.md Rule 1)
  </acceptance_criteria>
  <done>Bundles rebuilt from source; frontend + middleware tests + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff kiosk UI → middleware | Client builds body.items incl. gift_cert lines; never trusted for pricing |
| kiosk UI → /lookup (reload pre-check) | Read-only balance check; UX only, not authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-G7 | Tampering | client-supplied gift_cert rate/cert | mitigate | Server (44-09) re-validates cert format + rate bounds + forces item_id; client values are advisory only |
| T-44-G8 | Spoofing/repudiation | premature 'issued' feedback | mitigate | UI no longer toasts success on add; cert is real only after terminal payment + server activation |
| T-44-G9 | Tampering (regression) | 44-07 redeem tender / mgmt modal | mitigate | gift_cert LINE wiring is additive; _kioskGiftCard tender + kgcm-* modal untouched; frontend suite must stay green |
| T-44-G10 | Tampering | stale build artifact | mitigate | Bundles regenerated via npm run build; .min.js never hand-edited; grep verifies gift_cert present |
| T-44-G11 | Repudiation | post-payment activation failure (money taken, cert silently inactive) | mitigate | _kioskPushToTerminal / _adminDoSale show a prominent blocking alert with cert number(s) when confirm returns gift_card_activation_failed, so staff record + manually activate instead of seeing a plain success (D-08 parity) |
| T-44-SC | Tampering | npm installs | accept | No new packages added by this plan; no Package Legitimacy Gate required |
</threat_model>

<verification>
- `npm run build` succeeds; min bundles regenerated and contain gift_cert wiring
- `npm test` (frontend jsdom suite) green; `npm run lint` 0 errors; `cd zoho-middleware && npm test` green (CLAUDE.md Rule 1)
- grep parity: gift_cert wiring + gift_card_activation_failed alert present in BOTH js/kiosk.js and js/admin.js (D-08)
- Manual reasoning: modal add → cart line → kioskProceedToPayment → /api/kiosk/sale with gift_cert
  item → terminal charge (44-09 includes it in grandTotal) → confirm activates cert; on activation
  failure the confirm 201 flag triggers the staff alert.
</verification>

<success_criteria>
- Issue/reload on both surfaces adds a cart line and is paid via the real terminal checkout (G-44-01 closed)
- No modal path POSTs directly to /issue or /reload; no pre-payment success toast
- gift_cert lines are zero-tax, non-discounted, removable, qty 1 (D-03)
- Activation-failure (gift_card_activation_failed) surfaces a prominent staff alert on both surfaces (D-08)
- D-08 parity verified (kiosk.js == admin.js wiring + line shape + alert)
- Bundles rebuilt from source; frontend + middleware tests + lint pass; 44-07 redeem/mgmt untouched
</success_criteria>

<output>
Create `.planning/phases/44-kiosk-gift-card-certificate-lifecycle/44-10-SUMMARY.md` when done.
</output>
</parameter>
</invoke>
