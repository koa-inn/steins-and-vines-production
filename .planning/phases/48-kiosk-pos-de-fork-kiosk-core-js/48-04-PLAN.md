---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 04
type: execute
wave: 4
depends_on: ["48-03"]
files_modified:
  - js/admin.js
  - admin.html
autonomous: true
requirements: [KIOSK-01]
must_haves:
  truths:
    - "The admin-embedded kiosk consumes KioskCore for cart/payment/discount — the ~37 duplicated kiosk* definitions no longer exist in admin.js"
    - "The admin kiosk-sale path no longer POSTs a client-side create_batch (Pitfall 2 bug fixed)"
    - "The admin recipe-sale charge body forwards modified_ingredients (Pitfall 3 bug fixed) via the shared core builder"
    - "The admin idempotency key is unified to reference_number (no Math.random suffix, D-05)"
    - "The Manager Override capability SURVIVES on admin after migration (D-07): the admin recipe-sale path still sends `override` (via KioskCore's sale-body) and the client 409/`conflicts`→`#kiosk-stock-override-btn`→resubmit flow still works through KioskCore"
    - "The product-type discount is available on the admin-embedded kiosk (markup + discount-match.js + core logic)"
    - "The existing bottom-of-file test-export accessors in js/admin.js still return the relocated kiosk state by delegating to KioskCore (exact property names unchanged), so existing admin tests pass unmodified"
    - "Existing admin Jest suites pass unweakened"
  artifacts:
    - path: "js/admin.js"
      provides: "Slimmed admin consumer: KioskCore.init with credentials:'include', no duplicated kiosk payment/cart/discount definitions"
      contains: "KioskCore.init"
    - path: "admin.html"
      provides: "#tab-kiosk discount markup + discount-match.js script tag"
      contains: "kiosk-discount-zone"
  key_links:
    - from: "js/admin.js"
      to: "js/kiosk-core.js"
      via: "KioskCore.init({ buildAuthOptions: credentials:'include' })"
      pattern: "credentials.*include"
    - from: "admin.html"
      to: "js/lib/discount-match.js"
      via: "script tag"
      pattern: "discount-match\\.js"
    - from: "admin #kiosk-stock-override-btn"
      to: "js/kiosk-core.js override handler"
      via: "override affordance now driven by KioskCore, not admin's deleted local copy"
      pattern: "kiosk-stock-override-btn"
---

<objective>
Wire the admin-embedded kiosk (`admin.js` / `admin.html` `#tab-kiosk`) to consume `KioskCore`, deleting admin's ~37 duplicated `kiosk*` definitions (they now live in kiosk-core.js). Because kiosk.js's versions are canonical (D-02), this migration EXPLICITLY fixes the two drift bugs research found (D-05): the client-side duplicate `create_batch` loop is deleted (Pitfall 2), and the recipe-sale charge body now forwards `modified_ingredients` (Pitfall 3) via the shared core builder; the idempotency key unifies on `reference_number` (no `Math.random()` suffix). CRITICAL (D-07): deleting admin's `kioskProceedToPayment` also deletes admin's LOCAL Manager Override implementation — but the override is PRESERVED because KioskCore now owns it (ported from admin.js in plan 03), so admin must route its override affordance through KioskCore, not lose it. Give the admin kiosk the product-type discount (SC#3) by porting the ~45 lines of discount markup into `#tab-kiosk` and adding the missing `discount-match.js` script tag. Admin retains its cookie auth (`credentials:'include'`) — the one legitimate env seam.

Purpose: Collapse the second copy of the payment path (SC#1) and deliver discount parity on admin (SC#3), fixing the two admin-only money/cart bugs as intentional, verified changes (not silent side effects, D-05), while ensuring the Manager Override survives the migration (D-07).
Output: admin.js consumes KioskCore; admin.html has discount UI + discount-match.js; both bugs fixed; override survives via KioskCore; admin tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- admin.js current-state anchors (verified this session) -->
  js/admin.js:11001   var idempotencyKey = refNumber + '-' + Math.random().toString(36).slice(2,9);  (drop suffix, D-05)
  js/admin.js:11026   override: _kioskStockOverride || false  (admin's WORKING override field — now owned by KioskCore's sale-body per plan 03, D-07)
  js/admin.js:11066-11088  admin's 409/conflicts handler + #kiosk-stock-override-btn onclick (its LOCAL override — deleted here, now lives in KioskCore, D-07)
  js/admin.js:11102   override: saleBody.override || false  (confirm-body override — now owned by KioskCore)
  js/admin.js:11197   adminApiPost('create_batch', {...})  — client-side dup-batch loop ~11187-11212 (DELETE, Pitfall 2)
  js/admin.js:11017-11027  recipeSaleBody WITHOUT modified_ingredients  (Pitfall 3 — fixed by using core builder)
  js/admin.js:10921-11443  kioskProceedToPayment (523 lines, _adminDoSale closure) — replaced by KioskCore.proceedToPayment
  js/admin.js:9834-9849   kioskCalcTotals (no discount branch) — replaced by KioskCore.calcTotals (now discount-aware)
  js/admin.js:12755-12770 bottom module.exports test-export accessors (e.g. _kioskGetQuote/_kioskGetCart over admin's _kiosk* vars) — rewrite to delegate to KioskCore (WARNING fix)
  admin.js admin-only functions to KEEP (RESEARCH.md:354): kioskOpenModifyPanel, kioskSaveAsNewRecipe,
    kioskSaveRecipeQuickEdit, kioskShowAdminCustomItemModal, kioskShowAdminGiftCardIssueModal,
    kioskShowAdminGiftCardMgmtModal, kioskSubmitAdminCustomItem, kioskSubmitAdminGiftCardIssue

<!-- admin.js auth env (the legitimate seam, D-02) -->
  KioskCore.init({ mwUrl: kioskMwUrl(), buildAuthOptions: function () { return { credentials: 'include' }; } });
  admin.js has ZERO x-device-token references — cookie auth is retained, not unified (RESEARCH.md:336).

<!-- Manager Override on admin (D-07) -->
  admin.html already carries the override markup: #kiosk-stock-conflict + #kiosk-stock-override-btn
    (admin.html:663-665) — NO markup port needed. After deleting admin's local override handler,
    the button must be driven by KioskCore's ported handler (plan 03); the recipe-sale charge from
    admin flows through KioskCore's override-aware sale-body, still sending `override`.

<!-- admin.html anchors (verified this session) -->
  admin.html:997  <script src="js/admin.min.js?v=...">  — discount-match.js + kiosk-core.min.js tags added in plan 01
  admin.html:992  recipe-grouping.js  (block reference)
  Discount markup source: kiosk.html:184-222 (cart-zone + popover) and kiosk.html:426-468 (mgmt modal)
  CSS already shared: admin.html loads kiosk.min.css (all .kiosk-discount-* rules present) — no stylesheet work.

Note: the require guard + kiosk-core.min.js/discount-match.js <script> tags were added to admin.js/admin.html
  in plan 01. This plan does the actual KioskCore consumption + markup port.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire admin.js to KioskCore, delete duplicated definitions, fix both drift bugs + unify idempotency (D-05), preserve Manager Override via core (D-07)</name>
  <files>js/admin.js</files>
  <read_first>
    - js/admin.js:10921-11443 (admin kioskProceedToPayment + _adminDoSale + create_batch loop ~11187-11212 + idempotency 11001 + override field 11026 + 409/override handler 11066-11088 + confirm override 11102)
    - js/admin.js:11017-11027 (recipeSaleBody missing modified_ingredients — Pitfall 3)
    - js/admin.js:9834-9849 (admin kioskCalcTotals, no discount) and the other duplicated kiosk* definitions (grep `function kiosk` in admin.js to enumerate the ~37 to delete vs the 8 admin-only to keep)
    - js/admin.js:12755-12770 (bottom module.exports test-export accessors to rewrite as KioskCore delegates — WARNING fix)
    - js/kiosk.js:3300-3313 (canonical sale-body now living in kiosk-core.js — reference for what the core builder forwards, incl. override per plan 03)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:123-163 (admin migration: init env, delete create_batch loop, modified_ingredients fix, idempotency unify)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:198-208, 341-343 (Pitfall 2/3 detail, env seam table)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md D-07 (override reverse-drift — admin's local copy is deleted but the capability moves to KioskCore, not away)
    - zoho-middleware/routes/pos-recipe.js:320-335 (server 409 stock gate the override affordance must still satisfy)
    - tests/frontend/admin-recipe-modify.test.js, tests/frontend/admin-gift-card-mgmt.test.js (must stay green)
    - CLAUDE.md (grep all usages before delete; one logical change per commit; run full FE suite)
  </read_first>
  <action>
    Add `KioskCore.init({ mwUrl: kioskMwUrl(), buildAuthOptions: function () { return { credentials: 'include' }; } });` at admin's kiosk-tab bootstrap (PATTERNS.md:135-142) — admin keeps cookie auth, the legitimate env seam. Replace every admin call to a now-shared function (kioskProceedToPayment, kioskCalcTotals, kioskAddToCart, kioskRenderCart, the discount functions, SO logic, etc.) with the KioskCore.* method, then DELETE the ~37 duplicated definitions from admin.js. KEEP the 8 admin-only functions listed in <interfaces>. This deletion inherently fixes the drift bugs — make each an EXPLICIT, verifiable outcome (D-05, not silent):
      (1) Delete the client-side create_batch loop (js/admin.js ~11187-11212) — server auto-creates via pos.js:1219 (Pitfall 2).
      (2) The recipe-sale charge now flows through KioskCore's canonical sale-body, forwarding modified_ingredients (Pitfall 3).
      (3) Idempotency uses reference_number (no Math.random() suffix) via the core builder (D-05).
    MANAGER OVERRIDE SURVIVAL (D-07): deleting admin's kioskProceedToPayment also removes admin's LOCAL override implementation (the `override` field at :11026, the 409/`conflicts` handler + `#kiosk-stock-override-btn` onclick at :11066-11088, the confirm-body override at :11102). Do NOT let the capability die — it now lives in KioskCore (ported from admin.js in plan 03). Confirm admin's recipe-sale charge flows through KioskCore's override-aware sale-body (still sends `override`) and that `#kiosk-stock-override-btn` (admin.html:663-665, already present) is driven by KioskCore's ported handler after the delete — no admin-local override handler should remain.
    WARNING fix (test-export delegation): rewrite the bottom module.exports test-export block in js/admin.js (js/admin.js:12755-12770) so each `_kioskGetX`/`_kioskSetX` accessor whose underlying `_kioskX` state was removed from admin.js now delegates to the corresponding KioskCore accessor, preserving the EXACT exported property name so NO existing admin test file needs modification (CLAUDE.md rule 10).
    Wire admin's own discount UI event handlers (added markup in Task 2) to KioskCore discount methods. Keep admin-only recipe quick-edit / save-as-new / gift-card modals. Commit the bug fixes as their own logical commit(s) per CLAUDE.md. Run npm run build before commit.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>node -e "var a=require('./js/admin.js'); console.log('admin.js requires+loads OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function kioskProceedToPayment\|function kioskCalcTotals" js/admin.js` == 0 (payment/totals no longer duplicated in admin.js)
    - admin.js contains `KioskCore.init(` with `credentials: 'include'` and ZERO `x-device-token` references (`grep -c "x-device-token" js/admin.js` == 0)
    - create_batch is gone from the admin kiosk-sale path: `grep -n "create_batch" js/admin.js` shows no occurrence inside the former kioskProceedToPayment range (the remaining create_batch at ~7184 is the unrelated batch-admin path — confirm it is NOT the kiosk-sale loop)
    - No `Math.random()` in the admin sale/idempotency construction: the former `refNumber + '-' + Math.random()...` line (was 11001) is removed
    - MANAGER OVERRIDE SURVIVES (D-07): admin.js no longer defines a local override handler (`grep -c "kiosk-stock-override-btn" js/admin.js` == 0 — it moved to KioskCore in plan 03) AND admin's recipe-sale charge routes through KioskCore's override-aware sale-body (the override affordance is reachable on admin via the ported handler + admin.html:663-665 markup)
    - The bottom module.exports accessors in js/admin.js delegate relocated state to KioskCore with EXACT property names (no throw when called by existing admin tests)
    - The 8 admin-only functions still defined in admin.js (grep confirms kioskSaveAsNewRecipe, kioskShowAdminGiftCardMgmtModal, etc. remain)
    - `npm test` exits 0 (admin FE suites green, unmodified); `npm run lint` exits 0; git diff --stat shows no change under tests/
  </acceptance_criteria>
  <done>admin.js consumes KioskCore with cookie auth; the ~37 duplicated definitions are deleted (SC#1); both drift bugs (dup batch, missing modified_ingredients) and the idempotency-suffix are fixed via the shared core (D-05, explicit + verified); the Manager Override survives via KioskCore (D-07, no local copy left, still sends `override`); the bottom test-export accessors delegate to KioskCore; admin-only UI retained; admin FE suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Port the product-type discount markup into admin.html #tab-kiosk (SC#3 structure side)</name>
  <files>admin.html, js/admin.js</files>
  <read_first>
    - kiosk.html:184-222 (cart-zone discount button + applied + popover markup — verbatim source)
    - kiosk.html:426-468 (discount management modal markup — verbatim source)
    - admin.html — the #tab-kiosk panel and its cart/summary region (grep `tab-kiosk` and `kiosk-cart` to find the insertion point matching kiosk.html's relative position)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:167-206 (discount markup to port, discount-match.js already tagged in plan 01)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:301-306, 248-257 (3 things discount parity needs; markup + discount-match tag + core logic)
  </read_first>
  <action>
    Port the discount markup from kiosk.html:184-222 (kiosk-discount-zone button, kiosk-discount-applied row, kiosk-discount-popover) and kiosk.html:426-468 (management modal) verbatim into admin.html's #tab-kiosk panel at the same relative position as in kiosk.html (inside the cart/summary region), preserving all element IDs unchanged (RESEARCH.md:337 — IDs are identical across surfaces by design). The CSS is already shared via kiosk.min.css (loaded by admin.html) — no stylesheet work. The discount-match.js and kiosk-core.min.js <script> tags were added in plan 01; confirm they are present. Do NOT add any SO-import markup (Pitfall 5 scope fence). The #kiosk-stock-conflict / #kiosk-stock-override-btn override markup is ALREADY present in admin.html (663-665) — no port needed for the override. Wire admin.js's new #kiosk-discount-* event handlers to KioskCore discount methods (coordinate with Task 1). Run npm run build (stamps admin.html) before commit.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>grep -c "kiosk-discount-zone\|kiosk-discount-popover" admin.html</automated>
    <automated>grep -c "discount-match.js" admin.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "kiosk-discount-zone" admin.html` >= 1 and `grep -c "kiosk-discount-popover" admin.html` >= 1 (cart-zone + popover ported)
    - `grep -c "kiosk-discount" admin.html` is > 0 where it was 0 before this phase (discount DOM now present on admin)
    - `grep -c "discount-match.js" admin.html` == 1 (script tag present — added plan 01, confirmed here)
    - `grep -c "kiosk-so-" admin.html` == 0 (scope fence held — no SO UI)
    - `npm test` and `npm run lint` exit 0; `npm run build` succeeds and re-stamps admin.html
  </acceptance_criteria>
  <done>The product-type discount UI is present in admin.html #tab-kiosk with shared CSS + discount-match.js + KioskCore logic — the admin-embedded kiosk now has discount parity (SC#3); no SO UI added (Pitfall 5); admin FE suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser (OAuth cookie) → pos.js | admin kiosk uses `credentials:'include'` session cookie; must be preserved exactly |
| admin client → create_batch (Apps Script) | the dup-batch client call crosses here; deleting it removes duplicate/unlinked batch records |
| admin client → pos-recipe.js stock gate | override affordance must survive the delete of admin's local copy (now driven by KioskCore) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | Spoofing | admin auth seam | mitigate | admin injects ONLY `credentials:'include'` (cookie), zero x-device-token — verify `grep x-device-token js/admin.js` == 0 after migration; do not send both auth mechanisms |
| T-48-10 | Repudiation (duplicate batch) | client create_batch loop | mitigate | Delete the loop; server auto-creates one linked batch (pos.js:1219). Verified: no create_batch on the kiosk-sale path (Pitfall 2) |
| T-48-11 | Tampering (price mismatch) | recipe-sale charge body | mitigate | Route through canonical core builder forwarding modified_ingredients so charged price == edited preview (Pitfall 3) |
| T-48-12 | Tampering (duplicate charge) | idempotency key | mitigate | Unify on reference_number; drop admin's redundant Math.random() suffix (D-05) — no weaker scheme introduced |
| T-48-12O | Tampering (silent feature loss) | admin Manager Override | mitigate | Deleting admin's local override handler must NOT remove the capability — it now lives in KioskCore (D-07); verify admin sends `override` via the core sale-body and `#kiosk-stock-override-btn` is driven by KioskCore |
| T-48-SC | Tampering | npm installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- admin.js: no duplicated payment/cart/discount definitions; KioskCore.init cookie auth; no x-device-token.
- Both drift bugs fixed + verified (no client create_batch on sale path; modified_ingredients forwarded; idempotency unified).
- Manager Override survives via KioskCore (D-07): admin sends `override`, no admin-local override handler remains, `#kiosk-stock-override-btn` driven by core.
- Bottom module.exports accessors in admin.js delegate to KioskCore (exact names) — no test edits.
- admin.html: discount markup + discount-match.js present; no SO UI (Pitfall 5).
- admin FE + middleware suites green; lint clean; build re-stamps admin.html.
</verification>

<success_criteria>
The admin-embedded kiosk consumes the shared KioskCore (SC#1 — no second copy remains), has the product-type discount (SC#3), and the two admin-only money/cart drift bugs plus the idempotency suffix are fixed as explicit, verified changes (D-05) — with cookie auth preserved, the Manager Override surviving via KioskCore (D-07), and no SO UI added.
</success_criteria>

<output>
Create `.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-04-SUMMARY.md` when done.
</output>
