---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 02
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - js/kiosk-core.js
  - js/kiosk.js
autonomous: true
requirements: [KIOSK-01]
must_haves:
  truths:
    - "Cart building, catalog/recipe rendering, and totals (including the discount branch) live in js/kiosk-core.js, and js/kiosk.js consumes them via KioskCore.* / KioskCore accessors"
    - "The 12 product-type discount functions and their transitive helpers exist in kiosk-core.js, no longer defined in kiosk.js"
    - "Existing kiosk Jest suites pass unweakened after the migration"
  artifacts:
    - path: "js/kiosk-core.js"
      provides: "Cart/catalog/render/totals + full discount subsystem + module-scope state + accessors + auth-injection seam"
      contains: "kioskCalcTotals"
    - path: "js/kiosk.js"
      provides: "Slimmed standalone consumer wiring these functions to KioskCore"
      contains: "KioskCore.init"
  key_links:
    - from: "js/kiosk.js"
      to: "js/kiosk-core.js"
      via: "KioskCore.init({ mwUrl, buildAuthOptions }) at bootstrap"
      pattern: "KioskCore\\.init\\("
    - from: "js/kiosk-core.js discount branch"
      to: "js/lib/discount-match.js"
      via: "typeof discountMatches === 'function' guarded call"
      pattern: "discountMatches"
---

<objective>
Migrate the first half of the shared kiosk logic — cart building, product/recipe catalog rendering, totals (`kioskCalcTotals` including its discount branch), the 12-function product-type discount subsystem, their transitive helpers, and the shared module-scope state — out of `js/kiosk.js` into `js/kiosk-core.js` (kiosk.js is canonical per D-02). Wire `js/kiosk.js` to consume them through `KioskCore.*` and KioskCore accessors, and establish the `KioskCore.init({ mwUrl, buildAuthOptions })` auth-injection seam (D-02). Payment/checkout/dual-cart migration is plan 03; admin consumption is plan 04.

Purpose: Get the non-payment surface area — the largest count of functions and the discount subsystem (SC#3 logic side) — into the shared core first, so plan 03 can build the payment path on top of already-migrated cart/totals state.
Output: kiosk-core.js gains cart/catalog/render/totals + discount; kiosk.js slims to consumer wiring; kiosk tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Shared function inventory (RESEARCH.md:345-358). Non-payment subset migrated in THIS plan: -->
Cart/catalog/render/totals + selection (from the 37 shared, minus payment path):
  kioskAddRecipeToCart, kioskAddToCart, kioskCalcTotals, kioskCartHasKits, kioskCartIsEmpty,
  kioskCheckRecipeAvailability, kioskClearCart, kioskFmt, kioskItemTax, kioskLoadProducts,
  kioskLoadRecipes, kioskPopulateCategories, kioskRecipePrice, kioskRecipePriceForContext,
  kioskRenderAvailBanner, kioskRenderCart, kioskRenderProducts, kioskRenderRecipeIngredients,
  kioskRenderRecipes, kioskSelectCustomer, kioskSelectSaleType, kioskSetMode, kioskSetQty,
  kioskShowCustomerStep, kioskShowRecipePrompt, kioskShowView, kioskUpdateAddToCartButton,
  kioskUpdateSummaryPrice, kioskShowError
  (kioskFetchRecipeQuote / kioskScheduleRecipeQuote / kioskCheckTerminal / kioskSetTerminalStatus
   / kioskStartCheckout / kioskProceedToPayment / kioskShowReceipt → DEFER to plan 03, payment path)

Discount subsystem (12, kiosk.js-only today → move to core, D-04) RESEARCH.md:303:
  kioskApplyDiscount, kioskCalcDiscountAmount, kioskCollectAppliesTo, kioskDiscountScopeLabel,
  kioskLoadDiscountPresets, kioskPopulateDiscountForm, kioskRefreshAfterDiscountChange,
  kioskRemoveDiscount, kioskRenderDiscountMgmtList, kioskShowDiscountMgmt,
  kioskShowDiscountPopover, kioskUpdateDiscountDisplay

Discount transitive helpers that MUST move with the discount branch (RESEARCH.md:352):
  kioskGetItemType, kioskIsConsignment, kioskItemCategory, kioskR2

Module-scope state to relocate into kiosk-core.js private closure (RESEARCH.md:358):
  _kioskCart, _kioskDiscount, _kioskGiftCard, _kioskCustomer, _kioskRecipeContext,
  _kioskSelectedRecipe, _kioskSaleType, _kioskTargetVolumeL, _kioskModifiedIngredients,
  _kioskMillGrain, _kioskQuote, _kioskSaleData, _kioskMode
  → expose via accessors mirroring existing _kioskGetCart/_kioskSetSelectedRecipe test-export
    idiom (js/kiosk.js:5511-5513). Both consumers read/write via accessors after the move.

Auth-injection seam (D-02, RESEARCH.md:333-343): the ONLY real env difference is auth.
  kiosk.js env: KioskCore.init({ mwUrl: kioskMwUrl(), buildAuthOptions: function () {
    return { headers: { 'x-device-token': kioskDeviceToken() } }; }});
  mwUrl is byte-identical in both files (js/kiosk.js:571-574) — NOT an injected seam; keep
  kioskMwUrl() available (duplicate or expose KioskCore.mwUrl()).

D-06 naming: functions exposed on KioskCore drop the kiosk prefix (KioskCore.calcTotals,
  KioskCore.addToCart, KioskCore.applyDiscount, ...). Internal function names may keep the
  kiosk prefix; the namespace property is the de-prefixed one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move cart/catalog/render/totals + module state + init/auth seam into kiosk-core.js; wire kiosk.js</name>
  <files>js/kiosk-core.js, js/kiosk.js</files>
  <read_first>
    - js/kiosk.js:571-574 (kioskMwUrl, byte-identical helper) and js/kiosk.js:15-17 (kioskDeviceToken)
    - js/kiosk.js — the cart/catalog/render/totals function bodies listed in <interfaces> (grep each `function kioskX` to find its range before lifting; kioskCalcTotals is the totals core)
    - js/kiosk.js:5499-5533 (existing accessor/test-export idiom to mirror for relocated module state)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:69-91, 316-339 (init(env) seam, kioskMwUrl handling, auth seam)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:139-179, 331-358 (Pattern 1, environment seam enumeration, module-scope state boundary)
    - CLAUDE.md (ES5-only; grep all usages before moving any function; run full FE suite after shared-util change)
  </read_first>
  <action>
    Lift the cart/catalog/render/totals + selection functions listed in <interfaces> (Task-1 subset) verbatim from js/kiosk.js into js/kiosk-core.js as internal function declarations, using kiosk.js as canonical (D-02). Relocate the shared module-scope `var _kiosk*` state into kiosk-core.js's private closure and expose read/write accessors mirroring the existing `_kioskGetCart`/`_kioskSetSelectedRecipe` idiom (js/kiosk.js:5511-5513). Add `kcInit(env)` handling (from plan 01 skeleton) so any fetch inside these functions routes auth through `_kcEnv.buildAuthOptions()` — shallow-merge auth opts into the fetch options object (PATTERNS.md:85-90). Add each moved function to the `KioskCore` namespace with the D-06 de-prefixed property name. In js/kiosk.js: delete the moved definitions, add `KioskCore.init({ mwUrl: kioskMwUrl(), buildAuthOptions: function () { return { headers: { 'x-device-token': kioskDeviceToken() } }; } });` at bootstrap (DOMContentLoaded, before first cart/render call), and replace every internal reference to a moved function/state with the KioskCore accessor/method. Keep kioskMwUrl()/kioskDeviceToken()/device-token auth in kiosk.js (kiosk-only per D-46). Do NOT run npm run build inside this task if it churns unrelated bundles beyond what's needed — but you MUST run it before commit per CLAUDE.md rule 9.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>node -e "var K=require('./js/kiosk-core.js'); ['calcTotals','addToCart','renderCart','init'].forEach(function(m){ if(typeof K[m]!=='function'){console.error('missing '+m);process.exit(1)} }); console.log('core methods present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function kioskCalcTotals" js/kiosk.js` == 0 and `grep -c "kioskCalcTotals" js/kiosk-core.js` >= 1 (totals moved)
    - js/kiosk.js contains `KioskCore.init(` with `x-device-token` in its buildAuthOptions
    - KioskCore exposes calcTotals/addToCart/renderCart/init (node smoke check above passes)
    - `npm test` exits 0 (all existing kiosk FE tests green, unmodified)
    - `npm run lint` exits 0 (kiosk-core.js + kiosk.js ES5-clean)
    - git diff --stat shows no change under tests/
  </acceptance_criteria>
  <done>Cart/catalog/render/totals + shared state live in kiosk-core.js behind the KioskCore namespace and accessors; kiosk.js consumes them via KioskCore.init + method calls; kiosk FE suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Move the 12 discount functions + transitive helpers into kiosk-core.js; wire kiosk.js discount UI</name>
  <files>js/kiosk-core.js, js/kiosk.js</files>
  <read_first>
    - js/kiosk.js — the 12 discount function bodies + kioskGetItemType/kioskIsConsignment/kioskItemCategory/kioskR2 (grep each to find ranges); RESEARCH cites discount subsystem at js/kiosk.js:868-1084, 4787-4980+
    - js/kiosk.js:1018-1019 (existing `typeof discountMatches === 'function'` guarded call into js/lib/discount-match.js)
    - js/lib/discount-match.js (classifyDiscountItem/discountMatches — UNCHANGED, keep calling it)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:301-306 (discount-parity: 3 things required; logic side is this task) and 181-188 (Don't Hand-Roll: do not duplicate discount-match.js)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md D-04 (discount moves into core, no admin override)
  </read_first>
  <action>
    Move the 12 discount functions and the transitive helpers (kioskGetItemType, kioskIsConsignment, kioskItemCategory, kioskR2) from js/kiosk.js into js/kiosk-core.js (D-04). Ensure kioskCalcTotals's discount branch (already in core from Task 1) calls these in-core helpers and continues to call the external `discountMatches`/`classifyDiscountItem` globals guarded by `typeof discountMatches === 'function'` (RESEARCH.md:181-188 — do NOT duplicate discount-match.js into core). Expose the public discount functions on KioskCore with D-06 de-prefixed names (KioskCore.applyDiscount, KioskCore.removeDiscount, KioskCore.showDiscountPopover, KioskCore.showDiscountMgmt, ...). In js/kiosk.js: delete the moved definitions; keep the kiosk.js `#kiosk-discount-*` DOM event listeners as thin wrappers that call the KioskCore methods (PATTERNS.md:119 — discount UI event wiring stays in the consumer, logic moves to core). Run npm run build before commit.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>node -e "var K=require('./js/kiosk-core.js'); ['applyDiscount','removeDiscount','calcDiscountAmount'].forEach(function(m){ if(typeof K[m]!=='function'){console.error('missing '+m);process.exit(1)} }); console.log('discount methods present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function kioskApplyDiscount\|function kioskCalcDiscountAmount\|function kioskGetItemType" js/kiosk.js` == 0 (all discount + transitive helpers moved out of kiosk.js)
    - `grep -c "kioskApplyDiscount\|kioskCalcDiscountAmount" js/kiosk-core.js` >= 1
    - kiosk-core.js still references `discountMatches` guarded by `typeof discountMatches === 'function'` (not a re-implementation)
    - KioskCore exposes applyDiscount/removeDiscount/calcDiscountAmount (node smoke check passes)
    - `npm test` and `npm run lint` exit 0; no test files modified
  </acceptance_criteria>
  <done>The full product-type discount subsystem lives in kiosk-core.js (SC#3 logic side); kiosk.js retains only thin DOM event wrappers delegating to KioskCore; discount-match.js still consumed as a shared global; kiosk FE suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → pos.js API | cart/discount totals computed client-side, server re-validates (untouched) |
| relocated module state | _kiosk* state moves closure ownership; both consumers must go through accessors, not stale local vars |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | Tampering | discount calc relocation | mitigate | Keep discount-match.js as the single source for classify/match (guarded call); do NOT re-implement — prevents a third drifted copy (RESEARCH.md Don't Hand-Roll) |
| T-48-04 | Spoofing | auth-injection seam via KioskCore.init | mitigate | kiosk.js injects ONLY `x-device-token` header; buildAuthOptions merges auth without adding any stale x-api-key. kiosk-device-token.test.js T2/T3 (`x-api-key` undefined) stay green unmodified |
| T-48-05 | Repudiation | shared totals state ownership | accept | Server re-computes tax/discount independently in pos.js (untouched) — client totals are display-only; no money decision moves client-side |
| T-48-SC | Tampering | npm installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- kiosk FE Jest suite passes unweakened (SC#2, D-03).
- Discount logic present in kiosk-core.js, absent from kiosk.js (SC#3 logic side).
- Lint clean (ES5). Build regenerates bundles.
- No test file edited.
</verification>

<success_criteria>
Cart/catalog/render/totals and the full discount subsystem are in js/kiosk-core.js behind the KioskCore namespace; js/kiosk.js consumes them via init + method calls; the standalone kiosk behaves identically and its Jest suite is green — first half of the de-fork complete.
</success_criteria>

<output>
Create `.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-02-SUMMARY.md` when done.
</output>
