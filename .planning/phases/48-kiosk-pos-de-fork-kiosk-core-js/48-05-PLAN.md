---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 05
type: execute
wave: 5
depends_on: ["48-04"]
files_modified:
  - tests/frontend/kiosk-core-parity.test.js
autonomous: true
requirements: [KIOSK-01]
must_haves:
  truths:
    - "An automated parity test asserts the standalone and admin surfaces produce identical fetch URL + body (excluding reference_number/idempotency_key) for the same cart"
    - "The parity test asserts correct per-surface auth injection: kiosk sends x-device-token; admin sends credentials:'include' with no x-device-token"
    - "npm test, npm run lint, and npm run build are all clean with regenerated bundles"
  artifacts:
    - path: "tests/frontend/kiosk-core-parity.test.js"
      provides: "admin-vs-standalone payload + auth parity assertions (SC#2, D-03)"
      contains: "reference_number"
  key_links:
    - from: "tests/frontend/kiosk-core-parity.test.js"
      to: "js/kiosk.js + js/admin.js"
      via: "require both, build identical cart, compare fetch mock calls"
      pattern: "require\\(.*kiosk\\.js|require\\(.*admin\\.js"
---

<objective>
Prove the de-fork with the SC#2/D-03 automated parity test: build an identical cart through each surface's own wiring and assert both produce the same fetch URL and the same request body (after normalizing the non-deterministic `reference_number`/`idempotency_key`), while correctly diverging on auth (kiosk `x-device-token` header vs admin `credentials:'include'` cookie). Then run the final full gate: `npm test`, `npm run lint`, `npm run build` clean with all bundles regenerated, and confirm KIOSK-01 traceability.

Purpose: This is the machine-checkable half of "the two paths can no longer diverge" — it fails loudly if a future edit re-forks the payload. The manual iPad gate (SC#5) follows in plan 06.
Output: tests/frontend/kiosk-core-parity.test.js green; full suite + lint + build clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Analog: tests/frontend/kiosk-device-token.test.js (same stub/require/fetch-mock-assert idiom).
     Copy verbatim (PATTERNS.md:247-303): -->
Env stub block (kiosk-device-token.test.js:17-55): global.window/navigator/console/fetch(jest.fn),
  setTimeout/setInterval synchronous-fire mocks, global.alert, global.SHEETS_CONFIG {MIDDLEWARE_URL:'http://mw.test', ...}
injectEl helper (kiosk-device-token.test.js:64-75), beforeEach/afterEach reset (96-109),
  fetch-mock assertion via global.fetch.mock.calls[N][0] (URL) and [N][1] (options/body).

Test-export accessors to drive an identical cart on each surface (js/kiosk.js:5511-5513 idiom;
  admin mirrors at js/admin.js:12755-12757): use each file's _kioskSetCart/_kioskGetCart-style
  hooks + _kioskSetSelectedRecipe to seed the same cart before triggering the sale.

Parity comparison rules (RESEARCH.md:308-327):
  - Payloads are NON-deterministic (refNumber = 'KIOSK-'+Date.now()). A raw JSON.stringify equality
    fails. STRIP/normalize reference_number + idempotency_key before deep-equal, then assert the
    rest of the body is identical field-by-field.
  - After D-05, admin's idempotency_key === reference_number (Math.random suffix dropped) — the test
    documents/locks this.
  - Auth: kiosk call has headers['x-device-token'] set; admin call has credentials:'include' and NO
    x-device-token key (RESEARCH.md:303, Security Domain regression guard).

Do NOT modify existing tests (CLAUDE.md rule 10) — this is a NEW file only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/frontend/kiosk-core-parity.test.js (SC#2 / D-03)</name>
  <files>tests/frontend/kiosk-core-parity.test.js</files>
  <behavior>
    - For an identical seeded cart, kiosk's sale-trigger and admin's sale-trigger call fetch with the SAME URL (mwUrl + '/api/kiosk/sale' or the recipe-sale route)
    - For the same cart, the two request bodies are deep-equal AFTER removing reference_number and idempotency_key
    - kiosk's fetch options carry headers['x-device-token']; admin's carry credentials:'include' and headers['x-device-token'] is undefined
    - Both surfaces produce idempotency_key === reference_number (D-05 unification locked)
  </behavior>
  <read_first>
    - tests/frontend/kiosk-device-token.test.js (full file — stub/require/injectEl/fetch-mock-assert idiom to copy)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:243-303 (exact stub blocks, require pattern, parity assertion extension)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:308-329 (Parity Test Strategy — non-deterministic key normalization, auth divergence assertion)
    - js/kiosk.js:5501-5533 and js/admin.js:12755-12757 (test-export accessors to seed the cart)
    - CLAUDE.md (do NOT modify existing tests; ES5 lint applies to js/ not tests/)
  </read_first>
  <action>
    Create tests/frontend/kiosk-core-parity.test.js copying the env-stub, injectEl, and beforeEach/afterEach blocks from kiosk-device-token.test.js verbatim (PATTERNS.md:247-292). require both js/kiosk.js and js/admin.js (each internally require('./kiosk-core.js') via the plan-01 guard). Seed an identical cart on each surface via its own _kioskSetCart/_kioskSetSelectedRecipe test hooks, then trigger the sale through each surface's own wiring. Capture the fetch mock calls: assert the URL is identical, deep-equal the two bodies AFTER stripping reference_number + idempotency_key (RESEARCH.md:308-312 — raw stringify fails because of Date.now()), and assert idempotency_key === reference_number on both. Assert auth divergence: kiosk call has headers['x-device-token']; admin call has credentials:'include' and undefined x-device-token. Cover at least a plain product sale and a recipe sale (so modified_ingredients parity is exercised — Pitfall 3). Optionally mock Date.now to a fixed value to make the pre-strip comparison tighter, but stripping is the primary mechanism.
  </action>
  <verify>
    <automated>npx jest tests/frontend/kiosk-core-parity.test.js</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - tests/frontend/kiosk-core-parity.test.js exists and contains `reference_number` and `idempotency_key` normalization + a deep-equal body assertion
    - The test requires BOTH js/kiosk.js and js/admin.js
    - The test asserts kiosk `x-device-token` present AND admin `x-device-token` undefined + `credentials: 'include'`
    - `npx jest tests/frontend/kiosk-core-parity.test.js` exits 0 (parity holds)
    - `npm test` exits 0 overall; no PRE-EXISTING test file modified (git diff --stat: only the new file under tests/)
  </acceptance_criteria>
  <done>The automated admin-vs-standalone parity test passes: identical URL + body (modulo idempotency/reference) for the same cart, correct per-surface auth — SC#2/D-03 machine-checkable guard in place.</done>
</task>

<task type="auto">
  <name>Task 2: Final full gate — build, lint, both suites, KIOSK-01 traceability</name>
  <files>(build/lint/test gate — regenerates js/*.min.js artifacts, no source authored)</files>
  <read_first>
    - CLAUDE.md (before-every-commit gate: npm test AND cd zoho-middleware && npm test AND npm run lint; npm run build after JS changes)
    - package.json:18 (build script — regenerates main.js/main.min.js, admin.min.js, kiosk.min.js, kiosk-core.min.js + stamps)
    - .planning/ROADMAP.md §Phase 48 SC#4 (build clean, main.js/main.min.js and admin.min.js regenerated)
    - .planning/REQUIREMENTS.md (KIOSK-01 wording for traceability note in SUMMARY)
  </read_first>
  <action>
    Run npm run build to regenerate all bundles (main.js/main.min.js via concat, admin.min.js, kiosk.min.js, kiosk-core.min.js) and re-stamp HTML ?v= cache-busts (SC#4). Run the full gate: npm test, cd zoho-middleware && npm test, npm run lint — all must exit 0. Confirm SC#4's "no behaviour-changing logic beyond the discount-parity fix + the two D-05 drift fixes" holds by reviewing git diff for the phase. In the SUMMARY, record KIOSK-01 traceability: which plan delivered each of the 5 success criteria (SC#1 single source → plans 02/03/04; SC#2 parity → this plan; SC#3 discount → 02/04; SC#4 build/lint → this plan; SC#5 iPad → plan 06). Do NOT hand-edit any *.min.js.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>npm test</automated>
    <automated>cd zoho-middleware && npm test; cd ..</automated>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; js/kiosk-core.min.js, js/admin.min.js, js/kiosk.min.js, js/main.min.js all regenerated (newer mtime); admin.html/kiosk.html ?v= stamps updated
    - `npm test` exits 0 (full FE suite incl. new parity test)
    - `cd zoho-middleware && npm test` exits 0
    - `npm run lint` exits 0 (eslint js/ --max-warnings 0, ES5 rule holds)
    - SUMMARY records KIOSK-01 → SC#1..5 plan mapping
  </acceptance_criteria>
  <done>All bundles regenerated; FE + middleware suites + lint all green (SC#4); KIOSK-01 traceability recorded — the de-fork is machine-verified end to end, ready for the manual iPad gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → js/kiosk.js + js/admin.js | parity test drives both surfaces; a re-fork of the payload must fail the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-13 | Tampering | future payload re-fork | mitigate | Parity test locks identical URL+body (modulo idempotency) + per-surface auth — CI fails if the two surfaces diverge again (SC#2) |
| T-48-14 | Spoofing | auth-injection regression | mitigate | Parity test asserts admin has no x-device-token and kiosk has no stale x-api-key (reinforces kiosk-device-token T2/T3) |
| T-48-SC | Tampering | npm installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- kiosk-core-parity.test.js green: identical payload (modulo idempotency) + correct auth per surface.
- Full FE + middleware suites + lint clean; build regenerates all bundles (SC#4).
- No pre-existing test modified.
</verification>

<success_criteria>
The admin-vs-standalone parity test passes and the full build/lint/test gate is clean with all bundles regenerated — SC#2 and SC#4 satisfied, the de-fork is machine-verified.
</success_criteria>

<output>
Create `.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-05-SUMMARY.md` when done.
</output>
