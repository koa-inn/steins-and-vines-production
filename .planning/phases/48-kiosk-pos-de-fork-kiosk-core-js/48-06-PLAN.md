---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 06
type: execute
wave: 6
depends_on: ["48-05"]
files_modified: []
autonomous: false
requirements: [KIOSK-01]
must_haves:
  truths:
    - "A full kiosk sale (including a recipe/product-type discount) completes identically from the standalone kiosk URL and the admin-embedded kiosk tab on staging on iPad Safari"
    - "Terminal charge, void-on-failure, and dual-cart behaviour are intact on both surfaces after the de-fork"
  artifacts: []
  key_links:
    - from: "staging.steinsandvines.ca kiosk + admin kiosk tab"
      to: "prod middleware pos.js"
      via: "real terminal sale on iPad Safari"
      pattern: "manual"
---

<objective>
Human-verify SC#5: on staging on iPad Safari, perform a full kiosk sale — including a recipe/product-type discount — from BOTH the standalone kiosk URL and the admin-embedded kiosk tab, confirming identical behaviour and that terminal charge, void-on-failure, and dual-cart all remain intact after the de-fork. This gate cannot be automated: there is no separate staging middleware (staging calls prod middleware), so this is a real terminal transaction requiring a person, an iPad, and the Helcim terminal (D-03, RESEARCH.md:329).

Purpose: Confirm the relocation into kiosk-core.js preserved real-world money-path behaviour on the actual target device — the last acceptance criterion before KIOSK-01 is closed.
Output: Human sign-off (or logged defects → gap-closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the de-fork to staging</name>
  <files>(deploy only — pushes built artifacts to staging, no files authored)</files>
  <read_first>
    - CLAUDE.md §Deployment (Staging: git push origin main → staging.steinsandvines.ca; ALL changes go to staging first; enforce-cname workflow auto-corrects domain)
    - .planning/STATE.md (deploy notes: no separate staging middleware — staging site calls prod middleware)
  </read_first>
  <action>
    Confirm the working tree is built (npm run build already run in plan 05 — all *.min.js current and HTML stamped) and both test suites + lint are green. Push to staging per CLAUDE.md (git push origin main → staging.steinsandvines.ca). Do NOT push to production (production cutover is out of this phase's scope; prod rides a later coupled deploy). Confirm the staging site loads js/kiosk-core.min.js (no 404) on both kiosk.html and admin.html.
  </action>
  <verify>
    <automated>git status --porcelain && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Working tree built and clean (no stale *.min.js relative to sources); `npm run lint` exits 0
    - Pushed to staging (origin main); staging serves js/kiosk-core.min.js with HTTP 200 on both kiosk.html and admin.html (spot-check via curl or browser network tab)
    - Production NOT touched
  </acceptance_criteria>
  <done>The de-fork is live on staging with kiosk-core.min.js served on both surfaces; ready for the iPad manual test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: iPad Safari full-sale parity verification (SC#5)</name>
  <what-built>
    kiosk.js and admin.js now consume a single shared js/kiosk-core.js for all cart/payment/discount/dual-cart logic. The admin-embedded kiosk gained the product-type discount, the duplicate client-side batch creation was removed, the recipe-sale charge now forwards modified_ingredients, and the idempotency key was unified — all deployed to staging.
  </what-built>
  <how-to-verify>
    On an iPad running Safari, against staging.steinsandvines.ca (which calls the PROD middleware — these are REAL terminal charges; refund/reverse afterward as usual):

    STANDALONE KIOSK (kiosk.html):
    1. Open the standalone kiosk URL. Add a product to the cart; add a recipe sale.
    2. Apply a product-type discount — confirm it appears and adjusts the total.
    3. Proceed to payment; complete the terminal charge. Confirm the receipt shows and the Zoho invoice/payment is created with the discounted total.
    4. (Dual-cart) Import/held sales-order flow: confirm it still works as before.
    5. (Void path) Trigger or observe a failure path if feasible; confirm void-on-failure behaviour is unchanged (payment_voided rendered).

    ADMIN-EMBEDDED KIOSK (admin.html → Kiosk tab):
    6. Open admin, go to the Kiosk tab. Repeat steps 1-3: product + recipe + PRODUCT-TYPE DISCOUNT (this is NEW on admin — confirm it now exists and works) + terminal charge + receipt + Zoho invoice.
    7. Confirm a recipe sale where you EDIT ingredient quantities charges the EDITED price (modified_ingredients fix) — the invoice total must match the edited preview, not the base recipe.
    8. Confirm only ONE BrewPad batch is created per kit item sold via the admin kiosk tab (no duplicate) — check BrewPad "Needs Scheduling".

    Both surfaces must behave identically for the same cart. Note any divergence, console error, or money-path discrepancy.
  </how-to-verify>
  <resume-signal>Type "approved" if both surfaces complete a full discounted sale identically with terminal/void/dual-cart intact and the two admin fixes confirmed; otherwise describe the defects (they become gap-closure input).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iPad Safari (staging) → prod middleware/Helcim/Zoho | real money movement; the human gate confirms no regression in the live charge→invoice→void path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-15 | Tampering (real money) | live terminal charge on staging | mitigate | Human runs a real charge on the actual iPad/terminal and refunds after; confirms charged total == displayed discounted total on both surfaces (SC#5) |
| T-48-16 | Repudiation (duplicate batch) | admin kit sale | mitigate | Human confirms exactly one BrewPad batch per kit item on the admin surface (Pitfall 2 fix verified live) |
| T-48-SC | Tampering | npm installs | accept | Zero new dependencies this phase |
</threat_model>

<verification>
- Human confirms identical full-sale behaviour (incl. discount) on both surfaces on iPad Safari.
- Terminal/void/dual-cart intact; admin modified_ingredients + single-batch fixes confirmed live.
- Defects, if any, logged for gap-closure.
</verification>

<success_criteria>
A human has verified on staging on iPad Safari that a full discounted kiosk sale completes identically from both the standalone kiosk and the admin-embedded kiosk, with terminal/void/dual-cart intact and the two admin drift fixes confirmed live — SC#5 satisfied, KIOSK-01 acceptance complete.
</success_criteria>

<output>
Create `.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-06-SUMMARY.md` when done.
</output>
