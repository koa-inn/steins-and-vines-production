---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/kiosk-core.js
  - js/kiosk.js
  - js/admin.js
  - kiosk.html
  - admin.html
  - package.json
autonomous: true
requirements: [KIOSK-01]
must_haves:
  truths:
    - "js/kiosk-core.js loads as an ES5 IIFE and attaches window.KioskCore in the browser and module.exports under Node"
    - "Existing kiosk and admin frontend Jest suites still pass unmodified after kiosk.js/admin.js gain a Node-only require('./kiosk-core.js') guard"
    - "npm run build produces js/kiosk-core.min.js and stamps its ?v= cache-bust on both kiosk.html and admin.html"
  artifacts:
    - path: "js/kiosk-core.js"
      provides: "Skeleton shared module: IIFE, window.KioskCore namespace, KioskCore.init no-op, dual-mode export"
      contains: "window.KioskCore ="
    - path: "js/kiosk-core.min.js"
      provides: "Minified skeleton bundle (build artifact)"
    - path: "package.json"
      provides: "terser target + stamp regex clauses for kiosk-core.min.js"
      contains: "kiosk-core.js -o js/kiosk-core.min.js"
  key_links:
    - from: "js/kiosk.js"
      to: "js/kiosk-core.js"
      via: "Node-only require guard at top of IIFE"
      pattern: "require\\(['\"]\\./kiosk-core\\.js['\"]\\)"
    - from: "js/admin.js"
      to: "js/kiosk-core.js"
      via: "Node-only require guard at top of IIFE"
      pattern: "require\\(['\"]\\./kiosk-core\\.js['\"]\\)"
    - from: "admin.html"
      to: "js/kiosk-core.min.js"
      via: "script tag before admin.min.js"
      pattern: "kiosk-core\\.min\\.js"
---

<objective>
De-risk the single novel mechanic of this phase (RESEARCH.md Assumption A2 / Pitfall 4) BEFORE any bulk extraction: prove that a thin `window.KioskCore` skeleton plus a Node-only `require('./kiosk-core.js')` guard at the top of `kiosk.js`/`admin.js` keeps the existing Jest suites green with zero test edits, and wire the new `kiosk-core.min.js` bundle into the build and both HTML pages.

Purpose: If the guard/skeleton mechanism is wrong, catch it here — with an empty skeleton and no migrated logic — instead of after moving 37 functions on top of a broken foundation.
Output: `js/kiosk-core.js` skeleton, build wiring (`package.json` terser + stamp), `<script>` tags on both HTML pages, Node-only guards in both consumers, all suites green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-CONTEXT.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md
@.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Dual-mode export idiom to replicate (kiosk-core.js attaches window.KioskCore
     unconditionally + module.exports conditionally). Source: js/kiosk.js:5501-5533. -->
Existing pattern (js/kiosk.js:5501):
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = Object.assign(module.exports || {}, { ...accessors + fns... });
  }

Node-only guard to ADD at top of kiosk.js and admin.js IIFE (new idiom, PATTERNS.md:98-106):
  if (typeof window !== 'undefined' && !window.KioskCore && typeof require === 'function') {
    require('./kiosk-core.js');
  }

Build script anchors (verified this session):
  package.json:11  minify:js  — contains "terser js/kiosk.js -o js/kiosk.min.js -c -m"
  package.json:13  stamp:admin — regex-replaces admin.min.js?v=, kiosk.min.css?v= etc. in admin.html
  package.json:14  stamp:kiosk — regex-replaces kiosk.min.js?v=, kiosk.min.css?v= (uses /g)
HTML anchors:
  admin.html:997   <script src="js/admin.min.js?v=mr53l244" defer></script>
  admin.html:992   <script src="js/lib/recipe-grouping.js" defer></script>
  kiosk.html:24    <script src="js/kiosk.min.js?v=mr53l270"></script>  (no defer on kiosk.html)
  kiosk.html:18    <script src="js/lib/discount-match.js"></script>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the kiosk-core.js ES5 skeleton with window.KioskCore + dual-mode export</name>
  <files>js/kiosk-core.js</files>
  <read_first>
    - js/kiosk.js:1-21 (IIFE banner + 'use strict' + kioskDeviceToken helper — the wrapper shape to mirror)
    - js/kiosk.js:5499-5533 (dual-mode module.exports pattern to replicate, D-01)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:21-91 (exact kiosk-core.js skeleton shape, init(env)/_kcEnv seam, D-01/D-06)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:139-175 (Pattern 1 init(env), Pattern 2 dual-mode export)
    - CLAUDE.md (ES5-only rule; never hand-edit *.min.js)
  </read_first>
  <action>
    Create js/kiosk-core.js as an ES5 IIFE (`(function () { 'use strict'; ... })();`) with a banner comment identifying it as the shared kiosk cart/payment/void core (D-01). Inside, declare the private env seam `var _kcEnv = { mwUrl: '', buildAuthOptions: function () { return {}; } };` and `function kcInit(env)` that copies `env.mwUrl` and `env.buildAuthOptions` onto `_kcEnv` (RESEARCH.md:145-160). Build the public namespace object `var KioskCore = { init: kcInit };` — no migrated functions yet, this is the skeleton. Attach it dual-mode exactly per PATTERNS.md:52-66: `if (typeof window !== 'undefined') { window.KioskCore = KioskCore; }` and `if (typeof module !== 'undefined' && module.exports) { module.exports = KioskCore; }`. ES5 only: `var`, function declarations/expressions, no arrow functions / template literals / const / let / destructuring. Mirror the existing `Object.assign` usage only where kiosk.js already uses it. Do NOT move any kiosk* logic into this file yet — later plans populate it.
  </action>
  <verify>
    <automated>node -e "var K=require('./js/kiosk-core.js'); if(typeof K.init!=='function'){process.exit(1)}; console.log('KioskCore.init OK')"</automated>
    <automated>npx eslint js/kiosk-core.js --max-warnings 0</automated>
  </verify>
  <acceptance_criteria>
    - js/kiosk-core.js exists and contains the literal strings `window.KioskCore =` and `module.exports = KioskCore`
    - `require('./js/kiosk-core.js')` under Node returns an object with a `.init` function (no throw)
    - `npx eslint js/kiosk-core.js --max-warnings 0` exits 0 (ES5-clean at ecmaVersion:5)
    - grep confirms zero arrow functions / template literals / `const `/`let ` in js/kiosk-core.js
  </acceptance_criteria>
  <done>js/kiosk-core.js skeleton committed; ES5-clean; loads dual-mode; KioskCore.init is a no-op seam ready for later population.</done>
</task>

<task type="auto">
  <name>Task 2: Add Node-only require guards + build wiring + HTML script tags, prove existing suites stay green (A2 spike)</name>
  <files>js/kiosk.js, js/admin.js, kiosk.html, admin.html, package.json</files>
  <read_first>
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:98-107, 127-142 (Node-only require guard placement in kiosk.js and admin.js)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-PATTERNS.md:167-239 (admin.html/kiosk.html script tags, package.json minify:js + stamp regex — including the distinct kiosk-core regex warning)
    - .planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-RESEARCH.md:210-214, 271-299 (Pitfall 4 guard mechanism, Build Wiring section)
    - js/kiosk.js:1-21 (IIFE top — where the guard goes)
    - js/admin.js:1-15 (IIFE top — where the guard goes)
    - package.json:11-18 (minify:js, stamp:admin, stamp:kiosk, build)
    - admin.html:988-997 and kiosk.html:15-24 (script blocks)
    - tests/frontend/kiosk-device-token.test.js (existing suite that must stay green unmodified)
  </read_first>
  <action>
    (a) Add the Node-only guard `if (typeof window !== 'undefined' && !window.KioskCore && typeof require === 'function') { require('./kiosk-core.js'); }` immediately inside the IIFE at the top of BOTH js/kiosk.js and js/admin.js, before any KioskCore reference (PATTERNS.md:98-106,127-132). This is inert in the browser (real <script> tag runs first) and loads the skeleton under Jest so window.KioskCore exists.
    (b) package.json minify:js: add `&& terser js/kiosk-core.js -o js/kiosk-core.min.js -c -m` immediately before the existing `terser js/kiosk.js ...` clause (PATTERNS.md:227-231).
    (c) package.json stamp:kiosk AND stamp:admin: add one more `.replace(...)` clause each using the DISTINCT non-overlapping regex `/kiosk-core\.min\.js\?v=[^\"]+/g` (PATTERNS.md:233-239) — do not reuse the existing `kiosk\.min\.js` regex.
    (d) admin.html: insert before line 997 `<script src="js/lib/discount-match.js" defer></script>` and `<script src="js/kiosk-core.min.js?v=INITIAL" defer></script>` (discount-match added now so its tag exists; its consumers land in plan 04). kiosk.html: insert before line 24 `<script src="js/kiosk-core.min.js?v=INITIAL"></script>` (no defer — kiosk.html uses plain sync tags).
    (e) Run `npm run build` to generate js/kiosk-core.min.js and stamp both pages. Do NOT hand-edit any *.min.js.
    Keep kiosk.js/admin.js otherwise unchanged — they do not yet call KioskCore.
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>cd zoho-middleware && npm test; cd ..</automated>
    <automated>npm run lint</automated>
    <automated>npm run build && test -f js/kiosk-core.min.js && grep -q "kiosk-core.min.js" admin.html && grep -q "kiosk-core.min.js" kiosk.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "require('./kiosk-core.js')" js/kiosk.js js/admin.js` shows the guard present in both files
    - `npm test` exits 0 with ZERO test files modified (git diff --stat shows no change under tests/)
    - `cd zoho-middleware && npm test` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0 and js/kiosk-core.min.js exists; admin.html and kiosk.html each contain a `kiosk-core.min.js?v=` reference with a stamped (non-INITIAL) version after build
    - tests/frontend/kiosk-device-token.test.js still passes unmodified (A2 spike confirmed: guard does not break the require('../../js/kiosk.js') pattern)
  </acceptance_criteria>
  <done>Guard + build wiring landed; the novel Node-require mechanism (Pitfall 4/A2) is proven safe on the empty skeleton; both test suites + lint + build green with no test edits — the foundation is verified before bulk extraction begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client → pos.js API | untrusted cart/payload crosses here; server re-validates (untouched this phase) |
| Node/Jest module load order | new `require('./kiosk-core.js')` guard must not alter runtime auth or payload behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering | Node-only require guard in kiosk.js/admin.js | mitigate | Guard is gated on `typeof require === 'function'` AND `!window.KioskCore` — inert in browser; verified by existing suites staying green (SC#2 no-weakening) |
| T-48-02 | Spoofing | auth headers on fetch | accept | Skeleton contains no fetch/auth code yet; kiosk-device-token.test.js T2/T3 remain the guard, still green — no auth change in this plan |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | Zero new dependencies this phase (RESEARCH.md §Package Legitimacy Audit: N/A) — no install task, no supply-chain surface added |
</threat_model>

<verification>
- Both frontend and middleware Jest suites pass unmodified.
- `npm run lint` clean (ES5 rule holds on new file).
- `npm run build` regenerates kiosk-core.min.js + stamps both HTML pages.
- No test file under tests/ was edited (CLAUDE.md rule 10).
</verification>

<success_criteria>
The A2/Pitfall-4 guard mechanism is proven on an empty skeleton: window.KioskCore/module.exports exist, both suites + lint + build are green, and the bundle is wired into both pages — de-risking every subsequent extraction plan.
</success_criteria>

<output>
Create `.planning/phases/48-kiosk-pos-de-fork-kiosk-core-js/48-01-SUMMARY.md` when done.
</output>
