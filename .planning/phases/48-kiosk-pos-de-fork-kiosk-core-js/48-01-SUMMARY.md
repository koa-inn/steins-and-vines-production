---
phase: 48-kiosk-pos-de-fork-kiosk-core-js
plan: 01
subsystem: frontend-build
tags: [es5, iife, jest, terser, kiosk, de-fork, node-require-guard]

# Dependency graph
requires: []
provides:
  - "js/kiosk-core.js skeleton IIFE with window.KioskCore + dual-mode module.exports (D-01)"
  - "init(env)/_kcEnv auth-injection seam for later population by extraction plans"
  - "Node-only require('./kiosk-core.js') guard proven safe in both kiosk.js and admin.js (Pitfall 4/A2 de-risked)"
  - "kiosk-core.min.js wired into build (terser + stamp:kiosk + stamp:admin) and both HTML pages"
affects: [48-02, 48-03, 48-04, 48-05, 48-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dual-mode export (window.KioskCore unconditional + module.exports conditional) mirroring existing js/kiosk.js:5501-5533 idiom"
    - "Node-only require guard at top of consumer IIFE: `if (typeof window !== 'undefined' && !window.KioskCore && typeof require === 'function') { require('./kiosk-core.js'); }` — inert in browser, loads skeleton under Jest"

key-files:
  created:
    - js/kiosk-core.js
    - js/kiosk-core.min.js (build artifact)
  modified:
    - js/kiosk.js
    - js/admin.js
    - js/admin.min.js (build artifact)
    - js/kiosk.min.js (build artifact)
    - kiosk.html
    - admin.html
    - package.json

key-decisions:
  - "D-01 skeleton shape confirmed: init(env) is the only public seam populated in this plan; no kiosk* logic migrated yet"
  - "stamp:kiosk / stamp:admin use a distinct kiosk-core regex clause (non-overlapping with the existing kiosk.min.js clause) per PATTERNS.md guidance"

patterns-established:
  - "Node-only require guard pattern (new idiom, no prior in-repo precedent) — validated end-to-end on an empty skeleton before any real logic depends on it"

requirements-completed: [KIOSK-01]

# Metrics
duration: 8min
completed: 2026-07-03
---

# Phase 48 Plan 01: kiosk-core.js Skeleton + Node-Require Guard Spike Summary

**De-risked the Node-only `require('./kiosk-core.js')` guard mechanism (RESEARCH.md Pitfall 4/A2) on an empty `window.KioskCore` skeleton, and wired the new `kiosk-core.min.js` bundle into the build and both HTML pages — before any of the ~37 kiosk functions are migrated.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-07-03T15:40:43-07:00
- **Completed:** 2026-07-03T15:48:01-07:00
- **Tasks:** 2 completed
- **Files modified:** 8 (2 new, 6 modified; 2 of the modified are build artifacts)

## Accomplishments
- `js/kiosk-core.js` created as an ES5 IIFE with `window.KioskCore` + conditional `module.exports`, an `init(env)`/`_kcEnv` seam, and zero migrated business logic (pure skeleton)
- Node-only require guard added to the top of both `js/kiosk.js` and `js/admin.js` IIFEs — proven inert in the browser and functional under Jest with zero test-file edits
- Build wiring landed: `package.json` `minify:js` gained a `terser js/kiosk-core.js` target; `stamp:kiosk` and `stamp:admin` each gained a distinct `kiosk-core.min.js?v=` regex clause
- `kiosk.html` and `admin.html` each gained a `<script src="js/kiosk-core.min.js?v=...">` tag positioned before their respective consumer bundle; `admin.html` also gained the previously-missing `js/lib/discount-match.js` tag (needed for later discount-parity plans)
- `npm run build` regenerates `js/kiosk-core.min.js` and stamps both pages with live cache-bust versions

## Task Commits

Each task was committed atomically:

1. **Task 1: Create the kiosk-core.js ES5 skeleton with window.KioskCore + dual-mode export** - `c26d7c2` (feat)
2. **Task 2: Add Node-only require guards + build wiring + HTML script tags, prove existing suites stay green (A2 spike)** - `0d93ba6` (feat)

## Files Created/Modified
- `js/kiosk-core.js` - New ES5 IIFE skeleton; `window.KioskCore` = `{ init: kcInit }`; dual-mode export; no migrated logic yet
- `js/kiosk-core.min.js` - Build artifact (terser output), tracked per repo convention (all other `*.min.js` are tracked)
- `js/kiosk.js` - Added Node-only `require('./kiosk-core.js')` guard at top of IIFE
- `js/admin.js` - Added identical Node-only guard at top of IIFE
- `js/admin.min.js`, `js/kiosk.min.js` - Regenerated by `npm run build` (guard code now included)
- `kiosk.html` - Added `<script src="js/kiosk-core.min.js?v=...">` before the `kiosk.min.js` tag (no `defer`, matching existing plain-sync convention)
- `admin.html` - Added `<script src="js/lib/discount-match.js" defer>` and `<script src="js/kiosk-core.min.js?v=..." defer>` before the `admin.min.js` tag
- `package.json` - `minify:js` gained a `kiosk-core.js` terser clause; `stamp:kiosk`/`stamp:admin` each gained a distinct `kiosk-core\.min\.js\?v=` regex replace clause

## Decisions Made
- Kept `kcInit`/`_kcEnv` exactly as specified in PATTERNS.md/RESEARCH.md — copies `mwUrl` and `buildAuthOptions` onto a private module-scope object, no other seams added yet (later plans will extend `KioskCore` as functions are migrated)
- Used a backtick-free comment style in `js/kiosk-core.js` to keep the file unambiguously ES5-clean at a textual grep level (the plan's acceptance criteria explicitly grep for zero template-literal backticks)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking, environment-only] Symlinked `zoho-middleware/node_modules` for worktree test execution**
- **Found during:** Task 2 verification (`cd zoho-middleware && npm test`)
- **Issue:** This git worktree does not have its own `zoho-middleware/node_modules` (dependencies are only installed in the main repo checkout), so middleware Jest suites failed with `Cannot find module 'express'` etc. — a pre-existing environment gap unrelated to any file this plan touches.
- **Fix:** Created `zoho-middleware/node_modules` as a symlink to the main repo's already-installed `zoho-middleware/node_modules` (no new packages installed, no `npm install` run — purely linking existing installed dependencies so the required verification command could execute).
- **Files modified:** None tracked — `zoho-middleware/node_modules` is gitignore-pattern-matched (`node_modules/`) and was never staged; confirmed via `git status --short` showing it as untracked throughout, and it was never included in any `git add`.
- **Verification:** `cd zoho-middleware && npm test` → 77 suites / 1250 tests passed after the symlink was created.
- **Committed in:** N/A (not a tracked file; local worktree convenience only, discarded when the worktree is removed)

**2. [Rule 3 - Blocking] Reverted unrelated cache-bust stamps produced by `npm run build`**
- **Found during:** Task 2, after running `npm run build`
- **Issue:** `npm run build` runs `stamp:index`/`stamp:pages`/`stamp:brewpad` in addition to `stamp:kiosk`/`stamp:admin`, which bumped cache-bust `?v=` query strings on 16 unrelated pages (`about.html`, `index.html`, `products/*.html`, etc.) — none of these are in this plan's `files_modified` scope.
- **Fix:** Ran `git checkout --` on each unrelated file to discard the incidental stamp bumps, keeping only `admin.html`/`kiosk.html` (the two files this plan is scoped to touch) staged.
- **Files modified:** Reverted (not committed): `about.html`, `brewpad.html`, `contact.html`, `custom-labels.html`, `index.html`, `ingredients.html`, `products.html`, `reservation.html`, `products/additives.html`, `products/equipment.html`, `products/ferment-in-store.html`, `products/grains.html`, `products/hops.html`, `products/ingredients-supplies.html`, `products/packaging.html`, `products/yeast.html`
- **Verification:** `git status --short` after revert showed only the 8 plan-scoped files staged/modified.
- **Committed in:** N/A (explicitly reverted, not committed)

---

**Total deviations:** 2 auto-fixed (both Rule 3 - blocking/environment scope-containment, zero code-behavior changes)
**Impact on plan:** No scope creep — both deviations were pure test-infrastructure/build-noise containment, not functional changes. All plan-scoped acceptance criteria met exactly as written.

## Issues Encountered
None beyond the two deviations above.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- The A2/Pitfall-4 Node-require guard mechanism is proven safe on an empty skeleton: both frontend (947 tests) and middleware (1250 tests) Jest suites pass unmodified, lint is clean, and `npm run build` correctly produces + stamps `kiosk-core.min.js` on both pages.
- `js/kiosk-core.js` is ready to receive the ~37 promoted functions + 12 discount functions in subsequent waves of this phase (48-02 through 48-06) — the `init(env)` seam and dual-mode export are the load-bearing mechanics those plans will build on.
- No blockers identified for the next plan in this phase.

---
*Phase: 48-kiosk-pos-de-fork-kiosk-core-js*
*Completed: 2026-07-03*
