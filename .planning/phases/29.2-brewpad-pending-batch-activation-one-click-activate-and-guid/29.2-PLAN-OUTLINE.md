# Phase 29.2 — Plan Outline

**Phase:** 29.2 — BrewPad pending batch activation
**Mode:** standard (frontend-only — all plans modify `js/brewpad.js`, so waves are sequential)

All three plans modify the same file (`js/brewpad.js`), so they cannot run in parallel — each gets its own wave. The test file (`tests/frontend/brewpad-activation.test.js`) is created in Plan 01 and extended in later plans.

| Plan ID | Objective | Wave | Depends On | Requirements |
|---------|-----------|------|------------|--------------|
| 29.2-01 | Foundation + bug fix: create `tests/frontend/brewpad-activation.test.js` (regression test FIRST per CLAUDE.md rule 3), add `todayPacific()` helper at file top-level + export, and fix the status badge pending guard so pending batches route through the Activate confirm flow instead of silently cycling to Primary (brewpad.js:2497 handler). | 1 | — | none (urgent inserted phase) |
| 29.2-02 | Needs Scheduling activation: add Activate + Schedule & Activate buttons to Needs Scheduling rows (brewpad.js:1111–1126), wire one-click Activate delegation handler (`update_batch` with `start_date: todayPacific()`, post-mutation cache bust + `loadDashboard()`), and wire the Schedule & Activate button to look up the batch from `_allBatchesData` (fallback `get_batch` fetch). | 2 | 29.2-01 | none |
| 29.2-03 | Guided Schedule & Activate sheet + detail pane: build `openScheduleActivateSheet`/`closeScheduleActivateSheet`/`buildScheduleActivateSheetHtml` (unique `bp-sa-*` IDs, `_fermSchedules` guard, schedule select+preview, vessel/shelf/bin binding, two-step `update_batch` + `update_batch_schedule` orchestration with `step1Done` partial-failure pattern), and add pending-only Activate + Schedule & Activate buttons to the detail pane footer (brewpad.js:2395) with bindings. | 3 | 29.2-01, 29.2-02 | none |

## OUTLINE COMPLETE
