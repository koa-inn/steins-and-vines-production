---
phase: 29.2-brewpad-pending-batch-activation-one-click-activate-and-guid
plan: "05"
subsystem: api
tags: [apps-script, optimistic-lock, batch-activation, brewpad]

# Dependency graph
requires:
  - phase: 29.2-brewpad-pending-batch-activation-one-click-activate-and-guid
    provides: "BrewPad Needs Scheduling rows with one-click Activate delegation (js/brewpad.js:1130 already reads it.last_updated)"
provides:
  - "adminApi.gs needsScheduling summary emits last_updated per row, restoring the optimistic-lock version field on the NS Activate button"
affects:
  - "29.2 verification — optimistic lock now fires on concurrent Activate attempts (CR-02 closed)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Gap-closure pattern: Apps Script builder must emit all fields the frontend consumer reads — verify emitter and consumer are aligned at review time"

key-files:
  created: []
  modified:
    - apps-script/adminApi.gs

key-decisions:
  - "Human-confirmed deploy: adminApi.gs redeployed as new version of existing web-app deployment (URL unchanged); live data-version verification performed by the human per the deploy checkpoint instructions"
  - "No frontend change: js/brewpad.js:1130 already read it.last_updated correctly; the entire fix was a one-line addition to the Apps Script builder"

patterns-established:
  - "Apps Script manual deploy pattern: paste updated .gs file into editor, Save, Deploy > Manage deployments > edit active > New version > Deploy (URL unchanged)"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-06-12
---

# Phase 29.2 Plan 05: last_updated Emitted in needsScheduling Summary — Optimistic Lock Restored (CR-02)

**One-line addition to adminApi.gs needsScheduling.push() restores the optimistic-lock version field on the BrewPad Needs Scheduling Activate button, closing the CR-02 lost-update gap**

## Performance

- **Duration:** ~5 min (split over two agent sessions with human deploy step in between)
- **Started:** 2026-06-13T01:22:47Z (original session)
- **Completed:** 2026-06-12
- **Tasks:** 2 (1 auto + 1 human-action checkpoint)
- **Files modified:** 1 (apps-script/adminApi.gs)

## Accomplishments

- Added `last_updated: b.last_updated || ''` to the `needsScheduling.push()` object literal in adminApi.gs (line 1741), so the field now flows from the Google Sheet batch row through the dashboard-summary API payload to the BrewPad NS row's `data-version` attribute.
- Apps Script redeployed as a new version of the existing web-app deployment (URL unchanged) — confirmed by the human per the checkpoint instructions.
- CR-02 closed: `expectedVersion` on the one-click Activate call is no longer always empty; the server's existing optimistic-lock check now activates, preventing a concurrent Schedule-&-Activate from being silently overwritten.

## Task Commits

1. **Task 1: Emit last_updated in the needsScheduling summary builder** — `4f5104a` (fix)
2. **Task 2: Deploy updated adminApi.gs** — human-action checkpoint, confirmed by user; no code commit (deploy is outside the repo)

## Files Created/Modified

- `apps-script/adminApi.gs` — Added `last_updated: b.last_updated || ''` to `needsScheduling.push()` at line 1741; no other branches touched

## Decisions Made

- No frontend change required: `js/brewpad.js:1130` already consumed `it.last_updated` and emitted it as `data-version`; the delegation handler already sent it as `expectedVersion`. The entire fix was the missing emitter side in Apps Script.
- Human-confirmed Apps Script deploy: Claude cannot push to Apps Script (no clasp/CLI setup); the human pasted the updated file and redeployed the existing deployment as a new version. Live data-version verification was performed by the human per the checkpoint instructions — not fabricated by automated tooling.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

Apps Script was redeployed manually by the user (blocking human-action checkpoint). The web-app URL is unchanged. No new environment variables or configuration required.

## Next Phase Readiness

- Phase 29.2 all 5 plans complete. CR-01 (detail-pane Activate re-render) closed in plan 04; CR-02 (optimistic lock) closed in this plan.
- The phase is ready for overall verification and the v4.1 production deploy batch (phases 27–29.2).

---
*Phase: 29.2-brewpad-pending-batch-activation-one-click-activate-and-guid*
*Completed: 2026-06-12*
