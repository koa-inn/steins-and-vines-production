---
phase: 29.2-brewpad-pending-batch-activation
plan: 03
type: execute
wave: 3
depends_on:
  - 29.2-01
  - 29.2-02
files_modified:
  - js/brewpad.js
  - tests/frontend/brewpad-activation.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "openScheduleActivateSheet(batch) opens a bottom sheet with a schedule select, a start-date input defaulting to todayPacific(), and vessel/shelf/bin inputs"
    - "If _fermSchedules is empty when the sheet opens, reloadSchedules() runs first and the sheet still populates its schedule options"
    - "Selecting a schedule renders a step-by-step preview of that schedule's steps"
    - "Submitting the guided sheet calls update_batch (status=primary + start_date + optional vessel/shelf/bin) then update_batch_schedule, sequenced with the step1Done partial-failure flag"
    - "On full success the sheet closes, a 'Batch activated with N task(s) scheduled' toast shows, the cache is busted, and loadDashboard() re-renders"
    - "When step1 succeeds but step2 fails, a warning toast ('Batch activated, but the schedule didn't save') shows, the sheet closes, and the dashboard refreshes (batch is NOT left pending)"
    - "The batch detail pane footer renders Activate + Schedule & Activate buttons ONLY when statusKey === 'pending', and the existing bottling-invite + delete buttons are preserved"
    - "Every batch/schedule field interpolated into the sheet HTML passes through escapeHTML"
  artifacts:
    - path: "js/brewpad.js"
      provides: "openScheduleActivateSheet / closeScheduleActivateSheet / buildScheduleActivateSheetHtml + two-step step1Done orchestration + detail pane pending action buttons & bindings"
      contains: "function openScheduleActivateSheet"
    - path: "tests/frontend/brewpad-activation.test.js"
      provides: "Guided-sheet contract tests (step1Done routing reference + bp-sa-* id constants + escapeHTML guard)"
      contains: "describe('schedule & activate"
  key_links:
    - from: "js/brewpad.js .bp-needsched-sa-btn delegation (29.2-02) + detail pane Schedule & Activate button"
      to: "openScheduleActivateSheet(batch)"
      via: "named function call (forward reference resolved here)"
      pattern: "function openScheduleActivateSheet"
    - from: "guided sheet submit handler"
      to: "adminApiPost('update_batch') then adminApiPost('update_batch_schedule')"
      via: "step1Done-flagged sequential promise chain"
      pattern: "step1Done"
    - from: "js/brewpad.js renderBatchDetail footer (bp-detail-actions, line ~2395)"
      to: "#bp-activate-detail-btn / #bp-sa-detail-btn (pending only)"
      via: "statusKey === 'pending' guard"
      pattern: "bp-sa-detail-btn"
---

<objective>
Build the guided Schedule & Activate bottom sheet (the forward-referenced `openScheduleActivateSheet` that plan 29.2-02's `.bp-needsched-sa-btn` branch already calls) and add pending-only activation buttons to the batch detail pane footer.

Three deliverables, building on the Wave 1 foundation (`todayPacific()` helper, status-badge pending guard) and Wave 2 (Needs Scheduling row buttons + the `.bp-needsched-sa-btn` call site that expects this function to exist):

1. Append guided-sheet contract tests to `tests/frontend/brewpad-activation.test.js` FIRST (CLAUDE.md rule 3 spirit) — pure, deterministic assertions that pin the `bp-sa-*` id constants, the step1Done partial-failure routing reference, and the escapeHTML XSS guard. Do not modify the 29.2-01 / 29.2-02 describe blocks.
2. Add `openScheduleActivateSheet(batch)` / `closeScheduleActivateSheet()` / `buildScheduleActivateSheetHtml(batch)` inside the IIFE — a dynamically-created bottom sheet (unique id `bp-sa-sheet`, all inner ids prefixed `bp-sa-`) modeled on `openCreateSheet`/`closeCreateSheet` (js/brewpad.js:3473-3499) and `showConfirmSheet` (js/brewpad.js:1751). It guards an empty `_fermSchedules` via `reloadSchedules()`, renders a schedule select + step preview, binds vessel/shelf/bin inputs, and on submit runs the two-step `update_batch` + `update_batch_schedule` orchestration with the `step1Done` flag (ported verbatim from admin.js:7379-7424 per PATTERNS.md section 5).
3. Add pending-only Activate + Schedule & Activate buttons to the detail pane footer (`bp-detail-actions`, js/brewpad.js:2395) and wire their click bindings — Activate runs the one-click confirm flow; Schedule & Activate calls `openScheduleActivateSheet(b)`.

Purpose: This plan completes the activation UX. Without it, the Wave 2 Schedule & Activate buttons call an undefined function, and there is no way to activate a pending batch from its detail pane. After this plan, staff can activate (one-click) or schedule-and-activate (guided, with a schedule attached and tasks generated) from both the Needs Scheduling rows and the batch detail pane.

Output: New guided-sheet test block appended to `tests/frontend/brewpad-activation.test.js`, source edits to `js/brewpad.js` (three new sheet functions + two-step orchestration + detail pane footer buttons + bindings), regenerated `js/brewpad.min.js` via `npm run build`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-RESEARCH.md
@.planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-PATTERNS.md
@.planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-01-PLAN.md
@.planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Exact identifiers the executor needs. Extracted from RESEARCH.md + PATTERNS.md. Read the read_first anchors for the exact source bodies; do not re-read large source files end-to-end. -->

Delivered by earlier waves (already in the file when this plan runs):
  function todayPacific(offsetDays)  — top-level, YYYY-MM-DD in America/Vancouver. start_date default + payload source.
  Status-badge pending guard (29.2-01) and Needs Scheduling row buttons + .bp-needsched-sa-btn delegation (29.2-02).
  The .bp-needsched-sa-btn branch ALREADY calls openScheduleActivateSheet(batch) — THIS plan defines it.

Forward reference this plan must satisfy (the contract):
  function openScheduleActivateSheet(batch)
    - batch is a full batch record (from _allBatchesData or a get_batch fetch); batch.last_updated is the version.
    - Must be a named `function` declaration inside the IIFE so it hoists above the 29.2-02 call site.

New functions to add inside the IIFE (RESEARCH.md lines 95-100, PATTERNS.md section 5):
  function openScheduleActivateSheet(batch)        — guard _fermSchedules, build + show sheet, bind inputs + submit
  function closeScheduleActivateSheet()            — hide/remove the bp-sa-sheet element (mirror closeCreateSheet)
  function buildScheduleActivateSheetHtml(batch)   — returns the inner HTML string for the sheet form

Sheet element + inner ids (Pitfall 5 — MUST be unique, do not reuse bp-create-sheet ids):
  bp-sa-sheet              (the sheet container — created dynamically if absent, reused if present)
  bp-sa-sheet-inner       (the inner content wrapper, populated by buildScheduleActivateSheetHtml)
  bp-sa-schedule-select   (schedule <select>)
  bp-sa-schedule-preview  (step-by-step preview container)
  bp-sa-start-date        (date input, default value = todayPacific())
  bp-sa-vessel-search     (vessel search text input)
  bp-sa-vessel-dropdown   (vessel results dropdown)
  bp-sa-vessel            (hidden vessel_id input)
  bp-sa-shelf             (shelf input)
  bp-sa-bin               (bin input)
  bp-sa-submit            (submit button, label "Schedule & Activate")

Sheet open/close model — js/brewpad.js:3473-3499 (openCreateSheet / closeCreateSheet):
  display toggle + classList add/remove 'bp-create-sheet--open' on a setTimeout, backdrop-click-to-close handler,
  delayed focus of the first input. Mirror this lifecycle with a 'bp-sa-sheet--open' class (or reuse the
  existing bp-create-sheet--open animation class on the new element — match whatever CSS exists; PATTERNS.md
  section 5 lines 329-356 has the exact lifecycle).
Dynamic-create-if-absent model — showConfirmSheet js/brewpad.js:1751 (getElementById('bp-...') || createElement, append to body).

_fermSchedules guard (Pitfall 4): if _fermSchedules.length === 0, call reloadSchedules() then open the sheet in its
  callback (or show the sheet and populate the select once reloadSchedules resolves). reloadSchedules() is existing.

Schedule select + preview (PATTERNS.md lines 358-379, adapted from admin.js:7304-7323):
  On 'change', find the schedule in _fermSchedules by schedule_id, parse steps (s.steps_parsed || JSON.parse(s.steps)),
  render each as 'Day N: title [Transfer/Packaging badges] — description', every text field via escapeHTML.

Two-step submit orchestration (PATTERNS.md lines 388-426 — copy admin.js:7379-7424 verbatim, adapt identifiers only):
  var step1Done = false;
  adminApiPost('update_batch', { batch_id, expectedVersion: batch.last_updated, updates: batchUpdates })
    .then -> step1Done = true; newVersion = step1Result.newVersion || batch.last_updated;
             return adminApiPost('update_batch_schedule', { batch_id, expectedVersion: newVersion, schedule_snapshot: schedSteps })
               .then -> taskCount = step2Result.tasks_created || 0; success toast; closeScheduleActivateSheet(); cache-bust; loadDashboard();
    .catch -> if (!step1Done && version_conflict) -> 'Version conflict — refresh and try again'
              else if (step1Done) -> warning toast 'Batch activated, but the schedule didn't save...'; closeScheduleActivateSheet(); cache-bust; loadDashboard();
              else -> 'Failed: ' + msg;
              re-enable submitBtn + restore label.
  batchUpdates = { status: 'primary', start_date: <bp-sa-start-date value or todayPacific()>, plus vessel_id/shelf_id/bin_id when set }.
  schedSteps = the selected schedule's parsed steps array.

Detail pane footer — js/brewpad.js:2395-2401 (the bp-detail-actions div). statusKey is the lowercased status in scope.
  PATTERNS.md section 3 lines 202-214: render #bp-activate-detail-btn + #bp-sa-detail-btn ONLY when statusKey === 'pending',
  then the existing bottling-invite (#bp-bottling-invite-btn, gated on b.customer_email) and delete (#bp-delete-batch-btn) buttons.
Detail pane bindings — wire after detailPane.innerHTML = html, same section as bindVesselSearch (~line 2438).
  #bp-activate-detail-btn -> showConfirmSheet(...) -> adminApiPost('update_batch', { ..., updates:{ status:'primary', start_date: todayPacific() }, expectedVersion: b.last_updated })
    -> on success: showToast + re-render detail (renderBatchDetail) + cache-bust + renderBatchList (same post-mutation pattern as the status-badge handler at js/brewpad.js:2506-2526).
  #bp-sa-detail-btn -> openScheduleActivateSheet(b).

Reusable functions already in brewpad.js (do NOT rebuild):
  escapeHTML(str)                                                js/brewpad.js:6
  showConfirmSheet(message, okLabel, okCls, onOk)                js/brewpad.js:1751
  adminApiPost(action, payload) -> Promise<data>                js/brewpad.js:694
  adminApiGet(action, params) -> Promise<r>                     js/brewpad.js:674
  bindVesselSearch(input, dropdown, hidden, currentId)          js/brewpad.js:3304
  bindShelfInput(el) / bindBinInput(el)                         js/brewpad.js:3350 / 3356
  reloadSchedules()                                             existing
  showToast(msg, level) / loadDashboard() / renderBatchList()   existing
  callSyncZoho(batch_id, zoho_so_number, status)                existing
  todayPacific(offsetDays)                                      29.2-01 (top-level)

Module-scope state (read; only reassign the documented cache-bust flags):
  _fermSchedules    — schedule templates (populate-guard source)
  _vesselsData      — vessel list (bindVesselSearch reads it)
  _batchesData / _allBatchesData / _batchesLoaded / _eagerLoadTime / _dashLoadTime  — cache-bust flags

Post-mutation cache-bust + refresh (after full guided success, and after a detail-pane one-click Activate):
  _batchesLoaded = false; _allBatchesData = []; _eagerLoadTime = 0; _dashLoadTime = 0; loadDashboard();

update_batch_schedule response shape (RESEARCH.md lines 351-359): { ok:true, tasks_updated, tasks_created, tasks_removed }
  — step2Result.tasks_created is the toast count. On lock conflict: { ok:false, error:'version_conflict', message:'Batch was modified...' }.

Test boilerplate: tests/frontend/brewpad-activation.test.js already exists (29.2-01). Append a new describe block;
  reuse the existing `var bp = require('../../js/brewpad')`. Do NOT modify earlier describe blocks (CLAUDE.md rule 10).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append guided-sheet contract tests FIRST</name>
  <files>tests/frontend/brewpad-activation.test.js</files>
  <read_first>
    - tests/frontend/brewpad-activation.test.js (the full existing file from 29.2-01 / 29.2-02 — append below the existing describe blocks; do not edit them)
    - .planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-PATTERNS.md (section 5, lines 317-427 — bp-sa-* ids + step1Done orchestration the tests pin; lines 467-501 — test boilerplate style)
    - CLAUDE.md (rule 3: tests first; rule 10: do not modify existing tests)
  </read_first>
  <action>
    Append a new describe block `describe('schedule & activate guided sheet', ...)` to the END of tests/frontend/brewpad-activation.test.js. Do NOT touch the existing `todayPacific`, `pending status detection`, or `needs scheduling activation` blocks from 29.2-01 / 29.2-02.

    The sheet functions are DOM/event-driven and are exercised by the manual sanity check, not unit tests. Keep the new tests to pure, deterministic assertions that pin the contract this plan establishes:
    - Assert the stable `bp-sa-*` id constants this plan must emit are a fixed set: 'bp-sa-sheet', 'bp-sa-schedule-select', 'bp-sa-schedule-preview', 'bp-sa-start-date', 'bp-sa-vessel-search', 'bp-sa-vessel', 'bp-sa-shelf', 'bp-sa-bin', 'bp-sa-submit' (a plain array `.indexOf(...) !== -1` style check so a later rename of any id fails this test). This also documents Pitfall 5 (no collision with bp-create-sheet ids).
    - Assert the step1Done partial-failure routing reference as a pure decision table: given (step1Done, hasVersionConflict) inputs, the chosen outcome string is correct — `(!step1Done && versionConflict) -> 'version_conflict'`, `(step1Done) -> 'partial'` (schedule didn't save but batch activated), `(!step1Done && !versionConflict) -> 'failed'`. Write this as a small local helper inside the test (e.g. `function routeStep(step1Done, versionConflict){ ... }`) and assert all three branches. This pins the truthful-UI-state contract (Threat T-29.2-... Repudiation) without requiring DOM.
    - Assert `bp.escapeHTML('<x>')` does not contain a raw `<` (XSS guard the sheet HTML relies on for batch/schedule fields). `bp.escapeHTML` is already exported.
    - Assert `bp.todayPacific()` matches `/^\d{4}-\d{2}-\d{2}$/` (the start-date default the sheet uses).

    Use ES5 style (`var`, `function`, no arrow functions) to match the existing file. Reuse the existing `var bp = require('../../js/brewpad')` — do not re-require.
  </action>
  <verify>
    <automated>grep -q "describe('schedule & activate" tests/frontend/brewpad-activation.test.js && npx jest tests/frontend/brewpad-activation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/frontend/brewpad-activation.test.js` contains `describe('schedule & activate`
    - The new block asserts the `bp-sa-*` id constants including `bp-sa-sheet`, `bp-sa-schedule-select`, `bp-sa-submit`
    - The new block contains a pure step1Done routing helper asserting all three branches (`version_conflict`, `partial`, `failed`)
    - The new block asserts `bp.escapeHTML('<x>')` contains no raw `<` and `bp.todayPacific()` matches `/^\d{4}-\d{2}-\d{2}$/`
    - The existing 29.2-01 / 29.2-02 describe blocks are unchanged (`git diff` shows only additions below them)
    - `npx jest tests/frontend/brewpad-activation.test.js` exits 0
  </acceptance_criteria>
  <done>Guided-sheet markup + step1Done routing contract tests appended without modifying earlier blocks; activation test file green.</done>
</task>

<task type="auto">
  <name>Task 2: Build the guided Schedule & Activate bottom sheet (open/close/build + two-step orchestration)</name>
  <files>js/brewpad.js</files>
  <read_first>
    - js/brewpad.js lines 3473-3499 (openCreateSheet / closeCreateSheet — the sheet open/close lifecycle to mirror)
    - js/brewpad.js lines 1751-1786 (showConfirmSheet — dynamic-create-if-absent element pattern)
    - js/brewpad.js lines 3304-3364 (bindVesselSearch / bindShelfInput / bindBinInput — call signatures)
    - .planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-PATTERNS.md (section 5, lines 317-427 — sheet ids, schedule preview, two-step step1Done orchestration to copy verbatim with identifier swaps)
    - .planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-RESEARCH.md (Pattern 2 lines 139-190; Pitfall 4 lines 330-334 _fermSchedules guard; Pitfall 5 lines 336-340 id collision; lines 351-372 response shapes)
  </read_first>
  <action>
    Add three named `function` declarations inside the brewpad.js IIFE (place them near the other sheet helpers, e.g. after `closeCreateSheet` at ~line 3499). Use named `function` declarations (NOT function expressions) so they hoist above the 29.2-02 `.bp-needsched-sa-btn` call site.

    `buildScheduleActivateSheetHtml(batch)` — returns the inner HTML string for the sheet form, mirroring admin.js:7257-7300 (`_buildScheduleActivateModal`) but with the `bp-sa-*` ids from the interfaces block. Include: a heading naming the batch (escapeHTML(batch.batch_id || '') and escapeHTML(batch.product_name || '')), a schedule `<select id="bp-sa-schedule-select">` whose options come from `_fermSchedules` (option value = schedule_id, label = escapeHTML(schedule name)), a `<div id="bp-sa-schedule-preview">`, a `<input type="date" id="bp-sa-start-date">` (its `value` set to `todayPacific()`), a vessel search input + dropdown + hidden input (`bp-sa-vessel-search` / `bp-sa-vessel-dropdown` / `bp-sa-vessel`), shelf + bin inputs (`bp-sa-shelf` / `bp-sa-bin`), and a submit button `<button id="bp-sa-submit">Schedule &amp; Activate</button>`. Every interpolated batch/schedule field MUST pass through `escapeHTML(... || '')` (Threat: XSS).

    `openScheduleActivateSheet(batch)`:
    1. _fermSchedules guard (Pitfall 4): if `_fermSchedules.length === 0`, call `reloadSchedules()` and continue building the sheet in its resolution callback (so the select populates); otherwise build immediately.
    2. Get or create the sheet element: `var sheet = document.getElementById('bp-sa-sheet');` — if absent, `document.createElement('div')`, set `id = 'bp-sa-sheet'`, give it the same sheet container class the create sheet uses, append to `document.body` (showConfirmSheet pattern). Set its inner wrapper (`bp-sa-sheet-inner`) innerHTML from `buildScheduleActivateSheetHtml(batch)`.
    3. Show with the same lifecycle as `openCreateSheet` (display toggle + add the open class on a setTimeout, backdrop-click-to-close calling `closeScheduleActivateSheet`, delayed focus of the first input).
    4. After rendering: bind the schedule select `change` handler to render the step preview into `bp-sa-schedule-preview` (PATTERNS.md lines 358-379 — every text field via escapeHTML); call `bindVesselSearch(document.getElementById('bp-sa-vessel-search'), document.getElementById('bp-sa-vessel-dropdown'), document.getElementById('bp-sa-vessel'), '')`, `bindShelfInput(document.getElementById('bp-sa-shelf'))`, `bindBinInput(document.getElementById('bp-sa-bin'))`.
    5. Bind the submit button (`bp-sa-submit`) to the two-step orchestration below. Capture `batch` in the closure for `batch.last_updated`.

    Submit handler — copy the step1Done orchestration from PATTERNS.md lines 388-426 (admin.js:7379-7424) verbatim, swapping identifiers only:
    - `fermSchedulesData` → `_fermSchedules`; `closeModal()` → `closeScheduleActivateSheet()`; the admin refresh calls → the brewpad cache-bust block (`_batchesLoaded = false; _allBatchesData = []; _eagerLoadTime = 0; _dashLoadTime = 0; loadDashboard();`).
    - Read `submitBtn` = `bp-sa-submit`; disable it + set its text to a busy label before step 1.
    - `batchUpdates` = `{ status: 'primary', start_date: (bp-sa-start-date value || todayPacific()) }`, plus `vessel_id` / `shelf_id` / `bin_id` keys ONLY when those inputs have non-empty values.
    - `schedSteps` = the selected schedule's parsed steps array (`sched.steps_parsed || JSON.parse(sched.steps)`); if no schedule is selected, surface a toast and bail without calling the API (a schedule is required for the guided flow).
    - Step 1: `adminApiPost('update_batch', { batch_id: batch.batch_id, expectedVersion: batch.last_updated, updates: batchUpdates })`.
    - On `.then(step1Result)`: `step1Done = true`; `var newVersion = step1Result.newVersion || batch.last_updated`; `return adminApiPost('update_batch_schedule', { batch_id: batch.batch_id, expectedVersion: newVersion, schedule_snapshot: schedSteps })`; on its `.then(step2Result)`: `var taskCount = step2Result.tasks_created || 0`; `showToast('Batch activated with ' + taskCount + ' task' + (taskCount !== 1 ? 's' : '') + ' scheduled', 'success')`; `closeScheduleActivateSheet()`; cache-bust block; `loadDashboard()`.
    - `.catch(err)`: `var msg = err.message || 'Unknown error';` — if `!step1Done && (msg.indexOf('version_conflict') !== -1 || msg.indexOf('Batch was modified') !== -1)` show 'Version conflict — refresh and try again'; else if `step1Done` show the warning toast 'Batch activated, but the schedule didn\'t save — assign it from the batch detail', `closeScheduleActivateSheet()`, cache-bust, `loadDashboard()`; else show 'Failed: ' + msg. Always re-enable `submitBtn` and restore its 'Schedule & Activate' label.

    `closeScheduleActivateSheet()` — mirror `closeCreateSheet` (js/brewpad.js:3494-3499): remove the open class, hide on a setTimeout. Operate on the `bp-sa-sheet` element; no-op if absent.

    Do NOT wire the detail-pane buttons in this task — that is Task 3. ES5 style throughout: `var`, `function`, no arrow functions, no template literals. Do not run `npm run build` yet (Task 3 runs it after the final edit).
  </action>
  <verify>
    <automated>grep -q "function openScheduleActivateSheet" js/brewpad.js && grep -q "function closeScheduleActivateSheet" js/brewpad.js && grep -q "function buildScheduleActivateSheetHtml" js/brewpad.js && grep -q "bp-sa-sheet" js/brewpad.js && grep -q "step1Done" js/brewpad.js && grep -q "update_batch_schedule" js/brewpad.js && npx jest tests/frontend/brewpad-activation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function openScheduleActivateSheet" js/brewpad.js` returns 1, and `closeScheduleActivateSheet` + `buildScheduleActivateSheetHtml` are each defined once
    - The sheet uses the unique id `bp-sa-sheet` and the `bp-sa-*` inner ids (`bp-sa-schedule-select`, `bp-sa-schedule-preview`, `bp-sa-start-date`, `bp-sa-vessel-search`, `bp-sa-vessel`, `bp-sa-shelf`, `bp-sa-bin`, `bp-sa-submit`) — none reuse `bp-create-sheet`
    - `openScheduleActivateSheet` guards `_fermSchedules.length === 0` by calling `reloadSchedules()` before populating the select
    - The start-date input defaults to `todayPacific()` and the submit `batchUpdates` payload contains `status: 'primary'` + a `start_date`
    - The submit handler contains a `step1Done` flag, calls `adminApiPost('update_batch', ...)` then `adminApiPost('update_batch_schedule', ...)`, and the `.catch` distinguishes `!step1Done` version-conflict, `step1Done` partial-success (warning toast), and generic failure
    - On full success the handler calls `closeScheduleActivateSheet()`, runs the cache-bust block, and calls `loadDashboard()`
    - `bindVesselSearch`, `bindShelfInput`, `bindBinInput` are called with the `bp-sa-*` elements; every interpolated batch/schedule field passes through `escapeHTML`
    - `npx jest tests/frontend/brewpad-activation.test.js` exits 0
  </acceptance_criteria>
  <done>Guided sheet opens with schedule select + preview + vessel/shelf/bin, defaults start date to todayPacific, and submits via the two-step step1Done orchestration with truthful partial-failure handling; the 29.2-02 forward reference now resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Add pending-only detail pane Activate + Schedule & Activate buttons and bindings</name>
  <files>js/brewpad.js</files>
  <read_first>
    - js/brewpad.js lines 2395-2401 (the bp-detail-actions footer div — where the buttons are emitted; `statusKey` is the lowercased status in scope)
    - js/brewpad.js lines ~2438 (the detail-pane binding section after `detailPane.innerHTML = html`, where bindVesselSearch is wired — add the new button bindings here)
    - js/brewpad.js lines 2506-2526 (the status-badge post-mutation cache-bust pattern to reuse for the detail-pane one-click Activate success path)
    - .planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-PATTERNS.md (section 3, lines 188-247 — exact footer markup + binding pattern)
  </read_first>
  <action>
    Two edits, both in the batch detail pane render path.

    Edit A — footer markup (js/brewpad.js:2395-2401, the `bp-detail-actions` div), per PATTERNS.md lines 202-214:
    - When `statusKey === 'pending'`, prepend two buttons inside `bp-detail-actions`: `<button type="button" class="btn bp-btn-sm" id="bp-activate-detail-btn">Activate</button>` and `<button type="button" class="btn-secondary bp-btn-sm" id="bp-sa-detail-btn">Schedule &amp; Activate</button>`.
    - Leave the existing buttons unchanged and present: the bottling-invite button (`#bp-bottling-invite-btn`, still gated on `b.customer_email`) and the delete button (`#bp-delete-batch-btn`). Do not alter their classes, ids, or guards.

    Edit B — bindings (the detail-pane binding section after `detailPane.innerHTML = html`, ~line 2438), per PATTERNS.md lines 216-247:
    - `var activateDetailBtn = document.getElementById('bp-activate-detail-btn');` — if present, on click call `showConfirmSheet('Activate ' + b.batch_id + ' now? No schedule will be attached and the start date is set to today. Use "Schedule & Activate" if you need a schedule.', 'Activate', '', onOk)`. In onOk: disable the button, `adminApiPost('update_batch', { batch_id: b.batch_id, updates: { status: 'primary', start_date: todayPacific() }, expectedVersion: b.last_updated })`. On `.then`: set `b.status = 'primary'`, `showToast('Batch activated', 'success')`, `callSyncZoho(b.batch_id, b.zoho_so_number, 'active')`, then run the SAME post-mutation pattern as the status-badge handler at js/brewpad.js:2506-2526 (loop `_batchesData` + `_allBatchesData` setting that batch's status to 'primary', `_batchesLoaded = false; _dashLoadTime = 0;`, remove `sessionStorage` key `'sv-bp-batch-' + b.batch_id`), then re-render the detail (call the detail render function the file already uses for this — `renderBatchDetail` — and `renderBatchList()`). On `.catch`: `showToast('Failed: ' + err.message, 'error')` and re-enable the button.
    - `var saDetailBtn = document.getElementById('bp-sa-detail-btn');` — if present, on click call `openScheduleActivateSheet(b)` (defined in Task 2).

    Only render and bind these two buttons for pending batches (`statusKey === 'pending'`); non-pending detail panes must be unchanged. ES5 style throughout.

    Run `npm run build` after this edit to regenerate js/brewpad.min.js (CLAUDE.md rule 9). Then run `npm test` and `npm run lint` (CLAUDE.md rules 1-2) before finishing.
  </action>
  <verify>
    <automated>grep -q "bp-activate-detail-btn" js/brewpad.js && grep -q "bp-sa-detail-btn" js/brewpad.js && grep -q "openScheduleActivateSheet(b)" js/brewpad.js && npx jest tests/frontend/brewpad-activation.test.js && npx jest tests/frontend/brewpad-pending.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `js/brewpad.js` detail footer emits `#bp-activate-detail-btn` and `#bp-sa-detail-btn` guarded by `statusKey === 'pending'`
    - The existing `#bp-bottling-invite-btn` (gated on `b.customer_email`) and `#bp-delete-batch-btn` buttons remain present and unchanged
    - `#bp-activate-detail-btn` click runs `showConfirmSheet` then `adminApiPost('update_batch', ...)` with `updates: { status: 'primary', start_date: todayPacific() }` and `expectedVersion: b.last_updated`, followed by the cache-bust + re-render pattern
    - `#bp-sa-detail-btn` click calls `openScheduleActivateSheet(b)`
    - `npx jest tests/frontend/brewpad-activation.test.js` and `npx jest tests/frontend/brewpad-pending.test.js` both exit 0
    - `npm test` full frontend suite green and `npm run lint` clean
    - `js/brewpad.min.js` regenerated (`git diff --name-only` includes it after `npm run build`)
  </acceptance_criteria>
  <done>Pending batch detail panes show Activate + Schedule & Activate; Activate runs the one-click flow with start_date stamping + cache-bust + re-render; Schedule & Activate opens the guided sheet; existing footer buttons untouched; build artifact regenerated; full suite + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrewPad browser → Apps Script (`adminApiPost` / `adminApiGet`) | `update_batch` + `update_batch_schedule` mutations cross here; auth via existing `accessToken` on every call (unchanged) |
| Batch + schedule data → rendered sheet/detail HTML | Untrusted fields (`batch_id`, `product_name`, schedule names/step titles/descriptions) interpolated into the sheet form, the schedule preview, and the detail footer |
| Sheet form inputs → activation payload | `bp-sa-start-date`, vessel/shelf/bin inputs, and the selected schedule's steps read off the DOM and sent in `update_batch` / `update_batch_schedule` |
| Two-step orchestration → batch lifecycle state | A step-2 failure after a successful step-1 can leave the batch primary with no schedule — the UI must report this truthfully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.2-09 | Tampering (XSS) | `batch_id`, `product_name`, schedule name, schedule step `title`/`description` interpolated into the `bp-sa-sheet` form HTML and the schedule preview, plus `batch_id` in the detail footer/confirm message | mitigate | Every interpolated batch/schedule field passes through `escapeHTML(... || '')` (Task 2 + Task 3 acceptance criteria); button labels and ids are static literals; the confirm-sheet message is set as textContent by `showConfirmSheet`, not innerHTML |
| T-29.2-10 | Repudiation | Two-step partial failure (`update_batch` succeeds, `update_batch_schedule` fails) leaving the batch primary without a schedule while the UI implies full success | mitigate | The `step1Done` flag drives a truthful warning toast ('Batch activated, but the schedule didn\'t save — assign it from the batch detail') and still refreshes the dashboard so the now-primary batch is visible; the step1Done routing is unit-pinned in Task 1 |
| T-29.2-11 | Tampering | Stale `expectedVersion` on the guided `update_batch` or the chained `update_batch_schedule` overwrites a concurrent update | mitigate | Step 1 passes `expectedVersion: batch.last_updated`; step 2 passes `expectedVersion: step1Result.newVersion || batch.last_updated`; the Apps Script optimistic lock rejects with `version_conflict`, surfaced as 'Version conflict — refresh and try again' (no silent overwrite) |
| T-29.2-12 | Tampering | `start_date` / `vessel_id` / `shelf_id` / `bin_id` injected via the `update_batch` payload | accept | Client only sends `todayPacific()` (or the date-input value) and vessel ids drawn from `_vesselsData` via `bindVesselSearch`; these fields are in the Apps Script `allowedFields` allowlist and pass server-side `sanitizeInput` (adminApi.gs:2164) — unchanged |
| T-29.2-13 | Elevation of Privilege | Guided activation / `get_batch` lookup without auth | accept | Every `adminApiPost` / `adminApiGet` carries the existing `accessToken`; Apps Script validates server-side — unchanged by this plan |
| T-29.2-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH.md Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx jest tests/frontend/brewpad-activation.test.js` exits 0 (29.2-01 / 29.2-02 blocks + new guided-sheet block all green)
- `npx jest tests/frontend/brewpad-pending.test.js` exits 0 (no regression)
- `npm test` full frontend suite green (508+ tests stay passing — CLAUDE.md rule 1)
- `npm run lint` clean (CLAUDE.md rule 2)
- `npm run build` run after the source edits; `js/brewpad.min.js` regenerated (rule 9)
- Manual sanity (non-blocking): from a Needs Scheduling row OR a pending batch's detail pane, clicking "Schedule & Activate" opens the `bp-sa-sheet` bottom sheet with schedule options; selecting a schedule shows the step preview; the start date defaults to today (Pacific); submitting activates the batch with the schedule's tasks generated and shows "Batch activated with N task(s) scheduled". From a pending batch's detail pane, "Activate" opens an "Activate ... now?" confirm and, on confirm, flips the batch to Primary. Non-pending detail panes show no activation buttons.
</verification>

<success_criteria>
- Guided-sheet contract tests appended before the source edits (CLAUDE.md rule 3 spirit; earlier blocks untouched)
- `openScheduleActivateSheet` / `closeScheduleActivateSheet` / `buildScheduleActivateSheetHtml` defined inside the IIFE with unique `bp-sa-*` ids, the `_fermSchedules` populate guard, schedule select + preview, and vessel/shelf/bin binding
- Guided submit runs `update_batch` then `update_batch_schedule` via the `step1Done` flag, with truthful partial-failure handling (warning toast + refresh, never left silently pending)
- Detail pane renders Activate + Schedule & Activate ONLY for pending batches; Activate runs the one-click flow, Schedule & Activate calls `openScheduleActivateSheet(b)`; existing bottling-invite + delete buttons preserved
- The 29.2-02 forward reference to `openScheduleActivateSheet` now resolves; full frontend test suite + lint pass; `js/brewpad.min.js` regenerated
</success_criteria>

<output>
Create `.planning/phases/29.2-brewpad-pending-batch-activation-one-click-activate-and-guid/29.2-03-SUMMARY.md` when done.
</output>
