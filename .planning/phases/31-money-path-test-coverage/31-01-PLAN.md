---
phase: 31-money-path-test-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/server.js
  - zoho-middleware/package.json
  - zoho-middleware/jest.config.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "The Express app can be imported by a test file without binding a port (D-01, D-02)"
    - "Running the middleware directly (node server.js) still starts the HTTP server normally (D-02)"
    - "supertest is a declared, installed dev dependency of the middleware (D-03)"
    - "Coverage collection includes routes/**/*.js so no route file is silently excluded (D-05, D-08)"
  artifacts:
    - path: "zoho-middleware/server.js"
      provides: "Importable Express app + guarded listen"
      contains: "module.exports = app"
    - path: "zoho-middleware/package.json"
      provides: "supertest dev dependency"
      contains: "supertest"
    - path: "zoho-middleware/jest.config.js"
      provides: "Honest coverage glob including routes"
      contains: "routes/**/*.js"
  key_links:
    - from: "zoho-middleware/server.js"
      to: "app.listen"
      via: "require.main === module guard"
      pattern: "require\\.main === module"
    - from: "zoho-middleware/jest.config.js"
      to: "routes coverage"
      via: "collectCoverageFrom glob"
      pattern: "routes/\\*\\*/\\*\\.js"
---

<objective>
Make the Express app importable for supertest, install supertest, and expand coverage collection to include route files — the foundation that the Wave 2 route-level test plans depend on.

Purpose: Plans 02 and 03 import the real wired `app` via supertest (per D-01/D-02). That import is impossible until `server.js` exports `app` and stops binding a port on require. The honest-coverage glob change (D-05/TEST-03) lands here too so route coverage is reported the moment route tests exist.
Output: Refactored `server.js` (exports `app`, listen guarded), `supertest` in devDependencies and installed, `jest.config.js` collecting `routes/**/*.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-money-path-test-coverage/31-CONTEXT.md
@.planning/phases/31-money-path-test-coverage/31-PATTERNS.md

<interfaces>
<!-- Verified from the live codebase 2026-06-16. Executor: use these directly. -->

server.js (zoho-middleware/) — confirmed structure:
- line 33: `var app = express();`  ← KEEP EXACTLY AS-IS
- lines 41-413: middleware + route mounts (helmet, json body parser, cors, API-key guard, rate limiters, `app.use('/', require('./routes/...'))`)  ← UNTOUCHED
- listen chain (~line 428): `helcimLib.init();` then `cache.init().then(...).then(checkRedis).then(zohoAuth.init).then(function(){ var server = app.listen(PORT, function(){ /* startup logging + cron.schedule + cache pre-warm */ }); });`
- NO existing `module.exports`, NO existing `require.main` guard.
- API-key guard exemptions (lines 253-264): `/checkout` and any path starting `/webhooks/` skip the x-api-key check. GET also exempt.

jest.config.js (zoho-middleware/) — confirmed current state (no `!`-prefix exclusions exist):
```
collectCoverageFrom: ['lib/**/*.js']
coverageThreshold: { global: { lines: 35 }, './lib/validate.js': { lines: 98 }, './lib/logger.js': { lines: 98 } }
```

package.json (zoho-middleware/) — confirmed current devDependencies:
```
{ "eslint": "^9.39.4", "jest": "^29.7.0" }
```
supertest is NOT currently on disk in node_modules — a real `npm install` is required (the PATTERNS.md note claiming transitive presence is incorrect; verify and install for real).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor server.js to export app and guard listen (D-02)</name>
  <files>zoho-middleware/server.js</files>
  <read_first>
    - zoho-middleware/server.js (read the full file — especially line 33 `var app = express()` and the listen chain near line 428)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§"server.js (export refactor, D-02)" — exact before/after)
    - .planning/phases/31-money-path-test-coverage/31-CONTEXT.md (D-02)
  </read_first>
  <action>
    Wrap the entire startup chain (the `helcimLib.init();` line through the end of the `.then(...)` that calls `app.listen(PORT, ...)`, including the cron.schedule and cache pre-warm callbacks inside the listen callback) in an `if (require.main === module) { ... }` block so the server only binds a port when run directly via `node server.js`. After that block, add `module.exports = app;` as the last statement of the file. Do NOT touch `var app = express()` at line 33, the helmet/json/cors/API-key-guard/rate-limiter registrations, or any `app.use('/', require('./routes/...'))` mounts (lines 34-413). Use `require.main === module` (not a new env var) per D-02. The PORT constant and all startup logging stay inside the guarded block unchanged.
  </action>
  <verify>
    <automated>cd zoho-middleware && grep -q "require.main === module" server.js && grep -q "module.exports = app" server.js && node -e "process.env.NODE_ENV='test'; var app=require('./server'); if (typeof app !== 'function' && typeof app.use !== 'function') { process.exit(1); } console.log('app imported, no port bound');"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "require.main === module" zoho-middleware/server.js` returns >= 1
    - `grep -c "module.exports = app" zoho-middleware/server.js` returns 1
    - `node -e "require('./server')"` from within zoho-middleware exits 0 and does NOT print the startup/listen banner and does NOT hang (no port bound)
    - `var app = express();` still present unchanged at the original location (line ~33)
  </acceptance_criteria>
  <done>Importing server.js returns the Express app without binding a port; running node server.js directly still starts the listener.</done>
</task>

<task type="auto">
  <name>Task 2: Add supertest dev dependency and install it (D-03)</name>
  <files>zoho-middleware/package.json</files>
  <read_first>
    - zoho-middleware/package.json (current devDependencies block — confirmed `{ eslint, jest }` only)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§"package.json (add supertest, D-03)")
  </read_first>
  <action>
    Add `"supertest": "^7.0.0"` to the `devDependencies` object in zoho-middleware/package.json (keep eslint and jest). Then run `npm install` inside zoho-middleware to actually install it — supertest is NOT currently on disk, so a real install is required (the PATTERNS.md transitive-presence claim is wrong; confirm by checking node_modules/supertest exists after install). Note: package-lock.json is gitignored at repo root per [30-06] — only package.json is committed.
  </action>
  <verify>
    <automated>cd zoho-middleware && node -e "var d=require('./package.json').devDependencies; if(!d.supertest){process.exit(1)}; console.log('declared '+d.supertest)" && node -e "console.log('installed '+require('supertest/package.json').version)"</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/package.json` devDependencies contains a `supertest` key
    - `cd zoho-middleware && node -e "require('supertest')"` exits 0 (module resolves from node_modules)
    - Installed supertest major version is 7
  </acceptance_criteria>
  <done>supertest is declared in devDependencies and require('supertest') resolves from zoho-middleware/node_modules.</done>
</task>

<task type="auto">
  <name>Task 3: Expand jest coverage to include routes/** (D-05, D-08, TEST-03)</name>
  <files>zoho-middleware/jest.config.js</files>
  <read_first>
    - zoho-middleware/jest.config.js (full current config — confirmed: `collectCoverageFrom: ['lib/**/*.js']`, no `!`-prefix exclusions)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§"jest.config.js (coverage config changes, D-05..D-08)")
    - .planning/phases/31-money-path-test-coverage/31-CONTEXT.md (D-05, D-08)
  </read_first>
  <action>
    Add `'routes/**/*.js'` to the `collectCoverageFrom` array (keep the existing `'lib/**/*.js'`). Per D-08, confirm there are no stale `!`-prefix exclusions to remove — there are none in the live config, so add a one-line code comment noting that D-08 was verified (no stale exclusions found) so the reviewer knows it was checked, not skipped. Leave `coverageThreshold` UNCHANGED in this plan — the global number stays at its current value and per-file money-path thresholds are added in Plan 04 (Wave 3) after the route tests from Plans 02/03 exist and an honest coverage measurement can be taken. Do NOT raise or lower the existing global threshold here.
  </action>
  <verify>
    <automated>cd zoho-middleware && node -e "var c=require('./jest.config.js'); var hasRoutes=c.collectCoverageFrom.indexOf('routes/**/*.js')>=0; var hasLib=c.collectCoverageFrom.indexOf('lib/**/*.js')>=0; var noBang=c.collectCoverageFrom.every(function(g){return g.charAt(0)!=='!'}); if(!(hasRoutes&&hasLib&&noBang)){process.exit(1)}; console.log('coverage globs OK, no exclusions')"</automated>
  </verify>
  <acceptance_criteria>
    - `jest.config.js` `collectCoverageFrom` includes both `'lib/**/*.js'` and `'routes/**/*.js'`
    - No entry in `collectCoverageFrom` begins with `!` (D-08: honest, no exclusions)
    - `coverageThreshold` global value is unchanged from its pre-plan value (threshold tightening deferred to Plan 04)
    - `cd zoho-middleware && npm test` exits 0 (existing 323-test suite still passes; the new glob does not break the run even though route coverage may be low)
  </acceptance_criteria>
  <done>Coverage collection includes routes/**/*.js with no stale exclusions; existing suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| node require → server.js | Test process imports production server module; must not start a listener or change prod boot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering | server.js startup behavior | mitigate | Guard `app.listen` behind `require.main === module` so importing the app (tests) never binds a port; verify `node server.js` still boots normally and `require('./server')` does not — both asserted in Task 1 verify |
| T-31-02 | Denial of Service | port binding during tests | mitigate | require-guard prevents test imports from binding PORT or scheduling cron; Task 1 automated check confirms no hang/no banner on import |
| T-31-SC | Tampering | npm install (supertest) | mitigate | supertest is a mainstream, audited package; legitimacy confirmed via npmjs.com/package/supertest. No `[ASSUMED]`/`[SUS]` packages — no blocking checkpoint required |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` exits 0 (existing suite intact)
- `node -e "require('./server')"` in zoho-middleware exits 0 without binding a port
- jest.config.js collects both lib and routes globs
</verification>

<success_criteria>
- server.js exports app and guards listen (D-02)
- supertest installed and declared (D-03)
- routes/**/*.js in coverage collection, no stale exclusions (D-05/D-08/TEST-03 partial)
- Existing 323-test middleware suite still green
</success_criteria>

<output>
Create `.planning/phases/31-money-path-test-coverage/31-01-SUMMARY.md` when done.
</output>
