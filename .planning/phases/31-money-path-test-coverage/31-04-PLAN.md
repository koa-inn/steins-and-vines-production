---
phase: 31-money-path-test-coverage
plan: 04
type: execute
wave: 3
depends_on: ["31-01", "31-02", "31-03"]
files_modified:
  - zoho-middleware/jest.config.js
autonomous: true
requirements: [TEST-03]
must_haves:
  truths:
    - "npm run test:coverage reports a coverage table that includes rows for routes/checkout.js, routes/payments.js, routes/webhooks.js, and lib/helcim.js — with no stale `!`-prefix exclusions propping up the number (D-08)"
    - "The global lines threshold reflects the honest measured number (set just below actual, no inflation) (D-06)"
    - "Per-file thresholds protect the money-path files (checkout, payments, webhooks, helcim) so they cannot silently regress (D-07)"
    - "test:coverage passes (exits 0) with the finalized thresholds"
  artifacts:
    - path: "zoho-middleware/jest.config.js"
      provides: "Honest global threshold + per-file money-path thresholds"
      contains: "./routes/checkout.js"
  key_links:
    - from: "zoho-middleware/jest.config.js"
      to: "money-path files"
      via: "per-file coverageThreshold entries"
      pattern: "./routes/(checkout|payments|webhooks)\\.js"
---

<objective>
Finalize the honest coverage thresholds now that the route-level tests exist: measure the real post-change global coverage, set the global `lines` threshold just below it (D-06), and add per-file thresholds on the money-path files (D-07) so the money path cannot regress even while the global stays low. Completes TEST-03.

Purpose: Thresholds can only be set honestly AFTER the Wave 2 route tests run (they raise routes/** coverage from ~0). This plan runs last, reads the actual coverage numbers, and pins thresholds to reality — satisfying success criteria 3 (routes/** in the report) and 4 (honest number).
Output: `jest.config.js` with a measured global threshold and per-file thresholds on routes/checkout.js, routes/payments.js, routes/webhooks.js, lib/helcim.js.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-money-path-test-coverage/31-CONTEXT.md
@.planning/phases/31-money-path-test-coverage/31-PATTERNS.md
@.planning/phases/31-money-path-test-coverage/31-01-SUMMARY.md
@.planning/phases/31-money-path-test-coverage/31-02-SUMMARY.md
@.planning/phases/31-money-path-test-coverage/31-03-SUMMARY.md

<interfaces>
<!-- From 31-PATTERNS.md §jest.config.js and the live config (Plan 01 already added the routes glob). -->

After Plan 01, jest.config.js collectCoverageFrom = ['lib/**/*.js','routes/**/*.js'] and coverageThreshold still has the original { global:{lines:35}, './lib/validate.js':{lines:98}, './lib/logger.js':{lines:98} }.

D-06: set global.lines JUST BELOW the measured honest number (no inflation, no exclusions to prop it up).
D-07: add per-file thresholds for the money-path files. Set each just below that file's actual measured line coverage so it's a real floor, not a guess. The PATTERNS.md placeholders (checkout 60 / payments 80 / webhooks 70 / helcim 70) are STARTING GUESSES ONLY — replace with numbers derived from the real coverage table.

Coverage command: `cd zoho-middleware && npm run test:coverage` — prints a text table (file | % Stmts | % Branch | % Funcs | % Lines | ...).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure honest coverage and pin global + per-file thresholds (D-06, D-07)</name>
  <files>zoho-middleware/jest.config.js</files>
  <read_first>
    - zoho-middleware/jest.config.js (current state after Plan 01 — confirm routes glob present, thresholds still original)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§jest.config.js — D-05..D-08 target shape, placeholder numbers to REPLACE)
    - .planning/phases/31-money-path-test-coverage/31-CONTEXT.md (D-06, D-07, D-08)
  </read_first>
  <action>
    First run `cd zoho-middleware && npm run test:coverage` and read the printed coverage table. Record: the global % Lines, and the % Lines for routes/checkout.js, routes/payments.js, routes/webhooks.js, and lib/helcim.js. Then edit jest.config.js: set `coverageThreshold.global.lines` to an integer just below the measured global % Lines (D-06 — round DOWN by a couple of points for headroom, never above actual). Add per-file `coverageThreshold` entries for `./routes/checkout.js`, `./routes/payments.js`, `./routes/webhooks.js`, and `./lib/helcim.js`, each set just below that file's measured % Lines (D-07) — replace the PATTERNS.md placeholder guesses with the real measured floors. Keep the existing `./lib/validate.js` and `./lib/logger.js` entries unchanged. Do not add any `!`-prefix exclusions (D-08 — keep it honest). After editing, re-run `npm run test:coverage` and confirm it passes (exits 0) — if any threshold is set above actual and fails, lower it to just below the real number. Document the measured numbers in the SUMMARY.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm run test:coverage 2>&1 | tee /tmp/cov31.txt | tail -30; grep -Eq "routes/checkout\.js" /tmp/cov31.txt && grep -Eq "routes/payments\.js" /tmp/cov31.txt && grep -Eq "routes/webhooks\.js" /tmp/cov31.txt && grep -Eq "lib/helcim\.js" /tmp/cov31.txt && echo "ROUTE_ROWS_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm run test:coverage` exits 0 (thresholds met, honest)
    - The coverage text table contains rows for routes/checkout.js, routes/payments.js, routes/webhooks.js, and lib/helcim.js (success criterion 3)
    - jest.config.js coverageThreshold contains per-file entries for `./routes/checkout.js`, `./routes/payments.js`, `./routes/webhooks.js`, `./lib/helcim.js`
    - The global lines threshold is an integer <= the measured global % Lines (no inflation — D-06)
    - No `!`-prefix exclusions exist in collectCoverageFrom (D-08)
  </acceptance_criteria>
  <done>Coverage report includes routes/** rows, the global threshold reflects the honest measured number, and money-path files carry per-file floors; test:coverage passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/test gate → coverage thresholds | The threshold config is the regression gate; a dishonest (inflated or exclusion-propped) number would give a false safety signal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-09 | Repudiation | coverage threshold honesty | mitigate | Global threshold set just below the MEASURED number (D-06) and routes/** included with no `!` exclusions (D-08) — the reported number cannot be quietly propped up |
| T-31-10 | Tampering | money-path coverage regression | mitigate | Per-file floors on checkout/payments/webhooks/helcim (D-07) fail the build if a future change strips money-path test coverage |
</threat_model>

<verification>
- `cd zoho-middleware && npm run test:coverage` exits 0
- Coverage table shows routes/checkout.js, routes/payments.js, routes/webhooks.js, lib/helcim.js rows
- jest.config.js global threshold <= measured global; per-file money-path entries present; no `!` exclusions
</verification>

<success_criteria>
- test:coverage reports routes/** honestly (TEST-03 success criterion 3)
- Global threshold reflects the real number; money-path files protected by per-file floors (D-06, D-07)
- No stale exclusions (D-08, TEST-03 success criterion 4)
</success_criteria>

<output>
Create `.planning/phases/31-money-path-test-coverage/31-04-SUMMARY.md` when done.
</output>
