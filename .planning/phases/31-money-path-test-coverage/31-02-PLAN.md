---
phase: 31-money-path-test-coverage
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - zoho-middleware/__tests__/checkout-route.test.js
autonomous: true
requirements: [TEST-01]
must_haves:
  truths:
    - "POST /api/checkout success path returns a Zoho sales-order number when Zoho succeeds — exercised via supertest against the real exported app, reusing the existing mock harness (D-01, D-04)"
    - "When Zoho fails after the card is charged, the Helcim transaction is voided (void recovery)"
    - "When both Zoho fails AND the void fails, a void-failure alert email is emitted"
    - "Dual-cart shared-charge reversal path is exercised (second cart call against the same transaction)"
    - "The suite is fully green; Phase 32 fail-open gaps are documented as test.todo markers, not failures"
  artifacts:
    - path: "zoho-middleware/__tests__/checkout-route.test.js"
      provides: "supertest route-level tests for POST /api/checkout, all four locked paths"
      min_lines: 120
      contains: "require('supertest')"
  key_links:
    - from: "zoho-middleware/__tests__/checkout-route.test.js"
      to: "zoho-middleware/server.js"
      via: "supertest imports the real exported app"
      pattern: "require\\('\\.\\./server'\\)"
    - from: "checkout-route.test.js void-recovery test"
      to: "lib/helcim voidTransaction"
      via: "assert voidTransaction called with transaction_id"
      pattern: "voidTransaction"
    - from: "checkout-route.test.js alert test"
      to: "lib/mailer sendVoidFailureAlert"
      via: "assert alert called when void fails"
      pattern: "sendVoidFailureAlert"
---

<objective>
Add route-level supertest coverage for `POST /api/checkout` exercising the real wired app and middleware, covering the four locked checkout paths (TEST-01), with Phase-32 fail-open gaps documented as `test.todo`.

Purpose: D-01 mandates supertest against the real exported app (not direct-handler invocation, not the express-mock capture pattern) so the reCAPTCHA gate, referer guard, and rate-limit middleware — exactly where Phase 32's hardening lands — run for real under test. External services (Zoho, Helcim API, Redis, mailer) stay mocked; the route + middleware run live.
Output: `__tests__/checkout-route.test.js` — green characterization tests for success, void recovery, void-failure alert, dual-cart shared-charge reversal, plus `test.todo` markers for HARDEN-01/HARDEN-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/31-money-path-test-coverage/31-CONTEXT.md
@.planning/phases/31-money-path-test-coverage/31-PATTERNS.md
@.planning/phases/31-money-path-test-coverage/31-01-SUMMARY.md

<interfaces>
<!-- Confirmed from codebase. Executor: do NOT re-explore; use these. -->

server.js API-key guard (confirmed lines 253-264): `/api/checkout` is EXEMPT from the x-api-key guard (line 256 `if (req.path === '/checkout') return next();`). So checkout route tests do NOT set x-api-key. reCAPTCHA fail-OPEN when RECAPTCHA_SECRET_KEY unset (intentional current behavior — the gap Phase 32 closes).

Mock harness to reuse (from __tests__/checkout.test.js + the additional server-boot mocks in 31-PATTERNS.md §checkout-route.test.js):
- jest.mock('../lib/helcim') → isEnabled→true, initializeCheckout, getDepositAmount, voidTransaction (resolved by default), getTerminalDiagnostics, getDeviceCode→''
- jest.mock('../lib/zoho-api') → zohoPost, zohoGet (jest.fn, configured per test)
- jest.mock('../lib/cache') → get/set/del, acquireLock→true, isConnected→false (disables Redis rate-limit store)
- jest.mock('../lib/mailer') → sendReservationNotification, sendOfflineOrderNotification, sendVoidFailureAlert (all resolved)
- jest.mock('axios') → post resolved
- SERVER-BOOT mocks (required so require('../server') loads cleanly): '../lib/zohoAuth' (init resolved, isAuthenticated→true), '../lib/validateEnv' (jest.fn), '../lib/checkRedis' (jest.fn resolved), '../lib/checkMailer' (jest.fn), '../lib/brewpad-integration' (syncBatch, init), 'node-cron' (schedule jest.fn), '@sentry/node' (init, setupExpressErrorHandler)
- DO NOT mock express. DO NOT mock https (lets reCAPTCHA fail-open with key unset).

Supertest usage: `var request = require('supertest'); var app = require('../server');` then `request(app).post('/api/checkout').send(body).expect(...)`.

Four locked paths (D-10 characterization, exact expectations in 31-PATTERNS.md §"Four locked path test stubs"):
- PATH-1 success: zohoPost returns {salesorder_id, salesorder_number:'SO-001', total} → expect 2xx + salesorder_number in body
- PATH-2 void recovery: zohoPost for /salesorders throws → voidTransaction called with transaction_id, payment_voided:true
- PATH-3 void-failure alert: zohoPost throws AND voidTransaction rejects → mailer.sendVoidFailureAlert called with txnId
- PATH-4 dual-cart reversal: second cart call, same transaction_id, first already used (cache.get→'used' for otherUsedKey) → voidTransaction NOT called, payment_voided:false, sendVoidFailureAlert called
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Success + void-recovery route tests (PATH-1, PATH-2)</name>
  <files>zoho-middleware/__tests__/checkout-route.test.js</files>
  <behavior>
    - PATH-1: POST /api/checkout with a valid ferment-cart body and Zoho mocked to succeed → 2xx response whose body carries the Zoho salesorder_number ('SO-001'); helcim.voidTransaction is NOT called.
    - PATH-2: same request but zohoPost for the /salesorders create rejects → response indicates the charge was voided (payment_voided true) and helcim.voidTransaction was called once with the charge's transaction_id.
  </behavior>
  <read_first>
    - zoho-middleware/__tests__/checkout.test.js (mock harness lines 1-35 + mockRes helper — reuse the jest.mock set, but DO NOT carry over the express mock)
    - zoho-middleware/routes/checkout.js (POST /api/checkout handler + processCheckout: success response shape, void-recovery branch, the field name carrying the Helcim transaction id, and the exact success body keys)
    - zoho-middleware/server.js (confirm /api/checkout is API-key exempt and which middleware runs before the route)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§checkout-route.test.js — mock list, supertest pattern, fixture builder, four path stubs)
    - .planning/phases/31-money-path-test-coverage/31-CONTEXT.md (D-01, D-04, D-10)
  </read_first>
  <action>
    Create __tests__/checkout-route.test.js in ES5 style ('use strict', var, function — no const/let/arrow/template literals). Declare ALL jest.mock calls from the interfaces block at the top (the reuse set from checkout.test.js PLUS the server-boot mocks), explicitly NOT mocking express and NOT mocking https. Import `var request = require('supertest')` and `var app = require('../server')`. Add a `makeCheckoutBody(overrides)` fixture builder (per PATTERNS.md). Set process.env.API_SECRET_KEY before requiring app so the boot path is deterministic, and ensure RECAPTCHA_SECRET_KEY is unset so reCAPTCHA fails open (the route proceeds). Write the PATH-1 success test and the PATH-2 void-recovery test, asserting on the real HTTP response and on the helcim/zoho mock call arguments. Read the actual handler in routes/checkout.js to confirm the precise success status code, the response body key holding the order number, and the `payment_voided` field name — do not guess; match the real implementation (characterization stance). Configure zohoPost per-test via mockImplementation/mockResolvedValueOnce keyed on the Zoho path argument.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- checkout-route.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- checkout-route.test.js` exits 0
    - The file contains `require('supertest')` and `require('../server')` and does NOT contain a `jest.mock('express'` call
    - PATH-1 test asserts the response body contains the Zoho salesorder_number and asserts helcim.voidTransaction was NOT called
    - PATH-2 test asserts helcim.voidTransaction was called with the transaction_id and the response reflects a voided charge
  </acceptance_criteria>
  <done>Success and void-recovery paths pass via supertest against the real app with external services mocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Void-failure alert + dual-cart reversal + Phase-32 todos (PATH-3, PATH-4, D-10)</name>
  <files>zoho-middleware/__tests__/checkout-route.test.js</files>
  <behavior>
    - PATH-3: zohoPost rejects AND helcim.voidTransaction rejects → mailer.sendVoidFailureAlert is called with an object carrying the transaction id (txnId); the route still returns a handled response (not an unhandled 500 crash).
    - PATH-4: a second checkout call sharing the same transaction_id where the first cart already consumed the charge (cache.get returns 'used' for the other cart's used-key) → helcim.voidTransaction is NOT called, the response reports payment_voided false, and sendVoidFailureAlert is called (shared-charge reversal accounting).
    - Phase-32 gaps recorded as test.todo (no red): HARDEN-01 (unauthenticated checkout currently passes) and HARDEN-03 (duplicate charge_key not 409 when Redis down).
  </behavior>
  <read_first>
    - zoho-middleware/__tests__/checkout-route.test.js (the file from Task 1 — extend it, reuse its mocks and fixture builder)
    - zoho-middleware/routes/checkout.js (the void-failure-alert branch: what triggers sendVoidFailureAlert and the argument shape with txnId; the dual-cart shared-charge logic: how a second cart call detects the charge was already used via cache.get, the otherUsedKey naming, and how payment_voided is set in that branch)
    - zoho-middleware/lib/mailer.js (sendVoidFailureAlert signature — confirm the txnId/amount argument shape, no PII)
    - .planning/phases/31-money-path-test-coverage/31-PATTERNS.md (§"Four locked path test stubs" PATH-3/PATH-4 + §"Phase 32 gap markers")
  </read_first>
  <action>
    Extend checkout-route.test.js with the PATH-3 and PATH-4 tests, reusing the existing mock harness and fixture builder. For PATH-3, set zohoPost to reject for the salesorders create and helcim.voidTransaction to reject, then assert mailer.sendVoidFailureAlert was called with an argument containing the transaction id (read mailer.js + checkout.js to get the exact key name — do not guess). For PATH-4, drive the dual-cart shared-charge reversal: configure cache.get to return 'used' for the other cart's used-key (read checkout.js for the exact key-construction so the mock matches), send a second cart call with the same transaction_id, and assert voidTransaction was NOT called, payment_voided is false, and sendVoidFailureAlert fired. Add two `test.todo(...)` lines documenting HARDEN-01 and HARDEN-03 per D-10 so Phase 32 has an in-suite checklist while the suite stays green. Match every assertion to the real implementation behavior (characterization, not aspiration).
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- checkout-route.test.js 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- checkout-route.test.js` exits 0 with all four PATH tests passing and zero failing tests
    - PATH-3 test asserts mailer.sendVoidFailureAlert was called with an argument containing the transaction id
    - PATH-4 test asserts helcim.voidTransaction was NOT called and the response payment_voided field is false
    - The file contains at least two `test.todo(` markers referencing HARDEN-01 and HARDEN-03
    - `cd zoho-middleware && npm test` (full suite) exits 0 — no regression in the existing 323 tests
  </acceptance_criteria>
  <done>All four locked checkout paths are green via supertest; Phase-32 gaps recorded as test.todo; full middleware suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → POST /api/checkout | Untrusted checkout payload crosses into the charge+order path; tests exercise the real middleware chain (reCAPTCHA gate, referer guard, rate limit) |
| route → Helcim/Zoho/mailer | External money-side effects; mocked in tests but the void-recovery and alert paths are the safety-critical assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-03 | Repudiation | charge→order→void recovery | mitigate | PATH-2/PATH-3 tests assert the void + void-failure-alert side effects exist, so a future change that silently drops void recovery fails the suite |
| T-31-04 | Information Disclosure | void-failure alert payload | mitigate | PATH-3 asserts the alert argument carries txnId/amount only (no customer PII), locking in the no-PII contract from lib/mailer.js |
| T-31-05 | Elevation of Privilege | unauthenticated checkout (fail-open reCAPTCHA) | accept (this phase) | Documented as `test.todo('HARDEN-01')`; characterization stance keeps the current fail-open behavior green — Phase 32 flips it to fail-closed |
</threat_model>

<verification>
- `cd zoho-middleware && npm test -- checkout-route.test.js` exits 0, four PATH tests green
- `cd zoho-middleware && npm test` full suite exits 0
- File uses supertest against require('../server'), no express mock
</verification>

<success_criteria>
- POST /api/checkout covered for: success→Zoho order, void recovery, void-failure alert, dual-cart shared-charge reversal (TEST-01)
- Suite green; HARDEN-01 / HARDEN-03 recorded as test.todo (D-10)
</success_criteria>

<output>
Create `.planning/phases/31-money-path-test-coverage/31-02-SUMMARY.md` when done.
</output>
