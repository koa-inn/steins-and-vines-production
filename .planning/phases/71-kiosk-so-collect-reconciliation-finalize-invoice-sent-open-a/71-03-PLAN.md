---
phase: 71-kiosk-so-collect-reconciliation
plan: 03
type: execute
wave: 3
depends_on:
  - "71-01"
  - "71-02"
files_modified: []
autonomous: false
requirements:
  - "71-D1-finalize-invoice"
  - "71-D2-apply-to-invoice"

must_haves:
  truths:
    - "A real kiosk SO collection charged on the terminal reconciles in Zoho to a paid invoice with the payment applied (unused_amount 0)"
  artifacts: []
  key_links: []
---

<objective>
Final live verification. The unit tests in 71-01/71-02 mock Zoho, so they prove call-shape correctness but not that Zoho Books actually reconciles the way we expect (specifically: that `/invoices/fromsalesorder` + `/submit` + an `invoices`-array payment produces a truly paid invoice, and that the dedup pre-check reuses an existing draft SO-invoice rather than duplicating). This checkpoint confirms the real Zoho-side end-state after deploying to staging.

Purpose: money-path — do not declare the fix done on mocked tests alone.
Output: owner confirmation that a real SO collection reconciles to paid, with no duplicate invoice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/debug/kiosk-so-collect-draft-unapplied.md
@.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-01-SUMMARY.md
@.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The collect APPROVED webhook path (71-01) and the online SO-deposit path (71-02) now convert/reuse the SO invoice, mark it sent, and apply the customerpayment to the invoice (never salesorders_to_apply), with a fail-closed reconcile record on post-charge failure. Middleware deploys to staging (staging.steinsandvines.ca / Railway) on push to main. This is a server/webhook change — no kiosk client or public HTML changed.</what-built>
  <how-to-verify>
    1. Confirm the middleware change is deployed to staging (git push origin main → Railway staging). Do NOT force-push to production yet.
    2. Create a test Sales Order in Zoho with a small balance (e.g. a $1–$5 line) for a test customer.
    3. From the kiosk collect flow, collect payment on that SO via the Helcim Smart Terminal (use a real/test card per your Helcim staging setup).
    4. After the terminal APPROVES, open the SO in Zoho and confirm ALL of:
       - the SO's invoice status is PAID with balance $0 (not draft, not open/unpaid);
       - exactly ONE invoice exists for the SO (no duplicate);
       - the customer payment shows the invoice under applied_invoices and unused_amount is $0 (no orphaned advance);
       - the Helcim transaction id appears on the payment reference.
    5. (Optional, if a draft SO-invoice already existed) repeat against an SO that already has a draft invoice and confirm that existing invoice was reused and finalized — not duplicated.
    6. (Optional failure-path smoke) confirm nothing in normal operation writes a collect:reconcile-failure sentinel; if one appears, the fail-closed path fired — inspect the alert.
  </how-to-verify>
  <resume-signal>Reply "approved" if the invoice ends PAID with the payment applied (unused_amount 0) and no duplicate invoice; otherwise describe the observed Zoho end-state (invoice status, balance, applied_invoices, unused_amount, any duplicate) so the fix can be revised.</resume-signal>
</task>

</tasks>

<verification>
- Owner-driven live check on staging: real SO collection → paid invoice + applied payment (unused_amount 0), no duplicate.
- Production cutover (force-push to production) happens only AFTER staging approval, per CLAUDE.md deployment rules — not part of this plan.
</verification>

<success_criteria>
- A real kiosk SO collection reconciles in Zoho to a paid invoice with the payment applied and no duplicate invoice, confirmed by the owner on staging.
</success_criteria>

<output>
Create `.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-03-SUMMARY.md` recording the live-verify outcome (SO#, invoice#, final status, unused_amount) and the owner's approval or the observed discrepancy.
</output>
