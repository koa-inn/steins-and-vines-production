---
phase: 71-kiosk-so-collect-reconciliation
plan: 02
type: execute
wave: 2
depends_on:
  - "71-01"
files_modified:
  - zoho-middleware/routes/checkout.js
  - zoho-middleware/__tests__/checkout-so-deposit-reconcile.test.js
autonomous: true
requirements:
  - "71-D4-sibling-deposit"

must_haves:
  truths:
    - "An online deposit charged against a sales order (useInvoice false) books to a finalized invoice, not the sales order"
    - "The deposit customerpayment is applied via an invoices array — no salesorders_to_apply on the SO branch"
    - "No duplicate invoice is created for the SO (reuses the dedup-guarded finalize helper)"
  artifacts:
    - path: "zoho-middleware/routes/checkout.js"
      provides: "SO-deposit branch converts/reuses SO invoice then applies payment to invoice_id"
      contains: "ensureOpenInvoiceForSalesOrder"
    - path: "zoho-middleware/__tests__/checkout-so-deposit-reconcile.test.js"
      provides: "Regression test: SO-deposit path applies to an invoice, never salesorders_to_apply"
      contains: "salesorders_to_apply"
  key_links:
    - from: "zoho-middleware/routes/checkout.js"
      to: "money-path.ensureOpenInvoiceForSalesOrder"
      via: "finalize SO invoice before booking the deposit"
      pattern: "ensureOpenInvoiceForSalesOrder"
    - from: "zoho-middleware/routes/checkout.js"
      to: "zohoPost('/customerpayments', ...)"
      via: "invoices array on the SO branch"
      pattern: "invoices\\s*="
---

<objective>
Fix the sibling of the collect bug: the online **deposit-on-a-salesorder** path in `checkout.js:693` books the deposit customerpayment with `salesorders_to_apply` — the same orphaned-advance class of defect. When `useInvoice` is false (a deposit against a raw sales order), the payment must instead be applied to a finalized invoice, reusing the dedup-guarded helper introduced in 71-01 so the two money-paths cannot drift and no duplicate invoice is created.

Purpose: close the second `salesorders_to_apply` money-path so online SO deposits reconcile to a paid/partially-paid invoice, matching the collect fix.
Output: `checkout.js` SO branch rewritten to finalize-then-apply-to-invoice; a regression test.

Scope guard (CONTEXT decision 4): this is included because PATTERNS confirms it is low-risk — the correct pattern already exists 2 lines away (the `useInvoice` branch at checkout.js:690-691) and the block already has good observability (try/catch + captureExceptionSafe at 697-703). If during execution the SO-deposit case turns out to need deeper investigation (e.g. the deposit is partial and the invoice must NOT be marked fully paid, or the SO has no convertible lines), STOP and record the finding in the SUMMARY rather than forcing the fix — do not touch pos.js:2465 either way.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md

@.planning/debug/kiosk-so-collect-draft-unapplied.md
@.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-PATTERNS.md
@.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-01-SUMMARY.md

@zoho-middleware/routes/checkout.js

<interfaces>
<!-- Helper delivered by 71-01 (money-path.js). Consume, do not reimplement. -->
ensureOpenInvoiceForSalesOrder(soId) -> Promise<invoice_id>
  Dedup pre-check (reuses an existing linked invoice), converts via /invoices/fromsalesorder only when none exists, submits to mark open/sent, resolves the finalized invoice_id, rejects on genuine failure.

checkout.js SO-deposit block today (lines 680-704): async/await style. `useInvoice` true branch already correct (invoices array, line 691); the `else` branch (line 693) is the bug (salesorders_to_apply). Existing catch (697-703) uses captureExceptionSafe with tags { reqId, txnId, invoiceId }. `depositAmount` may be a PARTIAL deposit — the invoice may end partially paid, not fully paid.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing regression test for the SO-deposit path (RED)</name>
  <files>zoho-middleware/__tests__/checkout-so-deposit-reconcile.test.js</files>
  <read_first>
    - zoho-middleware/routes/checkout.js lines 679-735 (the payment-record block + the email block that follows)
    - zoho-middleware/__tests__/checkout-route.test.js and checkout.test.js (existing checkout harness/mock shape — clone whichever drives the POST /api/checkout handler with mocked zoho-api)
    - .planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-PATTERNS.md (checkout.js section)
  </read_first>
  <behavior>
    - SO-deposit (useInvoice false): a completed checkout with a transactionId, depositAmount > 0, and a soId (no invoice) calls ensureOpenInvoiceForSalesOrder(soId) and books the customerpayment with an `invoices` array against the resolved invoice_id with amount_applied equal to depositAmount.
    - The customerpayment call MUST NOT include salesorders_to_apply.
    - Invoice branch (useInvoice true) is unchanged: still applies invoices:[{invoice_id: soId, amount_applied}] and does NOT call the SO-conversion helper.
    - Partial deposit: amount_applied equals depositAmount (not the SO total) — the test asserts the applied amount is the deposit, not the full balance.
  </behavior>
  <action>Clone the existing checkout test harness (mock ../lib/zoho-api zohoPost/zohoGet, and mock ../lib/money-path exposing ensureOpenInvoiceForSalesOrder as a jest.fn resolving a fixed invoice_id). Drive the checkout handler through to the payment-record block with a body/state that sets useInvoice false, a transactionId, and depositAmount. Assert: money-path.ensureOpenInvoiceForSalesOrder was called with the soId; the /customerpayments zohoPost body carries an invoices array (invoice_id = the resolved id, amount_applied = depositAmount) and has no salesorders_to_apply key. Add a second case for useInvoice true asserting the helper is NOT called and the existing invoices-branch shape is preserved. These MUST fail against current checkout.js (which books salesorders_to_apply on the SO branch and never calls the helper). Confirm RED before Task 2. Do not modify existing tests.</action>
  <verify>
    <automated>cd zoho-middleware && npx jest checkout-so-deposit-reconcile 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - New test file exists with the SO-deposit and invoice-branch cases described.
    - Suite fails (RED) against unmodified checkout.js on the no-salesorders_to_apply and helper-called assertions.
    - No existing test modified (CLAUDE.md rule 10).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix the checkout.js SO-deposit branch to finalize + apply to invoice (GREEN)</name>
  <files>zoho-middleware/routes/checkout.js</files>
  <read_first>
    - zoho-middleware/routes/checkout.js top-of-file requires (add money-path import if absent) and lines 679-704 (the block to change)
    - zoho-middleware/lib/money-path.js ensureOpenInvoiceForSalesOrder (from 71-01)
    - 71-01-SUMMARY.md (confirm the helper signature/behavior as shipped)
  </read_first>
  <action>Import money-path at the top of checkout.js if not already required (var moneyPath = require('../lib/money-path'); or destructure ensureOpenInvoiceForSalesOrder). In the payment-record block (lines 680-704), change the `else` branch (currently line 693 salesorders_to_apply): when useInvoice is false and a soId is present, await moneyPath.ensureOpenInvoiceForSalesOrder(soId) to obtain a finalized invoice_id, then set paymentBody.invoices = an array of one object with invoice_id (the resolved id) and amount_applied = depositAmount. Remove the salesorders_to_apply assignment entirely. Keep the async/await style and the existing try/catch — the catch already calls captureExceptionSafe + log.error (fail-closed observability is already present here; do not downgrade it). Leave the useInvoice true branch (line 691) untouched. Update the notes string only if it still reads "Sales Order" — it may now be an invoice; keep it accurate (e.g. still reference the SO number for staff readability). Do NOT touch the confirmation-email block (708-735) or any other part of the function. No fenced code in notes — edit the file directly.</action>
  <verify>
    <automated>cd zoho-middleware && npx jest checkout-so-deposit-reconcile 2>&1 | tail -20 && grep -n "salesorders_to_apply" routes/checkout.js || echo "no salesorders_to_apply in checkout.js (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - Task 1 tests pass (GREEN).
    - `grep -n "salesorders_to_apply" zoho-middleware/routes/checkout.js` returns no match.
    - The SO-deposit branch awaits ensureOpenInvoiceForSalesOrder and applies the deposit via an invoices array with amount_applied = depositAmount; useInvoice branch unchanged.
    - Full middleware suite + lint green: `cd zoho-middleware && npm test` and `npm run lint`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public checkout client → middleware | Untrusted order + deposit request; a real online card charge (transactionId) has occurred before this booking step. |
| middleware → Zoho Books | Must book the online deposit to a bookable invoice, not strand it as an SO advance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-05 | Tampering/Integrity | checkout.js SO-deposit payment booking | mitigate | Book the deposit via an `invoices` array against a finalized invoice_id; remove salesorders_to_apply — the deposit reconciles instead of orphaning. |
| T-71-06 | Integrity | duplicate invoice for the SO | mitigate | Reuse ensureOpenInvoiceForSalesOrder's dedup pre-check — no second invoice created when one already exists. |
| T-71-07 | Repudiation | post-charge booking failure | accept (existing) | Existing try/catch + captureExceptionSafe + log.error already surfaces failure; preserved unchanged. Full reconcile-sentinel parity with the collect path is deferred unless the SUMMARY flags an orphan gap. |
| T-71-SC | Tampering | package installs | accept | No npm installs in this phase. |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` — full suite green.
- `cd zoho-middleware && npx jest checkout-so-deposit-reconcile` — new cases pass.
- `grep -n "salesorders_to_apply" zoho-middleware/routes/checkout.js` — no match.
- `npm run lint` — clean.
</verification>

<success_criteria>
- Online SO deposits book to a finalized invoice via an invoices array (partial deposit → amount_applied = depositAmount); no salesorders_to_apply on the SO branch; no duplicate invoice.
- useInvoice branch and email/observability behavior unchanged.
- No js/ or kiosk.html changes.
</success_criteria>

<output>
Create `.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-02-SUMMARY.md` when done. If the SO-deposit case was found to need deeper investigation (partial-deposit invoice state, non-convertible SO), record the finding and whether the fix was applied or deferred.
</output>
