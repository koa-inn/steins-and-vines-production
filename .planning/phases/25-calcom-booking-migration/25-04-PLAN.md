---
phase: 25-calcom-booking-migration
plan: 04
type: execute
wave: 3
depends_on: ["25-02", "25-03"]
files_modified:
  - zoho-middleware/routes/bookings.js
  - zoho-middleware/lib/zoho-api.js
  - zoho-middleware/lib/validateEnv.js
  - zoho-middleware/__tests__/bookings.test.js
autonomous: false
requirements: [BOOK-02, BOOK-03, BOOK-04, BOOK-05]

must_haves:
  truths:
    - "A real ferment-in-store checkout on staging creates a Cal.com booking AND the customer receives a Cal.com confirmation email (the acceptance proof closing the original missing-confirmation issue)"
    - "At least one additional event type beyond ferment-in-store is bookable through Cal.com via /api/bookings/services and POST /api/bookings"
    - "bookingsGet/bookingsPost usage and ZOHO_BOOKINGS_* env references are removed with no dead references; normalizeTimeTo24h is kept"
    - "The offline-fallback (req.zohoOffline) path still works after removal"
    - "Both test suites and lint are green; the Cal.com webhook delivered a real signed event on staging (confirming raw-body HMAC form)"
  artifacts:
    - path: "zoho-middleware/lib/zoho-api.js"
      provides: "Zoho Bookings helpers removed (bookingsGet/bookingsPost/BOOKINGS_API_BASE gone)"
    - path: "zoho-middleware/lib/validateEnv.js"
      provides: "ZOHO_BOOKINGS_* removed from OPTIONAL"
  key_links:
    - from: "staging checkout"
      to: "Cal.com booking + confirmation email"
      via: "POST /api/bookings -> createBooking -> attendee.email"
      pattern: "attendee"
---

<objective>
Prove the migration end-to-end on staging (real booking + confirmation email = the phase acceptance proof), enable the additional event type, then remove the now-dead Zoho Bookings code — in that order, because removal is only safe after Cal.com is verified (critical constraint #5).

Purpose: Success criteria #2 (staging booking + email), #3 (>=1 additional type), and #4 (Zoho removal, no dead refs, offline fallback preserved). The staging delivery also empirically confirms the webhook raw-body HMAC form (Assumption A2) from Plan 03.

Output: A verified staging deployment, a second working event type, and a clean codebase with no Zoho Bookings references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-calcom-booking-migration/25-CONTEXT.md
@.planning/phases/25-calcom-booking-migration/25-RESEARCH.md
@.planning/phases/25-calcom-booking-migration/25-01-SUMMARY.md
@.planning/phases/25-calcom-booking-migration/25-02-SUMMARY.md
@.planning/phases/25-calcom-booking-migration/25-03-SUMMARY.md

<interfaces>
<!-- Removal targets (grep-verified in RESEARCH Runtime State Inventory). -->

zoho-middleware/lib/zoho-api.js — REMOVE: bookingsGet (~185), bookingsPost (~203), BOOKINGS_API_BASE (~64), and their entries in module.exports (~305/313-314). KEEP: normalizeTimeTo24h (reused by routes/bookings.js POST), zohoGet/zohoPost (used by /api/contacts), all inventory helpers.
zoho-middleware/routes/bookings.js — REMOVE the bookingsGet/bookingsPost require lines (lines 7-8) if still present after Plan 02. KEEP normalizeTimeTo24h, zohoGet, zohoPost imports (contacts handler).
zoho-middleware/lib/validateEnv.js — REMOVE ZOHO_BOOKINGS_SERVICE_ID and ZOHO_BOOKINGS_STAFF_ID (~51-52). KEEP the CALCOM_* entries from Plan 01.

Railway env (manual, dashboard): set CALCOM_API_KEY, CALCOM_EVENT_TYPE_FERMENT, CALCOM_EVENT_TYPE_<additional>, CALCOM_WEBHOOK_SECRET on STAGING first; remove ZOHO_BOOKINGS_SERVICE_ID / ZOHO_BOOKINGS_STAFF_ID after verification.

Deploy: staging = `git push origin main`. Production deploy is NOT part of this phase (needs explicit human approval per CLAUDE.md — surfaced as a follow-up, not done here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable the additional event type</name>
  <files>zoho-middleware/routes/bookings.js, zoho-middleware/lib/validateEnv.js, zoho-middleware/__tests__/bookings.test.js</files>
  <read_first>
    - zoho-middleware/routes/bookings.js (services + POST handlers from Plan 02; how CALCOM_EVENT_TYPE_FERMENT is read)
    - zoho-middleware/lib/validateEnv.js (OPTIONAL array, CALCOM_* entries)
    - .planning/phases/25-calcom-booking-migration/25-01-SUMMARY.md (the chosen additional event type name + its event-type id)
    - .planning/phases/25-calcom-booking-migration/25-RESEARCH.md (Pitfall 5 — drive services from configured ids)
  </read_first>
  <action>
    Add support for the second event type chosen in Plan 01. Add env var CALCOM_EVENT_TYPE_<NAME> (e.g. CALCOM_EVENT_TYPE_CONSULTATION) and register it in validateEnv OPTIONAL with a descriptive desc. In the routes/bookings.js services handler, iterate over the configured event-type ids (ferment + additional, skipping any unset) calling listEventType per id so /api/bookings/services returns BOTH in the legacy services array. Accept an OPTIONAL event-type selector in the POST body mapping to the configured ids, defaulting to ferment when absent (the current frontend sends no selector — the default must keep the existing flow working). Extend bookings.test.js: services returns both event types when both ids are set; POST without a selector still books ferment. Per BOOK-03.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- bookings.test.js && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - services test asserts two event types returned when both env ids are set.
    - POST-without-selector test still books the ferment type (backward compatible).
    - `npm run lint` clean; full middleware suite green.
  </acceptance_criteria>
  <done>A second event type is configured and bookable; default behavior unchanged for the existing frontend.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Staging end-to-end verification — real booking + confirmation email</name>
  <read_first>
    - .planning/phases/25-calcom-booking-migration/25-RESEARCH.md (§5 Confirmation Emails — acceptance proof; §6 Webhooks — raw-body confirmation; Open Questions Q3/Q5)
    - CLAUDE.md (staging-first deploy rule; run both suites + lint before push)
  </read_first>
  <what-built>Cal.com-backed booking endpoints (Plan 02), the webhook endpoint (Plan 03), and the second event type (Task 1) — all automated and unit-tested. This checkpoint verifies the live behavior that cannot be unit-tested: a real booking and a real confirmation email.</what-built>
  <how-to-verify>
    1. Ensure both suites + lint are green locally: `npm test` (root) AND `cd zoho-middleware && npm test`, then `npm run lint`.
    2. Set the CALCOM_* env vars on Railway STAGING (CALCOM_API_KEY, CALCOM_EVENT_TYPE_FERMENT, the additional event-type id, CALCOM_WEBHOOK_SECRET). Point the Cal.com webhook subscriber URL at the staging middleware /api/webhooks/calcom.
    3. Deploy to staging: `git push origin main`.
    4. On staging.steinsandvines.ca, run a full ferment-in-store checkout: pick a date (calendar loads from Cal.com availability), pick a time slot (12-hour labels), enter a real test customer email, complete the booking step.
    5. Confirm in the Cal.com dashboard that the booking was created at the correct local (Squamish) time — not offset by the UTC bug (Pitfall 2).
    6. Confirm the test customer inbox RECEIVED the Cal.com confirmation email (this is the acceptance proof for the whole phase — the original missing-confirmation issue).
    7. Confirm the webhook fired: check the middleware staging logs for `calcom.webhook_received` with a VALID signature (this empirically confirms the raw-body HMAC form, Assumption A2). If the signature failed, adjust verifyWebhook to the correct body form (mirror helcim's multi-candidate approach) and redeploy.
    8. Confirm the additional event type is bookable (services lists it).
    9. Confirm the STAFF/host notification email arrived at the shop inbox (CONTEXT requires staff notification via Cal.com where possible). If only the customer email arrives, record that the default Cal.com host-notification target needs a dashboard/Workflow change, and capture it as a follow-up (do not block the phase on it).
    10. Cancel the test booking (Cal.com dashboard or the customer cancel link) and confirm BOTH: the BOOKING_CANCELLED webhook fired (staging logs show calcom.webhook_received), AND the freed slot reappears in GET /api/bookings/slots for that date — this proves the cancellation handler parsed the real payload start field and cleared the correct cache key (closes Plan 03 Assumption A2 for the cancellation path). If the slot does not reappear, fix the payload field path in routes/webhooks.js per Plan 03's fallback note and redeploy.
  </how-to-verify>
  <acceptance_criteria>
    - A Cal.com booking exists at the correct Squamish local time.
    - The test customer received the Cal.com confirmation email.
    - Staging logs show calcom.webhook_received with a verified signature.
    - The additional event type appears in /api/bookings/services.
    - The staff/host notification email arrived at the shop inbox (or a follow-up is recorded that the default Cal.com host target needs a dashboard/Workflow change).
    - After cancelling the test booking, the freed slot reappears in /api/bookings/slots for that date (cancellation webhook + cache invalidation confirmed end-to-end).
    - If any of the above fails, STOP before Task 3 (removal) — do not remove Zoho until Cal.com is proven.
  </acceptance_criteria>
  <resume-signal>Type "staging verified" (booking + email + webhook all confirmed) to proceed to Zoho removal, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove Zoho Bookings code paths and env refs</name>
  <files>zoho-middleware/lib/zoho-api.js, zoho-middleware/routes/bookings.js, zoho-middleware/lib/validateEnv.js</files>
  <read_first>
    - zoho-middleware/lib/zoho-api.js (bookingsGet ~185, bookingsPost ~203, BOOKINGS_API_BASE ~64, exports ~305/313-314)
    - zoho-middleware/routes/bookings.js (require lines; confirm no remaining booking call uses them)
    - zoho-middleware/lib/validateEnv.js (ZOHO_BOOKINGS_* ~51-52)
    - .planning/phases/25-calcom-booking-migration/25-RESEARCH.md (Runtime State Inventory — grep-verified removal list)
  </read_first>
  <action>
    Only after Task 2 verification passes. Grep the whole zoho-middleware for bookingsGet, bookingsPost, BOOKINGS_API_BASE, ZOHO_BOOKINGS_SERVICE_ID, ZOHO_BOOKINGS_STAFF_ID to confirm the only references are the ones being removed (CLAUDE.md rule #6). Remove bookingsGet, bookingsPost, and BOOKINGS_API_BASE definitions from lib/zoho-api.js and from its module.exports object. Remove the bookingsGet/bookingsPost require lines from routes/bookings.js if Plan 02 left them. Remove the two ZOHO_BOOKINGS_* entries from validateEnv OPTIONAL. KEEP normalizeTimeTo24h, zohoGet, zohoPost, and all inventory helpers (still used elsewhere). Run the full middleware suite after the shared-lib edit (CLAUDE.md rule #7). On Railway staging, remove the ZOHO_BOOKINGS_SERVICE_ID / ZOHO_BOOKINGS_STAFF_ID vars (manual dashboard step — surface it in the SUMMARY). Per BOOK-04.
  </action>
  <verify>
    <automated>cd zoho-middleware && grep -rn -E "bookingsGet|bookingsPost|BOOKINGS_API_BASE|ZOHO_BOOKINGS_SERVICE_ID|ZOHO_BOOKINGS_STAFF_ID" lib routes | grep -v '^\s*//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The grep (excluding comment lines) returns 0 references in lib/ and routes/.
    - `cd zoho-middleware && npm test` passes (full suite, after shared-lib edit).
    - `npm test` (root frontend) passes — no regression.
    - `npm run lint` clean.
    - normalizeTimeTo24h, zohoGet, zohoPost still exported from zoho-api.js.
    - The offline-fallback (req.zohoOffline -> PENDING-) path still present in routes/bookings.js.
  </acceptance_criteria>
  <done>No Zoho Bookings references remain in code or env config; offline fallback preserved; both suites + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging deploy -> live Cal.com | Real credentials + real customer email cross here during verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | Information disclosure | CALCOM_* secrets on Railway | mitigate | Set only in Railway dashboard (staging), never committed; verified absent from git. |
| T-25-12 | Tampering | Removing live code prematurely | mitigate | Removal (Task 3) gated behind the blocking staging-verification checkpoint (Task 2) — Zoho stays until Cal.com proven. |
| T-25-13 | Repudiation | Webhook signature form wrong | mitigate | Empirically confirmed via real staging delivery in Task 2 (Assumption A2 closed). |
</threat_model>

<verification>
- Staging booking created at correct local time + confirmation email received (acceptance proof).
- Staging logs show a verified calcom.webhook_received event.
- Additional event type bookable.
- grep returns 0 Zoho Bookings references in lib/ + routes/.
- `cd zoho-middleware && npm test`, root `npm test`, and `npm run lint` all green.
- Production deploy intentionally NOT performed here (requires explicit human approval per CLAUDE.md — note as follow-up).
</verification>

<success_criteria>
- End-to-end staging booking + Cal.com confirmation email verified (BOOK-02).
- At least one additional event type bookable (BOOK-03).
- Zoho Bookings code + env removed, no dead refs, offline fallback intact (BOOK-04).
- Both suites + lint green (BOOK-05).
</success_criteria>

<output>
Create `.planning/phases/25-calcom-booking-migration/25-04-SUMMARY.md` when done. Record: the staging booking outcome + email confirmation, the confirmed webhook HMAC body form, the additional event type, and the Railway env changes (staging) plus the pending production deploy follow-up.
</output>
