---
phase: 25-calcom-booking-migration
plan: 03
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - zoho-middleware/routes/webhooks.js
  - zoho-middleware/__tests__/calcom-webhook.test.js
autonomous: true
requirements: [BOOK-02, BOOK-05]

must_haves:
  truths:
    - "POST /api/webhooks/calcom verifies the x-cal-signature-256 HMAC over the raw body and rejects invalid signatures with 401"
    - "The endpoint is exempt from the API-key guard and tolerated by the referer guard (no Referer)"
    - "Valid webhooks respond 200 immediately and process asynchronously, logging via eventLog"
    - "BOOKING_CANCELLED webhooks invalidate the slots/availability cache for the affected date so freed slots reappear"
  artifacts:
    - path: "zoho-middleware/routes/webhooks.js"
      provides: "POST /api/webhooks/calcom handler mirroring the terminal webhook pattern"
      contains: "x-cal-signature-256"
    - path: "zoho-middleware/__tests__/calcom-webhook.test.js"
      provides: "Tests for signature accept/reject + cancellation cache invalidation"
      contains: "verifyWebhook"
  key_links:
    - from: "zoho-middleware/routes/webhooks.js"
      to: "zoho-middleware/lib/calcom.js"
      via: "verifyWebhook(rawBody, signature)"
      pattern: "calcom\\.verifyWebhook"
    - from: "POST /api/webhooks/calcom"
      to: "req.rawBody"
      via: "raw body for HMAC verification"
      pattern: "rawBody"
---

<objective>
Add the Cal.com webhook endpoint POST /api/webhooks/calcom to routes/webhooks.js, mirroring the existing Helcim terminal-webhook pattern, and verify-then-process Cal.com events (signature check, 200-fast, async log, cancellation cache invalidation).

Purpose: Per CONTEXT decision + critical constraint #3, Cal.com fires HMAC-signed webhooks (created/cancelled/rescheduled) that keep local availability state in sync. This endpoint runs independently of the booking handlers (different file, Plan 02 owns routes/bookings.js, this plan owns routes/webhooks.js) so it can execute in the same wave. It consumes verifyWebhook from the Plan 01 adapter.

Output: A signature-verified, key-guard-exempt webhook handler with tests, that on cancellation frees up the affected date's cached slots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-calcom-booking-migration/25-CONTEXT.md
@.planning/phases/25-calcom-booking-migration/25-RESEARCH.md
@.planning/phases/25-calcom-booking-migration/25-01-SUMMARY.md

<interfaces>
<!-- Patterns to mirror. Use directly. -->

EXISTING terminal webhook (zoho-middleware/routes/webhooks.js — the pattern to mirror):
- Route registered as `router.post(['/api/webhooks/terminal', '/webhooks/terminal'], fn)` (dual path so the /webhooks/ exemption applies).
- Reads headers, builds `var rawBody = req.rawBody ? req.rawBody.toString() : ''`.
- Calls verify; on failure `return res.status(401).json({ error: 'Invalid signature' })`.
- On success: `res.status(200).json({ received: true })` IMMEDIATELY, then processes async.
- Logs via `eventLog.logEvent('helcim.webhook_received', {...})`.

SERVER GUARDS (zoho-middleware/server.js — already verified, do NOT modify):
- Line 239: `if (req.path.indexOf('/webhooks/') === 0) return next();` — API-key guard exemption. The handler MUST be reachable at a path starting `/webhooks/` (register dual path like the terminal one: ['/api/webhooks/calcom', '/webhooks/calcom']).
- Line 73: referer guard skips when `!req.headers.referer` — Cal.com sends no Referer, so it passes. Confirm, do not change.
- Line 43: `req.rawBody = buf` captured by express.json verify — use req.rawBody for HMAC.

ADAPTER (zoho-middleware/lib/calcom.js — from Plan 01):
- `verifyWebhook(rawBody, signature)` -> boolean (HMAC-SHA256 hex over rawBody; fail-open when secret unset).

CACHE (zoho-middleware/lib/cache.js + C.CACHE_KEYS):
- C.CACHE_KEYS.SLOTS_PREFIX + date and C.CACHE_KEYS.AVAILABILITY_PREFIX + 'YYYY-MM' — the keys Plan 02 writes; cancellation must del these for the affected date/month.

CAL.COM WEBHOOK FACTS (RESEARCH §6):
- Header: single `x-cal-signature-256`, HMAC-SHA256, hex digest, over the raw body.
- Triggers: BOOKING_CREATED, BOOKING_CANCELLED, BOOKING_RESCHEDULED. Body includes triggerEvent + payload (with booking start datetime -> derive YYYY-MM-DD for cache invalidation).
- RESEARCH Assumption A2: raw-body-vs-stringify is unconfirmed — verifyWebhook signs raw body; the staging delivery in Plan 04 confirms empirically.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests for the Cal.com webhook handler</name>
  <files>zoho-middleware/__tests__/calcom-webhook.test.js</files>
  <read_first>
    - zoho-middleware/routes/webhooks.js (terminal handler structure, mock approach)
    - zoho-middleware/__tests__/bookings.test.js (express/cache/logger jest.mock pattern + handler extraction)
    - .planning/phases/25-calcom-booking-migration/25-RESEARCH.md (§6 Webhooks, §Validation Architecture BK-06)
  </read_first>
  <behavior>
    - Invalid signature: with calcom.verifyWebhook mocked to return false, POST /api/webhooks/calcom responds 401 and does NOT process the event.
    - Valid signature: verifyWebhook true -> responds 200 { received: true } and logs via eventLog.
    - Cancellation: triggerEvent BOOKING_CANCELLED with a payload start date -> cache.del is called for the slots key (and availability key) of that date.
    - Created/Rescheduled: logged, no destructive side effect (idempotent).
    - Cancellation with unparseable start: BOOKING_CANCELLED whose payload has no recognizable start field -> responds 200, logs a warning, and does NOT throw (cache.del not called).
  </behavior>
  <action>
    Create calcom-webhook.test.js mocking '../lib/calcom' (verifyWebhook jest.fn), '../lib/cache' (get/set/del jest.fn), '../lib/logger', '../lib/eventLog', and express (mirror bookings.test.js). Extract the POST /api/webhooks/calcom handler from the router mock. Assert the four behaviors. Build req with headers['x-cal-signature-256'] and req.rawBody set to a Buffer; mockRes returning status/json chainable. Per BOOK-05.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- calcom-webhook.test.js 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert: 401 on bad sig, 200 + eventLog on good sig, cache.del on BOOKING_CANCELLED.
    - Tests reference calcom.verifyWebhook mock and req.rawBody.
  </acceptance_criteria>
  <done>Red tests pinning the webhook contract exist; go green when Task 2 implements the handler.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /api/webhooks/calcom handler to routes/webhooks.js</name>
  <files>zoho-middleware/routes/webhooks.js</files>
  <read_first>
    - zoho-middleware/routes/webhooks.js (terminal handler: dual-path registration, rawBody, 401-on-bad-sig, 200-fast, async processing, eventLog)
    - zoho-middleware/lib/calcom.js (verifyWebhook signature)
    - zoho-middleware/lib/constants.js (C.CACHE_KEYS.SLOTS_PREFIX, AVAILABILITY_PREFIX)
    - .planning/phases/25-calcom-booking-migration/25-RESEARCH.md (§6 Webhooks, Pitfall 4, Security Domain table)
  </read_first>
  <action>
    Add to routes/webhooks.js a require for '../lib/calcom'. Register `router.post(['/api/webhooks/calcom', '/webhooks/calcom'], fn)` (dual path so the server.js:239 /webhooks/ exemption applies). In the handler: read `var signature = req.headers['x-cal-signature-256'] || ''` and `var rawBody = req.rawBody ? req.rawBody.toString() : ''`; if `!calcom.verifyWebhook(rawBody, signature)` log.warn + `return res.status(401).json({ error: 'Invalid signature' })`. On success: `res.status(200).json({ received: true })` immediately, then process async. Parse the trigger from req.body (triggerEvent). Always `eventLog.logEvent('calcom.webhook_received', { triggerEvent, ... })`. On BOOKING_CANCELLED: derive the affected date (YYYY-MM-DD) from the booking start in the payload (field path unconfirmed per RESEARCH A2 — try payload.startTime, then payload.booking?.start, then payload.start), then cache.del(C.CACHE_KEYS.SLOTS_PREFIX + date) and cache.del(C.CACHE_KEYS.AVAILABILITY_PREFIX + date.substring(0,7)) so freed slots reappear (mirror the cache-invalidation in routes/bookings.js POST). If none of those fields yield a parseable date, log.warn and SKIP cache invalidation rather than throwing (a quiet no-op is safe; Plan 04's staging delivery confirms the real field path and that the cache actually cleared). BOOKING_CREATED / BOOKING_RESCHEDULED: log only (idempotent, no destructive side effects per Security Domain replay mitigation). Do not modify server.js — confirm the path-prefix exemption already covers this route. ES5 style. Per BOOK-02, BOOK-05.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- calcom-webhook.test.js && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- calcom-webhook.test.js` passes.
    - `cd zoho-middleware && npm test` passes (full suite).
    - `npm run lint` clean.
    - `grep -n "x-cal-signature-256" zoho-middleware/routes/webhooks.js` matches.
    - `grep -n "/webhooks/calcom" zoho-middleware/routes/webhooks.js` shows the dual-path registration.
    - The existing terminal webhook handler is unchanged.
  </acceptance_criteria>
  <done>The Cal.com webhook endpoint verifies signatures, responds 200 fast, logs events, and frees cached slots on cancellation; suite + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cal.com -> /api/webhooks/calcom | Untrusted inbound POST; only the HMAC signature authenticates it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07 | Spoofing/Tampering | Forged cancellation webhook | mitigate | verifyWebhook HMAC-SHA256 hex over req.rawBody; 401 on mismatch (ASVS V13). |
| T-25-08 | Elevation of privilege | Guard bypass of webhook | mitigate | Dual-path registration under /webhooks/ uses server.js:239 key-guard exemption; referer guard tolerates no-Referer (server.js:73). Endpoint does nothing destructive beyond cache.del. |
| T-25-09 | Tampering | Webhook replay | accept | Side effects are idempotent (log + cache.del only); replay causes no harm. |
| T-25-10 | Denial of service | Slow webhook processing | mitigate | Respond 200 before async processing (mirror terminal pattern). |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` green (full middleware suite).
- `cd zoho-middleware && npm run lint` clean.
- grep confirms x-cal-signature-256 + dual-path /webhooks/calcom registration.
- server.js NOT modified (path-prefix exemption already covers the route).
</verification>

<success_criteria>
- Signature-verified Cal.com webhook endpoint exists, key-guard-exempt.
- 200-fast + async log; cancellation invalidates the date's cache.
- Tests + lint green; terminal webhook untouched.
</success_criteria>

<output>
Create `.planning/phases/25-calcom-booking-migration/25-03-SUMMARY.md` when done. Note that raw-body-vs-stringify HMAC form is confirmed empirically in Plan 04's staging delivery (Assumption A2).
</output>
