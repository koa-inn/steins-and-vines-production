---
phase: 19-hop-inventory-catalog
plan: 03
type: execute
wave: 2
depends_on: [19-01, 19-02]
files_modified:
  - package.json
  - js/modules/15-hops.min.js
  - css/hops.min.css
autonomous: false
requirements: [D-16, D-17]

must_haves:
  truths:
    - "npm run build completes successfully including hops assets"
    - "npm test passes (all frontend tests including 15-hops.test.js)"
    - "npm run lint passes without errors"
    - "hops.html loads in browser and shows hop cards with radar charts"
    - "Navigation links to hops page work from all public pages"
  artifacts:
    - path: "js/modules/15-hops.min.js"
      provides: "Minified standalone hops module"
    - path: "css/hops.min.css"
      provides: "Minified hops stylesheet"
    - path: "package.json"
      provides: "Updated build scripts with hops assets"
      contains: "hops.min.css"
  key_links:
    - from: "package.json"
      to: "css/hops.css"
      via: "minify:css script"
      pattern: "hops\\.css"
    - from: "package.json"
      to: "js/modules/15-hops.js"
      via: "minify:js script"
      pattern: "15-hops\\.js"
---

<objective>
Integrate the hops page into the build pipeline, run full test suite, lint, and build to produce deployable artifacts. Then human verification of the live hops page.

Purpose: Ensures all Phase 19 work is production-ready with minified assets, cache-busted URLs, passing tests, and clean lint. The human verification confirms the visual/interactive experience matches the design contract.

Output: Updated package.json build scripts, minified assets (15-hops.min.js, hops.min.css), verified working page
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-hop-inventory-catalog/19-CONTEXT.md
@.planning/phases/19-hop-inventory-catalog/19-01-SUMMARY.md
@.planning/phases/19-hop-inventory-catalog/19-02-SUMMARY.md

<interfaces>
From package.json (current build scripts):
```
"minify:css": "cleancss -o css/styles.min.css css/styles.css && ... && cleancss -o css/labels.min.css css/labels.css"
"minify:js": "npm run concat:js && terser js/main.js -o js/main.min.js -c -m && ... && terser js/modules/14-labels.js -o js/modules/14-labels.min.js -c -m"
"stamp:pages": "node -e \"...['products.html','ingredients.html','reservation.html','about.html','contact.html','products/ferment-in-store.html','products/ingredients-supplies.html','custom-labels.html']...\""
"concat:js": "cat js/lib/constants.js js/modules/01-config.js ... js/modules/13-init.js > js/main.js"
```

Key: 15-hops.js is NOT added to concat:js. It IS added to minify:js and stamp:pages.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build pipeline and verify full test/lint/build</name>
  <files>package.json</files>
  <read_first>
    - package.json (full scripts section -- lines with minify:css, minify:js, stamp:pages, concat:js)
    - js/modules/15-hops.js (verify it exists from Plan 02)
    - css/hops.css (verify it exists from Plan 01)
  </read_first>
  <action>
**Update `package.json` scripts** with three changes:

1. **`minify:css`** — Append `&& cleancss -o css/hops.min.css css/hops.css` to the end of the existing command chain.

2. **`minify:js`** — Append `&& terser js/modules/15-hops.js -o js/modules/15-hops.min.js -c -m` to the end of the existing command chain.

3. **`stamp:pages`** — Add `'hops.html'` to the filenames array. Also add two new replacement patterns for hops-specific assets:
   - `f=f.replace(/hops\\.min\\.css\\?v=[^\\\"]+/g,'hops.min.css?v='+v);`
   - `f=f.replace(/15-hops\\.min\\.js\\?v=[^\\\"]+/g,'15-hops.min.js?v='+v);`

**Do NOT modify `concat:js`** — 15-hops.js is a standalone module, not part of the main.js concatenation.

After updating package.json, run all verification commands:

```bash
npm test                    # All frontend tests including 15-hops.test.js
npm run lint                # ESLint clean
npm run build               # Full build: stamp + minify CSS/JS + concat
```

Verify build produced:
- `css/hops.min.css` exists and is non-empty
- `js/modules/15-hops.min.js` exists and is non-empty
- `hops.html` has cache-busted version strings on `hops.min.css?v=` and `15-hops.min.js?v=`

Also run middleware tests to confirm no regressions:
```bash
cd zoho-middleware && npm test
```
  </action>
  <verify>
    <automated>cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm run build 2>&1 | tail -5 && test -s css/hops.min.css && test -s js/modules/15-hops.min.js && echo "BUILD PASS" || echo "BUILD FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - package.json minify:css contains `cleancss -o css/hops.min.css css/hops.css`
    - package.json minify:js contains `terser js/modules/15-hops.js -o js/modules/15-hops.min.js -c -m`
    - package.json stamp:pages array includes `hops.html`
    - package.json stamp:pages contains `hops.min.css` replacement pattern
    - package.json stamp:pages contains `15-hops.min.js` replacement pattern
    - package.json concat:js does NOT contain `15-hops`
    - npm test exits 0 with all tests passing
    - npm run lint exits 0 with no errors
    - npm run build exits 0
    - css/hops.min.css exists and has size > 0
    - js/modules/15-hops.min.js exists and has size > 0
    - hops.html contains a cache-busted version string on hops.min.css
    - hops.html contains a cache-busted version string on 15-hops.min.js
  </acceptance_criteria>
  <done>Build pipeline includes hops assets, all tests pass, lint is clean, minified artifacts exist</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete hops catalog page with hop product cards, inline SVG radar charts showing sensory profiles, accordion expand/collapse panels, size variant toggle, flavor profile filters, search, sort, and Add to Cart integration with the ingredients cart. Navigation links on all public pages.</what-built>
  <how-to-verify>
    1. Open hops.html in browser (staging URL or local file)
    2. Verify the page loads with a green hero section showing "Hops" heading
    3. Check the Products dropdown nav -- should show 4 items: Ferment in Store, Ingredients and Supplies, Hops, Custom Labels
    4. If hop products exist in middleware/snapshot data:
       a. Verify hop cards appear in a grid layout
       b. Each card should show: hop name, alpha acid %, 2-3 flavor tags as green pills
       c. Click "Hop Details" on a card -- should expand with smooth animation
       d. Expanded panel should show: radar chart (if sensory data exists) OR "Sensory data coming soon" placeholder, origin, notes, size toggle (if multiple sizes), price, and Add to Cart button
       e. If two size variants exist: click between sizes and verify price updates
       f. Click Add to Cart -- verify item appears in ingredients cart sidebar
    5. Test the flavor filter buttons -- should filter hop cards
    6. Test the search box -- should filter by hop name
    7. Test the sort dropdown -- verify name and price sorting work
    8. Check responsive: resize browser to mobile width -- should stack to single column, radar chart should shrink
    9. Navigate to another page (e.g. index.html) and verify the Hops link in Products dropdown works
    10. Check that hops.html is included in sitemap.xml
  </how-to-verify>
  <resume-signal>Type "approved" if the hops page looks and works correctly, or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline | package.json scripts process source files into minified artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering | Build artifacts | accept | Minified JS/CSS are generated by trusted local tools (terser, clean-css-cli) from source files. No external inputs during build. |
| T-19-10 | Denial of Service | stamp:pages | accept | Stamp script processes a fixed list of local files. No network access, no amplification risk. |
</threat_model>

<verification>
- npm test exits 0
- npm run lint exits 0
- npm run build exits 0
- css/hops.min.css exists (non-empty)
- js/modules/15-hops.min.js exists (non-empty)
- Human confirms page loads and functions correctly
</verification>

<success_criteria>
Build pipeline produces valid minified artifacts for the hops page. All tests pass, lint is clean. Human confirms the hops catalog page renders correctly with product cards, radar charts, accordion expand, filters, search, sort, and cart integration working end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/19-hop-inventory-catalog/19-03-SUMMARY.md`
</output>
