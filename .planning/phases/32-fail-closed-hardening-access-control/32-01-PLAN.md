---
phase: 32-fail-closed-hardening-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/checkout-helpers.js
  - zoho-middleware/routes/checkout.js
  - zoho-middleware/__tests__/checkout-route.test.js
autonomous: true
requirements: [HARDEN-01, HARDEN-03]
must_haves:
  truths:
    - "In production, POST /api/checkout with RECAPTCHA_SECRET_KEY unset returns 4xx before the charge step"
    - "In production, POST /api/checkout with a missing reCAPTCHA token returns 4xx before the charge step"
    - "In production, a reCAPTCHA network error/timeout returns 4xx before the charge step"
    - "In dev (NODE_ENV unset), reCAPTCHA still fails open (graceful fallback preserved)"
    - "A second POST /api/checkout with the same transactionId when Redis is down returns 409 — no duplicate Zoho order"
  artifacts:
    - path: "zoho-middleware/lib/checkout-helpers.js"
      provides: "isProd-gated verifyRecaptcha: rejects on unset key and on timeout/network error in prod"
      contains: "process.env.NODE_ENV === 'production'"
    - path: "zoho-middleware/routes/checkout.js"
      provides: "checkTransactionIdAndProceed Redis-down catch returns 409; route reCAPTCHA catch rejects in prod"
      contains: "status(409)"
  key_links:
    - from: "routes/checkout.js (reCAPTCHA gate)"
      to: "lib/checkout-helpers.js#verifyRecaptcha"
      via: "await verifyRecaptcha(rcToken) -> {success:false} triggers res.status(400) before proceed()"
      pattern: "verifyRecaptcha"
    - from: "routes/checkout.js#checkTransactionIdAndProceed catch"
      to: "res.status(409)"
      via: "Redis-down catch returns 409 instead of runCheckout()"
      pattern: "status\\(409\\)"
---

<objective>
Make the money path's reCAPTCHA verification and transactionId replay guard fail CLOSED in production. Today both fail OPEN: `verifyRecaptcha()` returns `{success:true}` when the key is unset and on timeout, and `checkTransactionIdAndProceed()` calls `runCheckout()` in its Redis-down catch.

Purpose: An unauthenticated checkout attempt (no/invalid reCAPTCHA) or a Redis outage must not let a charge proceed or a duplicate Zoho order be created. Dev/CI (NODE_ENV unset) must stay fail-open so local development is unaffected (D-01).

Output: HARDEN-01 (reCAPTCHA fail-closed) and HARDEN-03 (replay-guard 409) closed, with the Phase 31 test.todo markers converted to real prod-path assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-fail-closed-hardening-access-control/32-CONTEXT.md
@.planning/phases/32-fail-closed-hardening-access-control/32-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- The isProd gate pattern (lib/logger.js:11) — reuse VERBATIM -->
var isProd = process.env.NODE_ENV === 'production';

<!-- verifyRecaptcha current fail-open sites (checkout-helpers.js) -->
<!-- ~line 47: if (!secret) return Promise.resolve({ success: true, score: 1.0 }); -->
<!-- ~line 75: return withTimeout(verifyPromise, 5000).catch(fn -> { success: true, score: 1.0 }); -->

<!-- checkout.js reCAPTCHA gate consumes the result (~line 146): -->
<!-- if (!captcha.success || captcha.score < 0.5) { ...void...; return res.status(400)...; } -->
<!-- route-level catch (~line 170) currently: log.warn + return proceed(); -->

<!-- checkout.js replay guard (~line 205-222), Redis-down catch currently returns runCheckout() -->
<!-- the 409 body already exists at the replay-hit branch: res.status(409).json({ error: 'Payment already processed' }) -->

<!-- Test harness facts (checkout-route.test.js): API_SECRET_KEY set before require('../server'); -->
<!-- cache.isConnected=false + cache.getClient->null simulate Redis-down (lines ~59-62) -->
<!-- NODE_ENV toggling pattern (logger.test.js:8,69,76): delete in beforeEach, set 'production' in prod describe, delete in afterEach -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reCAPTCHA fail-closed in production (HARDEN-01)</name>
  <read_first>
    - zoho-middleware/lib/checkout-helpers.js (the verifyRecaptcha function, ~lines 40-80)
    - zoho-middleware/routes/checkout.js (the reCAPTCHA gate ~lines 145-172, including the route-level catch at ~170)
    - zoho-middleware/__tests__/checkout-route.test.js (existing harness + the HARDEN-01 test.todo at ~line 327)
    - zoho-middleware/lib/logger.js (line 11 — the canonical isProd gate to reuse)
    - zoho-middleware/__tests__/logger.test.js (lines 8, 69, 76 — NODE_ENV toggling pattern)
    - .planning/phases/32-fail-closed-hardening-access-control/32-CONTEXT.md (D-01, D-03)
    - .planning/phases/32-fail-closed-hardening-access-control/32-PATTERNS.md (HARDEN-01 section)
  </read_first>
  <behavior>
    - Test (prod, unset key): NODE_ENV='production', RECAPTCHA_SECRET_KEY unset -> verifyRecaptcha returns success:false
    - Test (dev, unset key): NODE_ENV unset, RECAPTCHA_SECRET_KEY unset -> verifyRecaptcha returns success:true (fail-open preserved)
    - Test (prod, missing token): NODE_ENV='production', secret set, token '' -> success:false (already true today; assert it)
    - Test (prod, route, no/invalid reCAPTCHA): POST /api/checkout returns 400 and never reaches the charge step (processCheckout/zoho not called)
    - Test (dev, route, network error): route still proceeds (fail-open preserved)
  </behavior>
  <action>
    In `lib/checkout-helpers.js#verifyRecaptcha`: introduce `var isProd = process.env.NODE_ENV === 'production';` (reuse the logger.js:11 form). Flip BOTH fail-open returns per D-03: (a) the unset-key branch (~line 47) returns `{ success: false, score: 0 }` when `isProd`, else keeps the current `{ success: true, score: 1.0 }` dev fallback (keep the log.warn for dev); (b) the `withTimeout(...).catch` branch (~line 75) returns `{ success: false, score: 0 }` when `isProd`, else keeps the current warn+`{ success: true, score: 1.0 }`. Do NOT add any score-threshold gate (out of scope per CONTEXT domain). In `routes/checkout.js` the route-level catch (~line 170) currently does `log.warn(...); return proceed();` — flip it to `return res.status(400).json({ error: 'Request could not be verified. Please try again.' });` when `isProd`, else keep `return proceed();` (defense in depth per D-03/specifics). The existing `res.status(400)` at ~line 164 is the rejection-before-charge that satisfies the 4xx criterion (the charge happens later inside processCheckout). Add the prod-path tests to checkout-route.test.js, converting the HARDEN-01 test.todo (~line 327) to a real assertion; use the logger.test.js NODE_ENV toggle pattern (set in a prod describe, delete in afterEach) so other tests stay fail-open.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- checkout-route.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- checkout-route.test.js` exits 0 with no `test.todo` remaining for HARDEN-01
    - Source `lib/checkout-helpers.js` contains `process.env.NODE_ENV === 'production'` and both unset-key and timeout branches return `success:false` only when prod
    - In a test with NODE_ENV='production' and reCAPTCHA unset/invalid, POST /api/checkout returns HTTP 400 and the Zoho order-create mock is NOT called
    - In a test with NODE_ENV unset, reCAPTCHA unset still allows the request through (fail-open preserved)
  </acceptance_criteria>
  <done>reCAPTCHA rejects (4xx, before charge) in prod for unset-key, missing-token, and network-error cases; dev fail-open preserved; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay-guard returns 409 when Redis is down (HARDEN-03)</name>
  <read_first>
    - zoho-middleware/routes/checkout.js (checkTransactionIdAndProceed ~lines 205-222 — the Redis-down catch)
    - zoho-middleware/__tests__/checkout-route.test.js (Redis-down mock setup ~lines 59-62; HARDEN-03 test.todo ~line 328)
    - .planning/phases/32-fail-closed-hardening-access-control/32-CONTEXT.md (D-05)
    - .planning/phases/32-fail-closed-hardening-access-control/32-PATTERNS.md (HARDEN-03 section, incl. the two sibling fail-opens to NOT touch)
  </read_first>
  <behavior>
    - Test (Redis down, transactionId present): POST /api/checkout returns 409 { error: 'Payment already processed' } and no Zoho order is created
    - Test (Redis up, fresh transactionId): proceeds normally (unchanged happy path)
    - Test (Redis up, replayed transactionId): returns 409 (existing behavior, assert still holds)
  </behavior>
  <action>
    In `routes/checkout.js#checkTransactionIdAndProceed` (~line 205): change the Redis-down `catch (e)` (currently `// Redis unavailable — allow through (fail open)` then `return runCheckout();` at ~line 220) to `return res.status(409).json({ error: 'Payment already processed' });` per D-05 — reuse the exact 409 body already present at the replay-hit branch (~line 215). This is fail-closed: a Redis outage cannot let a duplicate transactionId create a second Zoho order. Do NOT modify the idempotency-key catch at ~line 138 (`proceed()` outer catch) — per D-05 and PATTERNS the success criterion is keyed on transactionId, not idempotency_key; leave it and note it in SUMMARY as a documented follow-up risk. Do NOT modify the promo lock (~line 353/366) — that is discount-burn, out of scope. Add a Redis-down 409 test by reusing the harness's `cache.isConnected=false` / `cache.getClient->null` setup (~lines 59-62) with a body carrying a transactionId; convert the HARDEN-03 test.todo (~line 328) to a real assertion.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- checkout-route.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test -- checkout-route.test.js` exits 0 with no `test.todo` remaining for HARDEN-03
    - Source `routes/checkout.js` Redis-down catch in `checkTransactionIdAndProceed` returns `res.status(409)` (no `runCheckout()` in that catch)
    - In a Redis-down test (cache.isConnected=false), a checkout with a transactionId returns HTTP 409 and the Zoho order-create mock is NOT called
    - The idempotency-key catch (~line 138) and promo lock (~line 353/366) are UNCHANGED
  </acceptance_criteria>
  <done>transactionId replay guard returns 409 when Redis is unavailable; no duplicate Zoho order; sibling fail-opens left untouched per scope; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Full middleware suite + lint gate</name>
  <read_first>
    - CLAUDE.md (rules 1, 2, 7 — run full suites and lint after touching shared lib/*.js)
    - zoho-middleware/jest.config.js (per-file coverage floors for routes/checkout.js must not regress)
  </read_first>
  <action>
    After Tasks 1-2, run the FULL middleware test suite and lint (CLAUDE.md rule 7: `lib/checkout-helpers.js` is shared). Confirm no characterization test for the old fail-open behavior is left red unintentionally; any intentionally-flipped assertion must be updated deliberately (this phase changes behavior — CLAUDE.md rule 10 caveat applies). Confirm the `routes/checkout.js` per-file coverage floor in jest.config.js still passes (hardening + new tests should raise, not lower it).
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npm test` exits 0 (full suite green)
    - `cd zoho-middleware && npm run lint` exits 0
    - `cd zoho-middleware && npm run test:coverage` does not drop the `./routes/checkout.js` per-file lines floor in jest.config.js
  </acceptance_criteria>
  <done>Full middleware suite + lint green; coverage floors held.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public storefront -> POST /api/checkout | untrusted customer request crosses into the charge/order path |
| middleware -> Redis (cache) | replay-guard state store; availability is not guaranteed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Spoofing | POST /api/checkout reCAPTCHA gate | mitigate | In prod, verifyRecaptcha returns success:false on unset key AND on network/timeout; route returns 400 before charge (HARDEN-01) |
| T-32-02 | Elevation/Repudiation | Bot/automated checkout when reCAPTCHA misconfigured | mitigate | Defense in depth: lib-level reject + route-level catch reject in prod; dev stays fail-open by NODE_ENV gate (D-01) |
| T-32-03 | Tampering (duplicate order) | transactionId replay guard, Redis-down catch | mitigate | catch returns 409 instead of runCheckout() — a Redis outage cannot create a duplicate Zoho order (HARDEN-03) |
| T-32-04 | DoS (false positive) | prod fail-closed rejecting legit customers if Redis flaps | accept | 409 on a transactionId only occurs for a duplicate/replayed txn or genuine Redis outage; correctness (no double charge) outweighs the rare retry; idempotency-key path still allows fresh checkouts |
| T-32-RISK | Tampering (duplicate order, residual) | idempotency-key catch (~checkout.js:138) | accept | Out of named scope (criterion keyed on transactionId); documented as a follow-up risk in SUMMARY, not closed here |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` green; HARDEN-01 and HARDEN-03 test.todo markers converted to passing assertions.
- Prod-path tests prove 4xx (reCAPTCHA) and 409 (Redis-down replay) before any charge/Zoho call.
- Dev fail-open paths still pass (NODE_ENV unset).
</verification>

<success_criteria>
- Roadmap criterion 1 met: prod POST /api/checkout without valid reCAPTCHA (or unset key) returns 4xx before the charge step.
- Roadmap criterion 3 met: second POST /api/checkout with same transactionId when Redis is unavailable returns 409; no duplicate Zoho order.
</success_criteria>

<output>
Create `.planning/phases/32-fail-closed-hardening-access-control/32-01-SUMMARY.md` when done. Note the idempotency-key catch (~checkout.js:138) as an unclosed follow-up risk.
</output>
