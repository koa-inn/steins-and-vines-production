---
phase: 68-kiosk-terminal-push-latency-cancel-double-charge-safety
plan: 03
type: execute
wave: 3
depends_on: ["68-01", "68-02"]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "On the live kiosk, a real (or refundable-test) sale produces per-stage timing telemetry that identifies the dominant latency stage (catalog rebuild vs gift-card lookup vs terminal push) — the data the deferred latency-reduction phase needs"
    - "On the live kiosk, cancelling a sale during/after the terminal push does NOT leave an orphan charge: a card tapped after cancel is voided (or not charged), never money-taken-with-no-invoice"
    - "The deploy order held: middleware (68-01/68-02) live before the frontend so old cached kiosk JS is unaffected"
  artifacts: []
  key_links:
    - from: "live kiosk sale + cancel"
      to: "Railway telemetry + Zoho/Helcim state"
      via: "owner-observed test on the real device"
      pattern: "no orphan charge; stage timings captured"
---

<objective>
Live-verify phase 68 on the real kiosk after 68-01 (instrumentation) and 68-02 (cancel safety) are deployed. Two proofs: (a) the telemetry captures a real slow-case stage breakdown so the follow-up latency phase has data, and (b) a cancel during a slow push cannot orphan a charge. Separate wave-3 checkpoint so it never blocks the autonomous code waves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-kiosk-terminal-push-latency-cancel-double-charge-safety-inst/68-CONTEXT.md
@.planning/phases/68-kiosk-terminal-push-latency-cancel-double-charge-safety-inst/68-01-PLAN.md
@.planning/phases/68-kiosk-terminal-push-latency-cancel-double-charge-safety-inst/68-02-PLAN.md

<deploy_ordering>
Middleware (68-01/68-02) rides a prod Railway deploy; no separate staging middleware (v4.2 decision). Deploy middleware, confirm the new /api/kiosk/telemetry route + cancel mark-and-void are live, then push the frontend and hard-refresh the kiosk iPad to pick up the new kiosk-core.min.js.
</deploy_ordering>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>68-01 instrumented /api/kiosk/sale with per-stage timing (kiosk.sale_stage_timing) + a client push-latency beacon (kiosk.terminal_push_latency via /api/kiosk/telemetry). 68-02 made cancel safe: /api/pos/cancel marks the ref cancelled, an approved-after-cancel charge is voided through moneyPath.voidWithTimeout, the client sends the ref on cancel and no longer drops an approved charge, and the 'tap card' prompt waits for the 202.</what-built>
  <how-to-verify>
    1. Confirm deploy order: middleware live (new /api/kiosk/telemetry responds; Railway logs show kiosk.sale_stage_timing on a sale) BEFORE the frontend reaches the kiosk; then hard-refresh the iPad (kiosk-core.min.js?v= changed).
    2. Latency capture: ring a normal card sale; in the Railway logs read the kiosk.sale_stage_timing events for that ref and note which stage dominates (expect catalog `cache: 'rebuild'` on a cold cache, or the gift-card lookup if a GC was applied). Capture the client kiosk.terminal_push_latency value. Record the numbers in the summary — this is the deferred phase's input.
    3. Cancel/orphan proof (the money-safety check): start a sale, and on the terminal-cancel step cancel from the kiosk; then complete the card tap on the reader anyway (simulating the reported "reader picks up when I cancel"). Confirm: NO orphan — either no charge occurred, or a charge that occurred was voided (check Helcim for a void/reversal on that transaction and confirm no unmatched invoice). If safe to do with a refundable test card, this is the definitive proof; otherwise verify via the logs that an approved-after-cancel routed through voidWithTimeout.
    4. Refund/reverse any real test charges and reverse any test invoices per the existing kiosk test-order cleanup.
  </how-to-verify>
  <resume-signal>Type "approved" with: the dominant latency stage + the stage timings observed, and confirmation that a cancel-then-tap did not orphan a charge (voided or not charged). Describe any mismatch instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live money path | Real terminal charge — verification uses refundable test charges cleaned up afterward, or log-based proof if a live charge is not desirable |
| deploy pipeline | Middleware-before-frontend ordering must hold (backward-compatible telemetry field + cancel flag) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-68-03-1 | Repudiation / orphan | live cancel-then-tap | mitigate | Step 3 directly proves no orphan (void or no-charge) on the real device |
| T-68-03-2 | Tampering / ordering | deploy sequence | mitigate | Step 1 confirms middleware live before frontend; new fields are additive/back-compat |
| T-68-SC | Supply chain | none | accept | Verification-only plan, no installs |
</threat_model>

<verification>
- Stage-timing + client latency captured for a real sale; dominant stage identified.
- Cancel-then-tap on the live device does not orphan a charge (voided or not charged).
- Deploy order confirmed; test charges refunded/reversed.
</verification>

<success_criteria>
- Owner-confirmed: the sale pipeline's real bottleneck is measured (input for the deferred latency phase), and a cancel can no longer produce an orphan charge on the live kiosk.
</success_criteria>

<output>
Create 68-03-SUMMARY.md recording the observed stage timings + dominant stage, the cancel/orphan proof result (voided or no-charge, with the Helcim/txn evidence), deploy-order confirmation, and test-charge cleanup. Flag the dominant-stage finding as the seed for the deferred latency-reduction phase.
</output>
