---
phase: 22-category-subpages-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/catalog-subpage.css
  - package.json
autonomous: false
requirements: [NAV-01, NAV-03]

must_haves:
  truths:
    - "All distinct cf_subcategory and cf_type values from live Zoho data are documented and confirmed"
    - "Sub-nav pill styles render as horizontal row of rounded pills with green border"
    - "Active pill gets filled green background with cream text via body[data-page] CSS selector"
    - "Sub-nav sticks below the site header using var(--header-height, 80px) with z-index 190"
    - "Search icon placeholder renders at the right end of sub-nav, visually disabled"
    - "Nav dropdown divider renders as a thin horizontal line separating groups"
    - "Build pipeline stamps cache-bust tokens on all 6 new/moved pages"
  artifacts:
    - path: "css/catalog-subpage.css"
      provides: "Sub-nav styles, active state selectors, dropdown divider"
      contains: ".ingredient-subnav"
    - path: "package.json"
      provides: "Updated stamp:pages list with 6 new page paths"
      contains: "products/grains.html"
  key_links:
    - from: "css/catalog-subpage.css"
      to: "body[data-page]"
      via: "CSS attribute selector for active pill state"
      pattern: "body\\[data-page=\"grains\"\\]"
    - from: "package.json"
      to: "products/grains.html"
      via: "stamp:pages file list"
      pattern: "products/grains\\.html"
---

<objective>
Verify Zoho subcategory values against live data, add sub-nav bar CSS styles (pills, sticky positioning, active state, search placeholder) and nav dropdown divider styles to catalog-subpage.css, and update the build pipeline stamp:pages list in package.json.

Purpose: The subcategory verification resolves RESEARCH.md assumptions A1-A4 (LOW confidence) before Plan 02 hardcodes SUBPAGE_CONFIG values. The CSS and build pipeline changes establish the visual foundation and infrastructure that Plans 02 and 03 depend on.

Output: Confirmed subcategory values documented in executor output; updated css/catalog-subpage.css with ~90 new lines of sub-nav CSS; updated package.json stamp:pages array.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-category-subpages-navigation/22-CONTEXT.md
@.planning/phases/22-category-subpages-navigation/22-RESEARCH.md
@.planning/phases/22-category-subpages-navigation/22-UI-SPEC.md
@.planning/phases/22-category-subpages-navigation/22-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify Zoho cf_subcategory values against live catalog</name>
  <decision>Confirm that the assumed cf_subcategory values in RESEARCH.md (A1-A4) match the actual Zoho data before Plan 02 hardcodes them into SUBPAGE_CONFIG.</decision>
  <context>
    RESEARCH.md flags assumptions A1-A4 with LOW confidence:
    - A1: Yeast Nutrient is the exact cf_subcategory value for yeast nutrient items
    - A2: Packaging items use Bottle, Bag; closures may use a different value
    - A3: Equipment uncategorized items fall back to cf_type = Equipment
    - A4: Additives items use Additive, Flavoring, Fruit, Oak

    The verify-subcategories.js script (in zoho-middleware/scripts/) calls /api/ingredients and prints ALL distinct cf_subcategory and cf_type values with item counts.

    PREREQUISITES (human must do these before running the script):
    1. Start local middleware: cd zoho-middleware && node server.js
    2. Authenticate with Zoho: visit http://localhost:3001/auth/zoho in browser
    3. Wait ~30s for cache to warm
  </context>
  <how-to-verify>
    After middleware is running and authenticated:

    1. Run: node zoho-middleware/scripts/verify-subcategories.js
    2. The script prints a "Breakdown" section listing every distinct subcategory value and its item count.
    3. Compare the output against these assumed SUBPAGE_CONFIG subcategory arrays:

       Grains:    ['Grain', 'Malt Extract']
       Yeast:     ['Yeast', 'Yeast Nutrient']
       Additives: ['Additive', 'Flavoring', 'Fruit', 'Oak']
       Packaging: ['Bottle', 'Bag'] + types: ['Packaging']
       Equipment: ['Fermenter', 'Hose/Tubing'] + types: ['Equipment']

    4. Report back:
       - The FULL list of distinct subcategory values from the script output (copy-paste the Breakdown section)
       - Whether any assumed value does NOT appear in the output
       - Whether any unexpected values appear that should be added to a category
       - Whether coverage is 100% or if items are missing subcategories

    The executor will use these confirmed values to update SUBPAGE_CONFIG arrays in Plan 22-02 if they differ from the assumed values. Document the confirmed values in the SUMMARY for this plan.
  </how-to-verify>
  <resume-signal>Paste the verify-subcategories.js output, or type the confirmed subcategory values</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add sub-nav and dropdown divider CSS to catalog-subpage.css</name>
  <files>css/catalog-subpage.css</files>
  <read_first>
    - css/catalog-subpage.css (current file -- 538 lines, append after the last line)
    - css/styles.css lines 235-242 (header sticky: position sticky, top 0, z-index 200)
    - css/styles.css lines 455-513 (nav-dropdown-menu rules, min-width 180px)
    - .planning/phases/22-category-subpages-navigation/22-UI-SPEC.md (spacing scale, colors, component inventory)
    - .planning/phases/22-category-subpages-navigation/22-PATTERNS.md (CSS code excerpts for sub-nav)
  </read_first>
  <action>
    Append new rules after line 538 of css/catalog-subpage.css. Two sections:

    SECTION 1: Ingredient Sub-nav (per D-01, D-02, D-03, NAV-01, NAV-03)

    .ingredient-subnav: position sticky, top var(--header-height, 80px), z-index 190 (below header 200 per UI-SPEC), background var(--color-cream), border-bottom 1px solid rgba(74,111,75,0.2), padding 0.5rem 0.

    .ingredient-subnav .container: display flex, align-items center, gap 0.5rem.

    .subnav-pills: display flex, align-items center, gap 8px (UI-SPEC corrected from 6px), overflow-x auto, -webkit-overflow-scrolling touch, scrollbar-width none, padding-bottom 2px. Hide webkit scrollbar with ::-webkit-scrollbar display none.

    .subnav-pill: display inline-flex, align-items center, padding 8px 16px (UI-SPEC corrected from 6px 14px), min-height 36px, border-radius 20px, border 1px solid var(--color-green), background transparent, color var(--color-green), font-family var(--font-body), font-size 0.8125rem, font-weight 700, white-space nowrap, text-decoration none, transition background 0.15s color 0.15s.

    .subnav-pill:hover: background rgba(74,111,75,0.08).

    .subnav-pill:focus-visible: outline 2px solid var(--color-green), outline-offset 2px.

    CSS-only active state selectors (per D-04, NAV-03 -- 7 rules for 7 pages):
    body[data-page="ingredients"] .subnav-pill[data-subnav="all"],
    body[data-page="hops"] .subnav-pill[data-subnav="hops"],
    body[data-page="grains"] .subnav-pill[data-subnav="grains"],
    body[data-page="yeast"] .subnav-pill[data-subnav="yeast"],
    body[data-page="additives"] .subnav-pill[data-subnav="additives"],
    body[data-page="packaging"] .subnav-pill[data-subnav="packaging"],
    body[data-page="equipment"] .subnav-pill[data-subnav="equipment"]
    All get: background var(--color-green), color var(--color-cream).

    .subnav-search-btn: flex-shrink 0, margin-left auto, background none, border none, padding 8px, min-height 36px, min-width 36px, cursor not-allowed, color var(--color-muted, #5f5f5f), opacity 0.5, border-radius 50%, display flex, align-items center, justify-content center. Per D-03 the button is disabled until Phase 23.

    SECTION 2: Nav Dropdown Divider (per D-08)

    .nav-dropdown-divider: border-top 1px solid rgba(74,111,75,0.2), margin 4px 0, pointer-events none.

    Override .nav-dropdown-menu min-width to 200px (from existing 180px in styles.css line 470) to accommodate the wider item list.
  </action>
  <verify>
    <automated>grep -c ".ingredient-subnav" css/catalog-subpage.css | xargs test 3 -le && echo "subnav rules present" && grep -c "data-page=" css/catalog-subpage.css | xargs test 7 -le && echo "7 active states" && grep -c ".nav-dropdown-divider" css/catalog-subpage.css | xargs test 1 -le && echo "divider present" && grep "min-width: 200px" css/catalog-subpage.css && echo "dropdown widened"</automated>
  </verify>
  <acceptance_criteria>
    - css/catalog-subpage.css contains .ingredient-subnav with position: sticky and top: var(--header-height, 80px) and z-index: 190
    - css/catalog-subpage.css contains 7 body[data-page="..."] selectors (ingredients, hops, grains, yeast, additives, packaging, equipment) setting background: var(--color-green)
    - css/catalog-subpage.css contains .subnav-pill with padding: 8px 16px and min-height: 36px
    - css/catalog-subpage.css contains .subnav-search-btn with cursor: not-allowed and opacity: 0.5
    - css/catalog-subpage.css contains .nav-dropdown-divider with border-top and pointer-events: none
    - css/catalog-subpage.css contains .nav-dropdown-menu with min-width: 200px
    - npm run build exits 0 (CSS minification succeeds)
  </acceptance_criteria>
  <done>Sub-nav pill styles, sticky positioning, CSS-only active state for all 7 pages, disabled search button style, and dropdown divider style are all present in catalog-subpage.css. Build minifies without error.</done>
</task>

<task type="auto">
  <name>Task 3: Update package.json stamp:pages for new/moved subpages</name>
  <files>package.json</files>
  <read_first>
    - package.json (full file -- focus on line 15, the stamp:pages script)
  </read_first>
  <action>
    Edit the stamp:pages script (line 15) file array.

    Remove: 'hops.html' (file is moving to products/hops.html in Plan 02).

    Add 6 entries: 'products/hops.html', 'products/grains.html', 'products/yeast.html', 'products/additives.html', 'products/packaging.html', 'products/equipment.html'.

    The existing regex replacements for catalog-subpage.min.css and 16-catalog-subpage.min.js already cover any page in the list regardless of path prefix, so no new regexes are needed.

    Final array should be: ['products.html','ingredients.html','reservation.html','about.html','contact.html','products/ferment-in-store.html','products/ingredients-supplies.html','custom-labels.html','test-subpage.html','products/hops.html','products/grains.html','products/yeast.html','products/additives.html','products/packaging.html','products/equipment.html']

    Note: stamp:pages will fail on the next npm run build until Plan 02 creates the actual HTML files, but that is expected -- the build is run AFTER all plans complete. The stamp:pages script reads files that must exist. To avoid a build-time error in this plan's verification, verify the JSON is valid and the array contains all expected entries without actually running stamp:pages.
  </action>
  <verify>
    <automated>node -e "var p=require('./package.json'); var s=p.scripts['stamp:pages']; var has=function(x){return s.indexOf(x)!==-1}; console.log('hops removed:', s.indexOf(\"'hops.html'\") === -1 || s.indexOf(\"'hops.html'\") > s.indexOf('products/hops.html')); console.log('grains:', has('products/grains.html')); console.log('yeast:', has('products/yeast.html')); console.log('additives:', has('products/additives.html')); console.log('packaging:', has('products/packaging.html')); console.log('equipment:', has('products/equipment.html')); console.log('hops moved:', has('products/hops.html'));"</automated>
  </verify>
  <acceptance_criteria>
    - package.json stamp:pages array does NOT contain standalone 'hops.html' (only 'products/hops.html')
    - package.json stamp:pages array contains all 6 new entries: products/hops.html, products/grains.html, products/yeast.html, products/additives.html, products/packaging.html, products/equipment.html
    - package.json is valid JSON (node -e "require('./package.json')" exits 0)
    - npm run lint exits 0 (no regressions)
    - npm test exits 0 (no regressions)
  </acceptance_criteria>
  <done>Build pipeline stamp:pages list includes all 6 new/moved subpage paths and no longer references root-level hops.html. JSON is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none | This plan modifies only CSS and build config -- no user input, no network calls, no auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | css/catalog-subpage.css | accept | CSS is author-controlled static file served from GitHub Pages; no user input enters stylesheet |
| T-22-02 | Tampering | package.json stamp:pages | accept | Build script runs locally; file paths are hardcoded strings, not user input |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- Subcategory values confirmed against live Zoho data (assumptions A1-A4 resolved)
- css/catalog-subpage.css has .ingredient-subnav, 7 active-state selectors, .nav-dropdown-divider
- package.json stamp:pages lists all 15 pages including 6 new entries
- npm test passes (no regressions)
- npm run lint passes
</verification>

<success_criteria>
Zoho subcategory values are verified and documented. Sub-nav CSS styles and dropdown divider style are present in catalog-subpage.css. Build pipeline is configured to stamp all new/moved subpages. All existing tests pass.
</success_criteria>

<output>
Create `.planning/phases/22-category-subpages-navigation/22-01-SUMMARY.md` when done.

IMPORTANT: Include the verified subcategory values in the SUMMARY under a "## Verified Subcategory Values" section. Plan 22-02 will read this SUMMARY to confirm or update SUBPAGE_CONFIG arrays before creating the HTML files.
</output>
