---
phase: 70-kiosk-tender-types
plan: 03
type: execute
wave: 3
depends_on: ["70-01", "70-02"]
files_modified: []
autonomous: false
requirements: [KIOSK-CASH, KIOSK-MOTO]

must_haves:
  truths:
    - "kiosk.html's new CSP was live-verified on a real staging kiosk session (Network + Console) with NO CSP violations — GSI sign-in, catalog load, and gift-card lookup all still work"
    - "A real cash sale on the kiosk booked a Zoho payment_mode:'cash' payment, decremented stock, and made no Helcim charge"
    - "A real HelcimPay phone-order charge on the kiosk booked only after the captured-amount verify, recorded payment_mode:'creditcard' with a card-not-present note, and was refunded"
    - "The deploy order (staging → live-verify → prod) was followed; production was not force-pushed before the CSP was confirmed clean on staging"
  artifacts: []
  key_links: []
---

<objective>
Live-verify Phase 70 on a real kiosk before it reaches production. This is the checkpoint the CONTEXT.md sequencing calls for: a real cash sale, a real HelcimPay phone-order charge (refunded), and — critically — a live browser check that kiosk.html's FIRST CSP does not silently break any existing kiosk feature. Static analysis derived the CSP domain list (RESEARCH Assumption A1); only a live Network/Console trace can confirm it.

Purpose: Catch a CSP-omission regression (the 2026-07-22 Meta-pixel class of failure) and confirm the money paths behave on real hardware/accounts before a prod force-push.
Output: Owner sign-off (or a defect list) gating the production deploy.

Deploy order (NON-NEGOTIABLE): push to staging first (`git push origin main` → staging.steinsandvines.ca); middleware money-path changes deploy to the prod Railway instance (no separate staging middleware — v4.2 decision), so the staging SITE calls prod middleware. Do NOT `git push production main --force` until Task 1's CSP + cash checks and Task 2's MOTO check pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/70-kiosk-tender-types-cash-tender-change-due-gift-card-split-an/70-01-SUMMARY.md
@.planning/phases/70-kiosk-tender-types-cash-tender-change-due-gift-card-split-an/70-02-SUMMARY.md
@docs/TRACKING.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live-verify kiosk.html CSP + a real cash sale on staging</name>
  <files>(none — verification checkpoint; no files modified)</files>
  <action>
    Claude has already deployed all Phase 70 code and passing tests; this checkpoint is human-only live verification on real hardware. Present the what-built + how-to-verify steps to the owner and PAUSE for the resume-signal. Do not force-push to production. If the owner reports CSP violations or booking issues, capture them as gaps for a follow-up gap-closure plan rather than proceeding.
  </action>
  <what-built>
    - A tender-aware kiosk sale pipeline with a Cash tender (books Zoho payment_mode:'cash', no Helcim, change-due UI, gift-card split) — 70-01.
    - kiosk.html's first scoped CSP + the HelcimPay.js script tag — 70-02 Task 3.
    All automated: full middleware + frontend suites green, lint clean, min bundle rebuilt.
  </what-built>
  <how-to-verify>
    1. Deploy: `git push origin main` (staging). Confirm Railway redeployed the middleware (prod instance; the enforce-cname workflow will settle the domain).
    2. Open the standalone kiosk on the iPad / a browser at the staging kiosk URL with DevTools OPEN (Console + Network tabs).
    3. CSP check — with the Console visible, exercise every existing kiosk feature and watch for "Refused to load … violates the following Content Security Policy":
       - Google Sign-In (GSI) completes (accounts.google.com).
       - Product catalog loads / refreshes (middleware svmiddleware-production.up.railway.app).
       - A gift-card lookup succeeds (Apps Script script.google.com).
       - Fonts render (fonts.googleapis.com / fonts.gstatic.com).
       There must be ZERO CSP violation errors. If any domain is blocked, STOP — the CSP domain list needs that domain added (70-02 Task 3) before proceeding.
    4. Real cash sale: ring a small cart, choose the Cash tender, enter an amount tendered greater than the total, confirm the change-due shows (tendered − remainder), and complete the sale.
    5. In Zoho Books, confirm: one invoice created, marked paid, with a customerpayment of payment_mode "cash" for the sale total; stock decremented; a batch created if applicable. Confirm NO Helcim transaction exists for this sale.
    6. (Optional) Cash + gift-card split: apply a gift certificate, confirm cash covers only the remainder and the gift-card leg is recorded.
  </how-to-verify>
  <verify>
    <human-check>CSP produces zero Console violations across GSI, catalog, gift-card lookup, and fonts; the cash sale books payment_mode:'cash' in Zoho with stock decremented and no Helcim charge.</human-check>
  </verify>
  <resume-signal>Type "approved" if CSP is clean and the cash sale booked correctly, or describe the violations / booking issues found.</resume-signal>
  <done>Owner confirms the kiosk CSP is non-breaking on a live session and a real cash sale booked correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-verify a real HelcimPay phone-order (MOTO) charge on staging (refunded)</name>
  <files>(none — verification checkpoint; no files modified)</files>
  <action>
    Human-only live verification of the MOTO money path on real hardware with a real card. Present the what-built + how-to-verify steps and PAUSE for the resume-signal. On approval, the phase is clear for a production deploy; if issues surface, capture them as gaps for follow-up rather than promoting to prod.
  </action>
  <what-built>
    - A "Phone order / card not present" MOTO tender that mounts Helcim's hosted iframe, verifies the captured amount server-side before booking, and books payment_mode:'creditcard' with a card-not-present note — 70-02.
  </what-built>
  <how-to-verify>
    1. On the staging kiosk, ring a small cart (a low real amount you will refund) and choose the "Phone order / card not present" tender.
    2. Confirm Helcim's hosted iframe appears (secure.helcim.app) and that there is NO card-number field rendered by our own page — the PAN entry is Helcim's iframe only.
    3. Key a real card into Helcim's iframe and complete the charge.
    4. Confirm the sale books: in Zoho Books, one invoice paid with a customerpayment payment_mode "creditcard", reference_number = the Helcim transaction id, and a note identifying it as a phone-order / card-not-present payment; stock decremented; batch created if applicable.
    5. In the Helcim dashboard, confirm a single captured transaction for the sale total (no duplicate, no orphan).
    6. Phantom-revenue sanity (design confidence): confirm the booking happened only after the capture — one Helcim charge matches one Zoho payment for the same amount.
    7. REFUND the test charge in the Helcim dashboard and reverse the test invoice/payment in Zoho.
  </how-to-verify>
  <verify>
    <human-check>The MOTO charge is captured in Helcim's iframe (no card field in our page), books payment_mode:'creditcard' only after the server capture-verify, and is refunded/reversed afterward.</human-check>
  </verify>
  <resume-signal>Type "approved" if the MOTO charge booked only after capture and was refunded, or describe issues found. On approval, the phase is clear for a production deploy (`git push production main --force`).</resume-signal>
  <done>Owner confirms the MOTO charge booked only after the captured-amount verify with no card field in our DOM, and was refunded; phase cleared for prod.</done>
</task>

</tasks>

<verification>
- Staging deploy completed; middleware redeployed (prod Railway instance).
- kiosk.html CSP produced zero Console violations across GSI, catalog, gift-card lookup, and fonts.
- A real cash sale booked payment_mode:'cash' with no Helcim charge; stock decremented.
- A real HelcimPay phone-order charge booked payment_mode:'creditcard' (card-not-present note) only after the captured-amount verify; refunded afterward.
- Production was NOT force-pushed until both checkpoints passed.
</verification>

<success_criteria>
- Owner-approved live verification of both tenders on real hardware/accounts.
- CSP confirmed non-breaking on a live kiosk session before prod.
- Phase 70 cleared for production deploy.
</success_criteria>

<output>
Create `.planning/phases/70-kiosk-tender-types-cash-tender-change-due-gift-card-split-an/70-03-SUMMARY.md` when done, recording the CSP verification result, the cash + MOTO booking outcomes (invoice numbers, txn ids), the refund/reversal cleanup, and the production deploy decision.
</output>
