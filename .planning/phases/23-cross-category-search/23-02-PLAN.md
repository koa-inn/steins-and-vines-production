---
phase: 23-cross-category-search
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - products/grains.html
  - products/yeast.html
  - products/additives.html
  - products/packaging.html
  - products/equipment.html
  - products/hops.html
  - products/ingredients-supplies.html
  - package.json
  - tests/frontend/17-search-overlay.test.js
autonomous: true
requirements: [SRCH-01, SRCH-02]

must_haves:
  truths:
    - "Search icon button is enabled and clickable on all 7 ingredient pages"
    - "search-overlay.min.css is loaded on all 7 ingredient pages after catalog-subpage.min.css"
    - "17-search-overlay.min.js is loaded on all 7 ingredient pages with defer attribute"
    - "npm run build produces minified CSS and JS for the overlay and stamps version strings"
    - "groupResultsByCategory and computeResultCap have passing unit tests"
  artifacts:
    - path: "tests/frontend/17-search-overlay.test.js"
      provides: "Unit tests for pure search functions"
      min_lines: 40
    - path: "css/search-overlay.min.css"
      provides: "Minified overlay styles"
    - path: "js/modules/17-search-overlay.min.js"
      provides: "Minified overlay module"
  key_links:
    - from: "products/grains.html"
      to: "js/modules/17-search-overlay.min.js"
      via: "script tag with defer"
      pattern: "17-search-overlay\\.min\\.js"
    - from: "package.json"
      to: "css/search-overlay.css"
      via: "cleancss minification in minify:css script"
      pattern: "search-overlay\\.min\\.css"
    - from: "package.json"
      to: "js/modules/17-search-overlay.js"
      via: "terser minification in minify:js script"
      pattern: "17-search-overlay\\.min\\.js"
---

<objective>
Wire the search overlay into all 7 ingredient pages, update build scripts, write unit tests, and verify end-to-end with `npm run build`.

Purpose: Makes the overlay functional on every ingredient page. After this plan, customers can use the search icon on any subpage to search across all ingredient categories.
Output: 7 updated HTML pages, updated package.json, unit test file, minified build artifacts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-cross-category-search/23-CONTEXT.md
@.planning/phases/23-cross-category-search/23-RESEARCH.md
@.planning/phases/23-cross-category-search/23-PATTERNS.md
@.planning/phases/23-cross-category-search/23-UI-SPEC.md
@.planning/phases/23-cross-category-search/23-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

js/modules/17-search-overlay.js exports:
  function groupResultsByCategory(fuseResults) -- groups by display category, sorts by count desc
  function computeResultCap(categoryCount) -- returns 10/7/5

css/search-overlay.css provides:
  .search-overlay-backdrop, .search-overlay-panel, .search-overlay-input,
  .search-result-row, .search-group-header, .search-view-all, .search-no-results,
  .subnav-search-btn override (cursor:pointer, opacity:1)

<!-- Per-page script loading variations (from RESEARCH.md) -->

| Page                        | Module loaded before overlay     | Insert overlay JS after |
|-----------------------------|----------------------------------|-------------------------|
| grains.html                 | 16-catalog-subpage.min.js        | after 16-               |
| yeast.html                  | 16-catalog-subpage.min.js        | after 16-               |
| additives.html              | 16-catalog-subpage.min.js        | after 16-               |
| packaging.html              | 16-catalog-subpage.min.js        | after 16-               |
| equipment.html              | 16-catalog-subpage.min.js        | after 16-               |
| hops.html                   | 15-hops.min.js (NOT 16-)         | after 15-               |
| ingredients-supplies.html   | main.min.js only                 | after main.min.js       |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire 7 HTML pages and update package.json build scripts</name>
  <files>products/grains.html, products/yeast.html, products/additives.html, products/packaging.html, products/equipment.html, products/hops.html, products/ingredients-supplies.html, package.json</files>
  <read_first>
    - products/grains.html (lines 24-30 for CSS loading block, lines 85-95 for subnav-search-btn, lines 185-195 for script loading block — this is the template for 5 similar pages)
    - products/hops.html (lines 70-80 for CSS loading, lines 85-95 for search button, lines 270-278 for script loading — different JS stack)
    - products/ingredients-supplies.html (lines 70-80 for CSS loading, lines 85-95 for search button, lines 300-310 for script loading — main.min.js only)
    - package.json (lines 6-16 for minify:css, minify:js, stamp:pages scripts — need exact current state)
    - .planning/phases/23-cross-category-search/23-PATTERNS.md (HTML pages section and package.json section)
    - .planning/phases/23-cross-category-search/23-RESEARCH.md (Per-Page Script Loading Variations table, Pitfalls 4-6)
  </read_first>
  <action>
    **HTML changes — apply to all 7 ingredient pages:**

    1. **Enable search button** — On each page, find the `.subnav-search-btn` element. Remove the `disabled` attribute. Remove the `title="Search coming soon"` attribute. Change `aria-label` from "Search ingredients (coming soon)" to "Open ingredient search". The SVG icon inside remains unchanged.

    2. **Add CSS link** — On each page, add a `<link>` tag for `search-overlay.min.css` AFTER the `catalog-subpage.min.css` link (all 7 pages load catalog-subpage.min.css). Use relative path `../css/search-overlay.min.css?v=mpshq381` (the version string will be auto-updated by stamp:pages).

    3. **Add JS script** — On each page, add a `<script defer>` tag for `17-search-overlay.min.js`. Insertion point varies by page:
       - grains.html, yeast.html, additives.html, packaging.html, equipment.html: ADD `<script src="../js/modules/17-search-overlay.min.js?v=mpshq381" defer></script>` AFTER the `16-catalog-subpage.min.js` script tag
       - hops.html: ADD the same script tag AFTER the `15-hops.min.js` script tag (NOT after 16- which does not exist on this page)
       - ingredients-supplies.html: ADD the same script tag AFTER the `main.min.js` script tag (this page loads neither 15- nor 16-)

    **package.json build script updates:**

    4. **minify:css** (line 6) — Append ` && cleancss -o css/search-overlay.min.css css/search-overlay.css` to the end of the existing minify:css command chain.

    5. **minify:js** (line 8) — Append ` && terser js/modules/17-search-overlay.js -o js/modules/17-search-overlay.min.js -c -m` to the end of the existing minify:js command chain.

    6. **stamp:pages** (line 15) — Inside the `.forEach()` body, add two new regex replacement lines after the existing `16-catalog-subpage.min.js` replacement:
       - `f=f.replace(/search-overlay\\.min\\.css\\?v=[^\\\"]+/g,'search-overlay.min.css?v='+v);`
       - `f=f.replace(/17-search-overlay\\.min\\.js\\?v=[^\\\"]+/g,'17-search-overlay.min.js?v='+v);`
       Use the exact same escaping convention as existing entries.

    7. **Run `npm run build`** — This will minify CSS, minify JS, concatenate modules, and stamp all pages with fresh version strings. Verify it completes without errors.
  </action>
  <verify>
    <automated>grep -l "17-search-overlay.min.js" products/grains.html products/yeast.html products/additives.html products/packaging.html products/equipment.html products/hops.html products/ingredients-supplies.html | wc -l | grep -q "^7$" && grep -l "search-overlay.min.css" products/grains.html products/yeast.html products/additives.html products/packaging.html products/equipment.html products/hops.html products/ingredients-supplies.html | wc -l | grep -q "^7$" && test -f css/search-overlay.min.css && test -f js/modules/17-search-overlay.min.js && npm run lint && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 HTML pages contain `<script` tag referencing `17-search-overlay.min.js` with `defer` attribute
    - All 7 HTML pages contain `<link` tag referencing `search-overlay.min.css`
    - Zero HTML pages contain `disabled` on the `.subnav-search-btn` element
    - All 7 HTML pages have `aria-label="Open ingredient search"` on the search button
    - grains/yeast/additives/packaging/equipment: overlay script appears AFTER 16-catalog-subpage.min.js
    - hops.html: overlay script appears AFTER 15-hops.min.js
    - ingredients-supplies.html: overlay script appears AFTER main.min.js
    - package.json minify:css contains `search-overlay.min.css css/search-overlay.css`
    - package.json minify:js contains `17-search-overlay.js -o js/modules/17-search-overlay.min.js`
    - package.json stamp:pages contains `search-overlay\\.min\\.css` regex
    - package.json stamp:pages contains `17-search-overlay\\.min\\.js` regex
    - css/search-overlay.min.css exists (created by npm run build)
    - js/modules/17-search-overlay.min.js exists (created by npm run build)
    - `npm run build` exits with code 0
    - `npm run lint` exits with code 0
  </acceptance_criteria>
  <done>All 7 ingredient pages load the search overlay module and CSS; build pipeline produces minified artifacts and stamps version strings; search button is enabled with correct aria-label on every page; lint passes</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for search overlay pure functions</name>
  <files>tests/frontend/17-search-overlay.test.js</files>
  <read_first>
    - tests/frontend/16-catalog-subpage.test.js (test structure pattern — how existing tests require standalone modules and test pure functions)
    - js/modules/17-search-overlay.js (module.exports block — confirm exported functions)
    - .planning/phases/23-cross-category-search/23-RESEARCH.md (Pattern 3: groupResultsByCategory, computeResultCap logic)
  </read_first>
  <action>
    Create tests/frontend/17-search-overlay.test.js following the same pattern as 16-catalog-subpage.test.js.

    Require the module: `var mod = require('../../js/modules/17-search-overlay.js');`

    **groupResultsByCategory tests:**

    Test 1 — "groups items by display category name": Pass an array of Fuse results with items in multiple subcategories (e.g. `[{item:{name:'A',cf_subcategory:'Grain'}}, {item:{name:'B',cf_subcategory:'Hops'}}, {item:{name:'C',cf_subcategory:'Grain'}}]`). Assert result is an array with 2 groups. Assert the first group (most matches) has category "Grains" with 2 items. Assert second group has category "Hops" with 1 item.

    Test 2 — "sorts groups by match count descending": Pass items where Hops has more matches than Grain. Assert first group is Hops.

    Test 3 — "collapses subcategories into display names": Pass items with cf_subcategory 'Bottle' and 'Bag'. Assert they are grouped under a single "Packaging" category, not two separate groups.

    Test 4 — "handles items with no cf_subcategory": Pass an item with cf_subcategory undefined or empty string. Assert it groups under "Other" or equivalent fallback.

    Test 5 — "returns empty array for empty input": Pass empty array. Assert result is [].

    **computeResultCap tests:**

    Test 6 — "returns 10 for 1 category": Assert computeResultCap(1) === 10

    Test 7 — "returns 10 for 2 categories": Assert computeResultCap(2) === 10

    Test 8 — "returns 7 for 3 categories": Assert computeResultCap(3) === 7

    Test 9 — "returns 7 for 4 categories": Assert computeResultCap(4) === 7

    Test 10 — "returns 5 for 5+ categories": Assert computeResultCap(5) === 5 and computeResultCap(7) === 5

    After creating the test file, run `npm test` and `npm run lint` to verify all tests pass and lint is clean.
  </action>
  <verify>
    <automated>cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - tests/frontend/17-search-overlay.test.js exists with 40+ lines
    - File contains at least 10 test cases (5 for groupResultsByCategory, 5 for computeResultCap)
    - `npm test` passes with 0 failures (all existing tests still pass, new tests pass)
    - `npm run lint` exits with code 0
    - Tests verify: grouping by display name, sort order by count, subcategory collapse (Bottle+Bag -> Packaging), empty input, cap values 10/7/5
    - Test file uses `require('../../js/modules/17-search-overlay.js')` (same pattern as 16-catalog-subpage.test.js)
  </acceptance_criteria>
  <done>Unit tests for groupResultsByCategory and computeResultCap pass; full test suite (existing + new) passes with 0 failures; lint clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML pages -> CDN | Static pages served from GitHub Pages; no new trust boundary vs existing pages |
| Build pipeline -> artifacts | npm scripts produce minified files from source; same pipeline as all other modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Tampering | HTML script injection | accept | All script tags are static references to known local files; same pattern as existing 15-hops/16-catalog references. No CDN or external URLs |
| T-23-07 | Tampering | Build pipeline integrity | accept | Same cleancss/terser pipeline used for all project assets; no new tools introduced |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase |
</threat_model>

<verification>
1. `npm run build` exits with code 0
2. `npm test` exits with code 0
3. `npm run lint` exits with code 0
4. All 7 pages contain both `search-overlay.min.css` and `17-search-overlay.min.js` references
5. Zero pages contain `disabled` on `.subnav-search-btn`
6. Version strings on overlay files match other files after build (stamp:pages applied)
</verification>

<success_criteria>
- Search overlay is wired into all 7 ingredient pages with correct script loading order
- Build pipeline produces minified artifacts and stamps version strings for new files
- Unit tests for pure search functions pass alongside all existing tests
- npm run build, npm test, and npm run lint all exit cleanly
</success_criteria>

<output>
Create `.planning/phases/23-cross-category-search/23-02-SUMMARY.md` when done
</output>
