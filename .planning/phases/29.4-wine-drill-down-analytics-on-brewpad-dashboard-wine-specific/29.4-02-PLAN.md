---
phase: 29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific
plan: 02
type: execute
wave: 2
depends_on: ["29.4-01"]
files_modified:
  - js/brewpad.js
autonomous: false
requirements: []   # No REQ-IDs; coverage contract is CONTEXT decisions D-01, D-04, D-05, D-06, D-10 + Claude's-Discretion items

must_haves:
  truths:
    - "A 'Wine Breakdown' card renders directly below the Batches-by-Month chart, without modifying that chart (D-01)"
    - "A segmented selector [Subcategory] [Brand] [Manufacturer] [Time] switches the active dimension on click/Enter/Space; subcategory is active on first load (D-04, D-05)"
    - "Selector state is in-memory and resets to subcategory on reload (D-06)"
    - "Horizontal bars render one row per dimension value with per-bar counts, top-8+'Other'+'Unknown' applied for high-cardinality dimensions (D-02, D-07, D-10)"
    - "The catalog snapshot is fetched once and cached; a fetch failure degrades gracefully without breaking the rest of the dashboard"
    - "Empty wine window shows a 'no wine batches' message"
  artifacts:
    - path: "js/brewpad.js"
      provides: "Wine Breakdown card render block, segmented selector, snapshot lazy-fetch, click/keydown handlers, in-memory state vars"
      contains: "Wine Breakdown"
  key_links:
    - from: "Wine Breakdown render block"
      to: "bucketWineDimension / applyTopN (Plan 01)"
      via: "direct call with _dashWineDimension + _dashWineSkuLookup"
      pattern: "bucketWineDimension\\("
    - from: "dimension selector chip"
      to: "renderDashboard"
      via: "data-bp-wine-dim click/keydown handler sets _dashWineDimension"
      pattern: "data-bp-wine-dim"
    - from: "snapshot fetch"
      to: "_dashWineSkuLookup"
      via: "buildSkuLookup(products) on success, _dashWineSkuError on failure, re-render"
      pattern: "zoho-snapshot\\.json"
---

<objective>
Add the "Wine Breakdown" card UI to the BrewPad dashboard render path in `js/brewpad.js`: a segmented dimension selector, lazy-loaded catalog snapshot, horizontal-bar breakdown driven by the Plan 01 helpers, graceful empty/error states, and click/keydown interaction. Then rebuild the bundle.

Purpose: Deliver the user-facing wine drill-down. The card reads `_allBatchesData` (already loaded), joins to the cached snapshot, and renders the selected dimension — letting staff see *what kind* of wine they make, not just "wine vs beer".

Output: New card block + selector + handlers + in-memory state in `js/brewpad.js`, plus regenerated build artifacts via `npm run build`. Ends with a human-verify checkpoint (visual + interaction in the live dashboard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-CONTEXT.md
@.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-PATTERNS.md
@.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Helpers from Plan 01 (already in js/brewpad.js, exported) and existing render-block landmarks. -->

From Plan 01 (top-level in js/brewpad.js):
buildSkuLookup(products) -> { sku: product }
bucketWineDimension(batches, scheduleCategoryById, skuLookup, dimension, monthsBack, now) -> [{label,count}]  ('Unknown' kept last)
applyTopN(buckets, n) -> [{label,count}]  ('Other' then 'Unknown' appended last)

Existing landmarks to render against / mirror:
- Batches-by-Month block: js/brewpad.js lines 1483-1595. New card goes immediately AFTER line 1595 (the block's outer closing brace) and BEFORE the "Attention items" comment / "Needs Attention" header at lines 1597-1608. The Batches-by-Month block is NOT modified (D-01).
- Section header pattern (line 1492): html += '<div class="bp-section-header">Wine Breakdown</div>';
- Legend/segmented chip markup (lines 1521-1524): role="button" tabindex="0" aria-pressed="..." data-bp-chart-type="..." — mirror but use data-bp-wine-dim to avoid colliding with the existing chart toggle handler.
- Click handler region: lines 6181-6193 (e.target.closest('[data-bp-chart-type]') inside dashInner.addEventListener('click', ...)).
- Keydown handler region: lines 6202-6217 (e.target.closest('[data-bp-chart-type]') Enter/Space).
- In-memory state var: line 487 var _dashChartHiddenTypes = {}; (add new _dashWine* vars in the same block).
- escapeHTML: used on every label (lines 1523, 1551, 1576).
- Empty-state class: bp-empty (line 1618 area).

Snapshot fetch path (confirmed in js/modules/07-catalog-kits.js line 21): fetch('/content/zoho-snapshot.json') -> .ok check -> .json() -> snap.products.

Lazy-load-then-re-render analog: js/brewpad.js recipe catalog fetch (~lines 2739-2753): if (!_cache) { fetch().then(set _cache; re-render); return; }.

Build command (CLAUDE.md): npm run build regenerates main.js + min artifacts; never edit main.js/main.min.js directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in-memory state, snapshot lazy-fetch, and the Wine Breakdown card render block</name>
  <read_first>
    - js/brewpad.js lines 1483-1620 (Batches-by-Month block to render after + bp-empty empty-state pattern; confirm exact insertion point at line 1595)
    - js/brewpad.js line 487 (the _dashChartHiddenTypes state-var block)
    - js/brewpad.js ~lines 2739-2753 (lazy-load-then-re-render analog)
    - js/modules/07-catalog-kits.js lines 20-28 (snapshot fetch + .ok + snap.products)
    - .planning/phases/29.4-.../29.4-PATTERNS.md "Wine Breakdown card render block" + "No Analog Found" palette section
    - .planning/phases/29.4-.../29.4-CONTEXT.md decisions D-01, D-02, D-04, D-05, D-06, D-10 + Claude's Discretion (colors, empty state, snapshot fetch, a11y)
  </read_first>
  <action>
    Step 1 — State vars: in the IIFE block at line ~487 (beside var _dashChartHiddenTypes = {};) add three vars: var _dashWineDimension = 'subcategory'; (default per D-04), var _dashWineSkuLookup = null; (null = not yet loaded), var _dashWineSkuError = false; — in-memory only, no localStorage (D-06).

    Step 2 — Lazy snapshot fetch: add a small function (e.g. loadWineSnapshot()) near the dashboard render code that, when _dashWineSkuLookup === null && !_dashWineSkuError, calls fetch('/content/zoho-snapshot.json'), checks r.ok (throw on non-ok), parses .json(), then _dashWineSkuLookup = buildSkuLookup(snap.products || []) and calls renderDashboard(); on .catch, set _dashWineSkuError = true and call renderDashboard(). Mirror the recipe-catalog lazy-load-then-re-render shape. Guard against a re-entrant fetch loop (only fetch when both null and not error and not already in-flight).

    Step 3 — Card render block: immediately AFTER the Batches-by-Month block's closing brace at line 1595 and BEFORE the "Attention items" comment at line ~1597, append the card. Do NOT edit the Batches-by-Month block (D-01). Build it as html += ... inline-style string concatenation (same style as the analog):
      - Section header: '<div class="bp-section-header">Wine Breakdown</div>'.
      - If _dashWineSkuError: render a brief muted load-error note (reuse bp-empty styling) and stop the card here — rest of dashboard unaffected. If _dashWineSkuLookup === null: trigger loadWineSnapshot() and render a brief "Loading…" note (reuse bp-empty); stop the card here.
      - Segmented selector: a flex row of four chips for subcategory, brand, manufacturer, time, each role="button" tabindex="0" aria-pressed="<true if == _dashWineDimension>" data-bp-wine-dim="<dim>" with human labels Subcategory / Brand / Manufacturer / Time. Mirror the legend-chip markup at lines 1521-1524 but with data-bp-wine-dim (NOT data-bp-chart-type). Active chip styled distinctly (e.g. filled background) and aria-pressed="true".
      - Data: var schedCatMap = buildScheduleCategoryById(_fermSchedules); then var rawBuckets = bucketWineDimension(_allBatchesData, schedCatMap, _dashWineSkuLookup, _dashWineDimension, 6); then var buckets = applyTopN(rawBuckets, 8); (D-07 top-8).
      - Empty state: if buckets is empty (zero in-window wine batches) render '<p class="bp-empty">No wine batches started in the last 6 months.</p>' (Claude's-Discretion empty state).
      - Horizontal bars: compute maxCount across buckets (floor 1). For each bucket render a row: a left label (escapeHTML(bucket.label)), a horizontal bar whose width is (count/maxCount)*100% (or a fixed pixel track), the per-bar count shown inline (D-02), and a total label for the card. Assign colors from a local palette array var WINE_DIM_PALETTE = ['#7b2d3b','#a34a2b','#c8852a','#d4a72c','#4a6f4b','#3a9aa6','#5b6dc8','#9c5bb5']; by index WINE_DIM_PALETTE[i % WINE_DIM_PALETTE.length], with 'Other' hardcoded to #9aa0a6 and 'Unknown' to #c0b8b0 (deterministic per-value, Claude's-Discretion colors). escapeHTML() every label and tooltip string.
      - a11y: selector chips carry aria-pressed (mirroring the legend chips); keep markup consistent with the existing chart's conventions.
    Write the actual JS into js/brewpad.js. Do not run build yet (Task 2).
  </action>
  <verify>
    <automated>node -e "var s=require('fs').readFileSync('js/brewpad.js','utf8');['Wine Breakdown','data-bp-wine-dim','_dashWineDimension','bucketWineDimension(','applyTopN(','zoho-snapshot.json','WINE_DIM_PALETTE'].forEach(function(t){if(s.indexOf(t)<0)throw new Error('missing '+t);});console.log('render OK');"</automated>
  </verify>
  <acceptance_criteria>
    - js/brewpad.js contains the string `<div class="bp-section-header">Wine Breakdown</div>` (or equivalent literal "Wine Breakdown" header)
    - js/brewpad.js contains `var _dashWineDimension = 'subcategory'`, `var _dashWineSkuLookup`, `var _dashWineSkuError`
    - js/brewpad.js contains `data-bp-wine-dim` and four selector chips (subcategory/brand/manufacturer/time)
    - js/brewpad.js calls `bucketWineDimension(` and `applyTopN(` with `_dashWineSkuLookup` and `_dashWineDimension`
    - js/brewpad.js fetches `/content/zoho-snapshot.json` and sets `_dashWineSkuError = true` in a catch
    - js/brewpad.js contains `WINE_DIM_PALETTE` and the empty-state string "No wine batches started in the last 6 months."
    - The Batches-by-Month block (lines 1483-1595) is byte-unchanged except for nothing inserted within it (D-01) — new code appended after line 1595 only
    - The node verify command above prints "render OK" and exits 0
  </acceptance_criteria>
  <done>Card render block, state vars, and lazy snapshot fetch are present; verify smoke exits 0; Batches-by-Month block untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire selector click/keydown handlers and rebuild the bundle</name>
  <read_first>
    - js/brewpad.js lines 6181-6217 (the data-bp-chart-type click + keydown handlers to mirror)
    - js/brewpad.js the card block added in Task 1 (the data-bp-wine-dim chips)
    - CLAUDE.md "Build Artifacts" + "After any JS module change run npm run build" rules
  </read_first>
  <action>
    Step 1 — Click handler: inside the existing dashInner.addEventListener('click', ...) block, alongside the data-bp-chart-type branch at lines 6181-6193, add a branch that does var dimChip = e.target.closest('[data-bp-wine-dim]'); if (dimChip) { var dim = dimChip.getAttribute('data-bp-wine-dim'); if (dim && dim !== _dashWineDimension) { _dashWineDimension = dim; renderDashboard(); } return; }. Mirror the existing branch's structure exactly. Place it so it does not shadow the existing data-bp-chart-type branch (the two data- attributes are distinct, so order is safe — but keep the early return).

    Step 2 — Keydown handler: inside the existing dashInner.addEventListener('keydown', ...) block (lines 6202-6217), add the same closest('[data-bp-wine-dim]') branch guarded by e.key === 'Enter' || e.key === ' ', with e.preventDefault() then set _dashWineDimension and renderDashboard(). Mirror the data-bp-chart-type keydown branch exactly.

    Step 3 — Build: run npm run build to regenerate js/main.js and the min artifacts (the build concatenates/minifies; brewpad.js is bundled per the build pipeline). Do not hand-edit any build artifact.
  </action>
  <verify>
    <automated>npm run build && node -e "var s=require('fs').readFileSync('js/brewpad.js','utf8');if(s.indexOf(\"closest('[data-bp-wine-dim]')\")<0)throw new Error('no wine-dim handler');console.log('handler OK')" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - js/brewpad.js contains `closest('[data-bp-wine-dim]')` in BOTH the click and keydown listener blocks
    - The wine-dim click branch sets `_dashWineDimension` and calls `renderDashboard()`
    - The wine-dim keydown branch is guarded by Enter/Space and calls `e.preventDefault()`
    - `npm run build` exits 0 (artifacts regenerated)
    - `npm test` exits 0 — full frontend suite green, including the Plan 01 wine-breakdown suite (no regression)
    - `npm run lint` exits 0 (CLAUDE.md pre-commit rule)
  </acceptance_criteria>
  <done>Selector handlers wired, bundle rebuilt, full test suite + lint green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    A "Wine Breakdown" card below the Batches-by-Month chart on the BrewPad dashboard: a segmented [Subcategory] [Brand] [Manufacturer] [Time] selector (default Subcategory), horizontal bars per dimension value over the last 6 months, top-8+'Other' collapsing for high-cardinality dimensions, an 'Unknown' bucket for join/data gaps, and normalized week-sorted Time bars. Snapshot is lazy-loaded from /content/zoho-snapshot.json with graceful failure.
  </what-built>
  <how-to-verify>
    1. Run the frontend locally (or push to staging: git push origin main) and open the BrewPad dashboard, logging in via Google OAuth.
    2. Confirm a "Wine Breakdown" card appears directly BELOW the existing Batches-by-Month chart, and that the Batches-by-Month chart still looks/behaves exactly as before (legend toggle still works).
    3. Default selection is Subcategory; bars show Red / White / Fruit / Rosé / Specialty-style values sorted by count, each with a count label and a total.
    4. Click Brand — bars switch to brand values; if more than 8 distinct, confirm a single 'Other' bar appears last (before any 'Unknown').
    5. Click Manufacturer, then Time — confirm Time bars are ordered 4, 5, 6, 8 weeks ascending (not by count) and that '5 week'/'5 weeks' are merged into one bar.
    6. Confirm an 'Unknown' bar appears last when there are wine batches whose SKU has no catalog match or an empty dimension value.
    7. Keyboard: Tab to a selector chip, press Enter/Space — the dimension switches; active chip shows aria-pressed=true (inspect or use a screen reader).
    8. Simulate snapshot failure (block /content/zoho-snapshot.json in devtools, reload) — confirm the card shows a graceful note and the REST of the dashboard still renders normally.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (wrong placement, sort order, missing Unknown/Other, broken a11y, fetch-failure breakage).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static snapshot file → client JS | /content/zoho-snapshot.json is static, same-origin, public catalog data (no PII, no secrets) |
| catalog dimension values → DOM | brand/manufacturer/subcategory/time strings from the snapshot are rendered into the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.4-01 | Tampering | dimension/bucket labels + tooltips rendered to DOM | mitigate | every label and tooltip string passed through the existing escapeHTML() before concatenation (mirrors lines 1523/1551/1576) |
| T-29.4-02 | Denial of Service | snapshot fetch failure | mitigate | fetch failure sets _dashWineSkuError and degrades to a note; never throws into renderDashboard; no infinite re-fetch loop (guarded by null+error flags) |
| T-29.4-03 | Information Disclosure | catalog snapshot contents | accept | snapshot is already public product data served to all site visitors; no new exposure |

No package installs in this phase (frontend-only, no new deps) → no supply-chain (T-29.4-SC) checkpoint required.
</threat_model>

<verification>
- node smoke: card markup, state vars, helper calls, palette, fetch path, both handlers present in js/brewpad.js.
- npm run build exits 0 (artifacts regenerated; main.js/min not hand-edited).
- npm test exits 0 (full frontend suite + Plan 01 wine-breakdown suite green).
- npm run lint exits 0.
- Human checkpoint: placement below Batches-by-Month (D-01), default Subcategory (D-04), segmented selector switching (D-05), in-memory reset (D-06), top-8+Other (D-07), Unknown last (D-10), Time week-sort + merge (D-12), graceful snapshot-failure degrade.
</verification>

<success_criteria>
- Wine Breakdown card renders below the untouched Batches-by-Month chart (D-01).
- Segmented selector defaults to Subcategory, switches dimension on click/Enter/Space, state in-memory (D-04, D-05, D-06).
- Horizontal bars with counts; top-8+'Other' and 'Unknown'-last applied via Plan 01 helpers (D-02, D-07, D-10).
- Snapshot lazy-loaded once and cached; failure degrades gracefully.
- Build + full test suite + lint all green; human verification approved.
</success_criteria>

<output>
Create `.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-02-SUMMARY.md` when done.
</output>
