---
phase: 29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/brewpad.js
  - tests/frontend/brewpad-wine-breakdown.test.js
autonomous: true
requirements: []   # No REQ-IDs map to this phase; coverage contract is CONTEXT decisions D-02, D-04, D-07, D-08, D-09, D-10, D-12

must_haves:
  truths:
    - "Given wine batches + a sku→product map, the breakdown returns one {label,count} per distinct dimension value over the last 6 months (D-02, D-03)"
    - "A dimension with >8 distinct values collapses its tail into a single 'Other' bucket that always sorts last (D-07, D-08, D-09)"
    - "A wine batch with no catalog match OR an empty dimension value lands in a distinct 'Unknown' bucket that sorts after 'Other' (D-10)"
    - "The 'time' dimension normalizes singular/plural variants ('5 week' → '5 weeks') and sorts by week number ascending; all other dimensions sort by count descending (D-02, D-12)"
    - "Helpers are pure, exported via the module.exports guard, and covered by a passing Jest suite"
  artifacts:
    - path: "js/brewpad.js"
      provides: "buildSkuLookup, normalizeWineTime, bucketWineDimension, applyTopN pure helpers + export entries"
      contains: "function bucketWineDimension"
    - path: "tests/frontend/brewpad-wine-breakdown.test.js"
      provides: "Jest coverage for all four new helpers"
      contains: "require('../../js/brewpad')"
  key_links:
    - from: "bucketWineDimension"
      to: "resolveBatchType"
      via: "wine membership gate (D-11)"
      pattern: "resolveBatchType\\([^)]*\\)\\s*===\\s*'wine'"
    - from: "bucketWineDimension"
      to: "skuLookup (built by buildSkuLookup)"
      via: "batch.product_sku join to derive dimension value"
      pattern: "product_sku"
    - from: "js/brewpad.js module.exports"
      to: "tests/frontend/brewpad-wine-breakdown.test.js"
      via: "destructured require"
      pattern: "bucketWineDimension:\\s*bucketWineDimension"
---

<objective>
Add the four pure helper functions that power the Wine Breakdown card to `js/brewpad.js`, export them via the existing `module.exports` guard, and cover them with a new Jest suite. This is the data/transform layer — no DOM, no fetch, no rendering.

Purpose: Isolate all the join/bucketing/normalization/top-N logic into pure, deterministic, unit-tested functions so the render layer (Plan 02) is a thin presentation shell. Mirrors the proven `bucketBatchesByMonthType` + `brewpad-dashboard-chart.test.js` pattern from Phase 29.3.

Output: Four exported helpers (`buildSkuLookup`, `normalizeWineTime`, `bucketWineDimension`, `applyTopN`) and `tests/frontend/brewpad-wine-breakdown.test.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-CONTEXT.md
@.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-PATTERNS.md
@CLAUDE.md
@TESTING.md

<interfaces>
<!-- Existing helpers in js/brewpad.js the new code reuses. Use directly — no exploration needed. -->

From js/brewpad.js (lines 163-258), top-level scope, ES5 `var`:
```
var KNOWN_BATCH_TYPES = { wine: true, beer: true, cider: true, seltzer: true };
function buildScheduleCategoryById(schedules)   // schedules[] -> { schedule_id: category(lowercased) }
function resolveBatchType(batch, scheduleCategoryById)  // -> 'wine'|'beer'|'cider'|'seltzer'|'other'
function bucketBatchesByMonthType(batches, scheduleCategoryById, monthsBack, now)  // last-N-month window analog
```

Month-window convention to mirror (from bucketBatchesByMonthType, lines 245-250):
- date preference: `b.start_date || b.created_at`
- month key: `String(dateStr).slice(0, 7)` ('YYYY-MM')
- `var refDate = now ? new Date(now) : new Date();` — optional `now` for deterministic tests
- last 6 months window (monthsBack default 6)

Catalog product shape (content/zoho-snapshot.json `products[]`, real fields):
```
{ sku: "27855", type: "Wine", subcategory: "Fruit", brand: "Orchard Breezin'",
  manufacturer: "RJS Craft Winemaking", time: "4 weeks", ... }
```
Join key: `products[].sku` (string) ↔ `batch.product_sku` (string).

Existing module.exports guard (lines 6845-6873): a single object literal appended after the IIFE; new helper names get added to it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the four pure helpers in js/brewpad.js</name>
  <read_first>
    - js/brewpad.js lines 163-258 (KNOWN_BATCH_TYPES, buildScheduleCategoryById, resolveBatchType, bucketBatchesByMonthType — the analogs to copy structure from)
    - js/brewpad.js lines 6845-6873 (the module.exports guard to extend)
    - .planning/phases/29.4-.../29.4-PATTERNS.md "new pure helpers to write" + "No Analog Found" sections (exact spec for normalizeWineTime and applyTopN)
    - .planning/phases/29.4-.../29.4-CONTEXT.md decisions D-02, D-03, D-07, D-08, D-09, D-10, D-11, D-12
  </read_first>
  <behavior>
    - buildSkuLookup([]) returns {}; buildSkuLookup([{sku:'27855',...}]) returns { '27855': product }; skips entries with no sku; later duplicate sku wins (last-write).
    - normalizeWineTime('5 week') returns label '5 weeks' and week 5; normalizeWineTime('4 weeks') returns '4 weeks'/4; normalizeWineTime('') or non-week strings return the trimmed input with week = a large sentinel (so they sort last); singular→plural merge so '5 week' and '5 weeks' aggregate into one bucket.
    - bucketWineDimension over a fixed `now`: counts ONLY batches where resolveBatchType(batch, schedCat) === 'wine' AND start_date/created_at falls in the last 6 months (D-11, D-03).
    - bucketWineDimension dimension='subcategory': returns [{label,count}] sorted by count desc (D-02); a wine batch whose sku misses the lookup, or whose product has empty subcategory, increments an 'Unknown' bucket (D-10).
    - bucketWineDimension dimension='time': values normalized via normalizeWineTime and sorted by week number ascending, not by count (D-12).
    - applyTopN(buckets, 8): when distinct non-Unknown values ≤ 8, returns them unchanged (order preserved); when > 8, keeps the 8 highest-count, folds the remainder into { label:'Other', count:sum }, appends 'Other' last; any { label:'Unknown' } bucket is pulled out and re-appended AFTER 'Other' (D-07, D-08, D-09, D-10).
  </behavior>
  <action>
    Insert the four helpers at top-level scope immediately after `bucketBatchesByMonthType` (after line 258) and before `isSessionStale`, matching the existing no-import ES5 `var`/`function` style.

    1. `buildSkuLookup(products)` — iterate `products`, build `{ String(p.sku): p }`, skip falsy sku. Structural analog: `buildScheduleCategoryById`.

    2. `normalizeWineTime(timeStr)` — return an object `{ label: <normalized string>, week: <integer> }`. Parse the leading integer with a regex on the digits; canonical label is `<n> weeks` (always plural) when a numeric week count is found; otherwise label = `String(timeStr).trim()` and `week = 9999` (sentinel so non-numeric times sort last). This is the singular/plural merge mechanism — both '5 week' and '5 weeks' map to label '5 weeks', week 5.

    3. `bucketWineDimension(batches, scheduleCategoryById, skuLookup, dimension, monthsBack, now)` — mirror `bucketBatchesByMonthType`'s window setup: `var refDate = now ? new Date(now) : new Date();`, build the set of in-window 'YYYY-MM' month keys (default 6 months). For each batch: skip if `resolveBatchType(b, scheduleCategoryById) !== 'wine'` (D-11); skip if `String(b.start_date || b.created_at).slice(0,7)` not in window. Look up `skuLookup[String(b.product_sku)]`; if no product OR the product's `dimension` field is empty → bucket label `'Unknown'`. Else read `product[dimension]`; for dimension `'time'`, pass through `normalizeWineTime(...).label` and track its `.week` for sort ordering. Aggregate into a `{ label: count }` tally, then emit `[{label,count}]`: for `dimension==='time'` sort by week ascending (keep 'Unknown' separate, last); for all other dimensions sort by count descending. Always keep the `'Unknown'` bucket out of the count-sort and append it last. Accept `now` as the final param for deterministic tests. Do NOT call applyTopN here — return the full sorted list; top-N collapsing is the render layer's call (or applied explicitly in tests).

    4. `applyTopN(buckets, n)` — split out any `{label:'Unknown'}` entry. Of the remainder, if length ≤ `n` return remainder + Unknown(if present). Else sort remainder by count desc, take first `n`, sum the rest into `{ label:'Other', count: <sum> }`, append 'Other' after the top-n, then append 'Unknown' last (D-09 'Other' last among normal buckets, D-10 'Unknown' after 'Other'). Pure — no mutation of the input array.

    Then extend the `module.exports` object (lines 6845-6873) by adding: `buildSkuLookup: buildSkuLookup, normalizeWineTime: normalizeWineTime, bucketWineDimension: bucketWineDimension, applyTopN: applyTopN`. Do not touch any existing export entry.

    No fenced code in the card render path — this task is helpers + exports only; the render block is Plan 02.
  </action>
  <verify>
    <automated>node -e "var bp=require('./js/brewpad'); ['buildSkuLookup','normalizeWineTime','bucketWineDimension','applyTopN'].forEach(function(f){ if(typeof bp[f]!=='function') throw new Error('missing '+f); }); console.log('exports OK');"</automated>
  </verify>
  <acceptance_criteria>
    - js/brewpad.js contains `function buildSkuLookup(`, `function normalizeWineTime(`, `function bucketWineDimension(`, `function applyTopN(`
    - js/brewpad.js module.exports contains `bucketWineDimension: bucketWineDimension` (and the other three)
    - bucketWineDimension's signature includes a trailing `now` parameter
    - bucketWineDimension uses `resolveBatchType(` and `=== 'wine'` as the membership gate (D-11)
    - The node `require` verify command above prints "exports OK" and exits 0
    - No existing export entry removed (grep still finds `bucketBatchesByMonthType: bucketBatchesByMonthType`)
  </acceptance_criteria>
  <done>All four helpers exist at top-level scope, are exported, and the require-smoke command exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Jest suite tests/frontend/brewpad-wine-breakdown.test.js</name>
  <read_first>
    - tests/frontend/brewpad-dashboard-chart.test.js lines 1-33 (the global-stub header block to copy verbatim) and lines 36-274 (describe/it structure + REF_DATE deterministic-time convention)
    - js/brewpad.js (the four helpers written in Task 1)
    - TESTING.md (frontend Jest jsdom SOP)
  </read_first>
  <behavior>
    - buildSkuLookup: empty array → {}; maps sku→product; skips falsy sku.
    - normalizeWineTime: '5 week'→{label:'5 weeks',week:5}; '4 weeks'→{label:'4 weeks',week:4}; '' / 'misc'→ sentinel week so it sorts last; '5 week' and '5 weeks' produce identical labels.
    - bucketWineDimension (REF_DATE fixed): only wine batches in the 6-month window are counted; non-wine batches excluded; out-of-window batches excluded; subcategory split sorted by count desc; sku miss OR empty dimension → 'Unknown' last; time dimension sorted by week asc.
    - applyTopN: ≤8 distinct unchanged; >8 distinct → top 8 + 'Other'(sum of tail) with 'Other' last; 'Unknown' always appended after 'Other'.
  </behavior>
  <action>
    Create `tests/frontend/brewpad-wine-breakdown.test.js`. Copy the global-stub header block from `tests/frontend/brewpad-dashboard-chart.test.js` lines 1-33 verbatim (the `global.document/window/navigator/google/fetch/localStorage/sessionStorage` stubs, the `js/lib/auth` wiring, and `var bp = require('../../js/brewpad');`) — change only the leading comment to describe the wine breakdown suite.

    Add one `describe` block per helper with `it()`/`expect()` synchronous tests covering every bullet in `<behavior>`. Use a fixed `var REF_DATE = '2026-03-15';` (mirroring the dashboard-chart test's deterministic `now` convention) for all bucketWineDimension calls. Build small inline fixture arrays of batch objects (with `category:'wine'`, `product_sku`, `start_date`) and a small `skuLookup` object literal — do not load the real snapshot. Cover the cardinality scenarios from CONTEXT specifics: subcategory (≤8, no collapse), a >8-distinct brand case to exercise applyTopN's 'Other', a sku-miss and empty-value case for 'Unknown', and a time case with '5 week'/'5 weeks' merge + ascending week sort.

    Do not modify any existing test file (CLAUDE.md rule 10).
  </action>
  <verify>
    <automated>npm test -- brewpad-wine-breakdown</automated>
  </verify>
  <acceptance_criteria>
    - File tests/frontend/brewpad-wine-breakdown.test.js exists and contains `require('../../js/brewpad')`
    - File contains a describe block for each of: buildSkuLookup, normalizeWineTime, bucketWineDimension, applyTopN
    - `npm test -- brewpad-wine-breakdown` exits 0 with all assertions passing
    - At least one bucketWineDimension test asserts a non-wine batch is excluded (D-11) and one asserts an 'Unknown' bucket sorts last (D-10)
    - At least one applyTopN test asserts 'Other' precedes 'Unknown' and follows the 8 top buckets (D-08/D-09/D-10)
    - Full suite still green: `npm test` exits 0 (no regression in existing brewpad tests)
  </acceptance_criteria>
  <done>New Jest suite passes and the full frontend suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static snapshot file → client JS | `content/zoho-snapshot.json` is static, same-origin, public catalog data (no PII, no secrets); consumed in Plan 02, not this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29.4-01 | Tampering | dimension labels rendered to DOM (Plan 02) | mitigate | all labels passed through existing `escapeHTML()` in the render layer (Plan 02); pure helpers here emit plain strings only |
| T-29.4-02 | Information Disclosure | catalog snapshot contents | accept | snapshot is already public product data served to all site visitors; no new exposure |

No package installs in this phase (frontend-only, no new deps) → no supply-chain (T-29.4-SC) checkpoint required.
</threat_model>

<verification>
- `node -e` require smoke confirms all four helpers exported.
- `npm test -- brewpad-wine-breakdown` green.
- `npm test` (full frontend suite) green — no regression.
- Helpers are pure: no `fetch`, `document`, or `window` references inside the four new functions (grep the function bodies).
</verification>

<success_criteria>
- buildSkuLookup, normalizeWineTime, bucketWineDimension, applyTopN exist, are pure, and exported.
- D-02 (count-sort), D-03 (6-month window), D-07/D-08 (top-8 generic), D-09 ('Other' last), D-10 ('Unknown' last), D-11 (resolveBatchType wine gate), D-12 (time normalize + week sort) all proven by passing tests.
- Full frontend Jest suite green.
</success_criteria>

<output>
Create `.planning/phases/29.4-wine-drill-down-analytics-on-brewpad-dashboard-wine-specific/29.4-01-SUMMARY.md` when done.
</output>
