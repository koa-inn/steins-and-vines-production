---
phase: 17-custom-labels-page
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - package.json
autonomous: false
requirements: [D-18]

must_haves:
  truths:
    - "npm run build completes without errors and stamps custom-labels.html"
    - "npm test passes with no regressions"
    - "npm run lint passes with no new errors"
    - "custom-labels.html renders correctly in browser with mockup tool functional"
  artifacts:
    - path: "js/main.min.js"
      provides: "Rebuilt minified main bundle"
    - path: "css/styles.min.css"
      provides: "Rebuilt minified CSS"
  key_links:
    - from: "package.json"
      to: "custom-labels.html"
      via: "stamp:pages script"
      pattern: "custom-labels.html"
---

<objective>
Integrate the new labels page into the build pipeline, run full test suite and lint, rebuild all artifacts, and verify the complete page works visually.

Purpose: Ensures the new page is production-ready — build stamps work, no regressions, and the mockup tool functions correctly.
Output: Updated package.json, rebuilt artifacts, human verification of visual result
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-custom-labels-page/17-01-SUMMARY.md
@.planning/phases/17-custom-labels-page/17-02-SUMMARY.md

<interfaces>
<!-- Build pipeline from package.json -->

stamp:pages script (current):
```
node -e "const fs=require('fs');const v=Date.now().toString(36);['products.html','ingredients.html','reservation.html','about.html','contact.html','products/ferment-in-store.html','products/ingredients-supplies.html'].forEach(function(p){let f=fs.readFileSync(p,'utf8');f=f.replace(/styles\\.min\\.css\\?v=[^\"]+/g,'styles.min.css?v='+v);f=f.replace(/main\\.min\\.js\\?v=[^\"]+/g,'main.min.js?v='+v);fs.writeFileSync(p,f);});console.log('Page cache versions updated to '+v);"
```

minify:css script (current):
```
cleancss -o css/styles.min.css css/styles.css && cleancss -o css/admin.min.css css/admin.css && cleancss -o css/batch.min.css css/batch.css && cleancss -o css/kiosk.min.css css/kiosk.css && cleancss -o css/brewpad.min.css css/brewpad.css
```

concat:js script does NOT include 14-labels.js — correct, since it loads as a separate <script> on custom-labels.html only.

14-labels.js needs its own minification step added to minify:js:
```
terser js/modules/14-labels.js -o js/modules/14-labels.min.js -c -m
```

custom-labels.html <script> tag should reference the minified version for production.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update build pipeline and run full build, test, and lint</name>
  <files>package.json, custom-labels.html</files>
  <read_first>
    package.json (current stamp:pages, minify:css, minify:js scripts)
    custom-labels.html (script tag referencing 14-labels.js — needs to become 14-labels.min.js)
  </read_first>
  <action>
**Per D-18:** Update the build pipeline to include the new labels page and module.

**1. Update `stamp:pages` in package.json:**
Add `'custom-labels.html'` to the array of pages in the stamp:pages script. The updated array should be:
```
['products.html','ingredients.html','reservation.html','about.html','contact.html','products/ferment-in-store.html','products/ingredients-supplies.html','custom-labels.html']
```

Also add a cache-busting replace for `labels.css` in the stamp:pages script. After the existing `main.min.js` replace, add:
```javascript
f=f.replace(/labels\.css\?v=[^"]+/g,'labels.css?v='+v);
f=f.replace(/14-labels\.min\.js\?v=[^"]+/g,'14-labels.min.js?v='+v);
```

**2. Update `minify:css` in package.json:**
Append `&& cleancss -o css/labels.min.css css/labels.css` to the existing minify:css command.

**3. Update `minify:js` in package.json:**
Append `&& terser js/modules/14-labels.js -o js/modules/14-labels.min.js -c -m` to the existing minify:js command.

**4. Update `custom-labels.html`:**
Change the labels CSS link from:
```html
<link rel="stylesheet" href="css/labels.css">
```
to:
```html
<link rel="stylesheet" href="css/labels.min.css?v=initial">
```

Change the labels JS script from:
```html
<script src="js/modules/14-labels.js" defer></script>
```
to:
```html
<script src="js/modules/14-labels.min.js?v=initial" defer></script>
```

(The `?v=initial` will be replaced by the stamp:pages script on build.)

**5. Run the full build and test suite:**
```bash
npm run build
npm test
npm run lint
```

All three must pass. Fix any lint errors in 14-labels.js (adjust ESLint config if needed for the new module pattern).
  </action>
  <verify>
    <automated>cd "/Users/koa/Library/CloudStorage/GoogleDrive-hello@steinsandvines.ca/Shared drives/Steins and Vines/5 IT/steins-and-vines-website" && npm run build 2>&1 | tail -5 && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "custom-labels.html" package.json` returns at least 1 match (in stamp:pages)
    - `grep "labels.css" package.json` returns at least 1 match (in minify:css)
    - `grep "14-labels" package.json` returns at least 1 match (in minify:js)
    - `test -f css/labels.min.css` is true (minified CSS created by build)
    - `test -f js/modules/14-labels.min.js` is true (minified JS created by build)
    - `grep "labels.min.css" custom-labels.html` returns 1 (uses minified version)
    - `grep "14-labels.min.js" custom-labels.html` returns 1 (uses minified version)
    - `npm run build` exits 0
    - `npm test` exits 0 with no test failures
    - `npm run lint` exits 0 with no errors
  </acceptance_criteria>
  <done>Build pipeline includes custom-labels.html in page stamps, labels.css in CSS minification, 14-labels.js in JS minification. All builds pass. All tests pass. Lint is clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete custom labels page with hero section, how-it-works steps, pricing table, interactive canvas mockup tool, design guidelines, and CTA. Navigation updated on all public pages. Build pipeline updated and rebuilt.</what-built>
  <how-to-verify>
1. Open `custom-labels.html` in your browser (staging URL or local file)
2. Verify the page has the green hero with "Custom Label Printing" headline
3. Verify the 3-step "How It Works" section is visible with numbered circles
4. Verify the pricing table shows 5 label types with prices and "$10 setup fee" callout
5. Verify the "Preview Your Label" section shows a canvas with the bottle template and "Your Design Here" placeholder
6. Click "Can" tab — canvas should switch to can template
7. Click "Wine Bottle" tab — canvas should switch back
8. Click "Upload Your Design" and select a PNG or JPG image under 5MB — it should appear in the label region on the template
9. Click "Reset" — image should clear and return to placeholder
10. Try uploading a file larger than 5MB — should show an alert
11. Verify the "Design Guidelines" section shows 4 guideline cards
12. Verify the CTA section has "Contact Us" button linking to contact.html
13. Visit index.html — Products dropdown should show "Custom Labels" as third option
14. Visit about.html — same check
15. Verify mobile responsiveness by resizing browser to phone width (steps should stack vertically, canvas should shrink)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline | New scripts added to package.json — no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering | build scripts | accept | Build scripts run locally, same trust level as existing scripts |
</threat_model>

<verification>
- npm run build exits 0
- npm test exits 0 with no failures
- npm run lint exits 0
- css/labels.min.css and js/modules/14-labels.min.js exist after build
- custom-labels.html references minified versions with cache-bust params
- Human confirms visual correctness and mockup tool functionality
</verification>

<success_criteria>
- Build pipeline fully updated (stamp, CSS minify, JS minify)
- All tests pass, lint clean
- Minified artifacts exist
- Human verifies page looks correct and mockup tool works
</success_criteria>

<output>
After completion, create `.planning/phases/17-custom-labels-page/17-03-SUMMARY.md`
</output>
