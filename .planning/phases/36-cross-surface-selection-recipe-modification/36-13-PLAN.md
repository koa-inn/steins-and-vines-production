---
phase: 36-cross-surface-selection-recipe-modification
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01, MOD-02]
must_haves:
  truths:
    - "GAP-4: the UI-SPEC now mandates a PROMINENT live price that is visible whenever a sale-type is selected and updates on EVERY volume/×factor/ingredient change (server-authoritative, displayed==charged), with the _kioskModifyPanelOpen gate explicitly forbidden on all quote write paths (MOD-02, D-06)"
    - "GAP-5: the UI-SPEC now specifies the scroll model for #kiosk-recipe-prompt (sale surfaces) and the #bp-recipe-attach-expanded DOM restructure into the scrollable BrewPad detail pane so the full ingredient list AND the accept/Add-to-Cart/Attach buttons are reachable on the iPad"
    - "GAP-7: the UI-SPEC records the audit polish (44px touch targets incl. .bp-ing-remove, font-size:1rem on modify-row inputs, price-preview card styling, element order, no-phantom-row empty state, off-palette autocomplete fix) and the recommended panel element order + scroll structure"
    - "The contract preserves server-authoritative pricing (D-06): the now-prominent price comes from the SERVER quote, never client math; BrewPad still shows NO price and makes NO quote/charge (D-10)"
  artifacts:
    - path: ".planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md"
      provides: "Updated design contract: live-price visibility rule, scroll model, audit polish, element order"
      contains: "Live Price Visibility"
  key_links:
    - from: "36-UI-SPEC.md live-price rule"
      to: "GET /api/kiosk/recipe-quote (server-authoritative)"
      via: "prominent price is the server quote total, never client math (D-06)"
      pattern: "server-authoritative"
---

<objective>
Update 36-UI-SPEC.md — the shared cross-surface design contract — to encode the fixes for the second-pass UAT gaps so all three surface plans (36-14 admin, 36-15 kiosk, 36-16 BrewPad) implement against ONE consistent spec (D-01).

This is a documentation-only plan (no code, no build). It leads the gap-closure wave so the surface plans have an authoritative contract to read.

Specifically extend the spec with:
- GAP-4 — Live Price Visibility contract: a prominent price visible whenever a sale-type is selected, updated on EVERY change (volume / ×factor / ingredient edits), sourced from the SERVER quote (displayed==charged, D-06). The `_kioskModifyPanelOpen` gate is explicitly forbidden on quote write paths. BrewPad shows NO price (D-10).
- GAP-5 — Scroll Model contract: `#kiosk-recipe-prompt` becomes a bounded scrollable flex column on the sale surfaces; `#bp-recipe-attach-expanded` is rendered INSIDE the scrollable BrewPad detail pane (not as a clipped global sibling).
- GAP-7 — Audit Polish: fold the UI-REVIEW.md Critical + High findings into the spec (44px touch targets incl. `.bp-ing-remove`, `font-size:1rem` on modify-row inputs, price-preview card styling, element order with save-as-new below Add-to-Cart, no-phantom-row empty state, off-palette kiosk autocomplete fix, `× factor` label colon fix on BrewPad).

Purpose: a single contract prevents the three surface implementations from diverging (the round-1 lesson). The surface plans cite this spec.
Output: an updated 36-UI-SPEC.md with new/clarified sections; no source files touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-REVIEW.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Live Price Visibility + Scroll Model contract sections to 36-UI-SPEC.md (GAP-4, GAP-5)</name>
  <files>.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md</files>
  <read_first>
    - 36-UI-REVIEW.md §"Recommended Element Order + Scroll Structure" (lines 25-54), §"Top 3 Priority Fixes" (lines 58-64), §"UI-SPEC Update Notes" (lines 213-225)
    - 36-HUMAN-UAT.md §"Second-pass UAT gaps" GAP-4 (live price root cause) + GAP-5 (scroll root cause)
    - 36-UI-SPEC.md existing §"Modified-Price Preview: Display Rules" (lines 252-267) and §"Modify-Panel Polish & Ordering (GAP-2)" (lines 350-409) — extend, do not duplicate
    - 36-CONTEXT.md D-06 (server-authoritative quote) + D-10 (BrewPad no price/charge)
  </read_first>
  <action>
    Append a new top-level section to 36-UI-SPEC.md titled "## Live Price Visibility (GAP-4, MOD-02, D-06)" stating:
    1. The visible/prominent recipe price (`#kiosk-recipe-summary-price`) MUST reflect the live SERVER quote total and update on EVERY change: target-volume edit, ×factor edit, ingredient add/remove/qty-change — regardless of whether the Modify Ingredients panel is open or closed.
    2. The `_kioskModifyPanelOpen` gate MUST NOT appear on any write path inside `kioskFetchRecipeQuote` (admin.js:11031/11043/11056/11066; kiosk.js:801/813/826/836). Name those exact line anchors in the spec as "remove these gates".
    3. `#kiosk-recipe-price-preview` MUST be moved OUT of `#kiosk-recipe-modify-wrap` and become a standalone element placed between the modify-wrap and the stock-conflict panel; it is shown as soon as a sale-type is selected (not only when modify is open).
    4. Server-authoritative rule (verbatim emphasis): the prominent price is the SERVER quote total (`GET /api/kiosk/recipe-quote`), never client-side math. The client renders what the server returns. Loading = "Calculating…" (--ink-tertiary); error = "Price unavailable — check connection" (--batch-danger), and the displayed price never silently shows a stale/base value once a quote is in flight.
    5. BrewPad: NO price preview, NO prominent live price, NO quote call on attach (D-10) — explicitly restate this so 36-16 does not add one.

    Append a second new section "## Scroll Model (GAP-5)" stating:
    1. Sale surfaces (admin + kiosk): `#kiosk-recipe-prompt` MUST be a bounded scrollable flex column — `height:100%; overflow-y:auto; -webkit-overflow-scrolling:touch` (applied via a dedicated class, e.g. `.kiosk-recipe-prompt-view`, toggled when the prompt replaces the grid) so the full ingredient list AND the sale-type/Add-to-Cart buttons are reachable on the iPad. Items 1–3 (back/name, summary, volume+factor row) should be `flex-shrink:0`; the rest scroll. Add-to-Cart MAY be `position:sticky; bottom:0` within the scroll container.
    2. BrewPad: `#bp-recipe-attach-expanded` MUST be rendered INSIDE the scrollable `.bp-batch-detail-pane` (via the `sectionBodyEl` passed to `wireAttachExpandedPanel`), NOT as a sibling of `.bp-batches-layout` where `.bp-batches-panel{overflow:hidden}` (brewpad.css:515) clips it. The static `#bp-recipe-attach-expanded` in brewpad.html becomes a template fragment / is injected into the scrollable pane.

    Reproduce (or reference by line) the "Recommended Element Order + Scroll Structure" block from 36-UI-REVIEW.md (lines 29-54) inside the spec so the canonical element order (1 back+name … 11 save-as-new BELOW Add-to-Cart) lives in the contract.

    Keep edits ES5-agnostic (this is a doc). Do not remove the existing GAP-2/GAP-3 sections — these new sections supplement them.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -q "Live Price Visibility" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "Scroll Model" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "_kioskModifyPanelOpen" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "bp-batch-detail-pane" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && echo PASS</automated>
  </verify>
  <done>36-UI-SPEC.md contains a "Live Price Visibility (GAP-4…)" section forbidding the _kioskModifyPanelOpen gate and mandating a server-authoritative prominent price updated on every change (with BrewPad excluded per D-10), and a "Scroll Model (GAP-5)" section specifying the kiosk-recipe-prompt scroll container + the BrewPad detail-pane injection; the recommended element order is captured.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Audit Polish contract section + update the checker sign-off note (GAP-7)</name>
  <files>.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md</files>
  <read_first>
    - 36-UI-REVIEW.md §"Consolidated Fix List" (Critical C1-C5, High H1-H6, Medium M1-M4, Low L1-L3, lines 229-265)
    - 36-UI-SPEC.md existing §"Spacing Scale" touch-target exceptions (lines 47-49), §"Typography" iOS guard (line 66), §"Color" (price-preview tokens), §"Copywriting Contract" (× factor label, line 424)
  </read_first>
  <action>
    Append a new top-level section "## Audit Polish (GAP-7 — from 36-UI-REVIEW.md)" that records the binding fixes as a checklist the surface plans must satisfy, grouped by surface where relevant:
    - Touch targets: `.bp-ing-remove` MUST be `min-height:44px; min-width:44px` (currently 36px, C5); `.admin-btn-sm` save-as-new on the admin kiosk surface MUST meet 44px (H4) — prefer `.btn-secondary` over `admin-btn-sm`; modify-row `.admin-input` (ing-search/ing-qty) MUST be `min-height:44px` (H3).
    - iOS zoom guard: modify-row inputs on the admin kiosk surface MUST be `font-size:1rem` (C4) — via `#tab-kiosk .admin-input { font-size:1rem }` or inline; admin kiosk volume inputs likewise (M4).
    - Price-preview card: `#kiosk-recipe-price-preview` MUST get a CSS class with background `--cellar-raised`, border `1px solid --ledger-line`, border-radius `--r-md`, padding `--sp-3 --sp-4` (H2) — a real card, not raw inline text.
    - Element order: `#kiosk-save-as-new-wrap` MUST appear BELOW `#kiosk-add-recipe-to-cart` on admin.html (currently before sale-type buttons, H1).
    - Empty state: NEVER render `<tr>` rows in the modify table while collapsed; the single placeholder row is the only empty-state (no phantom greyed inputs) — restate the existing GAP-2 rule and tie it to GAP-7.
    - Color: kiosk autocomplete dropdown MUST use the `.ing-autocomplete-drop` class (cellar-raised + `--cellar-border`), not hardcoded `#fff`/`#ccc` (H5/L1).
    - Spacing tokens: add `--sp-*` definitions to kiosk.css `:root` (M2); `#kiosk-recipe-volume-wrap` margin-bottom `--sp-6` + gap before modify-wrap (H6/M3).
    - Copy: remove the trailing colon from the BrewPad `× factor:` label (M1) so it matches "× factor" on all surfaces.
    - Group-header styling extracted to a CSS class on admin/kiosk (L3) — discretionary, note as "should".

    Update the "Checker Sign-Off" / Approval note at the bottom of the spec to record that the second-pass gap-closure extension (GAP-4/5/7 live-price + scroll + audit polish) was appended on 2026-06-22, leaving the dimension checkboxes for the ui-checker.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && grep -q "Audit Polish (GAP-7" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "bp-ing-remove" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "kiosk-save-as-new-wrap" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && echo PASS</automated>
  </verify>
  <done>36-UI-SPEC.md has an "Audit Polish (GAP-7…)" section enumerating the binding C/H fixes (44px targets incl. bp-ing-remove, font-size:1rem modify inputs, price-preview card, save-as-new placement, empty-state, off-palette autocomplete, × factor colon) and the Approval note records the second-pass extension date.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → 36-UI-SPEC.md | Documentation only; no executable surface. The risk is a contract that weakens the server-authoritative money rule. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-13-01 | Tampering | live-price contract | mitigate | The spec MUST state the now-prominent price is the SERVER quote total (D-06), never client math; surface plans inherit this. Without it, an executor could compute the prominent price client-side and break displayed==charged. |
| T-36-13-02 | Information disclosure | BrewPad price | mitigate | The spec restates D-10 (no price/quote/charge on BrewPad) so 36-16 does not add a price element to a non-money surface. |
| T-36-13-SC | Tampering | npm installs | accept | Doc-only plan; no package installs, no build. No legitimacy gate needed. |
</threat_model>

<verification>
- 36-UI-SPEC.md gains "Live Price Visibility (GAP-4…)", "Scroll Model (GAP-5)", and "Audit Polish (GAP-7…)" sections.
- The live-price section forbids the `_kioskModifyPanelOpen` gate and asserts server-authoritative pricing (D-06); BrewPad excluded (D-10).
- The scroll section specifies the kiosk-recipe-prompt scroll container and the BrewPad detail-pane injection.
- No source files modified; no build; no tests required (doc-only) — but the surface plans that follow will run the full CLAUDE.md pre-commit gate.
</verification>

<success_criteria>
36-UI-SPEC.md is the single authoritative contract for the second-pass fixes: a server-authoritative prominent live price updated on every change (GAP-4), a scroll model that keeps the full list + action buttons reachable on iPad for sale surfaces and BrewPad (GAP-5), and the audit polish checklist (GAP-7) — all consistent across admin/kiosk/BrewPad (D-01) with BrewPad's no-price/no-charge exception preserved (D-10).
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-13-SUMMARY.md` when done.
</output>
</content>
</invoke>
