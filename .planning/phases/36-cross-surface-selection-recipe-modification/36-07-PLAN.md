---
phase: 36-cross-surface-selection-recipe-modification
plan: 07
type: execute
wave: 4
depends_on: ["36-02", "36-04", "36-05", "36-06"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [SEL-01, SEL-02, MOD-01, MOD-02, MOD-03]
must_haves:
  truths:
    - "On staging, the same target-volume control appears and validates identically on admin, kiosk, and BrewPad attach (SEL-01, D-01)"
    - "A modified sale is priced server-authoritatively (displayed==charged) and the carried-through volume + modified list reach the invoice, snapshot, and batch record with no re-entry (SEL-02, MOD-02)"
    - "The locked-remove no-credit asymmetry (D-08) is understood and acknowledged by the owner; save-as-new creates a draft without touching the original (MOD-03)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "SEL-01/SEL-02/MOD-01/MOD-02/MOD-03 traceability rows flipped to Complete after UAT"
      contains: "SEL-01"
  key_links:
    - from: "staging UAT sign-off"
      to: "REQUIREMENTS.md traceability"
      via: "owner approval of cross-surface behavior"
      pattern: "Complete"
---

<objective>
Deploy the Phase 36 work to staging and run a human cross-surface UAT covering all three surfaces, the carry-through chain, the modified-price server-authoritative guarantee, the locked-remove asymmetry (D-08), and save-as-new. On owner approval, flip the SEL/MOD traceability rows in REQUIREMENTS.md to Complete.

Purpose: Phase 35's UAT could not verify locked-price scaling (only a dynamic recipe existed live) and never ran a full completed sale to the snapshot/batch. Phase 36 adds the modified-list money path and three surfaces — these need live human verification before the phase closes.
Output: Owner-approved staging verification + updated REQUIREMENTS traceability.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@.planning/phases/35-batch-scaling-engine/35-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to staging (frontend + middleware)</name>
  <read_first>
    - CLAUDE.md "Deployment" + "WORKFLOW RULE — STAGING FIRST" — staging is `git push origin main`; middleware deploys to the prod Railway instance (no staging middleware; staging site calls prod middleware — STATE.md decision)
    - .planning/phases/35-batch-scaling-engine/35-06-SUMMARY.md "Deploy Requirements" — the two-deploy pattern (Railway middleware + staging frontend) and graceful degradation
  </read_first>
  <action>
    Ensure all Phase 36 plans (36-01..36-06) are merged and the bundles (admin.min.js, kiosk.min.js, brewpad.min.js) are rebuilt. Run `npm test` and `cd zoho-middleware && npm test` and `npm run lint` — all must pass before deploy. Deploy the middleware (pos-recipe modified-list routes + brewpad-integration carry-through) via `railway up` from zoho-middleware/. Deploy the frontend to staging via `git push origin main`. Confirm the Apps Script create_batch redeploy from 36-02 is done (target_volume_l persists). Verify CNAME is the staging value before any push (project rule).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm test >/dev/null 2>&1 && cd zoho-middleware && npm test >/dev/null 2>&1 && echo SUITES-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - The command prints SUITES-GREEN (both suites pass before deploy).
    - `npm run lint` exits 0.
    - Staging frontend reflects the new bundles (cache-bust stamps updated) and the middleware /api/kiosk/recipe-quote accepts modified_ingredients (spot-check returns is_modified:true for a modified request).
  </acceptance_criteria>
  <done>Both test suites green, lint clean, middleware + staging frontend deployed, Apps Script redeploy confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full Phase 36 feature on staging: the target-volume control on admin/kiosk/BrewPad-attach (SEL-01); ingredient add/remove/substitute on all three (MOD-01); server-authoritative modified pricing on admin/kiosk (MOD-02); soft stock advisory + no-charge snapshot on BrewPad attach (D-10/D-11); carry-through into invoice/snapshot/batch (SEL-02); save-as-new on admin + BrewPad (MOD-03). NOTE: the locked-price increase could not be verified in Phase 35 because only a dynamic recipe was live — to test D-07/D-08 you need a locked-price recipe (set an existing recipe to locked pricing with a locked_price, or create one).
  </what-built>
  <how-to-verify>
    1. SEL-01 / D-01 — Open admin.html?tab=kiosk, kiosk.html, and a BrewPad batch detail attach panel. Select a recipe on each. Confirm the SAME "Target volume (L)" control appears with the identical "1.5x base N L" readout and the same no-base disabled message. The control must look and validate identically on all three.
    2. MOD-01 / D-03/D-04/D-05 — On each surface, expand "Modify Ingredients": remove a line, change a qty, and add an ingredient via autocomplete (substitute = remove then add). Confirm edits are at base quantities and the original recipe in the catalogue is unchanged afterward.
    3. MOD-02 / D-06 — On admin + kiosk, watch the price preview update after edits (server quote). Confirm the Add-to-Cart label shows "(Modified)" and the previewed total equals the amount the terminal would charge (run one completed modified sale and confirm the charged total matches the preview — displayed==charged).
    4. D-07 / D-08 (locked recipe) — Using a LOCKED-price recipe: ADD an ingredient and confirm the price goes UP by the added scaled line. REMOVE an ingredient and confirm the price does NOT go down (no credit). Acknowledge this asymmetry is intentional and owner-approved.
    5. SEL-02 — Complete one in-store modified recipe sale. Confirm the Zoho invoice line items reflect the scaled+modified ingredients, the created batch record shows the chosen target volume (not the base) without anyone re-entering it, and the recipe_snapshot carries modified_base_ingredients + is_modified.
    6. D-10 / D-11 (BrewPad attach) — On a BrewPad batch, attach a recipe at a scaled volume with a modification. Confirm: NO price is shown and NO charge happens; the stock advisory appears as a soft warning when scaled quantities exceed stock but the Attach button stays enabled; after attach the batch snapshot carries the scaled+modified ingredients + target volume.
    7. MOD-03 / D-12/D-13/D-14 — On admin and BrewPad, tap "Save as new recipe", name it, Save Draft. Confirm a new draft (inactive, dynamic) recipe appears with the BASE (pre-scale) quantities, the original recipe is untouched, and the new draft must pass the activation guardrail before going live.
    8. iPad Safari — Confirm kiosk + BrewPad inputs do not trigger iOS auto-zoom and all touch targets are comfortably tappable.
  </how-to-verify>
  <resume-signal>Type "approved" once all eight checks pass on staging (explicitly confirm the locked-remove no-credit asymmetry is understood), or describe any defect to route into a gap-closure plan.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Flip REQUIREMENTS traceability to Complete</name>
  <read_first>
    - .planning/REQUIREMENTS.md lines 67-83 (traceability table) — the SEL-01/SEL-02/MOD-01/MOD-02/MOD-03 rows to update
  </read_first>
  <action>
    After owner approval only: update the REQUIREMENTS.md traceability table — set SEL-01, SEL-02, MOD-01, MOD-02, MOD-03 (Phase 36) Status to Complete, and check their requirement checkboxes in the v4.3 Requirements list. Do NOT touch other rows.
  </action>
  <verify>
    <automated>grep -E "SEL-01|SEL-02|MOD-01|MOD-02|MOD-03" /Users/koa/dev/steins-and-vines-website/.planning/REQUIREMENTS.md | grep -c Complete</automated>
  </verify>
  <acceptance_criteria>
    - The grep prints 5 (all five Phase 36 requirement rows marked Complete).
    - Other traceability rows (RDISP/SCALE/BPR) are unchanged.
  </acceptance_criteria>
  <done>All five SEL/MOD requirements marked Complete in REQUIREMENTS.md after owner UAT approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staging deploy -> live middleware | Staging frontend calls the prod Railway middleware (no staging instance); a bad deploy could affect the live money path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-21 | Tampering | deploying untested money-path code | mitigate | Task 1 gates the deploy on both test suites green + lint clean before `railway up` / `git push origin main`; CI also gates (Phase 33). |
| T-36-22 | Denial of Service | locked-pricing regression unverified | mitigate | The UAT explicitly requires a LOCKED-price recipe to exercise D-07/D-08 live (the Phase 35 follow-up that could not be verified). The checkpoint blocks on owner confirmation of the asymmetry. |
| T-36-SC | Tampering | npm/pip installs | accept | No new packages in this plan. No package legitimacy gate required. |
</threat_model>

<verification>
- Both test suites green + lint clean before deploy.
- Owner sign-off on all eight UAT checks (blocking).
- REQUIREMENTS traceability updated only after approval.
</verification>

<success_criteria>
All three surfaces verified on staging with identical controls, modified sales priced server-authoritatively (displayed==charged), the locked-remove asymmetry acknowledged, carry-through into invoice/snapshot/batch confirmed, save-as-new creating an untouched-original draft, and the five SEL/MOD requirements marked Complete.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-07-SUMMARY.md` when done.
</output>
