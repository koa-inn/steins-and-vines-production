---
phase: 36-cross-surface-selection-recipe-modification
plan: 11
type: execute
wave: 4
depends_on: [36-08, 36-09]
files_modified:
  - brewpad.html
  - js/brewpad.js
  - js/brewpad.min.js
  - js/admin.min.js
  - js/kiosk.min.js
  - js/main.min.js
  - js/batch.min.js
  - css/brewpad.css
  - css/brewpad.min.css
  - css/admin.min.css
  - css/styles.min.css
  - css/batch.min.css
  - css/kiosk.min.css
  - tests/frontend/brewpad-recipe-attach-modify.test.js
  - tests/frontend/brewpad-recipe-volume-factor.test.js
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01]
must_haves:
  truths:
    - "GAP-3: the BrewPad attach volume control has a synced ×factor input identical to admin/kiosk; both sync directions work with bounds + no-base disable (SEL-01, D-01)"
    - "GAP-2: the BrewPad modify panel pre-populates base ingredients on expand (edit-at-base, D-04) and uses the polished/reordered layout consistent with the sale surfaces, with NO price preview (D-10)"
    - "The ×factor math is client-side display only and BrewPad attach never calls a quote/charge — the factor only changes the displayed/recorded target_volume_l (D-10)"
  artifacts:
    - path: "brewpad.html"
      provides: "× factor input markup beside #bp-target-volume (.bp-input, iOS guard)"
      contains: "bp-target-factor"
    - path: "js/brewpad.js"
      provides: "Ported synced factor wiring (display/record only, no quote) + polished layout"
      contains: "bp-target-factor"
    - path: "tests/frontend/brewpad-recipe-volume-factor.test.js"
      provides: "factor↔litres sync + bounds + no-base + no-charge tests for BrewPad"
  key_links:
    - from: "js/brewpad.js factor input handler"
      to: "_bpTargetVolumeL + scale-factor readout"
      via: "litres recompute, no quote/charge (D-10)"
      pattern: "_bpTargetVolumeL"
---

<objective>
Port the synced ×factor control and the polished modify-panel layout to the BREWPAD recipe-attach surface (D-01 — identical control on all three surfaces), respecting BrewPad's record-keeping-only semantics (D-10: no price preview, no quote, no charge):
- GAP-3 — synced "× factor" input beside Target volume (L), two-way synced, using the `.bp-input` class + iOS zoom guard. The factor only changes the displayed/recorded target_volume_l; it never triggers a quote or charge.
- GAP-2 — polished/reordered volume + modify-panel layout consistent with the sale surfaces (minus the price preview, D-10); confirm base ingredients pre-populate on expand (edit-at-base, D-04 — already wired at brewpad.js:4094-4118).

Note: GAP-1 (catalog-on-open) does NOT apply to BrewPad — brewpad.js:4036 already calls `loadIngredientCatalogForRecipes()` on attach-panel expand (per UAT scope_correction).

Purpose: keep the control consistent with the admin reference (36-09) while preserving the no-charge attach path.
Output: brewpad.html markup, brewpad.js wiring, brewpad.css polish, rebuilt bundles, and a new factor test (+ any needed extension to the existing attach-modify test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-09-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Real, current brewpad.js anchors (verified). Mirror the admin (36-09) factor math, NO quote. -->

brewpad.js wireAttachExpandedPanel (the Phase 36 attach control wiring):
  ~line 4026: function wireAttachExpandedPanel(b, sectionBodyEl)
  ~line 4036: loadIngredientCatalogForRecipes().then(...)   // catalog already loaded on open
  ~line 4046: var volWrap = document.getElementById('bp-recipe-volume-wrap');
  ~line 4047: var volInput = document.getElementById('bp-target-volume');
  ~line 4048: var factorRdout = document.getElementById('bp-scale-factor-readout');
  ~line 4052: _bpTargetVolumeL = baseVol > 0 ? baseVol : null;
  ~line 4072: volInput.oninput → _bpTargetVolumeL = val>0?val:null; recompute factor + readout
  ~line 4094-4118: on first modify-expand, deep-copy base ingredients into _bpModifiedIngredients (D-04)

brewpad.js state (~line 1243-1245):
  var _bpTargetVolumeL = null;
  var _bpModifiedIngredients = null;
  _bpResolvedRecipe (resolved recipe object; .ingredients = base list, .batch_size_l = base vol)

brewpad.js snapshot build (~line 1257-1291): scaled(_bpModifiedIngredients||base, factor),
  target_volume_l from _bpTargetVolumeL, modified_base_ingredients, is_modified —
  written via update_batch (adminApiPost) with NO pricing/charge (D-10).

BrewPad uses .bp-input class (NOT .admin-input); brewpad.css already has font-size:1rem (iOS guard).
NO price preview on BrewPad (D-10) — do not add a quote call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the synced ×factor input + polished layout to BrewPad attach (markup + wiring + CSS + tests), no quote/charge</name>
  <files>brewpad.html, js/brewpad.js, css/brewpad.css, tests/frontend/brewpad-recipe-attach-modify.test.js, tests/frontend/brewpad-recipe-volume-factor.test.js</files>
  <read_first>
    - 36-09-SUMMARY.md (admin factor handler to port — swap to .bp-input, _bp state, and REMOVE the re-quote call)
    - 36-UI-SPEC.md §"Synced ×factor Control" and §"3. BrewPad Recipe-Attach Surface" (no price preview, D-10)
    - brewpad.html `#bp-recipe-volume-wrap` block
    - js/brewpad.js lines 4026-4130 (wireAttachExpandedPanel: volume wiring + modify toggle + deep-copy base ingredients) and lines 1257-1310 (snapshot scaling using _bpTargetVolumeL)
    - css/brewpad.css `.bp-input` / `.bp-scale-readout` rules + existing font-size:1rem guard
  </read_first>
  <behavior>
    - Test BFAC-1 (factor → litres): factor 1.5 on a base-60L recipe sets `_bpTargetVolumeL` to 90 and the litres input value to 90.
    - Test BFAC-2 (litres → factor): litres 90 on base-60L sets the factor input value to "1.50".
    - Test BFAC-3 (bounds): factor ≤ 0 rejected; factor > 10 clamped so litres ≤ base×10.
    - Test BFAC-4 (no-base disable): no batch_size_l disables BOTH litres and factor inputs and shows the "Set batch size (L)…" copy.
    - Test BFAC-5 (no quote/charge, D-10): editing the factor does NOT call any /api/kiosk/recipe-quote, /recipe-sale, or Helcim path — it only updates _bpTargetVolumeL + readout (so the attached snapshot records the chosen volume).
    - Test BFAC-6 (snapshot carries volume): after setting factor 1.5 on base-60L, the snapshot/scale path reads target_volume_l = 90 (the recorded volume reflects the factor edit).
  </behavior>
  <action>
    1. brewpad.html: inside `#bp-recipe-volume-wrap`, add `<input type="number" id="bp-target-factor" class="bp-input" min="0.1" step="0.1" inputmode="decimal" />` with a static "× factor" label, RIGHT of `#bp-target-volume` on the same row. (BrewPad uses .bp-input which already carries font-size:1rem — no inline style needed.) Keep `#bp-scale-factor-readout` beneath the row.
    2. css/brewpad.css: mirror the admin/kiosk flex one-row layout (litres + factor side-by-side, readout beneath, 44px min-height). Apply the reordered/polished layout from the UI-SPEC for BrewPad (volume+factor row → readout → Modify toggle → rows → +Add → soft stock advisory → Attach button → save-as-new). NO price-preview element on BrewPad (D-10).
    3. js/brewpad.js wireAttachExpandedPanel (~line 4046+): add `var factorInput = document.getElementById('bp-target-factor');`. Port the admin factor handler but REMOVE the re-quote call (BrewPad has no quote, D-10): on resolve with baseVol>0 set factorInput.value='1.0', disabled=false, max=10; no-base ⇒ disabled=true (BFAC-4). `factorInput.oninput` clamps factor to (0,10], computes litres = nearest-0.5(factor*baseVol), writes volInput, updates `_bpTargetVolumeL` + `_bpScaleFactor`(if present)/the readout string — and does NOTHING else (no fetch). Extend the existing litres `oninput` to also set `factorInput.value = factor.toFixed(2)` (BFAC-2). Add the comment: "display/record only — BrewPad attach never charges (D-10); factor only changes the recorded target_volume_l". ES5/var, escapeHTML for dynamic text.
    4. Create tests/frontend/brewpad-recipe-volume-factor.test.js implementing BFAC-1..BFAC-6, reusing the require + DOM-fixture pattern from tests/frontend/brewpad-recipe-attach-modify.test.js. For BFAC-5 stub global.fetch + adminApiPost with jest.fn and assert NEITHER a recipe-quote/recipe-sale URL nor a Helcim call is made on factor edit. For BFAC-6, drive the snapshot/scale helper and assert target_volume_l reflects the factor-derived litres.
    5. If the existing brewpad-recipe-attach-modify test needs the new factor node in its fixture to keep passing, add only the node — do NOT change existing assertions (CLAUDE.md rule 10).
    6. Run `npm test -- brewpad-recipe-volume-factor brewpad-recipe-attach-modify` and `npm run lint`.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/brewpad-recipe-volume-factor.test.js 2>&1 | grep -qE "BFAC-1|Tests:.*passed" && grep -q "bp-target-factor" brewpad.html && grep -q "bp-target-factor" js/brewpad.js && echo PASS</automated>
  </verify>
  <done>brewpad.html has the #bp-target-factor input (.bp-input) beside the litres box; brewpad.js syncs factor↔litres both directions with bounds + no-base disable and NO quote/charge; brewpad.css renders the polished layout consistent with the sale surfaces minus the price preview (D-10); BFAC-1..BFAC-6 pass; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build BrewPad bundles and run full frontend suite</name>
  <files>js/brewpad.min.js, js/admin.min.js, js/kiosk.min.js, js/main.min.js, js/batch.min.js, css/brewpad.min.css, css/admin.min.css, css/styles.min.css, css/batch.min.css, css/kiosk.min.css</files>
  <read_first>
    - package.json "build" script (regenerates ALL min files)
    - CLAUDE.md "Build Artifacts" + "Before Every Commit" rules
  </read_first>
  <action>
    Run `npm run build` to regenerate the bundles (never hand-edit *.min.js). The build rewrites all min outputs — expected. Then run the FULL frontend suite `npm test` and `npm run lint`. Do NOT touch the middleware (money path frozen).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build >/dev/null 2>&1 && grep -q "bp-target-factor" js/brewpad.min.js && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>`npm run build` succeeds; brewpad.min.js contains the factor wiring; the full frontend Jest suite passes; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrewPad iPad browser → update_batch (Apps Script via adminApiPost) | Attach writes a scaled+modified recipe_snapshot with target_volume_l; NO pricing or charge crosses this boundary (D-10). |
| BrewPad iPad browser → /api/ingredients | Catalog fetch for the modify-panel autocomplete (already loaded on attach-panel open). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11-01 | Tampering | ×factor → charge | mitigate | BrewPad attach must never call a quote/sale/Helcim path; the factor only updates the recorded target_volume_l (BFAC-5 asserts no quote/charge fetch). This preserves D-10. |
| T-36-11-02 | Tampering / XSS | modify-row ingredient names on BrewPad | mitigate | Row rendering reuses the existing escapeHTML path; the static "× factor" label needs no escaping. |
| T-36-11-03 | Tampering | recorded target_volume_l in snapshot | mitigate | The factor-derived litres flow into the existing snapshot scaling path (BFAC-6); no new write surface — reuses the verified update_batch snapshot. |
| T-36-11-SC | Tampering | npm installs | accept | No new package installs — only existing devDeps via npm run build/test. No legitimacy gate needed. |
</threat_model>

<verification>
- `npx jest tests/frontend/brewpad-recipe-volume-factor.test.js` passes (BFAC-1..BFAC-6), including the no-quote/no-charge assertion (D-10).
- `grep bp-target-factor` matches in brewpad.html, js/brewpad.js, js/brewpad.min.js.
- `npm test` full frontend suite green; `npm run lint` clean.
- Middleware untouched; no quote/charge introduced on the attach path.
</verification>

<success_criteria>
The BrewPad recipe-attach surface has a synced ×factor control identical to admin/kiosk (both sync directions, bounds, no-base disable) that only changes the displayed/recorded target_volume_l — never a quote or charge (D-10) — plus the polished/reordered modify-panel layout with base-ingredient pre-population and no price preview. Bundles rebuilt; full frontend suite green.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-11-SUMMARY.md` when done.
</output>
