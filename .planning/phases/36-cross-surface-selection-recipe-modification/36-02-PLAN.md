---
phase: 36-cross-surface-selection-recipe-modification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/brewpad-integration.js
  - zoho-middleware/__tests__/brewpad-integration.test.js
autonomous: false
requirements: [SEL-02]
must_haves:
  truths:
    - "When a scaled recipe sale creates a batch, the batch payload carries target_volume_l and scale_factor derived from the frozen recipe_snapshot — staff never re-enter the volume (SEL-02, D-02)"
  artifacts:
    - path: "zoho-middleware/lib/brewpad-integration.js"
      provides: "detectRecipeSale forwards target_volume_l + scale_factor from the snapshot onto the create_batch payload"
      contains: "target_volume_l"
    - path: "zoho-middleware/__tests__/brewpad-integration.test.js"
      provides: "Test proving detectRecipeSale puts snapshot.target_volume_l on the batch payload"
      contains: "target_volume_l"
  key_links:
    - from: "zoho-middleware/lib/brewpad-integration.js detectRecipeSale"
      to: "Apps Script create_batch"
      via: "batchPayload.target_volume_l"
      pattern: "target_volume_l"
---

<objective>
Close the SEL-02 carry-through gap: the recipe_snapshot already carries `target_volume_l` and `scale_factor` (Phase 35, pos-recipe.js confirm handler lines 507-517), but `detectRecipeSale` does NOT forward those fields onto the `create_batch` payload — so the auto-created batch record does not persist the chosen volume. Add them to the batch payload and flag the matching Apps Script `create_batch` field-persistence change as a human-action checkpoint (Apps Script changes are not in CI and require manual redeploy — STATE.md blocker).

Purpose: SEL-02 requires the chosen batch size to flow into the created batch record without re-entry. Today it stops at the snapshot.
Output: Extended batch payload + test + a human-action checkpoint to redeploy Apps Script with the new persisted fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md

<interfaces>
From zoho-middleware/lib/brewpad-integration.js (lines 380-396):
detectRecipeSale(recipeId, recipeSnapshot, invoiceNumber, customerName, contactId) builds batchPayload and calls callAppsScriptCreateBatch(batchPayload). recipeSnapshot already contains: name, style, abv, locked_price, service_fee, materials_fee, target_volume_l, scale_factor, ingredients (scaled). callAppsScriptCreateBatch (lines 68-112) POSTs { ...batchPayload, action:'create_batch', server_token }.

Snapshot source (pos-recipe.js confirm, lines 507-517) — target_volume_l: targetVolumeLConfirm, scale_factor: scaleFactorConfirm are present on the snapshot object passed to detectRecipeSale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Forward target_volume_l + scale_factor onto the batch payload (test-first)</name>
  <read_first>
    - zoho-middleware/lib/brewpad-integration.js lines 368-408 (detectRecipeSale + module.exports) — the batchPayload object to extend
    - zoho-middleware/lib/brewpad-integration.js lines 68-112 (callAppsScriptCreateBatch) — confirm payload pass-through and retry-queue behavior
    - zoho-middleware/__tests__/brewpad-integration.test.js (existing) — match its mocking of callAppsScriptCreateBatch / axios and its detectRecipeSale test style
    - zoho-middleware/routes/pos-recipe.js lines 507-525 — confirm the snapshot already carries target_volume_l/scale_factor before detectRecipeSale is called
  </read_first>
  <behavior>
    - Given a recipeSnapshot with target_volume_l=30 and scale_factor=1.5, detectRecipeSale must include target_volume_l:30 and scale_factor:1.5 on the batchPayload passed to callAppsScriptCreateBatch.
    - When the snapshot omits target_volume_l (legacy/unscaled sale), the payload sets target_volume_l to null (or recipeSnapshot.batch_size_l fallback) and does NOT throw.
    - Existing detectRecipeSale fields (product_sku, recipe_id, zoho_so_number, customer_* ) remain unchanged.
  </behavior>
  <action>
    Write the test first (RED): add an it() to brewpad-integration.test.js that spies/mocks callAppsScriptCreateBatch, calls detectRecipeSale with a snapshot carrying target_volume_l=30, scale_factor=1.5, and asserts the captured payload has those two fields. Then implement (GREEN): in detectRecipeSale, add to batchPayload — target_volume_l: (recipeSnapshot && recipeSnapshot.target_volume_l) || null and scale_factor: (recipeSnapshot && recipeSnapshot.scale_factor) || null (per PATTERNS lines 694-696). Keep the call fire-and-forget (.catch(function(){})). Do not change callAppsScriptCreateBatch.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website/zoho-middleware && npx jest brewpad-integration.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "target_volume_l" zoho-middleware/lib/brewpad-integration.js` shows the field inside the detectRecipeSale batchPayload.
    - `cd zoho-middleware && npx jest brewpad-integration.test.js` green, including the new assertion that the payload carries target_volume_l=30 and scale_factor=1.5.
    - `cd zoho-middleware && npm test` full suite green.
  </acceptance_criteria>
  <done>detectRecipeSale forwards target_volume_l + scale_factor; test proves it; full middleware suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>The middleware now sends target_volume_l + scale_factor on the create_batch payload. For these to persist onto the batch row, the Apps Script create_batch handler must accept and write the two new fields to the Batches sheet (Apps Script changes are NOT in CI — STATE.md blocker: manual redeploy required).</what-built>
  <how-to-verify>
    1. Open the Apps Script project (adminApi.gs) bound to the Batches Google Sheet.
    2. In the create_batch handler, add target_volume_l and scale_factor to the accepted/written field set (mirror an existing numeric field like batch_size_l). If a Batches column does not exist for target_volume_l, add the column header.
    3. Deploy the Apps Script (Deploy -> Manage deployments -> redeploy the active Web App).
    4. From a staging recipe sale at a non-1x volume, confirm the created batch row shows target_volume_l populated (e.g. 30) without anyone re-typing it.
  </how-to-verify>
  <resume-signal>Type "deployed" once the Apps Script create_batch persists target_volume_l and a staging batch row shows the carried-through volume, or describe what blocked it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pos-recipe confirm -> detectRecipeSale -> Apps Script | Server-computed snapshot values (not client-supplied) flow to the batch record; target_volume_l originates from the server-validated confirm handler, not the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-04 | Tampering | batch target_volume_l | mitigate | target_volume_l is read from the server-built recipe_snapshot (pos-recipe confirm), never from a client field on the batch request. detectRecipeSale only ever receives the server snapshot object. |
| T-36-05 | Repudiation | batch provenance | accept | source:'kiosk_recipe' + zoho_so_number already stamp provenance; no change needed. |
| T-36-SC | Tampering | npm/pip installs | accept | No new packages. No package legitimacy gate required. |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest brewpad-integration.test.js` green.
- `cd zoho-middleware && npm test` full suite green; `npm run lint` clean.
- Human-action: Apps Script redeployed and a staging batch row shows carried-through target_volume_l.
</verification>

<success_criteria>
SEL-02 carry-through complete end-to-end: snapshot -> batch payload -> persisted batch field, with no re-entry of the volume at the batch step.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-02-SUMMARY.md` when done.
</output>
