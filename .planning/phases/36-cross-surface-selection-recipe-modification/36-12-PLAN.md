---
phase: 36-cross-surface-selection-recipe-modification
plan: 12
type: execute
wave: 5
depends_on: [36-09, 36-10, 36-11]
files_modified:
  - .planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements: [SEL-01, MOD-01]
must_haves:
  truths:
    - "The three gap-closure surface changes (admin/kiosk/BrewPad) are deployed to staging for a second UAT pass"
    - "A human re-verifies GAP-1 (catalog loads on panel open), GAP-2 (clean/consistent layout + edit-at-base pre-population), GAP-3 (synced ×factor on all three surfaces) on staging"
    - "The displayed==charged guarantee still holds end-to-end and BrewPad attach still never charges (D-10) after the UI changes"
  artifacts:
    - path: ".planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md"
      provides: "Re-UAT results recording GAP-1/2/3 closure (or new gaps)"
      contains: "Re-UAT"
  key_links:
    - from: "staging deploy"
      to: "human UAT on three surfaces"
      via: "git push origin main → staging.steinsandvines.ca"
      pattern: "staging"
---

<objective>
Deploy the gap-closure changes (36-09/10/11) to staging and run a focused second human UAT pass on the three closed gaps. This makes the re-UAT explicit (per the orchestrator's end-state requirement) so the owner can confirm GAP-1/2/3 are resolved before the phase is signed off.

Purpose: gaps were UI/UX issues that can only be confirmed in a browser/on the iPad; an automated suite cannot prove the layout is clean or the controls feel consistent across surfaces.
Output: a deploy to staging and recorded re-UAT results in 36-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gate — full frontend suite + middleware regression gate + lint green</name>
  <files>(verification only)</files>
  <read_first>
    - CLAUDE.md "Before Every Commit" (rule 1: run npm test AND cd zoho-middleware && npm test; never commit/deploy failing tests; rule 2: lint clean)
  </read_first>
  <action>
    Confirm the working tree from 36-09/10/11 is clean and all gates pass before pushing to staging. Run `npm test` (full frontend Jest), `npm run lint`, and the middleware regression gate `cd zoho-middleware && npm test` (required by CLAUDE.md rule 1 before any commit/deploy; expected to PASS unchanged since no middleware files were touched — do NOT modify middleware or author new middleware tests). If anything fails, STOP and report rather than deploying.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm test 2>&1 | grep -qE "Tests:.*passed" && npm run lint >/dev/null 2>&1 && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>Full frontend suite passes, the middleware regression gate (`cd zoho-middleware && npm test`) passes unchanged, and lint is clean; the tree is ready to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to staging</name>
  <files>(deploy only)</files>
  <read_first>
    - CLAUDE.md "Deployment" + "WORKFLOW RULE — STAGING FIRST" (staging = git push origin main; never push production without approval; CNAME is gitignored / managed by enforce-cname.yml)
  </read_first>
  <action>
    Commit the gap-closure changes (if not already committed by the surface plans) and deploy to STAGING ONLY: `git push origin main` → staging.steinsandvines.ca. Do NOT push to production (the production push is a separate, human-approved step after this UAT passes). After pushing, CONFIRM the push actually succeeded — do not rely on counting unpushed commits alone — by asserting that the remote staging head now matches the local HEAD (`git ls-remote origin -h refs/heads/main` returns the same SHA as `git rev-parse HEAD`). A rejected/failed push (non-fast-forward, auth error) must be caught here and reported, not silently treated as success. Note the staging URL for the UAT checkpoint.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && git push origin main 2>&1 && LOCAL=$(git rev-parse HEAD) && REMOTE=$(git ls-remote origin -h refs/heads/main | awk '{print $1}') && echo "local=$LOCAL remote=$REMOTE" && [ "$LOCAL" = "$REMOTE" ] && [ "$(git log origin/main..HEAD --oneline 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && echo PUSH_CONFIRMED</automated>
  </verify>
  <done>`git push origin main` completed successfully and the staging remote head (`git ls-remote origin -h refs/heads/main`) matches the local HEAD SHA, proving the push landed (a rejected push is caught, not masked); nothing is pushed to production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Gap-closure changes for Phase 36 are live on staging:
    - GAP-1: the admin modify-panel ingredient autocomplete now loads its catalog on panel open (no Recipes-tab prerequisite).
    - GAP-2: the volume + modify-panel region is polished and reordered, consistent across admin / kiosk / BrewPad, with base ingredients pre-populated as editable rows on expand (edit-at-base).
    - GAP-3: a synced "× factor" input sits beside the Target volume (L) box on all three surfaces, two-way synced.
  </what-built>
  <how-to-verify>
    On staging.steinsandvines.ca (use the in-store iPad for kiosk + BrewPad where possible):

    1. GAP-1 (admin, fresh load): Open admin.html?tab=kiosk WITHOUT first visiting the Recipes tab. Select a recipe → expand "Modify Ingredients" → focus the ingredient search field. Expected: the autocomplete lists catalog items immediately (no empty dropdown).
    2. GAP-3 sync (each of admin, kiosk, BrewPad): Select a recipe with a base batch size. Type 1.5 in "× factor" → the Target volume (L) box updates to 1.5 × base and the "1.5× base N L" readout matches. Then type a litres value → the factor box updates to litres ÷ base. Try factor 0 / a huge factor → bounds hold (no negative/absurd litres). Try a recipe with NO base size → BOTH inputs are disabled with the "Set batch size (L)…" message.
    3. GAP-2 layout (each surface): Confirm the volume + factor inputs sit cleanly on one row with the readout beneath; the "Modify Ingredients" panel is collapsed by default (no phantom empty row); on expand the base ingredients appear as editable rows; the "+ Add Ingredient" affordance looks intentional; touch targets are comfortable on the iPad.
    4. Money path unchanged (admin/kiosk): make a modified sale and confirm the displayed (Modified) price equals the charged amount (displayed==charged still holds).
    5. BrewPad no-charge (D-10): attach a recipe at a scaled volume via the factor input — confirm NO price/charge appears; the attach just records the chosen volume.
    6. Record the result of each in 36-HUMAN-UAT.md (mark GAP-1/2/3 resolved, or log any new gap with surface + symptom).
  </how-to-verify>
  <resume-signal>Type "approved" if GAP-1/2/3 are resolved on all three surfaces and the money path is unchanged; otherwise describe the remaining issues (surface + symptom) for a further gap-closure cycle.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin/main (staging) | Deploy boundary; staging only, never production without explicit human approval. |
| human ↔ staging browser/iPad | UAT verification of UI behavior and the money path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-12-01 | Tampering | accidental production deploy | mitigate | This plan pushes ONLY to origin/main (staging) per CLAUDE.md STAGING-FIRST; production is a separate human-approved step. The deploy task explicitly forbids a production push. |
| T-36-12-02 | Repudiation | UAT sign-off | mitigate | Results recorded in 36-HUMAN-UAT.md with per-gap pass/fail so the owner sign-off is auditable. |
| T-36-12-03 | Tampering | silent failed staging push | mitigate | The deploy verify asserts the remote staging head SHA equals local HEAD (git ls-remote), so a rejected/non-fast-forward push is caught instead of being treated as a successful deploy. |
| T-36-12-SC | Tampering | npm installs | accept | No package installs in this plan. No legitimacy gate needed. |
</threat_model>

<verification>
- Full frontend suite + middleware regression gate + lint green before deploy.
- Changes pushed to origin/main (staging) only and the push is CONFIRMED landed (remote head SHA == local HEAD); production untouched.
- Human confirms GAP-1/2/3 resolved across admin/kiosk/BrewPad and the money path (displayed==charged, BrewPad no-charge) is unchanged; results recorded in 36-HUMAN-UAT.md.
</verification>

<success_criteria>
The gap-closure changes are live on staging (push confirmed by remote-head match) and a human has re-verified all three gaps closed (or logged precise follow-up gaps), with the money path confirmed unchanged. Phase 36 is ready for owner sign-off / production promotion.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-12-SUMMARY.md` when done, and update 36-HUMAN-UAT.md with the re-UAT results.
</output>
