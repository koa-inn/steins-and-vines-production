---
phase: 36-cross-surface-selection-recipe-modification
plan: 17
type: execute
wave: 5
depends_on: [36-14, 36-15, 36-16]
files_modified:
  - .planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements: [SEL-01, SEL-02, MOD-01, MOD-02, MOD-03]
must_haves:
  truths:
    - "The second-pass gap-closure changes (admin/kiosk/BrewPad: live price, scroll, polish) are deployed to staging for a third UAT pass"
    - "A human re-verifies GAP-4 (live price updates on volume/×factor/ingredient change with the modify panel CLOSED, displayed==charged), GAP-5 (full list + accept/Add-to-Cart/Attach reachable on iPad), GAP-6 (×factor/modify visible on admin.html?tab=kiosk after hard refresh), and GAP-7 (UI polish) on staging"
    - "The still-pending original UAT items (#1-#8 cross-surface parity, carry-through, locked add/remove asymmetry, save-as-new, iPad touch/zoom) are re-confirmed in the same pass; BrewPad attach still never charges (D-10)"
  artifacts:
    - path: ".planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md"
      provides: "Third-pass re-UAT results recording GAP-4/5/6/7 closure (or new gaps) plus the re-confirmed original items"
      contains: "Third-pass"
  key_links:
    - from: "staging deploy"
      to: "human UAT on three surfaces"
      via: "git push origin main → staging.steinsandvines.ca"
      pattern: "staging"
---

<objective>
Deploy the second-pass gap-closure changes (36-14 admin / 36-15 kiosk / 36-16 BrewPad) to staging and run a focused human UAT pass that (a) re-tests the new GAP-4/5/6/7 fixes and (b) re-confirms the still-pending original UAT items (#1-#8) that were never signed off. This plan supersedes/extends the 36-12 UAT checkpoint.

These are UI/UX + iPad-ergonomics fixes that an automated suite cannot prove — they must be confirmed in a browser/on the in-store iPad. The phase is ready for owner sign-off / production promotion only after this passes.

Purpose: make the third UAT pass explicit and auditable so the owner can confirm the second-pass blockers (live price + scroll) are resolved before promotion.
Output: a staging deploy and recorded third-pass re-UAT results in 36-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-REVIEW.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy gate — full frontend suite + middleware regression gate + lint green</name>
  <files>(verification only)</files>
  <read_first>
    - CLAUDE.md "Before Every Commit" (rule 1: npm test AND cd zoho-middleware && npm test; never deploy failing tests; rule 2: lint clean)
  </read_first>
  <action>
    Confirm the working tree from 36-14/15/16 is clean and all gates pass before pushing to staging. Run `npm test` (full frontend Jest), `npm run lint`, and the middleware regression gate `cd zoho-middleware && npm test` (required by CLAUDE.md rule 1; expected PASS unchanged — no middleware files were touched this phase). If anything fails, STOP and report rather than deploying.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm test 2>&1 | grep -qE "Tests:.*passed" && npm run lint >/dev/null 2>&1 && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>Full frontend suite passes, the middleware regression gate passes unchanged, and lint is clean; the tree is ready to deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to staging (origin/main only)</name>
  <files>(deploy only)</files>
  <read_first>
    - CLAUDE.md "Deployment" + "WORKFLOW RULE — STAGING FIRST" (staging = git push origin main; never push production without approval)
    - ⚠ CNAME CORRECTION (the CLAUDE.md "CNAME is in .gitignore" line is STALE): CNAME IS tracked in git (`git ls-files CNAME` returns it) and its current value `staging.steinsandvines.ca` is CORRECT for the origin/staging push. `enforce-cname.yml` is BROKEN (403) and does NOT auto-manage it. Do NOT untrack, delete, or modify CNAME during this deploy — untracking it 404s the site. This plan's push performs NO CNAME manipulation; leave CNAME exactly as-is.
  </read_first>
  <action>
    Commit the second-pass gap-closure changes (if not already committed by the surface plans) and deploy to STAGING ONLY: `git push origin main` → staging.steinsandvines.ca. Do NOT push to production (a separate human-approved step after this UAT passes). After pushing, CONFIRM the push actually landed — do not rely on counting unpushed commits alone — by asserting the remote staging head matches local HEAD (`git ls-remote origin -h refs/heads/main` SHA == `git rev-parse HEAD`). A rejected/failed push (non-fast-forward, auth error) must be caught here and reported, not treated as success. Note the staging URL for the UAT checkpoint.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && git ls-files CNAME | grep -q CNAME && git push origin main 2>&1 && LOCAL=$(git rev-parse HEAD) && REMOTE=$(git ls-remote origin -h refs/heads/main | awk '{print $1}') && echo "local=$LOCAL remote=$REMOTE" && [ "$LOCAL" = "$REMOTE" ] && [ "$(git log origin/main..HEAD --oneline 2>/dev/null | wc -l | tr -d ' ')" = "0" ] && echo PUSH_CONFIRMED</automated>
  </verify>
  <done>CNAME remains tracked (value staging.steinsandvines.ca, untouched); `git push origin main` completed and the staging remote head matches local HEAD SHA (a rejected push is caught, not masked); nothing pushed to production.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Second-pass gap-closure changes for Phase 36 are live on staging:
    - GAP-4: on admin + kiosk the PROMINENT recipe price now updates from the server quote on EVERY change (target volume, × factor, ingredient add/remove/qty) even with the Modify panel CLOSED — displayed == charged. BrewPad still shows NO price (D-10).
    - GAP-5: the recipe sale/modify panel now scrolls so the full ingredient list AND the accept (Ferment-in-Store / Take-Out) + Add-to-Cart buttons are reachable on long recipes; on BrewPad the expanded attach panel + Attach Recipe button render inside the scrollable detail pane.
    - GAP-6: on admin.html?tab=kiosk the × factor / Modify feature is present (rebuilt admin bundle) and the inputs no longer trigger iOS auto-zoom (font-size:1rem).
    - GAP-7: UI polish — price-preview card, 44px touch targets (incl. BrewPad Remove), cleaner spacing/order, save-as-new below Add-to-Cart, cellar-palette autocomplete, × factor label consistent.
  </what-built>
  <how-to-verify>
    On staging.steinsandvines.ca (use the in-store iPad for kiosk + BrewPad). Do a HARD REFRESH / clear cache first (GAP-6 cache guidance) before testing the admin Kiosk view.

    SECOND-PASS GAPS:
    1. GAP-4 live price (admin + kiosk, Modify panel CLOSED): select a recipe with a base size. Change the target volume → the PROMINENT price updates immediately. Change the × factor → it updates. Open Modify, add/remove an ingredient and change a qty → the prominent price updates each time. Then add to cart and confirm the displayed price equals the charged amount (displayed==charged). On a recipe with a long list, confirm no stale/base price is shown.
    2. GAP-5 scroll (admin + kiosk): on a recipe with many ingredients + Modify expanded, scroll the panel and confirm you can reach the whole list AND the sale-type + Add-to-Cart buttons. On BrewPad: expand the attach modify panel on a long recipe and confirm the Attach Recipe button is reachable (not clipped).
    3. GAP-6 (admin.html?tab=kiosk): after the hard refresh, confirm the × factor input + Modify feature ARE visible and work the same as kiosk.html; confirm focusing an input does NOT zoom the iPad.
    4. GAP-7 polish (all 3): price-preview reads as a card; touch targets feel comfortable (incl. the BrewPad Remove ×); the volume+factor row and Modify panel look clean and consistent; save-as-new (admin/BrewPad) sits below the primary action.
    5. BrewPad no-charge (D-10): attach a recipe at a scaled volume via the factor — confirm NO price/charge appears anywhere on the attach flow.

    STILL-PENDING ORIGINAL ITEMS (re-confirm in the same pass — these were never signed off):
    6. Cross-surface control parity (#1): the identical target-volume + × factor control behaves the same on admin / kiosk / BrewPad.
    7. Displayed==charged end-to-end (#2) + locked add/remove asymmetry (#3, the $125.50 worked example): adding a discrete ingredient at 1.5× increases the charge by the scaled added line; removing leaves the locked charge unchanged (owner acknowledges the intentional asymmetry).
    8. SEL-02 carry-through (#4/#5): a sale at a non-1× volume flows the volume through cart → invoice → snapshot → batch row (target_volume_l + scale_factor) with no re-entry (requires the Apps Script create_batch redeploy + the Batches sheet columns from item #5 — confirm those LIVE steps are done).
    9. Save-as-new (#7): admin + BrewPad save-as-new creates a new draft recipe; kiosk has none; original untouched.
    10. iPad touch/zoom (#8): no iOS auto-zoom on any input; modify panel + autocomplete touch-friendly.

    Record the result of EACH (second-pass GAP-4/5/6/7 + re-confirmed original #1-#8) in 36-HUMAN-UAT.md — mark resolved/passed, or log any new gap with surface + symptom + suspected cause for a further gap-closure cycle.
  </how-to-verify>
  <resume-signal>Type "approved" if GAP-4/5/6/7 are resolved across all surfaces, the still-pending original items pass, and the money path (displayed==charged, BrewPad no-charge) is unchanged; otherwise describe the remaining issues (surface + symptom) for a further gap-closure cycle.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → origin/main (staging) | Deploy boundary; staging only, never production without explicit human approval. |
| human ↔ staging browser/iPad | UAT verification of UI behavior, the live-price/scroll fixes, and the money path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-17-01 | Tampering | accidental production deploy | mitigate | This plan pushes ONLY to origin/main (staging) per CLAUDE.md STAGING-FIRST; production is a separate human-approved step. The deploy task explicitly forbids a production push. |
| T-36-17-02 | Tampering | now-prominent price = client math | mitigate | The UAT explicitly re-verifies displayed==charged on a real/test transaction so a client-side price regression (vs the server quote) is caught by a human before promotion. |
| T-36-17-03 | Tampering | silent failed staging push | mitigate | The deploy verify asserts the remote staging head SHA equals local HEAD (git ls-remote), so a rejected/non-fast-forward push is caught instead of being treated as a successful deploy. |
| T-36-17-04 | Tampering | CNAME untracking → 404 | mitigate | The deploy step asserts CNAME stays tracked (git ls-files CNAME) and performs no CNAME manipulation (enforce-cname.yml is broken; untracking 404s the site). |
| T-36-17-SC | Tampering | npm installs | accept | No package installs in this plan. No legitimacy gate needed. |
</threat_model>

<verification>
- Full frontend suite + middleware regression gate + lint green before deploy.
- Changes pushed to origin/main (staging) only and CONFIRMED landed (remote head SHA == local HEAD); CNAME stays tracked; production untouched.
- Human confirms GAP-4/5/6/7 resolved across admin/kiosk/BrewPad, re-confirms the still-pending original items (#1-#8), and verifies the money path (displayed==charged, BrewPad no-charge) is unchanged; results recorded in 36-HUMAN-UAT.md.
</verification>

<success_criteria>
The second-pass gap-closure changes are live on staging (push confirmed by remote-head match, CNAME untouched) and a human has re-verified GAP-4/5/6/7 closed plus the still-pending original UAT items, with the money path confirmed unchanged. Phase 36 is ready for owner sign-off / production promotion (or precise follow-up gaps are logged).
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-17-SUMMARY.md` when done, and update 36-HUMAN-UAT.md with the third-pass re-UAT results.
</output>
</content>
