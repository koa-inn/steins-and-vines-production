---
phase: 36-cross-surface-selection-recipe-modification
plan: 15
type: execute
wave: 3
depends_on: [36-13, 36-14]
files_modified:
  - kiosk.html
  - js/kiosk.js
  - css/kiosk.css
  - tests/frontend/kiosk-recipe-live-price.test.js
  - js/kiosk.min.js
  - js/main.min.js
  - js/admin.min.js
  - js/brewpad.min.js
  - js/batch.min.js
  - css/kiosk.min.css
  - css/styles.min.css
  - css/admin.min.css
  - css/brewpad.min.css
  - css/batch.min.css
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01, MOD-02]
must_haves:
  truths:
    - "GAP-4: on the kiosk (kiosk.html) recipe-sale surface the prominent #kiosk-recipe-summary-price updates from the SERVER quote on EVERY change (volume, ×factor, ingredient add/remove/qty) with the modify panel CLOSED — the _kioskModifyPanelOpen gate is removed and #kiosk-recipe-price-preview is standalone, shown once a sale-type is selected (MOD-02, D-06)"
    - "GAP-5: the kiosk #kiosk-recipe-prompt is a bounded scrollable container so the full ingredient list AND the Ferment-in-Store/Take-Out accept + Add-to-Cart buttons are reachable on the iPad"
    - "GAP-7: the kiosk audit polish is applied identically to admin — price-preview card, 44px touch targets on modify inputs, --sp-* tokens in :root, volume-wrap spacing, and the autocomplete dropdown uses the cellar-palette .ing-autocomplete-drop class instead of hardcoded #fff/#ccc"
    - "The prominent price is the SERVER quote total, never client math (D-06); displayed==charged holds on the iPad POS"
  artifacts:
    - path: "js/kiosk.js"
      provides: "Ungated quote write paths + prominent-price update from server quote + cellar-palette autocomplete"
      contains: "kiosk-recipe-summary-price"
    - path: "kiosk.html"
      provides: "Standalone #kiosk-recipe-price-preview outside modify-wrap + scroll-view on prompt"
      contains: "kiosk-recipe-price-preview"
    - path: "css/kiosk.css"
      provides: "Scroll container, price-preview card, --sp-* tokens, .ing-autocomplete-drop, 44px modify inputs"
      contains: "ing-autocomplete-drop"
  key_links:
    - from: "kiosk.js kioskFetchRecipeQuote success"
      to: "#kiosk-recipe-summary-price"
      via: "server quote total written regardless of _kioskModifyPanelOpen (D-06)"
      pattern: "kiosk-recipe-summary-price"
---

<objective>
Port the GAP-4 / GAP-5 / GAP-7 fixes from the admin reference (36-14) to the KIOSK recipe-sale surface (kiosk.html / js/kiosk.js / css/kiosk.css) — the primary iPad POS where these gaps were confirmed. Keep the control identical to admin (D-01); kiosk has NO save-as-new (D-05 / spec §2).

- GAP-4 (BLOCKER) — remove the `_kioskModifyPanelOpen` gates from the four write paths in `kioskFetchRecipeQuote` (kiosk.js:801/813/826/836), move `#kiosk-recipe-price-preview` OUT of `#kiosk-recipe-modify-wrap`, show it once a sale-type is selected, and update the prominent `#kiosk-recipe-summary-price` (kiosk.js:1683 updater) from the SERVER quote total on EVERY change. Server-authoritative — displayed==charged (D-06).
- GAP-5 (BLOCKER) — give `#kiosk-recipe-prompt` a bounded scrollable container (`.kiosk-product-pane` is overflow:hidden, css/kiosk.css:405) so the full list + Ferment-in-Store/Take-Out + Add-to-Cart are reachable on the iPad.
- GAP-7 (enhancement) — apply the kiosk audit polish: price-preview card (H2), 44px modify-row inputs (H3), `--sp-*` tokens in `:root` (M2), volume-wrap spacing (H6/M3), and replace the hardcoded `#fff`/`#ccc` autocomplete dropdown with the cellar-palette `.ing-autocomplete-drop` class (H5/L1, kiosk.js:963).

iOS zoom guard already correct on kiosk volume inputs; ensure modify-row inputs keep `font-size:1rem`. FRONT-END ONLY — no zoho-middleware files. The prominent price is rendered from the server quote; the client never computes the charge.

Purpose: the kiosk iPad is where staff complete real sales — they must see the live, server-true price and reach the accept buttons on long recipes.
Output: kiosk.html markup, kiosk.js wiring, kiosk.css polish, rebuilt bundles, kiosk live-price tests appended to the shared test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-REVIEW.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-14-SUMMARY.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-10-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live anchors (2026-06-22). kiosk.js is a STANDALONE bundle. -->

kiosk.js kioskFetchRecipeQuote (the GAP-4 gates to REMOVE):
  ~800: var previewEl = document.getElementById('kiosk-recipe-price-preview');
  ~801: if (previewEl && _kioskModifyPanelOpen)   // "Calculating…" — UNGATE
  ~810: _kioskQuote = result.data;
  ~812-813: var el = ...; if (el && _kioskModifyPanelOpen)   // success — UNGATE
  ~824-826: var errEl = ...; if (errEl && _kioskModifyPanelOpen)  // error — UNGATE
  ~834-836: var errEl2 = ...; if (errEl2 && _kioskModifyPanelOpen)  // catch — UNGATE
  ~1435: summaryHtml builds '<div id="kiosk-recipe-summary-price" ...>'  (prominent price)
  ~1683: function updating #kiosk-recipe-summary-price from computed_price — extend to prefer
         _kioskQuote.total when a current quote exists (server-authoritative, D-06).
  ~1740-1742: _kioskQuote.total is already trusted for the cart price — reuse that as the truth source.
  ~963: kioskShowIngredientAutocomplete builds dropdown via inline cssText
        ('background:#fff;border:1px solid #ccc;...') — replace with .ing-autocomplete-drop class (H5/L1)

kiosk.html (verified):
  108: <div id="kiosk-recipe-prompt" style="display:none;">      ← scroll-view (GAP-5)
  129: <div id="kiosk-recipe-modify-wrap" style="display:none;">
  137: <div id="kiosk-recipe-price-preview" style="display:none;"></div>  ← MOVE out of modify-wrap (GAP-4)
  150: <div class="kiosk-sale-type-btns">
  159: <button ... id="kiosk-add-recipe-to-cart" ...>
  (NO #kiosk-save-as-new-wrap on kiosk — correct, do not add)

css/kiosk.css:
  405: .kiosk-product-pane { ... overflow:hidden }   ← clips the prompt (GAP-5)
  :root has no --sp-* tokens (only fallback literals) — add them (M2)
  kiosk.css already sets .admin-input { font-size:1rem } and .kiosk-volume-input { min-height:44px }

NO zoho-middleware files. NEVER hand-edit *.min.js — run npm run build.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-4 — ungate kiosk quote write paths + render the prominent server-quote price on every change</name>
  <files>kiosk.html, js/kiosk.js, tests/frontend/kiosk-recipe-live-price.test.js</files>
  <read_first>
    - 36-UI-SPEC.md §"Live Price Visibility (GAP-4…)"
    - 36-14-SUMMARY.md (the admin reference implementation — mirror its summary-price-prefers-quote approach)
    - js/kiosk.js lines 780-846 (kioskFetchRecipeQuote gates) and 1435 + 1681-1745 (#kiosk-recipe-summary-price build + updater + _kioskQuote.total trust)
    - kiosk.html lines 108-162 (#kiosk-recipe-prompt block)
    - tests/frontend/kiosk-recipe-live-price.test.js (created in 36-14 — append kiosk cases) and the kiosk require/DOM-fixture pattern in existing kiosk tests
  </read_first>
  <behavior>
    - Test KLP-1 (volume change, panel CLOSED): with _kioskModifyPanelOpen=false, a successful quote updates #kiosk-recipe-summary-price to the SERVER total (kiosk surface).
    - Test KLP-2 (×factor change, panel CLOSED): factor edit triggers a quote whose total is written to the prominent price with the panel closed.
    - Test KLP-3 (ingredient change): add/remove/qty change updates the prominent price from the server quote.
    - Test KLP-4 (price-preview visibility): #kiosk-recipe-price-preview is shown once a sale-type is selected, independent of modify panel state.
    - Test KLP-5 (server-authoritative, D-06): the displayed total comes only from _kioskQuote.total; a failed quote shows the error/loading copy, never a fabricated/stale total.
  </behavior>
  <action>
    1. kiosk.js kioskFetchRecipeQuote: REMOVE the `&& _kioskModifyPanelOpen` guard from all four write paths (~801, 813, 826, 836), keeping the null checks.
    2. On success (`_kioskQuote = result.data`), ALSO update the prominent `#kiosk-recipe-summary-price` via the existing updater (~1683): make it prefer `_kioskQuote.total` (formatCurrency) when a current quote exists for the selected recipe, else computed_price. Call it from the quote success branch. Server-authoritative — no client charge math (D-06).
    3. kiosk.html: MOVE `#kiosk-recipe-price-preview` (line 137) OUT of `#kiosk-recipe-modify-wrap` so it is a standalone sibling between modify-wrap and the sale-type buttons; show it (display:'') when a sale-type is selected.
    4. Append KLP-1..KLP-5 to tests/frontend/kiosk-recipe-live-price.test.js using the kiosk require + DOM-fixture pattern. Stub the quote fetch to a known total; assert #kiosk-recipe-summary-price reflects it with _kioskModifyPanelOpen=false. ES5/var; escapeHTML for dynamic text.
    5. Run `npm test -- kiosk-recipe-live-price` and `npm run lint`.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/kiosk-recipe-live-price.test.js 2>&1 | grep -qE "KLP-1|Tests:.*passed" && grep -q "kiosk-recipe-price-preview" kiosk.html && grep -q "kiosk-recipe-summary-price" js/kiosk.js && echo PASS</automated>
  </verify>
  <done>The four quote write paths in kiosk.js are ungated; the prominent #kiosk-recipe-summary-price updates from the server quote total on volume/factor/ingredient change with the modify panel CLOSED; #kiosk-recipe-price-preview is standalone and shown on sale-type select; KLP-1..KLP-5 pass; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-5 scroll + GAP-7 polish (kiosk markup + CSS), then build + full gate</name>
  <files>kiosk.html, css/kiosk.css, js/kiosk.js, js/kiosk.min.js, js/main.min.js, js/admin.min.js, js/brewpad.min.js, js/batch.min.js, css/kiosk.min.css, css/styles.min.css, css/admin.min.css, css/brewpad.min.css, css/batch.min.css</files>
  <read_first>
    - 36-UI-SPEC.md §"Scroll Model (GAP-5)" + §"Audit Polish (GAP-7…)"
    - 36-UI-REVIEW.md Consolidated Fix List C1, H2/H3/H5/H6, M2/M3, L1 (kiosk rows)
    - css/kiosk.css line 405 (.kiosk-product-pane overflow:hidden), :root block (missing --sp-* tokens), admin.css:3119-3141 (.ing-autocomplete-drop reference rule to mirror)
    - js/kiosk.js line 963 (kioskShowIngredientAutocomplete inline cssText)
    - package.json "build" script; CLAUDE.md "Before Every Commit" (npm test AND cd zoho-middleware && npm test AND npm run lint)
  </read_first>
  <action>
    GAP-5 (scroll): add `.kiosk-recipe-prompt-view { height:100%; overflow-y:auto; -webkit-overflow-scrolling:touch; }` to kiosk.css and apply it to `#kiosk-recipe-prompt` when shown (matching the admin approach in 36-14). Keep the grid view scroll behavior. Optionally make `#kiosk-add-recipe-to-cart` sticky-bottom within the scroll container.
    GAP-7 (polish):
      - --sp-* tokens (M2): add `--sp-1:4px; --sp-2:8px; --sp-3:12px; --sp-4:16px; --sp-6:24px; --sp-8:32px;` to kiosk.css `:root`.
      - Price-preview card (H2): add `.kiosk-price-preview { background:var(--cellar-raised); border:1px solid var(--ledger-line); border-radius:var(--r-md); padding:var(--sp-3) var(--sp-4); font-size:13px; margin:var(--sp-2) 0; }` and apply the class to `#kiosk-recipe-price-preview` in kiosk.html.
      - Touch targets (H3): add `#kiosk-recipe-modify-table .admin-input, .kiosk-recipe-modify-table .admin-input { min-height:44px; }` to kiosk.css.
      - Spacing (H6/M3): `#kiosk-recipe-volume-wrap { margin-bottom:var(--sp-6); }` and a `margin-top:var(--sp-4)` on `#kiosk-recipe-modify-wrap`.
      - Autocomplete palette (H5/L1): replace the inline `cssText` in kiosk.js:963 (`background:#fff;border:1px solid #ccc`) with the `.ing-autocomplete-drop` class, and add a `.ing-autocomplete-drop` rule block to kiosk.css mirroring admin.css:3119-3141 (cellar-raised background, --cellar-border, max-height:200px, overflow-y:auto, z-index above the new scroll container, option padding 14px 12px for 44px tap height). Keep ES5/var and escapeHTML for option text.
    Then `npm run build` (regenerates ALL bundles — never hand-edit *.min.js). Run the FULL frontend suite `npm test` and `npm run lint`. Per CLAUDE.md rule 1, run the middleware regression gate `cd zoho-middleware && npm test` — must PASS unchanged; do NOT modify any middleware file (money path frozen).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build >/dev/null 2>&1 && grep -q "kiosk-recipe-prompt-view\|overflow-y:auto" css/kiosk.css && grep -q "ing-autocomplete-drop" css/kiosk.css && grep -q "kiosk-price-preview" css/kiosk.css && grep -q "kiosk-recipe-summary-price" js/kiosk.min.js && npm test 2>&1 | grep -qE "Tests:.*passed" && npm run lint >/dev/null 2>&1 && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>#kiosk-recipe-prompt scrolls so the accept + Add-to-Cart buttons are reachable on iPad; kiosk.css defines --sp-* tokens; the price-preview is a styled card; modify-row inputs are ≥44px; the autocomplete dropdown uses the cellar-palette .ing-autocomplete-drop class (no #fff/#ccc); rebuilt kiosk.min.js carries the changes; full frontend suite + middleware regression gate + lint green; no middleware files modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk iPad browser → GET /api/kiosk/recipe-quote | The prominent kiosk price reflects this server quote on every change; displayed total MUST equal the charge (D-06). |
| kiosk iPad browser → modify-row / autocomplete ingredient input | Dynamic ingredient names rendered into the modify table + dropdown — must stay escapeHTML'd. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-15-01 | Tampering | now-prominent kiosk recipe price | mitigate | #kiosk-recipe-summary-price is written ONLY from the server quote total (_kioskQuote.total); no client charge math (D-06). KLP-5 asserts the displayed total comes from the quote response and a failed quote shows error/loading copy, never a fabricated value. |
| T-36-15-02 | Tampering / XSS | autocomplete + modify-row ingredient names | mitigate | Switching the dropdown to the .ing-autocomplete-drop class must not drop the escapeHTML on option text; reuse the existing escaped render path. |
| T-36-15-03 | Information disclosure | stale displayed price | mitigate | Ungating means the visible price always tracks the latest quote; the "Calculating…" loading state prevents showing a stale base price during a pending quote. |
| T-36-15-SC | Tampering | npm installs | accept | No new package installs — only existing devDeps via npm run build/test. No legitimacy gate needed. |
</threat_model>

<verification>
- `npx jest tests/frontend/kiosk-recipe-live-price.test.js` passes the appended KLP-1..KLP-5: the prominent kiosk price updates from the SERVER quote on volume/factor/ingredient change with the modify panel CLOSED.
- The four `_kioskModifyPanelOpen` gates removed from kiosk.js; `#kiosk-recipe-price-preview` standalone in kiosk.html, shown on sale-type select.
- `#kiosk-recipe-prompt` scrolls; kiosk.css has --sp-* tokens, price-preview card, 44px modify inputs, and the cellar-palette .ing-autocomplete-drop (no #fff/#ccc).
- `npm test` full frontend suite green; `cd zoho-middleware && npm test` green (regression gate, unchanged); `npm run lint` clean; no middleware files modified.
</verification>

<success_criteria>
On the kiosk iPad POS the prominent price is the live SERVER quote and updates on every volume/×factor/ingredient change regardless of panel state (GAP-4, displayed==charged, D-06); the recipe prompt scrolls so the accept + Add-to-Cart buttons are reachable on long recipes (GAP-5); the kiosk audit polish matches admin (GAP-7) including the cellar-palette autocomplete. Bundles rebuilt; full frontend suite + middleware regression gate + lint green with no middleware files changed.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-15-SUMMARY.md` when done.
</output>
</content>
