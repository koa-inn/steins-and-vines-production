---
phase: 36-cross-surface-selection-recipe-modification
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01]
must_haves:
  truths:
    - "The UI-SPEC documents a synced ×factor input beside Target volume (L) with two-way sync math and bounds (GAP-3, D-01, SEL-01)"
    - "The UI-SPEC documents the polished/reordered modify-panel + volume-control layout consistent across all three surfaces (GAP-2, D-01, MOD-01)"
    - "The UI-SPEC states the ×factor math is client-side display-only and the server quote remains authoritative for price"
  artifacts:
    - path: ".planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md"
      provides: "Extended design contract for the ×factor control + modify-panel polish"
      contains: "× factor"
  key_links:
    - from: "36-UI-SPEC.md ×factor section"
      to: "admin/kiosk/brewpad volume-control wiring"
      via: "shared IDs kiosk-target-factor / bp-target-factor + sync rules"
      pattern: "factor"
---

<objective>
Extend the Phase 36 UI design contract (36-UI-SPEC.md) to cover two gap-closure enhancements before any surface code is written:
1. GAP-3 — a synced "× factor" number input placed beside the existing "Target volume (L)" box, two-way synced with litres, identical on all three surfaces (D-01, SEL-01).
2. GAP-2 — the polished / reordered modify-panel + volume-control layout (clean empty-state, intentional add-row affordance, touch-friendly ordering), consistent across admin / kiosk / BrewPad.

Purpose: The three surface plans (36-09/10/11) each implement against this one contract so the controls stay byte-for-byte consistent (D-01). Writing the spec first prevents three divergent interpretations.

Output: An updated 36-UI-SPEC.md with a new "×factor control" subsection and an updated "Surface-by-Surface Interaction Contract" reflecting the reordered/polished layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-HUMAN-UAT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md

<interfaces>
<!-- Existing control IDs the spec already defines (from 36-UI-SPEC.md + 36-PATTERNS.md). -->
<!-- The ×factor input must slot beside these without renaming them. -->

Admin / Kiosk volume control:
  #kiosk-recipe-volume-wrap  (container, display:none until recipe selected)
  #kiosk-target-volume       (litres input: min 0.5, step 0.5, inputmode decimal)
  #kiosk-scale-factor-readout (renders "1.5× base 20 L")

BrewPad volume control:
  #bp-recipe-volume-wrap
  #bp-target-volume          (uses .bp-input class, not .admin-input)
  #bp-scale-factor-readout

Existing sync math (display only — server is authoritative for price):
  factor = litres / base_batch_size_l
  litres = factor × base_batch_size_l
  bounds: litres > 0 AND litres ≤ base × 10 ; no base ⇒ control disabled
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the synced ×factor control subsection to 36-UI-SPEC.md (GAP-3)</name>
  <files>.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md</files>
  <read_first>
    - 36-UI-SPEC.md §"Surface-by-Surface Interaction Contract" (the existing volume-control descriptions for all three surfaces)
    - 36-HUMAN-UAT.md "GAP-3" block (owner decision 2026-06-21: factor input beside litres, two-way synced, same bounds, both disabled when no base)
    - 36-CONTEXT.md D-01 (identical control on all three surfaces)
  </read_first>
  <action>
    Add a new subsection titled "## Synced ×factor Control (GAP-3, SEL-01, D-01)" after the "Modified-Price Preview: Display Rules" section. Document, as a design contract (prose + tables, NO implementation code):

    - New input element IDs: `#kiosk-target-factor` (admin + kiosk surfaces, reusing the kiosk-prefixed convention already used in admin.html and ported to kiosk.html) and `#bp-target-factor` (BrewPad). Placed immediately to the RIGHT of the existing Target volume (L) input, inside the same `#…-recipe-volume-wrap`, with an inline label "× factor".
    - Input attributes: type=number, min just-above-0 (step 0.1), inputmode decimal; on kiosk/brewpad add font-size:1rem (iOS zoom guard, per existing Typography rule).
    - Two-way sync contract (display-only): editing factor → litres = factor × base_batch_size_l (rounded to the existing 0.5 L granularity used by the litres box per D-01); editing litres → factor = litres ÷ base (shown to 2 decimals to match the existing "1.5×" readout precision). The "1.5× base 20 L" readout string stays and reflects whichever field was last edited.
    - Bounds: identical to today — litres > 0 AND litres ≤ ~10× base; factor > 0 AND ≤ ~10. No-base recipe ⇒ BOTH the litres input AND the factor input are disabled, and the readout shows the existing "Set batch size (L) on this recipe to enable scaling" copy.
    - Server-authoritative note (MUST appear verbatim in the spec): "The ×factor↔litres sync is client-side display only. The charged price always comes from GET /api/kiosk/recipe-quote (sale surfaces) or is absent (BrewPad attach, D-10). The factor input never becomes the source of the charge; it only changes the displayed target_volume_l that the quote endpoint already receives."
    - Add a row to the Copywriting Contract table: `× factor label | "× factor"`.
    - State explicitly that this control is identical on all three surfaces (D-01) — only the class prefix differs (`.admin-input`/`.kiosk-volume-input` vs `.bp-input`).
  </action>
  <verify>
    <automated>grep -q "× factor" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "kiosk-target-factor" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "bp-target-factor" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "client-side display only" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && echo PASS</automated>
  </verify>
  <done>The UI-SPEC has a ×factor control subsection naming both element IDs, the two-way sync math, the bounds + no-base disable, and the explicit "display-only / server-authoritative price" rule; the copywriting table includes the "× factor" label.</done>
</task>

<task type="auto">
  <name>Task 2: Document the polished/reordered modify-panel + volume layout (GAP-2)</name>
  <files>.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md</files>
  <read_first>
    - 36-UI-SPEC.md §"1. Admin Recipe-Sale Surface" layout ordering list, §"Empty ingredient table state", and the modify-wrap layout block
    - 36-HUMAN-UAT.md "GAP-2" block (cramped layout; awkward order target-volume → readout → Modify toggle → rows → Add → sale buttons; empty modify row looks broken)
  </read_first>
  <action>
    Add a subsection "## Modify-Panel Polish & Ordering (GAP-2)" after the ×factor subsection. Document (prose + an ordered layout list, NO code) the corrected layout, applied identically to all three surfaces:

    - Volume + factor row: "Target volume (L)" input and the "× factor" input sit on ONE row (flex, gap --sp-4), with the "1.5× base 20 L" readout directly beneath them (not interleaved between controls). Inputs must not appear "greyed/empty/broken" — when a base exists they are pre-filled and enabled; the disabled (no-base) state is the only greyed state and shows the disable copy.
    - Corrected element order inside the recipe prompt: (1) Back + recipe name, (2) grouped ingredient summary, (3) volume+factor row + readout, (4) "Modify Ingredients" collapsible toggle (collapsed by default — empty modify rows must NEVER render before the panel is expanded), (5) when expanded: the editable rows table, then the "+ Add Ingredient" button, (6) price preview (sale surfaces only) + locked-price notice, (7) stock conflict / soft advisory, (8) sale/attach buttons (with Save-as-new where applicable).
    - Empty-state polish: confirm the existing single placeholder row copy ("No ingredients — use '+ Add Ingredient' to build a custom list") is the intentional empty state; the bare empty `<tbody>` with greyed inputs is the bug being removed. State that a freshly-added blank row is acceptable (it has its own autocomplete + qty inputs) but the panel must not show a phantom empty row before any add.
    - Touch-friendliness reminder: all inputs/buttons keep the 44px minimum on kiosk + BrewPad (existing Spacing rule) — the reorder must not shrink any touch target.
    - Note that this layout is CONSISTENT across admin / kiosk / BrewPad (D-01) and that the price-preview step is omitted on BrewPad (D-10).
    - Flip the Checker Sign-Off "Approval: pending" line to note the gap-closure extension was added (leave the dimension checkboxes for the ui-checker; do not self-approve).
  </action>
  <verify>
    <automated>grep -q "Modify-Panel Polish & Ordering" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && grep -q "collapsed by default" .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md && echo PASS</automated>
  </verify>
  <done>The UI-SPEC documents the reordered volume+factor row, the corrected prompt element order, the empty-state polish, and states the layout is identical across all three surfaces with BrewPad omitting the price preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits a planning document only — no runtime trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-08-01 | Tampering | ×factor design contract | mitigate | Spec MUST state the factor sync is display-only and the server quote stays authoritative for price, so downstream surface plans cannot turn the client factor into the charge source. |
| T-36-08-02 | Information disclosure | n/a | accept | Doc-only change; no PII, no secrets, no runtime surface. |
</threat_model>

<verification>
- `grep "× factor"`, `grep "kiosk-target-factor"`, `grep "bp-target-factor"`, `grep "client-side display only"`, `grep "Modify-Panel Polish & Ordering"` all match in 36-UI-SPEC.md.
- The spec still describes one identical control on all three surfaces (D-01) and omits the price preview on BrewPad (D-10).
</verification>

<success_criteria>
36-UI-SPEC.md is extended with a complete ×factor control contract (IDs, two-way sync math, bounds, no-base disable, display-only/server-authoritative rule) and a polished/reordered modify-panel layout, both stated as identical across admin/kiosk/BrewPad. No code files touched.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-08-SUMMARY.md` when done.
</output>
