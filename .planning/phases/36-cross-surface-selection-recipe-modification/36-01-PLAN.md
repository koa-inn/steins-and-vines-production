---
phase: 36-cross-surface-selection-recipe-modification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/recipe-scaling.js
  - zoho-middleware/__tests__/recipe-scaling.test.js
autonomous: true
requirements: [MOD-02]
must_haves:
  truths:
    - "A locked-price recipe with an added ingredient prices as locked×factor + fees + (added scaled qty × catalog rate) (D-07)"
    - "A locked-price recipe with a removed ingredient prices unchanged at locked×factor + fees — no credit (D-08)"
    - "A dynamic recipe with any modification prices as Σ(scaled_qty × rate over the modified list) + fees (D-09)"
  artifacts:
    - path: "zoho-middleware/lib/recipe-scaling.js"
      provides: "computeModifiedRecipeTotal pure helper exported (MOD-02, D-07/D-08/D-09)"
      contains: "computeModifiedRecipeTotal"
    - path: "zoho-middleware/__tests__/recipe-scaling.test.js"
      provides: "Worked-example fixtures for locked-add, locked-remove, dynamic-modify"
      contains: "computeModifiedRecipeTotal"
  key_links:
    - from: "zoho-middleware/routes/pos-recipe.js"
      to: "recipe-scaling.computeModifiedRecipeTotal"
      via: "module export consumed by quote/sale/confirm in plan 36-03"
      pattern: "computeModifiedRecipeTotal"
---

<objective>
Add a pure `computeModifiedRecipeTotal` helper to `lib/recipe-scaling.js` that prices a MODIFIED ingredient list server-authoritatively under both locked and dynamic pricing modes, implementing the locked-add / locked-remove asymmetry (D-07/D-08) and dynamic natural repricing (D-09). This is the pricing primitive that plan 36-03 wires into the quote/sale/confirm routes (MOD-02).

Purpose: Server-authoritative pricing is a hard project rule — the client never computes the modified price. Keeping the math in a pure, independently-tested helper preserves the displayed-==-charged guarantee from Phase 35.
Output: One new exported pure function + a middleware test suite covering the CONTEXT worked examples as named fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md

<interfaces>
From zoho-middleware/lib/recipe-scaling.js:
- scaleIngredient(ing, factor) -> shallow-cloned ingredient with scaled quantity (never mutates input)
- scaleIngredients(ingredients, factor) -> array of scaled ingredients
- computeScaledRecipeTotal(recipe, scaledIngredients, catalogMap, saleType) -> grand total (2 dp). EXISTING dynamic+locked scaled pricer; the new helper reuses it for the locked base total and the dynamic full-list total.
- checkScaledStock(scaledIngredients, catalogMap) -> { ok, conflicts }
- CONTINUOUS_UNITS / DISCRETE_UNITS

recipe fields used: recipe.locked_price (Number, 0 => dynamic), recipe.pricing_mode, recipe.service_fee, recipe.materials_fee, recipe.batch_size_l.
catalogMap shape: { [item_id]: { rate, available_stock, item_name, unit, ... } }.
ingredient shape: { item_id, item_name, quantity, unit, cf_type, cf_subcategory, display_group }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for computeModifiedRecipeTotal (RED)</name>
  <read_first>
    - zoho-middleware/lib/recipe-scaling.js (whole file, ~199 lines) — computeScaledRecipeTotal signature, scaleIngredient/scaleIngredients, the immutable shallow-clone pattern at lines 55-76, the export block at lines 192-199
    - zoho-middleware/__tests__/recipe-scaling.test.js (existing Phase 35 suite) — match its describe/it style, fixture construction, recipe + catalogMap object shapes
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md lines 112-120 — the worked examples that become named fixtures (§Specifics: 1.5× locked-add = $125.50)
  </read_first>
  <behavior>
    - Locked-add 1.0x: recipe { locked_price:45, service_fee:45, materials_fee:5, batch_size_l:20 }, original = [], modified = [hop item_id 'H1' quantity 1 unit 'pcs'], catalog H1 rate $4 -> total = 45 + 45 + 5 + (1*4) = $99.00. Assert the literal 99.
    - Locked-add 1.5x (target 30 base 20): locked portion scales (45*1.5=67.50), fees flat (45+5), added hop scales identically to base ingredients per D-04 (discrete 'pcs' -> Math.max(1, Math.ceil(1*1.5))=2 pcs -> 2*$4=$8) -> total = 67.50 + 50 + 8 = $125.50. Assert the LITERAL named expected value 125.50 — hard-coded in the test, NOT computed from scaleIngredient at assert time. (Owner decision 2026-06-21: added ingredients scale with the rest of the modified list; CONTEXT §Specifics confirms $125.50.)
    - Locked-remove 1.0x and 1.5x: recipe locked_price 45 + 2 original ingredients, modified drops one -> total === locked_price*factor + service_fee + materials_fee, identical to the unmodified locked total. NO credit (D-08). Unchanged at any factor.
    - Dynamic-modify: recipe { locked_price:0, pricing_mode:'dynamic' }, modified = [grain 5kg @ $2/kg, added hop 1pcs @ $4] at 1.0x -> total = (5*2)+(1*4)+service_fee+materials_fee. At 1.5x grain linear 7.5kg, hop ceil 2pcs.
    - Determinism + immutability: same inputs return same number twice; original and modified arrays unchanged after the call.
  </behavior>
  <action>
    Add a `describe('computeModifiedRecipeTotal', ...)` block to recipe-scaling.test.js. Build fixtures with HARD-CODED named expected literals: LOCKED_ADD_1X asserts `=== 99`, LOCKED_ADD_1_5X asserts `=== 125.50` (the owner-approved value from CONTEXT §Specifics — written as a number literal in the assertion, never derived from scaleIngredient or computeScaledRecipeTotal at assert time), LOCKED_REMOVE asserts equality to the unmodified locked total (locked_price*factor + fees) and is unchanged at both 1.0x and 1.5x, DYNAMIC_MODIFY, and an immutability case (original + modified arrays deep-unchanged after the call). Call the helper with the signature it will expose: computeModifiedRecipeTotal(recipe, originalIngredients, modifiedBaseIngredients, catalogMap, scaleFactor, saleType). Tests fail because the export does not exist yet.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website/zoho-middleware && npx jest recipe-scaling.test.js -t computeModifiedRecipeTotal 2>&1 | grep -qE "is not a function|TypeError|✕|failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The command above prints RED-CONFIRMED (suite fails because the function is undefined).
    - The new describe block has at least 5 it() cases: LOCKED_ADD_1X, LOCKED_ADD_1_5X, LOCKED_REMOVE, DYNAMIC_MODIFY, immutability.
    - LOCKED_ADD_1X asserts a concrete literal: `computeModifiedRecipeTotal(...) === 99`.
    - LOCKED_ADD_1_5X asserts a concrete literal: `computeModifiedRecipeTotal(...) === 125.50` — the exact expected number is hard-coded in the test (a bare `125.50`/`125.5` numeric literal in the assertion), NOT computed from scaleIngredient or computeScaledRecipeTotal at assert time.
    - LOCKED_REMOVE asserts the total equals `locked_price * factor + service_fee + materials_fee` and is identical at 1.0x and 1.5x (no credit, D-08).
  </acceptance_criteria>
  <done>Failing test suite committed with all CONTEXT §Specifics worked-example fixtures, including the literal 125.50 assertion for LOCKED_ADD_1_5X.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement computeModifiedRecipeTotal (GREEN)</name>
  <read_first>
    - zoho-middleware/lib/recipe-scaling.js lines 55-76 (scaleIngredient immutable clone) and the computeScaledRecipeTotal body — reuse it; do not re-derive fee math
    - zoho-middleware/lib/recipe-scaling.js lines 192-199 (export block) — add the new export here
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 625-668 (pure-function contract) and lines 558-577 (locked-add logic sketch)
  </read_first>
  <action>
    Implement computeModifiedRecipeTotal(recipe, originalIngredients, modifiedBaseIngredients, catalogMap, scaleFactor, saleType) as a pure function (no I/O, no require). Branch on Number(recipe.locked_price) > 0 (locked) else dynamic. Dynamic: scale the FULL modifiedBaseIngredients via scaleIngredients(modifiedBaseIngredients, scaleFactor), return computeScaledRecipeTotal(recipe, scaledModified, catalogMap, saleType) (adds and removals fall out — D-09). Locked: lockedBase = computeScaledRecipeTotal(recipe, scaleIngredients(originalIngredients, scaleFactor), catalogMap, saleType) (yields locked_price*factor + fees; removals do not reduce it — D-08). Build an originalIds object map once. For each item in modifiedBaseIngredients whose item_id is NOT in originalIds (ADDED — D-07): look up catalogMap[item_id]; if found, scaled = scaleIngredient(item, scaleFactor) (the added line scales identically to base ingredients per D-04); add (Number(scaled.quantity)||0) * (Number(catalogEntry.rate)||0); skip silently if no catalog entry. Round once at the end: Math.round(total*100)/100. Add a JSDoc block (all six params + return). Export alongside computeScaledRecipeTotal; also export CONTINUOUS_UNITS/DISCRETE_UNITS if not already. Never mutate the input arrays.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website/zoho-middleware && npx jest recipe-scaling.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npx jest recipe-scaling.test.js` reports all tests passing (GREEN) including the new computeModifiedRecipeTotal block — the LOCKED_ADD_1_5X literal 125.50 assertion passes.
    - `grep -n "computeModifiedRecipeTotal" zoho-middleware/lib/recipe-scaling.js` shows both the function definition and a line inside the module.exports object.
    - `grep -v '^\s*//' zoho-middleware/lib/recipe-scaling.js | grep -c "ing.quantity = "` is 0 (no input mutation introduced).
  </acceptance_criteria>
  <done>computeModifiedRecipeTotal implemented, exported, all recipe-scaling tests green, no input mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route handler (36-03) -> this helper | Modified ingredient list (client-originated) reaches pricing math here; this helper must price only from server-side catalogMap rates, never a client-supplied rate/price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Tampering | computeModifiedRecipeTotal added-line pricing | mitigate | Rate for every added line is read ONLY from catalogMap[item_id].rate (server catalog), never from the client-supplied ingredient object. Test asserts an added item with a client-sent "rate" field is ignored and the catalog rate is used. |
| T-36-02 | Tampering | locked-remove credit | mitigate | Locked base total is computed from recipe.locked_price independent of the modified list, so removing lines cannot reduce the charge below locked*factor+fees (D-08). Covered by LOCKED_REMOVE test. |
| T-36-03 | Information Disclosure | helper inputs | accept | Pure function, no logging, no PII — nothing to disclose. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (pure helper + tests, existing jest). No package legitimacy gate required. |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest recipe-scaling.test.js` all green.
- `cd zoho-middleware && npm test` full suite green (no regression in pos-recipe or other consumers of recipe-scaling).
- `npm run lint` clean.
</verification>

<success_criteria>
computeModifiedRecipeTotal exists, is exported, handles locked-add (D-07), locked-remove no-credit (D-08), and dynamic-modify (D-09); the CONTEXT worked examples pass as named tests with the literal 125.50 for the 1.5× locked-add fixture; no input mutation; full middleware suite green.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-01-SUMMARY.md` when done.
</output>
