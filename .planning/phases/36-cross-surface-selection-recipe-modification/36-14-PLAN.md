---
phase: 36-cross-surface-selection-recipe-modification
plan: 14
type: execute
wave: 2
depends_on: [36-13]
files_modified:
  - admin.html
  - js/admin.js
  - css/admin.css
  - tests/frontend/kiosk-recipe-live-price.test.js
  - js/main.min.js
  - js/admin.min.js
  - js/kiosk.min.js
  - js/brewpad.min.js
  - js/batch.min.js
  - css/styles.min.css
  - css/admin.min.css
  - css/kiosk.min.css
  - css/brewpad.min.css
  - css/batch.min.css
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01, MOD-02]
must_haves:
  truths:
    - "GAP-4: on the admin kiosk-sale surface the prominent recipe price updates from the SERVER quote on EVERY change (target volume, ×factor, ingredient add/remove/qty) with the modify panel CLOSED — the _kioskModifyPanelOpen gate is removed and #kiosk-recipe-price-preview is a standalone element shown once a sale-type is selected (MOD-02, D-06)"
    - "GAP-5: the admin #kiosk-recipe-prompt is a bounded scrollable container so the full ingredient list AND the sale-type/Add-to-Cart buttons are reachable on the iPad"
    - "GAP-6: the admin kiosk-tab modify-row + volume inputs use font-size:1rem (no iOS auto-zoom) and the ×factor/modify wiring is confirmed present in the rebuilt admin bundle; cache/hard-refresh guidance is documented in the SUMMARY"
    - "GAP-7: the admin audit polish is applied — price-preview card styling, save-as-new moved below Add-to-Cart, 44px touch targets, volume-wrap spacing — per 36-UI-SPEC"
    - "The now-prominent price is the SERVER quote total, never client math (D-06); displayed==charged still holds"
  artifacts:
    - path: "js/admin.js"
      provides: "Ungated quote write paths + prominent-price update from server quote on every change"
      contains: "kiosk-recipe-summary-price"
    - path: "admin.html"
      provides: "Standalone #kiosk-recipe-price-preview outside modify-wrap + save-as-new below Add-to-Cart + scroll-view class on prompt"
      contains: "kiosk-recipe-price-preview"
    - path: "tests/frontend/kiosk-recipe-live-price.test.js"
      provides: "Tests asserting the prominent price updates from the quote on volume/factor/ingredient change with the modify panel CLOSED"
      contains: "modify panel closed"
  key_links:
    - from: "admin.js kioskFetchRecipeQuote success"
      to: "#kiosk-recipe-summary-price + #kiosk-recipe-price-preview"
      via: "server quote total written regardless of _kioskModifyPanelOpen (D-06)"
      pattern: "kiosk-recipe-summary-price"
---

<objective>
Close GAP-4, GAP-5, GAP-6, and GAP-7 on the ADMIN kiosk-sale surface (admin.html?tab=kiosk / js/admin.js / css/admin.css). The admin surface is the reference implementation — kiosk (36-15) and BrewPad (36-16) follow it.

- GAP-4 (BLOCKER) — the prominent price never updates from the live quote. Remove the `_kioskModifyPanelOpen` gates from the four write paths in `kioskFetchRecipeQuote` (admin.js:11031/11043/11056/11066), move `#kiosk-recipe-price-preview` OUT of `#kiosk-recipe-modify-wrap`, show it once a sale-type is selected, and update the prominent `#kiosk-recipe-summary-price` from the SERVER quote total on EVERY change (volume / ×factor / ingredient edit). Server-authoritative — displayed==charged (D-06).
- GAP-5 (BLOCKER) — the recipe prompt clips its action buttons. Give `#kiosk-recipe-prompt` a bounded scrollable container so the full list + sale-type/Add-to-Cart buttons are reachable on the iPad.
- GAP-6 (investigate) — the ×factor/modify feature "didn't show" on admin.html?tab=kiosk though it works on kiosk.html. Add the missing `font-size:1rem` on admin kiosk-tab modify-row + volume inputs (the iPad auto-zoom flagged by UI-REVIEW C4/M4), confirm the rebuilt admin bundle contains the factor wiring, check the admin render path for a JS error, and document the cache/hard-refresh guidance in the SUMMARY. If a real render bug surfaces, fix it; if it is purely cache, the font-size fix + the verification note is the deliverable.
- GAP-7 (enhancement) — apply the admin audit polish: price-preview card styling (H2), save-as-new below Add-to-Cart (H1), 44px touch targets on modify inputs/save-as-new (H3/H4), volume-wrap spacing (H6/M3), group-header class (L3, discretionary).

Money path is FRONT-END ONLY — no zoho-middleware files. The prominent price is rendered from the server quote; the client never computes the charge.

Purpose: give staff confidence the displayed price equals the charge before committing, and make long recipes completable on the iPad.
Output: admin.html markup, admin.js wiring, admin.css polish, rebuilt bundles, a new live-price regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-REVIEW.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-09-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live anchors (2026-06-22). admin.js kiosk IIFE. -->

admin.js kioskFetchRecipeQuote (the GAP-4 gates to REMOVE):
  ~11030: var previewEl = document.getElementById('kiosk-recipe-price-preview');
  ~11031: if (previewEl && _kioskModifyPanelOpen)        // "Calculating…" — UNGATE
  ~11042-11043: var el = ...; if (el && _kioskModifyPanelOpen)    // success write — UNGATE
  ~11055-11056: var errEl = ...; if (errEl && _kioskModifyPanelOpen)  // error — UNGATE
  ~11065-11066: var errEl = ...; if (errEl && _kioskModifyPanelOpen)  // catch — UNGATE
  On success the result is stored in _kioskQuote (see kiosk.js:810 parallel) — write the
  server total into #kiosk-recipe-summary-price too (this is the prominent price).
  ~11240: summaryHtml builds '<div id="kiosk-recipe-summary-price" ...>' (the prominent price)
  ~11580: function that updates #kiosk-recipe-summary-price from computed_price (base) — extend
          to prefer the live _kioskQuote total when present (server-authoritative, D-06).

admin.html (verified line anchors):
  626: <div id="kiosk-recipe-prompt" style="display:none;">   ← needs scroll-view class (GAP-5)
  647: <div id="kiosk-recipe-modify-wrap" style="display:none;">
  655: <div id="kiosk-recipe-price-preview" style="display:none;"></div>  ← MOVE out of modify-wrap (GAP-4)
  669: <div id="kiosk-save-as-new-wrap" style="display:none;">  ← MOVE to below add-to-cart (H1)
  678: <div class="kiosk-sale-type-btns">
  687: <button ... id="kiosk-add-recipe-to-cart" ...>

css/admin.css:
  2198: .kiosk-product-pane { ... overflow:hidden }   ← parent that clips the prompt (GAP-5)
  .admin-input { font-size:13px }                     ← iOS zoom risk on admin kiosk tab (GAP-6/C4)

NOTE: admin.js is concatenated into main.min.js AND drives the kiosk tab of admin.html.
kiosk.js/brewpad.js are SEPARATE standalone bundles — NOT touched by this plan.
NO zoho-middleware files. NEVER hand-edit *.min.js — run npm run build.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-4 — ungate the quote write paths + render the prominent server-quote price on every change (admin)</name>
  <files>admin.html, js/admin.js, tests/frontend/kiosk-recipe-live-price.test.js</files>
  <read_first>
    - 36-UI-SPEC.md §"Live Price Visibility (GAP-4…)" (from 36-13) + §"Modified-Price Preview: Display Rules"
    - js/admin.js lines 11020-11080 (kioskFetchRecipeQuote — the four gated write paths) and lines 11230-11260 + 11578-11600 (#kiosk-recipe-summary-price build + its updater)
    - admin.html lines 626-690 (#kiosk-recipe-prompt block: modify-wrap, price-preview, save-as-new, sale-type btns, add-to-cart)
    - 36-09-SUMMARY.md (current admin quote/factor wiring) and tests/frontend for the admin kiosk require/DOM-fixture pattern
  </read_first>
  <behavior>
    - Test ALP-1 (volume change, panel CLOSED): with _kioskModifyPanelOpen=false, a successful quote response updates #kiosk-recipe-summary-price to the SERVER total (not the base computed_price).
    - Test ALP-2 (×factor change, panel CLOSED): editing the factor triggers a quote whose total is written to the prominent price with the panel closed.
    - Test ALP-3 (ingredient change): an ingredient add/remove/qty change updates the prominent price from the server quote.
    - Test ALP-4 (price-preview visibility): #kiosk-recipe-price-preview is shown (not display:none) once a sale-type is selected, independent of the modify panel state.
    - Test ALP-5 (server-authoritative, D-06): the displayed total comes ONLY from the quote response (_kioskQuote.total); the test asserts no client-side recomputation path writes a different number. A failed/empty quote shows the loading/error copy, never a stale fabricated total.
  </behavior>
  <action>
    1. admin.js kioskFetchRecipeQuote: REMOVE the `&& _kioskModifyPanelOpen` guard from all four write paths (~11031, 11043, 11056, 11066) so the preview element is written regardless of panel state. Keep the null checks (`if (previewEl)` etc.).
    2. On a successful quote (where `_kioskQuote = result.data`), ALSO update the prominent `#kiosk-recipe-summary-price` with the server total via the existing summary-price updater (~11580): make that updater prefer `_kioskQuote.total` (formatCurrency from js/lib/utils.js) when a current quote exists for the selected recipe, else fall back to computed_price. Call it from the quote success branch. Server-authoritative — never compute the charge client-side (D-06).
    3. admin.html: MOVE `#kiosk-recipe-price-preview` (line 655) OUT of `#kiosk-recipe-modify-wrap` (close the modify-wrap div BEFORE the price-preview) so it is a standalone sibling between the modify-wrap and the stock-conflict/sale region. Ensure it is shown (display:'') when a sale-type is selected (wire in the sale-type select handler / kioskShowRecipePrompt path), not only on modify-open.
    4. Create tests/frontend/kiosk-recipe-live-price.test.js implementing ALP-1..ALP-5 using the admin kiosk require + DOM-fixture pattern. Stub the quote fetch to resolve a known total; assert #kiosk-recipe-summary-price reflects it with _kioskModifyPanelOpen=false. ES5/var; escapeHTML for any dynamic text.
    5. Run `npm test -- kiosk-recipe-live-price` and `npm run lint`.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/kiosk-recipe-live-price.test.js 2>&1 | grep -qE "ALP-1|Tests:.*passed" && ! grep -nE "kiosk-recipe-price-preview'\);\s*$" js/admin.js >/dev/null; grep -c "_kioskModifyPanelOpen" js/admin.js | grep -qvE '^0$' && echo CHECK_GATES_REMOVED && grep -q "kiosk-recipe-price-preview" admin.html && echo PASS</automated>
  </verify>
  <done>The four quote write paths in admin.js are ungated; the prominent #kiosk-recipe-summary-price updates from the server quote total on volume/factor/ingredient change with the modify panel CLOSED; #kiosk-recipe-price-preview is a standalone element shown once a sale-type is selected; ALP-1..ALP-5 pass; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: GAP-5 scroll + GAP-6 font-size + GAP-7 polish (admin markup + CSS), then build + full gate</name>
  <files>admin.html, css/admin.css, js/admin.js, js/main.min.js, js/admin.min.js, js/kiosk.min.js, js/brewpad.min.js, js/batch.min.js, css/styles.min.css, css/admin.min.css, css/kiosk.min.css, css/brewpad.min.css, css/batch.min.css</files>
  <read_first>
    - 36-UI-SPEC.md §"Scroll Model (GAP-5)" + §"Audit Polish (GAP-7…)" (from 36-13)
    - 36-UI-REVIEW.md Consolidated Fix List C1/C4, H1/H2/H3/H4/H6, M3/M4 (admin rows)
    - css/admin.css lines 2198+ (.kiosk-product-pane overflow:hidden), .admin-input (font-size:13px), #kiosk-recipe-volume-wrap, .admin-btn-sm, .ing-autocomplete-drop (lines ~3119-3141)
    - admin.html lines 626-690 (#kiosk-recipe-prompt, save-as-new-wrap order)
    - package.json "build" script (regenerates ALL min files); CLAUDE.md "Before Every Commit" (run npm test AND cd zoho-middleware && npm test AND npm run lint)
  </read_first>
  <action>
    GAP-5 (scroll): add a class `.kiosk-recipe-prompt-view { height:100%; overflow-y:auto; -webkit-overflow-scrolling:touch; }` to admin.css and apply it to `#kiosk-recipe-prompt` when it is shown (in the JS that reveals the prompt, or statically on the element). Keep the grid view's existing scroll behavior on `.kiosk-product-pane`. Optionally make `#kiosk-add-recipe-to-cart` `position:sticky; bottom:0` within the scroll container so it stays reachable.
    GAP-6 (iPad zoom / visibility): add `#tab-kiosk .admin-input { font-size:1rem; }` and `#tab-kiosk .kiosk-volume-input { font-size:1rem; }` to admin.css (C4/M4) so the admin kiosk-tab modify-row + volume inputs don't trigger iOS auto-zoom. The ×factor/modify wiring already exists in admin.js (36-09) — this build regenerates admin.min.js so the deployed bundle is current; the verify step asserts the factor wiring is present in the rebuilt admin.min.js. Record in the SUMMARY: the most likely GAP-6 cause was stale cached admin.html/admin.min.js (or the iOS-zoom making the controls feel "missing"); the remedy is this rebuild + the font-size fix + a hard-refresh / clear-cache note for the operator.
    GAP-7 (polish):
      - Price-preview card (H2): add `.kiosk-price-preview { background:var(--cellar-raised); border:1px solid var(--ledger-line); border-radius:var(--r-md); padding:var(--sp-3) var(--sp-4); font-size:13px; margin:var(--sp-2) 0; }` to admin.css and apply the class to `#kiosk-recipe-price-preview` in admin.html.
      - Save-as-new placement (H1): MOVE `#kiosk-save-as-new-wrap` (admin.html line 669) to AFTER `#kiosk-add-recipe-to-cart` (line 687+).
      - Touch targets (H3/H4): add `#kiosk-recipe-modify-table .admin-input { min-height:44px; }` to admin.css; ensure the save-as-new button meets 44px (replace `admin-btn-sm` with `.btn-secondary`, or add `min-height:44px`).
      - Spacing (H6/M3): set `#kiosk-recipe-volume-wrap { margin-bottom:var(--sp-6); }` and add `margin-top:var(--sp-4)` (or equivalent) before `#kiosk-recipe-modify-wrap`.
      - (Discretionary L3) extract the modify group-header inline style to a `.kiosk-modify-group-header td` CSS class if low-risk.
    Then run `npm run build` (never hand-edit *.min.js — it regenerates all bundles). Run the FULL frontend suite `npm test` and `npm run lint`. Per CLAUDE.md rule 1, also run the middleware regression gate `cd zoho-middleware && npm test` — it must PASS unchanged; do NOT modify any middleware file or author middleware tests (money path frozen).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build >/dev/null 2>&1 && grep -q "kiosk-target-factor" js/admin.min.js && grep -q "kiosk-recipe-prompt-view\|overflow-y:auto" css/admin.css && grep -q "kiosk-price-preview" css/admin.css && npm test 2>&1 | grep -qE "Tests:.*passed" && npm run lint >/dev/null 2>&1 && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>#kiosk-recipe-prompt scrolls so the action buttons are reachable; admin kiosk-tab modify/volume inputs are font-size:1rem (no iOS zoom); the rebuilt admin.min.js contains the ×factor wiring; price-preview is a styled card; save-as-new sits below Add-to-Cart; 44px touch targets and volume-wrap spacing applied; full frontend suite + middleware regression gate + lint all green; no middleware files modified; SUMMARY documents the GAP-6 cache/hard-refresh guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin iPad browser → GET /api/kiosk/recipe-quote | The prominent price now reflects this server quote on every change; the displayed total MUST equal what the server will charge (D-06). |
| admin iPad browser → modify-row ingredient input | Dynamic ingredient names rendered into the modify table — must stay escapeHTML'd. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14-01 | Tampering | now-prominent recipe price | mitigate | The prominent #kiosk-recipe-summary-price is written ONLY from the server quote total (_kioskQuote.total); no client-side charge math (D-06). ALP-5 asserts the displayed total comes from the quote response and a failed quote shows loading/error copy, never a fabricated value. |
| T-36-14-02 | Tampering / XSS | modify-row + autocomplete ingredient names | mitigate | Reuse the existing escapeHTML path for all dynamic row/dropdown text; no raw innerHTML of catalog strings. |
| T-36-14-03 | Information disclosure | stale displayed price | mitigate | Removing the gate means the visible price always tracks the latest quote; loading state ("Calculating…") prevents showing a stale base price during a pending quote. |
| T-36-14-SC | Tampering | npm installs | accept | No new package installs — only existing devDeps via npm run build/test. No legitimacy gate needed. |
</threat_model>

<verification>
- `npx jest tests/frontend/kiosk-recipe-live-price.test.js` passes (ALP-1..ALP-5): the prominent price updates from the SERVER quote on volume/factor/ingredient change with the modify panel CLOSED.
- The four `_kioskModifyPanelOpen` gates are removed from admin.js kioskFetchRecipeQuote; `#kiosk-recipe-price-preview` is a standalone element in admin.html shown on sale-type select.
- `#kiosk-recipe-prompt` scrolls (overflow-y:auto class); admin kiosk-tab inputs are font-size:1rem; rebuilt admin.min.js contains the factor wiring.
- Audit polish applied (price-preview card, save-as-new below add-to-cart, 44px targets, spacing).
- `npm test` full frontend suite green; `cd zoho-middleware && npm test` green (regression gate, unchanged); `npm run lint` clean; no middleware files modified.
</verification>

<success_criteria>
On the admin kiosk-sale surface the prominent price is the live SERVER quote and updates on every volume/×factor/ingredient change regardless of panel state (GAP-4, displayed==charged, D-06); the recipe prompt scrolls so the full list + action buttons are reachable on iPad (GAP-5); the modify/volume inputs no longer trigger iOS zoom and the rebuilt admin bundle carries the factor wiring with cache guidance recorded (GAP-6); the admin audit polish is applied per 36-UI-SPEC (GAP-7). Bundles rebuilt; full frontend suite + middleware regression gate + lint green with no middleware files changed.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-14-SUMMARY.md` when done (include the GAP-6 cache/hard-refresh guidance).
</output>
</content>
