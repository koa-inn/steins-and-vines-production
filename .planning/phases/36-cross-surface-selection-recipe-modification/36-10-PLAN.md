---
phase: 36-cross-surface-selection-recipe-modification
plan: 10
type: execute
wave: 3
depends_on: [36-08, 36-09]
files_modified:
  - kiosk.html
  - js/kiosk.js
  - js/kiosk.min.js
  - js/admin.min.js
  - js/main.min.js
  - js/batch.min.js
  - js/brewpad.min.js
  - css/kiosk.css
  - css/kiosk.min.css
  - css/admin.min.css
  - css/styles.min.css
  - css/batch.min.css
  - css/brewpad.min.css
  - tests/frontend/kiosk-recipe-modify.test.js
  - tests/frontend/kiosk-recipe-volume-factor.test.js
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01]
must_haves:
  truths:
    - "GAP-3: the kiosk volume control has a synced ×factor input identical to admin; both sync directions work with bounds + no-base disable (SEL-01, D-01)"
    - "GAP-2: the kiosk modify panel pre-populates base ingredients on expand (edit-at-base, D-04) and uses the polished/reordered layout consistent with admin"
    - "The ×factor input keeps the iOS zoom guard (font-size:1rem) so kiosk iPad does not auto-zoom"
    - "The ×factor math is client-side display only; the charged price still comes from GET /api/kiosk/recipe-quote"
  artifacts:
    - path: "kiosk.html"
      provides: "× factor input markup beside #kiosk-target-volume with iOS zoom guard"
      contains: "kiosk-target-factor"
    - path: "js/kiosk.js"
      provides: "Ported synced factor wiring + polished layout"
      contains: "kiosk-target-factor"
    - path: "tests/frontend/kiosk-recipe-volume-factor.test.js"
      provides: "factor↔litres sync + bounds + no-base disable tests for kiosk"
  key_links:
    - from: "js/kiosk.js factor input handler"
      to: "kioskScheduleRecipeQuote"
      via: "litres recompute then debounced re-quote"
      pattern: "kioskScheduleRecipeQuote"
---

<objective>
Port the admin gap-closure work to the KIOSK recipe-sale surface (D-01 — identical control on all three surfaces):
- GAP-3 — synced "× factor" input beside Target volume (L), two-way synced, with the kiosk iOS zoom guard (font-size:1rem).
- GAP-2 — polished/reordered volume + modify-panel layout consistent with admin; confirm base ingredients pre-populate on expand (edit-at-base, D-04 — already wired at kiosk.js:1592-1605).

Note: GAP-1 (catalog-on-open) does NOT apply to kiosk — kiosk.js:1614 already lazy-loads the catalog via `kioskLoadIngredientCatalog()` on panel open. No kiosk catalog fix needed (per UAT scope_correction). Save-as-new is intentionally NOT exposed on kiosk (UI-SPEC §2).

Purpose: keep the control byte-for-byte consistent with the admin reference (36-09).
Output: kiosk.html markup, kiosk.js wiring, kiosk.css polish, rebuilt bundles, and a new factor test (+ any needed extension to the existing kiosk-recipe-modify test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-09-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Real, current kiosk.js anchors (verified). Mirror the admin (36-09) implementation. -->

kiosk.js volume-control wiring (inside kioskShowRecipePrompt):
  ~line 1531: var volWrap = document.getElementById('kiosk-recipe-volume-wrap');
  ~line 1532: var volInput = document.getElementById('kiosk-target-volume');
  ~line 1533: var factorRdout = document.getElementById('kiosk-scale-factor-readout');
  (litres oninput → recompute factor + readout → kioskScheduleRecipeQuote)

kiosk.js modify panel (already lazy-loads catalog — no GAP-1 here):
  ~line 1571: _kioskModifiedIngredients = null;  // reset on recipe change
  ~line 1592-1605: on first expand, deep-copy base ingredients into _kioskModifiedIngredients (D-04)
  ~line 1614: kioskLoadIngredientCatalog();  // catalog already loaded on open
  ~line 851:  function kioskLoadIngredientCatalog()
  ~line 796-797: kioskFetchRecipeQuote appends &modified_ingredients=... when modified

kiosk.js state (~line 741): var _kioskModifiedIngredients = null;
kiosk.js quote/debounce: kioskFetchRecipeQuote / kioskScheduleRecipeQuote (mirror admin)

iOS zoom guard: all kiosk inputs need font-size:1rem (UI-SPEC Typography rule).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port the synced ×factor input + polished layout to kiosk (markup + wiring + CSS + tests)</name>
  <files>kiosk.html, js/kiosk.js, css/kiosk.css, tests/frontend/kiosk-recipe-volume-factor.test.js</files>
  <read_first>
    - 36-09-SUMMARY.md (the admin implementation this ports — copy the factor handler + layout verbatim, swapping the kiosk class/iOS guard)
    - 36-UI-SPEC.md §"Synced ×factor Control" and §"Modify-Panel Polish & Ordering"
    - kiosk.html `#kiosk-recipe-prompt` / `#kiosk-recipe-volume-wrap` block
    - js/kiosk.js lines 1531-1630 (volume wiring + modify toggle + deep-copy base ingredients)
    - css/kiosk.css volume/modify rules + the existing font-size:1rem inputs (iOS guard)
  </read_first>
  <behavior>
    - Test KFAC-1 (factor → litres): factor 1.5 on a base-20L recipe sets `_kioskTargetVolumeL` to 30 and the litres input value to 30.
    - Test KFAC-2 (litres → factor): litres 30 on base-20L sets the factor input value to "1.50".
    - Test KFAC-3 (bounds): factor ≤ 0 rejected; factor > 10 clamped so litres ≤ base×10.
    - Test KFAC-4 (no-base disable): no batch_size_l disables BOTH litres and factor inputs and shows the "Set batch size (L)…" copy.
    - Test KFAC-5 (display-only): editing factor calls kioskScheduleRecipeQuote(); no client price.
    - Test KFAC-6 (iOS guard): the #kiosk-target-factor input has font-size:1rem (inline style or class) to prevent iOS auto-zoom.
  </behavior>
  <action>
    1. kiosk.html: inside `#kiosk-recipe-volume-wrap`, add `<input type="number" id="kiosk-target-factor" class="kiosk-volume-input" min="0.1" step="0.1" inputmode="decimal" style="font-size:1rem;" />` with a static "× factor" label, RIGHT of `#kiosk-target-volume` on the same row (identical IDs to admin per D-01; the only difference is the iOS font-size guard). Keep `#kiosk-scale-factor-readout` beneath the row.
    2. css/kiosk.css: mirror the admin flex one-row layout (litres + factor side-by-side, readout beneath, 44px min-height touch targets, no greyed inputs unless disabled). Apply the reordered/polished layout from the UI-SPEC. Ensure the factor input keeps font-size:1rem.
    3. js/kiosk.js volume wiring (~line 1531+): add `var factorInput = document.getElementById('kiosk-target-factor');`. Copy the admin (36-09) factor handler verbatim: on recipe select with baseVol>0 set factorInput.value='1.0', disabled=false, max=10; no-base ⇒ disabled=true (KFAC-4). `factorInput.oninput` clamps factor to (0,10], computes litres = nearest-0.5(factor*baseVol), writes volInput, updates `_kioskTargetVolumeL`/`_kioskScaleFactor`/readout, calls `kioskScheduleRecipeQuote()` (KFAC-1/5). Extend the existing litres `oninput` to also set `factorInput.value = factor.toFixed(2)` (KFAC-2). Add the "client-side display only — server authoritative (D-06)" comment. ES5/var, escapeHTML for any dynamic text.
    4. Create tests/frontend/kiosk-recipe-volume-factor.test.js implementing KFAC-1..KFAC-6, reusing the require + DOM-fixture pattern from tests/frontend/kiosk-recipe-modify.test.js and kiosk-recipe-quote.test.js. Stub fetch with jest.fn (assert re-quote called for KFAC-5; assert no /api/recipes POST so save-as-new stays absent on kiosk). Assert the factor input font-size for KFAC-6.
    5. If the existing kiosk-recipe-modify test needs new DOM nodes (factor input) to keep passing, add only the node to its fixture — do NOT alter existing assertions (CLAUDE.md rule 10).
    6. Run `npm test -- kiosk-recipe-volume-factor kiosk-recipe-modify kiosk-recipe-quote` and `npm run lint`.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/kiosk-recipe-volume-factor.test.js 2>&1 | grep -qE "KFAC-1|Tests:.*passed" && grep -q "kiosk-target-factor" kiosk.html && grep -q "kiosk-target-factor" js/kiosk.js && grep -q "font-size:1rem" kiosk.html && echo PASS</automated>
  </verify>
  <done>kiosk.html has the #kiosk-target-factor input (with iOS zoom guard) beside the litres box; kiosk.js syncs factor↔litres both directions with bounds + no-base disable + re-quote; kiosk.css renders the polished one-row layout consistent with admin; KFAC-1..KFAC-6 pass; save-as-new stays absent on kiosk; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build kiosk bundles and run full frontend suite + middleware regression gate</name>
  <files>js/kiosk.min.js, js/admin.min.js, js/main.min.js, js/batch.min.js, js/brewpad.min.js, css/kiosk.min.css, css/admin.min.css, css/styles.min.css, css/batch.min.css, css/brewpad.min.css</files>
  <read_first>
    - package.json "build" script (regenerates ALL min files)
    - CLAUDE.md "Build Artifacts" + "Before Every Commit" rules (rule 1: run npm test AND cd zoho-middleware && npm test before every commit)
  </read_first>
  <action>
    Run `npm run build` to regenerate the bundles (never hand-edit *.min.js). The build rewrites all min outputs — expected; this is why the three surface plans are sequenced into separate waves. Then run the FULL frontend suite `npm test` and `npm run lint`. This plan changes NO middleware files, but per CLAUDE.md rule 1 the middleware test suite must still run before commit as a regression gate — run `cd zoho-middleware && npm test` (it is expected to PASS unchanged; do NOT modify any middleware files or author new middleware tests — the money path is frozen).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build >/dev/null 2>&1 && grep -q "kiosk-target-factor" js/kiosk.min.js && npm test 2>&1 | grep -qE "Tests:.*passed" && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>`npm run build` succeeds; kiosk.min.js contains the factor wiring; the full frontend Jest suite passes; the middleware regression gate (`cd zoho-middleware && npm test`) passes unchanged with no middleware files modified; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk iPad browser → /api/kiosk/recipe-quote | Client sends recipe_id + target_volume_l (+ modified_ingredients); server returns the authoritative price. The factor input only changes the displayed target_volume_l. |
| kiosk iPad browser → /api/ingredients | Catalog fetch for the modify-panel autocomplete (already lazy-loaded on open). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-10-01 | Tampering | ×factor → price | mitigate | factor↔litres sync is client-side display only; the charge comes from the server quote (KFAC-5 asserts re-quote). Comment + test lock it. |
| T-36-10-02 | Tampering / XSS | modify-row ingredient names on kiosk | mitigate | Row rendering reuses the existing escapeHTML path; the static "× factor" label needs no escaping. |
| T-36-10-03 | Elevation of privilege | save-as-new on kiosk | mitigate | Kiosk must NOT expose save-as-new (UI-SPEC §2). Test asserts no /api/recipes POST path is wired on kiosk. |
| T-36-10-SC | Tampering | npm installs | accept | No new package installs — only existing devDeps via npm run build/test. No legitimacy gate needed. |
</threat_model>

<verification>
- `npx jest tests/frontend/kiosk-recipe-volume-factor.test.js` passes (KFAC-1..KFAC-6).
- `grep kiosk-target-factor` matches in kiosk.html, js/kiosk.js, js/kiosk.min.js.
- `grep "font-size:1rem"` present on the kiosk factor input (iOS guard).
- `npm test` full frontend suite green; `cd zoho-middleware && npm test` green (regression gate, unchanged); `npm run lint` clean.
- Middleware untouched (no zoho-middleware files modified); save-as-new not exposed on kiosk.
</verification>

<success_criteria>
The kiosk recipe-sale surface has a synced ×factor control identical to admin (both sync directions, bounds, no-base disable, iOS zoom guard, display-only with server-authoritative price) and the polished/reordered modify-panel layout with base-ingredient pre-population. Bundles rebuilt; full frontend suite green; middleware regression gate green with no middleware files changed.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-10-SUMMARY.md` when done.
</output>
