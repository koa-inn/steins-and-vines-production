---
phase: 36-cross-surface-selection-recipe-modification
plan: 05
type: execute
wave: 3
depends_on: ["36-03"]
files_modified:
  - kiosk.html
  - js/kiosk.js
  - tests/frontend/kiosk-recipe-modify.test.js
autonomous: true
requirements: [SEL-01, SEL-02, MOD-01, MOD-02]
must_haves:
  truths:
    - "The kiosk recipe-sale surface now has the SAME target-volume control as admin (ported verbatim) with the identical factor readout and no-base disable (SEL-01, D-01)"
    - "Kiosk staff can add/remove/change ingredients at base quantities; the modified list is priced server-authoritatively via recipe-quote and carried into the sale/snapshot (MOD-01, MOD-02, SEL-02)"
    - "Save-as-new is NOT exposed on kiosk (UI-SPEC §2); the modified list still flows into the sale"
  artifacts:
    - path: "kiosk.html"
      provides: "Ported #kiosk-recipe-volume-wrap + #kiosk-stock-conflict + #kiosk-recipe-modify-wrap with iOS-zoom-safe inputs"
      contains: "kiosk-recipe-volume-wrap"
    - path: "js/kiosk.js"
      provides: "Ported Phase 35 volume/quote state + Phase 36 modify panel + catalog load"
      contains: "_kioskModifiedIngredients"
    - path: "tests/frontend/kiosk-recipe-modify.test.js"
      provides: "Tests for volume control port, modified quote URL, (Modified) label"
      contains: "modified_ingredients"
  key_links:
    - from: "js/kiosk.js kioskFetchRecipeQuote"
      to: "GET /api/kiosk/recipe-quote"
      via: "target_volume_l + modified_ingredients params"
      pattern: "recipe-quote"
---

<objective>
Port the Phase 35 target-volume control to the standalone kiosk surface (kiosk.html / js/kiosk.js) — which today LACKS it — and add the identical Phase 36 ingredient-modification panel + server-authoritative price preview. This closes SEL-01 on kiosk (the same control on every surface, D-01), SEL-02 (the chosen size carries into the sale/snapshot), and MOD-01/MOD-02 (modified sale priced server-side). Save-as-new is deliberately NOT exposed on kiosk (UI-SPEC §2) — the modified list still flows into the sale.

Purpose: kiosk.js is a separate bundle from admin.js; the Phase 35 control and 35-06 quote wiring exist only in admin.js and must be ported, not imported.
Output: kiosk.html markup + js/kiosk.js wiring + tests + rebuilt kiosk bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md

<interfaces>
Quote endpoint (36-03): GET /api/kiosk/recipe-quote?recipe_id=&sale_type=&target_volume_l=&modified_ingredients=<JSON> -> { ok, recipe_id, total, scale_factor, pricing_mode, is_modified, ingredients[...], stock }.
Source to PORT FROM (js/admin.js kiosk IIFE): volume wiring 11150-11190, kioskFetchRecipeQuote/kioskScheduleRecipeQuote 10965-10998, kioskUpdateAddToCartButton 11285-11314, loadIngredientCatalogForRecipes 8519-8546, attachIngredientRowListeners/showIngredientAutocomplete 8847-8953.
Existing js/kiosk.js: state ~725-731 (_kioskRecipeContext, MISSING Phase 35 vars), kioskMwUrl 753-756, kioskShowRecipePrompt 1180-1315, kioskSelectSaleType 1316-1335, kioskUpdateAddToCartButton 1337-1358, kioskAddRecipeToCart/processRecipeData 1403-1513. API key header: SHEETS_CONFIG.MW_API_KEY (line 1056).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: kiosk.html — port Phase 35 control + add modify panel (iOS-zoom-safe)</name>
  <read_first>
    - kiosk.html lines 108-125 (current #kiosk-recipe-prompt — lacks the Phase 35 control)
    - admin.html lines 626-659 (the LOCKED Phase 35 markup to port)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 90-138 (kiosk.html port instructions + iOS zoom guard) and lines 60-86 (modify markup)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md lines 165-191 (kiosk placement + touch-friendliness) and lines 279-300 (copy)
  </read_first>
  <action>
    Replace the current kiosk.html #kiosk-recipe-prompt body with the full admin structure (PATTERNS lines 109-135): port #kiosk-recipe-volume-wrap (label + #kiosk-target-volume number input min 0.5 step 0.5 + #kiosk-scale-factor-readout), #kiosk-recipe-modify-wrap (same IDs/classes as admin: #kiosk-modify-toggle, #kiosk-modify-tbody, #kiosk-modify-add-row, #kiosk-recipe-price-preview, #kiosk-locked-price-notice), and #kiosk-stock-conflict (with #kiosk-stock-override-btn), keeping #kiosk-avail-banner and the existing sale-type/Add-to-Cart markup. Add style="font-size:1rem;" (or a CSS class) to ALL <input> elements inside the prompt to prevent iOS Safari auto-zoom (PATTERNS line 138, UI-SPEC line 180). Do NOT add the save-as-new markup (UI-SPEC §2: not exposed on kiosk). Use exact UI-SPEC copy.
  </action>
  <verify>
    <automated>grep -c "kiosk-recipe-volume-wrap\|kiosk-target-volume\|kiosk-recipe-modify-wrap\|kiosk-modify-tbody\|kiosk-stock-conflict" kiosk.html</automated>
  </verify>
  <acceptance_criteria>
    - The grep prints 5 (all five IDs now present in kiosk.html — they were absent before).
    - `grep -c "kiosk-save-as-new-btn" kiosk.html` is 0 (save-as-new not exposed on kiosk).
    - Every <input> inside #kiosk-recipe-prompt has font-size:1rem (verify: `grep -n "font-size:1rem\|font-size: 1rem" kiosk.html` shows the prompt inputs).
  </acceptance_criteria>
  <done>Phase 35 control + Phase 36 modify panel ported into kiosk.html with iOS-zoom-safe inputs; no save-as-new.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: js/kiosk.js — port volume/quote state + add modify panel + catalog load</name>
  <read_first>
    - js/kiosk.js lines 725-731 (state), 753-756 (kioskMwUrl), 1180-1315 (kioskShowRecipePrompt), 1316-1335 (kioskSelectSaleType), 1337-1358 (kioskUpdateAddToCartButton), 1403-1513 (kioskAddRecipeToCart/processRecipeData), 1056 (api key header)
    - js/admin.js lines 10965-10998, 11150-11190, 11285-11314, 8519-8546, 8847-8953 (the source functions to port)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 370-448 (kiosk.js port sketch incl. state vars, kioskLoadIngredientCatalog, kioskShowRecipePrompt wiring, kioskSelectSaleType re-quote, processRecipeData quote use) and Shared Patterns (799-817) data-ing-idx caveat
  </read_first>
  <behavior>
    - Selecting a recipe on kiosk pre-fills the volume input at base batch_size_l, shows the "1.0x base N L" readout, and disables the input with the "Set batch size…" message when no base size (D-01/D-11).
    - Changing the volume re-quotes (debounced) and updates the Add-to-Cart price from the server quote (no client-side pricing).
    - Expanding Modify Ingredients renders grouped editable rows; add/remove/change updates _kioskModifiedIngredients and re-quotes with modified_ingredients; price preview reflects the modified total; Add-to-Cart gains "(Modified)".
    - Adding to cart uses _kioskQuote.ingredients (scaled+modified) for line items (carry-through into the sale, SEL-02/MOD-02).
    - Back/clear resets _kioskModifiedIngredients to null and collapses the panel.
  </behavior>
  <action>
    Port the Phase 35 state vars (_kioskTargetVolumeL, _kioskScaleFactor, _kioskStockOverride, _kioskQuote, _kioskQuoteTimer) and add _kioskModifiedIngredients=null, _kioskModifyPanelOpen=false (PATTERNS 376-385). Port kioskFetchRecipeQuote + kioskScheduleRecipeQuote (admin 10965-10998) extended with the modified_ingredients param. Port the volume-control wiring block (admin 11150-11190) into kioskShowRecipePrompt verbatim (same IDs). Add kioskScheduleRecipeQuote() to kioskSelectSaleType (PATTERNS 405-408). Port kioskUpdateAddToCartButton's quote-price + "(Modified)" suffix logic. Port loadIngredientCatalogForRecipes as kioskLoadIngredientCatalog (PATTERNS 425-445) and call it when the prompt opens. Add renderKioskModifyRows + attachKioskModifyRowListeners (grouped via groupRecipeIngredients if available, data-ing-idx=indexOf(ing), escapeHTML, empty-state row). On first modify-toggle expand, deep-copy the recipe base ingredients into _kioskModifiedIngredients (never mutate the source). Extend processRecipeData to use _kioskQuote.ingredients when matched (PATTERNS 411-419). Reset on back/clear. Export test helpers (_kioskGetModifiedIngredients/_kioskSetModifiedIngredients + any needed for jsdom). ES5/var; escapeHTML.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/kiosk-recipe-modify.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/frontend/kiosk-recipe-modify.test.js (new) asserts: the volume input pre-fills batch_size_l and the readout shows "1.0x base N L"; kioskFetchRecipeQuote URL includes target_volume_l and (when modified) modified_ingredients; kioskUpdateAddToCartButton appends "(Modified)"; the source recipe object is unchanged after edits. All pass.
    - `grep -n "_kioskModifiedIngredients\|kioskLoadIngredientCatalog\|indexOf(ing)" js/kiosk.js` shows the state var, the catalog loader, and the flat-index data-ing-idx.
    - `grep -c "kioskSaveAsNewRecipe" js/kiosk.js` is 0 (save-as-new intentionally absent on kiosk).
  </acceptance_criteria>
  <done>Volume control + quote wiring + modify panel ported to kiosk.js; modified list priced via server quote; source recipe never mutated; tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Build kiosk bundle + lint</name>
  <read_first>
    - CLAUDE.md "Build Artifacts" + "kiosk.js and brewpad.js are standalone bundles" note in UI-SPEC lines 335-336
  </read_first>
  <action>
    Run `npm run build` to regenerate js/kiosk.min.js (and cache-bust stamps). Then run `npm run lint` and the full frontend suite; fix any lint/test failures introduced by this plan only.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build && npm run lint && npx jest tests/frontend/kiosk-recipe-modify.test.js tests/frontend/kiosk-recipe-quote.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0; `grep -c "_kioskModifiedIngredients" js/kiosk.min.js` is >= 1 (wiring made it into the bundle).
    - `npm run lint` exits 0.
    - Both named kiosk test files pass.
  </acceptance_criteria>
  <done>kiosk bundle rebuilt, lint clean, kiosk tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kiosk iPad browser -> middleware | Staff-operated POS; modified ingredient list is client-originated; pricing must remain server-authoritative. |
| dynamic HTML rendering | Ingredient names rendered into modify rows on the iPad surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-14 | Tampering | client price computation on kiosk | mitigate | kiosk.js only previews via the recipe-quote endpoint; the charge comes from /confirm (36-03). No price math in kiosk.js. Test asserts the displayed price is sourced from the quote. |
| T-36-15 | Injection (XSS) | ingredient names in modify rows | mitigate | escapeHTML wraps all dynamic names (project rule). Test injects a malicious name and asserts it is escaped. |
| T-36-16 | Elevation of Privilege | unauthenticated kiosk sale | mitigate | All quote/sale/confirm calls carry x-api-key (MW_API_KEY) and hit the BEER_SALES_ENABLED-gated routes; kiosk is staff-only (UI-SPEC line 189). No additional permission boundary surfaced. No change weakens existing guards. |
| T-36-SC | Tampering | npm/pip installs | accept | No new packages. No package legitimacy gate required. |
</threat_model>

<verification>
- `npx jest tests/frontend/kiosk-recipe-modify.test.js tests/frontend/kiosk-recipe-quote.test.js` green.
- `npm test` full frontend suite green; `npm run lint` clean; `npm run build` exits 0.
- Cross-surface human UAT deferred to 36-08.
</verification>

<success_criteria>
Kiosk has the identical Phase 35 volume control (SEL-01/D-01), a working modify panel priced server-authoritatively (MOD-01/MOD-02), the chosen size + modified list carried into the sale (SEL-02), and no save-as-new affordance; kiosk bundle rebuilt.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-05-SUMMARY.md` when done.
</output>
