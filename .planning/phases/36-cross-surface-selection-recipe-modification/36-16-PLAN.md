---
phase: 36-cross-surface-selection-recipe-modification
plan: 16
type: execute
wave: 4
depends_on: [36-13, 36-14, 36-15]
files_modified:
  - brewpad.html
  - js/brewpad.js
  - css/brewpad.css
  - tests/frontend/brewpad-recipe-attach-modify.test.js
  - js/brewpad.min.js
  - js/main.min.js
  - js/admin.min.js
  - js/kiosk.min.js
  - js/batch.min.js
  - css/brewpad.min.css
  - css/styles.min.css
  - css/admin.min.css
  - css/kiosk.min.css
  - css/batch.min.css
autonomous: true
gap_closure: true
requirements: [SEL-01, MOD-01]
must_haves:
  truths:
    - "GAP-5: the BrewPad expanded attach panel renders INSIDE the scrollable .bp-batch-detail-pane (via sectionBodyEl) instead of the clipped global #bp-recipe-attach-expanded sibling, so the full ingredient list AND the Attach Recipe button are reachable on the iPad"
    - "GAP-7: the BrewPad audit polish is applied — .bp-ing-remove is 44px×44px, the × factor label has no trailing colon (matches admin/kiosk), and touch targets/spacing follow 36-UI-SPEC"
    - "BrewPad attach still shows NO price, makes NO quote and NO charge after the restructure (D-10) — the factor only changes the recorded target_volume_l"
  artifacts:
    - path: "js/brewpad.js"
      provides: "wireAttachExpandedPanel injects the expanded panel content into sectionBodyEl (scrollable detail pane)"
      contains: "sectionBodyEl"
    - path: "brewpad.html"
      provides: "× factor label colon removed; attach-expanded becomes a template/injected fragment"
      contains: "bp-target-factor"
    - path: "css/brewpad.css"
      provides: ".bp-ing-remove 44px + scrollable detail-pane styling"
      contains: "bp-ing-remove"
  key_links:
    - from: "js/brewpad.js wireAttachExpandedPanel"
      to: ".bp-batch-detail-pane (scrollable sectionBodyEl)"
      via: "expanded panel injected into the scrollable pane, NOT the clipped sibling (GAP-5)"
      pattern: "sectionBodyEl"
---

<objective>
Close GAP-5 and GAP-7 on the BREWPAD recipe-attach surface (brewpad.html / js/brewpad.js / css/brewpad.css). BrewPad has NO money path — GAP-4 does NOT apply here (D-10: no price preview, no quote, no charge). The control stays identical to admin/kiosk minus the price (D-01).

- GAP-5 (BLOCKER) — `#bp-recipe-attach-expanded` is a sibling of `.bp-batches-layout` inside `.bp-batches-panel { overflow:hidden }` (brewpad.css:515), so the expanded modify panel + Attach button get clipped on long recipes. Restructure so the expanded panel content is rendered INSIDE the scrollable `.bp-batch-detail-pane` (via the `sectionBodyEl` passed to `wireAttachExpandedPanel`), not the clipped global sibling. The static `#bp-recipe-attach-expanded` becomes a template fragment / is injected into the scrollable pane.
- GAP-7 (enhancement) — apply the BrewPad audit polish: `.bp-ing-remove { min-height:44px; min-width:44px; padding:10px 12px; }` (currently 36px, C5); remove the trailing colon from the `× factor:` label (brewpad.html:103, M1) so it matches "× factor" on admin/kiosk; confirm touch targets + spacing follow 36-UI-SPEC. Keep the no-price/no-quote/no-charge attach semantics (D-10).

FRONT-END ONLY — no zoho-middleware files. brewpad.js is a STANDALONE bundle.

Purpose: staff must reach the Attach button on long recipes on the iPad, and the modify-row controls must be touch-safe — without ever introducing a price/charge on the record-keeping attach surface.
Output: brewpad.html template/markup change, brewpad.js panel-injection refactor, brewpad.css polish, rebuilt bundles, an extension to the existing attach-modify test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-REVIEW.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-11-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Verified live anchors (2026-06-22). brewpad.js is a STANDALONE bundle. -->

brewpad.html (verified):
  73: <div id="bp-panel-batches" class="bp-panel bp-batches-panel" ...>
  83: <div class="bp-batch-detail-pane" id="bp-batch-detail-pane" ...>   ← SCROLLABLE target pane
  94: <div id="bp-recipe-attach-expanded" style="display:none;">         ← clipped global sibling (GAP-5)
  103: <label>&times; factor:</label>                                    ← drop the trailing colon (M1)

css/brewpad.css:
  515: .bp-batches-panel { overflow:hidden; ... }   ← the clip source (GAP-5)
  535: .bp-batch-detail-pane { ... overflow-y:auto ... }  ← the scrollable pane to inject into
  2537: .bp-ing-remove { min-height:36px; min-width:36px }  ← bump to 44px (C5)
  .bp-input already font-size:1rem (iOS guard OK)

js/brewpad.js (from 36-11, verified anchors):
  ~4026: function wireAttachExpandedPanel(b, sectionBodyEl)   ← sectionBodyEl lives inside .bp-batch-detail-pane
  ~4036: loadIngredientCatalogForRecipes().then(...)          ← catalog already loaded on open (GAP-1 N/A)
  ~4046-4072: volume-wrap + #bp-target-volume + #bp-target-factor wiring (synced factor, NO quote, D-10)
  ~4094-4118: deep-copy base ingredients into _bpModifiedIngredients on first expand (edit-at-base, D-04)
  snapshot build (~1257-1310): target_volume_l from _bpTargetVolumeL → update_batch (NO charge, D-10)

D-10 INVARIANT: BrewPad attach NEVER calls /api/kiosk/recipe-quote, /recipe-sale, or Helcim, and shows
NO price preview. The restructure must not introduce any of these. The factor only changes the recorded
target_volume_l.

NO zoho-middleware files. NEVER hand-edit *.min.js — run npm run build.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GAP-5 — inject the expanded attach panel into the scrollable detail pane; GAP-7 polish (markup + wiring + CSS + tests)</name>
  <files>brewpad.html, js/brewpad.js, css/brewpad.css, tests/frontend/brewpad-recipe-attach-modify.test.js</files>
  <read_first>
    - 36-UI-SPEC.md §"Scroll Model (GAP-5)" (BrewPad detail-pane injection) + §"Audit Polish (GAP-7…)" (.bp-ing-remove 44px, × factor colon)
    - 36-UI-REVIEW.md §"Top 3 Priority Fixes" #3 (the bp-recipe-attach-expanded restructure) + Consolidated Fix List C2/C5/M1
    - 36-11-SUMMARY.md (current wireAttachExpandedPanel + factor wiring shipped — preserve the no-quote/no-charge path)
    - brewpad.html lines 73-110 (bp-batches-panel, bp-batch-detail-pane, #bp-recipe-attach-expanded, × factor label)
    - js/brewpad.js lines 4020-4130 (wireAttachExpandedPanel, sectionBodyEl) and the snapshot build (~1257-1310)
    - css/brewpad.css lines 515-540 (.bp-batches-panel / .bp-batch-detail-pane overflow) and 2537-2546 (.bp-ing-remove)
  </read_first>
  <behavior>
    - Test BSC-1 (panel inside scrollable pane): after wireAttachExpandedPanel runs, the expanded attach panel content is a descendant of the scrollable detail pane (sectionBodyEl / .bp-batch-detail-pane), NOT a child of the clipped .bp-batches-panel sibling. Assert via DOM containment.
    - Test BSC-2 (Attach button reachable): the #bp-recipe-attach-confirm-btn (or equivalent Attach Recipe button) is present inside the injected/scrollable container (not orphaned in the clipped sibling).
    - Test BSC-3 (no quote/charge after restructure, D-10): expanding/attaching still makes NO call to /api/kiosk/recipe-quote, /recipe-sale, or any Helcim path; stub global.fetch + adminApiPost and assert none of those URLs are hit. (Regression-guard the D-10 invariant through the DOM move.)
    - Test BSC-4 (factor still records volume): editing #bp-target-factor still updates _bpTargetVolumeL / the recorded target_volume_l (the restructure didn't break the synced control) and still calls NO quote.
  </behavior>
  <action>
    1. GAP-5 restructure (C2): change `wireAttachExpandedPanel(b, sectionBodyEl)` in brewpad.js so the expanded panel's content is rendered/appended INTO `sectionBodyEl` (which lives inside the scrollable `.bp-batch-detail-pane`) rather than toggling the static `#bp-recipe-attach-expanded` global sibling's `display`. Two viable approaches — pick the lower-risk one and document it in the SUMMARY:
       (a) In brewpad.html, convert `#bp-recipe-attach-expanded` (line 94) into a `<template id="bp-recipe-attach-expanded-tpl">` (or keep it hidden as a source fragment) and, in wireAttachExpandedPanel, clone its inner content into sectionBodyEl on open and remove it on close/recipe-change; OR
       (b) Keep the markup but move the element node into sectionBodyEl at wire time (appendChild) and back/hidden on teardown.
       Either way: the expanded panel + Attach button must end up as descendants of the scrollable detail pane so they are never clipped by `.bp-batches-panel{overflow:hidden}`. Preserve all existing IDs (`#bp-target-volume`, `#bp-target-factor`, `#bp-scale-factor-readout`, the modify table, the Attach button) so the existing wiring + tests keep resolving them. ES5/var; escapeHTML for dynamic text.
    2. GAP-7 (M1): remove the trailing colon — change `&times; factor:` to `&times; factor` at brewpad.html:103 so it matches admin/kiosk.
    3. GAP-7 (C5): css/brewpad.css `.bp-ing-remove` → `min-height:44px; min-width:44px; padding:10px 12px;`. Confirm the other modify-row inputs/buttons retain ≥44px per 36-UI-SPEC. Ensure the injected panel inherits the scrollable pane's overflow (no new overflow:hidden wrapper introduced around it).
    4. D-10 guard: do NOT add any price-preview element, quote call, or charge to the attach path. The factor remains display/record-only.
    5. Extend tests/frontend/brewpad-recipe-attach-modify.test.js with BSC-1..BSC-4 using the existing require + DOM-fixture pattern (build a fixture with .bp-batch-detail-pane + sectionBodyEl). For BSC-3 stub global.fetch + adminApiPost and assert NO recipe-quote/recipe-sale/Helcim URL is hit. Do NOT change existing assertions (CLAUDE.md rule 10) — only add the new fixture nodes/cases needed.
    6. Run `npm test -- brewpad-recipe-attach-modify` and `npm run lint`.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/brewpad-recipe-attach-modify.test.js 2>&1 | grep -qE "BSC-1|Tests:.*passed" && grep -q "sectionBodyEl" js/brewpad.js && ! grep -q "times; factor:" brewpad.html && grep -qE "bp-ing-remove" css/brewpad.css && echo PASS</automated>
  </verify>
  <done>The expanded attach panel (incl. the Attach Recipe button) renders inside the scrollable .bp-batch-detail-pane so it is never clipped; the × factor label has no trailing colon; .bp-ing-remove is 44px×44px; the attach path still makes NO quote/charge and shows NO price (D-10); BSC-1..BSC-4 pass; lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build BrewPad bundles + run full frontend suite + middleware regression gate</name>
  <files>js/brewpad.min.js, js/main.min.js, js/admin.min.js, js/kiosk.min.js, js/batch.min.js, css/brewpad.min.css, css/styles.min.css, css/admin.min.css, css/kiosk.min.css, css/batch.min.css</files>
  <read_first>
    - package.json "build" script (regenerates ALL min files)
    - CLAUDE.md "Build Artifacts" + "Before Every Commit" (rule 1: npm test AND cd zoho-middleware && npm test before commit; rule 2: lint clean)
  </read_first>
  <action>
    Run `npm run build` to regenerate the bundles (never hand-edit *.min.js — the build rewrites all min outputs). Then run the FULL frontend suite `npm test` and `npm run lint`. This plan changes NO middleware files, but per CLAUDE.md rule 1 the middleware suite must still run as a regression gate — `cd zoho-middleware && npm test` (expected PASS unchanged; do NOT modify any middleware file or author middleware tests — money path frozen).
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build >/dev/null 2>&1 && grep -q "sectionBodyEl" js/brewpad.min.js && grep -q "bp-target-factor" js/brewpad.min.js && npm test 2>&1 | grep -qE "Tests:.*passed" && npm run lint >/dev/null 2>&1 && cd zoho-middleware && npm test 2>&1 | grep -qE "Tests:.*passed" && echo PASS</automated>
  </verify>
  <done>`npm run build` succeeds; brewpad.min.js carries the panel-injection + factor wiring; the full frontend Jest suite passes; the middleware regression gate (`cd zoho-middleware && npm test`) passes unchanged with no middleware files modified; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrewPad iPad browser → update_batch (Apps Script via adminApiPost) | Attach writes a scaled+modified recipe_snapshot with target_volume_l; NO pricing or charge crosses this boundary (D-10). |
| BrewPad iPad browser → /api/ingredients | Catalog fetch for the modify-panel autocomplete (already loaded on attach-panel open). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-16-01 | Tampering | attach path → charge | mitigate | The DOM restructure must NOT introduce a quote/sale/Helcim call or a price element; BrewPad attach stays record-keeping only (D-10). BSC-3 asserts no recipe-quote/recipe-sale/Helcim URL is hit after the move. |
| T-36-16-02 | Tampering / XSS | modify-row ingredient names in the injected panel | mitigate | The injected/cloned panel reuses the existing escapeHTML row-render path; the static "× factor" label needs no escaping. |
| T-36-16-03 | Tampering | recorded target_volume_l in snapshot | mitigate | The factor-derived litres still flow into the existing snapshot scaling path (BSC-4); no new write surface — reuses the verified update_batch snapshot. |
| T-36-16-SC | Tampering | npm installs | accept | No new package installs — only existing devDeps via npm run build/test. No legitimacy gate needed. |
</threat_model>

<verification>
- `npx jest tests/frontend/brewpad-recipe-attach-modify.test.js` passes BSC-1..BSC-4: the expanded panel + Attach button are descendants of the scrollable detail pane, and attach makes NO quote/charge (D-10).
- `grep sectionBodyEl` matches in brewpad.js + brewpad.min.js; the `× factor:` colon is gone; `.bp-ing-remove` is 44px.
- `npm test` full frontend suite green; `cd zoho-middleware && npm test` green (regression gate, unchanged); `npm run lint` clean; no middleware files modified.
</verification>

<success_criteria>
On the BrewPad attach surface the expanded modify panel + Attach Recipe button are reachable on long recipes because they render inside the scrollable detail pane (GAP-5); the × factor label matches the sale surfaces and the Remove button meets the 44px touch target (GAP-7); and attach still shows NO price and makes NO quote/charge (D-10). Bundles rebuilt; full frontend suite + middleware regression gate + lint green with no middleware files changed.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-16-SUMMARY.md` when done (document which restructure approach (a/b) was taken).
</output>
</content>
