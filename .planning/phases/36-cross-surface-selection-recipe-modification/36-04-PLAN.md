---
phase: 36-cross-surface-selection-recipe-modification
plan: 04
type: execute
wave: 3
depends_on: ["36-03"]
files_modified:
  - admin.html
  - js/admin.js
  - tests/frontend/admin-recipe-modify.test.js
autonomous: true
requirements: [MOD-01, MOD-02, MOD-03, SEL-01]
must_haves:
  truths:
    - "On the admin recipe-sale surface, staff can expand a Modify Ingredients panel to add/remove/change ingredients at base quantities without altering the saved recipe (MOD-01, D-03/D-04)"
    - "The admin price preview shows the server-authoritative modified total via the recipe-quote dry-run (MOD-02, D-06) and the Add-to-Cart label gains a (Modified) suffix"
    - "Staff can Save as new recipe (draft, dynamic, base quantities) leaving the original untouched (MOD-03, D-12/D-13/D-14)"
  artifacts:
    - path: "admin.html"
      provides: "#kiosk-recipe-modify-wrap modify panel + #kiosk-recipe-price-preview + save-as-new markup"
      contains: "kiosk-recipe-modify-wrap"
    - path: "js/admin.js"
      provides: "_kioskModifiedIngredients state, modify-row listeners, quote with modified_ingredients, save-as-new POST"
      contains: "_kioskModifiedIngredients"
    - path: "tests/frontend/admin-recipe-modify.test.js"
      provides: "Tests: modified flag in quote URL, (Modified) label, save-as-new payload shape"
      contains: "modified_ingredients"
  key_links:
    - from: "js/admin.js kioskFetchRecipeQuote"
      to: "GET /api/kiosk/recipe-quote?modified_ingredients=..."
      via: "_kioskModifiedIngredients JSON-encoded query param"
      pattern: "modified_ingredients"
    - from: "js/admin.js kioskSaveAsNewRecipe"
      to: "POST /api/recipes"
      via: "modified base list, pricing_mode:dynamic, status:draft"
      pattern: "/api/recipes"
---

<objective>
Extend the EXISTING Phase 35 admin recipe-sale control (`#kiosk-recipe-prompt`) with the Phase 36 ingredient-modification panel, the server-authoritative modified-price preview, the "(Modified)" Add-to-Cart label, and the Save-as-new-recipe affordance. The admin surface already has the target-volume control (Phase 35) and the recipe-quote wiring (35-06) — this plan adds modification on top (MOD-01/MOD-02) and save-as-new (MOD-03). SEL-01 on admin is already satisfied by Phase 35; this plan keeps that control and reuses it.

Purpose: Admin is the reference surface — the modify UX defined here is ported verbatim to kiosk (36-05) and brewpad (36-06).
Output: admin.html markup + admin.js wiring + frontend tests + rebuilt bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-CONTEXT.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md
@.planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md

<interfaces>
Quote endpoint (from 36-03): GET /api/kiosk/recipe-quote?recipe_id=&sale_type=&target_volume_l=&modified_ingredients=<JSON> -> { ok, recipe_id, total, scale_factor, pricing_mode, is_modified, ingredients[{item_id,item_name,unit,base_quantity,quantity,rate,line_total}], stock }.
Save endpoint: POST /api/recipes with { name, style, batch_size_l, pricing_mode:'dynamic', status:'draft', ingredients:[...] } -> 201 { ok, recipe_id }.
Existing admin.js kiosk IIFE state (lines 9806-9810): _kioskTargetVolumeL, _kioskScaleFactor, _kioskStockOverride, _kioskQuote, _kioskQuoteTimer. Existing functions: kioskFetchRecipeQuote (10965-10998), kioskScheduleRecipeQuote, kioskUpdateAddToCartButton (11285-11314), kioskShowRecipePrompt (volume wiring 11150-11190). Recipe-builder autocomplete: attachIngredientRowListeners/showIngredientAutocomplete (8847-8953), loadIngredientCatalogForRecipes (8519-8546). SHEETS_CONFIG.MW_API_KEY header pattern (9814-9817).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: admin.html modify panel + price preview + save-as-new markup</name>
  <read_first>
    - admin.html lines 626-659 (the LOCKED Phase 35 #kiosk-recipe-prompt structure) — insert points
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 60-86 (exact new markup to insert) and lines 27-58 (surrounding structure)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md lines 101-162 (admin modify panel layout, placement order, copy)
  </read_first>
  <action>
    Insert #kiosk-recipe-modify-wrap between #kiosk-recipe-volume-wrap and #kiosk-stock-conflict (UI-SPEC order: volume -> modify -> stock-conflict -> avail-banner -> sale-type). Per PATTERNS lines 60-86: collapsible #kiosk-modify-toggle ("Modify Ingredients") -> #kiosk-modify-panel (table.kiosk-recipe-modify-table with tbody#kiosk-modify-tbody + #kiosk-modify-add-row "+ Add Ingredient") -> #kiosk-recipe-price-preview -> #kiosk-locked-price-notice (the D-07/D-08 asymmetry copy, --ink-tertiary 11px). After #kiosk-avail-banner add #kiosk-save-as-new-wrap (button#kiosk-save-as-new-btn + inline prompt #kiosk-save-as-new-prompt with input#kiosk-new-recipe-name + #kiosk-save-draft-btn + #kiosk-save-cancel-btn + #kiosk-save-as-new-feedback). Use exact copy from UI-SPEC Copywriting Contract (lines 273-300). All net-new elements start display:none. Do not alter the Phase 35 volume/stock-conflict markup.
  </action>
  <verify>
    <automated>grep -c "kiosk-recipe-modify-wrap\|kiosk-modify-tbody\|kiosk-recipe-price-preview\|kiosk-save-as-new-btn\|kiosk-locked-price-notice" admin.html</automated>
  </verify>
  <acceptance_criteria>
    - The grep prints 5 (all five IDs present).
    - `grep -n "Adding ingredients increases the price" admin.html` shows the locked-price asymmetry notice copy verbatim.
    - The Phase 35 IDs #kiosk-recipe-volume-wrap and #kiosk-stock-conflict are still present and unmodified (still 1 match each).
  </acceptance_criteria>
  <done>Modify panel, price preview, locked notice, and save-as-new markup inserted at the UI-SPEC positions; Phase 35 markup untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: admin.js modify state + rows + quote-with-modified + (Modified) label</name>
  <read_first>
    - js/admin.js lines 9806-9810 (Phase 35 state vars) and 10965-10998 (kioskFetchRecipeQuote) and 11150-11190 (volume wiring) and 11285-11314 (kioskUpdateAddToCartButton)
    - js/admin.js lines 8847-8953 (attachIngredientRowListeners/showIngredientAutocomplete) and 8519-8546 (loadIngredientCatalogForRecipes) — the autocomplete + catalog pattern to reuse
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 146-366 (admin.js extension sketch incl. state vars, attachKioskModifyRowListeners, quote extension, label suffix, state-reset, exports) and the Shared Patterns groupRecipeIngredients note (lines 799-817) — data-ing-idx MUST be ingredients.indexOf(ing)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md lines 252-321 (price preview display rules + grouped display in modify panel)
  </read_first>
  <behavior>
    - Expanding the modify panel renders the current recipe ingredients grouped by cf_type (groupRecipeIngredients), each editable row with autocomplete search, qty input, unit, and a Remove button; data-ing-idx maps to _kioskModifiedIngredients.indexOf(ing).
    - Removing a row, changing a qty, or adding a row updates _kioskModifiedIngredients and triggers a debounced re-quote that passes modified_ingredients to the server; the price preview shows the server total (or "Calculating…" / "Price unavailable").
    - When _kioskModifiedIngredients is a non-null array, the Add-to-Cart label gains the " (Modified)" suffix and the cart line items use _kioskQuote.ingredients (scaled+modified).
    - On Back / clear cart, _kioskModifiedIngredients resets to null and the panel collapses.
    - Removing all rows shows the empty-state placeholder row; "+ Add Ingredient" remains usable.
  </behavior>
  <action>
    Add state vars _kioskModifiedIngredients=null and _kioskModifyPanelOpen=false (PATTERNS 155-157). Add renderKioskModifyRows() (grouped via groupRecipeIngredients, escapeHTML all names, data-ing-idx=ingredients.indexOf(ing), empty-state row per UI-SPEC) and attachKioskModifyRowListeners() (PATTERNS 284-314: .ing-remove splices _kioskModifiedIngredients then re-renders + re-quotes; .ing-qty change updates quantity + re-quotes; .ing-search input -> showIngredientAutocomplete). Wire #kiosk-modify-toggle to lazily clone the recipe's base ingredients into _kioskModifiedIngredients on first expand (deep copy so the saved recipe is never mutated — D-04 canonical edit state at base qty), reveal #kiosk-recipe-modify-wrap when a recipe is selected, and #kiosk-modify-add-row to push a blank row. Extend kioskFetchRecipeQuote to append &modified_ingredients=encodeURIComponent(JSON.stringify(_kioskModifiedIngredients)) when non-null (PATTERNS 175-176); render the returned total into #kiosk-recipe-price-preview with loading/error states. Show #kiosk-locked-price-notice only when pricing_mode==='locked' and the panel has been expanded. Extend kioskUpdateAddToCartButton for the "(Modified)" suffix (PATTERNS 263-267). Add the reset (PATTERNS 351-356) on the back button and kioskClearCart. Add test-helper exports _kioskGetModifiedIngredients/_kioskSetModifiedIngredients (PATTERNS 363-365). ES5/var only; escapeHTML all dynamic HTML.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/admin-recipe-modify.test.js</automated>
  </verify>
  <acceptance_criteria>
    - tests/frontend/admin-recipe-modify.test.js (new) asserts: kioskFetchRecipeQuote includes modified_ingredients in the URL when _kioskModifiedIngredients is set; kioskUpdateAddToCartButton appends "(Modified)" when modified; removing a row splices _kioskModifiedIngredients; the saved/original recipe object is unchanged after edits (deep-copy proof, D-04/D-14). All pass.
    - `grep -n "_kioskModifiedIngredients\|modified_ingredients\|(Modified)" js/admin.js` shows the state var, the query param, and the label suffix.
    - `grep -n "indexOf(ing)" js/admin.js` confirms data-ing-idx uses the flat-array index (PATTERNS pattern #7 caveat).
  </acceptance_criteria>
  <done>Modify panel renders + edits drive a server-quote modified price + (Modified) label; original recipe never mutated; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: admin.js Save-as-new-recipe + build</name>
  <read_first>
    - js/admin.js lines 11207-11234 (kioskSaveRecipeQuickEdit PUT pattern) — adapt for POST save-as-new
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-PATTERNS.md lines 317-366 (kioskSaveAsNewRecipe sketch) and lines 731-766 (recipes.js POST + activation-guardrail note: dynamic recipes have locked_price=0, save is status:'draft' so the guardrail is NOT triggered at save time; activation is a separate admin step)
    - .planning/phases/36-cross-surface-selection-recipe-modification/36-UI-SPEC.md lines 155-162 (save-as-new affordance + feedback copy)
  </read_first>
  <behavior>
    - The "Save as new recipe" affordance is visible only when modifications exist; clicking it reveals the inline name prompt.
    - "Save Draft" POSTs to /api/recipes with the MODIFIED BASE list (pre-scale, D-12), pricing_mode:'dynamic' (D-13), status:'draft' (D-14), the recipe's base batch_size_l, and the name; success shows "Recipe saved as draft. Activate in admin to use." and error shows "Could not save recipe — try again."
    - The original recipe is never PUT/patched — only a new POST occurs (D-14).
  </behavior>
  <action>
    Add kioskSaveAsNewRecipe(name, modifiedBaseIngredients) per PATTERNS 320-348: POST /api/recipes with headers incl. x-api-key, body { name, style: _kioskSelectedRecipe.style||'', batch_size_l: _kioskSelectedRecipe.batch_size_l||null, pricing_mode:'dynamic', status:'draft', ingredients: modifiedBaseIngredients }. Wire #kiosk-save-as-new-btn (reveal prompt), #kiosk-save-draft-btn (validate non-empty name, call kioskSaveAsNewRecipe with _kioskModifiedIngredients, render feedback into #kiosk-save-as-new-feedback), #kiosk-save-cancel-btn (hide prompt). Show #kiosk-save-as-new-wrap only when _kioskModifiedIngredients is a non-null array. Use the exact UI-SPEC feedback copy. ES5/var; escapeHTML. Then run `npm run build` to regenerate js/admin.min.js + cache-bust stamps.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npx jest tests/frontend/admin-recipe-modify.test.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts kioskSaveAsNewRecipe POSTs to /api/recipes with pricing_mode:'dynamic', status:'draft', and ingredients === the modified base list (not the scaled list). Passes.
    - A test asserts NO PUT/patch to /api/recipes/:id occurs during save-as-new (original untouched, D-14).
    - `npm run build` exits 0; `grep -c "kioskSaveAsNewRecipe" js/admin.min.js` is >= 1 (wiring made it into the bundle).
  </acceptance_criteria>
  <done>Save-as-new POSTs a draft dynamic recipe with the base list; original untouched; bundle rebuilt; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> middleware | Modified ingredient list + recipe name are client-originated; pricing must remain server-authoritative; names flow into save-as-new and the snapshot. |
| dynamic HTML rendering of ingredient/recipe names | Catalog/recipe names rendered into modify-panel rows and feedback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-11 | Tampering | client computing the price | mitigate | The admin surface only PREVIEWS via GET /api/kiosk/recipe-quote; the charged total still comes from /confirm (36-03). No price is computed in admin.js. Test asserts the displayed price is sourced from the quote response. |
| T-36-12 | Injection (XSS) | ingredient/recipe names in modify rows + feedback | mitigate | All dynamic names rendered via escapeHTML (project rule). Add a test injecting a name with `<img onerror>` and asserting it is escaped in the rendered row. |
| T-36-13 | Tampering | save-as-new mutating the original recipe | mitigate | Save uses POST /api/recipes only; the modify edit state is a deep copy of the base list, never a reference to _kioskSelectedRecipe.ingredients. Tests assert no PUT and original unchanged. |
| T-36-SC | Tampering | npm/pip installs | accept | No new packages (uses existing jest + build). No package legitimacy gate required. |
</threat_model>

<verification>
- `npx jest tests/frontend/admin-recipe-modify.test.js` green.
- `npm test` full frontend suite green; `npm run lint` clean; `npm run build` exits 0.
- Manual: not required here — 36-08 (UAT) covers cross-surface human verification.
</verification>

<success_criteria>
Admin recipe-sale supports add/remove/change at base quantities (MOD-01), server-authoritative modified price preview + (Modified) label (MOD-02), and save-as-new draft dynamic recipe (MOD-03) — original recipe never mutated; SEL-01 control retained from Phase 35; bundle rebuilt.
</success_criteria>

<output>
Create `.planning/phases/36-cross-surface-selection-recipe-modification/36-04-SUMMARY.md` when done.
</output>
