---
phase: 77-ferment-in-store-catalog-filter-panel-ux-scrollable-compact-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/styles.css
  - css/styles.min.css
autonomous: false
requirements: [UX-CATALOG-FILTERS]

must_haves:
  truths:
    - "Opening 'Filters & Sort' on the Wine catalogue (238 kits) shows a panel capped at ~60vh that scrolls internally; the product grid stays near the top of the viewport instead of being pushed far down."
    - "Filter chips flow into multiple columns filling the container width — no ~380px empty side gutters on desktop >=1200px."
    - "The right-aligned filter-label affordance (Type:, Brand:, etc. in a reserved left gutter) is preserved."
    - "The fix holds on desktop AND mobile, on BOTH products/ferment-in-store.html AND products.html."
    - "The #mobile-catalog-bar sticky mobile variant is visually unchanged (its own id-specificity rules still win)."
  artifacts:
    - path: "css/styles.css"
      provides: "Desktop base rules for the catalog filter panel — capped-height scroll region + full-width filter rows"
      contains: "max-height"
    - path: "css/styles.min.css"
      provides: "Regenerated minified stylesheet actually served by the pages"
      contains: "max-height"
  key_links:
    - from: "css/styles.css .catalog-collapsible.open"
      to: "css/styles.min.css (served as styles.min.css?v=...)"
      via: "npm run build (minify:css + stamp:pages)"
      pattern: "max-height"
    - from: "css/styles.css base .catalog-filter-row / .catalog-collapsible"
      to: "#mobile-catalog-bar id-specificity overrides (styles.css:6656-6691) + @media max-width:768px (styles.css:6818)"
      via: "CSS specificity + source order — mobile rules still win, no regression"
      pattern: "mobile-catalog-bar"
---

<objective>
Make the "Filters & Sort" panel on the Ferment-in-Store catalog usable and compact on desktop: cap its height with an internal scroll region and let filter-chip rows fill the full container width instead of a fixed 640px centered strip. This is the owner's LOCKED "Compact + scrollable (CSS-led)" direction — two targeted changes to existing desktop base rules only. No interaction-model redesign, no JS change.

Purpose: On the Wine catalogue (238 kits -> dozens of Brand/Style/Producer chips) the opened panel currently expands to an enormous inline height with no scroll and wastes ~380px on each horizontal side, dominating the page.
Output: Edited desktop base rules in `css/styles.css`, regenerated `css/styles.min.css` (the actually-served file), and a human-verified responsive UAT across the desktop/mobile x two-page matrix on the Wine worst case.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<orientation>
CRITICAL FACTS (verified during planning — do not re-derive from scratch):

1. Pages serve the MINIFIED stylesheet: `products/ferment-in-store.html:58` and `products.html:77` both load
   `css/styles.min.css?v=...`. Editing `css/styles.css` alone has NO visible effect until `npm run build`
   regenerates `styles.min.css` and re-stamps the `?v=` cache-bust query. A build task is therefore MANDATORY
   here even though no JS changes (this overrides the usual "build only if JS changed" heuristic).

2. The three selectors you edit are DESKTOP BASE rules in `css/styles.css`:
   - `.catalog-collapsible` (currently `align-items: center`)  — around line 2008-2014
   - `.catalog-collapsible.open` (currently only `display: flex`, NO max-height, NO overflow) — around line 2016-2018
   - `.catalog-filter-row` (currently `width: 40rem; ... padding-left: 8.5rem`) — around line 2020-2030
   - `.catalog-filter-label` (the right-aligned gutter label; DO NOT change) — around line 2032-2041

3. MOBILE IS ALREADY SOLVED and MUST NOT REGRESS — these rules already override the base via higher
   specificity / later source order, so your base-rule edits do not reach mobile:
   - `#mobile-catalog-bar .catalog-collapsible.open` (id specificity) already has `max-height: 50vh; overflow-y: auto`
     and `#mobile-catalog-bar .catalog-filter-row { width: 100%; padding-left: 0 }` (styles.css:6656-6691).
   - `@media (max-width: 768px) .catalog-collapsible.open { max-height: 50vh; overflow-y: auto }` (styles.css:6818).
   The `#mobile-catalog-bar` element is created by JS on screens < 1024px, so the base rules you edit apply on
   desktop (>=1024px) in practice.

4. `css/catalog-subpage.css` does NOT define `.catalog-collapsible`, `.catalog-filter-row`, or
   `.catalog-filter-label` — no conflict there. All work is in `css/styles.css`.

5. This is a SHARED component: identical `.catalog-*` markup exists in both `products/ferment-in-store.html:180-219`
   and `products.html:144+`. One CSS change affects both pages — both must be verified.

6. This phase adds NO third-party service, NO new input/endpoint, NO inline style/script — the CSP `<meta>` tags
   are NOT affected and MUST NOT be touched. Do NOT reintroduce any beer/cider nav links if you touch shared markup
   (you should not need to touch any HTML).
</orientation>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cap panel height + reclaim width in the desktop catalog filter base rules</name>
  <files>css/styles.css</files>
  <read_first>
    - css/styles.css lines ~2008-2041 (the base `.catalog-collapsible`, `.catalog-collapsible.open`, `.catalog-filter-row`, `.catalog-filter-label` rules you are editing)
    - css/styles.css lines ~6625-6696 (the `#mobile-catalog-bar` variant — confirm its id-specificity rules already set max-height/overflow and filter-row width:100%, so your base edits will not regress mobile)
    - css/styles.css lines ~6712-6822 (the `@media (max-width:1024px)` / `(max-width:768px)` blocks — confirm the 768px `.catalog-collapsible.open` already caps at 50vh)
    - css/styles.css lines ~2896-2905 (the `@media (min-width:1024px) and (min-height:750px)` sticky `.catalog-controls` rule — context for why capping the open panel matters on desktop)
  </read_first>
  <action>
    Make three edits to EXISTING base rules in `css/styles.css` (do not add new selectors, do not touch `.catalog-filter-label`, `#mobile-catalog-bar`, any `@media` block, or any HTML):

    1. In `.catalog-collapsible` (the base rule, ~line 2008): change `align-items: center` to `align-items: stretch` so filter rows fill the container width instead of being centered around a fixed strip.

    2. In `.catalog-collapsible.open` (~line 2016): add a capped internal scroll region — add `max-height: 60vh;` and `overflow-y: auto;` (also add `-webkit-overflow-scrolling: touch;` for parity with the existing mobile rules). The 60vh value is the TUNE target from the locked design; keep the existing `display: flex`.

    3. In `.catalog-filter-row` (the base rule, ~line 2020): change `width: 40rem` to `width: 100%` so long chip groups flow into many columns across the full available width. KEEP `max-width: 100%` and KEEP `padding-left: 8.5rem` (that gutter, paired with the untouched `.catalog-filter-label` negative margin, preserves the right-aligned label affordance).

    Do NOT edit `css/styles.min.css` by hand — it is regenerated in Task 2.
  </action>
  <verify>
    <automated>grep -A6 '^\.catalog-collapsible {' css/styles.css | grep -q 'align-items: stretch' &amp;&amp; grep -A6 '^\.catalog-collapsible\.open {' css/styles.css | grep -q 'max-height' &amp;&amp; grep -A6 '^\.catalog-collapsible\.open {' css/styles.css | grep -q 'overflow-y: auto' &amp;&amp; ! grep -A12 '^\.catalog-filter-row {' css/styles.css | grep -q 'width: 40rem'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `css/styles.css` `.catalog-collapsible.open` now contains both `max-height:` (~60vh) and `overflow-y: auto`.
    - Source: the base `.catalog-filter-row` rule no longer contains `width: 40rem` (replaced with `width: 100%`); `max-width: 100%` and `padding-left: 8.5rem` remain.
    - Source: the base `.catalog-collapsible` rule uses `align-items: stretch` (not `center`).
    - Source: `.catalog-filter-label`, `#mobile-catalog-bar *`, and every `@media` block are byte-for-byte unchanged.
    - Source: no HTML file changed; no CSP meta touched; no inline styles added.
  </acceptance_criteria>
  <done>The three desktop base rules are edited exactly as specified; mobile and label rules untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild served assets and run the commit gates</name>
  <files>css/styles.min.css</files>
  <read_first>
    - package.json (the `build`, `minify:css`, and `stamp:pages` scripts — confirm build regenerates `css/styles.min.css` and re-stamps the `?v=` query on `products.html` + `products/ferment-in-store.html` among other pages)
    - CLAUDE.md (Non-Negotiable Rules: run `npm test` and middleware tests + `npm run lint` before commit; never edit `styles.min.css` by hand)
  </read_first>
  <action>
    Run `npm run build` so the hand-edited `css/styles.css` propagates into the served `css/styles.min.css`
    and the page `?v=` cache-bust stamps are refreshed. NOTE: `stamp:pages` re-stamps the `?v=` query string on
    ALL listed public pages — that cross-page cache-version churn is EXPECTED and benign; do not try to suppress it.

    Then run the commit gates required by CLAUDE.md:
    - `npm run lint` (must pass, `--max-warnings 0`)
    - `npm test` (frontend Jest — no logic changed, so this is a regression sanity check; must stay green)
    - `cd zoho-middleware && npm test` (middleware is entirely unaffected by a CSS change; run it only to honor the
      pre-commit rule — it must stay green, no new tests expected)

    Do NOT write or modify any test file (no behavior changed, and CLAUDE.md rule 10 forbids editing tests unasked).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; grep -q 'max-height:60vh' css/styles.min.css || grep -q 'max-height:60vh}' css/styles.min.css; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Source: `css/styles.min.css` contains the minified `.catalog-collapsible.open` rule with `max-height:60vh` and `overflow-y:auto` (minifier may drop the space/semicolon — match on `max-height:60vh` and `overflow-y:auto`).
    - Source: `css/styles.min.css` no longer contains `width:40rem` on the catalog-filter-row rule.
    - Build: `npm run build` exits 0; `products.html` and `products/ferment-in-store.html` `styles.min.css?v=` stamps changed to a new value.
    - Gate: `npm run lint` passes with no warnings; `npm test` passes; `cd zoho-middleware && npm test` passes.
  </acceptance_criteria>
  <done>`styles.min.css` carries the change, cache stamps refreshed, all three gates green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual responsive UAT — desktop/mobile x two pages on the Wine worst case</name>
  <what-built>
    Desktop catalog "Filters & Sort" panel now caps at ~60vh with an internal scrollbar, and filter chips fill the
    full container width instead of a centered 640px strip. Change lives in `styles.min.css`, shared by both
    `products/ferment-in-store.html` and `products.html`. Mobile (`#mobile-catalog-bar`) was intentionally left as-is.
  </what-built>
  <how-to-verify>
    Verify against the real Wine catalogue (238 kits -> dozens of Brand/Style/Producer chips — the worst case).
    Serve the site locally (or use staging) and hard-refresh (cache-bust). Check ALL of the matrix below:

    DESKTOP (viewport width >= 1200px), on products/ferment-in-store.html (Ferment-in-Store tab, Wine filter) AND on products.html:
    1. Click "Filters & Sort". The panel opens CAPPED at roughly 60% of viewport height and scrolls INTERNALLY
       (scrollbar inside the panel) — the product grid stays near the top of the viewport, not pushed far down.
    2. Filter chips flow into MULTIPLE columns filling the container width — there are NO large (~380px) empty
       gutters on the left/right sides.
    3. The row labels (Type:, Brand:, Producer:, Sort:, etc.) are still right-aligned in a reserved left gutter
       (the label affordance is preserved, not broken/overlapping).

    MOBILE (viewport width <= 768px, e.g. iPhone/iPad-Safari emulation), on BOTH pages:
    4. The sticky bottom `#mobile-catalog-bar` still renders correctly; tapping filters opens the panel UPWARD,
       still capped (~50vh) and scrollable, chips full-width — i.e. NO visual regression vs before this change.

    TABLET edge (viewport 769-1023px), on either page:
    5. The bottom bar variant still behaves as before (no new empty gutters, no broken height).
  </how-to-verify>
  <resume-signal>Type "approved" if all five checks pass on both pages, or describe which cell of the matrix failed (page + viewport + symptom) so the CSS values can be tuned.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase restyles an existing static filter panel. It adds no inputs, endpoints, data flows, third-party services, or trust boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | CSP integrity | accept | No new attack surface: CSS-only change, no inline styles/scripts, no new domains — CSP `<meta>` tags are untouched, so no CSP relaxation occurs. Honest disposition: no new threat introduced. |

No package installs, no third-party integrations, no server/middleware changes → no supply-chain (T-*-SC) threat applies. No high-severity threats identified.
</threat_model>

<verification>
- `grep` proves `styles.css` base rules edited (max-height + overflow-y on `.catalog-collapsible.open`; `align-items: stretch`; no `width: 40rem` on `.catalog-filter-row`).
- `npm run build` succeeds and `styles.min.css` carries the change with refreshed page cache stamps.
- `npm run lint`, `npm test`, and `cd zoho-middleware && npm test` all pass.
- Human UAT confirms the compact scrollable + full-width behavior on the Wine worst case across desktop/mobile x ferment-in-store.html/products.html, with the mobile sticky bar unregressed.
</verification>

<success_criteria>
- The desktop catalog filter panel is capped (~60vh) with internal scroll; the product grid is no longer pushed far down (UX-CATALOG-FILTERS).
- Filter chips fill the full container width; no ~380px side gutters on wide desktop.
- The right-aligned label affordance is preserved.
- `products.html` and `products/ferment-in-store.html` both benefit; the `#mobile-catalog-bar` mobile variant is visually unchanged.
- No JS, no HTML, no CSP, and no middleware changed; all commit gates green.
</success_criteria>

<output>
Create `.planning/phases/77-ferment-in-store-catalog-filter-panel-ux-scrollable-compact-/77-01-SUMMARY.md` when done.
</output>
