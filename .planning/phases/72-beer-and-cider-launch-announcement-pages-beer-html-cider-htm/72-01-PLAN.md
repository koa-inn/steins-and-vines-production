---
phase: 72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - beer.html
  - cider.html
  - sitemap.xml
  - package.json
autonomous: true
requirements: [OWNER-LAUNCH-72]
must_haves:
  truths:
    - "Opening /beer shows a 'now offering beer' announcement page visually indistinguishable from about.html"
    - "Opening /cider shows a '100% Okanagan Juice Cider' announcement page"
    - "Each page has hero + what-it-is + availability/dates + price + booking CTA + FAQ + cross-link sections"
    - "Business content (price/dates/what's-included/CTA text) is present as CLEARLY-MARKED [PLACEHOLDER: ...] text, not invented values"
    - "Primary CTA is a .btn link to products/ferment-in-store.html with a call/visit fallback"
    - "Each page carries correct per-page og:* AND twitter:* tags (not inherited from about.html)"
    - "Both pages are stamped by npm run build and listed in sitemap.xml"
  artifacts:
    - path: "beer.html"
      provides: "Beer launch announcement page (about.html shell + index.html primitives)"
      min_lines: 200
      contains: "products/ferment-in-store.html"
    - path: "cider.html"
      provides: "Cider launch announcement page"
      min_lines: 200
      contains: "products/ferment-in-store.html"
    - path: "sitemap.xml"
      provides: "/beer and /cider clean-URL entries"
      contains: "steinsandvines.ca/beer"
    - path: "package.json"
      provides: "beer.html + cider.html registered in stamp:pages array"
      contains: "beer.html"
  key_links:
    - from: "beer.html"
      to: "products/ferment-in-store.html"
      via: ".btn primary CTA link"
      pattern: "products/ferment-in-store\\.html\\\" class=\\\"btn"
    - from: "beer.html"
      to: "cider.html"
      via: "cross-link"
      pattern: "href=\\\"cider\\.html"
    - from: "cider.html"
      to: "beer.html"
      via: "cross-link"
      pattern: "href=\\\"beer\\.html"
    - from: "package.json"
      to: "beer.html"
      via: "stamp:pages hardcoded array"
      pattern: "'beer\\.html'"
---

<objective>
Author two new static launch announcement pages — `beer.html` and `cider.html` — by cloning the `about.html` page shell and assembling the `<main>` from existing `index.html` section primitives, then register both pages for SEO (`sitemap.xml`) and cache-stamping (`package.json` `stamp:pages`) and run the build/lint/test gate.

Purpose: Ship the public announcement + booking funnel for the beer and 100% Okanagan Juice Cider offerings, on-brand and CSP-clean, with owner-fillable placeholder business content.
Output: `beer.html`, `cider.html`, updated `sitemap.xml`, updated `package.json`, regenerated build artifacts.

All work happens on a feature branch (suggested: `feat/beer-cider-launch-72`). No production deploy in this phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/beer-cider-launch-pages.md
@.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PATTERNS.md
@about.html
@index.html
@package.json
@sitemap.xml

<interfaces>
<!-- The executor copies these verbatim — no exploration needed. -->

CSP <meta> to copy VERBATIM (about.html line 19 — NOT reservation.html's payment CSP):
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://tracker.metricool.com https://connect.facebook.net https://www.googleadservices.com https://googleads.g.doubleclick.net; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https://www.googletagmanager.com https://www.google-analytics.com https://www.facebook.com https://www.google.com https://www.google.ca https://googleads.g.doubleclick.net https://*.google-analytics.com; connect-src 'self' https://docs.google.com https://*.googleusercontent.com https://script.google.com https://sheets.googleapis.com https://www.googleapis.com https://svmiddleware-production.up.railway.app https://svmiddleware-staging.up.railway.app https://o4511012754358272.ingest.de.sentry.io https://www.google-analytics.com https://www.googletagmanager.com https://*.analytics.google.com https://tracker.metricool.com https://www.facebook.com https://www.google.com https://googleads.g.doubleclick.net https://www.googleadservices.com https://*.google-analytics.com; frame-src 'self' https://www.googletagmanager.com https://www.google.com https://td.doubleclick.net">

about.html anchor lines for the shell:
  - <head> boilerplate: lines 1-76 (GTM, meta, CSP@19, JSON-LD, fonts, favicon). Per-page CHANGE: meta description (21), og:title (22), og:description (23), og:url (25), twitter:title + twitter:description (grep the twitter:* tags in about.html's head — they must NOT inherit about.html's copy), canonical (29), <title> (30).
  - header + nav: lines ~98-121 (canonical nav with Products mega-dropdown + nav-search-item)
  - footer: lines 287-310 (copy verbatim)
  - scripts: lines 312-315 ONLY (sentry.min.js, sentry-init.js, sheets-config.js, main.min.js) — DROP the search-overlay lines 75 and 317

Stylesheet: announcement pages load ONLY css/styles.min.css?v=PLACEHOLDER (no search-overlay/hops/catalog-subpage CSS).

index.html primitives to lift into <main> (copy markup, swap text):
  - Hero/announcement banner: .beer-banner .beer-banner--green block (lines 222-235). DROP the waitlist <form> (228-232); keep .beer-banner-badge + h2 + p, add a .btn CTA.
  - "What it is" section: .intro (lines 211-216 / 241-246)
  - Availability/dates: .how-it-works-steps / .how-it-works-step (lines 256-277)
  - Price: .intro paragraph prose (voice like line 255)
  - Primary CTA: .btn link (line 172 pattern) → products/ferment-in-store.html
  - Section divider between sections: <div class="section-icon"><img src="images/Icon_wine.svg" alt="" aria-hidden="true" loading="lazy"></div> (lines 218-220)

CTA markup pattern (exact):
  <a href="products/ferment-in-store.html" class="btn" data-content="beer-cta">[PLACEHOLDER: CTA text — e.g. Reserve Your Kit]</a>
  <a href="tel:+16045674565" class="btn btn-secondary">or call (604) 567-4565</a>

Contact fallback facts (locked): phone 1-604-567-4565, email hello@steinsandvines.ca, Unit 11-38918 Progress Way, Squamish BC.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author beer.html</name>
  <files>beer.html</files>
  <action>Create beer.html by cloning the about.html shell and assembling the &lt;main&gt; from index.html primitives listed in &lt;interfaces&gt;.

HEAD: copy about.html lines 1-76 verbatim. Copy the CSP &lt;meta&gt; from &lt;interfaces&gt; VERBATIM (do NOT use reservation.html's payment-augmented CSP). Change these text fields for beer: meta description, og:title, og:description, og:url (https://steinsandvines.ca/beer — clean URL, no .html), canonical (https://steinsandvines.ca/beer), and &lt;title&gt; (unique, e.g. "Now Brewing: Craft Beer | Steins &amp; Vines"). ALSO update the Twitter Card tags — a verbatim head copy would inherit about.html's twitter:title / twitter:description; set beer-specific twitter:title and twitter:description (mirroring the new og:title / og:description), and twitter:url if present. First `grep -n 'twitter:\|og:' about.html` to locate every tag so none are missed. Stylesheet: ONLY css/styles.min.css?v=PLACEHOLDER (the ?v= is filled by build in Task 3 — never hand-edit it). Drop the search-overlay stylesheet line.

BODY: set &lt;body data-page="beer"&gt;. Copy the canonical header + nav (about.html ~98-121) including the two new nav entries &lt;li&gt;&lt;a href="beer.html"&gt;Beer&lt;/a&gt;&lt;/li&gt; and &lt;li&gt;&lt;a href="cider.html"&gt;Cider&lt;/a&gt;&lt;/li&gt; placed between the Products dropdown &lt;li&gt; and the About &lt;li&gt;. Set class="active" on this page's own Beer link.

MAIN (in order, section-icon dividers between sections): (1) Hero = .beer-banner .beer-banner--green with badge "Now Available", h1 announcement headline ("Now brewing: beer at Steins &amp; Vines" tone) — use a single h1 for the page, then h2 for subsequent sections; drop the waitlist form. (2) "What it is" .intro — 2-4 sentence hook marked [PLACEHOLDER: what-it-is — beer]. (3) Availability &amp; dates as .how-it-works-steps or a &lt;dl&gt; inside .content, values marked [PLACEHOLDER: available-from], [PLACEHOLDER: ready-in], [PLACEHOLDER: seasonal note]. (4) Price .intro prose marked [PLACEHOLDER: price] and [PLACEHOLDER: what's included]. (5) Primary CTA block using the exact CTA markup from &lt;interfaces&gt; (button label as [PLACEHOLDER: CTA text]) plus the tel fallback and a one-line visit note (Unit 11-38918 Progress Way, Squamish BC; hello@steinsandvines.ca). (6) Short static FAQ — 2-3 plain &lt;h3&gt;/&lt;p&gt; Q&amp;As inside .content ("Do I need experience?", "How long until it's ready?", "Can I bring my own recipe?") with placeholder answers where facts are unknown. (7) Cross-link: .btn to cider.html ("Prefer cider? &rarr;") and a .btn-secondary/link back to index.html.

If a real photo is wanted, use a flagged placeholder &lt;picture&gt; per the about.html webp+srcset convention (e.g. images/launch/beer-1600w.jpg) with meaningful alt, and record the missing filename for the handoff — do NOT commit a multi-MB image; reuse an existing on-brand asset (e.g. images/cheers.jpg) as interim if needed.

FOOTER: copy about.html 287-310 verbatim. SCRIPTS: copy about.html 312-315 ONLY (sentry.min.js, sentry-init.js, sheets-config.js defer, main.min.js?v=PLACEHOLDER defer). Do NOT add search-overlay or any js/modules/* bundle. Add NO new inline JS (keeps ES5/CSP clean).

Accessibility: exactly one h1, then h2 section headings; descriptive link text; alt on all images (aria-hidden="" on decorative icons); the CTA is a keyboard-focusable anchor.</action>
  <verify>
    <automated>test -f beer.html && grep -q 'data-page="beer"' beer.html && grep -q 'products/ferment-in-store.html" class="btn' beer.html && grep -q 'href="cider.html"' beer.html && grep -q 'PLACEHOLDER' beer.html && grep -q 'svmiddleware-production.up.railway.app' beer.html && grep -qi 'twitter:title' beer.html && ! grep -qi 'helcim\|gstatic.com/recaptcha' beer.html && [ "$(grep -c '<h1' beer.html)" -eq 1 ]</automated>
  </verify>
  <done>beer.html exists, uses the about.html shell + non-payment CSP, links to the booking flow and cider.html, has beer-specific og:* AND twitter:* tags, contains visible [PLACEHOLDER: ...] markers, has exactly one h1, and loads only styles.min.css + the minimal script set.</done>
</task>

<task type="auto">
  <name>Task 2: Author cider.html</name>
  <files>cider.html</files>
  <action>Create cider.html the same way as beer.html (Task 1), with cider content. HEAD text fields for cider: title (e.g. "Now Fermenting: 100% Okanagan Juice Cider | Steins &amp; Vines"), meta description, og:title, og:description, og:url + canonical = https://steinsandvines.ca/cider (clean URL). ALSO set cider-specific twitter:title and twitter:description (do NOT inherit about.html's Twitter copy) — same grep-for-all-og/twitter-tags discipline as Task 1. &lt;body data-page="cider"&gt;. In the nav, set class="active" on the Cider link (Beer link not active).

MAIN: hero headline in the "Now fermenting: 100% Okanagan Juice Cider" tone; "what it is" hook marked [PLACEHOLDER: what-it-is — cider] and [PLACEHOLDER: what makes it special — 100% Okanagan juice / source]; availability/dates with [PLACEHOLDER: seasonal juice note]; price [PLACEHOLDER: price] + [PLACEHOLDER: what's included]; the same CTA block (→ products/ferment-in-store.html) with [PLACEHOLDER: CTA text] + tel fallback + visit note; 2-3 static FAQ Q&amp;As; cross-link .btn to beer.html ("Prefer beer? &rarr;") + link to index.html. Same footer, same minimal script set, no new inline JS. Do NOT invent ABV/style/price facts — leave them as marked placeholders.</action>
  <verify>
    <automated>test -f cider.html && grep -q 'data-page="cider"' cider.html && grep -q 'products/ferment-in-store.html" class="btn' cider.html && grep -q 'href="beer.html"' cider.html && grep -q 'PLACEHOLDER' cider.html && grep -q 'svmiddleware-production.up.railway.app' cider.html && grep -qi 'twitter:title' cider.html && [ "$(grep -c '<h1' cider.html)" -eq 1 ]</automated>
  </verify>
  <done>cider.html exists mirroring beer.html structure with cider placeholder content, cider-specific og:*/twitter:* tags, links to the booking flow and beer.html, and passes the same shell/CSP/heading checks.</done>
</task>

<task type="auto">
  <name>Task 3: Register pages (sitemap + build config) and run the build/lint/test gate</name>
  <files>sitemap.xml, package.json</files>
  <action>Register the two new pages so SEO and cache-stamping pick them up.

sitemap.xml: add two &lt;url&gt; blocks copying the shape of an existing monthly entry (e.g. the /about block, lines 63-68): &lt;loc&gt;https://steinsandvines.ca/beer&lt;/loc&gt; and &lt;loc&gt;https://steinsandvines.ca/cider&lt;/loc&gt; (clean URLs, no .html), lastmod 2026-08-22, changefreq monthly, priority 0.7.

package.json: in the stamp:pages script (line 17) hardcoded page array, add 'beer.html' and 'cider.html' to the array literal. These pages only reference styles.min.css and main.min.js, so the existing replace patterns in stamp:pages already cover them — no new replace rules needed. This edit is REQUIRED: without it the ?v= cache-bust on the new pages never stamps and CSP/asset caching drifts.

Then run the build so the two new pages get their ?v= stamped: `npm run build`. No new js/modules were added, so module concatenation is unchanged, but build must run to stamp the new pages and keep artifacts consistent (scripts/check-artifact-drift.sh guards drift). Then run the DoD test gate: `npm run lint` (ES5, --max-warnings 0) and `npm test` (jest). NOTE on CLAUDE.md rule #1 (npm test AND `cd zoho-middleware && npm test`): this phase touches ZERO middleware files, so the middleware suite is **N/A — no middleware changes** (consciously waived, not skipped). Fix any frontend lint/test failures before finishing. Commit build artifacts (js/main.min.js, css/*.min.css) if they changed.</action>
  <verify>
    <automated>grep -q 'steinsandvines.ca/beer' sitemap.xml && grep -q 'steinsandvines.ca/cider' sitemap.xml && grep -q "'beer.html'" package.json && grep -q "'cider.html'" package.json && grep -Eq 'styles\.min\.css\?v=[a-z0-9]+' beer.html && grep -Eq 'styles\.min\.css\?v=[a-z0-9]+' cider.html && npm run lint && npm test</automated>
  </verify>
  <done>Both pages appear in sitemap.xml and package.json stamp:pages; npm run build stamped their ?v= tokens; eslint and frontend jest pass (middleware suite N/A); build artifacts committed if changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static page | Static HTML served by GitHub Pages; no server-side input handling on these pages |
| page → middleware (booking) | Pages only *link* to products/ferment-in-store.html; no direct fetch added here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | CSP `<meta>` on new pages | mitigate | Copy the about.html CSP VERBATIM (whitelists existing middleware/analytics only); do NOT widen with payment/Helcim origins from reservation.html |
| T-72-02 | Information disclosure | new inline JS leaking config | mitigate | Ship zero new inline JS; behaviour comes from existing main.min.js only |
| T-72-03 | Elevation | new external CDN/tracker | accept | No new external origins introduced; static content + existing assets only |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (no new dependencies) |
</threat_model>

<verification>
- `npm run lint` passes (ES5, --max-warnings 0)
- `npm test` passes (existing jest suite unchanged); middleware suite N/A — no middleware changes
- `npm run build` run; beer.html and cider.html carry stamped `styles.min.css?v=` and `main.min.js?v=`
- Opening beer.html / cider.html in a browser shows no CSP console violations
- CSP `<meta>` on both pages is byte-identical to about.html line 19 (no payment origins)
- Each page's og:* and twitter:* tags are page-specific (beer vs cider), not inherited from about.html
</verification>

<success_criteria>
- beer.html and cider.html exist, visually mirror about.html, and contain all seven spec sections
- All business content (price/dates/what's-included/CTA text) is clearly-marked [PLACEHOLDER: ...] — nothing invented
- Primary CTA links to products/ferment-in-store.html; call/visit fallback present
- Pages cross-link to each other and back to index.html
- Per-page og:* + twitter:* tags correct
- Both pages registered in sitemap.xml and package.json stamp:pages; build/lint/frontend-test green
</success_criteria>

<output>
Create `.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-01-SUMMARY.md` when done. Record the exact list of [PLACEHOLDER: ...] markers and any flagged placeholder image filenames (e.g. images/launch/beer-1600w.jpg) that the owner must fill before production promotion.
</output>
