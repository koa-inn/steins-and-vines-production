---
phase: 72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm
plan: 02
type: execute
wave: 2
depends_on: ["72-01"]
files_modified:
  - index.html
  - about.html
  - contact.html
  - custom-labels.html
  - hops.html
  - ingredients.html
  - products.html
  - reservation.html
  - 404.html
  - products/ferment-in-store.html
  - products/ingredients-supplies.html
  - products/hops.html
  - products/grains.html
  - products/yeast.html
  - products/additives.html
  - products/packaging.html
  - products/equipment.html
autonomous: true
requirements: [OWNER-LAUNCH-72]
must_haves:
  truths:
    - "Beer and Cider appear in the nav on every public page (root + products/ + 404)"
    - "The homepage features the two new pages via on-brand launch banners linking to beer.html and cider.html"
    - "The stale 'Beer Is Coming / Coming Soon' waitlist banner (form + hidden iframe) no longer contradicts a live beer.html"
    - "Staff pages (admin/kiosk/brewpad/batch) are untouched"
  artifacts:
    - path: "index.html"
      provides: "Nav with Beer+Cider, homepage launch feature banners, reconciled stale beer banner"
      contains: "href=\"beer.html\""
    - path: "hops.html"
      provides: "Nav updated with Beer+Cider (previously stale nav variant)"
      contains: "href=\"beer.html\""
    - path: "404.html"
      provides: "Nav updated with Beer+Cider using absolute /-prefixed links"
      contains: "href=\"/beer.html\""
  key_links:
    - from: "index.html"
      to: "beer.html"
      via: "nav link + homepage feature banner"
      pattern: "href=\\\"beer\\.html"
    - from: "index.html"
      to: "cider.html"
      via: "nav link + homepage feature banner"
      pattern: "href=\\\"cider\\.html"
    - from: "products/ferment-in-store.html"
      to: "beer.html"
      via: "nav link (relative — served from /products/)"
      pattern: "href=\\\"\\.\\./beer\\.html"
    - from: "404.html"
      to: "beer.html"
      via: "nav link (absolute — served at arbitrary depths)"
      pattern: "href=\\\"/beer\\.html"
---

<objective>
Propagate the Beer + Cider nav entries consistently across every public page, feature the two new pages on the homepage with on-brand launch banners, and reconcile the now-stale "Beer Is Coming / Coming Soon" waitlist banner on `index.html` so it no longer contradicts a live `beer.html`.

Purpose: Make the new pages discoverable from every page and remove the factual contradiction on the homepage once beer launches.
Output: Updated nav in 17 public pages, homepage launch banners, reconciled stale banner, rebuilt/stamped artifacts.

Continues on the same feature branch as 72-01. No production deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PATTERNS.md
@index.html

<interfaces>
<!-- Canonical nav source of truth: index.html lines 134-157 (Products mega-dropdown + nav-search-item). -->

Two nav <li> entries to add on every public page, placed BETWEEN the Products dropdown <li> and the About <li>:
  <li><a href="beer.html">Beer</a></li>
  <li><a href="cider.html">Cider</a></li>

Path note: pages in products/ are served from /products/, so their nav links to the new root pages must be RELATIVE:
  <li><a href="../beer.html">Beer</a></li>
  <li><a href="../cider.html">Cider</a></li>
(The existing products/*.html nav already uses ../ for root links like ../about.html and ../custom-labels.html — match that exact prefix.)

SPECIAL CASE — 404.html: it deliberately uses ABSOLUTE /-prefixed nav links (`/index.html`, `/about.html`, `/products/ferment-in-store.html`) because GitHub Pages serves it at arbitrary URL depths, and it has a SIMPLER nav (no Products mega-dropdown, no nav-search-item — just Home / Products / About / Contact at lines 33-36). For 404.html use absolute links and match its simpler shape:
  <li><a href="/beer.html">Beer</a></li>
  <li><a href="/cider.html">Cider</a></li>
Insert them between the Products <li> (line 34) and the About <li> (line 35).

Nav drift note: hops.html, ingredients.html, products.html carry an OLDER nav variant (missing nav-search-item / simpler dropdown). Do NOT rewrite those navs to the canonical version — only INSERT the two new <li> entries in the same Products→About position. (Full nav-drift cleanup is out of scope for this phase — flag as a follow-up.)

Homepage stale banner to reconcile: index.html lines 222-235 (.beer-banner .beer-banner--green, badge "Coming Soon", "Beer Is Coming", waitlist <form> 228-232) PLUS its hidden submit-target iframe at index.html:402 (<iframe name="beer-waitlist-iframe" id="beer-waitlist-iframe" style="display:none;">). The beer-banner--green modifier lives in css/styles.css ~1450-1462.

Precedent for a same-edit-across-all-pages change: commit 43d49378 (out-of-stock across all shop surfaces).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Beer + Cider nav entries to all public pages</name>
  <files>index.html, about.html, contact.html, custom-labels.html, hops.html, ingredients.html, products.html, reservation.html, 404.html, products/ferment-in-store.html, products/ingredients-supplies.html, products/hops.html, products/grains.html, products/yeast.html, products/additives.html, products/packaging.html, products/equipment.html</files>
  <action>In each listed public page, insert the two nav &lt;li&gt; entries into the .nav-list, positioned between the Products dropdown &lt;li&gt; and the About &lt;li&gt; (mirror how 43d49378 touched all shop surfaces in one consistent edit).

For ROOT pages (index, about, contact, custom-labels, hops, ingredients, products, reservation) use: &lt;li&gt;&lt;a href="beer.html"&gt;Beer&lt;/a&gt;&lt;/li&gt; and &lt;li&gt;&lt;a href="cider.html"&gt;Cider&lt;/a&gt;&lt;/li&gt;.

For products/*.html pages use the ../ relative prefix: &lt;li&gt;&lt;a href="../beer.html"&gt;Beer&lt;/a&gt;&lt;/li&gt; and &lt;li&gt;&lt;a href="../cider.html"&gt;Cider&lt;/a&gt;&lt;/li&gt; (match the existing ../about.html style already used in the products/ nav).

SPECIAL CASE — 404.html ONLY: it uses ABSOLUTE /-prefixed nav links and a simpler nav (Home / Products / About / Contact, lines 33-36, no Products dropdown, no nav-search-item). Use &lt;li&gt;&lt;a href="/beer.html"&gt;Beer&lt;/a&gt;&lt;/li&gt; and &lt;li&gt;&lt;a href="/cider.html"&gt;Cider&lt;/a&gt;&lt;/li&gt; inserted between the Products &lt;li&gt; (34) and the About &lt;li&gt; (35). Do NOT convert 404.html to relative links or to the canonical dropdown nav.

Do NOT normalize the drifted navs (hops.html, ingredients.html, products.html) to the canonical version — only add the two new &lt;li&gt; in the correct position, preserving each page's existing nav variant otherwise. Do NOT set class="active" on Beer/Cider anywhere here (the active state lives only on beer.html/cider.html themselves, created in 72-01).

Explicitly OUT OF SCOPE — do NOT touch: admin.html, kiosk.html, brewpad.html, batch.html.

After editing, confirm every page still has exactly one Beer link and one Cider link in its nav.</action>
  <verify>
    <automated>for f in index.html about.html contact.html custom-labels.html hops.html ingredients.html products.html reservation.html; do grep -q 'href="beer.html"' "$f" && grep -q 'href="cider.html"' "$f" || { echo "MISSING nav in $f"; exit 1; }; done; grep -q 'href="/beer.html"' 404.html && grep -q 'href="/cider.html"' 404.html || { echo "MISSING abs nav in 404.html"; exit 1; }; for f in products/ferment-in-store.html products/ingredients-supplies.html products/hops.html products/grains.html products/yeast.html products/additives.html products/packaging.html products/equipment.html; do grep -q 'href="../beer.html"' "$f" && grep -q 'href="../cider.html"' "$f" || { echo "MISSING nav in $f"; exit 1; }; done; for s in admin.html kiosk.html brewpad.html batch.html; do ! grep -q 'href=".*beer.html"' "$s" || { echo "STAFF page touched: $s"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>All 17 public pages (8 root + 404 with absolute links + 8 products/) carry Beer + Cider nav links with correct path conventions; the 4 staff pages are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Homepage launch banners + reconcile the stale "Beer Is Coming" banner</name>
  <files>index.html</files>
  <action>On index.html, feature the two new pages and remove the now-false "Coming Soon" beer banner.

Default reconciliation (owner will confirm in 72-03): repurpose the existing .beer-banner .beer-banner--green block (lines 222-235) into a LIVE "Now Available" Beer feature — change the badge "Coming Soon" &rarr; "Now Available", update the headline/subhead text, DELETE the waitlist &lt;form&gt; (228-232) and the confirm &lt;p&gt; (232), and replace them with a .btn linking to beer.html ("[PLACEHOLDER: CTA text — e.g. Explore Beer &rarr;]"). Then add a SECOND on-brand banner immediately after (with its own section-icon divider) for Cider — clone the same .beer-banner block, badge "Now Available", headline "100% Okanagan Juice Cider", subhead placeholder, and a .btn to cider.html. If a distinct colour modifier exists in css/styles.css for the second banner, use it to differentiate; otherwise both green is acceptable (zero new CSS).

ALSO remove the now-orphaned hidden submit-target iframe at index.html:402 (&lt;iframe name="beer-waitlist-iframe" id="beer-waitlist-iframe" style="display:none;"&gt;&lt;/iframe&gt;) — with the form gone it targets nothing. The submit handler at js/modules/12-checkout.js:1690 is null-guarded, so removing the form + iframe causes no JS regression. (If you choose to leave the iframe as harmless dead markup instead, you MUST call that out explicitly in the SUMMARY — do not silently leave it.)

Do NOT delete the waitlist handler code from js/modules — only the homepage markup (form + iframe) is removed; the beer-waitlist handler becomes dead markup-wise on this page. Note the removal in the SUMMARY so the owner is aware (the waitlist decision is confirmed in 72-03).

After the markup edits, run `npm run build` (re-stamps index.html + pages, keeps artifacts consistent), then run the test gate: `npm run lint` and `npm test`. NOTE on CLAUDE.md rule #1 (npm test AND `cd zoho-middleware && npm test`): this task touches ZERO middleware files, so the middleware suite is **N/A — no middleware changes** (consciously waived, not skipped). Fix any frontend lint/test failures. Commit changed build artifacts.</action>
  <verify>
    <automated>grep -q 'href="beer.html"' index.html && grep -q 'href="cider.html"' index.html && ! grep -q 'Beer Is Coming' index.html && ! grep -q 'beer-waitlist-form' index.html && ! grep -q 'beer-waitlist-iframe' index.html && npm run lint && npm test</automated>
  </verify>
  <done>index.html shows two "Now Available" launch banners linking to beer.html and cider.html; the "Beer Is Coming" waitlist form AND its hidden iframe are gone; lint + frontend tests pass; artifacts rebuilt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static page | Static HTML; nav edits and banner markup only, no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-04 | Tampering | inconsistent nav across pages | mitigate | Verify script asserts Beer+Cider present on all 17 public pages and absent on 4 staff pages |
| T-72-05 | Repudiation | broken relative/absolute links from products/ and 404 | mitigate | products/*.html use ../beer.html; 404.html uses /beer.html; verify greps the exact form per page group |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- Beer + Cider present in nav on all 8 root + 404 + 8 products/ public pages; absent on admin/kiosk/brewpad/batch
- products/*.html use ../ relative links; root pages use bare links; 404.html uses /-absolute links
- index.html has two live launch banners; no "Beer Is Coming", no waitlist form, no orphaned waitlist iframe
- `npm run lint` and `npm test` pass; middleware suite N/A (no middleware changes); artifacts rebuilt via `npm run build`
- No CSP console violations on the homepage after the banner edit
</verification>

<success_criteria>
- Consistent Beer/Cider nav across every public page (drifted navs get the two links inserted, not rewritten; 404 keeps its absolute-link convention)
- Homepage features both new pages and no longer advertises beer as "Coming Soon"; waitlist form + iframe removed
- Staff pages untouched; build/lint/frontend-test green
</success_criteria>

<output>
Create `.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-02-SUMMARY.md` when done. Record: the exact list of pages edited, the stale-banner reconciliation choice made (repurpose vs remove) so the owner can confirm/override in 72-03, whether the waitlist iframe was removed or left as dead markup, and any [PLACEHOLDER: ...] text added to the homepage banners.
</output>
