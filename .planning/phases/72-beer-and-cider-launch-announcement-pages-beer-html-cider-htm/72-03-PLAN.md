---
phase: 72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm
plan: 03
type: execute
wave: 3
depends_on: ["72-02"]
files_modified:
  - .planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md
autonomous: false
requirements: [OWNER-LAUNCH-72]
must_haves:
  truths:
    - "A written staging→prod promote-steps runbook exists listing exact commands and the placeholder-fill checklist"
    - "The owner has visually verified both pages, the nav, and the homepage on staging"
    - "The owner has confirmed the stale-banner reconciliation disposition and the placeholder-fill list before any prod promotion"
    - "No production deploy is performed by the executor"
  artifacts:
    - path: ".planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md"
      provides: "Owner-facing promote runbook + placeholder-fill checklist + banner-disposition record"
      min_lines: 20
  key_links: []
---

<objective>
Produce the owner-facing promote-steps handoff and gate the phase on an owner verification checkpoint. The executor stops at a committed feature branch on staging; the owner promotes to production themselves after filling real business content.

Purpose: Honor the locked decision — no production deploy by the executor — and give the owner a precise runbook plus the definitive "fill before promotion" checklist.
Output: `72-PROMOTE-STEPS.md`, an owner sign-off checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Deployment facts from CLAUDE.md (authoritative): -->
Staging:    git push origin main            → staging.steinsandvines.ca
Production: git push production main --force → steinsandvines.ca
Rule: ALL changes go to staging first; never push directly to production without staging approval.
Prod runbook precedent: docs/ (see recent 0591ffb0 production deploy runbook).

Locked decision: executor stops at a committed feature branch; owner performs prod promotion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the promote-steps runbook + placeholder-fill checklist</name>
  <files>.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md</files>
  <action>Write a concise owner-facing runbook that aggregates the handoff from 72-01 and 72-02. Include, in order:

1. Summary of what shipped: beer.html, cider.html, site-wide nav (list the 17 pages), homepage launch banners, stale-banner reconciliation choice, sitemap.xml + package.json stamp additions.

2. FILL-BEFORE-PROMOTION checklist — the definitive list of every [PLACEHOLDER: ...] marker across beer.html, cider.html, and the homepage banners (pull the exact list from the 72-01 and 72-02 SUMMARYs), grouped by page. Cover the spec's "placeholders to fill" set: beer/cider one-line what-it-is, price, what's-included, availability/dates, exact CTA text; plus any flagged placeholder image filenames (e.g. images/launch/beer-1600w.jpg) still needing a real photo. State explicitly that real values must NOT be guessed by the executor.

3. Stale-banner disposition record: what 72-02 did to the "Beer Is Coming" waitlist banner, flagged for owner confirm/override.

4. Staging deploy steps (feature branch → staging): the exact git commands to merge the feature branch and `git push origin main` to staging.steinsandvines.ca, then how to smoke-test /beer, /cider, nav, homepage, and check the browser console for CSP violations.

5. Production promotion steps (OWNER ONLY, after placeholders filled + staging approved): `git push production main --force` → steinsandvines.ca, per CLAUDE.md and the docs/ runbook precedent. Explicitly note the executor does NOT run this.

6. Post-promotion checks: verify /beer and /cider resolve as clean URLs, nav consistent, no console CSP errors, sitemap picked up.

Keep it tight and command-accurate; this is a runbook, not prose.</action>
  <verify>
    <automated>test -f .planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md && grep -q 'git push production main --force' .planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md && grep -qi 'placeholder' .planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-PROMOTE-STEPS.md</automated>
  </verify>
  <done>72-PROMOTE-STEPS.md exists with the shipped-changes summary, the full placeholder-fill checklist, the banner-disposition record, and staging + production (owner-only) deploy steps.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Two new launch pages (beer.html, cider.html), site-wide Beer/Cider nav, homepage launch banners with the stale "Beer Is Coming" waitlist reconciled, sitemap + build-config registration, and a promote-steps runbook — all committed to the feature branch (no prod deploy).</what-built>
  <how-to-verify>
    1. Serve the site locally or push the feature branch to staging (`git push origin main` per CLAUDE.md) and open staging.steinsandvines.ca.
    2. Visit /beer and /cider: confirm they look consistent with about.html, the primary CTA goes to the ferment booking flow, the call/visit fallback is present, and the pages cross-link + link home.
    3. Confirm every [PLACEHOLDER: ...] marker is acceptable as a placeholder and note the real values you will fill (price, dates, what's-included, CTA text, images).
    4. Check the nav on a few pages (home, a products/* page, hops.html) — Beer + Cider present and links resolve.
    5. On the homepage, confirm the stale "Beer Is Coming / waitlist" banner is gone and the reconciliation choice (see 72-PROMOTE-STEPS.md) is what you want — approve or request a different disposition (e.g. remove Cider banner, keep waitlist, etc.).
    6. Open the browser console on /beer and /cider — confirm no CSP violations.
  </how-to-verify>
  <resume-signal>Type "approved" to finalize the feature branch handoff, or describe changes needed (banner disposition, placeholder markers, nav placement) for a revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → production | Executor must NOT push to production; owner-only gate enforced by checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-06 | Elevation | executor pushing to prod with placeholder content | mitigate | Blocking human-verify checkpoint; runbook marks prod push as owner-only after placeholders filled |
| T-72-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase |
</threat_model>

<verification>
- 72-PROMOTE-STEPS.md exists with staging + owner-only prod steps and the full placeholder checklist
- Owner has verified pages/nav/homepage on staging and confirmed the banner disposition
- No production deploy performed by the executor
</verification>

<success_criteria>
- Complete, command-accurate promote runbook handed off
- Owner sign-off recorded (approved or revision requested)
- Feature branch left committed; production promotion remains the owner's action
</success_criteria>

<output>
Create `.planning/phases/72-beer-and-cider-launch-announcement-pages-beer-html-cider-htm/72-03-SUMMARY.md` when done. Record the owner's checkpoint decision (approved / revision) and the final banner disposition.
</output>
