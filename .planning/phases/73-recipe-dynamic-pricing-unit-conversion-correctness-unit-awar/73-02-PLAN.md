---
phase: 73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - zoho-middleware/routes/recipes.js
  - zoho-middleware/__tests__/recipes.test.js
autonomous: true
requirements: [AC-01, AC-02, AC-03, D-02, D-04]

must_haves:
  truths:
    - "The recipe DETAIL computed_price is unit-converted"
    - "The recipe LIST computed_price is unit-converted"
    - "SV-R-000004 (with a corrected Whirlfloc line) recomputes to ~$88-95, not $1,896.98"
    - "A single un-priceable line marks that recipe's price as errored without aborting the whole list response"
  artifacts:
    - path: "zoho-middleware/routes/recipes.js"
      provides: "enrichWithComputedPrice + enrichListPrices wired to ingredientLineCost with read-path fail-closed"
      contains: "ingredientLineCost"
    - path: "zoho-middleware/__tests__/recipes.test.js"
      provides: "SV-R-000004 regression + read-path fail-closed tests + completed catalog fixtures"
      contains: "88"
  key_links:
    - from: "zoho-middleware/routes/recipes.js enrichWithComputedPrice"
      to: "scaling.ingredientLineCost"
      via: "per-line call replacing the L119 multiply"
      pattern: "ingredientLineCost"
    - from: "zoho-middleware/routes/recipes.js enrichListPrices"
      to: "scaling.ingredientLineCost"
      via: "per-line call replacing the L197 multiply"
      pattern: "ingredientLineCost"
---

<objective>
Wire the two recipe `computed_price` read-path sum-sites (detail + list) in
`routes/recipes.js` to the shared `ingredientLineCost` helper so the displayed dynamic
price is unit-converted, and pin the `SV-R-000004` money regression (~$88-95).

Purpose: `computed_price` is what BrewPad/kiosk display before a sale — it must match the
sale (AC-03) and no longer be ~20× too high (AC-02). This is a READ path: fail-closed here
means marking the offending recipe's price as errored (not a 4xx, since the GET also returns
non-price recipe data), and a bad line must not abort the whole list response.

Output: `enrichWithComputedPrice` + `enrichListPrices` converted and read-path fail-closed;
regression tests including `SV-R-000004` ≈ $88-95; completed catalog test fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@./CLAUDE.md
@.planning/phases/73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar/73-PRICING-BUG-HANDOFF.md
@.planning/phases/73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar/73-PATTERNS.md
@zoho-middleware/lib/recipe-scaling.js

<interfaces>
<!-- Consumes the helper delivered by 73-01. -->
var scaling = require('../lib/recipe-scaling');   // mirror pos-recipe.js L13
scaling.ingredientLineCost(item, line) -> { ok:true, convertedQty, cost } | { ok:false, error }

Read-path fail-closed shape (this plan's decision — NOT a 4xx, unlike the sale path):
  When any line is non-convertible, set recipe.computed_price = null and
  recipe.pricing_error = <the helper's error string> for THAT recipe only.
  Never throw out of the list map (enrichListPrices runs Promise.all over many recipes;
  one bad recipe must not reject the whole response).

SV-R-000004 corrected math (from the handoff evidence table, "correct" column):
  0.648 + 1.08 + 0.045 + 0.03 + 0.32(whirlfloc/tablet) + 11.28 + 4.20 + 20.00 + 0.50
  = 38.10 ingredients + service_fee 45 + materials_fee 5 = ~88.10  → assert in [88, 95].
  NOTE: the ~$88 figure assumes the Whirlfloc line's invalid `L` unit is corrected to a
  count unit (owner data action D-01). Model the regression fixture in that corrected state
  (whirlfloc line unit 'pcs', item unit 'pcs', per-tablet rate ~0.32). D-04: do NOT edit the
  live recipe — this is a fixture-driven test only.
</interfaces>

<analog_excerpts>
<!-- From zoho-middleware/routes/recipes.js — the exact sites and map idiom. -->
enrichWithComputedPrice (L106-141), buggy sum @ L119:
  total += (Number(ing.quantity) || 0) * ing.rate;
enrichListPrices (L143-206), buggy sum @ L197:
  if (entry) total += (Number(ing.quantity) || 0) * (Number(entry.rate) || 0);
Catalog map idiom (reuse verbatim): catalog.forEach(item => { if (item && item.item_id) map[item.item_id] = item; });
Existing imports L1-10 (add `var scaling = require('../lib/recipe-scaling');`).
Fees are added AFTER the ingredient sum: total += service_fee; total += materials_fee;
  Math.round(total * 100) / 100. Keep the fee/round logic; only the per-line term changes.

Test harness: zoho-middleware/__tests__/recipes.test.js L1-72 (mocked express + cache).
  beforeEach currently sets cache.get.mockResolvedValue(null) — new tests need a non-null
  INGREDIENTS_ALL catalog fixture WITH `unit` fields on every entry.
</analog_excerpts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regression tests first — SV-R-000004 recompute + read-path fail-closed + completed fixtures</name>
  <files>zoho-middleware/__tests__/recipes.test.js</files>
  <read_first>
    - zoho-middleware/__tests__/recipes.test.js (harness L1-72; any dynamic-mode computed_price tests; existing beforeEach catalog mocks)
    - .planning/phases/73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar/73-PRICING-BUG-HANDOFF.md (evidence table — per-line item/rate/unit/qty for SV-R-000004)
    - .planning/phases/73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar/73-RESEARCH.md (Pitfall 2 — recipes.test.js dynamic-mode fixtures also lack item-side `unit`)
  </read_first>
  <action>
    Write failing tests (RED) before any route change (CLAUDE.md rule #3):
    (a) SV-R-000004 regression: build an INGREDIENTS_ALL catalog fixture (with `unit` fields) and a
    recipe+ingredients fixture matching the handoff evidence table in its CORRECTED state (bulk hops
    unit 'kg'; recipe lines in 'g'/'ml'; Whirlfloc corrected to 'pcs' ~0.32/tablet; service_fee 45,
    materials_fee 5); assert the enriched `computed_price` is between 88 and 95 (and NOT ~1896).
    (b) Read-path fail-closed: a recipe with one cross-family line (recipe 'g' vs item 'pcs') → after
    enrichment that recipe has `computed_price === null` and a `pricing_error` string naming the line,
    and the handler does NOT reject/500.
    (c) List resilience: a list of two dynamic recipes where one has a bad line → the good recipe still
    gets a numeric computed_price and the response resolves (one bad recipe never aborts the list).
    Also COMPLETE any existing dynamic-mode catalog fixtures in this file by adding matching `unit`
    fields (fixture completion, not assertion weakening — CLAUDE.md rule #10).
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/recipes.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the SV-R-000004 corrected-fixture `computed_price` is in [88, 95].
    - A test asserts a cross-family line yields `computed_price === null` + a `pricing_error` naming the item, with no thrown/5xx.
    - A test asserts a two-recipe list with one bad recipe still returns a numeric price for the good one.
    - Before Task 2 these new tests FAIL (RED) for the right reason (current code produces the inflated/unconverted number).
  </acceptance_criteria>
  <done>Failing regression + fail-closed + list-resilience tests written; existing fixtures completed with units.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire enrichWithComputedPrice + enrichListPrices to ingredientLineCost (read-path fail-closed)</name>
  <files>zoho-middleware/routes/recipes.js</files>
  <read_first>
    - zoho-middleware/routes/recipes.js (imports L1-10; enrichWithComputedPrice L106-141; enrichListPrices L143-206; Promise.all map ~L180)
    - zoho-middleware/lib/recipe-scaling.js (ingredientLineCost contract from 73-01)
  </read_first>
  <behavior>
    - Detail: a dynamic recipe with a g-line vs kg-item gets the converted (small) computed_price.
    - Detail/list: a non-convertible line sets computed_price = null + pricing_error naming the line, no throw.
    - List: one bad recipe among many still resolves the whole list (others priced normally).
  </behavior>
  <action>
    Add `var scaling = require('../lib/recipe-scaling');` to the imports. In enrichWithComputedPrice,
    replace the L119 `total +=` with `var r = scaling.ingredientLineCost(entry, ing);` — on `r.ok`
    add `r.cost` (still set `ing.rate`/`tax_*` for display as today); on `!r.ok` mark
    `recipe.computed_price = null; recipe.pricing_error = r.error;` and stop pricing that recipe
    (return without setting a numeric price). Keep the fee-add + 2dp round for the happy path.
    In enrichListPrices, apply the same per-line replacement at L197; ensure a non-convertible line
    inside the per-recipe map sets that recipe's `computed_price = null` + `pricing_error` and does
    NOT throw out of the Promise.all map (catch/guard per-recipe so one failure never rejects the
    list). Run the FULL middleware suite (rule #7).
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test 2>&1 | tail -20 && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `routes/recipes.js` L119 and L197 no longer contain a bare `* ing.rate` / `* (Number(entry.rate)` multiply; both call `scaling.ingredientLineCost`.
    - The SV-R-000004 regression test passes (computed_price in [88, 95]).
    - The read-path fail-closed + list-resilience tests pass (null price + pricing_error, no 5xx, list not aborted).
    - `cd zoho-middleware && npm test` exits 0 and `npm run lint` is clean.
  </acceptance_criteria>
  <done>Both read-path sum-sites converted and read-path fail-closed; SV-R-000004 money regression green; suite + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BrewPad/kiosk client → GET /api/recipes | Client requests recipe pricing; recipe/ingredient unit+quantity flow into the display price |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | enrichWithComputedPrice / enrichListPrices | mitigate | `rate` read only from the server catalog entry; unit conversion via the shared helper; non-convertible → null price + named error, never a silent number |
| T-73-04 | Denial of service | enrichListPrices | mitigate | One un-priceable recipe marks only its own price errored; per-recipe guard prevents a single bad line from rejecting the whole list response |
| T-73-03 | Information disclosure | pricing_error field | mitigate | `pricing_error` carries only item name + unit strings; no rate/tax/catalog internals |
| T-73-SC | Tampering | npm installs | accept | No package installs this phase |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` exits 0; `npm run lint` clean.
- SV-R-000004 fixture regression asserts computed_price ∈ [88, 95].
- `grep -n "ingredientLineCost" zoho-middleware/routes/recipes.js` shows both read-path sites wired.
</verification>

<success_criteria>
- Detail + list `computed_price` are unit-converted and match the sale (AC-01/AC-03).
- SV-R-000004 recomputes to ~$88-95 (AC-02).
- Read-path fail-closed marks one recipe errored without aborting the list (D-02).
- No live recipe data edited (D-04) — regression is fixture-driven.
</success_criteria>

<output>
Create `.planning/phases/73-recipe-dynamic-pricing-unit-conversion-correctness-unit-awar/73-02-SUMMARY.md` when done.
</output>
