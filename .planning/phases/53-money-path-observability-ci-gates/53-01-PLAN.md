---
phase: 53-money-path-observability-ci-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/server.js
  - zoho-middleware/lib/sentry-scrub.js
  - zoho-middleware/__tests__/sentry-beforeSend.test.js
autonomous: true
requirements: [OBS-01]
user_setup: []

must_haves:
  truths:
    - "A Sentry event carrying a customer email or raw payment amount is scrubbed before it leaves the process"
    - "All captured money-path errors group into one Sentry issue per error class (fingerprint)"
    - "Only safe correlation ids (reqId, txnId, invoice/SO id) survive scrubbing in tags"
  artifacts:
    - path: "zoho-middleware/lib/sentry-scrub.js"
      provides: "scrubEvent(event) + fingerprintFor(event) — pure, unit-testable PII scrub + error-class fingerprint"
      exports: ["scrubEvent", "fingerprintFor"]
    - path: "zoho-middleware/server.js"
      provides: "Sentry.init beforeSend wired to scrubEvent + fingerprintFor"
      contains: "beforeSend"
    - path: "zoho-middleware/__tests__/sentry-beforeSend.test.js"
      provides: "regression proof: seeded email + raw amount are stripped before send"
  key_links:
    - from: "zoho-middleware/server.js Sentry.init"
      to: "lib/sentry-scrub.js scrubEvent"
      via: "beforeSend callback"
      pattern: "beforeSend"
    - from: "lib/sentry-scrub.js"
      to: "lib/redact.js maskEmail"
      via: "require + reuse masking convention"
      pattern: "maskEmail"
---

<objective>
Add the global Sentry `beforeSend` PII scrub and error-class fingerprint to the middleware (D-03, D-04). This is the primary mitigation for the phase's dominant HIGH-severity threat: customer PII or raw payment amounts leaking into Sentry via the money-path `captureException` calls added in plan 53-02.

Purpose: Guarantee that no matter what a money-path capture attaches, customer emails / raw amounts are masked and every event groups by error class before send. This mitigation lands FIRST (wave 1) so the capture calls in 53-02 are already protected when they arrive.
Output: `lib/sentry-scrub.js` (pure scrub + fingerprint), a `beforeSend` wired into `server.js`'s existing `Sentry.init`, and a regression test proving a seeded email + raw amount are scrubbed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-money-path-observability-ci-gates/53-CONTEXT.md
@.planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md

<interfaces>
Existing masking convention to reuse (zoho-middleware/lib/redact.js):
  function maskEmail(email)  // 'jacob@gmail.com' -> 'j***@gmail.com'; '(none)' for empty/malformed
  module.exports = { maskEmail: maskEmail };

Existing Sentry.init (zoho-middleware/server.js:8-14) — DSN-gated:
  var Sentry = require('@sentry/node');
  if (process.env.SENTRY_DSN) {
    Sentry.init({ dsn: ..., environment: ..., tracesSampleRate: 0.1 });
  }
  // Error handler at server.js:590 — Sentry.setupExpressErrorHandler(app) — leave unchanged.

@sentry/node v10.42.0 is already a dependency (no install needed).
beforeSend signature: (event, hint) => event | null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create lib/sentry-scrub.js — pure PII scrub + error-class fingerprint</name>
  <files>zoho-middleware/lib/sentry-scrub.js</files>
  <read_first>
    - zoho-middleware/lib/redact.js (whole file — reuse maskEmail, mirror its "mask, don't drop" convention)
    - zoho-middleware/lib/eventLog.js (top comment — the ZERO PII POLICY this scrub enforces)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ "server.js — beforeSend + fingerprint config", § "PII scrub convention")
  </read_first>
  <behavior>
    - scrubEvent(event): masks event.user.email via redact.maskEmail; deletes/masks any email-shaped string in event.request.data, event.extra, and event.tags values; removes raw monetary values (keys matching /amount|total|price|balance|grandTotal/i) from tags/extra; leaves safe correlation tags (reqId, txnId, invoiceId, salesOrderId, salesorder_id) untouched. Never throws on malformed/undefined event fields.
    - fingerprintFor(event): returns a single-element array keyed by error class — the exception type (event.exception.values[0].type) or event.exception.values[0].value's error name, falling back to ['Error']. Same error class across a burst yields the same fingerprint.
    - Both functions are pure (no Sentry SDK import, no network) so they unit-test directly.
  </behavior>
  <action>Create zoho-middleware/lib/sentry-scrub.js exporting scrubEvent(event) and fingerprintFor(event). Require lib/redact and reuse maskEmail for any email-shaped value. In scrubEvent: mask event.user && event.user.email; iterate event.tags and event.extra and event.contexts, mask values matching an email regex and delete keys whose name matches /amount|total|price|balance|grandtotal/i EXCEPT the allowlist [reqId, txnId, invoiceId, salesOrderId, salesorder_id, invoice_id]; if event.request exists, delete event.request.data and event.request.cookies and event.request.headers (they can carry auth headers / bodies with email+amount). Preserve event.exception. Return the mutated event. In fingerprintFor: read event.exception.values[0].type; if absent, derive from the message; return [type || 'Error']. Guard every property access so undefined structures return safely. Write in middleware ES2020 style (const/arrow allowed here — this is zoho-middleware, NOT frontend).</action>
  <verify>
    <automated>cd zoho-middleware && node -e "const s=require('./lib/sentry-scrub'); const e={user:{email:'jacob@gmail.com'},tags:{reqId:'abc',txnId:'t1',amount:'19.99'},request:{data:{email:'x@y.com'}},exception:{values:[{type:'TypeError'}]}}; const o=s.scrubEvent(e); if(o.user.email.indexOf('jacob@')===0&&o.user.email.includes('***')&&!o.tags.amount&&o.tags.reqId==='abc'&&!o.request.data){console.log('SCRUB_OK')}else{process.exit(1)}; if(s.fingerprintFor(e)[0]==='TypeError'){console.log('FP_OK')}else{process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/lib/sentry-scrub.js` exists and exports `scrubEvent` and `fingerprintFor`.
    - The verify node one-liner prints `SCRUB_OK` and `FP_OK` (email masked to `j***@gmail.com`-shape, `tags.amount` deleted, `tags.reqId` preserved, `request.data` removed, fingerprint = `['TypeError']`).
    - `require('./lib/redact')` is used inside the module (grep: `maskEmail` appears).
  </acceptance_criteria>
  <done>scrubEvent masks emails and drops raw amounts while preserving safe correlation tags; fingerprintFor keys by error class; both are pure and pass the inline node check.</done>
</task>

<task type="auto">
  <name>Task 2: Wire beforeSend into server.js Sentry.init + regression test</name>
  <files>zoho-middleware/server.js, zoho-middleware/__tests__/sentry-beforeSend.test.js</files>
  <read_first>
    - zoho-middleware/server.js:1-15 (the existing Sentry.init block) and server.js:585-592 (setupExpressErrorHandler)
    - zoho-middleware/lib/sentry-scrub.js (created in Task 1)
    - zoho-middleware/__tests__/jest.setup.js (env isolation pattern used by all middleware tests)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ "DSN-gated Sentry calls")
  </read_first>
  <action>In zoho-middleware/server.js, require the new lib/sentry-scrub at the top near the Sentry require. Inside the existing `Sentry.init({...})` object (still behind `if (process.env.SENTRY_DSN)`), add a `beforeSend: function (event, hint) { event = scrub.scrubEvent(event); event.fingerprint = scrub.fingerprintFor(event); return event; }` property alongside `dsn`, `environment`, `tracesSampleRate`. Do NOT change `tracesSampleRate: 0.1`, the environment default, or the `setupExpressErrorHandler(app)` call at line 590. Then create zoho-middleware/__tests__/sentry-beforeSend.test.js that unit-tests the scrub path directly against lib/sentry-scrub (do NOT boot the server): assert a seeded event with `user.email='jacob@gmail.com'`, `tags.amount='19.99'`, `request.data={email,total}` comes back with email masked, amount/total keys gone, request.data removed, and reqId/txnId/invoiceId tags preserved; assert fingerprintFor groups two events of the same error class to the same fingerprint and two different classes to different fingerprints. Use plain jest expect (node testEnvironment).</action>
  <verify>
    <automated>cd zoho-middleware && npx jest sentry-beforeSend --runInBand 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/server.js` `Sentry.init` object contains a `beforeSend` key that calls `scrub.scrubEvent` and sets `event.fingerprint = scrub.fingerprintFor(event)`.
    - `cd zoho-middleware && npx jest sentry-beforeSend` exits 0.
    - The test asserts BOTH: (a) a seeded customer email and raw amount are absent from the returned event, and (b) same-class errors share a fingerprint.
    - `cd zoho-middleware && npm test` exits 0 (no regression); `cd zoho-middleware && npx eslint server.js lib/sentry-scrub.js` reports 0 errors.
  </acceptance_criteria>
  <done>server.js scrubs and fingerprints every Sentry event via beforeSend; a regression test proves PII (email + raw amount) never survives to send, and the full middleware suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| middleware process -> Sentry SaaS | Every captured event crosses to a third-party service; anything on the event (user, request body/headers, tags, extra, breadcrumbs) is exfiltrated unless scrubbed here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | Sentry event `user.email` / `request.data` (customer email) | mitigate | `beforeSend` -> `scrubEvent` masks `event.user.email` via `redact.maskEmail` and deletes `event.request.data/cookies/headers`; regression test asserts a seeded email is masked before send |
| T-53-02 | Information Disclosure | Sentry tags/extra carrying raw payment amounts | mitigate | `scrubEvent` deletes tag/extra keys matching `/amount|total|price|balance|grandtotal/i` outside the safe-id allowlist; test asserts `tags.amount` is stripped |
| T-53-03 | Information Disclosure | auth headers / cookies auto-attached by `setupExpressErrorHandler` request context | mitigate | `scrubEvent` deletes `event.request.headers` and `event.request.cookies` so `API_SECRET_KEY` / session cookies never reach Sentry |
| T-53-SC | Tampering | npm supply chain | accept | No new packages installed this plan (`@sentry/node@10.42.0` already present); nothing to audit |
</threat_model>

<verification>
- `cd zoho-middleware && npm test` — full suite green.
- `cd zoho-middleware && npx jest sentry-beforeSend` — scrub regression passes.
- `cd zoho-middleware && npx eslint server.js lib/sentry-scrub.js` — no new lint errors (note: `--max-warnings 0` is enabled later in 53-06; keep this file warning-free).
</verification>

<success_criteria>
A Sentry event seeded with a customer email and a raw payment amount is provably scrubbed (email masked, amount removed) and fingerprinted by error class before send, wired through the existing DSN-gated `Sentry.init` in `server.js`. Directly satisfies ROADMAP SC#1's "tagged, PII-scrubbed" requirement (the scrub half; capture calls follow in 53-02).
</success_criteria>

<output>
Create `.planning/phases/53-money-path-observability-ci-gates/53-01-SUMMARY.md` when done.
</output>
