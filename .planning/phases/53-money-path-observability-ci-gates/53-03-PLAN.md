---
phase: 53-money-path-observability-ci-gates
plan: 03
type: execute
wave: 3
depends_on: ["53-02"]
files_modified:
  - zoho-middleware/routes/catalog.js
  - zoho-middleware/routes/checkout.js
  - zoho-middleware/routes/pos.js
  - zoho-middleware/routes/webhooks.js
  - zoho-middleware/routes/auth.js
  - zoho-middleware/routes/consignment.js
  - zoho-middleware/routes/payments.js
  - zoho-middleware/routes/pos-recipe.js
  - zoho-middleware/routes/recipes.js
  - zoho-middleware/routes/taxes.js
  - zoho-middleware/lib/cache.js
  - zoho-middleware/lib/calcom.js
  - zoho-middleware/lib/checkout-helpers.js
  - zoho-middleware/lib/helcim.js
  - zoho-middleware/lib/recipe-scaling.js
  - zoho-middleware/lib/reconcile.js
  - zoho-middleware/lib/zohoAuth.js
autonomous: true
requirements: [OBS-01]
user_setup: []

must_haves:
  truths:
    - "eslint routes/ lib/ server.js reports 0 warnings in zoho-middleware — all 60 existing warnings cleared in their own commit before the gate flip (D-05)"
    - "The full middleware test suite still passes (cleanup is behavior-neutral)"
  artifacts:
    - path: "zoho-middleware/routes/catalog.js"
      provides: "no-unused-vars + eqeqeq warnings cleared (26 warnings, the bulk)"
    - path: "zoho-middleware/lib/zohoAuth.js"
      provides: "warnings cleared (5)"
  key_links:
    - from: "eslint routes/ lib/ server.js"
      to: "0-warning state"
      via: "unused vars removed, == -> === where safe"
      pattern: "0 problems"
---

<objective>
Clear all 60 existing eslint warnings in the middleware workspace (41 `no-unused-vars` + 19 `eqeqeq`) in a single behavior-neutral commit (D-05), so the `--max-warnings 0` gate flip in 53-06 lands as a clean diff. This runs AFTER 53-02 so it also cleans any warnings the Sentry `captureException` additions introduced in checkout.js/pos.js/webhooks.js.

Purpose: Prerequisite cleanup for the L12 lint gate. No logic changes — remove dead bindings and tighten loose equality only.
Output: `eslint routes/ lib/ server.js` reports 0 problems; middleware suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/53-money-path-observability-ci-gates/53-CONTEXT.md
@.planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md

<measured_baseline>
Confirmed 2026-07-03 via `eslint routes/ lib/ server.js`: 60 warnings.
By rule: no-unused-vars 41, eqeqeq 19.
By file: routes/catalog.js 26, lib/helcim.js 5, lib/zohoAuth.js 5, routes/recipes.js 4, routes/pos.js 3, lib/checkout-helpers.js 2, lib/reconcile.js 2, routes/auth.js 2, routes/checkout.js 2, routes/webhooks.js 2, and 1 each in lib/cache.js, lib/calcom.js, lib/recipe-scaling.js, routes/consignment.js, routes/payments.js, routes/pos-recipe.js, routes/taxes.js.
server.js has 0 middleware warnings.
Re-measure at implementation — 53-02 may have shifted checkout.js/pos.js/webhooks.js counts.
</measured_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear eqeqeq warnings (19) via safe autofix + review</name>
  <files>zoho-middleware/routes/*.js, zoho-middleware/lib/*.js (eqeqeq sites — see baseline)</files>
  <read_first>
    - zoho-middleware/eslint.config.js (confirm rules: no-unused-vars 'warn', eqeqeq 'warn')
    - the live `eslint routes/ lib/ server.js` output (run it first to get exact eqeqeq locations)
  </read_first>
  <action>Run `cd zoho-middleware && npx eslint routes/ lib/ server.js --fix` to auto-resolve the eqeqeq warnings eslint can safely fix (it converts == and != to === and !== only where type-safe and leaves == null comparisons alone). Then run eslint again; for any remaining eqeqeq warning, inspect the site — if it is an intentional loose-equality/null check, convert to an explicit === null or === undefined form, or add a targeted eslint-disable-line eqeqeq with a one-line reason; otherwise tighten to ===. Do NOT touch no-unused-vars in this task. Run npm test after to confirm no behavior change.</action>
  <verify>
    <automated>cd zoho-middleware && npx eslint routes/ lib/ server.js -f json 2>/dev/null | node -e "let d=JSON.parse(require('fs').readFileSync(0));let n=0;d.forEach(f=>f.messages.forEach(m=>{if(m.ruleId==='eqeqeq')n++}));console.log('eqeqeq_remaining='+n);process.exit(n===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint routes/ lib/ server.js` reports 0 remaining `eqeqeq` warnings (verify command prints `eqeqeq_remaining=0` and exits 0).
    - Any surviving loose-equality kept intentionally carries an inline `eslint-disable-line eqeqeq` with a reason.
    - `cd zoho-middleware && npm test` exits 0.
  </acceptance_criteria>
  <done>All eqeqeq warnings resolved with no behavior change; middleware suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Clear no-unused-vars warnings (41) + verify 0 total</name>
  <files>zoho-middleware/routes/*.js, zoho-middleware/lib/*.js (unused-var sites — catalog.js is 26)</files>
  <read_first>
    - the live `eslint routes/ lib/ server.js -f stylish` output (exact unused-var locations, catalog.js is 26 of them)
    - zoho-middleware/routes/catalog.js (bulk of the unused vars)
  </read_first>
  <action>For each no-unused-vars warning, remove the dead binding: unused require() lines, unused destructured names, unused function params (drop trailing params, or if a middle param must stay for positional reasons rename to a leading-underscore and confirm eslint config treats it as ignored — if not, remove or comment). For an unused catch binding, convert `catch (e)` to `catch` (optional-catch-binding is ES2019, valid in middleware ecmaVersion 2020). Do NOT delete a variable that has side effects in its initializer — if the require/call has a side effect, keep the call but drop the assignment. Make NO logic changes. Re-run eslint until routes/ lib/ server.js reports 0 problems total (both rules). Run npm test.</action>
  <verify>
    <automated>cd zoho-middleware && npx eslint routes/ lib/ server.js 2>&1 | tail -3 && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd zoho-middleware && npx eslint routes/ lib/ server.js` reports `0 problems` (no warnings, no errors).
    - `cd zoho-middleware && npm test` exits 0 (behavior-neutral).
    - No coverage floor in jest.config.js regressed (test run does not fail on thresholds).
    - Diff contains only removals of unused bindings and equality tightening — no functional edits.
  </acceptance_criteria>
  <done>Middleware lint is at 0 warnings; the gate flip in 53-06 will produce a clean diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (no runtime surface change) | Lint-only cleanup; no trust boundary is crossed or altered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-07 | Tampering | behavior drift while "cleaning" (accidentally removing a side-effecting call or changing == null semantics) | mitigate | eslint --fix leaves == null alone; unused-var removals keep side-effecting calls; full `npm test` gate after each task proves behavior-neutral |
| T-53-SC | Tampering | npm supply chain | accept | No new packages installed |
</threat_model>

<verification>
- `cd zoho-middleware && npx eslint routes/ lib/ server.js` — 0 problems.
- `cd zoho-middleware && npm test` — full suite green.
</verification>

<success_criteria>
The middleware workspace lints clean (0 warnings across routes/, lib/, server.js) with no behavior change, in its own commit — the prerequisite for the L12 `--max-warnings 0` gate in 53-06.
</success_criteria>

<output>
Create `.planning/phases/53-money-path-observability-ci-gates/53-03-SUMMARY.md` when done.
</output>
