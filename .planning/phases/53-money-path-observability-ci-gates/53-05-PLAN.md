---
phase: 53-money-path-observability-ci-gates
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - package-lock.json
  - zoho-middleware/package-lock.json
  - package.json
  - zoho-middleware/package.json
  - .nvmrc
  - zoho-middleware/.nvmrc
  - .github/workflows/tests.yml
  - docs/RUNBOOK.md
  - zoho-middleware/jest.config.js
autonomous: true
requirements: [OBS-01]
user_setup: []

must_haves:
  truths:
    - "npm ci succeeds in both the repo root and zoho-middleware — package-lock.json committed for both root and middleware (D-07)"
    - "CI (tests.yml) runs npm ci (not npm install) in every working-directory, and Railway installs via npm ci automatically once the middleware lockfile is committed (D-08)"
    - "Node 20 is pinned via an engines field and a .nvmrc in both root and middleware (D-09)"
    - "jest.config.js enforces a per-file line-coverage floor on routes/pos.js just below its measured coverage"
  artifacts:
    - path: "zoho-middleware/package-lock.json"
      provides: "committed middleware lockfile (was untracked) — prerequisite for npm ci + Railway Nixpacks auto-detect"
    - path: ".nvmrc"
      provides: "Node 20 pin (root)"
    - path: "zoho-middleware/.nvmrc"
      provides: "Node 20 pin (middleware)"
    - path: "zoho-middleware/jest.config.js"
      provides: "./routes/pos.js coverage floor entry (net-new, D-10)"
      contains: "./routes/pos.js"
  key_links:
    - from: ".github/workflows/tests.yml install steps"
      to: "npm ci"
      via: "run: npm ci in each working-directory"
      pattern: "npm ci"
    - from: "committed zoho-middleware/package-lock.json"
      to: "Railway Nixpacks npm ci auto-detect"
      via: "lockfile presence"
      pattern: "package-lock"
---

<objective>
Land the CI/build dependency gates: commit both `package-lock.json` files, pin Node 20 (`engines` + `.nvmrc` in both workspaces), switch CI and Railway installs to `npm ci`, document the Railway side effect in the RUNBOOK, and add the `pos.js` per-file coverage floor (D-07, D-08, D-09, D-10 / L1, L2, L13). Independent of the Sentry and lint-cleanup work — runs in wave 1.

Purpose: Pin exact dependency versions and the runtime so the hardened money path can't drift from a floating dependency or Node bump, and stop `pos.js` coverage from silently regressing.
Output: committed lockfiles, `.nvmrc` x2, `engines` fields, `npm ci` in tests.yml, a RUNBOOK note, and a calibrated `pos.js` coverage floor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/53-money-path-observability-ci-gates/53-CONTEXT.md
@.planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md

<measured_baseline>
- Both lockfiles currently UNTRACKED (confirmed `git ls-files` returns neither). Root package-lock.json and zoho-middleware/package-lock.json both exist on disk.
- tests.yml has 3 `npm install` occurrences: test-middleware (working-directory zoho-middleware), test-frontend (root), test-e2e (root). All node-version pinned to '20' (hardcoded, 4 setup-node steps).
- Neither package.json has an `engines` block.
- routes/pos.js measured line coverage: 81.08% (via `cd zoho-middleware && npx jest --coverage`) — floor 80 gives ~1pt headroom (D-10). Re-confirm at implementation.
- zoho-middleware/railway.toml only sets watchPatterns (no build/install command) — Nixpacks auto-detects npm ci from lockfile presence; no railway.toml edit needed (PATTERNS.md § RUNBOOK.md).
</measured_baseline>

<interfaces>
Existing jest.config.js coverageThreshold block (lines 17-27) — extend, don't rewrite:
  coverageThreshold: {
    global: { lines: 62 },
    './routes/checkout.js':  { lines: 52 },
    './routes/payments.js':  { lines: 36 },
    './routes/webhooks.js':  { lines: 62 },
    './lib/helcim.js':       { lines: 25 },
    './lib/validate.js': { lines: 98 },
    './lib/logger.js':   { lines: 98 }
  },
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Node 20 (engines + .nvmrc) and commit both lockfiles</name>
  <files>.nvmrc, zoho-middleware/.nvmrc, package.json, zoho-middleware/package.json, package-lock.json, zoho-middleware/package-lock.json</files>
  <read_first>
    - package.json (root) and zoho-middleware/package.json (add engines; confirm no existing engines block)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ package.json, § .nvmrc)
  </read_first>
  <action>Create `.nvmrc` at repo root containing a single line `20`, and `zoho-middleware/.nvmrc` containing `20`. Add `"engines": { "node": "20.x" }` to both package.json files (root and zoho-middleware) — place it near the top-level fields (after "private"/"main"). Do NOT change any dependency versions. Then `git add package-lock.json zoho-middleware/package-lock.json` so both lockfiles become tracked. Verify `npm ci` succeeds in each workspace against its committed lockfile: run it in the repo root and in zoho-middleware (adding engines does not invalidate a lockfile — npm ci only compares the dependency tree). If npm ci reports the lockfile is out of sync with package.json, regenerate the lockfile with `npm install --package-lock-only` (no dependency version changes) and re-run npm ci.</action>
  <verify>
    <automated>npm ci >/dev/null 2>&1 && echo ROOT_CI_OK; cd zoho-middleware && npm ci >/dev/null 2>&1 && echo MW_CI_OK; node -e "const r=require('../package.json'),m=require('./package.json');if(r.engines.node&&m.engines.node){console.log('ENGINES_OK')}else{process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `.nvmrc` and `zoho-middleware/.nvmrc` exist, each containing `20`.
    - Both package.json files contain `"engines": { "node": "20.x" }` (verify prints `ENGINES_OK`).
    - `git ls-files package-lock.json zoho-middleware/package-lock.json` lists BOTH (now tracked).
    - `npm ci` succeeds in root (`ROOT_CI_OK`) and in zoho-middleware (`MW_CI_OK`) with no lockfile-sync error.
  </acceptance_criteria>
  <done>Node 20 is pinned in both workspaces and both lockfiles are committed and npm-ci-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Switch CI to npm ci + document Railway side effect</name>
  <files>.github/workflows/tests.yml, docs/RUNBOOK.md</files>
  <read_first>
    - .github/workflows/tests.yml (the 3 `npm install` lines: test-middleware, test-frontend, test-e2e)
    - docs/RUNBOOK.md (Railway deploy / Deploy History sections — where to add the note)
    - zoho-middleware/railway.toml (confirm it only sets watchPatterns — no install command to change)
  </read_first>
  <action>In .github/workflows/tests.yml, change every `- run: npm install` to `- run: npm ci` (3 occurrences: the zoho-middleware working-directory step in test-middleware, the root step in test-frontend, and the root step in test-e2e). Leave the `node-version: '20'` steps as-is (already matches .nvmrc; optional to switch to node-version-file, not required). Do NOT alter the audit or lint steps. In docs/RUNBOOK.md, add a short note under the Railway/deploy section stating: Railway's Nixpacks builder auto-detects `npm ci` (over `npm install`) now that `zoho-middleware/package-lock.json` is committed — no `railway.toml` change was needed; a future contributor removing the lockfile would silently revert Railway to `npm install`.</action>
  <verify>
    <automated>grep -c "npm ci" .github/workflows/tests.yml && ! grep -q "npm install" .github/workflows/tests.yml && echo NO_NPM_INSTALL && grep -qi "npm ci" docs/RUNBOOK.md && echo RUNBOOK_NOTED</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/tests.yml` contains `npm ci` in all 3 install steps and no remaining `npm install` (verify prints `NO_NPM_INSTALL`).
    - `docs/RUNBOOK.md` documents the Railway `npm ci` auto-detect side effect (verify prints `RUNBOOK_NOTED`).
    - No other tests.yml step (lint, audit, node-version) changed.
  </acceptance_criteria>
  <done>CI installs deterministically via npm ci in every job, and the Railway install-command change is documented.</done>
</task>

<task type="auto">
  <name>Task 3: Add routes/pos.js coverage floor (D-10 / L13)</name>
  <files>zoho-middleware/jest.config.js</files>
  <read_first>
    - zoho-middleware/jest.config.js (the coverageThreshold block, lines 17-27 — mirror the comment style of the existing per-file floors)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ jest.config.js — add pos.js floor)
  </read_first>
  <action>Re-measure pos.js line coverage with `cd zoho-middleware && npx jest --coverage 2>/dev/null | grep 'pos.js'` (expected ~81%). Add a NET-NEW entry `'./routes/pos.js': { lines: <floor> },` to the coverageThreshold object in jest.config.js, with the floor set ~1pt below the measured value (80 if measured ~81), following the existing comment convention ("measured X% — floor set at Y, Npt headroom"). Do NOT change the global or any existing per-file floor. Confirm the suite still passes with the new floor enforced.</action>
  <verify>
    <automated>cd zoho-middleware && grep -q "'./routes/pos.js'" jest.config.js && echo POS_FLOOR_PRESENT && npx jest --coverage 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `zoho-middleware/jest.config.js` coverageThreshold contains a `'./routes/pos.js': { lines: N }` entry with a self-documenting comment (verify prints `POS_FLOOR_PRESENT`).
    - N is <= measured pos.js line coverage and within ~1pt of it (no artificial slack, no over-tight floor).
    - `cd zoho-middleware && npx jest --coverage` exits 0 (the new floor passes; no other threshold regressed).
  </acceptance_criteria>
  <done>routes/pos.js has a calibrated per-file coverage floor so its money-path coverage cannot silently regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/CI dependency install -> running artifact | Lockfile + install-command determine exactly which dependency code runs in prod (Railway) and CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-10 | Tampering | floating dependency versions (npm install resolving a newer/malicious minor at deploy time) | mitigate | Commit both lockfiles + switch CI/Railway to `npm ci` (exact-tree install); Node pinned via engines/.nvmrc |
| T-53-11 | Tampering | future contributor deletes the middleware lockfile, silently reverting Railway to npm install | mitigate | RUNBOOK note documents the auto-detect dependency; existing artifact-drift CI job + audit step remain |
| T-53-12 | Repudiation | pos.js money-path regressions slipping in untested | mitigate | Per-file coverage floor fails CI if pos.js coverage drops below the calibrated line |
| T-53-SC | Tampering | npm supply chain | accept | No NEW packages added — lockfiles pin the EXISTING tree; committing them reduces (not increases) supply-chain risk. No package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm ci` in root and `cd zoho-middleware && npm ci` — both succeed against committed lockfiles.
- `grep "npm ci" .github/workflows/tests.yml` — 3 hits, no `npm install` remaining.
- `cd zoho-middleware && npx jest --coverage` — passes with the new pos.js floor.
- `git ls-files package-lock.json zoho-middleware/package-lock.json` — both tracked.
</verification>

<success_criteria>
CI and Railway install via `npm ci`, Node 20 is pinned by engines + .nvmrc in both workspaces, and a calibrated `pos.js` coverage floor is enforced — satisfying ROADMAP SC#2 (L1/L2) and SC#4 (L13).
</success_criteria>

<output>
Create `.planning/phases/53-money-path-observability-ci-gates/53-05-SUMMARY.md` when done.
</output>
