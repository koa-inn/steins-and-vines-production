---
phase: 53-money-path-observability-ci-gates
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - js/admin.js
  - js/brewpad.js
  - js/batch.js
  - js/kiosk.js
  - js/modules/06-featured.js
  - js/modules/07-catalog-kits.js
  - js/modules/08-catalog-ingredients.js
  - js/modules/09-catalog-services.js
  - js/modules/11-cart.js
  - js/modules/12-checkout.js
  - js/modules/12c-checkout-scheduling.js
  - js/modules/13-init.js
  - js/modules/15-hops.js
  - js/modules/16-catalog-subpage.js
  - js/modules/17-search-overlay.js
  - js/main.js
  - js/main.min.js
  - js/admin.min.js
  - js/brewpad.min.js
  - js/batch.min.js
  - js/kiosk.min.js
autonomous: true
requirements: [OBS-01]
user_setup: []

must_haves:
  truths:
    - "eslint js/ reports 0 warnings"
    - "js/ source parses under ecmaVersion 5 (no ES6 syntax remains) — the ES5 rule in 53-06 can pass"
    - "Built artifacts (main.js/main.min.js + admin/kiosk/brewpad/batch mins) are regenerated from the cleaned sources"
  artifacts:
    - path: "js/admin.js"
      provides: "39 warnings cleared + line-789 optional-chaining converted to ES5"
    - path: "js/brewpad.js"
      provides: "31 warnings cleared"
  key_links:
    - from: "eslint js/"
      to: "0-warning + ES5-clean state"
      via: "eqeqeq tightened, console statements resolved, ?. removed"
      pattern: "0 problems"
    - from: "js/modules/*.js sources"
      to: "js/main.js + js/main.min.js"
      via: "npm run build (concat + minify)"
      pattern: "main.min.js"
---

<objective>
Clear all 125 existing eslint warnings in the frontend `js/` tree (99 `eqeqeq` + 26 `no-console`) AND remove the single ES6 construct (optional chaining `?.` at `admin.js:789`) so the ES5-only lint rule in 53-06 can pass (D-05, D-06). Then rebuild the JS artifacts. Independent of the middleware/Sentry work — runs in wave 1.

Purpose: Prerequisite cleanup for the L12 lint gate on the frontend. The measured ES6 scope is tiny — a single `?.` in admin.js (all other `const`/`let`/`=>` matches PATTERNS.md flagged are inside comments; confirmed the tree parses under ecmaVersion 5 with exactly one error).
Output: `eslint js/` reports 0 problems; `js/` parses under ecmaVersion 5; `npm run build` regenerates the artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/53-money-path-observability-ci-gates/53-CONTEXT.md
@.planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md

<measured_baseline>
Confirmed 2026-07-03 via `eslint js/`: 125 warnings.
By rule: eqeqeq 99, no-console 26.
By file: admin.js 39, brewpad.js 31, 17-search-overlay.js 9, kiosk.js 8, 08-catalog-ingredients.js 7, 16-catalog-subpage.js 7, 15-hops.js 6, 07-catalog-kits.js 4, 06-featured.js 3, 09-catalog-services.js 3, batch.js 2, 12-checkout.js 2, 12c-checkout-scheduling.js 2, 11-cart.js 1, 13-init.js 1.

ES6 under ecmaVersion 5: exactly ONE parse error across the whole tree —
  js/admin.js:789  `document.getElementById('res-status-filter')?.value || 'pending';`  (optional chaining).
All `const`/`let`/`=>` grep hits in brewpad.js / 16-catalog-subpage.js / recipe-grouping.js are inside COMMENTS (not code) — no conversion needed there. Re-confirm by running the ecmaVersion-5 check in Task 3.

Build note: js/modules/06-17 are concatenated into js/main.js (see package.json concat:js order). admin.js/kiosk.js/brewpad.js/batch.js are standalone but minified. Any source edit requires `npm run build` (NEVER edit main.js/*.min.js by hand — CLAUDE.md rule 8/9).
</measured_baseline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clear eqeqeq warnings (99) via safe autofix + review</name>
  <files>js/*.js + js/modules/*.js (eqeqeq sites — see baseline)</files>
  <read_first>
    - eslint.config.js (root — rules: eqeqeq 'warn', no-console 'warn'; files js/**/*.js, sourceType script)
    - the live `eslint js/` output (exact eqeqeq locations)
  </read_first>
  <action>Run `npx eslint js/ --fix` to auto-resolve the eqeqeq warnings eslint can safely fix (== / != -> === / !== where type-safe; == null comparisons are left alone by the fixer). Re-run eslint; for each remaining eqeqeq warning inspect the site — intentional null/undefined loose checks become explicit === null form or get a targeted eslint-disable-line eqeqeq with a reason; otherwise tighten to ===. Do NOT touch no-console in this task and do NOT run the build yet. Run `npm test` (frontend jsdom suite) to confirm no behavior change.</action>
  <verify>
    <automated>npx eslint js/ -f json 2>/dev/null | node -e "let d=JSON.parse(require('fs').readFileSync(0));let n=0;d.forEach(f=>f.messages.forEach(m=>{if(m.ruleId==='eqeqeq')n++}));console.log('eqeqeq_remaining='+n);process.exit(n===0?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `eslint js/` reports 0 remaining `eqeqeq` warnings (verify prints `eqeqeq_remaining=0`, exits 0).
    - `npm test` exits 0.
    - No `js/main.js` or `*.min.js` edited by hand (build happens in Task 3).
  </acceptance_criteria>
  <done>All 99 eqeqeq warnings resolved with no behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve no-console warnings (26) + convert admin.js:789 optional chaining to ES5</name>
  <files>js/admin.js + no-console sites across js/</files>
  <read_first>
    - the live `eslint js/` output filtered to `no-console` (26 locations)
    - js/admin.js around line 789 (the `?.value` optional chaining)
  </read_first>
  <action>For each no-console warning: if the console call is genuine debug noise, remove it; if it is intentional operational logging (error reporting the site relies on), keep it and add a targeted `// eslint-disable-line no-console` with a one-line reason — do NOT blanket-disable the rule file-wide. For admin.js line ~789, replace the optional-chaining expression `document.getElementById('res-status-filter')?.value || 'pending'` with an ES5-safe equivalent: assign the element to a var first and guard it (e.g. `var _el = document.getElementById('res-status-filter'); ... (_el ? _el.value : '') || 'pending'`). Preserve the exact 'pending' fallback behavior. Do NOT run the build yet. Run `npm test`.</action>
  <verify>
    <automated>npx eslint js/ -f json 2>/dev/null | node -e "let d=JSON.parse(require('fs').readFileSync(0));let n=0;d.forEach(f=>f.messages.forEach(m=>{if(m.ruleId==='no-console')n++}));console.log('no_console_remaining='+n);process.exit(n===0?0:1)" && grep -c '?\.' js/admin.js</automated>
  </verify>
  <acceptance_criteria>
    - `eslint js/` reports 0 remaining `no-console` warnings (verify prints `no_console_remaining=0`, exits 0).
    - `grep -c '?\.' js/admin.js` prints `0` (optional chaining removed).
    - The `res-status-filter` value still defaults to `'pending'` when the element is absent (behavior preserved).
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>All no-console warnings resolved and the only ES6 construct in js/ converted to ES5.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm ES5-clean + 0 warnings, then rebuild artifacts</name>
  <files>js/main.js, js/main.min.js, js/admin.min.js, js/kiosk.min.js, js/brewpad.min.js, js/batch.min.js</files>
  <read_first>
    - package.json (root — `build`, `concat:js`, `minify:js` scripts; CLAUDE.md rules 8/9 on artifacts)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ eslint.config.js root — ES5 confirmation)
  </read_first>
  <action>Prove `js/` now parses under ecmaVersion 5 by running eslint with a temporary throwaway flat config that sets `languageOptions.ecmaVersion: 5` for `js/**/*.js` (create it, run `npx eslint js/ -c <tmpfile>`, then delete the tmp file) — expect 0 parse errors. Then run `npx eslint js/` and confirm 0 warnings total. Finally run `npm run build` to regenerate js/main.js, js/main.min.js, and the standalone mins (admin/kiosk/brewpad/batch + module mins) from the cleaned sources. Commit the regenerated artifacts alongside the source edits (the build also restamps cache-busting query strings in HTML — that churn is expected per prior phases).</action>
  <verify>
    <automated>npx eslint js/ 2>&1 | tail -2 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The temporary ecmaVersion-5 eslint run reports 0 parse errors across js/ (the tmp config file is deleted afterward).
    - `npx eslint js/` reports `0 problems`.
    - `npm run build` completes without error and regenerates js/main.js + js/main.min.js (git shows them modified).
    - `npm test` exits 0.
  </acceptance_criteria>
  <done>js/ is warning-free and ES5-parseable, and the shipped JS artifacts are rebuilt from the cleaned sources — the ES5 rule + `--max-warnings 0` in 53-06 can now pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (client-side lint cleanup) | No server/trust boundary altered; risk is behavior drift in shipped browser JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Tampering | behavior drift from == null tightening or removing a console the code depended on | mitigate | eslint --fix leaves == null alone; no-console removals reviewed per-site; frontend jsdom `npm test` gate after each task |
| T-53-09 | Tampering | stale/hand-edited build artifact (main.min.js) shipping code that diverges from source | mitigate | Task 3 regenerates via `npm run build`; CLAUDE.md rule 8/9 (never hand-edit artifacts) enforced; artifact-drift CI job (existing) guards drift |
| T-53-SC | Tampering | npm supply chain | accept | No new packages installed |
</threat_model>

<verification>
- `npx eslint js/` — 0 problems.
- ecmaVersion-5 throwaway-config run — 0 parse errors.
- `npm run build` — succeeds, artifacts regenerated.
- `npm test` — frontend jsdom suite green.
</verification>

<success_criteria>
The frontend `js/` tree lints clean (0 warnings) and parses as ES5 (single `?.` converted), with artifacts rebuilt — the prerequisite for the ES5 rule + `--max-warnings 0` gate in 53-06.
</success_criteria>

<output>
Create `.planning/phases/53-money-path-observability-ci-gates/53-04-SUMMARY.md` when done.
</output>
