---
phase: 53-money-path-observability-ci-gates
plan: 06
type: execute
wave: 4
depends_on: ["53-03", "53-04", "53-05"]
files_modified:
  - eslint.config.js
  - package.json
  - zoho-middleware/package.json
autonomous: true
requirements: [OBS-01]
user_setup: []

must_haves:
  truths:
    - "npm run lint fails on ANY warning in both workspaces (--max-warnings 0)"
    - "eslint js/ fails on ES6 syntax — the frontend block enforces ecmaVersion 5"
    - "The middleware eslint config stays ecmaVersion 2020 (await/const legitimately used)"
    - "Both lint scripts exit 0 today (because 53-03/53-04 already cleaned all warnings and ES6)"
  artifacts:
    - path: "eslint.config.js"
      provides: "root js/ block set to ecmaVersion 5 (ES5 enforcement, D-06)"
      contains: "ecmaVersion: 5"
    - path: "package.json"
      provides: "root lint script with --max-warnings 0"
      contains: "--max-warnings 0"
    - path: "zoho-middleware/package.json"
      provides: "middleware lint script with --max-warnings 0"
      contains: "--max-warnings 0"
  key_links:
    - from: ".github/workflows/tests.yml Lint steps"
      to: "--max-warnings 0 gate"
      via: "npm run lint (already called in both jobs) now fails on warnings"
      pattern: "--max-warnings 0"
---

<objective>
Flip the lint gate ON as a clean, isolated commit (D-05, D-06 / L12): add `--max-warnings 0` to both `lint` scripts and set the frontend `js/` eslint block to `ecmaVersion: 5` so CI fails on any new warning and on ES6 syntax. This lands LAST — after the middleware cleanup (53-03), frontend cleanup + ES6 removal (53-04), and the `engines` package.json edits (53-05) — so the flip is a minimal, green diff.

Purpose: Make the lint gate genuinely enforcing. Everything it would flag was already removed by the prerequisite plans, so this commit turns the gate on without touching source.
Output: `--max-warnings 0` in both lint scripts, `ecmaVersion: 5` on the root js/ block; both `npm run lint` runs exit 0; middleware config stays ES2020.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/53-money-path-observability-ci-gates/53-CONTEXT.md
@.planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md

<interfaces>
Root eslint.config.js (js/**/*.js block) — change ONLY ecmaVersion:
  files: ['js/**/*.js'],
  languageOptions: { ecmaVersion: 2020 -> 5, sourceType: 'script', globals: {...globals.browser} },
  rules: { eqeqeq: 'warn', 'no-console': 'warn' }

zoho-middleware/eslint.config.js — LEAVE ecmaVersion: 2020 (D-06: middleware uses await/const legitimately). No edit.

Lint scripts:
  root package.json:        "lint": "eslint js/"                         -> add ` --max-warnings 0`
  middleware package.json:  "lint": "eslint routes/ lib/ server.js"      -> add ` --max-warnings 0`

tests.yml Lint steps already call `npm run lint` in both jobs — no workflow edit needed; the script change flips the gate automatically.

DEPENDENCY GUARD: This plan requires 53-03 (middleware 0 warnings), 53-04 (frontend 0 warnings + ES6 removed), and 53-05 (package.json engines already added, avoids a same-line conflict). Re-run both lint checks at the START of this plan; if either is non-zero, STOP — a prerequisite plan is incomplete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce --max-warnings 0 on both lint scripts</name>
  <files>package.json, zoho-middleware/package.json</files>
  <read_first>
    - package.json (root — scripts.lint) and zoho-middleware/package.json (scripts.lint)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ package.json — --max-warnings 0)
  </read_first>
  <action>Precheck: run `npm run lint` (root) and `cd zoho-middleware && npm run lint` — both MUST currently report 0 warnings (proof 53-03/53-04 are done). If not, stop and report the incomplete prerequisite. Then edit the root package.json `lint` script from `eslint js/` to `eslint js/ --max-warnings 0`, and the zoho-middleware package.json `lint` script from `eslint routes/ lib/ server.js` to `eslint routes/ lib/ server.js --max-warnings 0`. Do not touch the engines fields added in 53-05. Re-run both lint scripts — both must exit 0.</action>
  <verify>
    <automated>npm run lint >/dev/null 2>&1 && echo ROOT_LINT_PASS; cd zoho-middleware && npm run lint >/dev/null 2>&1 && echo MW_LINT_PASS; cd .. && grep -q "max-warnings 0" package.json && grep -q "max-warnings 0" zoho-middleware/package.json && echo FLAG_PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - Both package.json `lint` scripts contain `--max-warnings 0` (verify prints `FLAG_PRESENT`).
    - `npm run lint` (root) exits 0 (`ROOT_LINT_PASS`) and `cd zoho-middleware && npm run lint` exits 0 (`MW_LINT_PASS`).
    - A deliberately-introduced throwaway warning (e.g. a temporary `x == y`) makes `npm run lint` exit non-zero — revert the throwaway after confirming.
  </acceptance_criteria>
  <done>Both lint gates fail on any warning; with the cleaned trees they pass today.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce ES5 on the frontend eslint block</name>
  <files>eslint.config.js</files>
  <read_first>
    - eslint.config.js (root — the `files: ['js/**/*.js']` block, ecmaVersion currently 2020)
    - zoho-middleware/eslint.config.js (confirm it stays 2020 — do NOT edit)
    - .planning/phases/53-money-path-observability-ci-gates/53-PATTERNS.md (§ eslint.config.js root — ES5 enforcement)
  </read_first>
  <action>In the root eslint.config.js, change `ecmaVersion: 2020` to `ecmaVersion: 5` in the `files: ['js/**/*.js']` languageOptions block ONLY. Leave sourceType 'script', the globals, and the rules unchanged. Do NOT edit zoho-middleware/eslint.config.js (it must stay ecmaVersion 2020 for await/const per D-06). Run `npm run lint` (which now also carries --max-warnings 0 from Task 1) — it must exit 0, proving js/ is fully ES5 (53-04 removed the only ?.). As a negative check, temporarily add `const z = 1;` to any js/ module, run `npm run lint`, confirm it FAILS with a parse error, then remove the throwaway.</action>
  <verify>
    <automated>grep -q "ecmaVersion: 5" eslint.config.js && grep -q "ecmaVersion: 2020" zoho-middleware/eslint.config.js && echo CONFIG_SPLIT_OK && npm run lint >/dev/null 2>&1 && echo LINT_ES5_PASS</automated>
  </verify>
  <acceptance_criteria>
    - Root `eslint.config.js` js/ block uses `ecmaVersion: 5`; `zoho-middleware/eslint.config.js` still uses `ecmaVersion: 2020` (verify prints `CONFIG_SPLIT_OK`).
    - `npm run lint` (root, with --max-warnings 0) exits 0 (`LINT_ES5_PASS`).
    - The negative check (temporary `const`) causes `npm run lint` to fail, confirming ES6 is now rejected; throwaway removed afterward.
  </acceptance_criteria>
  <done>The frontend enforces ES5 syntax at lint time; the middleware retains ES2020; the gate is fully live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (CI config gate) | No runtime surface change; risk is a gate that does not actually enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-13 | Repudiation | a lint gate that silently doesn't fail (flag typo, wrong config) | mitigate | Both tasks include a negative check: an intentional warning / ES6 construct must make `npm run lint` fail before the throwaway is reverted |
| T-53-14 | Elevation of scope | accidentally forcing ES5 on the middleware (breaking legit await/const) | mitigate | Task 2 edits only the root config's js/ block; acceptance asserts middleware config stays ecmaVersion 2020 |
| T-53-SC | Tampering | npm supply chain | accept | No new packages installed |
</threat_model>

<verification>
- `npm run lint` (root) — exits 0, fails on an injected warning or ES6 construct.
- `cd zoho-middleware && npm run lint` — exits 0, fails on an injected warning.
- `grep "ecmaVersion" eslint.config.js zoho-middleware/eslint.config.js` — root 5, middleware 2020.
- CI `Lint frontend` / `Lint middleware` steps (unchanged) now enforce the gate via the updated scripts.
</verification>

<success_criteria>
Lint runs with `--max-warnings 0` in both workspaces and the frontend enforces ES5 (middleware stays ES2020); CI fails on a new warning and on ES6 syntax. Closes ROADMAP SC#3 (L12) as a clean, isolated commit on top of the prerequisite cleanups.
</success_criteria>

<output>
Create `.planning/phases/53-money-path-observability-ci-gates/53-06-SUMMARY.md` when done.
</output>
