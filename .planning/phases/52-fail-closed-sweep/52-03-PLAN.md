---
phase: 52-fail-closed-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/__tests__/pos-sale-quarantine.test.js
  - zoho-middleware/__tests__/giftcard-account-failclosed.test.js
autonomous: true
requirements: [RESIL-01]

must_haves:
  truths:
    - "The legacy POST /api/pos/sale route no longer creates invisible orphan charges — it is quarantined (returns a hard error, no terminal charge) after a grep confirms it is dead (M2)"
    - "The gift-card clearing customerpayment no longer falls back to a hardcoded account_id — if ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID is unset the redemption fails closed before charging (M3)"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "Quarantined legacy /api/pos/sale + env-required gift-card clearing account"
      contains: "ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID"
    - path: "zoho-middleware/__tests__/pos-sale-quarantine.test.js"
      provides: "Asserts legacy /api/pos/sale returns the quarantine status and does not reach the terminal"
    - path: "zoho-middleware/__tests__/giftcard-account-failclosed.test.js"
      provides: "Asserts redemption fails closed when the clearing account env is unset"
  key_links:
    - from: "zoho-middleware/routes/pos.js"
      to: "process.env.ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID"
      via: "env-required pre-flight check, no hardcoded fallback"
      pattern: "ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID"
---

<objective>
Close the two pos.js money-path corners the audit flagged: the legacy `POST /api/pos/sale` orphan-charge route (M2) and the hardcoded gift-card clearing `account_id` fallback (M3). Both live in pos.js — held in ONE plan so there is no write conflict with any other plan.

Purpose: M2 eliminates a charge-then-swallow-Zoho-failure orphan invisible even to the reconcile backstop; M3 stops gift-card redemptions from posting to a guessed ledger when the env is unset/mis-pointed. Delivers ROADMAP Success Criterion 2.
Output: legacy route quarantined (documented grep result), gift-card clearing account env-required and fail-closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-fail-closed-sweep/52-CONTEXT.md
@.planning/phases/52-fail-closed-sweep/52-PATTERNS.md

<interfaces>
Planner grep already run (2026-07-03) — DOCUMENT and re-confirm in Task 1:
- `grep -rn "pos/sale" js/` → ZERO frontend callers. Only references: docs/*, openapi.yaml, server.js rate-limit mount (`app.use('/api/pos/sale', paymentLimiter)`), and the route def itself. Route is DEAD.

Current M2 fail-open (pos.js ~1312 route def; orphan swallow at ~1425-1435):
- `.catch(payErr){ log.error('...(non-fatal)...'); }` — charged, no Zoho, no void.
- `.catch(invoiceErr){ log.error('...(non-fatal, txn=...)'); }` — invisible orphan.

Current M3 fail-open (pos.js:1044, inside the kiosk/sale/confirm payment chain):
- `account_id: process.env.ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID || '109900000000873231'` — hardcoded fallback ledger.

Env-required house pattern (PATTERNS.md Pattern D): server.js:293-295 returns 503 when a required secret is unset; `giftCardActivationFailed` flag pattern at pos.js:1076-1077 is the "flag for manual review, don't propagate to the void trigger" idiom.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: M2 — grep-confirm dead, then quarantine legacy /api/pos/sale</name>
  <read_first>
    - zoho-middleware/routes/pos.js (READ the CURRENT legacy route: def ~1312, orphan swallow ~1400-1444)
    - zoho-middleware/server.js (the `app.use('/api/pos/sale', paymentLimiter)` mount ~line 489)
    - zoho-middleware/__tests__/pos.test.js or __tests__/pos-auth-tier.test.js (route-test structure analog)
  </read_first>
  <files>zoho-middleware/routes/pos.js, zoho-middleware/__tests__/pos-sale-quarantine.test.js</files>
  <action>
    First RE-RUN and record the grep in the commit body: `grep -rn "pos/sale" js/` (expect zero) and `grep -rn "api/pos/sale" .` excluding node_modules/tests (expect only docs, openapi.yaml, server.js mount, route def). This confirms the route is dead.
    Disposition — CONDITIONAL on the grep:
    - DEAD branch (expected, per planner grep): QUARANTINE, do not delete (preserve for audit trail). Make the handler return `410 Gone` immediately with `{ error: 'Legacy POS sale endpoint retired — use /api/kiosk/sale' }` BEFORE any `helcimLib` terminal call, so no charge can ever occur. Leave the old orphan-prone body below the early return, or excise it and keep a comment referencing this plan. Keep the `app.use('/api/pos/sale', paymentLimiter)` mount (harmless; the route now 410s).
    - LIVE branch (only if grep unexpectedly finds a real caller): STOP and do NOT auto-harden — a live money path needs the void+pending-record treatment (money-path.voidWithTimeout / rejectWithVoid) and a human checkpoint. Record the finding and halt for re-planning rather than shipping an un-UAT'd money-path change.
    Write NEW `__tests__/pos-sale-quarantine.test.js` asserting POST /api/pos/sale returns 410 and that `helcimLib` terminal-charge functions are never called.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/pos-sale-quarantine.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Commit body records the grep result (`js/` callers: 0; only docs/openapi/server-mount/route-def reference it).
    - POST /api/pos/sale returns 410 and the test asserts no terminal-charge (`helcimLib`) call is made.
    - No `(non-fatal)` orphan-swallow catch remains reachable in the legacy route.
    - `cd zoho-middleware && npm test` exits 0 (baseline green; no existing test edited).
  </acceptance_criteria>
  <done>Commit: `fix(52-03): quarantine dead legacy /api/pos/sale (410) — no orphan charge (M2)` with grep evidence in the body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: M3 — gift-card clearing account fails closed when env unset</name>
  <read_first>
    - zoho-middleware/routes/pos.js (READ the CURRENT kiosk/sale/confirm payment chain ~1020-1100, especially the Payment-2 block at :1044 and the `giftCardActivationFailed` flag at :1076-1077)
    - zoho-middleware/server.js:293-295 (the env-required 503 house pattern)
    - zoho-middleware/__tests__/pos.test.js (route-test structure analog)
  </read_first>
  <files>zoho-middleware/routes/pos.js, zoho-middleware/__tests__/giftcard-account-failclosed.test.js</files>
  <behavior>
    - RED test: with `ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID` UNSET and a gift-card redemption (`gcApplied > 0`), the confirm path does NOT post a customerpayment to the hardcoded `109900000000873231` account — instead it fails closed (redemption flagged for manual review / rejected) and no payment carries the literal fallback account_id.
    - Preserved: with the env SET, the gift-card clearing payment posts with the env's account_id exactly as today.
  </behavior>
  <action>
    First write NEW `__tests__/giftcard-account-failclosed.test.js` asserting the behavior above and confirm RED (today it silently uses the `|| '109900000000873231'` literal). Then in pos.js: remove the `|| '109900000000873231'` fallback. Add a PRE-FLIGHT check — before the terminal charge / before the gift-card Payment-2 block runs — that reads `var gcClearingAccount = process.env.ZOHO_GIFT_CARD_CLEARING_ACCOUNT_ID;` and, when a gift-card redemption is in play (`gcApplied > 0` / `gcCertNum`) but `gcClearingAccount` is falsy, fails CLOSED: do NOT post to a guessed ledger. Prefer the pre-charge position so the customer is never charged against an un-postable redemption; if the code structure forces a post-charge discovery, mirror the `giftCardActivationFailed` idiom (set the manual-review flag, skip the mis-posted payment, surface it to staff) rather than posting to a literal account. Keep the env-set path byte-identical. Confirm GREEN.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/giftcard-account-failclosed.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "873231" zoho-middleware/routes/pos.js` returns nothing (hardcoded fallback removed).
    - New test asserts that with the env unset, no customerpayment is posted with account_id `109900000000873231`, and the redemption fails closed / is flagged for manual review.
    - New test asserts that with the env set, the clearing payment still posts with the env account_id.
    - `cd zoho-middleware && npm test` exits 0 (baseline green; no existing test edited).
  </acceptance_criteria>
  <done>RED→GREEN. Commits: `test(52-03): failing test — gift-card clearing uses hardcoded account when env unset` then `fix(52-03): gift-card clearing account fails closed when env unset (M3)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/pos/sale | Untrusted sale request could trigger a terminal charge with no reconciliation (orphan) |
| operator config → gift-card ledger | A missing/mis-pointed env silently routes money to the wrong Zoho account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-M2 | Repudiation / Denial of Service | legacy POST /api/pos/sale orphan-charge | mitigate | Quarantine to 410 before any terminal charge (grep-confirmed dead); no charge → no invisible orphan. Live-caller branch halts for human-gated re-plan |
| T-52-M3 | Tampering (financial integrity) | gift-card clearing account_id fallback | mitigate | Remove hardcoded `109900000000873231`; require env, fail closed (flag for manual review) rather than post to a guessed ledger |
| T-52-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/pos-sale-quarantine.test.js __tests__/giftcard-account-failclosed.test.js` passes.
- `cd zoho-middleware && npm test` exits 0 (full 62-suite/1187-test baseline green — must not weaken the 45-hardened kiosk/sale confirm path).
- `cd zoho-middleware && npm run lint` clean.
- `grep -n "873231" zoho-middleware/routes/pos.js` returns nothing.
</verification>

<success_criteria>
Legacy /api/pos/sale is quarantined (M2) and the hardcoded gift-card account_id fallback fails closed rather than silently using a default (M3) — ROADMAP Success Criterion 2.
</success_criteria>

<output>
Create `.planning/phases/52-fail-closed-sweep/52-03-SUMMARY.md` when done.
</output>
