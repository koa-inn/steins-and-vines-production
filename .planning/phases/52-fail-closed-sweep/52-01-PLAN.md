---
phase: 52-fail-closed-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/lib/redis-guard.js
  - zoho-middleware/__tests__/redis-guard.test.js
autonomous: true
requirements: [RESIL-01]

must_haves:
  truths:
    - "A single shared closed-on-Redis-error helper exists that both money and security guards can call"
    - "When the guarded Redis operation throws, the helper returns a discriminated failclosed result (in prod / when alwaysClosed)"
    - "The helper's prod/dev decision is overridable via opts so tests force fail-closed without touching NODE_ENV"
  artifacts:
    - path: "zoho-middleware/lib/redis-guard.js"
      provides: "closedOnRedisError(fn, opts) discriminated-result fail-closed wrapper"
      exports: ["closedOnRedisError"]
      contains: "failclosed"
    - path: "zoho-middleware/__tests__/redis-guard.test.js"
      provides: "Unit test proving the helper returns failclosed when fn throws"
      contains: "failclosed"
  key_links:
    - from: "zoho-middleware/lib/redis-guard.js"
      to: "zoho-middleware/lib/money-path.js"
      via: "mirrors the discriminated { status: ... } fail-closed contract"
      pattern: "status.*failclosed"
---

<objective>
Build the single shared "closed-on-Redis-error" helper that the promo (M1) and rate-limit (M4/M5) guards will call. This makes fail-closed-under-Redis a one-place, tested invariant instead of a per-call-site pattern that leaks in the corners (AUDIT §4 insight 2).

Purpose: This is the substrate for Plan 52-02 (M1/M4/M5). Those fixes CALL this helper; it must land and be tested first (earlier wave).
Output: `lib/redis-guard.js` exporting `closedOnRedisError`, plus a unit test proving fail-closed-on-throw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-fail-closed-sweep/52-CONTEXT.md
@.planning/phases/52-fail-closed-sweep/52-PATTERNS.md

<interfaces>
<!-- The NEW helper must mirror this exact contract from lib/money-path.js. -->
<!-- money-path.js is the "contract twin" (PATTERNS.md Pattern A). -->

From zoho-middleware/lib/money-path.js (the shape to replicate):
- Discriminated result objects, never throw on the guarded path: `{ status: 'replay'|'acquired'|'contention'|'failclosed', cached? }`
- The always-fail-closed variant (assertTxnNotReplayed, lines 90-105) returns `{ status: 'failclosed' }` on ANY Redis error, no dev/prod distinction.
- The prod-only variant (acquireIdempotencyLock, lines 52-76) returns `{ status: 'failclosed' }` only when `isProd`, else fails OPEN ('acquired') for local dev convenience.
- `isProd` is derived from `('isProd' in opts) ? !!opts.isProd : (process.env.NODE_ENV === 'production')` so tests override without touching NODE_ENV.
- Dependency injection: the cache lib is passed in as an argument (`cacheLib`), never hard-required, so Jest mocking is transparent.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing unit test defining the redis-guard contract</name>
  <read_first>
    - zoho-middleware/lib/money-path.js (the discriminated-result / fail-closed contract twin — read acquireIdempotencyLock lines 52-76 AND assertTxnNotReplayed lines 90-105)
    - zoho-middleware/__tests__/redis-failclosed.test.js (test-structure analog — jest.mock-before-require, DI of a throwing cacheLib)
  </read_first>
  <files>zoho-middleware/__tests__/redis-guard.test.js</files>
  <behavior>
    - Test: `closedOnRedisError(fn)` where `fn` resolves a value returns `{ status: 'value', value: <that value> }`.
    - Test: `closedOnRedisError(fn, { isProd: true })` where `fn` throws returns `{ status: 'failclosed' }` (prod fails closed).
    - Test: `closedOnRedisError(fn, { alwaysClosed: true })` where `fn` throws returns `{ status: 'failclosed' }` regardless of isProd (always-closed variant for the money/security invariant).
    - Test: `closedOnRedisError(fn, { isProd: false })` where `fn` throws returns `{ status: 'value', value: <opts.devFallback> }` (dev fail-open convenience, matching money-path's dev path).
  </behavior>
  <action>
    Create a NEW test file `__tests__/redis-guard.test.js` (rule 10 — never edit existing tests). Require the not-yet-implemented `../lib/redis-guard` and assert the four behaviors above against the exported `closedOnRedisError(fn, opts)`. Inject a `fn` that returns a Promise (resolve for the happy path, reject/throw for the failclosed paths) — no real Redis. Run it and CONFIRM RED (module missing / assertions fail) before Task 2. Use `opts.devFallback` as the value returned on dev-mode error and `opts.isProd`/`opts.alwaysClosed` as the discriminators.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/redis-guard.test.js 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/redis-guard.test.js` is a NEW file (git status shows untracked; no existing test modified).
    - Running it FAILS (RED) because `lib/redis-guard.js` does not yet exist / `closedOnRedisError` is undefined.
    - The test asserts the exact discriminated shapes `{ status: 'value', value }` and `{ status: 'failclosed' }`.
  </acceptance_criteria>
  <done>Failing test committed as the RED step: `test(52-01): add failing contract test for redis-guard`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement lib/redis-guard.js</name>
  <read_first>
    - zoho-middleware/lib/money-path.js (mirror its structure: 'use strict', var log = require('./logger'), lazy-require any dep, opts.isProd override)
    - zoho-middleware/__tests__/redis-guard.test.js (the contract just written in Task 1)
  </read_first>
  <files>zoho-middleware/lib/redis-guard.js</files>
  <action>
    Create `lib/redis-guard.js` exporting `async function closedOnRedisError(fn, opts)`. Compute `isProd` as `('isProd' in options) ? !!options.isProd : (process.env.NODE_ENV === 'production')`. In a try block, `var value = await fn();` and return `{ status: 'value', value: value }`. In the catch: if `options.alwaysClosed || isProd`, log a warn (`'[redis-guard] Redis unavailable — fail closed: ' + e.message`) and return `{ status: 'failclosed' }`; otherwise (dev, not alwaysClosed) log a warn and return `{ status: 'value', value: options.devFallback }`. Accept an optional `options.label` for the log prefix. `module.exports = { closedOnRedisError: closedOnRedisError };`. Keep the `{ status: ... }` shape byte-for-byte consistent with money-path so checkout.js and server.js read the same way. Run the Task 1 test and CONFIRM GREEN.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/redis-guard.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `lib/redis-guard.js` exports a function `closedOnRedisError` returning `{ status: 'failclosed' }` when its injected `fn` throws and `isProd`/`alwaysClosed` is set.
    - `cd zoho-middleware && npx jest __tests__/redis-guard.test.js` passes (GREEN).
    - `cd zoho-middleware && npm test` exits 0 — the full baseline (62 suites / 1187 tests) plus the new suite stays green.
  </acceptance_criteria>
  <done>Helper implemented; RED→GREEN complete. Commit: `feat(52-01): add shared closed-on-Redis-error helper (redis-guard)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app code → Redis | Redis may be unavailable/throw mid-op; the app must decide open-vs-closed at this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Elevation of Privilege | Per-call-site fail-open drift (systemic) | mitigate | Centralize the fail-closed decision in one tested helper (`closedOnRedisError`) so M1/M4/M5 cannot each re-introduce a fail-open corner |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — helper reuses existing `logger` only |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/redis-guard.test.js` passes.
- `cd zoho-middleware && npm test` exits 0 (full suite green).
- `cd zoho-middleware && npm run lint` clean.
</verification>

<success_criteria>
Shared helper exists, exports `closedOnRedisError`, returns `{ status: 'failclosed' }` on throw (prod/alwaysClosed), and a passing unit test proves it (maps to ROADMAP Success Criterion 1, substrate half).
</success_criteria>

<output>
Create `.planning/phases/52-fail-closed-sweep/52-01-SUMMARY.md` when done.
</output>
