---
phase: 52-fail-closed-sweep
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - zoho-middleware/routes/checkout.js
  - zoho-middleware/server.js
  - zoho-middleware/__tests__/promo-failclosed.test.js
  - zoho-middleware/__tests__/ratelimit-failclosed-52.test.js
autonomous: true
requirements: [RESIL-01]

must_haves:
  truths:
    - "During a simulated Redis outage the FIRSTBATCH promo is NOT repeatable — no $20 discount is applied when the Redis check throws (M1)"
    - "When the rate-limit store's Redis increment throws mid-op, the limiter falls through to in-process memStore accounting instead of returning totalHits:0 (M4)"
    - "In production the loopback rate-limit skip is disabled so a spoofed X-Forwarded-For loopback cannot defeat PIN throttling (M5)"
    - "The shared redis-guard helper is the single decision point for all three fail-closed corners"
  artifacts:
    - path: "zoho-middleware/routes/checkout.js"
      provides: "Promo re-validation routed through closedOnRedisError (no fail-open $20)"
      contains: "closedOnRedisError"
    - path: "zoho-middleware/server.js"
      provides: "Rate-limit store mid-op error falls to memStore; loopback skip gated to non-prod"
      contains: "memStore"
    - path: "zoho-middleware/__tests__/promo-failclosed.test.js"
      provides: "Regression: promo not repeatable during simulated outage"
      contains: "FIRSTBATCH"
    - path: "zoho-middleware/__tests__/ratelimit-failclosed-52.test.js"
      provides: "Rate-limit store returns limiting count (not 0) when increment throws"
  key_links:
    - from: "zoho-middleware/routes/checkout.js"
      to: "zoho-middleware/lib/redis-guard.js"
      via: "require + closedOnRedisError around the promo lock/redemption check"
      pattern: "closedOnRedisError"
    - from: "zoho-middleware/server.js"
      to: "in-process memStore fallback"
      via: "mid-op catch routes to memStore accounting instead of totalHits:0"
      pattern: "memStore\\[key\\]"
---

<objective>
Apply the shared redis-guard helper (from 52-01) to the two remaining Redis fail-open corners the audit flagged: the FIRSTBATCH promo re-validation (M1, checkout.js) and the rate-limit store's mid-op error path + loopback skip (M4/M5, server.js). Fail-closed becomes the tested behavior at both call sites.

Purpose: Closes the repeatable-promo-during-outage money hole (M1) and the limiter-fails-open + loopback-spoof gaps (M4/M5). Delivers ROADMAP Success Criteria 1 (application half) and 6 (promo regression test).
Output: Both guards route their Redis errors through the fail-closed decision; new regression tests prove each returns closed on throw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-fail-closed-sweep/52-CONTEXT.md
@.planning/phases/52-fail-closed-sweep/52-PATTERNS.md
@.planning/phases/52-fail-closed-sweep/52-01-SUMMARY.md

<interfaces>
From zoho-middleware/lib/redis-guard.js (created in 52-01):
- `async function closedOnRedisError(fn, opts)` → `{ status: 'value', value } | { status: 'failclosed' }`
- opts: `{ isProd?, alwaysClosed?, devFallback?, label? }`

Current M1 fail-open (checkout.js:365-388, async/await handler):
- Lock acquisition catch sets `lockAcquired = true` (fail open).
- Redemption-check catch sets `promoDiscount = 20` (fail open — repeatable $20).

Current M4/M5 fail-open (server.js — line numbers verified vs. CONTEXT's STALE refs):
- server.js:377-379 — loopback skip returns `{ totalHits: 1 }` for any loopback key (M5).
- server.js:380-387 — the in-process memStore accounting branch to fall back to.
- server.js:391-393 — null-client branch returns `{ totalHits: 0 }` (M4, also fix).
- server.js:408-410 — mid-op `.catch` returns `{ totalHits: 0 }` (M4, primary fix).
- The security limiters (apiLimiter/paymentLimiter/pinLimiter, server.js:450-482) deliberately have NO redisUnavailableSkip — tightening this store is the single correct point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: M1 — promo FIRSTBATCH fails closed during Redis outage</name>
  <read_first>
    - zoho-middleware/routes/checkout.js (READ the CURRENT promo block ~lines 358-399; do not trust CONTEXT line numbers)
    - zoho-middleware/lib/redis-guard.js (the helper from 52-01)
    - zoho-middleware/lib/money-path.js (acquireIdempotencyLock — the fail-closed idiom to mirror)
    - zoho-middleware/__tests__/promo.test.js and __tests__/checkout.test.js (test-structure analogs)
  </read_first>
  <files>zoho-middleware/routes/checkout.js, zoho-middleware/__tests__/promo-failclosed.test.js</files>
  <behavior>
    - RED test: with `cache.get` (or the promo Redis read) stubbed to THROW, POST /checkout with `promo_code: 'FIRSTBATCH'` does NOT apply the $20 discount — assert the resulting order total / discount reflects `promoDiscount === 0`.
    - RED test: a second identical POST during the simulated outage is likewise not discounted (not repeatable).
    - Existing behavior preserved: with Redis up and the key unredeemed, the $20 promo still applies; with the key already redeemed, it is rejected.
  </behavior>
  <action>
    First write NEW `__tests__/promo-failclosed.test.js` asserting the behavior above and confirm RED (today it applies $20 on throw — fail open). Then in checkout.js: require `../lib/redis-guard`. Route BOTH the lock acquisition (currently `cache.acquireLock(promoKey, 30)` with a fail-open `lockAcquired = true` catch) AND the redemption check (currently `cache.get(promoKey)` with a fail-open `promoDiscount = 20` catch) through `closedOnRedisError(fn, { alwaysClosed: true, label: 'checkout.promo' })`. On a `failclosed` result, set `promoDiscount = 0` and skip the discount (do NOT acquire the lock, do NOT grant the promo). Use `alwaysClosed: true` because this is a money guard — the invariant must hold in every environment. Leave the happy-path (`value`) branches behaving exactly as today. Confirm GREEN.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/promo-failclosed.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts POST /checkout does NOT apply the FIRSTBATCH discount when the promo Redis read throws (promoDiscount === 0), and is not repeatable across two outage-time requests.
    - `checkout.js` requires `../lib/redis-guard` and branches on `{ status: 'failclosed' }` to set `promoDiscount = 0`.
    - The old `promoDiscount = 20` fail-open catch and `lockAcquired = true` fail-open catch are gone.
    - `cd zoho-middleware && npm test` exits 0 (baseline stays green; no existing test edited).
  </acceptance_criteria>
  <done>RED→GREEN. Commits: `test(52-02): failing test — promo repeatable during Redis outage` then `fix(52-02): promo FIRSTBATCH fails closed via redis-guard (M1)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: M4/M5 — rate-limit store fails closed on mid-op error and gates loopback skip to non-prod</name>
  <read_first>
    - zoho-middleware/server.js (READ the CURRENT makeRedisStore ~lines 356-437; CONTEXT's :335/:352/:320 are STALE — the real corners are the loopback branch ~377, memStore branch ~380-387, null-client ~391-393, mid-op catch ~408-410)
    - zoho-middleware/lib/redis-guard.js (the helper from 52-01)
    - zoho-middleware/__tests__/redis-failclosed.test.js (the direct DoD template — supertest + real lib/cache, jest.mock before requires)
    - zoho-middleware/lib/cache.js (isConnected / getClient semantics)
  </read_first>
  <files>zoho-middleware/server.js, zoho-middleware/__tests__/ratelimit-failclosed-52.test.js</files>
  <behavior>
    - RED test: when the Redis client's `incr` throws mid-op (Redis reports connected but the op fails), `makeRedisStore(...).increment(key)` returns an ACCUMULATING count from memStore (e.g. hits 1,2,3...), NOT `{ totalHits: 0 }` — i.e. the limiter still trips.
    - RED test: with `NODE_ENV=production`, a loopback key (`::1` / `127.0.0.1`) during the mem-fallback still ACCUMULATES (loopback skip disabled) so a spoofed `X-Forwarded-For: ::1` cannot defeat PIN throttling.
    - Preserved: in non-production, the loopback skip still returns `{ totalHits: 1 }` (test isolation convenience unchanged).
  </behavior>
  <action>
    First write NEW `__tests__/ratelimit-failclosed-52.test.js` (do NOT edit the existing redis-failclosed.test.js) asserting the two behaviors above; confirm RED. Then in server.js makeRedisStore.increment:
    (M4) Replace the mid-op `.catch(function(){ return { totalHits: 0, ... }; })` at ~408-410 and the null-client `{ totalHits: 0 }` at ~391-393 so that on any Redis error/absent-client they fall through to the SAME in-process memStore accounting used at ~380-387 (increment memStore[key].hits and return that count). Extract the memStore accounting into a small local helper (e.g. `function countInProcess(key){ ... }`) and call it from the connected-but-failed path, the null-client path, AND the not-connected path so all three share one accounting. Route the decision through `closedOnRedisError(function(){ return c.incr(redisKey)...; }, { isProd, alwaysClosed: true, label: 'ratelimit' })` and on `failclosed` return `countInProcess(key)`.
    (M5) Gate the loopback skip at ~377-379 behind `process.env.NODE_ENV !== 'production'` — in production, loopback keys go through `countInProcess` like any other key. Keep the non-prod short-circuit for test/health-check isolation.
    Confirm GREEN. Note: the D-07 comments explaining the memStore fallback stay accurate — update them to note loopback is prod-gated now.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/ratelimit-failclosed-52.test.js && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - New test asserts `increment` returns an accumulating count (limiter trips) when the Redis `incr` throws — never `{ totalHits: 0 }`.
    - New test asserts that with `NODE_ENV=production` a loopback key accumulates (skip disabled); non-prod still returns `totalHits:1`.
    - `server.js` no longer has any `{ totalHits: 0 }` return in the increment path.
    - `cd zoho-middleware && npm test` exits 0 — the existing `redis-failclosed.test.js` and full 62-suite baseline stay green (must not weaken D-07 limiters).
  </acceptance_criteria>
  <done>RED→GREEN. Commits: `test(52-02): failing test — rate-limit store fails open on mid-op error / loopback` then `fix(52-02): rate-limit store fails closed to memStore; loopback skip prod-gated (M4/M5)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /checkout | Untrusted promo_code + repeated requests cross here during a Redis outage |
| client → rate-limited /api routes | Spoofable `X-Forwarded-For` header; Redis may be down mid-request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-M1 | Elevation of Privilege / Repudiation | checkout.js promo FIRSTBATCH | mitigate | Route lock + redemption check through `closedOnRedisError({alwaysClosed})`; on failclosed set `promoDiscount = 0` (no repeatable $20 during outage) |
| T-52-M4 | Denial of Service | server.js makeRedisStore.increment mid-op catch | mitigate | Fall through to in-process memStore accounting instead of `totalHits:0`, so security limiters (pin/payment/api) keep tripping during a Redis outage |
| T-52-M5 | Spoofing | server.js loopback rate-limit skip | mitigate | Gate the loopback skip to `NODE_ENV !== 'production'` so a forged `X-Forwarded-For: ::1` cannot bypass PIN throttling in prod |
| T-52-SC | Tampering | npm installs | accept | No new packages — reuses redis-guard + existing memStore |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/promo-failclosed.test.js __tests__/ratelimit-failclosed-52.test.js` passes.
- `cd zoho-middleware && npm test` exits 0 (full 62-suite/1187-test baseline green).
- `cd zoho-middleware && npm run lint` clean.
- Grep confirms no `totalHits: 0` remains in server.js increment path: `grep -n "totalHits: 0" server.js` returns nothing in makeRedisStore.
</verification>

<success_criteria>
The shared helper is applied to M1 (promo), M4 (mid-op error), and M5 (loopback skip); tests assert each returns closed when its Redis call throws (Success Criterion 1); a regression test asserts the promo is not repeatable during a simulated outage (Success Criterion 6).
</success_criteria>

<output>
Create `.planning/phases/52-fail-closed-sweep/52-02-SUMMARY.md` when done.
</output>
