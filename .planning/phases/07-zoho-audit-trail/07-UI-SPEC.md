---
phase: 7
slug: zoho-audit-trail
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-04
---

# Phase 7 — UI Design Contract: Zoho Audit Trail

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Note:** This project uses vanilla JS (ES5/IIFE), plain CSS in `css/brewpad.css`, and DOM manipulation — no React, no shadcn, no component library. All template sections that reference shadcn/Radix are marked "not applicable". Design tokens are CSS custom properties defined in `css/brewpad.css :root`.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (plain CSS) |
| Preset | not applicable |
| Component library | none (DOM manipulation via `var` / IIFE in `js/brewpad.js`) |
| Icon library | Unicode characters / emoji (existing pattern: `←`, `●`, `○`, `×`) |
| Font | `--font-body: 'Lato', 'Helvetica Neue', Arial, sans-serif` (body); `--font-display: 'Georgia', 'Times New Roman', serif` (headings) |

---

## Spacing Scale

Declared values (multiples of 4 — inferred from existing `brewpad.css` measurements):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, badge margin, dot-to-line spacing in timeline |
| sm | 8px | Info-row padding, gap inside search results, section-title margin-bottom |
| md | 16px | Detail pane inner padding, section gap, `.bp-lifecycle-timeline` margin-bottom, `.bp-link-so-wrap` margin-bottom |
| lg | 24px | Between major timeline sections |
| xl | 32px | Not used in this phase |
| 2xl | 48px | Not used in this phase |
| 3xl | 64px | Not used in this phase |

Exceptions:
- Timeline dot diameter: 12px (not on scale — smallest presentational element)
- Timeline connector line width: 2px
- Info row padding: 4px top/bottom (matches existing `.bp-detail-info-row`)
- Badge padding: 2px 8px vertical/horizontal (matches existing `.bp-status-badge`)
- Kiosk badge padding: 1px 6px (matches existing `.bp-kiosk-badge`)

---

## Typography

All sizes derived from existing `brewpad.css` patterns. No new font sizes are introduced.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / info row | 14px (0.875rem) | 400 (regular) | 1.5 |
| Label / section title | 12px (0.75rem) | 700 (bold), uppercase, letter-spacing 0.06em | 1.2 |
| Badge | 11.5px (0.72rem) | 700 (bold), uppercase, letter-spacing 0.04em | 1 |
| Timeline event label | 14px (0.875rem) | 400 regular (pending: `--ink-muted`), 700 bold (completed) | 1.4 |

Source: CONTEXT.md (D-07–D-09, D-11), existing `css/brewpad.css` patterns (`bp-status-badge`, `bp-detail-info-row`, `bp-detail-section-title`).

---

## Color

All values come from existing `css/brewpad.css :root` tokens. No new colors are introduced.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) surface | `--cellar-canvas: #e8e2ca` | BrewPad background |
| Secondary (30%) raised | `--cellar-raised: #faf8f2` | Detail pane cards, info blocks, timeline track background |
| Accent (10%) — cellar-green | `--cellar-green: #4a6f4b` | Active timeline dots (completed events), "Link to Sales Order" button background on hover, SO search input focus ring |
| Destructive | `--batch-danger: #a83232` | Delete Batch button (existing — no new destructive actions in this phase) |

Accent (`--cellar-green`) reserved for:
- Filled timeline dots for completed lifecycle events
- Primary "Link to Sales Order" button (same treatment as existing `btn` class)
- SO search confirm/select action

Semantic colors for sync indicator (existing tokens):
- Sync in-progress: `--batch-info: #4a6f8a` with `--batch-info-bg` background (matches kiosk badge pattern)
- Sync failed: `--batch-danger: #a83232` with `--batch-danger-bg` background
- Sync succeeded: disappears (no persistent success state, per D-11)

Source: CONTEXT.md (D-11), `css/brewpad.css :root`.

---

## Component Inventory

New CSS classes to be added to `css/brewpad.css`. All follow existing `.bp-` namespace.

### Timeline Section

```
.bp-lifecycle-timeline          — wrapper div, margin-bottom 16px (md — matches .bp-detail-section)
.bp-lifecycle-title             — reuse .bp-detail-section-title (12px, 700, uppercase, --ink-tertiary)
.bp-timeline-track              — vertical flex column, gap 0px (dots and lines are adjacent)
.bp-timeline-event              — flex row, align-items flex-start, gap 12px, padding 0 0 16px 0
.bp-timeline-event:last-child   — padding-bottom 0
.bp-timeline-spine              — flex column, align-items center, width 20px, flex-shrink 0
.bp-timeline-dot                — circle 12×12px, border-radius 50%; filled = --cellar-green; hollow = border 2px solid --ledger-emphasis, background transparent
.bp-timeline-dot--done          — background: var(--cellar-green)
.bp-timeline-dot--pending       — background: transparent; border: 2px solid var(--ledger-emphasis)
.bp-timeline-line               — width 2px, flex 1, min-height 16px, background: var(--ledger-line); hidden on last event
.bp-timeline-body               — flex 1, padding-top 0
.bp-timeline-label              — font-size 0.875rem; weight 700 when done, 400 when pending; color --ink-primary when done, --ink-muted when pending
.bp-timeline-date               — font-size 0.75rem, color --ink-tertiary, margin-top 2px
.bp-timeline-pending-note       — font-size 0.75rem, color --ink-muted, font-style italic; text: "(pending)"
```

### Link to Sales Order Button and Search

```
.bp-link-so-wrap                — flex column, gap 8px, margin-bottom 16px (md — inside .bp-detail-section)
.bp-link-so-btn                 — reuse .btn .bp-btn-sm (existing green button styles)
.bp-so-search-wrap              — flex row, gap 8px, align-items center
.bp-so-search-input             — reuse .bp-inline-input (existing text input styles), flex 1, placeholder: "Customer name or SO number…"
.bp-so-results                  — absolutely positioned dropdown, background --cellar-raised, border 1px solid --ledger-line, border-radius --r-md, max-height 200px, overflow-y auto, z-index 100
.bp-so-result-item              — padding 8px 12px, font-size 0.875rem, cursor pointer, border-bottom 1px solid --ledger-soft
.bp-so-result-item:last-child   — border-bottom none
.bp-so-result-item:hover        — background --cellar-canvas
.bp-so-result-name              — font-weight 700, color --ink-primary, display block
.bp-so-result-meta              — font-size 0.75rem, color --ink-secondary (SO number + date)
.bp-so-linked-display           — flex row, gap 8px, align-items center, font-size 0.875rem (shows confirmed link)
.bp-so-linked-text              — color --ink-primary (SO number)
.bp-so-change-btn               — font-size 0.75rem, color --cellar-green, background none, border none, cursor pointer, text-decoration underline; label: "Change Linked Order"
```

### Sync Status Indicator

```
.bp-sync-indicator              — inline-flex, align-items center, gap 4px, font-size 0.72rem; placed in Zoho Ref info-row, after the SO number
.bp-sync-indicator--syncing     — color --batch-info; text: "syncing…"
.bp-sync-indicator--failed      — color --batch-danger; text: "sync failed — will retry"
(no --synced state; indicator disappears on success)
```

Source: CONTEXT.md (D-07–D-09, D-11), existing `brewpad.css` patterns.

---

## Interaction Contracts

### Link to Sales Order Flow

1. Batch detail view always shows "Link to Sales Order" button in the SO info area (below Zoho Ref row, or replacing the empty SO area if none set). Button label: **"Link to Sales Order"** (no SO linked) or reveals search immediately on click when already linked.
2. On click: button hides; `.bp-so-search-wrap` appears inline with a text input and a **"Close search"** link (when no SO is currently linked) or a **"Keep current link"** link (when an SO is already linked).
3. User types 2+ characters → debounced 400ms → `GET /api/batch/search-so?q={query}` → results appear in `.bp-so-results` dropdown beneath input.
4. While fetching: input shows no spinner (keep it simple), results area shows a single `.bp-so-result-item` with text "Searching…" in `--ink-muted`.
5. No results: single item with "No matching orders found" in `--ink-muted`.
6. Staff clicks a result: dropdown closes, search UI hides, `.bp-so-linked-display` appears showing SO number + "Change Linked Order" link. `POST /api/batch/sync-zoho` is fired fire-and-forget with batch_id + SO data.
7. "Change Linked Order" link: replaces `.bp-so-linked-display` with the search input again (same flow from step 2, dismissal link reads "Keep current link").
8. "Close search" / "Keep current link" link: search UI hides, returns to previous state (either button or linked display).

### Timeline Rendering

1. Four events, always rendered in order: "Sale & SO Created", "Batch Created", "Fermentation Started", "Batch Completed".
2. Events with a date: filled dot (`.bp-timeline-dot--done`), bold label, formatted date below label.
3. Events without a date: hollow dot (`.bp-timeline-dot--pending`), regular-weight label in `--ink-muted`, "(pending)" note below label instead of a date.
4. Connector line between events: always visible between adjacent events (including between a done and a pending event). Color: `--ledger-line`. Line on last event: hidden.
5. Sale date comes from the linked SO's `date` field. If no SO is linked, the Sale event renders as pending.
6. `created_at` from Batches sheet → "Batch Created" event date.
7. `fermentation_started_at` → "Fermentation Started" event date (set by Apps Script when status changes to Active).
8. `completed_at` → "Batch Completed" event date (set by Apps Script when status changes to Complete).

### Sync Indicator Behavior

1. When a Zoho sync is triggered (SO linked, or status transition occurs): `.bp-sync-indicator--syncing` appears in the Zoho Ref row.
2. On sync success: indicator element is removed from DOM (no success badge).
3. On sync failure (middleware returns error or Redis queue is used): indicator changes to `.bp-sync-indicator--failed`.
4. On next successful retry sweep: indicator is removed.
5. No user action is required to dismiss sync failure — it auto-clears when retry succeeds.

Source: CONTEXT.md (D-04, D-06, D-08, D-09, D-11).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Timeline section title | "Lifecycle" |
| Timeline event: sale | "Sale & SO Created" |
| Timeline event: batch created | "Batch Created" |
| Timeline event: fermentation started | "Fermentation Started" |
| Timeline event: batch completed | "Batch Completed" |
| Pending event note | "(pending)" |
| Primary CTA: no SO linked | "Link to Sales Order" |
| Primary CTA: SO linked, change | "Change Linked Order" |
| SO search placeholder | "Customer name or SO number…" |
| SO search: loading | "Searching…" |
| SO search: no results | "No matching orders found" |
| SO search dismiss: no SO linked | "Close search" |
| SO search dismiss: SO already linked | "Keep current link" |
| Sync indicator: in progress | "syncing…" |
| Sync indicator: failed | "sync failed — will retry" |
| Empty state: no SO linked (Zoho Ref row) | "— Link to Sales Order" (dash + link/button) |
| Error: SO search fails (network) | "Search unavailable — check connection" (shown inline in results dropdown) |
| Error: link action fails | Toast: "Failed to link sales order. Try again." (`.bp-toast--error`) |

Destructive actions: None new in this phase. Existing "Delete Batch" button is unchanged.

Source: CONTEXT.md (D-04, D-06, D-07, D-08, D-09, D-11), REQUIREMENTS.md (ZOHO-01, ZOHO-02, ZOHO-03).

---

## Placement in Batch Detail View

The batch detail view in `js/brewpad.js` renders sections sequentially. New Phase 7 sections insert as follows:

```
[Header: Batch ID + Status badge + action buttons]
[Info grid: Product, Customer, Start, Zoho Ref + sync indicator]   ← extend existing
  └─ [Link to Sales Order button / linked display / search UI]       ← NEW, below Zoho Ref row
[Location section]                                                   ← existing, unchanged
[Lifecycle section]                                                  ← NEW, after Location  ← PRIMARY VISUAL ANCHOR
[Tasks section]                                                      ← existing, shifted down
[Measurements section]                                               ← existing, shifted down
[Notes section]                                                      ← existing, shifted down
[Footer actions]                                                     ← existing, unchanged
```

**Primary visual anchor:** The Lifecycle timeline section. It is the largest new visual element in this phase and should draw the eye immediately upon scrolling past the info grid.

Source: CONTEXT.md (D-07), `js/brewpad.js` line ~1547 (batch detail render).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — vanilla CSS/JS, no component registry |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
