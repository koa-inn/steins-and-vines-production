---
phase: 14-kiosk-recipe-sales-inventory-batch-creation
plan: 04
type: execute
wave: 2
depends_on: [14-03]
files_modified:
  - css/kiosk.css
autonomous: true
requirements: [KSK-01]

must_haves:
  truths:
    - "Mode toggle bar has active/inactive visual states with --barrel accent"
    - "Recipe cards reuse existing .kiosk-product-card styles without modification"
    - "Sale-type buttons are 64px tall with selected/unselected visual states"
    - "Availability warning banner has yellow left border; block banner has red left border"
    - "Milling toggle is visible only for take-out (JS-controlled, CSS provides styling)"
  artifacts:
    - path: "css/kiosk.css"
      provides: "Recipe browser CSS: mode toggle, sale-type buttons, availability banners, milling toggle"
      contains: "kiosk-mode-toggle"
  key_links:
    - from: "css/kiosk.css"
      to: "admin.html"
      via: "CSS class names matching HTML element classes"
      pattern: "kiosk-mode-toggle|kiosk-sale-type-btn|kiosk-avail"
---

<objective>
Add all CSS styles for the kiosk recipe browser components: mode toggle bar, sale-type buttons, availability banners, milling toggle, and recipe prompt layout.

Purpose: Delivers the visual design contract from 14-UI-SPEC.md. All new CSS classes use the kiosk- prefix and follow existing kiosk.css patterns.
Output: Modified css/kiosk.css with new component styles
</objective>

<execution_context>
@.planning/phases/14-kiosk-recipe-sales-inventory-batch-creation/14-UI-SPEC.md
@.planning/phases/14-kiosk-recipe-sales-inventory-batch-creation/14-PATTERNS.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- CSS custom properties available from kiosk.css -->
From css/kiosk.css (lines 15-62):
```css
--font-body: Lato, 'Helvetica Neue', Arial, sans-serif;
--font-display: Georgia, 'Times New Roman', serif;
--cellar-canvas: #e8e2ca;
--cellar-raised: #faf8f2;
--cellar-surface: #f5f1e6;
--barrel: #77462d;
--stain: #370e13;
--cellar-green: #4a6f4b;
--ink-primary: #2a1f17;
--ink-secondary: #5c4a3a;
--ink-tertiary: #8a7a6a;
--ink-muted: #b8a898;
--ledger-soft: #d5cbb8;
--batch-success: #3d7a40;
--batch-warning: #b87a1a;
--batch-danger: #a83232;
```

From css/kiosk.css — existing product card styles (lines 498-598):
```css
.kiosk-product-card { background: var(--cellar-raised); border: 1px solid var(--ledger-soft); border-radius: 8px; cursor: pointer; min-height: 180px; }
.kiosk-product-card:hover { border-color: var(--barrel); box-shadow: 0 2px 8px rgba(119,70,45,0.12); }
.kiosk-product-body { padding: 0.75rem; }
.kiosk-product-name { font-weight: 700; font-size: 0.9rem; line-height: 1.25; }
.kiosk-product-price { font-weight: 700; color: var(--barrel); font-size: 0.9rem; }
.kiosk-product-stock { font-size: 0.72rem; color: var(--ink-tertiary); }
```

From 14-UI-SPEC.md — new component CSS classes needed:
- .kiosk-mode-toggle, .kiosk-mode-toggle__btn, .kiosk-mode-toggle__btn--active
- .kiosk-avail-dot (8px circle)
- .kiosk-recipe-prompt
- .kiosk-sale-type-btn, .kiosk-sale-type-btn--selected
- .kiosk-sale-type-btns (2-column grid)
- .kiosk-milling-toggle
- .kiosk-avail-warning, .kiosk-avail-block
- .kiosk-recipe-selected-name
- .kiosk-add-recipe-btn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recipe browser CSS to kiosk.css</name>
  <files>css/kiosk.css</files>
  <read_first>
    - css/kiosk.css (see product card styles at lines 479-598, type badge at around line 530-545, stock status at lines 575-593, button styles)
    - .planning/phases/14-kiosk-recipe-sales-inventory-batch-creation/14-UI-SPEC.md (see Component Inventory, Layout Contract, Interaction States, Color section)
  </read_first>
  <action>
Append the following CSS rules at the END of `css/kiosk.css` (before the final closing comment if any). All new classes use the `kiosk-` prefix and reference existing CSS custom properties.

```css
/* ===== RECIPE BROWSER (Phase 14) ===== */

/* Mode toggle bar */
.kiosk-mode-toggle {
  display: flex;
  gap: 0;
  border-bottom: 1px solid var(--ledger-soft);
  margin-bottom: 0.75rem;
}

.kiosk-mode-toggle__btn {
  flex: 1;
  background: none;
  border: none;
  border-bottom: 2px solid transparent;
  padding: 0.5rem 1rem;
  font-family: var(--font-body);
  font-size: 0.875rem;
  font-weight: 400;
  color: var(--ink-secondary);
  cursor: pointer;
  transition: color 0.15s, border-color 0.15s;
  -webkit-user-select: none;
  user-select: none;
}

.kiosk-mode-toggle__btn:hover {
  color: var(--barrel);
}

.kiosk-mode-toggle__btn--active {
  border-bottom-color: var(--barrel);
  font-weight: 700;
  color: var(--ink-primary);
  cursor: default;
}

/* Recipe prompt pane */
.kiosk-recipe-selected-name {
  font-family: var(--font-display);
  font-size: 1.25rem;
  font-weight: 700;
  color: var(--ink-primary);
  margin: 1rem 0;
}

.kiosk-sale-type-btns {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.75rem;
  margin: 1rem 0;
}

.kiosk-sale-type-btn {
  min-height: 64px;
  font-size: 1rem;
  font-weight: 700;
  font-family: var(--font-body);
  border-radius: 8px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s, border-color 0.15s;
  -webkit-user-select: none;
  user-select: none;
}

.kiosk-sale-type-btn--selected {
  background: var(--stain);
  color: var(--cellar-canvas);
  border-color: var(--stain);
}

.kiosk-sale-type-btn--selected:hover {
  background: var(--stain);
  color: var(--cellar-canvas);
}

/* Milling toggle */
.kiosk-milling-toggle {
  margin: 0.75rem 0;
  font-size: 0.875rem;
  color: var(--ink-secondary);
}

.kiosk-milling-toggle label {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  cursor: pointer;
  -webkit-user-select: none;
  user-select: none;
}

.kiosk-milling-toggle input[type="checkbox"] {
  width: 20px;
  height: 20px;
  accent-color: var(--barrel);
}

/* Add to Cart button (recipe) */
.kiosk-add-recipe-btn {
  width: 100%;
  margin-top: 1rem;
  min-height: 48px;
  font-size: 1rem;
}

/* Availability banners */
.kiosk-avail-warning {
  background: #fef9e7;
  border-left: 3px solid var(--batch-warning);
  padding: 0.75rem 1rem;
  margin: 0.75rem 0;
  font-size: 0.875rem;
  color: var(--ink-primary);
  border-radius: 4px;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.5rem;
}

.kiosk-avail-block {
  background: #fdeaea;
  border-left: 3px solid var(--batch-danger);
  padding: 0.75rem 1rem;
  margin: 0.75rem 0;
  font-size: 0.875rem;
  color: var(--ink-primary);
  border-radius: 4px;
}

/* Availability dot (on recipe cards) */
.kiosk-avail-dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  margin-left: 4px;
  vertical-align: middle;
}

.kiosk-avail-dot--ok { background: var(--batch-success); }
.kiosk-avail-dot--low { background: var(--batch-warning); }
.kiosk-avail-dot--out { background: var(--batch-danger); }
.kiosk-avail-dot--unknown { background: var(--ink-muted); }
```
  </action>
  <verify>
    <automated>grep -c 'kiosk-mode-toggle' css/kiosk.css && grep -c 'kiosk-sale-type-btn' css/kiosk.css && grep -c 'kiosk-avail-warning' css/kiosk.css && grep -c 'kiosk-avail-block' css/kiosk.css && grep -c 'kiosk-avail-dot' css/kiosk.css && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - kiosk.css contains `.kiosk-mode-toggle` with `display: flex`
    - kiosk.css contains `.kiosk-mode-toggle__btn--active` with `border-bottom-color: var(--barrel)` and `font-weight: 700`
    - kiosk.css contains `.kiosk-sale-type-btn` with `min-height: 64px`
    - kiosk.css contains `.kiosk-sale-type-btn--selected` with `background: var(--stain)` and `color: var(--cellar-canvas)`
    - kiosk.css contains `.kiosk-avail-warning` with `border-left: 3px solid var(--batch-warning)`
    - kiosk.css contains `.kiosk-avail-block` with `border-left: 3px solid var(--batch-danger)`
    - kiosk.css contains `.kiosk-avail-dot` at 8px with color variants (--ok, --low, --out, --unknown)
    - kiosk.css contains `.kiosk-milling-toggle` styles
    - `npm run build` succeeds (minified CSS generated)
  </acceptance_criteria>
  <done>All recipe browser CSS added to kiosk.css. Mode toggle, sale-type buttons, availability banners, milling toggle, and availability dot all styled per UI-SPEC. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | CSS-only changes; no trust boundaries crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Information Disclosure | CSS class names | accept | Class names are non-sensitive; they describe UI components, not data |
</threat_model>

<verification>
1. `npm run build` — succeeds, minified CSS updated
2. `grep -c 'kiosk-mode-toggle' css/kiosk.css` returns >= 3 (parent + btn + active)
3. `grep -c 'kiosk-sale-type-btn' css/kiosk.css` returns >= 3 (base + selected + hover)
4. All CSS custom properties used (`--barrel`, `--stain`, `--batch-warning`, `--batch-danger`, `--batch-success`, `--ink-muted`) are defined in kiosk.css
</verification>

<success_criteria>
- Mode toggle bar has clear active/inactive states
- Sale-type buttons are large (64px) with distinct selected state
- Availability banners have appropriate warning/danger colors
- All styles use existing CSS custom properties (no magic hex values except banner backgrounds)
- Build produces updated minified CSS
</success_criteria>

<output>
After completion, create `.planning/phases/14-kiosk-recipe-sales-inventory-batch-creation/14-04-SUMMARY.md`
</output>
