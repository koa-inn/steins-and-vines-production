---
phase: 29-refresh-from-zoho-admin-ui
plan: 06
type: execute
wave: 3
depends_on: ["29-04", "29-05"]
files_modified:
  - js/main.js
  - js/main.min.js
  - js/admin.min.js
  - js/brewpad.min.js
autonomous: false
requirements: [ZSYNC-01, ZSYNC-02]
gap_closure: true
closes_gaps:
  - "Regenerate build artifacts and run the full gate (frontend + middleware + lint) before the gap fixes ship to staging"
user_setup: []

must_haves:
  truths:
    - "Build artifacts (js/main.min.js, js/admin.min.js, js/brewpad.min.js) are regenerated from the gap-fixed js/admin.js and js/brewpad.js sources"
    - "The full frontend Jest suite, the full middleware Jest suite, and ESLint all pass before the fixes are committed"
    - "The case-normalization, name-split, conflict-detection, and trim fixes are present in the minified admin/brewpad artifacts (not just source)"
    - "All gap-fix source + rebuilt artifacts are committed (and, on human approval, pushed to staging)"
  artifacts:
    - path: "js/admin.min.js"
      provides: "Rebuilt admin bundle containing the gap fixes"
      contains: "customer_firstname"
    - path: "js/brewpad.min.js"
      provides: "Rebuilt brewpad bundle containing the gap fixes"
      contains: "customer_firstname"
  key_links:
    - from: "js/admin.js + js/brewpad.js gap fixes"
      to: "js/admin.min.js / js/brewpad.min.js build artifacts"
      via: "npm run build (terser)"
      pattern: "customer_firstname"
---

<objective>
Regenerate build artifacts from the gap-fixed sources and run the full CLAUDE.md commit gate before the Phase 29 gap fixes ship to staging. Per CLAUDE.md rules 1, 2, 7, 9: after any JS module change run `npm run build`; run BOTH the full frontend and middleware Jest suites; run `npm run lint`; never edit the .min.js artifacts by hand. This plan does NOT change behavior — it builds, gates, and (on human approval) ships the work from 29-04 and 29-05.

Purpose: ensure the minified bundles staff actually load contain the fixes, and that nothing regressed across the full test surface before staging.
Output: regenerated build artifacts, a green full-suite + lint run, and a commit (push to staging gated on human approval).

**Sequencing:** wave 3 — runs after both 29-04 (wave 1) and 29-05 (wave 2) have landed all source + test changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-refresh-from-zoho-admin-ui/29-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild artifacts and run the full frontend + middleware + lint gate</name>
  <read_first>
    - CLAUDE.md (Commands section: npm run build, npm test, cd zoho-middleware && npm test, npm run lint; Non-Negotiable Rules 1, 2, 7, 8, 9)
    - package.json (confirm the build/test/lint script names)
  </read_first>
  <action>
    Run `npm run build` from the repo root to regenerate js/main.js, js/main.min.js, js/admin.min.js, and js/brewpad.min.js from the updated sources. Do not hand-edit any .min.js file.

    Then run the full gate in order and confirm each is green:
    1. `npm test` (full frontend Jest suite, jsdom) — must pass with zero failures.
    2. `cd zoho-middleware && npm test` (full middleware Jest suite) — must pass with zero failures (this catches the 29-04 middleware normalization change against the whole middleware suite, not just batch-customer).
    3. `npm run lint` (ESLint) — must pass with zero errors.

    If any step fails, STOP and report the failure with the failing test/lint output — do not commit. (A failure here means a 29-04/29-05 change regressed something; it should be fixed in those plans' scope, not patched ad hoc in the build step.)

    Confirm the fixes are present in the minified artifacts as a build-correctness check: `grep -c "customer_firstname" js/admin.min.js js/brewpad.min.js` (terser preserves string literals / property names accessed dynamically, but firstname/lastname are object keys written as updates — expect >= 1 in each unless terser mangled the key; if 0, verify the source change is actually in js/admin.js/js/brewpad.js and rebuild). Also confirm `grep -c "customer-by-number" js/admin.min.js js/brewpad.min.js` each still return 1 (endpoint wiring intact).
  </action>
  <verify>
    <automated>npm test && cd zoho-middleware && npm test && cd .. && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` completes without error and updates js/main.min.js, js/admin.min.js, js/brewpad.min.js (newer mtime than before)
    - `npm test` passes (full frontend suite, zero failures) — at minimum the previously-passing 485 plus the new gap tests
    - `cd zoho-middleware && npm test` passes (full middleware suite, zero failures)
    - `npm run lint` exits 0
    - `grep -c "customer-by-number" js/admin.min.js js/brewpad.min.js` each return 1
  </acceptance_criteria>
  <done>Artifacts rebuilt from gap-fixed sources; full frontend suite, full middleware suite, and ESLint all green; minified bundles contain the wired endpoint and the fixes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Regenerated build artifacts and ran the full gate after the Phase 29 gap fixes (CR-01 case-normalization, CR-02 name-split visibility, WR-01 conflict detection, WR-03 entity rendering, WR-04 trim parity). The fixes are committed locally and ready to push to staging.
  </what-built>
  <how-to-verify>
    Per CLAUDE.md the staging-first rule requires human approval before any push. After Task 1's gate is green and the work is committed:
    1. Confirm `git status` is clean and `git log -1 --stat` shows the gap-fix commit touching js/admin.js, js/brewpad.js, zoho-middleware/routes/pos.js, the two frontend test files, the middleware test file, and the rebuilt .min.js artifacts.
    2. On approval, push to staging: `git push origin main`.
    3. After the staging deploy completes, open the admin Batches modal and the BrewPad detail pane on staging (iPad Safari per success criterion #4) and confirm: (a) refreshing a batch with a lowercase zoho_so_number succeeds (no "Refresh failed"), (b) the customer NAME visibly changes after refresh on a batch that has firstname/lastname, (c) no console errors.
  </how-to-verify>
  <resume-signal>Type "approved" after staging push + iPad Safari spot-check, or describe any issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source → build artifact (terser) | Minified bundles are what production/staging clients execute; they must faithfully reflect reviewed source |
| Local working tree → staging remote (git push origin main) | Human-gated deployment boundary per CLAUDE.md staging-first rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Tampering | Hand-edited .min.js diverging from source | mitigate | Artifacts are regenerated via `npm run build` only; no manual .min.js edits (CLAUDE.md rule 8/9). grep check confirms wired endpoint present in both bundles. |
| T-29-06-02 | Elevation of Privilege | Unreviewed code reaching production | mitigate | Staging-first human checkpoint (blocking) gates the push; production is not touched by this plan. |
| T-29-06-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed; `npm run build`/`npm test` use the existing lockfile. If an executor runs an install that changes the lockfile, halt for a blocking-human legitimacy check. |
</threat_model>

<verification>
- `npm run build` regenerates the four artifacts.
- `npm test` (frontend), `cd zoho-middleware && npm test` (middleware), `npm run lint` all pass.
- `grep -c "customer-by-number" js/admin.min.js js/brewpad.min.js` each return 1.
- Human-verified on staging (iPad Safari): lowercase-ref refresh succeeds and name visibly updates, no console errors.
</verification>

<success_criteria>
Build artifacts regenerated from the gap-fixed sources; full frontend suite, full middleware suite, and lint green; gap fixes present in the minified bundles; committed and (on human approval) pushed to staging with an iPad Safari spot-check confirming CR-01 and CR-02 are resolved.
</success_criteria>

<output>
Create `.planning/phases/29-refresh-from-zoho-admin-ui/29-06-SUMMARY.md` when done.
</output>
