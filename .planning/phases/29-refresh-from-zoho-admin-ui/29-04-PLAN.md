---
phase: 29-refresh-from-zoho-admin-ui
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/pos.js
  - zoho-middleware/__tests__/batch-customer.test.js
  - js/brewpad.js
  - js/admin.js
  - tests/frontend/brewpad-zoho-refresh.test.js
  - tests/frontend/admin-zoho-refresh.test.js
autonomous: true
requirements: [ZSYNC-01, ZSYNC-02]
gap_closure: true
closes_gaps:
  - "CR-01: Case-sensitivity contract mismatch between frontend isValidZohoNumber (/i) and middleware case-sensitive validation"
user_setup: []

must_haves:
  truths:
    - "A batch whose zoho_so_number is lowercase (inv-000123 / so-42) can be refreshed successfully — middleware normalizes case before validating, so the case-insensitive UI gate and the middleware accept the same set of refs (single coherent contract)"
    - "If the middleware ever returns 400 invalid_number, both refresh handlers show a distinct message (not the generic 'Refresh failed — try again')"
    - "Frontend tests assert the chosen case-insensitive contract intentionally (no longer baking in a broken contract); middleware tests prove lowercase refs pass validation"
  artifacts:
    - path: "zoho-middleware/routes/pos.js"
      provides: "Case-normalized validation: number uppercased before the /^INV-\\d+$/ and /^SO-\\d+$/ checks"
      contains: "toUpperCase"
    - path: "js/brewpad.js"
      provides: "400 invalid_number branch in the refresh fetch handler; corrected doc comment at line 45"
    - path: "js/admin.js"
      provides: "400 invalid_number branch in the refresh fetch handler"
    - path: "zoho-middleware/__tests__/batch-customer.test.js"
      provides: "Test asserting lowercase inv-/so- refs pass the prefix validation (do not 400)"
  key_links:
    - from: "frontend isValidZohoNumber gate"
      to: "middleware /api/batch/customer-by-number validation"
      via: "case-insensitive contract on both layers"
      pattern: "toUpperCase"
---

<objective>
Close gap CR-01: the frontend Refresh button gate (`isValidZohoNumber`, case-insensitive `/^(INV|SO)-\d+$/i`) accepts lowercase Zoho refs that the middleware then rejects with HTTP 400 `invalid_number`. Pick ONE contract and align every layer.

**Contract chosen: case-insensitive (Option b — normalize in middleware).** Rationale: the verifier explicitly prefers "the more robust option"; D-08's stated intent is case-insensitive; pos.js:1409 already does a case-insensitive exact-match against Zoho documents, so the validation regex is the only case-sensitive layer and is internally inconsistent with its own downstream match. Normalizing input means lowercase-ref batches actually refresh (feature works) instead of having the button silently hidden. A 400 handler is added to both frontend surfaces as defense-in-depth so any future contract drift surfaces a clear message instead of "Refresh failed".

Purpose: lowercase-ref batches stop hitting unhandled 400s; ZSYNC-02 "clearly unavailable rather than erroring" is restored.
Output: middleware normalization + frontend 400 handling + corrected doc comment + updated/added tests across both suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-refresh-from-zoho-admin-ui/29-VERIFICATION.md
@.planning/phases/29-refresh-from-zoho-admin-ui/29-REVIEW.md
@.planning/phases/29-refresh-from-zoho-admin-ui/29-CONTEXT.md
@CLAUDE.md

<interfaces>
Middleware validation (zoho-middleware/routes/pos.js:1376-1384), current state:
- `var number = (req.query.number || '').trim();`
- `var isInvoice = /^INV-\d+$/.test(number);`
- `var isSO = /^SO-\d+$/.test(number);`
- 400 body on failure: `{ error: 'invalid_number', message: 'number must match INV-NNNN or SO-NNNN format' }`
- Downstream (pos.js:1409) already matches docs case-insensitively: `dn.toLowerCase() === number.toLowerCase()`

Frontend gate is identical in both files: `/^(INV|SO)-\d+$/i` (brewpad.js:48, admin.js:9572).
Both refresh fetch handlers currently branch on `r.status === 404` and `r.status === 502` but NOT `400` (brewpad.js:2580-2582, admin.js:6070-6072).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize Zoho ref case in middleware validation + regression test</name>
  <read_first>
    - zoho-middleware/routes/pos.js (lines 1370-1420 — the /api/batch/customer-by-number handler, validation block, and the downstream case-insensitive exact-match at 1409)
    - zoho-middleware/__tests__/batch-customer.test.js (existing test structure, how it mocks zohoGet and asserts status codes)
    - CLAUDE.md (rule 3: write a regression test FIRST; rule 12: cd zoho-middleware before middleware commands)
  </read_first>
  <action>
    Per CLAUDE.md rule 3, FIRST add a failing regression test to zoho-middleware/__tests__/batch-customer.test.js asserting that a lowercase ref (`number=inv-000123`) does NOT return 400 invalid_number — it should proceed to the Zoho lookup path (mock zohoGet to return a matching doc and assert 200, or assert the response is not status 400 with error 'invalid_number'). Add a companion case for `so-42`. Keep existing tests untouched (this is an additive new test the verification report requires).

    Then change the validation block at pos.js:1376-1380. Replace the existing `.trim()`-only normalization with trim + uppercase so the case-sensitive regexes receive an uppercased value. Concretely: keep a trimmed-original variable for use in user-facing messages/Zoho filter if needed, but compute the validation input as `(req.query.number || '').trim().toUpperCase()` and run `/^INV-\d+$/.test(...)` and `/^SO-\d+$/.test(...)` against that uppercased value. Ensure the value passed downstream to the Zoho filter (`filterParams[filterKey] = number`) uses the uppercased number so the document lookup is consistent with what was validated (Zoho document numbers are uppercase; the existing 1409 match lowercases both sides so either is safe, but use the uppercased value for clarity). Do NOT alter the 400 error body shape (`{ error: 'invalid_number', message: ... }`) — frontend branches on `error === 'invalid_number'`.

    Run `cd zoho-middleware && npm test` and confirm the new tests pass and no existing batch-customer tests regress.
  </action>
  <verify>
    <automated>cd zoho-middleware && npx jest __tests__/batch-customer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - zoho-middleware/routes/pos.js contains `toUpperCase` in the validation block before the `/^INV-\d+$/` / `/^SO-\d+$/` tests (source assertion: `grep -n "toUpperCase" zoho-middleware/routes/pos.js` returns a line in the 1376-1390 range)
    - New test cases for `inv-000123` and `so-42` exist in batch-customer.test.js and assert the response is NOT 400 invalid_number
    - `cd zoho-middleware && npx jest __tests__/batch-customer.test.js` exits 0 with all tests passing
    - The 400 error body still uses `error: 'invalid_number'` (unchanged)
  </acceptance_criteria>
  <done>Lowercase inv-/so- refs pass middleware validation and reach the Zoho lookup; regression tests green; uppercase refs still pass; genuinely malformed refs (no digits, wrong prefix) still 400.</done>
</task>

<task type="auto">
  <name>Task 2: Add 400 invalid_number handling in both frontend refresh handlers + fix false doc comment + align tests</name>
  <read_first>
    - js/brewpad.js (lines 41-49 the isValidZohoNumber helper and false doc comment at line 45; lines 2576-2685 the refresh fetch chain including the 404/502 status branches and both catch blocks)
    - js/admin.js (lines 9562-9573 the isValidZohoNumber helper and its JSDoc; lines 6065-6137 the refresh fetch chain and catch block)
    - tests/frontend/brewpad-zoho-refresh.test.js (lines 35-67 — the isValidZohoNumber describe block, including the inv-5/so-999 case-insensitive assertions at 45-50)
    - tests/frontend/admin-zoho-refresh.test.js (lines 103-150 — the admin isValidZohoNumber tests including inv-5/so-123 case-insensitive assertions)
    - CLAUDE.md (rule 8/9: never edit min.js; run npm run build after JS changes — but build is done in plan 29-06, not here)
  </read_first>
  <action>
    In BOTH js/brewpad.js (fetch chain at ~2579-2583) and js/admin.js (fetch chain at ~6069-6072), add an explicit branch for `r.status === 400` that reads the JSON body and throws a typed object `{ status: 400, error: d.error }` (mirroring the existing 404/502 branches). Then in each handler's outer `.catch`, add a branch that detects `err && err.status === 400` (or `err.error === 'invalid_number'`) and shows a distinct toast: use the message "This Zoho reference is not a valid INV/SO number" (error style). Place this branch BEFORE the generic "Refresh failed — try again" fallback. Do NOT remove the existing 404/502 branches.

    Fix the provably-false doc comment at js/brewpad.js:45. Replace the sentence asserting "a 400 invalid_number response can never fire from the UI" with an accurate statement: the gate is case-insensitive and matches the middleware contract after middleware case-normalization (plan 29-04 Task 1); the handler still defends against a 400 invalid_number for robustness. Apply the equivalent correction to the admin JSDoc at js/admin.js:9564-9568 — per 29-REVIEW.md CR-01 it makes the same false claim ("matches the format the … endpoint accepts"); rewrite it to state the same case-normalization contract.

    Keep the `/^(INV|SO)-\d+$/i` regex unchanged in BOTH helpers — the chosen contract is case-insensitive, so the existing case-insensitive gate is correct and the inv-5/so-999/so-123 test assertions remain valid (the contract now matches the middleware). Add ONE new assertion to each test suite's isValidZohoNumber describe block documenting the contract decision: a comment plus an assertion that `isValidZohoNumber('inv-000123')` and `isValidZohoNumber('so-42')` are `true` AND a note (test name) clarifying these now succeed against the middleware. Do not weaken or invert the existing case-insensitive assertions — the verification report explicitly authorizes updating these tests to the chosen contract; the chosen contract keeps them true.

    Run `npm test -- tests/frontend/brewpad-zoho-refresh.test.js tests/frontend/admin-zoho-refresh.test.js` and confirm green.
  </action>
  <verify>
    <automated>npx jest tests/frontend/brewpad-zoho-refresh.test.js tests/frontend/admin-zoho-refresh.test.js --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "status === 400" js/brewpad.js js/admin.js` returns a match in each file's refresh fetch chain
    - Each handler's catch maps the 400 case to a message containing "valid INV/SO number" (or equivalent distinct from "Refresh failed — try again")
    - js/brewpad.js line ~45 no longer contains the literal phrase "can never fire from the UI"; the new comment references the middleware case-normalization contract
    - The isValidZohoNumber JSDoc in js/admin.js (lines ~9562-9568) no longer asserts the broken invariant: `grep -n "endpoint accepts" js/admin.js` returns no match inside that JSDoc range, and the rewritten comment references the middleware case-normalization contract
    - Both `/^(INV|SO)-\d+$/i` regexes are unchanged (source assertion: `grep -c "INV|SO)-\\\\d+\$/i" js/brewpad.js js/admin.js` each return 1)
    - `npx jest tests/frontend/brewpad-zoho-refresh.test.js tests/frontend/admin-zoho-refresh.test.js --no-coverage` exits 0
  </acceptance_criteria>
  <done>Both surfaces handle a 400 invalid_number with a clear distinct message; the false invariant comment is corrected; case-insensitive gate retained to match the normalized middleware; test suites green and intentionally assert the case-insensitive contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/BrewPad client → middleware GET /api/batch/customer-by-number | `number` query param is attacker/operator-influenced input crossing into the middleware and onward to Zoho's filter API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | `number` query param → Zoho filter (pos.js:1376-1393) | mitigate | After uppercasing, value still passes the strict `/^INV-\d+$/` / `/^SO-\d+$/` regex BEFORE any Zoho call; non-conforming input is rejected with 400. `toUpperCase()` does not widen the accepted character set (only INV/SO + digits survive the regex), so no injection surface is added. |
| T-29-04-02 | Information Disclosure | 400/404/502 error bodies returned to client | accept | Error bodies expose only the ref number the caller already supplied and a static format string — no PII, no Zoho internals. |
| T-29-04-03 | Spoofing | Endpoint auth | mitigate | Existing `x-api-key === process.env.MW_API_KEY` check (pos.js:1371-1374) is unchanged; this plan does not touch auth. |
| T-29-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan; no install tasks. If any executor adds a dependency, halt for a blocking-human legitimacy check. |
</threat_model>

<verification>
- `cd zoho-middleware && npx jest __tests__/batch-customer.test.js` passes (lowercase refs no longer 400).
- `npx jest tests/frontend/brewpad-zoho-refresh.test.js tests/frontend/admin-zoho-refresh.test.js --no-coverage` passes.
- `grep -n "toUpperCase" zoho-middleware/routes/pos.js` shows normalization in the validation block.
- `grep -n "status === 400" js/brewpad.js js/admin.js` shows a 400 branch in each handler.
</verification>

<success_criteria>
Frontend case-insensitive gate and middleware validation accept the same set of refs (single coherent contract). Lowercase-ref batches refresh successfully end-to-end. Any 400 invalid_number is surfaced with a distinct message in both surfaces. The false doc comment is corrected. Both test suites and the middleware suite are green.
</success_criteria>

<output>
Create `.planning/phases/29-refresh-from-zoho-admin-ui/29-04-SUMMARY.md` when done.
</output>
