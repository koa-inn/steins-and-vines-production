---
phase: 29-refresh-from-zoho-admin-ui
plan: 03
type: execute
wave: 2
depends_on: ["29-01", "29-02"]
files_modified:
  - js/main.js
  - js/main.min.js
  - js/admin.min.js
  - js/brewpad.min.js
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [ZSYNC-01, ZSYNC-02]
must_haves:
  truths:
    - "Full frontend + middleware test suites and lint pass before the feature ships to staging"
    - "Build artifacts are regenerated from the updated js/admin.js and js/brewpad.js sources"
    - "The refresh fetch token is present in both js/brewpad.min.js and js/admin.min.js (the two separate minified artifacts), not js/main.min.js"
    - "Updated sources + rebuilt artifacts + new tests are committed and pushed to the staging remote (git push origin main)"
    - "The Refresh-from-Zoho feature works end-to-end on staging.steinsandvines.ca on iPad Safari with no console errors (success criterion #4)"
    - "REQUIREMENTS.md traceability reflects ZSYNC-01/02 as verified by this phase"
  artifacts:
    - path: "js/brewpad.min.js"
      provides: "Rebuilt minified BrewPad bundle including the refresh handler"
    - path: "js/admin.min.js"
      provides: "Rebuilt minified admin bundle including the refresh handler"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected traceability for ZSYNC-01/02"
  key_links:
    - from: "js/admin.js + js/brewpad.js sources"
      to: "minified build artifacts"
      via: "npm run build"
      pattern: "npm run build"
    - from: "committed sources + artifacts + tests"
      to: "staging remote (origin/main)"
      via: "git push origin main"
      pattern: "git push origin main"
---

<objective>
Finalize Phase 29: regenerate build artifacts from the updated sources, run the full gate (frontend tests, middleware tests, lint), commit and push to the staging remote, and verify the Refresh-from-Zoho feature works end-to-end on iPad Safari with no console errors (ROADMAP success criterion #4). Then correct the REQUIREMENTS.md traceability that was prematurely ticked.

Purpose: Plans 29-01 and 29-02 deliberately skipped `npm run build` and on-device verification; this plan consolidates the build + verification so the feature ships as one validated unit, honoring the staging-first rule and the v4.1 batched-prod-deploy hold.
Output: Rebuilt `js/main.js`/`*.min.js`, a committed + pushed staging deploy, a human-verified UAT pass, and a REQUIREMENTS.md correction.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-refresh-from-zoho-admin-ui/29-CONTEXT.md
@.planning/phases/29-refresh-from-zoho-admin-ui/29-01-SUMMARY.md
@.planning/phases/29-refresh-from-zoho-admin-ui/29-02-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build artifacts, run the full pre-commit gate, then commit and push to staging</name>
  <files>js/main.js, js/main.min.js, js/admin.min.js, js/brewpad.min.js</files>
  <read_first>
    - CLAUDE.md "Commands", "Non-Negotiable Rules", and "Deployment" (build + dual test suites + lint before commit; never edit min files by hand; staging = `git push origin main`; CNAME is gitignored and enforce-cname.yml auto-corrects the domain after every push)
    - .planning/phases/29-refresh-from-zoho-admin-ui/29-01-SUMMARY.md and 29-02-SUMMARY.md (what shipped, any caveats)
  </read_first>
  <action>
    Run `npm run build` at the repo root to regenerate the concatenated `js/main.js` and all minified artifacts (`js/main.min.js`, `js/admin.min.js`, `js/brewpad.min.js`) from the updated `js/admin.js` and `js/brewpad.js` sources. Do NOT hand-edit any `.min.js` file. Then run the full gate that CLAUDE.md requires before any commit:
    - `npm test` (frontend Jest, jsdom) — must pass, including the two new files tests/frontend/brewpad-zoho-refresh.test.js and tests/frontend/admin-zoho-refresh.test.js.
    - `cd zoho-middleware && npm test` — must pass (no middleware changes this phase, but the rule is non-negotiable; confirms nothing regressed).
    - `npm run lint` (full ESLint, not a single-file run).
    If any suite or lint fails, fix the source (js/admin.js or js/brewpad.js), rebuild, and re-run until green. Do not modify existing tests to make them pass (CLAUDE.md rule 10).

    Once the full gate is green, commit and push to STAGING (CLAUDE.md staging-first rule is non-negotiable):
    1. Confirm the CNAME file is NOT being committed: `git status --porcelain CNAME` must be empty (CNAME is gitignored; enforce-cname.yml auto-pins the domain after the push, so do NOT touch or stage CNAME — the legacy cat-CNAME swap dance does not apply).
    2. Stage the updated sources, rebuilt artifacts, and new tests: `git add js/admin.js js/brewpad.js js/main.js js/main.min.js js/admin.min.js js/brewpad.min.js tests/frontend/brewpad-zoho-refresh.test.js tests/frontend/admin-zoho-refresh.test.js`.
    3. Commit with a conventional message: `git commit -m "feat(29): Refresh-from-Zoho on BrewPad + admin batch surfaces (ZSYNC-01/02)"`.
    4. Push to the staging remote: `git push origin main`. Do NOT push to production (`production` remote / `git push production main` is held per the v4.1 batched-deploy STATE.md blocker until 27/27.1/28/29/29.1 ship together).
    After the push, let enforce-cname.yml settle the domain.
  </action>
  <verify>
    <automated>npm run build && npm test && ( cd zoho-middleware && npm test ) && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and `git status --porcelain js/main.min.js js/admin.min.js js/brewpad.min.js` shows the artifacts changed (rebuilt from new sources) before commit.
    - `npm test` exits 0 with tests/frontend/brewpad-zoho-refresh.test.js and tests/frontend/admin-zoho-refresh.test.js both present in the run output.
    - `cd zoho-middleware && npm test` exits 0.
    - `npm run lint` exits 0.
    - `grep -c "customer-by-number" js/brewpad.min.js` returns a value >= 1 (the brewpad refresh fetch minified into js/brewpad.min.js — NOT js/main.min.js; the build concatenates only js/modules/01..13 into main.js/main.min.js, and brewpad.js is minified to its own separate js/brewpad.min.js artifact).
    - `grep -c "customer-by-number" js/admin.min.js` returns a value >= 1 (the admin refresh fetch minified into js/admin.min.js, the separate admin.js artifact — also not in main.min.js).
    - `git status --porcelain CNAME` is empty (CNAME never staged/committed; enforce-cname.yml manages the domain).
    - After the push, `git log origin/main..HEAD --oneline` is empty (the feature commit reached the staging remote).
  </acceptance_criteria>
  <done>All artifacts rebuilt from source; frontend + middleware suites and full lint pass; the customer-by-number fetch is present in BOTH js/brewpad.min.js and js/admin.min.js; the feature commit is pushed to the staging remote (origin/main).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The "Refresh from Zoho" feature on both batch detail surfaces — BrewPad detail pane and admin Batches modal. Each shows the linked Zoho number, Email/Phone rows, and a "Refresh from Zoho" button (hidden when the batch has no usable INV-/SO- link). Clicking it pulls the current customer name/email/phone from Zoho and updates the displayed rows in place, with distinct toasts for success / already-up-to-date / partial / voided-doc / not-found / Zoho-error.
    Build is green (frontend + middleware tests + lint) and the feature commit has already been pushed to staging via `git push origin main` in Task 1 (enforce-cname.yml has settled the domain). This checkpoint is on-device verification only.
  </what-built>
  <how-to-verify>
    On an iPad (Safari) against https://staging.steinsandvines.ca — success criterion #4 is explicitly iPad Safari with no console errors. Connect Safari Web Inspector (or watch the on-page behavior) and check the console is clean throughout.

    BrewPad surface (open the BrewPad admin, pick a batch):
    1. Open a batch that has a valid linked invoice (zoho_so_number like INV-000###). Confirm the detail pane shows a "Refresh from Zoho" button in the Invoice section plus Email and Phone rows.
    2. Tap "Refresh from Zoho". Expect the button to show a loading state, then a toast. If Zoho has newer/different customer info, the Customer/Email/Phone rows update WITHOUT the pane scrolling or fully re-rendering. If nothing changed, expect "Already up to date" and no write.
    3. Tap it again immediately — expect "Already up to date" (no-change short-circuit).
    4. Open a batch with NO linked invoice (or a WALK-IN / malformed link). Confirm there is NO "Refresh from Zoho" button (the existing "Link to Invoice" affordance is what shows). No erroring request fires (console stays clean).

    Admin Batches modal (admin panel -> Batches -> open a batch):
    5. Open a batch with a valid linked invoice. Confirm the info grid shows a "Zoho Ref" row with the number + a Refresh button, plus Email and Phone rows.
    6. Tap Refresh — expect the grid rows to update in place and a success toast. Re-tap -> "Already up to date".
    7. Open a batch with no link. Confirm the Zoho Ref row reads "Not linked" with NO Refresh button.
    8. (If a known voided/deleted invoice exists) refresh a batch linked to it and confirm a warning-style toast still applies the data.

    Throughout: confirm the browser console shows no errors on iPad Safari (criterion #4).
  </how-to-verify>
  <resume-signal>Type "approved" if both surfaces work and the console is clean on iPad Safari, or describe the issues (which surface, which step, console errors).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Correct REQUIREMENTS.md traceability</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the prematurely-ticked ZSYNC-01/02 entries at lines ~13-14 and the Traceability table ~36-37)
    - .planning/phases/29-refresh-from-zoho-admin-ui/29-CONTEXT.md <deferred> note about the premature [x]
  </read_first>
  <action>
    Now that the phase is verified on staging, REQUIREMENTS.md should reflect reality. The checklist entries for ZSYNC-01 and ZSYNC-02 (lines ~13-14) and the Traceability table rows for ZSYNC-01/ZSYNC-02 (mapped to Phase 29) already read "Complete"/`[x]`; confirm they are accurate now that verification passed and leave them ticked. Do NOT introduce any feature change here — this is a bookkeeping confirmation only. If the human-verify checkpoint surfaced any gap that was fixed, ensure the wording still matches what shipped. No other REQUIREMENTS.md edits.
    Do NOT push to production (v4.1 prod deploy is held until phases 27/27.1/28/29/29.1 ship together — STATE.md blocker). Staging push already happened in Task 1.
  </action>
  <verify>
    <automated>grep -n "ZSYNC-01\|ZSYNC-02" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` ZSYNC-01 and ZSYNC-02 entries and the Traceability rows read Complete and map to Phase 29.
    - No feature/source files changed by this task (`git status --porcelain` shows only .planning/REQUIREMENTS.md if anything).
  </acceptance_criteria>
  <done>REQUIREMENTS.md traceability accurately reflects the verified Phase 29 completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local source -> staging (GitHub Pages) | build artifacts deployed publicly via `git push origin main` |
| CNAME / domain pinning | wrong CNAME push could mis-route staging vs production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-D1 | Tampering | Hand-edited minified artifacts could diverge from source | mitigate | Only `npm run build` produces min files; never hand-edit; grep js/brewpad.min.js and js/admin.min.js for the new token to confirm it came from source |
| T-29-D2 | Repudiation/Deploy | Accidentally staging/committing CNAME or pushing to the wrong remote | mitigate | CNAME is gitignored — confirm `git status --porcelain CNAME` is empty before commit; push only to `origin` (staging); enforce-cname.yml auto-pins the domain after the push |
| T-29-D3 | Elevation | Premature production push while v4.1 batch is held | mitigate | This plan pushes to STAGING ONLY (`origin`); production (`production` remote) is explicitly held per STATE.md blocker until 27/27.1/28/29/29.1 ship together |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed (`npm run build`/`npm test` use existing deps); stop and add a legitimacy checkpoint if any install is attempted |
</threat_model>

<verification>
- `npm run build` + `npm test` + middleware `npm test` + `npm run lint` all green (task 1).
- The refresh fetch token present in js/brewpad.min.js AND js/admin.min.js (task 1 grep gates).
- Feature commit pushed to staging remote; `git log origin/main..HEAD` empty after push (task 1).
- Human-verify checkpoint passes on iPad Safari with a clean console on both surfaces (task 2).
- REQUIREMENTS.md traceability confirmed (task 3).
- Production NOT pushed (held per STATE.md v4.1 batch blocker).
</verification>

<success_criteria>
- Build artifacts regenerated from updated sources; full gate green; feature committed and pushed to staging.
- Refresh-from-Zoho verified working end-to-end on staging, iPad Safari, no console errors (ROADMAP criterion #4) for both surfaces, including the no-link hidden-button state (ZSYNC-02).
- REQUIREMENTS.md accurately reflects ZSYNC-01/02 completion.
</success_criteria>

<output>
Create `.planning/phases/29-refresh-from-zoho-admin-ui/29-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
