---
phase: 24-seo-staging-deploy
plan: 02
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - products/grains.html
  - products/yeast.html
  - products/additives.html
  - products/packaging.html
  - products/equipment.html
  - css/catalog-subpage.min.css
  - css/search-overlay.min.css
  - js/main.min.js
autonomous: false

requirements: [BUILD-02]

must_haves:
  truths:
    - "npm run build completes without errors and re-stamps all 5 subpages"
    - "npm test passes (frontend) so the deploy is not shipping a regression"
    - "The 5 subpages are pushed to the staging repo (origin/main)"
    - "All 5 subpages load on staging.steinsandvines.ca with zero console errors"
    - "Each loaded subpage shows its unique title, valid canonical/og tags, and LocalBusiness JSON-LD in the rendered head"
  artifacts:
    - path: "js/main.min.js"
      provides: "Re-minified build artifact after stamp"
    - path: "css/catalog-subpage.min.css"
      provides: "Re-minified subpage CSS after stamp"
  key_links:
    - from: "git push origin main"
      to: "staging.steinsandvines.ca"
      via: "GitHub Pages deploy of staging repo"
      pattern: "origin"
---

<objective>
Build, stamp, push to STAGING ONLY, and run a QA pass verifying all 5 SEO-enriched
subpages load on staging.steinsandvines.ca with no console errors and correct
rendered SEO heads.

Per the MANDATORY staging-first rule in CLAUDE.md, this plan deploys to staging
(`git push origin main`) only. Production deploy is a separate, human-approved
step and is explicitly OUT OF SCOPE for this phase (phase goal: "verified on
staging").

Purpose: Satisfy BUILD-02 success criterion 3 — subpages load correctly on
staging with no console errors.
Output: Built, stamped, staging-deployed subpages; a verified QA pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-seo-staging-deploy/24-01-PLAN.md

<build_notes>
- `npm run build` runs: stamp + stamp:admin/kiosk/brewpad/index/pages + minify:css + minify:js.
  The `stamp:pages` script ALREADY includes all 5 subpages
  (products/grains.html, products/yeast.html, products/additives.html,
  products/packaging.html, products/equipment.html), so it will re-stamp their
  `?v=` cache-bust query strings. No script edit needed.
- CLAUDE.md "Before Every Commit" (Non-Negotiable): run `npm test` (frontend) AND
  `cd zoho-middleware && npm test` AND `npm run lint` — all three are required gates,
  not optional. This plan touches only static HTML + build artifacts (no middleware
  source), so the middleware suite should already be green; running it is mandatory
  hygiene per the rule, and catches any pre-existing red before this commit rides on it.
- Deploy command for staging: `git push origin main`. NEVER `git push production`
  in this plan. CNAME is in .gitignore and auto-pinned by enforce-cname.yml — do
  not touch CNAME.
</build_notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, stamp, run tests, and push to staging</name>
  <files>products/grains.html, products/yeast.html, products/additives.html, products/packaging.html, products/equipment.html, css/catalog-subpage.min.css, css/search-overlay.min.css, js/main.min.js</files>
  <read_first>
    - package.json (the `build`, `stamp:pages`, `minify:css`, `minify:js`, and `test` scripts — confirm the 5 subpages are in stamp:pages before running)
    - CLAUDE.md (Non-Negotiable Rules: run tests before commit; staging deploy = `git push origin main`; never touch CNAME)
  </read_first>
  <action>
    From PROJECT_ROOT: run `npm run build` and confirm it exits 0 (stamps the 5 subpages and re-minifies CSS/JS). Then run the three mandatory pre-commit gates from CLAUDE.md and confirm ALL pass: `npm test` (frontend Jest), `npm run lint`, and `cd zoho-middleware && npm test` (then return to PROJECT_ROOT). Stage the 5 modified subpages plus regenerated build artifacts (css/*.min.css, js/main.min.js) with `git add`, commit with message `feat(24): add SEO meta + JSON-LD to 5 category subpages`, and push to STAGING with `git push origin main`. After pushing, fetch and confirm the new HEAD commit is reflected on `origin/main` (a reachable-remote check is not sufficient — verify the actual commit landed). Do NOT run `git push production`. Do NOT modify or commit CNAME. If the working tree contains unrelated changes, stage only the files this phase touched plus the build artifacts the build regenerated.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && npm run build && npm test && npm run lint && (cd zoho-middleware && npm test) && for f in grains yeast additives packaging equipment; do grep -q 'catalog-subpage.min.css?v=' products/$f.html || { echo "FAIL stamp $f"; exit 1; }; done && git log -1 --oneline | grep -qi seo && git fetch origin && [ "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" ] && echo BUILD_AND_PUSH_OK || { echo "FAIL: HEAD not reflected on origin/main"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0 (no frontend regression)
    - `npm run lint` exits 0 (CLAUDE.md pre-commit gate)
    - `cd zoho-middleware && npm test` exits 0 (CLAUDE.md pre-commit gate)
    - Each of the 5 subpages has a re-stamped `catalog-subpage.min.css?v=` query string after build
    - A commit referencing SEO exists as HEAD
    - After push, `git rev-parse HEAD` equals `git rev-parse origin/main` (the commit actually landed on staging); no `git push production` was run
    - CNAME file is unchanged (not staged, not committed)
  </acceptance_criteria>
  <done>Build and frontend tests pass; the 5 subpages and regenerated artifacts are committed and pushed to origin/main (staging) only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    SEO meta tags, og:* block, twitter:card, and LocalBusiness JSON-LD were added
    to all 5 category subpages and deployed to staging via `git push origin main`.
  </what-built>
  <how-to-verify>
    1. Wait ~1-2 min for GitHub Pages to rebuild the staging repo.
    2. In a browser with DevTools open (Console tab), visit each URL and confirm it
       loads with ZERO console errors:
       - https://staging.steinsandvines.ca/products/grains
       - https://staging.steinsandvines.ca/products/yeast
       - https://staging.steinsandvines.ca/products/additives
       - https://staging.steinsandvines.ca/products/packaging
       - https://staging.steinsandvines.ca/products/equipment
    3. On each page, View Source (or Elements) and confirm the head contains: a
       unique `<title>`, one `<meta name="description">`, the og:title/og:description/
       og:url/og:image/og:site_name tags, a `<link rel="canonical">` pointing to
       `https://steinsandvines.ca/products/{category}` (production domain, not staging),
       and a `LocalBusiness` JSON-LD `<script>`.
    4. Confirm the product grid still renders and the search overlay still opens
       (no regression from the head edits).
    5. Optional sanity: paste a page URL into a rich-results / OG debugger and
       confirm the title + description preview render.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 staging subpages load clean with correct SEO heads, or describe any console errors / missing tags found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> origin (staging GitHub Pages) | committed static files published to staging domain |
| build artifacts -> public | minified CSS/JS served to anonymous visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Elevation of Privilege | git push target | mitigate | Plan pushes to `origin` (staging) ONLY; production push is human-gated and out of scope. Acceptance criterion asserts no `git push production` ran. |
| T-24-04 | Tampering | CNAME / domain routing | mitigate | CNAME is in .gitignore and auto-pinned by enforce-cname.yml; task explicitly forbids staging or committing CNAME, preventing a wrong-domain publish. |
| T-24-05 | Information Disclosure | committed build artifacts | mitigate | Build only re-minifies existing public CSS/JS; no .env or credentials are staged. `git add` is scoped to the 5 subpages + regenerated min artifacts. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — `npm run build` and `npm test` use already-installed devDependencies. No supply-chain surface introduced. |
</threat_model>

<verification>
- `npm run build` and `npm test` both pass locally before push.
- Commit pushed to origin/main (staging) only; CNAME untouched; production not pushed.
- Human verifies all 5 staging subpages load with zero console errors and render the
  correct unique SEO head (title, description, og:*, canonical to production domain, JSON-LD).
</verification>

<success_criteria>
Satisfies BUILD-02 success criterion 3: all 5 subpages load correctly on
staging.steinsandvines.ca with no console errors. Combined with Plan 24-01,
the full phase goal — each subpage discoverable by search engines and the full
feature set verified on staging — is met.
</success_criteria>

<output>
Create `.planning/phases/24-seo-staging-deploy/24-02-SUMMARY.md` when done.
</output>
