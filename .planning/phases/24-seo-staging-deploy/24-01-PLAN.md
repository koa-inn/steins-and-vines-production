---
phase: 24-seo-staging-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - products/grains.html
  - products/yeast.html
  - products/additives.html
  - products/packaging.html
  - products/equipment.html
autonomous: true
requirements: [BUILD-02]

must_haves:
  truths:
    - "Each of the 5 subpages has a unique, Squamish-focused title tag"
    - "Each subpage has a meta description (150-160 chars, Squamish/Sea-to-Sky focused)"
    - "Each subpage has a complete og:* block (title, description, type, url, image, site_name)"
    - "Each subpage has a twitter:card meta tag"
    - "Each subpage has a self-referencing canonical pointing to steinsandvines.ca (not staging)"
    - "Each subpage carries a LocalBusiness JSON-LD block identical to the reference page"
  artifacts:
    - path: "products/grains.html"
      provides: "Full SEO head for Grains subpage"
      contains: '"@type": "LocalBusiness"'
    - path: "products/yeast.html"
      provides: "Full SEO head for Yeast subpage"
      contains: '"@type": "LocalBusiness"'
    - path: "products/additives.html"
      provides: "Full SEO head for Additives subpage"
      contains: '"@type": "LocalBusiness"'
    - path: "products/packaging.html"
      provides: "Full SEO head for Packaging subpage"
      contains: '"@type": "LocalBusiness"'
    - path: "products/equipment.html"
      provides: "Full SEO head for Equipment subpage"
      contains: '"@type": "LocalBusiness"'
  key_links:
    - from: "each subpage og:url and canonical"
      to: "https://steinsandvines.ca/products/{category}"
      via: "absolute production URL"
      pattern: "https://steinsandvines\\.ca/products/(grains|yeast|additives|packaging|equipment)"
---

<objective>
Add the complete SEO head block to each of the 5 new category subpages
(grains, yeast, additives, packaging, equipment) so they match the established
Apr-14 SEO pattern already present on `products/ingredients-supplies.html`.

Each subpage currently has only GTM, CSP, a generic `<title>`, and a correct
`<link rel="canonical">`. They are missing: `<meta name="description">`, the
full `og:*` block, `<meta name="twitter:card">`, and the `LocalBusiness`
JSON-LD `<script type="application/ld+json">`. Titles are generic and must be
made unique and Squamish-focused.

Purpose: Satisfy BUILD-02 — each subpage discoverable with unique SEO meta.
Output: 5 subpages with full, unique SEO heads matching the site convention.

NOTE FOR EXECUTOR: sitemap.xml already contains all 5 subpage URLs (added in
Phase 22) and all 5 subpages are already registered in the `stamp:pages` build
script. No sitemap or build-list change is required in this plan. Verify the
sitemap entries are present (acceptance criterion below) but do not re-add them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<reference>
The source-of-truth SEO head shape lives in `products/ingredients-supplies.html`
(head block, roughly lines 22-66). Replicate its exact structure for each subpage:

- `<meta name="description" content="...">` — 150-160 chars, Squamish/Sea-to-Sky
  focused, category-specific.
- `<meta property="og:title">`, `og:description`, `og:type` (= "website"),
  `og:url` (absolute production URL), `og:image`
  (= "https://steinsandvines.ca/images/icon-512.png"),
  `og:site_name` (= "Steins &amp; Vines").
- `<meta name="twitter:card" content="summary">`.
- `<title>` — unique per category, form: `{category phrase} in Squamish | Steins &amp; Vines`.
- `<script type="application/ld+json">` LocalBusiness block — copy VERBATIM from the
  reference (same name, telephone, priceRange "$$", address, geo, openingHours,
  image, sameAs). This block is identical across all pages by design.

Placement: insert the description / og / twitter tags AFTER the CSP
`<meta http-equiv="Content-Security-Policy">` line and BEFORE the existing
`<title>`. The canonical is already present and correct (leave it). Rewrite the
existing generic `<title>` to the unique form. Insert the JSON-LD `<script>`
immediately AFTER the `<title>`, matching the reference order.

Path note: subpages live in `products/` so asset paths use a `../` prefix, but
og:url / canonical / JSON-LD URLs are ABSOLUTE production URLs
(https://steinsandvines.ca/...). Never emit a staging URL into these files.
</reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add full SEO head to grains, yeast, and additives subpages</name>
  <files>products/grains.html, products/yeast.html, products/additives.html</files>
  <read_first>
    - products/ingredients-supplies.html (reference SEO head — copy exact shape of description, og:*, twitter:card, title form, and the LocalBusiness JSON-LD block verbatim)
    - products/grains.html (target — generic title + canonical only)
    - products/yeast.html (target)
    - products/additives.html (target)
  </read_first>
  <action>
    For each of the 3 files, insert SEO meta tags after the CSP `meta http-equiv="Content-Security-Policy"` line and before the existing `title`: `meta name="description"`, `meta property="og:title"`, `og:description`, `og:type` (="website"), `og:url`, `og:image` (="https://steinsandvines.ca/images/icon-512.png"), `og:site_name` (="Steins &amp; Vines"), and `meta name="twitter:card"` (="summary").
    Rewrite each generic `title` to a unique Squamish-focused phrase. Per-category guidance (description 150-160 chars):
    grains -> title "Grains &amp; Malt for Brewing in Squamish | Steins &amp; Vines"; description covers base and specialty grains plus malt extract for home brewers in Squamish / the Sea-to-Sky corridor.
    yeast -> title "Brewing &amp; Wine Yeast in Squamish | Steins &amp; Vines"; description covers ale, lager, and wine yeast strains plus nutrient.
    additives -> title "Brewing Additives &amp; Flavorings in Squamish | Steins &amp; Vines"; description covers additives, flavorings, fruit, and oak.
    Set og:url to absolute `https://steinsandvines.ca/products/{category}` (NOT staging). Leave the existing correct canonical untouched.
    Immediately after the rewritten `title`, insert the LocalBusiness JSON-LD `script type="application/ld+json"` copied VERBATIM from products/ingredients-supplies.html (do not alter name, telephone, priceRange, address, geo, openingHours, image, or sameAs).
    Do NOT touch GTM, CSP, stylesheet links, or the body.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && for f in grains yeast additives; do test "$(grep -c 'name="description"' products/$f.html)" = "1" && test "$(grep -c 'property="og:url"' products/$f.html)" = "1" && test "$(grep -c '"@type": "LocalBusiness"' products/$f.html)" = "1" && test "$(grep -c 'name="twitter:card"' products/$f.html)" = "1" || { echo "FAIL meta $f"; exit 1; }; grep -q "href=\"https://steinsandvines.ca/products/$f\"" products/$f.html || { echo "FAIL canonical $f"; exit 1; }; grep -qi staging products/$f.html && { echo "FAIL staging leaked $f"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'name="description"' on each of grains/yeast/additives == 1
    - grep -c 'property="og:title"' / 'og:description' / 'og:url' / 'og:image' / 'og:site_name' on each == 1
    - grep -c 'name="twitter:card"' on each == 1
    - grep -c '"@type": "LocalBusiness"' on each == 1
    - Each file contains canonical href "https://steinsandvines.ca/products/{category}"
    - og:url value equals the canonical value on each page
    - No occurrence of the string "staging" in any of the 3 files
    - The three `<title>` strings are distinct from each other and from the other two subpages
  </acceptance_criteria>
  <done>grains.html, yeast.html, additives.html each carry the full SEO head (unique title, description, og:* block, twitter:card, LocalBusiness JSON-LD) with production canonical/og:url and no staging URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Add full SEO head to packaging and equipment subpages</name>
  <files>products/packaging.html, products/equipment.html</files>
  <read_first>
    - products/ingredients-supplies.html (reference SEO head)
    - products/packaging.html (target — generic title + canonical only)
    - products/equipment.html (target)
  </read_first>
  <action>
    Apply the identical SEO-head insertion described in Task 1 to packaging.html and equipment.html. Per-category guidance (description 150-160 chars):
    packaging -> title "Bottling &amp; Packaging Supplies in Squamish | Steins &amp; Vines"; description covers bottles, bags, and closures for finishing your wine or beer.
    equipment -> title "Brewing &amp; Winemaking Equipment in Squamish | Steins &amp; Vines"; description covers fermenters, tubing, and brewing equipment.
    Set og:url to absolute `https://steinsandvines.ca/products/{category}`. Insert the same VERBATIM LocalBusiness JSON-LD block after the rewritten `title`. Leave existing canonical, GTM, CSP, stylesheet links, and body untouched.
  </action>
  <verify>
    <automated>cd "$PROJECT_ROOT" && for f in packaging equipment; do test "$(grep -c 'name="description"' products/$f.html)" = "1" && test "$(grep -c 'property="og:url"' products/$f.html)" = "1" && test "$(grep -c '"@type": "LocalBusiness"' products/$f.html)" = "1" && test "$(grep -c 'name="twitter:card"' products/$f.html)" = "1" || { echo "FAIL meta $f"; exit 1; }; grep -q "href=\"https://steinsandvines.ca/products/$f\"" products/$f.html || { echo "FAIL canonical $f"; exit 1; }; grep -qi staging products/$f.html && { echo "FAIL staging leaked $f"; exit 1; }; done; echo "===unique titles check==="; cd "$PROJECT_ROOT" && grep -h '<title>' products/grains.html products/yeast.html products/additives.html products/packaging.html products/equipment.html | sort | uniq -d | grep . && { echo "FAIL duplicate title"; exit 1; }; echo OK; echo "===sitemap entries present (Phase 22, do not modify)==="; for u in grains yeast additives packaging equipment; do grep -q "https://steinsandvines.ca/products/$u" sitemap.xml || { echo "FAIL sitemap missing $u"; exit 1; }; done; echo SITEMAP_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'name="description"' on packaging and equipment == 1 each
    - Full og:* block (5 tags) and twitter:card present on each == 1
    - grep -c '"@type": "LocalBusiness"' on each == 1
    - Each contains canonical "https://steinsandvines.ca/products/{category}"; og:url matches
    - No "staging" string in either file
    - All 5 subpage `<title>` tags are mutually unique (uniq -d finds no duplicates)
    - sitemap.xml already contains `<loc>` entries for all 5 subpages (verified, not modified)
  </acceptance_criteria>
  <done>packaging.html and equipment.html carry the full SEO head; all 5 subpage titles are unique; sitemap entries confirmed present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed HTML -> public GitHub Pages | static meta/JSON-LD served to anonymous crawlers and visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | committed subpage HTML / JSON-LD | mitigate | No secrets, API keys, or PII added — only public business info already present site-wide (name, public phone, address). Acceptance criteria forbid the "staging" string; reviewer confirms only the established LocalBusiness fields are copied verbatim. |
| T-24-02 | Tampering | og:url / canonical URLs | mitigate | All og:url and canonical values are pinned to the absolute production domain `https://steinsandvines.ca`; verify step greps for and rejects any "staging" URL leaking into production-bound files. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure static HTML edits. No supply-chain surface introduced. |
</threat_model>

<verification>
- All 5 subpages each contain exactly one meta description, full og:* block, twitter:card, and LocalBusiness JSON-LD.
- Canonical and og:url on every subpage point to the production domain; no "staging" string present.
- All 5 `<title>` tags are mutually unique and Squamish-focused.
- sitemap.xml entries for all 5 subpages confirmed present (not modified — Phase 22 added them).
</verification>

<success_criteria>
Satisfies BUILD-02 success criteria 1 and 2: each subpage has a unique title,
meta description, og:title/og:description, canonical, and LocalBusiness JSON-LD;
sitemap.xml includes all 5 subpages. (Criterion 3 — staging load with no console
errors — is verified in Plan 24-02 after the build + deploy.)
</success_criteria>

<output>
Create `.planning/phases/24-seo-staging-deploy/24-01-SUMMARY.md` when done.
</output>
