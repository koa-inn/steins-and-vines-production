---
context: default
phase: v4.4-audit-remediation (between phases)
task: null
total_tasks: 5
status: paused
last_updated: 2026-06-26T22:44:20.239Z
---

# Resume Point — v4.4 Audit Remediation (+ new feature)

## Critical Anti-Patterns (do NOT repeat)

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| sed on .md tables | `sed -i 's|...|...|'` with `\|` delimiter corrupted `REQUIREMENTS.md` (pipes in the table row). Restored via `git checkout`. | advisory | Use the Edit tool for markdown table edits, never sed with `\|` delimiter. |
| kiosk fork blind spot | A kiosk fix in `kiosk.js` does NOT cover the in-store kiosk, which is `admin.html?tab=kiosk` (`admin.js`, forked #14). The customer-search 403 needed fixing in BOTH. | blocking | Any kiosk change (incl. the new custom-item feature) must touch `kiosk.js` AND `admin.js` until Phase 42 de-forks them. |

<current_state>
Mid-milestone, between phases. v4.4 (Audit Remediation): 38 deferred, 39 done (staging), 40 done (LIVE prod), 41 done (staging). Phase 42 (kiosk de-fork) NOT started — awaiting owner review. A NEW owner feature request (kiosk manual/custom line item) is queued and design-approved. All committed + pushed to `origin/main`. Working tree clean except runtime scratch.
</current_state>

<completed_work>
- P38 HYGIENE-01 — DEFERRED (owner)
- P39 DEPLOY-04 snapshot publish — DONE (staging, 303a060)
- P40 ASSET-01 facility webp/srcset — DONE (LIVE prod, 17f9995)
- P41 CART-01 SKU-keyed cart — DONE (staging, b2fdac1; 928 FE tests green)
- PROD hotfix: kiosk customer-search x-api-key — DONE (LIVE prod, 50465bc; kiosk.js x3 + admin.js x2)
- (earlier) kiosk product-type discounts + edit/toggle — LIVE prod
</completed_work>

<remaining_work>
1. NEW FEATURE — kiosk manual/custom line item with notes (design approved; see next_action)
2. Phase 42 — KIOSK-01 kiosk de-fork into `js/kiosk-core.js` (HIGH RISK; owner review first)
</remaining_work>

<decisions_made>
- Cart keyed by `getProductKey()` = SKU primary, name|brand fallback (derived at lookup, not persisted)
- 12-checkout: kept zoho_item_id-primary ternaries; milling-grain keys (1051/1087) untouched
- P38 deferred (gitignore would break GSD; prod already strips, staging exposure low-sev)
- Custom line item → Zoho ad-hoc invoice line + note in line description
</decisions_made>

<blockers>
None. (Non-blocking human actions: hard-refresh iPad kiosks; decide staging→prod promotion of 39/41; confirm beer licence.)
</blockers>

## Infrastructure State
- `origin/main` (staging) is AHEAD of prod: Phases 39 & 41 + planning are staging-only. Prod has 40 + customer-search fix + discount feature + images.
- BEER_SALES_ENABLED = true in prod (kiosk recipe sales active).
- Middleware: single Railway instance (prod), already current. Deploy via `railway up` from repo root.
- Prod deploy = `gated-deploy.yml` (workflow_dispatch) — handles CNAME swap, tests, tag, runbook.

## Required Reading (in order)
1. `PROJECT_ASSESSMENT.md` — the audit driving v4.4 (items #6/#10/#14/#15/#18)
2. `.planning/ROADMAP.md` "Phase Details (v4.4)" + `.planning/REQUIREMENTS.md`
3. memory `kiosk_contacts_apikey.md` — the kiosk fork + x-api-key gotcha

<context>
v4.4 milestone built via GSD; small phases executed inline (no per-phase PLAN dirs). The kiosk fork (#14) is the recurring pain — fixes/features must hit kiosk.js AND admin.js until Phase 42. Money path hardened in v4.2; the new custom-item path + Phase 42 must not weaken it.
</context>

<next_action>
Start with: the kiosk manual/custom line-item feature. (1) Read `zoho-middleware/routes/pos.js` — `/api/kiosk/sale` rejects any line item whose item_id is not in the catalog cache; add a "custom item" path that accepts a line with no catalog item_id, trusts the staff-entered price (with sane bounds), and records the note in the Zoho invoice line description. (2) Build the kiosk UI (description + price + qty + note) in BOTH `js/kiosk.js` and `js/admin.js`. Regression-test the server path. OR, if owner prefers, start Phase 42 (kiosk de-fork) first.
</next_action>
