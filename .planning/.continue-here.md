---
context: default
phase: null (incident/ops thread — PARALLEL to active Phase 44)
task: null
total_tasks: 6
status: paused
last_updated: 2026-06-29T21:17:20.568Z
---

# ⚠️ READ FIRST — this is a separate thread

This handoff covers an **incident-response / ops session**, **not** GSD phase work.
A **different session is actively working Phase 44 (kiosk gift-card lifecycle)** in
another window and has **uncommitted** changes in the tree (`STATE.md`, `ROADMAP.md`,
`SECURITY.md`). **Do not disturb Phase 44 files or its uncommitted work.** If you meant
to resume Phase 44, see `.planning/phases/44-*` and recent `git log` — not this file.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Accepting the behavioral explanation | The customer said "his phone died," and I initially ran with it. The real cause was a server-side validation 400 *after* the HelcimPay iframe charged the card. | advisory | For any money-path incident, pull `railway logs` (`sv-logs`) and find the actual `/api/checkout` status before theorizing. |

<current_state>
Incident fully handled and committed; both middleware fixes are LIVE on prod (verified).
Remaining work is follow-ups, one of which is blocked on a user-provided Helcim export.
All of this thread's code is committed — nothing of mine is uncommitted.
</current_state>

<completed_work>

- **Reconciled the orphaned charge** — customer **Jacob MacDonald** (CUS-00151), Helcim txn **50641064**, **$125.52**. Created **INV-000118** (9 ingredient lines + Milling Fee + a **$5.48** entity discount to match the exact charge) + customer **payment #119**; added `thornfull@gmail.com` to the contact. Invoice paid, balance $0.
- **Root-cause fix** (`fa1e649`) — `checkout.js` rejected sub-1kg weight lines (`vQty < 1`) → 400 after the card was charged → orphan. Changed to `vQty <= 0` (matches `pos.js`). Regression test added. Deployed (`railway up`), verified live.
- **Hardening** (`8562ff7`) — `rejectWithVoid()`: every early `/api/checkout` 400 now voids the already-charged payment. Tests added. Deployed (`railway up`), verified live.
- **Filed #107** — incident + remaining hardening + audit (priority:critical).
- **Issue triage** — closed **33** stale/done/duplicate issues with evidence comments (**98 → 65**). Real live set ≈ 8.
- **Tooling** (`1b43106`) — Obsidian vault at repo root (`.obsidian/` + `notes/` gitignored; `notes/START-HERE.md`); zsh shortcuts `~/.sv-commands.zsh` (type `sv-help`); Warp Workflows written but free-tier-gated → using zsh.
</completed_work>

<remaining_work>

- **Orphan audit (Apr→Jun 2026)** — BLOCKED on the user's Helcim export. Cross-match online Helcim charges vs Zoho invoices (`reference_number` = txn id); other customers likely hit by the same bug.
- **#107 webhook reconciliation backstop** — not started. The real safety net (auto-flag online charges with no matching Zoho order).
- **#2** dual API-key + key-in-URL — ~30 min quick fix (deferred by user).
- **#10** PII in logs — 3 `customerEmail` log refs remain.
- **#1** session-auth the PII routes (`/api/orders/recent`) — kept OPEN intentionally.
</remaining_work>

<decisions_made>

- Reconcile in Zoho (not refund); $5.48 discount trues the invoice to the exact $125.52 charged.
- Keep #1/#2 OPEN — real PII exposure + query-param key leak, verified still in code (not by-design).
- zsh aliases over Warp Workflows (Warp gated on free tier; zsh is portable + cloud-free).
</decisions_made>

<blockers>
- Orphan audit: needs Helcim online-transactions export (only authoritative source). — human action
- Prod startup: Resend email transport `verify()` FAILED ("API key restricted to only send emails") → void-failure **alerts** may not send. Verify transactional email actually works. — technical
</blockers>

## Infrastructure State
- Prod middleware redeployed twice today via `railway up`; both fixes live + verified (latest host `b81d8726c8ff`).
- Local `main` is ahead of `origin` (shared with the Phase 44 session); this thread's commits: `fa1e649`, `8562ff7`, `1b43106`.
- Obsidian vault = repo root, `node_modules` excluded in Warp/Obsidian settings.

<context>
The incident is closed and shipped. What's left is a blocked audit (waiting on you) plus
deferred security quick-wins. Nothing here is mid-edit. Keep clear of the Phase 44 session.
</context>

<next_action>
Start with: if the Helcim export is available, run the orphan-charge audit (cross-match
online charges → Zoho by reference_number). Otherwise, do the #2 quick fix (+#10) on
staging, or start the #107 webhook reconciliation backstop.
</next_action>
