---
context: default
phase: 71-kiosk-so-collect-reconciliation (waves 1 merged; code-review fix pending)
status: in_progress
last_updated: 2026-08-14
head_note: run `git log --oneline -8` to orient
---

# Continue Here — resume after /clear (2026-08-14)

## ⛳ IMMEDIATE NEXT ACTION — deploy to staging, then owner runs the 71-03 live-verify
The Phase 71 code-review fixes are **DONE and committed** (`3525aee7`, tests green **1400 middleware**). Remaining work on the phase is the **71-03 blocking-human live-verify** (a real kiosk SO collection on staging → PAID invoice). That needs the middleware deployed to staging and the owner at the kiosk.

**What was fixed (commit 3525aee7):**
- **BL-01 (critical):** collect APPROVED apply now wrapped in a per-transaction lock — `reconcile:collect:txn:<txn>` (deliberately NOT the shared `reconcile:txn:` key: the concurrent `reconcilePendingCharge(transactionId)` contends on that same key for the same txn and could starve the apply into never booking the payment). Acquire→work→release-only-on-acquired; skip-without-release on contention.
- **WR-01:** `cache.del(pendingKey)` is now best-effort in its own swallowed continuation — a Redis blip after a booked payment no longer masquerades as a "failed after charge" alert.
- **WR-03:** amount_applied clamped to the invoice's read-back `balance_due` (GET /invoices/{id}), not the stale `ctx.amount`; >1c divergence logged.
- **IN-01:** invoice_id surfaced onto the fail-closed reconcile-failure sentinel.
- **WR-02 / IN-02:** deferred (WR-02 → 71-03 live-verify; IN-02 cosmetic).

**Full findings + resolution:** `.planning/phases/71-.../71-REVIEW.md` (status: fixes_applied).

**To continue:** deploy to staging (`git push origin main` → Railway staging — a server/webhook-only change, no client/HTML touched) then have the owner run the **71-03 live-verify** per `71-03-PLAN.md` (create a small test SO, collect on the terminal, confirm the invoice ends PAID / balance $0 / exactly one invoice / payment applied with unused_amount 0 / Helcim txn on the payment reference). On "approved", write `71-03-SUMMARY.md` and the phase is complete. **Do NOT force-push to production until staging is approved.**

## Broader open threads (this multi-day session)
- **Live verifications pending** (owner, at the kiosk / authenticated BrewPad): phases **64, 67, 68, 69, 70** — each has a `*-HUMAN-UAT.md` in its phase dir.
- **Phase 70 (kiosk cash + phone-order card)** is on **staging only**; its **prod push is gated on the staging CSP live-verify** (`70-HUMAN-UAT.md` test #1).
- **Legal policy drafts** — review-ready first pass in `docs/legal-drafts/` + an artifact review page. Second pass is waiting on owner answers to the 6-question decision cluster (ship-vs-pickup, cancellation/deposit/started-batch, return terms, equipment warranty, 19+ check, cookie banner).
- **SO-collect historical data cleanup** — owner-gated Zoho audit + repair (draft `SO-` invoices with unapplied payments), separate from the phase 71 code fix. Read-only audit first, per-record confirm.
- Latent, out-of-scope, recorded: DECLINED-path idem-key mismatch (`ctx.idempotencyKey` vs stored `idempotency_key`) in the collect flow.

## How to orient in the fresh session
`/gsd-progress` for the roadmap · `git log --oneline -12` for recent commits · this file for the immediate action.
