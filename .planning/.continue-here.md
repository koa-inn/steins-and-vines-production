---
context: default
phase: 71-kiosk-so-collect-reconciliation (waves 1 merged; code-review fix pending)
status: in_progress
last_updated: 2026-08-14
head_note: run `git log --oneline -8` to orient
---

# Continue Here — resume after /clear (2026-08-14)

## ⛳ IMMEDIATE NEXT ACTION — apply the Phase 71 code-review fixes
Phase 71 (kiosk SO-collect reconciliation) plans **71-01 (core fix)** and **71-02 (dead-code cleanup)** are **committed and merged to main, tests green (1393 middleware)**. Then a code review found a **MONEY-PATH CRITICAL that is NOT yet fixed**:

- **BL-01 (critical):** the rewritten collect APPROVED path in `zoho-middleware/routes/webhooks.js` (~198-234) applies the payment with **no idempotency lock**, so two overlapping Helcim webhook deliveries (at-least-once) could double-book a `/customerpayments`. The terminal path guards this with `cache.acquireLock('reconcile:txn:' + transactionId, 60)` (see `reconcile.js:206` and `voidCancelledApprovedCharge` ~webhooks.js:313) — the collect path must adopt the SAME lock (acquire before work; skip-without-release on contention; release only on the acquired path).
- **WR-01:** a `cache.del(pendingKey)` failure AFTER a successful payment must NOT fire the "failed after charge" reconcile alert nor leave a re-delivery double-apply.
- **WR-03:** apply against the invoice's actual `balance_due` (read back), not the stale `ctx.amount`, so a prior deposit / tax rounding can't leave a silently partial-paid invoice.
- **WR-02:** DEFER — covered by the 71-03 live-verify checkpoint (the dedup relies on an assumed `salesorder.invoices[].invoice_id` shape, mock-tested only).

**Full findings:** `.planning/phases/71-kiosk-so-collect-reconciliation-finalize-invoice-sent-open-a/71-REVIEW.md` (committed, status: issues_found).

**To resume the fix:** `/gsd-code-review 71 --fix` (reads 71-REVIEW.md, applies fixes) — OR just tell the session "apply the phase 71 code-review fixes (BL-01 lock + WR-01 + WR-03; defer WR-02)". Then continue Phase 71: run `/gsd-execute-phase 71` (completed plans are skipped via their SUMMARYs) → it proceeds to phase verify + the **71-03 live-verify checkpoint** (a real kiosk SO collection reconciling to a PAID invoice on staging — human gate).

## Broader open threads (this multi-day session)
- **Live verifications pending** (owner, at the kiosk / authenticated BrewPad): phases **64, 67, 68, 69, 70** — each has a `*-HUMAN-UAT.md` in its phase dir.
- **Phase 70 (kiosk cash + phone-order card)** is on **staging only**; its **prod push is gated on the staging CSP live-verify** (`70-HUMAN-UAT.md` test #1).
- **Legal policy drafts** — review-ready first pass in `docs/legal-drafts/` + an artifact review page. Second pass is waiting on owner answers to the 6-question decision cluster (ship-vs-pickup, cancellation/deposit/started-batch, return terms, equipment warranty, 19+ check, cookie banner).
- **SO-collect historical data cleanup** — owner-gated Zoho audit + repair (draft `SO-` invoices with unapplied payments), separate from the phase 71 code fix. Read-only audit first, per-record confirm.
- Latent, out-of-scope, recorded: DECLINED-path idem-key mismatch (`ctx.idempotencyKey` vs stored `idempotency_key`) in the collect flow.

## How to orient in the fresh session
`/gsd-progress` for the roadmap · `git log --oneline -12` for recent commits · this file for the immediate action.
