{
  "version": "1.0",
  "timestamp": "2026-06-26T22:44:20.239Z",
  "phase": "v4.4",
  "phase_name": "Audit Remediation",
  "phase_dir": ".planning (between phases — 39/40/41 executed inline, no per-phase PLAN dirs)",
  "plan": null,
  "task": null,
  "total_tasks": 5,
  "status": "paused",
  "completed_tasks": [
    {"id": "P38", "name": "HYGIENE-01 repo hygiene", "status": "deferred", "note": "owner deferred; prod already strips .planning, staging exposes it (legacy Pages), gitignore would break GSD"},
    {"id": "P39", "name": "DEPLOY-04 nightly snapshot publishes", "status": "done", "commit": "303a060", "where": "staging"},
    {"id": "P40", "name": "ASSET-01 facility image webp/srcset", "status": "done", "commit": "17f9995", "where": "LIVE ON PROD"},
    {"id": "P41", "name": "CART-01 SKU-keyed cart identity", "status": "done", "commit": "b2fdac1", "where": "staging", "note": "getProductKey() centralized across 11-cart + 06/07/08/12/15/16/17; 928 FE tests green"},
    {"id": "PROD-HOTFIX", "name": "kiosk customer-search 403 (x-api-key on /api/contacts)", "status": "done", "commit": "50465bc", "where": "LIVE ON PROD via gated-deploy", "note": "fixed kiosk.js x3 + admin.js x2 (forked)"}
  ],
  "remaining_tasks": [
    {"id": "FEATURE", "name": "Kiosk manual/custom line item with notes", "status": "not_started", "note": "NEW owner request. Design approved: Zoho ad-hoc invoice line (name+rate+qty) + note in line description; server needs a custom-item path (generic /api/kiosk/sale rejects non-catalog item_ids); trust staff-entered price w/ bounds; confirm tax handling; build kiosk.js AND admin.js (forked)."},
    {"id": "P42", "name": "KIOSK-01 kiosk POS de-fork into js/kiosk-core.js", "status": "not_started", "note": "HIGH RISK money path; awaiting owner review before starting; behaviour-preserving + parity tests required"}
  ],
  "blockers": [],
  "human_actions_pending": [
    {"action": "Hard-refresh each iPad kiosk (admin.html?tab=kiosk) once", "context": "drop stale admin.min.js so the customer-search fix + discounts take effect", "blocking": false},
    {"action": "Decide when to promote staging->prod", "context": "Phases 39 & 41 + planning are on staging only; prod has 40 + customer-search fix + discount feature + images", "blocking": false},
    {"action": "Confirm licence supports live beer sales", "context": "BEER_SALES_ENABLED is true in prod; kiosk recipe sales active", "blocking": false}
  ],
  "decisions": [
    {"decision": "Phase 41 keys cart lines by getProductKey() = SKU primary, name|brand fallback", "rationale": "SKU is consistent across surfaces; catalog populated brand but search overlay left it blank -> duplicate lines. Keys derived at lookup time (not persisted) so stored carts don't orphan.", "phase": "41"},
    {"decision": "Kept zoho_item_id-primary ternaries in 12-checkout (717/1226); only routed name|brand comparisons through getProductKey; left milling-grain keys (1051/1087) untouched", "rationale": "zoho_item_id already deduped correctly; avoid destabilizing the milling feature / 11<->12 circular state", "phase": "41"},
    {"decision": "Phase 38 deferred not gitignored", "rationale": "GSD tracks state by committing .planning; D-01 keeps it tracked + strips at prod deploy. Staging exposure low severity (noindex; PROJECT_ASSESSMENT.md already gitignored).", "phase": "38"},
    {"decision": "Custom kiosk line item = Zoho ad-hoc invoice line + note in description", "rationale": "owner-approved; simplest reconcile-later path", "phase": "FEATURE"}
  ],
  "uncommitted_files": ["zoho-middleware/ingredients-cache.json (runtime cache — ignore)", "zoho-middleware/dump.rdb + .planning/ui-reviews/ (untracked runtime/scratch)"],
  "next_action": "Start the kiosk manual/custom line-item feature (design approved). Investigate the /api/kiosk/sale catalog-validation path in zoho-middleware/routes/pos.js (it rejects non-catalog item_ids) and add a custom-item path; build the kiosk UI in kiosk.js AND admin.js. OR start Phase 42 de-fork if owner prefers.",
  "context_notes": "Long session. v4.4 audit-remediation milestone created via GSD; executed phases inline (no per-phase PLAN dirs) because they were small/well-specified. Everything pushed to origin/main (HEAD ~277c368). Staging is AHEAD of prod (39/41 not promoted). The kiosk fork (#14) is why the customer-search 403 needed fixing in 2 files and why the new feature must touch both kiosk.js+admin.js — Phase 42 de-fork would end this. Money path was hardened in v4.2; custom line item + Phase 42 must not weaken it. CAUTION: do NOT use sed with '|' delimiter on REQUIREMENTS.md tables — it corrupted the file once (restore via git checkout). Use the Edit tool for .md table edits."
}
