{
  "version": "1.0",
  "timestamp": "2026-07-10T01:28:53.366Z",
  "phase": "54",
  "phase_name": "gift-card-management-on-the-kiosk-surface",
  "phase_dir": ".planning/phases/54-gift-card-management-on-the-kiosk-surface",
  "status": "paused",
  "session_theme": "Phase 54 gift-card mgmt shipped + verified + secured to staging; then a string of kiosk fixes (Back-button, stock-refresh bust, Clear-customer) + Metricool CSP; discovered a LIVE prod bug (products blank after first sale); paused before the iPad UAT.",
  "completed_this_session": [
    {"item": "Phase 54 — kiosk gift-card management (lookup+void), full GSD loop", "detail": "verified 10/11 + owner override on D-54-01 placement; secured 5/5 (54-SECURITY.md); D-54-GC adds gift-card/void to kiosk device-token scope, supersedes D-46-02/T-46-07"},
    {"item": "Kiosk Device-Settings Back button (escape the token-screen trap)", "commit": "be47e55"},
    {"item": "Metricool CSP allowlist on 15 public GTM pages (tag added via GTM by owner)", "commit": "23633ac"},
    {"item": "RUNBOOK: Pending Production Promotions + Metricool CSP<->GTM publish ordering", "commit": "35f8d69"},
    {"item": "FIX 1: allow 'device' tier on /api/kiosk/products?bust=1 (routes/catalog.js:808) + test", "commit": "0d9fe73"},
    {"item": "FIX 2: Clear button on kiosk cart-view customer row (reset to Walk-in)", "commit": "05800a4"}
  ],
  "blockers": [
    {"description": "LIVE PROD BUG: kiosk product grid blanks after the first sale. After a sale, kioskLoadProducts(true) -> GET /api/kiosk/products?bust=1 under the DEVICE TOKEN, which the M7 (Phase 52-05) bust gate refused (['legacy','session'] only) -> 403. Started with the Phase 46 auth cutover (2026-07-08). Affects the REAL production kiosk + staging.", "type": "technical", "workaround": "Reload the page (a non-bust load returns 200 and repopulates)."}
  ],
  "human_actions_pending": [
    {"action": "EXPEDITE Fix 1 (0d9fe73) to the PRODUCTION middleware", "context": "Fix 1 is a middleware change; the middleware runs ONLY on the prod Railway instance (deployed from the production repo). There is NO staging middleware. So Fix 1 does not take effect until the prod middleware redeploys. The live store kiosk is degraded now (blank/stale products after every sale). Deploy it standalone rather than waiting for the full 48/54 cutover.", "blocking": true},
    {"action": "Run the iPad UAT (standalone kiosk), then paste results back", "context": "Run-sheet: https://claude.ai/code/artifact/8d0c5cad-f309-41fd-a61e-19a3030ac665 . Open staging.steinsandvines.ca/kiosk.html in SAFARI (not the Home Screen prod icon); paste device token once. Real money (calls prod terminal/Zoho): refund card, throwaway cert, cleanup. NOTE: the blank-products bug will bite during UAT until Fix 1 hits prod middleware — reload to recover. Then /gsd:verify-work 48 and 54.", "blocking": false},
    {"action": "Metricool: add the Custom HTML tag in GTM (GTM-NHRCGLC5)", "context": "Test in GTM Preview against staging first; PUBLISH only after the prod CSP is live (RUNBOOK 'Pending Production Promotions' Stage-3 checklist). Publishing early = CSP-blocked on prod (harmless console error).", "blocking": false}
  ],
  "decisions": [
    {"decision": "D-54-GC: kiosk device token may void gift certs", "rationale": "owner-accepted, supersedes D-46-02/T-46-07; void is status-only, reason-required, logged"},
    {"decision": "D-54-01 placement OVERRIDDEN: Gift Cards button on the always-visible shell bar (not a gated settings area)", "rationale": "opening the panel is a harmless lookup; void stays multi-step gated; matches 'everything on the kiosk' intent"},
    {"decision": "Fix 1 to prod middleware should be EXPEDITED standalone", "rationale": "live daily-ops bug; isolated + low-risk; don't wait for the full 48/54 cutover"}
  ],
  "next_feature_captured": {
    "name": "Phase 55 candidate — gift-card searchable list on the kiosk panel (so staff need not remember cert IDs)",
    "design": "Certs stay BEARER by default (give to anyone). OPTIONALLY attach a customer at sale/issue time; if attached, the customer name shows in the list and is searchable by name; scroll+tap also works for anonymous certs.",
    "scope": "Apps Script schema change (add customer field to GiftCards sheet — MANUAL redeploy) + capture optional customer in the issue flow + new kiosk-scoped gift-card LIST/search endpoint (get_gift_cards exists in Apps Script but is admin-only) + kgcm- panel list/search UI.",
    "security": "list of certs + customer names + balances = bulk PII on the device token — owner-accept like D-54-GC.",
    "routing": "GSD follow-up phase to 54 (discuss->plan->execute). Owner leaning: park until after the UAT."
  },
  "uncommitted_files": [],
  "unpushed_commits_to_origin": 0,
  "next_action": "Fix the frontend resilience gap: js/kiosk-core.js kioskLoadProducts() (~line 789-802) must check r.ok before reading r.json(); on a failed response DO NOT wipe _kioskProducts and DO NOT set _kioskProductsLoaded=true (keep showing current products). Same shape for kioskLoadRecipes(). THEN coordinate expediting Fix 1 (0d9fe73) to the prod middleware. THEN the owner runs the iPad UAT.",
  "context_notes": "Everything above is on origin/main (staging) @ 05800a4, tree clean, sync 0/0. Production is unchanged (still Stage-1 cutover: phases 46-53). Staging frontend calls the PROD middleware, so middleware fixes need a prod middleware deploy. The blank-products bug is the same root cause as Fix 1 (bust=1 403) PLUS a frontend gap that blanks the grid on any error response — fix both. See RUNBOOK 'Pending Production Promotions' for what's queued for the Stage-3 prod deploy."
}
