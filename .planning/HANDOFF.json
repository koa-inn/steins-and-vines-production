{
  "version": "1.0",
  "timestamp": "2026-06-29T21:17:20.568Z",
  "phase": "incident/ops thread (PARALLEL to active Phase 44 — not phase work)",
  "phase_name": "Online-checkout orphaned-charge incident + issue triage + tooling",
  "phase_dir": ".planning (default context)",
  "plan": null,
  "task": null,
  "total_tasks": 6,
  "status": "paused",
  "completed_tasks": [
    {"id": 1, "name": "Reconciled orphaned Helcim charge in Zoho", "status": "done", "note": "Customer Jacob MacDonald (CUS-00151), Helcim txn 50641064, $125.52. Created INV-000118 (9 ingredient lines + Milling Fee + $5.48 entity discount to match the exact charge) + customer payment #119; added thornfull@gmail.com to the contact. Balance $0, paid."},
    {"id": 2, "name": "Fixed root-cause bug (sub-1kg weight quantities)", "status": "done", "commit": "fa1e649", "note": "checkout.js vQty<1 -> vQty<=0 (matches pos.js). Regression test added. Deployed via railway up; verified live."},
    {"id": 3, "name": "Hardened: void-on-early-reject", "status": "done", "commit": "8562ff7", "note": "Every early /api/checkout 400 now voids the already-charged payment (rejectWithVoid). Tests added. Deployed via railway up; verified live."},
    {"id": 4, "name": "Filed issue #107", "status": "done", "note": "Incident writeup + remaining hardening + audit. priority:critical / type:bug."},
    {"id": 5, "name": "Issue triage", "status": "done", "note": "Closed 33 stale/done/duplicate issues (board 98 -> 65) with evidence comments. Real live set ~8."},
    {"id": 6, "name": "Tooling/planning setup", "status": "done", "commit": "1b43106", "note": "Obsidian vault at repo root (.obsidian/ + notes/ gitignored; notes/START-HERE.md). zsh shortcuts ~/.sv-commands.zsh sourced from .zshrc (type sv-help). Warp Workflows written to ~/.warp/workflows but greyed out (free-tier gated) -> using zsh instead."}
  ],
  "remaining_tasks": [
    {"id": 7, "name": "Orphan-charge audit (Apr->Jun 2026)", "status": "blocked", "note": "Cross-match online Helcim charges vs Zoho invoices (reference_number = txn id). Other customers likely affected by the same sub-1kg bug. BLOCKED on the user's Helcim export — Railway CLI logs can't cover the window (retention + large --since is capped)."},
    {"id": 8, "name": "#107 webhook reconciliation backstop", "status": "not_started", "note": "Helcim webhook already receives every cardTransaction; flag/auto-reconcile online charges with no matching Zoho order after N minutes. The real safety net."},
    {"id": 9, "name": "#2 dual API-key + key-in-URL quick fix", "status": "not_started", "note": "~30 min: point pos.js routes at the unified key; drop req.query.api_key (headers only). Deferred by user."},
    {"id": 10, "name": "#10 PII in logs", "status": "not_started", "note": "3 log refs still contain customerEmail in checkout.js/pos.js."},
    {"id": 11, "name": "#1 session-auth the PII routes", "status": "not_started", "note": "/api/orders/recent guarded only by the browser-public MW_API_KEY. Kept OPEN intentionally (real exposure, not by-design)."}
  ],
  "blockers": [
    {"description": "Orphan audit needs the Helcim online-transactions export (Apr->Jun 2026)", "type": "human_action", "workaround": "none — Helcim is the only authoritative source of all online charges"},
    {"description": "Prod startup logs show Resend email transport verification FAILED ('API key restricted to only send emails') — void-failure ALERTS may not send", "type": "technical", "workaround": "verify transactional email actually sends; may be a false-alarm verify() call"}
  ],
  "human_actions_pending": [
    {"action": "Provide Helcim online/HelcimPay transactions export Apr->Jun 2026", "context": "to run the orphan-charge audit for other affected customers", "blocking": true},
    {"action": "Confirm transactional email (Resend) actually sends from prod", "context": "void-failure alerts ride on it; startup verify failed", "blocking": false}
  ],
  "decisions": [
    {"decision": "Reconcile the orphan in Zoho (INV-000118 + $5.48 reconciling discount) rather than refund", "rationale": "customer paid once and wanted the order; cart total didn't reconcile cleanly to the charge so a discount line trues it to the exact $125.52", "phase": "incident"},
    {"decision": "Keep #1 and #2 OPEN (not 'close as by-design')", "rationale": "real PII exposure (orders/recent on a browser-public key) + a genuine query-param key leak / dual-var footgun — verified still present in code", "phase": "incident"},
    {"decision": "Use zsh aliases (~/.sv-commands.zsh) instead of Warp Workflows", "rationale": "Warp Workflows greyed out (free-tier/account gated); zsh is tier/account/cloud-free and portable across terminals", "phase": "tooling"}
  ],
  "uncommitted_files": [],
  "next_action": "If the Helcim export is available -> run the orphan-charge audit (cross-match online charges vs Zoho by reference_number). Otherwise -> #2 quick fix (+#10) on staging, or start the #107 webhook reconciliation backstop.",
  "context_notes": "THIS IS A SEPARATE THREAD from the active Phase 44 (kiosk gift-card lifecycle) GSD work running in another window — do NOT disturb Phase 44 files or its uncommitted STATE.md/ROADMAP.md/SECURITY.md. All of this session's code is committed (fa1e649, 8562ff7, 1b43106) and both middleware fixes are LIVE on prod (railway up x2, verified). Key lesson: the 'phone died' framing was WRONG — the real cause was a server-side validation 400 after the HelcimPay iframe already charged the card; always check railway logs (sv-logs) for money-path incidents before accepting a behavioral explanation. If you actually meant to resume Phase 44, see .planning/phases/44-* and recent git log, not this handoff."
}
