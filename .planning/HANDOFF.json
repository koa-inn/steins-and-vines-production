{
  "version": "1.0",
  "timestamp": "2026-07-01T19:33:26.701Z",
  "phase": "45",
  "phase_name": "Security and money-path hardening audit (critical and high) — Live-Card UAT validation",
  "phase_dir": ".planning/phases/45-security-and-money-path-hardening-audit-critical-and-high",
  "plan": "45-09 (gift-card split-tender) — post-execution live UAT",
  "task": "F2 investigated + fixed (A+B); UAT still paused after step 4a",
  "total_tasks": 8,
  "status": "paused",
  "completed_tasks": [
    {"id": 1, "name": "UAT Step 1 — Issue cert (cart + terminal)", "status": "done", "note": "INV-000127 $10, Gift Card Sales liability, no tax at sale, cert GC-000001 active."},
    {"id": 2, "name": "UAT Step 2 — Balance lookup", "status": "done", "note": "$10."},
    {"id": 3, "name": "UAT Step 3 — Reload (cart + terminal)", "status": "done", "note": "INV-000128 $5, liability, no tax, balance $10 -> $15."},
    {"id": 4, "name": "UAT Step 4a — Partial redeem (split tender)", "status": "done_with_findings", "note": "Redemption booked (cert $15 -> $10) via manual-confirm recovery (INV-000129). Surfaced F2 + F3."},
    {"id": "F1", "name": "F1 fix — gift-card balance read wrong field", "status": "done", "commit": "51f3c64", "note": "current_balance not balance. Deployed + verified live."},
    {"id": "F3", "name": "F3 fix — exempt custom line default-taxed by Zoho", "status": "done_needs_live_verify", "commit": "97e8124", "note": "Tag exempt custom lines with ZOHO_TAX_ZERO_ID. Deployed. NEEDS end-to-end live verify (next exempt custom-line sale books tax_total:0)."},
    {"id": "F4", "name": "F4 filed — cart re-sync forces staff off payment screen", "status": "done", "note": "GitHub issue #108."},
    {"id": "F2", "name": "F2 investigated + FIXED (A+B)", "status": "done_needs_deploy_and_live_verify", "commit": "d8bf965 + e029108", "note": "ROOT CAUSE via Railway logs: premature manual-confirm race — 'Confirm Manually' button appeared at 15s while a real ~21s approval was still processing; staff preempted the poll/webhook, booking with transaction_id='manual-confirm' (no real Helcim id). Proven by same-session HEALTHY auto-confirm INV-000130 @ ~21s via API-poll fallback; no webhook/token-scope errors in logs. NOTE the F2 txn's own logs (INV-000129) had rotated out (3 deploys restarted the container; CLI tails only current deployment) — conclusion built from the adjacent healthy sale + code. Fix A (js/kiosk.js): manual-confirm reveal 15s -> POLL_TIMEOUT_MS(45s). Fix B (routes/pos.js): /confirm verifies manual-confirm via helcimLib.pollTerminalResult(reference_number) before booking — approved->real id, declined->400, unverifiable->409 fail-closed; real txn id (auto-confirm) trusted, skips lookup. Closes phantom-revenue + txn-id-fidelity gaps. Regression F2-A/B/C/D in pos-money-defects.test.js RED->GREEN. Full suites green (mw 1122, fe 928), lint 0 errors."}
  ],
  "remaining_tasks": [
    {"id": "F2-deploy-verify", "name": "Deploy F2 fix (d8bf965 + e029108) to prod, then live-verify", "status": "not_started", "note": "A real manual-confirm should now record the true Helcim txn id on the creditcard customerpayment; a manual-confirm with NO charge should refuse to book (400/409). NOTE fixes are committed on main but NOT yet pushed/deployed."},
    {"id": "F3-verify", "name": "Verify F3 fix end-to-end live", "status": "not_started", "note": "Throwaway exempt custom-line probe sale must book tax_total:0 in Zoho."},
    {"id": "UAT-4b", "name": "UAT Step 4b — Full redeem (terminal skipped)", "status": "not_started"},
    {"id": "UAT-5", "name": "UAT Step 5 — Void certificate", "status": "not_started"},
    {"id": "UAT-6", "name": "UAT Step 6 — Over-balance clamp", "status": "not_started"},
    {"id": "UAT-7", "name": "UAT Step 7 — Redeem-failure -> needs_manual_review", "status": "not_started", "note": "unit-test covered; live re-run pending."},
    {"id": "UAT-8", "name": "UAT Step 8 — Double-tap idempotency", "status": "not_started"},
    {"id": "cleanup", "name": "Reverse UAT test transactions on owner's card", "status": "not_started", "note": "Void cert GC-000001 ($10 active); reverse INV-000127 ($10), INV-000128 ($5), INV-000129 ($10.50 partially_paid). Real card charges to refund: issue $10 + reload $5 + redeem $5 = $20."},
    {"id": "build-churn", "name": "Reconcile build churn + stashes", "status": "not_started", "note": "Working tree holds pre-existing npm run build churn (about.html/admin.*/products/* + zoho-middleware/ingredients-cache.json). stash@{0} '45-09 deploy prep: park build churn + resume edits' and stash@{1} 'stale-build-stamps-pre-29.4' pending. This session's `git reset` moved pre-existing STAGED churn -> unstaged (nothing lost). STATE.md continuity update is also uncommitted."}
  ],
  "blockers": [
    {"description": "F2 fix committed (d8bf965 + e029108) but NOT deployed and NOT live-verified", "type": "human_action", "workaround": "deploy to prod (git push origin main = staging; production per CLAUDE.md deploy steps) then run one live manual-confirm to confirm real Helcim id is recorded"},
    {"description": "F3 fix deployed but not confirmed end-to-end against live Zoho", "type": "technical", "workaround": "one throwaway exempt custom-line probe sale, assert tax_total:0"},
    {"description": "Build churn + 2 stashes unreconciled in working tree", "type": "technical", "workaround": "owner to decide: keep/drop churn, pop or drop stash@{0}/stash@{1}"}
  ],
  "human_actions_pending": [
    {"action": "Deploy the F2 commits (d8bf965 backend + e029108 frontend) and live-verify at the kiosk", "context": "money-path change; must confirm real Helcim id recorded and no-charge manual-confirm refuses to book", "blocking": false},
    {"action": "Verify F3 end-to-end (next exempt custom-line sale books tax_total:0)", "context": "F3 fix 97e8124 deployed but only proven by unit tests + arithmetic", "blocking": false},
    {"action": "Reverse UAT test transactions (~$20 on owner card) + void cert GC-000001 + reverse INV-000127/128/129", "context": "live controlled session ran on the owner's own card; test artifacts", "blocking": false},
    {"action": "Remove/refresh the stale GH_TOKEN in ~/.zshrc:16", "context": "invalid token shadows valid keychain creds; gh works only via 'env -u GH_TOKEN gh'", "blocking": false}
  ],
  "decisions": [
    {"decision": "F2 root cause is a premature manual-confirm race, NOT a broken webhook/poll", "rationale": "Railway logs show the webhook + API-poll paths both healthy (INV-000130 auto-confirmed at ~21s); no 401/403 token-scope errors. The 15s manual button appeared ~6s before a normal approval completes.", "phase": "45"},
    {"decision": "F2 fix = A (frontend delay) + B (server-side verification), owner-approved", "rationale": "A stops the race; B makes manual-confirm safe regardless (real id + proof-of-charge), closing a latent phantom-revenue path where manual-confirm booked a card payment without verifying a charge occurred.", "phase": "45"},
    {"decision": "B fails closed (409) when the terminal result is unverifiable", "rationale": "booking without proof-of-charge is the phantom-revenue risk; the 45-08 reconciliation sweep settles a genuinely-orphaned real charge. Consistent with the money-path fail-closed philosophy.", "phase": "45"},
    {"decision": "F2 severity downgraded from 'orphan charge (lost money)' to 'UX timing race + txn-id fidelity gap'", "rationale": "manual-confirm recovered the money same-second; the durable defects were the missing real txn id + the latent no-verification phantom risk.", "phase": "45"},
    {"decision": "Committed F2 as 2 clean file-scoped commits on main, leaving pre-existing build churn untouched", "rationale": "owner chose 'my files only'; churn/stash reconcile is a separate decision.", "phase": "45"}
  ],
  "uncommitted_files": [
    "Pre-existing build churn (NOT this session's fix): about.html, admin.html, brewpad.html, contact.html, custom-labels.html, index.html, ingredients.html, js/admin.js, js/admin.min.js, products.html, products/*.html, reservation.html, zoho-middleware/ingredients-cache.json.",
    ".planning/STATE.md (session-continuity update for F2, intentionally left uncommitted)."
  ],
  "next_action": "Deploy the F2 commits (d8bf965 + e029108) to staging/prod, then live-verify F2 at the kiosk (real manual-confirm records true Helcim id; no-charge manual-confirm refuses to book). Then verify F3 live (exempt custom-line probe -> tax_total:0), re-run UAT steps 4b/5/6/7/8, and finish with cleanup (void cert + reverse invoices + refund ~$20). Separately, reconcile build churn + stash@{0}/stash@{1}.",
  "context_notes": "Fable 5 still unavailable for this account (checked 2026-07-01; /model fable blocked). Session was Opus 4.8. All Phase 45 money-path waves + 45-09 gift-card split-tender are deployed to prod (image sha256:5ec14723 after F3); the F2 fix is the ONLY undeployed change. KEY LESSON carried from F1: derive test mock shapes from a real captured response/contract, not from the code under test (F1's mocks mirrored an invented 'balance' key -> false green). For F2, Railway CLI only tails the current deployment — the incident txn's logs had rotated out after 3 in-session deploys; the conclusion was built from an adjacent same-session healthy sale (INV-000130) plus code, which is a reliable-enough basis but not a direct repro of INV-000129. Full detail in 45-09-UAT-FINDINGS.md (F2 section)."
}
