---
phase: quick-260611-94q
plan: 01
subsystem: api
tags: [zoho-books, contacts, brewpad, tdd, express]

# Dependency graph
requires:
  - phase: checkout-helpers
    provides: buildContactPayload (nested contact_persons structure)
provides:
  - POST /api/contacts builds contact via buildContactPayload with nested email/phone/name
  - buildContactsRoutePayload exported helper for unit testing
  - Regression test asserting INV-000078 class guard (top-level email/phone undefined)
  - BrewPad sends explicit first_name/last_name for exact-split contact creation
affects: [Phase 28, Phase 29, any code calling POST /api/contacts]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "buildContactsRoutePayload wraps buildContactPayload and applies explicit first/last override"
    - "TDD: RED test commit before GREEN fix commit"

key-files:
  created:
    - zoho-middleware/__tests__/contacts.test.js
  modified:
    - zoho-middleware/routes/bookings.js
    - js/brewpad.js
    - js/brewpad.min.js

key-decisions:
  - "buildContactsRoutePayload added to bookings.js (not checkout-helpers) to keep route-specific override logic local"
  - "Explicit first_name/last_name override only triggers when first_name is non-empty; falls back to whitespace split otherwise"
  - "buildContactsRoutePayload exported on module.exports for unit testability without mocking Express routes"

patterns-established:
  - "INV-000078 class bug: email/phone must always be nested under contact_persons, never at top level of Zoho contact payload"

requirements-completed: [QUICK-260611-94q]

# Metrics
duration: 15min
completed: 2026-06-11
---

# Quick Task 260611-94q: Fix POST /api/contacts Dropping Email/Phone/Name Summary

**POST /api/contacts now nests email/phone under contact_persons via buildContactPayload, with BrewPad sending explicit first_name/last_name for exact-split contact creation**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-06-11T13:40:00Z
- **Completed:** 2026-06-11T13:55:00Z
- **Tasks:** 2 (Task 1: TDD fix with 3 commits; Task 2: BrewPad + build)
- **Files modified:** 4 source files + 18 build artifacts

## Accomplishments
- Fixed INV-000078 class bug in POST /api/contacts: email/phone no longer sent at top level (which Zoho Books silently drops) — now nested under contact_persons
- Added `buildContactsRoutePayload` helper in bookings.js, reusing `buildContactPayload` from checkout-helpers with an explicit first/last name override
- Wrote 10-case regression test (contacts.test.js) asserting nested payload, INV-000078 top-level guard, explicit override, is_primary_contact
- BrewPad new-customer save now sends `first_name` and `last_name` alongside `name` so the server uses exact values instead of re-splitting

## Task Commits

Each task was committed atomically:

1. **RED: Failing regression test** - `36282f0` (test)
2. **GREEN: Fix handler + add buildContactsRoutePayload** - `fd93089` (feat)
3. **Task 2: BrewPad first_name/last_name + rebuild** - `f934c8f` (feat)

## Files Created/Modified
- `zoho-middleware/__tests__/contacts.test.js` - 10-case regression test for buildContactsRoutePayload
- `zoho-middleware/routes/bookings.js` - Added buildContactsRoutePayload helper, require checkout-helpers, replaced broken inline contactPayload, export helper
- `js/brewpad.js` - Added first_name and last_name to POST /api/contacts body
- `js/brewpad.min.js` - Rebuilt via npm run build

## Decisions Made
- `buildContactsRoutePayload` lives in `bookings.js` (not `checkout-helpers`) to keep route-specific override logic with the route; the pure `buildContactPayload` base in checkout-helpers is shared
- Explicit first_name/last_name override only applies when first_name is a non-empty trimmed string — preserves whitespace-split fallback for kiosk (which sends combined name only)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Worktree had no `node_modules` — ran `npm install` at both root and `zoho-middleware/` before tests could run. This is a normal worktree setup step, not a bug.

## User Setup Required

None - no external service configuration required. Fix takes effect on next deploy to staging.

## Next Phase Readiness
- Fix is ready for staging deploy and human UAT: create a customer via BrewPad new-customer form and confirm the resulting Zoho contact carries email, phone, and split first/last name on the primary contact person
- Kiosk new-customer path (buildContactPayload whitespace split) is unchanged and correct — no changes needed there

---
*Phase: quick-260611-94q*
*Completed: 2026-06-11*
