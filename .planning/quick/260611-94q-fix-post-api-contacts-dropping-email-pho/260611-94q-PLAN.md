---
phase: quick-260611-94q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - zoho-middleware/routes/bookings.js
  - zoho-middleware/__tests__/contacts.test.js
  - js/brewpad.js
  - js/brewpad.min.js
autonomous: true
requirements: [QUICK-260611-94q]

must_haves:
  truths:
    - "POST /api/contacts creates a Zoho contact whose primary contact person carries email, phone, first_name, last_name"
    - "The created contact payload has NO top-level email/phone (Zoho silently drops them — the INV-000078 bug class)"
    - "When the request body supplies explicit first_name/last_name, those are used verbatim instead of a whitespace split of name"
    - "BrewPad new-customer form sends first_name and last_name (plus name) so the split is exact"
  artifacts:
    - path: "zoho-middleware/routes/bookings.js"
      provides: "POST /api/contacts using buildContactPayload with first_name/last_name override"
      contains: "buildContactPayload"
    - path: "zoho-middleware/__tests__/contacts.test.js"
      provides: "Regression test asserting nested contact_persons + no top-level email"
      contains: "contact_persons"
    - path: "js/brewpad.js"
      provides: "New-customer POST body including first_name/last_name"
      contains: "first_name"
  key_links:
    - from: "zoho-middleware/routes/bookings.js"
      to: "zoho-middleware/lib/checkout-helpers.js"
      via: "require buildContactPayload"
      pattern: "require\\(.*checkout-helpers"
    - from: "js/brewpad.js"
      to: "POST /api/contacts"
      via: "fetch body with first_name/last_name"
      pattern: "first_name"
---

<objective>
Fix `POST /api/contacts` dropping email/phone/first/last name when creating Zoho
contacts from the BrewPad and kiosk customer-create forms.

The handler in `zoho-middleware/routes/bookings.js` builds a contact payload with
`email`/`phone` at the TOP LEVEL, which Zoho Books silently drops (same bug class
as the INV-000078 fix). It never builds a `contact_persons` array and never
parses first/last name. The known-good `buildContactPayload()` helper already
exists in `zoho-middleware/lib/checkout-helpers.js` and nests these correctly.

Purpose: Newly created contacts must carry their email, phone, and split name so
order-confirmation email delivery works and staff don't re-key customer details.
Output: Fixed handler reusing buildContactPayload, a regression test, and BrewPad
sending explicit first_name/last_name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Known-good helper. Already exported from checkout-helpers.js. Reuse it. -->

From zoho-middleware/lib/checkout-helpers.js (buildContactPayload, ~line 208):
```javascript
function buildContactPayload(customerName, customerEmail, customerPhone) {
  var name = (customerName || '').trim();
  var parts = name ? name.split(/\s+/) : [];
  var firstName = parts.length ? parts[0] : name;
  var lastName = parts.length > 1 ? parts.slice(1).join(' ') : '';
  var person = {
    first_name: firstName,
    last_name: lastName,
    email: customerEmail,
    is_primary_contact: true
  };
  if (customerPhone) person.phone = customerPhone;
  return {
    contact_name: name,
    contact_type: 'customer',
    contact_persons: [person]
  };
}
// module.exports already includes: buildContactPayload
```

From zoho-middleware/routes/bookings.js (current broken payload, ~line 394):
```javascript
var contactPayload = {
  contact_name: body.name || body.email,
  contact_type: 'customer',
  email: body.email,      // <-- top-level: Zoho drops this
  phone: body.phone || '' // <-- top-level: Zoho drops this
};
```

Regression test pattern to mirror — zoho-middleware/__tests__/checkout.test.js:278-302:
asserts `payload.contact_persons[0].email`, `payload.email` toBeUndefined,
first_name/last_name/is_primary_contact, and phone nesting.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix POST /api/contacts handler + write regression test</name>
  <files>zoho-middleware/routes/bookings.js, zoho-middleware/__tests__/contacts.test.js</files>
  <behavior>
    Write the regression test FIRST (per CLAUDE.md rule 3), then fix the handler.
    The test must exercise the payload-building logic the route uses. Since the
    route's payload construction is currently inline, refactor it to call a small
    exported helper so it is unit-testable without mocking the whole Express route
    (mirror how checkout.test.js tests buildContactPayload directly).

    Recommended approach: add a thin wrapper in bookings.js (or call
    buildContactPayload directly) that accepts (name, email, phone, firstName,
    lastName) and, when firstName is a non-empty string, overrides the whitespace
    split with the explicit names while keeping the contact_persons nesting.

    Test cases (contacts.test.js):
    - Given name "Anne MacDougall", email, no phone -> contact_persons[0].email is the email, payload.email is undefined (the INV-000078 guard)
    - Given full name -> contact_persons[0].first_name="Anne", last_name="MacDougall", is_primary_contact===true
    - Given phone provided -> contact_persons[0].phone set, payload.phone undefined
    - Given explicit first_name="Mary Jane", last_name="Watson Parker" -> those exact values used (NOT a whitespace re-split), contact_name still the combined display name
    - contact_type === 'customer'
  </behavior>
  <action>
    Require buildContactPayload from '../lib/checkout-helpers' at the top of
    bookings.js (it is already exported). Replace the inline `contactPayload`
    object (~lines 394-399) so the contact is built via buildContactPayload(
    body.name || body.email, body.email, body.phone). When body.first_name is a
    non-empty trimmed string, override contact_persons[0].first_name and
    .last_name with the explicit body.first_name / body.last_name (default
    last_name to '' if absent) so the split is exact rather than whitespace-
    guessed; keep contact_name as the combined display name. Do NOT set top-level
    email/phone — that is the bug. Preserve the existing response shapes exactly:
    201 { contact_id, created: true } on create, the "already exists" name-search
    fallback, the offline branch, and all 400 validation responses. Keep ES5
    style (var). To make this unit-testable, expose the override logic as a small
    pure helper (e.g. buildContactsRoutePayload or reuse buildContactPayload +
    override inline within a tested helper) and require it from the test.
  </action>
  <verify>
    <automated>cd zoho-middleware && npm test -- contacts.test.js</automated>
  </verify>
  <done>contacts.test.js passes: nested contact_persons carries email/phone/first_name/last_name + is_primary_contact, top-level email/phone undefined, explicit first_name/last_name override the split, response shapes unchanged. Full middleware suite green.</done>
</task>

<task type="auto">
  <name>Task 2: BrewPad sends explicit first_name/last_name + rebuild bundles</name>
  <files>js/brewpad.js, js/brewpad.min.js</files>
  <action>
    In js/brewpad.js new-customer save handler (~line 3737), add first_name and
    last_name to the POST /api/contacts JSON body alongside the existing name,
    email, phone. Use the already-collected `firstName` and `lastName` vars
    (from bp-nc-firstname / bp-nc-lastname). Keep `name` in the body for backward
    compatibility. ES5 var style only; brewpad.js is a self-contained IIFE — do
    not refactor it. Kiosk needs no change (buildContactPayload's split handles a
    combined name). After editing, run the build to regenerate js/brewpad.min.js.
  </action>
  <verify>
    <automated>cd /Users/koa/dev/steins-and-vines-website && npm run build && grep -c "first_name" js/brewpad.js && grep -c "first_name" js/brewpad.min.js</automated>
  </verify>
  <done>brewpad.js POST body includes first_name and last_name (plus name); npm run build succeeds; js/brewpad.min.js regenerated and contains the new fields. `npm run lint` clean.</done>
</task>

</tasks>

<verification>
- `cd zoho-middleware && npm test` — full middleware suite green, new contacts.test.js included
- `npm test` (root) — frontend suite green
- `npm run lint` — no errors
- `npm run build` — regenerates brewpad.min.js (and other bundles) cleanly
- Manual sanity (optional, post-deploy on staging): create a customer via BrewPad new-customer form; confirm the resulting Zoho contact carries email, phone, and split first/last name on the primary contact person
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (BrewPad/kiosk) -> middleware POST /api/contacts | Untrusted customer name/email/phone input crosses here |
| middleware -> Zoho Books API | Contact create payload crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94q-01 | Tampering | POST /api/contacts body (email/phone/name) | mitigate | Existing length + email-format validation in handler retained; first_name/last_name inherit the existing name length guard (reject >200) |
| T-94q-02 | Information Disclosure | logs in /api/contacts error path | accept | No new PII introduced; existing logging unchanged by this fix |
| T-94q-03 | Spoofing | x-api-key on POST /api/contacts | accept | Existing MW_API_KEY + CORS + Referer guard cover this route; unchanged |
</threat_model>

<success_criteria>
- POST /api/contacts builds the contact via buildContactPayload (nested contact_persons), no top-level email/phone
- Explicit body.first_name/last_name override the whitespace split when provided
- Regression test (contacts.test.js) asserts nesting + the INV-000078 top-level guard and passes
- BrewPad sends first_name/last_name; brewpad.min.js rebuilt
- Both test suites green, lint clean, response shapes of /api/contacts unchanged
</success_criteria>

<output>
Create `.planning/quick/260611-94q-fix-post-api-contacts-dropping-email-pho/260611-94q-SUMMARY.md` when done
</output>
