# OS files
.DS_Store
Thumbs.db

# Business data
pricelist.xlsx
style_guide.md

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/
.obsidian/

# Personal Obsidian notes / scratch planning — NOT tracked, NOT served on staging
notes/

# Environment secrets
.env

# Node
node_modules/
package-lock.json

# Test coverage output
coverage/
zoho-middleware/coverage/
test-results/

# Local cache files
zoho-middleware/products-cache.json
.claude/

# Source images (HEIC originals — JPG versions are used)
*.heic

# Unreferenced internal pages (kept locally, not deployed)
coming-soon.html
mockup-wine-labels.html
product-label-templates.html

# Cleanup script (one-time use)
cleanup.sh

# Domain managed via GitHub Pages settings, not this file
CNAME

# Internal assessment — do not deploy (repo root is served publicly)
PROJECT_ASSESSMENT.md

# Redis dump files — may contain cached PII/tokens (D-15)
*.rdb
zoho-middleware/dump.rdb
